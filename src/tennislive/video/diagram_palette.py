"""示意图的调色板——**一处出处**，别再各画各的。

账号所有者 2026-08-16：「卡片上的文字和前景的文字的颜色看起来很土，前景的颜色
很鲜艳，卡片上的文字就比较暗和模糊。」

查下去是两件事叠在一起，**颜色只是其中一件**：

1. **scrim 盖在示意图上面**，顶部压暗 36%。那一半修在 `explainer.py` 的
   `.scrim--diagram` 上，见那儿的注释。
2. **新写的几个示意图模块用了自己调的颜色**，和卡片本身那套对不上。卡片下半
   （`.point` / `.ask` / `.kicker`）用的是 `#f4fbf7` 正文加 `#c6f65a` 亮绿，
   而 `no1_charts` / `rulebook_cards` 里我写的是 `#e7f3ec` 正文加 `#8fd6a8`
   薄荷绿、`#e0b13a` 暗金——**同一张卡上两套色，下半鲜艳上半发闷**。

⚠️ **这不是「我调得不好看」，是分叉。** `explainer.py` 里那 48 张老示意图早就
一路写着 `#c6f65a`（出现 119 次），也就是这套色**本来就定过**；只有后加的这几个
模块没跟上。「一个数写两处必分叉」，颜色也一样，所以收成这一份。

⚠️ **数值跟着卡片 CSS 走，不是另调一套**：`INK` / `LIME` / `SOFT` 抄的就是
`.point` / `.point i` / `.foot` 那三行。判据 `test_示意图的颜色要和卡片本身
是同一套` 直接从 `_slide_html` 的 CSS 里把它们抠出来比——CSS 改了这儿不改，
当场红。
"""

from __future__ import annotations

#: 正文。和卡片 `.point` / `.slide` 的 `color:#f4fbf7` 同一个值。
INK = "#f4fbf7"

#: 次级说明（副标题、注脚、英文条文名）。和卡片 `.foot` 的 `#cfe6d8` 同一个值。
#:
#: ⚠️ **别再往暗里调。** CLAUDE.md 记着一次：「一度用到 `#7f958a`，反馈是
#: 『文字有点模糊』——那不是分辨率问题，是对比度不够，眼睛对不上焦就读成
#: 『糊』」。`#a9bcb2`（这一版之前用的）是同一个坑，只是浅一档。
SOFT = "#cfe6d8"

#: 强调色。和卡片 `.point i` / `.ask` / `.kicker` 的 `#c6f65a` 同一个值。
#:
#: ⚠️ 「一屏只留一个强调色」那条照旧管用——这是**那一个**，不是多一个。
LIME = "#c6f65a"

#: 大面积填充（条形图的条、方框的底）。亮绿铺一大片会盖过文字，所以填充另用
#: 一档薄荷绿，**只当底不当字**。写字一律用上面三个。
FILL = "#8fd6a8"

#: 第二强调色，只在**真的要分两类**时用（准／不准、现在／以后）。
#: 比原来的 `#e0b13a` 亮一档——那个暗金压在深绿上，和 `#c6f65a` 摆一起就是
#: 账号所有者说的「土」。
AMBER = "#ffd166"
