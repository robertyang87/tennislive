name: codex-search-pegula-interview
on:
  push:
    branches: [main]
    paths: [".github/workflows/codex-search-pegula-interview.yml"]
permissions:
  actions: write
  contents: read
jobs:
  dispatch-subs:
    runs-on: ubuntu-latest
    steps:
      - name: Generate Pegula subtitle lines after lead-in fix
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh workflow run interview-clip.yml -R ${{ github.repository }} --ref main \
            -f slug=pegula-swiatek-cincinnati-2026-sf -f mode=subs
