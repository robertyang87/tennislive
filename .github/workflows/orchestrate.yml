name: orchestrate

# 无人值守编排器：定时扫赛果/赛程 → 打分路由 → 去重 → 自动 dispatch probe。
# 定时班次是真 apply；手动入口保留 dry-run，方便只看计划不产出。
#
# 候选报告写进 job summary（`$GITHUB_STEP_SUMMARY`），账号所有者 / AI 在 run 页面
# 就能看到「今天该做哪几条」，比翻日志便宜。

on:
  schedule:
    # 每 10 分钟扫一次，并避开 :00/:30 的拥堵分钟。官方集锦上线到发现的
    # 最坏等待从 30 分钟压到 10 分钟，给下载、剪辑、质检和发布留出 SLA。
    - cron: "1,11,21,31,41,51 * * * *"
  workflow_dispatch:
    inputs:
      apply:
        description: "真 dispatch probe（并自动备料）。不给就只扫候选+报告"
        required: false
        type: boolean
        default: false
      max:
        description: "单次最多点几条 run（无人值守防错一片）"
        required: false
        type: number
        # ⚠️ 和 tools/orchestrate.py 的 DEFAULT_MAX 是同一个数（账号所有者
        # 2026-08-18 定「最大任务放开到 8」）——两处不同的话，CLI 干跑按 8 排、
        # 工作流真点 run 按 3 砍，后几场排到下一班白等 30 分钟。
        default: 8
      no_assemble:
        description: "dispatch 后不跑 assemble 备料"
        required: false
        type: boolean
        default: false

permissions:
  contents: write   # apply 分支要把 data/orchestration_state.json 提交回仓库
  actions: write    # apply 分支 gh workflow run 派发别的生产工作流

# 编排器一次只跑一班：两班并跑会各自读同一份 state、各自 dispatch 同一批候选
# （同一场 probe 点两遍），再抢着提交 state 撞 push。
# ⚠️ cancel-in-progress 必须是 false——上一班可能正在 dispatch + 写 state，
# 掐掉它就是「点了 run 没记账」，下一班全部失忆重点。排队等，不掐。
concurrency:
  group: orchestrate
  cancel-in-progress: false

jobs:
  orchestrate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          # 编排器只碰 src（digest/rating）、tools（orchestrate.py）、data（state），
          # 一个字节都不碰 output/assets——全量 checkout 白等一分半。
          sparse-checkout: |
            src
            tools
            data
            specs

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      # ⚠️ `yt-dlp` 不能漏：detect_highlights 的探测就是靠它搜——漏装的话
      # FileNotFoundError 一度被吞成空列表，每一班都安静地零候选，和「集锦
      # 还没发」长得一模一样（现在 search() 会当场抛，但装上才是正解）。
      # `[default]` 也不能省，它才带 yt-dlp-ejs，见 CLAUDE.md 装 yt-dlp 那条。
      - name: 装依赖
        run: pip install -q -e . "yt-dlp[default]"

      # ⚠️ **没有这一步，整条无人值守链是哑的，而且它绿着哑。**
      # 2026-08-25 量出来：orchestrate 定时跑了 330 趟，趟趟 success，而
      # `data/orchestration_state.json` 至今是 `{"dispatched": {}}`——一条 run
      # 都没点过。日志里每趟都写着真因：
      #     [vet] <url> 元数据探不到（重试过一次）——先不用，下一班再探
      #     [<slug>] 集锦还没探到，跳过，下次再探
      # 而同一条 URL 在沙箱里探得到（官方频道 / 379s / 1080p，三项全过）。
      # 也就是候选和集锦都在，**只有 runner 看不见**——YouTube 对机房 IP 的老病
      # （CLAUDE.md「昨日一分」栽的是同一个：十个源里五个 YouTube 系全 error，
      # 11 天 66 趟只出 3 条片，最后整条线被停掉）。
      # match-reel.yml 早有解法，`detect_highlights.py` 也**本来就读 `YT_COOKIES`**，
      # 只是从来没人给它。同一天 `mode=cookies` 自检在 runner 上实测
      # 「YouTube 下载可用。」，证明这把钥匙是活的。
      # ⚠️ 只接 cookies，**不起 PO token provider**：这条线一天跑 144 趟，每趟多
      # 一次 docker pull ＋ 最多 50 秒探活是实打实的开销，而它要的只是元数据
      # （`--flat-playlist --print`），不取媒体流。接了还不够再说。
      - name: 落 cookies（配了 Secret 才有）
        id: cookies
        env:
          YT_COOKIES_TXT: ${{ secrets.YT_COOKIES_TXT }}
        run: |
          if [ -n "$YT_COOKIES_TXT" ]; then
            printf '%s' "$YT_COOKIES_TXT" > "$RUNNER_TEMP/yt-cookies.txt"
            echo "path=$RUNNER_TEMP/yt-cookies.txt" >> "$GITHUB_OUTPUT"
            echo "已写入 cookies（$(wc -l < "$RUNNER_TEMP/yt-cookies.txt") 行）"
          else
            echo "::warning::未配置 YT_COOKIES_TXT——探集锦元数据会被 YouTube 挡住，"
            echo "::warning::而挡住的样子是「集锦还没探到」，和「真的还没发」分不开。"
          fi

      # ⚠️ apply 分支要自己写 GH_TOKEN：gh workflow run 派发别的 workflow 靠它。
      # checkout 那步用的 token 默认只读，不显式传 dispatch 会在第 N 秒报 403
      # （permission 不够），和「没匹配到候选」长得不一样。
      - name: 扫赛果/赛程并报告候选
        env:
          GH_TOKEN: ${{ github.token }}
          # detect_highlights._cookie_args() 读这个；没配就是空串，行为不变。
          YT_COOKIES: ${{ steps.cookies.outputs.path }}
        run: |
          # ⚠️ pipefail 不能省：默认 shell 下 `python … | tee` 的退出码是 tee 的，
          # 编排器崩了这一步照样绿——「脚本炸了」和「今天没候选」长得一模一样。
          set -o pipefail
          ARGS=()
          if [ "${{ github.event_name }}" = "schedule" ]; then
            # 定时班次无人值守真执行；--max 走 CLI 的单一默认值，避免 YAML/CLI 漂移。
            ARGS+=(--apply)
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ] && \
             [ "${{ github.event.inputs.apply }}" = "true" ]; then
            ARGS+=(--apply --max "${{ github.event.inputs.max }}")
            if [ "${{ github.event.inputs.no_assemble }}" = "true" ]; then
              ARGS+=(--no-assemble)
            fi
          fi
          PYTHONPATH=src python tools/orchestrate.py "${ARGS[@]}" 2>&1 | tee "$GITHUB_STEP_SUMMARY"

      # apply 记了 state（data/orchestration_state.json），要提交回仓库——否则
      # 下一趟又把它当「没 dispatch 过」重复点 run。apply 之外不产生任何提交，
      # 所以只有 apply 分支 commit。
      - name: 提交 state（定时或手动 apply）
        # always：某一场资源源站报错时，其他成功 dispatch 的场次仍要先记账，
        # 否则下一班会重复派发；失败状态保留给后面的告警步骤。
        if: always() && (github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.event.inputs.apply == 'true'))
        run: |
          git config user.name "Claude"
          # ⚠️ 邮箱必须是 noreply@anthropic.com，别改成别的：GitHub 认不出的
          # 邮箱会把提交标成 Unverified（`test_提交产物的工作流一律用Claude的身份`）。
          git config user.email "noreply@anthropic.com"
          git add data/orchestration_state.json
          if git diff --cached --quiet; then
            echo "state 没有变化，不提交"
          else
            python tools/check_staged_file_sizes.py
            git commit -m "orchestrate: 记录已 dispatch 状态"
            # ⚠️ 裸 `git push` 一撞（别的 run 刚推过 main）整步就红，而 state
            # **已经点完 run 了**——不落库的话下一班失忆重点。state 是纯文本
            # JSON，`pull --rebase` 处理得动（frame-grab 那套 fetch+重放是给
            # 二进制产物的，这里不用）；rebase 冲突就 abort 再试，最多 5 次。
            for attempt in $(seq 1 5); do
              if git push origin "HEAD:${{ github.ref_name }}"; then
                exit 0
              fi
              echo "push 被拒（第 $attempt 次），rebase 到最新再试"
              git pull --rebase origin "${{ github.ref_name }}" || git rebase --abort
              sleep $((attempt * 5 + RANDOM % 10))
            done
            echo "::error::state 连续 5 次推送失败——已 dispatch 的 slug 没落库，下一班会重复点 run"
            exit 1
          fi

      - name: 扫描或派发失败时通知
        if: failure() || cancelled()
        env:
          PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
        run: |
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo "::error::赛果发现/资源探测/派发失败，请检查 $RUN_URL"
          if [ -n "$PUSHPLUS_TOKEN" ]; then
            BODY="$(jq -nc \
              --arg token "$PUSHPLUS_TOKEN" \
              --arg url "$RUN_URL" \
              '{token:$token,
                title:"⚠️ 网球视频自动编排异常",
                content:("赛果发现、集锦探测或任务派发发生故障；其他可用比赛已继续处理。请查看：<a href=\"" + $url + "\">Actions 日志</a>"),
                template:"html"}')"
            curl -sS --fail -m 20 --retry 3 --retry-all-errors \
              -X POST https://www.pushplus.plus/send \
              -H "Content-Type: application/json" \
              -d "$BODY" \
              || echo "::warning::自动编排故障的 PushPlus 通知发送失败"
          fi
