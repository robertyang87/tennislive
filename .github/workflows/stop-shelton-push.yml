name: stop-shelton-push
on:
  push:
    branches: [main]
    paths: [.github/workflows/stop-shelton-push.yml]
permissions:
  actions: write
jobs:
  stop:
    runs-on: ubuntu-latest
    steps:
      - env:
          GH_TOKEN: ${{ github.token }}
        run: gh run cancel 34329659845 --repo robertyang87/tennislive
