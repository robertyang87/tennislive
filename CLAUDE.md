# 网球时差 · 协作约定

给在这个仓库里干活的人（和 AI）看的。都是踩过坑之后定下来的。

## 发现明确的问题就直接改，别回来问

用户的原话：「发现明确的问题就直接改掉，不用找我确认」「以后都按这个要求」。

**「明确」的意思是判据摆得出来**——一句话能说清它错在哪、错的证据是什么。
这类不要问，改完在回复里说一声就行：

- 数据和文案对不上（推送卡的注脚写着「因为亚洲赛季和七个大师赛官方不发切片」，
  却盖在德国草地 500 的条目上）
- 该接的表没接上（卡片印着 `Karolina Muchova`，而 `src/tennislive/zh/players.py`
  里写着**穆霍娃**）
- 调用和签名对不上、正则少了词边界、扫深没走到底——这一类一律直接修

**还要问的只剩一种：改法本身有多个都说得通的方向，而选哪个取决于你想要什么。**
比如「这个源画面有瑕疵，是整个丢掉还是标记后保留」——那不是「明确的问题」，
是口径选择。

顺带：修完要**留下判据**（测试或文档里的一句「为什么这么改」），
别只把症状按下去。仓库里几乎每条规矩都是这么长出来的。

### ⭐ 他说的和数据对不上——摆证据，然后按数据走，别停下来等

账号所有者 2026-08-07：「**这种问题就不要问我了，直接进入下一步就好**」。

来路：萨巴伦卡那条封面的台头写着「第二轮」，他说「**是第三轮啊**」。我查了三处，
全指向第二轮——WTA 接口 `RoundID='2'`（张帅那场才是 `'3'`，晚两天）、签表结构
（96 签前 32 号种子轮空，1 号种子首战在第二轮）、以及**我们自己片子里烧着的
转播条 `ADVANCES TO ROUND 3`**（赢下这场才晋级第三轮）。

然后我**把证据摆出来、停下来等他表态**。那一轮是白花的。

**这不是口径选择。** 上面那条说的口径选择是「有几个答案都说得通，选哪个取决于
你想要什么」；而**事实分歧只有一个正确答案，证据一摆就定了**。两者长得像，
分界很硬：

| | 判据 | 怎么做 |
|---|---|---|
| **口径选择** | 换个选择，两个结果**都对**（整段丢掉 vs 标记保留） | **问** |
| **事实分歧** | 只有一个对，而我已经查到了 | **摆证据 + 按数据走**，别等 |

⚠️ **还要看这一步可不可逆。** 当时卡住的是「在分支上渲一版」——4 分钟、
可以重渲、什么都没发出去。**为一个可逆的动作停下来等人，是最贵的等法**：
真错了他一句话我就改，而停下来必然花掉一个来回。可逆 + 我有证据 → 直接做。
只有**做出去收不回来**（微信、公开发布）且我心里没底，才轮得到停。

⚠️ 而「按数据走」不等于闷头做：**证据照旧要写出来**，写在回复里、写进 spec 的
`_why`、写进 PR 正文。他扫一眼就能否掉，成本比一次问答低得多。

## ⭐ 校验要按改动的风险分级，不要默认拉满——赛果视频尤其要快

账号所有者 2026-08-21：「我期望的是保证质量的前提下，不要过度设计和校验。
要做到又好又快。赛果的视频有时效性的。」

来路：一批 spec 修正，内容纯粹是改一句说明文字、给旁白补一个钟点、加一个数据
统计块——结果为它跑了两遍全量测试（各 5 分钟）、下载已发布的成片逐帧核对一个
文档字段、顺手又去修一条无关的旧 CI 判据误报还为此再等一轮全量。这条本身没做
错任何一步，但摞在一起就是过度校验：一个纯文档改动，换来了十几分钟的等待，
而赛果类内容每多等一轮，热度就凉一分。

**质量不是靠"能验的都验一遍"堆出来的，是靠"验对地方"。** 校验的成本要和改动
的风险匹配，不是每次都拉满：

| 改动是什么 | 校验到什么程度就够 |
|---|---|
| spec 里的说明文字 / `_why` 注解（不改 `segments`/`narration`/时间码/画面） | 只跑改动涉及的几条目标测试，**不用等全量**——本文件反复用到的三级判据（形状／叙事／产物）里，纯文档字段只碰得到最外层 |
| 改了 `narration`/`segments`/封面这类会影响产物的字段 | `--dry-run`（0.2 秒）→ 本地听感/预览 → 一次全量，**不用重复跑第二遍** |
| 改了代码里的判据/正则/公共函数 | 目标测试 + 反向验证 + **一次**全量（不是两次） |
| 已经渲完、已经推送的片子 | 已发的不重渲——文档字段可以事后补，产物不用再验 |

⚠️ **"跑两次全量"这个动作本身就是过度校验的信号。** 第一次全量是为了确认没有
回归，第二次往往是因为半路又顺手改了别的东西——**那第二个改动应该单独用它
自己的目标测试来验，不该让整条链条为它重新拉满一遍**。全量测试存在的意义是
「rebase 之后要重跑」和「改了公共代码怕连带面广」，不是每一次改动都要经过的
仪式。

### 顺手发现的无关问题：记下来，别在时效任务里展开修

「发现明确的问题就直接改掉，不用找我确认」这条规矩没有变——变的是**在赛果这
类时效任务中途撞上一个无关问题时，先掂量修复本身要不要再拖一轮验证**。判据
两条，占一条就该先记下来、等时效任务交付完再回头处理：

- **这个无关问题会不会拖慢当前这条时效任务的交付？** 会的话先记（写进
  `_note`、留一句 PR 描述或者单独起一个改动），赛果发完再修
- **修复本身要不要额外再等一轮全量才放心？** 要的话说明它不是"顺手"的规模，
  该单独处理，不该占用时效任务的窗口

真正"顺手"的规模是那种几秒钟能改完、一条目标测试就能钉住、不需要专门等一轮
全量的——本文件里那些"注释被误伤/判据扫得太宽"的修法大多是这个量级，那类才
配叫顺手。

### 赛果视频有时效性，默认走"产物自证"的快路径，别习惯性地加码

已经建好的三级校验本身就是这条线上最便宜的组合，**按顺序走完就够，不要额外
加码**：

    --dry-run（0.2 秒，查 spec 形状）→ 本地预览/听感 → 渲染出片，
    render.json / 真实成片是最终判据

**不需要每次都下载已发布的成片逐帧核对**——那是"真的怀疑这一条有问题"时的
排查手段，不是常规校验流程的一部分。已经确认过、已经推送的片子，文档字段
（比如源片剪辑说明这类）可以事后补记，不需要为了补一句文档就把整条成片拉
下来看六帧。

### 哑场那道闸：拿真实产物判，别在离线估阶段死磕

账号所有者 2026-08-21：「哑场的时长判断也不用太苛刻。」

`--dry-run` 阶段的离线估（`speech_seconds()`）本身**允许偏保守**——它的职责
是「提前让人看见」，不是拍板。真正说了算的是 render 用真实 TTS 合出来之后
量到的数（4.0 秒硬闸）。**别在离线估阶段就反复裁剪窗口去凑一个"看起来更
安全"的余量**——那不但拖慢交付，离线估本身还偏保守（同一批样本对比 Azure
实测普遍快 3.5%~6.6%），裁得越狠越可能把画面裁得比需要的还紧，反而伤了内容
完整性（和「片长可以商量，画质不商量」「不要砍片长」是同一条底线）。

判据：`--dry-run` 报「悬」或「哑场」只是提醒，**渲一次拿 `render.json` 或
`mode=narration` 的真实结果做决定**，不要凭离线估计的数字反复回去改 spec。

⚠️ **这三条不是降低质量的门槛，是把力气用在真正决定质量的地方**——精准、
四道选图闸门、贴近比赛事实这些硬标准一个字都没有松；松的只是「同一件事重复
验证几遍」这类不产生新信息的动作。判据是问一句：**这一次多花的时间，换回来
的是一个新的、之前不知道的事实，还是只是把已经知道的事实再确认一遍？**
前者值得做，后者是过度校验。

## 片子做完就合并、就推微信，不要再问一遍

账号所有者 2026-08-04 的原话：「**以后都不要问，直接 merge 完就推微信啊**」。

在这之前我的做法是：渲完 → 报告 → 「要推微信的话说一声，那一步不可逆」。
**那个「不可逆」是真的，但它不是每次都要重新确认一遍的理由**——授权已经给过了，
再问一次只是把决定推回给对方，而对方要的是片子发出去。

所以一条片子的默认终点是**发出去**，不是「等指示」：

    渲（push=false，在分支上）→ 合进 main → 推微信

顺序不能反，而且**推必须排在合并之后**——`GitHub Pages 只服务 main`，
复制页那个按钮在分支上永远是 404（这条本文件里栽过三次，见「推送正文里的
每个链接」）。

**当前账号整改期例外——2026-08-07 已经解除。** 原来的做法是：第一次采用
「原创比赛复盘」结构的新片，先在分支上 `render`，完成成片检查后交给账号所有者
审核，**不自动推微信**，为的是先确认新增的解说、声音和版式确实落在视频里，
不是把平台给出的诊断当成流量保证。

账号所有者 2026-08-07 的原话：「我可以在推微信后再去确认审核」——**这条例外
已经撤销**，`match_review` 格式的新片回到本节开头那条常规终点：
`渲 → 合并 → 推送`，推完账号所有者自己看。**不要求「先渲、等批准、再推」这道
额外的停顿了**——那本来就是「这一步不可逆所以我确认一下」的变种，本节早就说过
它不构成一个该停下来的理由。

配套挖出一个之前一直没被踩过的坑：`match-reel.yml` 的手动 `mode=push` 一直
按**今天**的日期拼产物目录（`OUT_DATE=$(TZ=Asia/Shanghai date +%F)`）。
这条路一直没出过事，不是因为它是对的，是因为**从没被这样用过**——旧流程里
`match_review` 格式的片子根本不走自动推送，人要推也是渲完当天顺手推；
`auto-push-reel.yml`（合并触发的那条自动推送）走的是另一条路，直接盯着
`render.json` 这个文件本身变化，不需要重新拼日期，所以它没有这个问题。
**这条例外一撤销，`mode=push` 就撞上了它自己一直没被验证过的那半条路**：
`eala-pegula-final` 8/4 渲的、8/7 才想起来手动推，按今天拼目录会拼到一个
当天根本没写过东西的路径，下游那道「push 模式先确认成片真的落地了」的闸
会报「既不在这个 commit 里」，而真实原因是**问错日子了**。**现在
`mode=push` 改成按 slug 反查 `output/*/reel/<slug>/render.json` 落在哪个
日期目录下**，同一个 slug 渲过不止一次就取最晚那份，判据在
`test_push模式按slug反查目录不按今天的日期`。

**仍然要停下来的只有两种**：

- **闸拦住了**：`check_explainer_landed` 报不合格、成片链接探不到、
  质检 FATAL——那是「东西不对」，不是「要不要发」
- **口径选择**：本文件开头那条说的那种，改法有几个方向都说得通而选哪个
  取决于对方想要什么

「这一步不可逆所以我确认一下」**不在这两种里面**。

### 「还差三张图，要我去找吗」——这种不要问

账号所有者 2026-08-03：「**必须要啊，这种问题以后不要问，直接去找就好了。**」

上面那条「发现明确的问题就直接改」管的是**改法明确**的情形；这条管的是
**下一步明确**的情形，形状一样：**当一件事只有一个说得通的下一步时，
问出口就是在浪费一个来回。**

判据同样是「判据摆得出来」：

| 情形 | 怎么做 |
|---|---|
| 片子缺三张图，而没有图就发不出去 | **直接去找**。找不到再报，并说清每条渠道的结果 |
| 测试红着，缺口测试自己指出来了 | **直接补**，别问「要我补吗」 |
| 只有一个说得通的下一步 | 做完再说 |
| **改法有多个方向，选哪个取决于你想要什么** | **这才要问** |

「要我继续吗」「要我去找吗」「要不要我接着做」——这几句话出现之前，
先问自己：**对方除了「要」还能回答什么？** 只有一个答案的问题不是问题，
是一次多余的往返。

## 同一件事，不同栏目各讲一次不算重复

账号所有者 2026-08-04：商竣程蒙特利尔那场，`pr-allowance`（保护排名）和
`shang-rublev`（开球之前）同一天都讲了 6-3 6-3 和次轮对卢布列夫。我问要不要
错开发，回答是「**不用，不同栏目**」「**直接发**」。

**栏目是承诺，不是话题的分区。** 同一场球在「开球之前」是这一场的前瞻，在
「保护排名」里是那条规则的当期例证——读者从两个栏目来，要的是两件事。
判据是**这一屏在回答哪个栏目的问题**，不是「这个事实出现过没有」。

真正要躲的重复是**同一栏目内**的：`shang-rublev` 就不许重讲 `shang-nishikori`
已经铺开的五个月空白和成都夺冠那一周（写在 `_SCRIPTS` 那条的注释里）。

### ⚠️ 上面这两个 slug 都是解说片——有一条同名的 reel，别混

2026-08-13 核出来的：`shang-rublev` 是**两条片子**，同名不同栏目、讲的还不是
同一场球。实跑 `explainer_column()` 量的，不是推的：

| 哪一条 | 栏目 | 讲哪场 |
|---|---|---|
| `_SCRIPTS["shang-rublev"]`（解说片） | **开球之前** | 8/2 胜巴列霍 6-3 6-3 ＋ 前瞻卢布列夫 |
| `specs/reels/shang-rublev.json` | **赛场之上** | 8/5 胜卢布列夫 7-5 4-6 7-6(5) |

`shang-nishikori` 也是解说片，也是「开球之前」——所以上面那句话点的是
**解说片内部的同栏目一对**，跟 reel 那条无关。⚠️ **按 slug 认领规矩、产物或
豁免，在这条上会静默认错人**（本文件里「voice 文件按序号认领」「render.json
按序号认领」是同一个形状，都是认领的钥匙不唯一）。reel 那条要找同栏目的邻居，
看的是 `nishikori-shang`，不是 `shang-nishikori`。

### ⚠️ 2026-08-05 账号所有者收窄了这条的读法——是划边界，不是撤销

写 `specs/reels/shang-rublev.json`（赛场之上）时我把上面那句读成「这个事实一个
字都不许再碰」，整段躲开了伤停；而同栏目的 `nishikori-shang` 第 0 段确实已经
说过「商竣程脚伤停了五个月，受保护排名进正赛」。账号所有者的原话：
「**5 个月的伤停和保护排名能用啊？很多人不知道历史的，都要交代一下。**」——
刷到这条的人多半没看过上一条，来路不交代就看不懂这场胜利有多重。

所以判据是**「铺开重讲」还是「一句带过」**，不是「这个事实出现过没有」：

| | 怎么做 |
|---|---|
| 上一条**铺开讲过的主体**（`zhang-putintseva` 的 24 连败／595／九年没赢过） | **一个字都别碰**——`zhang-sabalenka` 的 `_no_repeat` 就是这么写的 |
| **看不懂这场球所必需的来路**（伤停五个月、靠保护排名进正赛） | **一句带过**，别展开 |

⚠️ **而 `_SCRIPTS` 那条注释本来就是这么做的**——它写着「伤停只留一句带过
（第 ② 屏），成都那一周退到『两年前的他』当背景（第 ④ 屏）」，实现也照做了。
**规矩写绝了，实现一直是对的**；写绝的那句话把下一个人推去躲整段，才要账号
所有者来纠正。这条规矩本身一直活着：`zhang-sabalenka.json` 的 `_no_repeat`、
`zhang-ostapenko.json` 里直接点名引用它的那句，都是 08-05 之后写的。

## 后台任务：开了就要关

等 GitHub Actions 出结果时会开后台轮询任务。**拿到结果或改变思路后，立刻 `TaskStop` 关掉。**

一次会话里堆积过 11 个没关的轮询任务，全在空转拉 git。开新的之前先看一眼有没有旧的还在跑；同一件事只留一个在盯。

### ⚠️ `until ! pgrep -f 'X'` 会匹配到它自己，于是永远退不出

2026-08-03 开了两个等 pytest 的循环：

    until ! pgrep -f 'pytest tests/'; do sleep 10; done

pytest 早跑完了，循环还在转——因为 **`pgrep -f` 扫的是整条命令行，而这个循环
自己的命令行里就写着 `pytest tests/`**，它把自己数了进去。

**它和「任务还在跑」长得一模一样**：没有输出、进程在、退出码没有。是清点残留
后台任务时才发现的。这跟本文件里那条「判据扫得太宽，被自己的注释误伤」是同一
个形状——**检查的范围把检查自己包进去了**。

- 等进程结束用 **PID**（`while kill -0 $PID 2>/dev/null; do …`），别用名字
- 非要按名字，就排除自己：`pgrep -f 'pytest tests/' | grep -v $$`，
  或者让被等的那条命令自己写一个完成标记文件，循环只看那个文件
- 更省事的是**让被等的命令自己在后台跑**（`run_in_background`），跑完自然
  有通知，不用另起一个循环去盯

**同一天的第二个变种：等待条件被当前状态自己满足了。** 等新产物落库时写的是

    until git log --oneline origin/main -1 | grep -q 'special-exempt'; do …

——而**当时的 HEAD 就是上一版 special-exempt 的提交**，条件一上来就为真，循环
立刻退出，看起来像「已经好了」。

**等「新东西出现」要比变化量（SHA、条数、时间戳），别比内容特征**——
特征往往当前就成立。这两条和「判据扫得太宽，被自己的注释误伤」是一家的：
**检查把它想排除的东西包进了自己的范围。**

## 上下文会满，满之前自己压

账号所有者：「上下文满的时候，自己压缩一下……不然信息会丢失，而且你会做得很慢。」

上下文不是免费的仓库。它满了会被截断，**被截断的部分不会告诉你它丢了什么**——
和这份文档里反复出现的那类毛病一样：不吭声。

**要长期记住的东西，写进文件，别留在上下文里。** 仓库里的
`CLAUDE.md`、spec 的 `_note` / `_verified_clean`、PR 正文、提交信息，
这些是持久的；对话不是。今天那句「whisper 还没跑成功过」就是先写进
spec 才没丢——但它后来过时了，所以**写进去还要记得改**。

三个具体的省法，都是今天量出来的：

- **`list_workflow_runs` 少用**：它给每条 run 都塞一整份 repository 元数据，
  **两条 run 就 65 KB**。知道 run id 就直接 `list_workflow_jobs`；非要列表
  就 `per_page=1`，或者落到文件再用脚本挑字段
- **`get_job_logs` 一次要够**。`tail_lines` 给小了就得反复拉，而每次都把
  **同样的尾巴再拉一遍**——今天同一份日志拉了四次，前三次全是重复内容。
  先按时间戳估算目标行离结尾多远，一次拉到位
- **报告让工具自己 print 进日志**（见上面那条沙箱限制），比事后翻 artifact
  或反复拉日志便宜得多

**主动压：** 每完成一个阶段，把「现在在哪、卡在哪、下一步做什么」写成一段
放进回复里。这样即使上下文被截断，接上的人（包括我自己）读最后一条就能续，
不用重新推理整条链路。

## 空结果 ≠ 不存在

抓图时反复吃过这个亏：一次结果为空，就当成"这张照片不存在"，然后换选题降级。三次都不是。

- **被限流**：Commons 搜索连续返回 429，五条查询全空 —— 看起来和"没有"一模一样
- **分类名猜错**：`2025 French Open` 这类名字不存在，实际叫别的；年份分类还藏在 `... by year` 容器里，深一层
- **任务超时**：遍历跑满 `timeout-minutes` 被砍，候选目录空着
- **解析 bug**：`Category:2024 French Open` 按空格切出来是 `Category:2024`，不是纯数字，38 届分类全被判为"无年份"丢掉

所以：**空结果先自证是真空**。加退避重试、打印被丢弃的原因和数量、让接口自己报出分类叫什么名字，别猜。

反过来同样成立：**非空结果 ≠ 存在**。给选题探配图时，"温网赶鸽鹰"报了 16 张可用 —— 全是 1880 年代《Forest and Stream》杂志 PDF；"性别之战"报了 23 张 —— 1928 年芝加哥小熊队合影。授权和尺寸都过了，跟话题毫无关系。差点拿这个数字去排选题优先级。

搜索结果要**自证对题**：让每个话题声明必须出现在文件自身标题/描述里的关键词，排除 PDF，再看数字。见 `tools/probe_topic_photos.py`。

还有一层：**零命中先怀疑自己的查询词**。同一轮里"温网全白着装"的球员槽位报 0，换成 `Wimbledon Championships 2024 match` 立刻出 11 张——是查询词写错了，不是照片不存在。而"木拍"换了三组词仍然只有 2 张可疑结果，那才是真的没有。

**查数据也一样，而且更容易骗过自己。** 用户说「今日赛程里没有郑钦文」，我在
`digest` 的三个桶里按名字搜，0 命中；重抓一次仍是 0；于是差点回复「ESPN 的 feed
里没有这场」。其实是我**自己的探测脚本解析错了层级**——ESPN 的结构是
`events[].groupings[].competitions[]`，我按 `events[].competitions[]` 取，整份
294 场的数据被读成 0 场。判据摆在眼前却没看：同一个接口我们的管线取到了 83 场，
探测脚本却报 0，**两个数对不上就说明是探测错了，不是数据没有**。

改对之后一眼看到 `Zheng Qinwen vs Alexandra Eala / STATUS_SCHEDULED`。所以：
**自己写的探测脚本报空时，先拿一个已知非空的口径对一下**，别急着下结论。

### ⭐ 源有三类，查空一类不等于查空全部

2026-08-06，账号所有者点名要做「莱巴金娜的 UE 和 Winner 齐飞」。我查了三个地方——

    flashscore  df_st_1/2/3     Ace、双误、一二发、破发点、总分      ❌ 没有 UE/Winner
    WTA API     /matches        比分、种子、用时、轮次              ❌
    WTA API     /matches/<id>/stats（官方统计端点）                 ❌ 字段表里根本没这两项

⚠️ **第一行那句话 2026-08-14 修正过：它只对 WTA 巡回赛成立，大满贯的女子比赛
flashscore 一直有 Winners/UE。** 详见本节末尾「上面那句范围写宽了」那一段——
**引用这张表之前先读那一段**，别照着它得出「女子一律没有」。

——然后回复「这两个数三个源都没有」，还自作主张拿另一个口径（Ace 15 / 双误 16）
**把账号所有者定的主题换掉了**。他顶了回来：「你不找你总都肯定找不到呀……
不然这个亮点就没了，这个主题就要用这个去切入啊」。

再找，**第一下就中**：WTA 官网这场的比赛页里挂着一篇赛后稿，标题原文就是

> **58 winners, 58 errors: Rybakina delivers mixed bag to beat Kasatkina in Toronto**

而且正文把每个细节都写了：开场连中六个制胜分、58 里含 15 个 Ace、58 个非受迫
失误里含 16 个双误、**连续两个双误丢掉第二盘**、决胜盘发球胜赛局里三个双误、
第二个赛点才拿下。**比我准备用来替代它的那套数据好得多。**

**根子是我把「源」等同于「数据接口」了。** 一场球的数字至少有三类出处，
形状完全不同，查空一类完全不代表另外两类也空：

| 类别 | 例子 | 什么时候它才有 |
|---|---|---|
| **数据接口** | flashscore feed、WTA `/stats`、ESPN | 结构化、全覆盖，但**字段是固定的**——没有这一栏就是永远没有 |
| **编辑稿** | 赛事官网的 Match Reaction、赛事方新闻、通讯社赛报 | **只有值得写的那几场才有**，但记者手上是全套转播统计，UE/Winner/连续几个双误这类「接口里没有的」全在这儿 |
| **画面自证** | 烧死的记分条、转播的赛后统计图、场地上刷的字 | 集锦里本来就有，只是要用眼睛读 |

⚠️ **接口没有某一栏，恰恰说明该去翻编辑稿**——不是说明这个数不存在。
数据商按自己的字段表供数，赛事方按故事写稿，两边覆盖的东西根本不是一套。

⚠️ **更坏的不是查不到，是「查不到之后自己把主题改了」。** 账号所有者给的是
**切入角度**，不是一条可选的素材。角度拿不到料，正确动作是**继续找**并如实说
卡在哪儿，不是换一个我找得到的角度交差——那等于替他改了选题。

**WTA 这条路以后直接用**（这次挖出来的，省得重找）：

    赛事 id     GET api.wtatennis.com/tennis/tournaments/?from=&to=      → tournamentGroup.id
    这一场      GET api.wtatennis.com/tennis/tournaments/<id>/<年>/matches?states=C
                ↑ 里面有 MatchID、SeedA/SeedB、ResultString、MatchTimeTotal
    官方统计    GET api.wtatennis.com/tennis/tournaments/<id>/<年>/matches/<MatchID>/stats
                ↑ setnum 0=全场 1/2/3=分盘；有 Ace/双误/一二发/破发点，**没有 UE/Winner**
                  （32 个字段整份打出来核过，含 `winner`/`unforced` 的一个都没有；
                   十个端点变体全 404，`?extended=true` 这类参数被**静默忽略**）
    编辑稿      GET www.wtatennis.com/tournament/<id>/<城市>/<年>/scores/<MatchID>
                ↑ **这一页的 HTML 里挂着这场的赛后稿链接**，UE/Winner 在那儿

⚠️ **`tournamentGroup.name` 和 `city` 会打架**：多伦多这站 name 写着 `MONTREAL`、
city 写着 `TORONTO`（flashscore 也是 Toronto，tennisabstract 标 Montreal）。
**印城市以 `city` 为准。**

⚠️ **SofaScore 在这台沙箱恒 403**（`api.` 和 `www.` 两个入口都试过，带 Referer
也一样），别再把它排进候选。

#### ⚠️ 比赛用时**不要只信 flashscore**——2026-08-15 一天里它错了两场，方向都是偏长

海报比分板上那个用时（`cover.scoreboard.duration_source`）一直是从 flashscore
`df_sui_1` 的 `RB` / `RC`＋`RD`＋`RE` 取的。**它自己加得起来，所以单看它看不出问题**；
同一天两条片子各拿另外两个源核了一次，两次都对不上，而且**都是 flashscore 偏长**：

| 这一场 | 转播全屏台标 | WTA 官方 `settime` | flashscore | 差 |
|---|---|---|---|---|
| `sonmez-anisimova` | **36' + 41' = 1:17** | 36:10＋40:29＝**1:16:42** | 0:36＋0:42＝**1:18** | +1.3 分 |
| 张帅－戴伊（`4CYI9Ick`） | —— | 35:18＋35:43＋38:16＝**1:49:17** | 0:36＋0:44＝＋0:39＝**1:59** | **+10 分** |

第二场那 10 分钟不是四舍五入能解释的。**判据是「自洽」不是「加得起来」**：
WTA 官方那三个分盘用时加起来和它自己的 `MatchTimeTotal` 差 1~3 秒，是三个数互相
印证；flashscore 只有分盘和总计两个数，而它们出自同一次计算，对上了也不说明什么。

**所以印用时之前要交叉核一次**，两条路都很便宜：

    WTA 官方  GET api.wtatennis.com/tennis/tournaments/<id>/<年>/matches/<MatchID>/stats
              → setnum 1/2/3 各有 `settime`，setnum 0 有 `MatchTimeTotal`
    画面自证  源片里那张赛果全屏台标，右下角就印着每盘用时（`36'  41'`）

⚠️ **`MatchID` 不是 flashscore 的 id**，要按球员名去 `…/matches?states=C` 里认。

⚠️ **同一次核对还抖出 flashscore 两个 feed 自己打架**：张帅那场
`df_st_1` 报总分 **83:74**（和 WTA 官方一致），而 `df_mh_1` 报 **82:74**——
少的那一分正是本文件早写过的「逐分不列制胜的那一分」。`match_stat_hooks` 的
「总分差」那一行读的是 `df_mh_1`，**所以那个数天生少一分**，写进文案之前
拿 `df_st_1` 或 WTA 官方对一下。

#### ⭐ 2026-08-14 修正：上面那句「flashscore 没有 UE/Winner」**范围写宽了**

账号所有者问「女子比赛缺制胜分和 UE，有什么渠道能拿到」。**去量了一遍，发现
分界线根本不是 ATP/WTA，是「巡回赛 vs 大满贯」**——同一个 `df_st_1` feed：

| 抽样（都实测过） | 统计项数 | Winners / Unforced errors |
|---|---|---|
| 温网 女单 伊埃拉-斯瓦泰克 `0EU7qszi` | 23 | ✅ **有** |
| 美网 女单 萨巴伦卡-阿尼西莫娃 `M3WOyNpQ` | 23 | ✅ **有** |
| 澳网 女单 高芙-斯维托丽娜 `x8nLC0V1` | 22 | ✅ **有** |
| 法网 女单 `vBdPktcE` | 20 | ✅ **有** |
| ATP1000 蒙特利尔决赛 `8zQf6RtH` | 20 | ✅ 有 |
| **WTA1000 多伦多决赛** `OACDWR4j` | 17 | ❌ 无 |
| **WTA1000 辛辛那提 R1** `2uGeYNog` | 17 | ❌ 无 |
| **WTA500 柏林** `C6gUGttH` | 17 | ❌ 无 |

也就是说**四大满贯的女子比赛一直拿得到，而我们已经在用那个 feed**——一行代码
都不用改。缺口只剩 **WTA 巡回赛（1000/500/250）**，那三档恰好少的就是
`Winners` / `Unforced errors` / `Net points won` 这一组「击球质量」项。

⚠️ **这条老判据当年不是量错的，是「查了一场就写成了一类」。** 那次查的是
多伦多（WTA1000），结论却写成了整个 WTA——**而「WTA 都没有」和「这一场没有」
在当时的产物上长得一模一样**。和本文件里「空结果先自证是真空」是一家的，
只是这次要自证的不是空不空，是**空的范围有多大**。

**WTA 巡回赛这一段，2026-08-14 探到底了，下面这些别再重探**（都自证过是真空）：

| 探过什么 | 结果 |
|---|---|
| flashscore `df_st_2/3/4` | 和 `df_st_1` **同一份**（字节数一样）；`df_stx/sts/sum/mi_1` 全空 |
| WTA 官方 `/stats` 的**全部 32 个字段** | `winner` / `unforced` / `error` 一个都不含 |
| WTA API 端点变体 ×10（`/statistics` `/pointbypoint` `/insights` `/extendedstats` `/shotquality` …） | 全 404 |
| `/stats?extended=true` / `?type=full` | **参数被静默忽略**——返回长度和裸的一模一样（1640B），别被 200 骗了 |
| WTA 比赛页 HTML | `unforced` 0 次；49 个 `winner"` **全是 CSS 类名** |
| tennisexplorer | **根本没有技术统计**那一块 |
| SofaScore | 照旧 403 |
| **画面自证**（WTA 官方集锦片尾） | ❌ 奖杯 → 采访 → 台标 → 订阅卡，**没有统计图**（两张缩略图墙逐格看过） |

### ⭐⭐ 2026-08-16：这张表少了一行，而那一行有——**TNNS Live**

账号所有者问「怎么 ATP 的统计卡片里没有制胜分和 UE 了」，我按 flashscore 逐场
量完答「这两场接口里没有」，他回了一句「**tnns live 有啊**」外加截图。

⚠️ **这张表当年从来没有 TNNS 这一行**——2026-08-07 探 TNNS 时的结论是
「要开 Chromium，成本大于收益，**不接入**」，于是它连「查过」的资格都没拿到。
**「没接入」和「查过没有」在这张表上长得一模一样**，而前者被读成了后者。

**现在实测通了，ATP / WTA 都有**（`tools/tnns_stats.py`，判据是分盘合计＝全场）：

| 这一场 | 制胜分 | 非受迫失误 | 对得上什么 |
|---|---|---|---|
| ATP 兹维列夫-诺里（run 31958829098） | 41 / 25 | 35 / 36 | 分盘三块加起来＝全场 ✅，七项和 flashscore 逐个一致 |
| **WTA 汤森德-莱巴金娜**（run 31959466961） | **17 / 13** | **14 / 22** | 分盘两块加起来＝全场 ✅，**Townsend 那两个数和列序跟 app 截图一字不差** |

⚠️ **所以上面那句「WTA 巡回赛这两项拿不到」不再成立**，`_no_stats_why` 也不该
再写「查遍无」——**先跑一趟 `tnns-stats.yml`**，闸在
`test_数据图缺制胜分和UE的要说清TNNS那趟跑出来是什么`。

⚠️ **只取 Winners/UE，别取别的。** TNNS 的一发分母（诺里 `64/113`）和
`first_in + second_total`（**111**）对不上，而数据图画的是
`first_in / first_total`，要用和式才自洽。

⚠️ **列顺序从 `players` 读，不能从比分行推。** 莱巴金娜那场比分行是
「Rybakina 6 6 / Townsend 3 4」，而 Stats 那一屏的列头是
**Townsend 在前**——两者相反。填反了就是「stats.a 跟的是 cover.matchup[0]」
那条老坑的又一次。

⚠️ **数字会赛后修订。** 同一场账号所有者 00:27 截的图是莱巴金娜 11/21，
几小时后接口给的是 13/22（汤森德那两个数没变）。解码错不可能只错一个人、
分盘还加得起来——**引用之前重跑一趟，别拿几小时前的数当准**。

⚠️⚠️ **认错场次会拿回一个假的「没有」，而它和真的没有长得一模一样。**
2026-08-16 我给 `townsend-rybakina` 写的 `_winners_ue_why` 是
「跑过 tnns-stats（**id=73467044**），TNNS 这场也没有这两行」——**那个 id 认到的
不是这一场**。重跑（`who=Rybakina,Townsend`，run 31969012305）当场拿到
`[17, 13]` / `[14, 22]`，**和上面这张表里记的一字不差**——也就是说答案一直印在
这份文档里，而我把一个查错的结果当成事实写进了 spec，还发了出去。

- **按 `--who <姓>,<姓>` 加 `--date` 认，别钉 `--match-id`**：id 是 TNNS 内部的，
  换一天就串；姓名加日期是可读、可复核的
- **报告里的「选手顺序」那一行就是自证**（`（选手顺序 ['Townsend', 'Rybakina']）`）
- ⚠️ **核的是 run 日志，不是谁转述的那一句。** 2026-08-18 `eala-anisimova`：转述里写着
  「拿到 **47/45 对 20/27**」——这句话有两种读法（「两个人的制胜分 / 两个人的 UE」
  还是「一个人的制胜分-UE / 另一个人的」），**而两种读法的分盘合计都对得上**
  （47=15+16+16、45=25+13+7），所以自证那一关分不出来。我照着错的那种读法差点把
  一条**本来是对的** spec 改成错的。日志自己把答案印得明明白白，**照抄那两行就行**：

      制胜分 [20, 47]   非受迫失误 [27, 45]   （选手顺序 ['Eala', 'Anisimova']）
        "winners": 20,  "ue": 27    ← Eala
        "winners": 47,  "ue": 45    ← Anisimova

  `tnns_stats.py` 末尾那段本来就按球员名逐行标好了，还带着「a/b 跟 cover.matchup
  的顺序」的警告。**一句压缩过的转述比原始日志短，代价是它把归属压没了。**
  ——两个名字对不上这一场，后面的数一个都不能用
- 这是「空结果先自证是真空」的又一个实例，只是这次要自证的不是空不空，
  而是**这份空是不是这一场的**

⚠️ **三个坑写在 `tools/tnns_stats.py` 的注释里，别照着旧结论重探**：
载荷是 K/P 字典压缩且**分盘挂在 `data.data`（套了两层）**；响应的
`content-type` 是 `text/html` 不是 json；**页面内 fetch 只能同域**，
跨到 `api.` 子域要用顶层导航。

**剩下两条真能用的，但都是加料不是主力：**

- **Match Charting Project**（`tennisabstract.com/charting/`）：2026 年已标注
  **422 场女子**，给的是 `Winners (FH/BH)` / `UFE (FH/BH)`，**比单个数字还细**。
  ⚠️ **但它是志愿者标的，而且偏早轮**——多伦多 2026 只标到 R16（12 场），
  **QF / SF / 决赛一场都没有**，而那正是我们做片子的轮次。所以：值得每条片子
  去碰一次运气，不能指望它。**怎么用见下一节，别现搓**
  - ⚠️⚠️ **它还落后大约三天，所以「当天做当天的片子」结构性地一定查空。**
    2026-08-15 01:40Z 刷新过索引（18107 条）之后量的：**最新一条是 2026-08-12**
    的蒙特利尔/多伦多 1/4 决赛，而 8-13 开打的辛辛那提**一条都没有**。
    也就是说这条路对「刚打完就做」的片子几乎必然为空——**空不等于以后也没有**，
    过几天再查同一场往往就标上了。所以查空的时候要在 spec 里写清楚
    「查过、当时还没标」，而不是写成「这场没有」；真需要那两个数就等两三天回头补，
    别当场下「拿不到」的结论。判据：`tools/mcp_stats.py` 打出来的索引最新日期
    和比赛日期差几天，一比就知道是「真没标」还是「还没轮到」
- **编辑稿**（WTA 官网 Match Reaction / by-the-numbers）：莱巴金娜那次
  「58 winners, 58 errors」就是从这儿来的。⚠️ **但它按故事角度写，不按字段写**
  ——同样去查多伦多决赛那篇，通篇只有「a forehand winner sealed a hold」这种
  叙述，**一个计数都没有**。碰运气，同样不能当渠道

**所以现在的口径是：**

    大满贯（含女子）  → flashscore 直接有，`stats` 块照 ATP 那套填
    ATP 巡回赛        → ⚠️ **见下面 08-15 那条修正，这一行是错的**
    WTA 巡回赛        → 先碰 MCP、再碰编辑稿；两边都没有就写 `_no_stats_why`

最后那一条正是 `test_赛场之上要么带数据统计图要么说清为什么不带` 留的活口——
**查不到就如实不画，别硬凑一张缺两项的图**。

##### ⭐⭐ 2026-08-15 再修正一次：**它根本不按巡回赛分，是一场一场的**

账号所有者问「为什么 ATP 比赛没有制胜分和 UE」。查下来**两件事，都不是他问的
那个答案**：

**① 有没有，是按场算的。** 2026-08-13 辛辛那提当天 **43 场逐场量了一遍**
（`df_st_1` 的 `Match` 那一栏数 `SG÷` 的项数：有这两项是 20 项，没有是 17 项）：

| | 有制胜分/UE | 没有 |
|---|---|---|
| ATP（23 场） | **3 场**（土方-孟菲尔斯、斯瓦伊达-贝卢奇、法里亚-布鲁克斯比） | 20 场 |
| WTA（20 场） | 0 场 | 20 场 |

也就是说**同一站、同一天、同一轮，隔壁球场就没有**——`baez-dimitrov` 和
`hijikata-monfils` 都是辛辛那提 ATP1000 首轮，一个 17 项一个 20 项。
上面那句「ATP 巡回赛 → 直接有」是**拿蒙特利尔决赛一场推出来的**，正是这份文档
自己在上一条里写过的毛病——「查了一场就写成了一类」，**这是第三次**。

⚠️ **判据只有一个：跑一次看它给不给。** 别按巡回赛推，也别按球场推
（两场都写着 Center Court）。

**② 而这一次真正出事的不是数据，是我没抄。** `hijikata-monfils` 那场
flashscore **有** `Winners 30/35`、`Unforced errors 39/55`——我在旁白里用了 UE
（「第三盘二十六个非受迫失误」），**却没把这两个字段填进 `stats` 块**，于是成片
里那张数据图少了两行。⚠️ **它一个字都不报**：`render_stat_card.OPTIONAL_FIELDS`
允许这两项缺席（多数场次真的没有），所以**「漏抄」和「本来就没有」在产物上长得
一模一样**。

**修法不是提醒自己下次记得抄，是把手抄这一步去掉：**

    python tools/match_stat_hooks.py <fsid> --stats-block
    → 「制胜分 / 非受迫失误：**这场有，必须填**」＋ 一份能直接粘的 stats.a / stats.b

拿两条已发的 spec 回归过（`boulter-volynets` 十二项全等、`baez-dimitrov` 只差一个
我当时没记的 `pts_total`），**它不是另写一份实现，是把我手抄的那份换成机器抄**。
判据 `test_可缺的字段两处必须是同一份` **自己推导不维护名单**：可缺集合必须等于
`render_stat_card.OPTIONAL_FIELDS`，而且那张图要画的每个字段都得在表里。

⚠️ `first_total` **不取接口的 `Service Points Won` 分母**，按 `first_in +
second_total` 算——两者偶尔差 1（let 的记法差异），而一发成功率画的是
`first_in / first_total`，用和式才自洽。

⚠️ **已发的 `hijikata-monfils` 不重渲**（消息发出去收不回来），这条只管以后。

##### ⭐ `stats.a` 跟的是 `cover.matchup[0]`，**不是 feed 的 home/away**——填反了那张图会把赢家印成输家

2026-08-15 `sonmez-anisimova` 踩的，**而且我在同一条 spec 里写了一段注释把它讲反**。
`match_stat_hooks --stats-block` 吐出来的 `a`/`b` 是**按 flashscore 的 home/away**
（`SH`/`SI`）排的，而 `render_stat_card` 第 19 行白纸黑字写着

> `"a"` … 对应 `cover.matchup[0]`（**版式顺序，不是赢家顺序**）

这两个顺序**只在 home 恰好排在 matchup 第一位时才一致**。这一条正好相反
（feed 是森梅兹-阿尼西莫娃，matchup 是阿尼西莫娃-森梅兹），于是数据图变成：

    左边  阿尼西莫娃（10）  ACE 2 · 双误 7 · 破发点 3/7 · 总得分 47   ← 全是森梅兹的
    右边  森梅兹（50）      ACE 7 · 双误 5 · 破发点 6/12 · 总得分 69  ← 全是阿尼西莫娃的

**也就是说这张图在宣称赢球的那个人被打成 47:69。**

⚠️ **四道闸一道都没响**：`--dry-run`、全量测试、`--check-narration`、
`check_reel_landed` 全绿，渲染也不报错——**图上没有任何一个数是自相矛盾的**，
每一栏单看都是真的，错的只有它挂在谁名下。这是「一个数写两处必分叉」的
又一个变种，只不过分叉的不是数，是**顺序**。

⚠️ **别指望「赢的人总分更高」当兜底**：`boulter-volynets` 就是输的那个人小分
更多（本文件前面记过），拿它当判据会在真实数据上误判。

**唯一肉眼可查的破绽是脸**：`headshot` 就在 `stats` 块里，所以它跟着数字走——
名字换了人，脸没换，于是**左边那张脸和左边那个名字对不上**。判据就钉在这儿，
`test_数据图的头像和matchup里的名字必须是同一个人` **自己从 `specs/` 推、不维护名单**：
同一个头像文件在别的 spec 里挂过哪个中文名，这里就必须还是那个名；填反了那张
头像会同时挂上两个名字，当场红（正是这次抓到它的方式——`wta-326907.jpg` 一边是
`sonmez-kasatkina` 的「森梅兹」、一边是这条的「阿尼西莫娃」）。两个方向都反向验证过。

⚠️ 它的边界要知道：**新球员第一次出现时这条是哑的**（没有第二处可比）。那时候
只能自己对一眼——**打开渲出来的数据图，看左边那张脸是不是左边那个名字**。

###### ⭐⭐ 2026-08-17 它抓到了第三种，也是它本来就为之而生的那一种：**真的 a/b 填反了，而且已经发出去了**

`tsitsipas-royer`（2026-08-15 发的）里 `atp-TE51.png` 挂着「鲁瓦耶」，而当天新写的
`tsitsipas-auger-aliassime` 里同一张图挂着「西西帕斯」——闸红。**这次不是译名问题，
是那条已发的 spec 真的把 a/b 填反了**：

    左边  西西帕斯（赢家）   ACE 2 · 双误 4 · 总得分 70   ← 全是鲁瓦耶的
    右边  鲁瓦耶             ACE 4 · 双误 2 · 总得分 85   ← 全是西西帕斯的

flashscore 逐项核过（`AE(home)=Royer V.` / `AF(away)=Tsitsipas S.`、
`Total Points Won home=70/155 away=85/155`），**西西帕斯赢的是 85 分那一半**，
而他也确实 7-6(4) 7-5 赢了这场球——**那张数据图在宣称赢球的那个人被打成 70:85**。

⚠️⚠️ **最刺眼的是：那条 spec 自己的两处注释把错的规矩写成了对的。** 原文是

> 「**home = 鲁瓦耶 = spec 里的 a**」「`stats.a/b` 跟 feed 的 home/away 走，
> **别按 matchup 的顺序填**」

——和 CLAUDE.md 白纸黑字的那条正好相反。**它不是忘了规矩，是把规矩记反了然后
理直气壮地写进了注解**，于是下一个照着这条 spec 抄的人会一起错。这比忘了更难发现：
一条写着理由的错，读起来比没写理由的对更可信。

⚠️ **四道闸一道都没响**（和 `sonmez-anisimova` 那次一模一样）：`--dry-run`、
全量测试（当时）、`check_reel_landed`、渲染本身全绿——**图上每个数单看都是真的**。
抓到它的只有「同一张官方头像不许挂两个中文名」这一条，而且**它是被下一条片子触发的**：
只有当同一个球员第二次出现，这条判据才有第二个样本可比。**这就是本文件说的
「新球员第一次出现时这条是哑的」那半句的另一面——它不是当场生效，是欠着，
下一次相遇时才结账。**

⚠️ **已发的成片不重渲**（微信那条消息发出去收不回来），所以那条片子里的数据图
仍然是反的；改 spec 是为了它以后不再渲错、也为了那道闸不再红。`push.summary` 一个字没动。

###### ⭐ 而它 2026-08-16 抓到的不是 a/b 填反，是**译名不一致**——同一个人两个中文名

`noskova-boulter` 写完之后这条当场红：`wta-319125.jpg` 挂着 `{博尔特: 1, 布尔特: 1}`。
两条 spec 都没填反 a/b——**是 `boulter-volynets` 手打了「博尔特」**（那是
Usain Bolt 的译名），而 `player_zh("Katie Boulter")` 和
`player_names_top500.json` 第 69 位给的都是「**布尔特**」。

⚠️ **那条 spec 的 `cover._name_why` 白纸黑字写着「两个中文名都走 `player_zh()`」**
——规矩写对了，实现是假的，而且一路发了出去。CLAUDE.md 里
「判断改没改对，不要看文件内容，直接调 `player_zh()`」说的正是这个动作，
而写那条 spec 的人（我）**写了那句注释，没跑那行代码**。

**这条判据因此比它 docstring 说的更宽**：它拦的是「同一张官方头像挂了两个中文名」，
而那可以是 a/b 填反，**也可以是两条 spec 对同一个人的译法不一样**。后者机器分不出来，
但两种都必须修——同一个账号不能把同一个人叫两个名字。

⚠️ **修的方向是「以译名表为准」，不是「跟已发的那条对齐」**：跟着错的对齐，
下一条片子还会错。所以改的是那条已发 spec，而不是新写的这条。

⚠️ **但 `push.summary` 冻住不动。** 它要和已经发出去的 `copy.html` 里那句标题
**逐字相同**（`test_推送标题从spec算得出来` 的第 ② 层就是拿它比的）；跟着改会把
那条测试打红，而那正是 CLAUDE.md 写过的「拿被顶替掉的当基准等于要求今天的代码
去重现一个已经改掉的错」反过来的一面。于是**成片和复制页里仍然是旧名字、spec 里
是新名字**——这一条分叉写进 `_push_summary_why` 认领掉，不是手滑。

⚠️ 顺带：`test_人名要以译名表为准` **拦不住「博尔特」**——它扫的是「和规范名只差
一个字」的近似串，而「博尔特」本身是另一个真实球员的规范译名，不是错别字。
所以那条测试和这条头像判据**各拦一半**，缺一个都会漏。

#### ⭐ Match Charting Project 这条路怎么走：`tools/mcp_stats.py`

账号所有者 2026-08-14：「**那把这个 mcp 方式记录下来啊**」。写成脚本了，
别每次现搓（⚠️ 这里的 MCP 是 **Match Charting Project**，和 Model Context
Protocol 无关，仓库里两个缩写会撞）。

    python3 tools/mcp_stats.py find --who Eala               # 这个人被标过哪几场
    python3 tools/mcp_stats.py find --who Eala --year 2026
    python3 tools/mcp_stats.py show 20260802-W-Washington-F-Alexandra_Eala-Jessica_Pegula

实跑出来（我们发过的 `eala-pegula-final` 那场，MCP 真的标了）：

    ⚠️ 志愿者标注，不是官方统计——charted by jeffsackmann
      [全场 ] Alexandra Eala   制胜分 11（正手8 反手3 其它0）  非受迫失误 21（正手13 反手5 其它3）
      [全场 ] Jessica Pegula   制胜分 23（正手13 反手5 其它5）  非受迫失误 35（正手19 反手15 其它1）
      [SET 3] Alexandra Eala   制胜分  5（正手4 反手1 其它0）  非受迫失误  1（正手1 反手0 其它0）
      [SET 3] Jessica Pegula   制胜分  4（正手3 反手1 其它0）  非受迫失误 13（正手7 反手6 其它0）

**它可信，是拿两个源对出来的，不是声明出来的。** 同一场和 flashscore 官方
交叉核对，**七项全中，其中三项精确到小数**：

| | flashscore（官方） | MCP |
|---|---|---|
| 佩古拉 Ace | 4 ÷ 71 发球分 = **5.6%** | `A% 5.6%` |
| 伊埃拉 双误 | 3 ÷ 70 = **4.3%** | `DF% 4.3%` |
| 佩古拉 双误 | 1 ÷ 71 = **1.4%** | `DF% 1.4%` |
| 一发进球率 | 64% / 65% | 64.3% / 64.8% |
| 破发点救下 | 转化 2/3、5/9 反推 | `1/3`、`4/9` |

列映射读错的话这七项不可能同时对上——所以 `Winners`/`UFE` 那两栏可以信。

⚠️ **四个坑：**

- **别按赛事名过滤，按球员姓找。** 同一站男子叫 `Canada_Masters`、女子叫
  `Toronto`。我第一次按 `-Toronto-` 扫，男子那 14 场一场都没进来，
  **而漏掉的样子和「这一站没人标」一模一样**
- **名字是全名带下划线**（`Alexandra_Eala`），不是 flashscore 的 `Eala A.`。
  拿后者去搜必然零命中
- **正手加反手不等于总数。** `16 (6/7)` → 6+7=13，还差 3 个（截击、高压这些
  不在这两栏）。别把「6/7」当成完整拆分印出去；`split_count()` 把差额算成
  `other` 就是为了让人一眼看见它不是零
- **这是志愿者标的**，每场页面写着 `charted by <谁>`。写进 spec 的
  `stats._source` **必须点明出处**，别和 flashscore 的官方口径混着说成一回事

##### ⚠️ 而我给这套写的第一条判据，拦的是一个不存在的坑

第一版注释和测试都写着「赛事名自己带下划线，所以不能按 `-` 无脑切五段」。
反向验证时把正则退回 `[^-]+` 版本，**那条测试照样绿**——去量才发现那句话是
**编的**：全部 18099 条里赛事名 762 种，**一个连字符都没有**（下划线不影响按
`-` 切），轮次那一段永远是 14 个大写标记之一（`R128/R64/R32/R16/QF/SF/F/RR/
Q1/Q2/Q3/BR`…）。无脑切也切得对。

**一个听起来很合理的理由，和一个量过的理由，写在注释里长得一模一样。**
现在那条判据钉的是真事实（两条巡回赛赛事名对不上，抹掉它当场红），假理由
连同它的来路一起留在注释里。同一族的还有本文件里「edge-tts 同理」那次——
**没量过的推断写进注释，之后每个人都拿它当判据**。

⚠️ **fixture 也栽了一次**：第一版我把索引拼成 `<a href="X">X</a>`，而真索引的
链接文字是人读的那一串（`2026-08-02 Washington F: Alexandra Eala vs …`），
于是同一个 slug 出现两次、条数翻倍。**手搓的 fixture 只能验函数的局部行为，
验不了它和真页面对不对得上**——判据现在钉死「链接文字不许等于 slug」，
谁再手搓一份就当场红。

##### ⚠️⚠️ 最坏的一种：**编出来的判据 + 正确的结论**——没有人会去查它

2026-08-17 `rybakina-townsend` 的转写核对。第 20 行两份 ASR 打架
（YouTube `never played before` / whisper `we played before`），我判成后者，
把理由写进了 `_verified_clean`：

> 第 14 行（主持人）`you've played her before.`
> 解说（窗口开头）`as she did in the first meeting between the two
> on the US hard courts last year`

**第一条是真的。第二条是我编的**——全文搜 `first meeting` / `last year` /
`US hard`，源片字幕里一个都没有。而**结论是对的**：去查 tennisexplorer 的
mutual 页，莱巴金娜 2-0 汤森德，上一次是 2024 迈阿密第三轮 6-3 6-7 6-4。

⚠️ **这比「理由编错了」危险一档，因为它自带免检**：上面那条（赛事名带连字符）
错在结论一起错，反向验证一跑就露；这一条**结论经得起查，于是没有人有理由去查
它的理由**。它会在仓库里躺着，直到有人照着「转播说过 X」去写下一条片子。

**判据是「我搜过这个串吗」，不是「这句话像不像源片说的」。** 引用源片的话，
落笔之前 grep 一次（`json.load(cap_*.json3)` 拼成全文再 find），十秒钟；
grep 不到就**别写那一条**——手上真有的那一条（第 14 行）本来就够用，
再去查一个外部源（H2H）比编一句解说便宜得多，也硬得多。

### 「排除了 A 和 B」不等于「就是 C」

上一条的近亲，而且更能骗过自己。2026-08-03 抽帧连着两趟全红，八档 client 一律
报 `Requested format is not available`。我的推理是：

    报的不是 403                  → 不是 IP 被挡
    报的不是「Sign in to confirm」 → 不是 cookie 问题
    ∴ 管线是通的，问题在片源（官方集锦现在只给 DRM）

**第三步是凭空多出来的。** 真实的候选里还有第四项——「我装少了」——而它
**报出一模一样的错**。真因是 `frame-grab.yml` 写着 `pip install -q -e . yt-dlp`，
**裸的**：少了 `[default]` 就没有 `yt-dlp-ejs`，解不了 n challenge，
格式表里只剩故事板，于是连我兜底的 `worst` 都匹配不上。

我不但写了错结论，还照着它排了「下一步三条路」去指导后面的工作。
改对之后**同样两条视频，第一趟就成功了**。

- 空结果那条说的是「不要把空当成不存在」；这条是**「不要把『我想到的原因都
  排除了』当成『我想到了所有原因』」**
- 自证的办法很便宜，而且我已经写好了（失败时跑一次 `--list-formats`）——
  **只是先写了结论**。⚠️ 排查工具写出来了不等于用过了

#### 抄一条已经跑通的工作流时，**注释也要抄**

上面那个真因，一字不差地写在 `match-reel.yml` 那行 pip 的注释里：

> `yt-dlp` 要装 `[default]`，它才带 `yt-dlp-ejs`……少了它不会报「装少了」，
> 而是 `n challenge solving failed` + `Only images are available`，
> **看起来像「这个视频没有格式」或者「cookie 过期了」**。

我写新工作流时是**照着结构抄的，没照着注释抄**。而这个仓库的注释正是教训的
存放处——**结构告诉你怎么写，注释告诉你为什么必须那么写**。

这是「加新能力就要同时改三处」的又一个变种：新调用没继承旧调用的前提。
判据 `test_装yt_dlp一律要带default` **自动推导，不维护白名单**（凡是
`pip install` 里出现 yt-dlp 的都要带 `[default]`），当天就在另外两条工作流里
抓到同样的漏配。

**排查一条新流水线时，先跟仓库里做同一件事、已经跑通的那条逐行 diff**——
尤其是装依赖那几行。这一步十秒钟，那天省下的是两趟 run 加一个错结论。

#### 顺带三条工具上的坑

- **`list_workflow_runs` 会撑爆上下文**：一次 40 万字符（每条 run 塞一整份
  repository 元数据）。落到文件再用 `tools/gh_runs.py` 挑字段
- **`pull_request_read get_status` 在这个仓库恒空**（见后面单独一节）
- **抽帧的 `--width` 给小了是静默降采样**：源片 1920 我写了 1600，
  画面看着挺好，只有并排才看得出亏了

## 北京日历会把美洲赛事的比赛日拦腰切断

接着上一条。那场球在 feed 里，却没进「今日赛程」——`espn.py` 按
`m.start_utc.astimezone(BEIJING).date() != d` 一刀切，而这场是
`2026-07-28T17:00Z`（美东 13:00），换算成北京是 **7/29 01:00**，被判成"明天"。

华盛顿的比赛日 11:00–23:00 美东 = 北京 23:00 到次日 11:00，**整个夜场都落在
北京的次日**。对中国观众来说"今晚看什么"本来就包含凌晨那几场，按自然日切等于
每天砍掉一半内容。

- 赛程卡单独把窗口延到**北京次日 12:00**（`render/schedule_time.py` 的
  `NEXT_DAY_CUTOFF_HOUR`）。12:00 是从赛程推出来的，不是拍的：美东夜场最晚打到
  北京次日 11:00 上下，欧洲赛事最早也要北京 17:00 才开赛，12:00 落在两者中间
- **不要去改 `fetch_day`**：daily / flash / content 等五条线共用它，改了会波及全部
- **前一天还在、今天不见了**，往往不是撤赛，而是那场**刚拿到时间**，于是从
  "无时间所以留在今天"掉进了"有时间但属于明天"。时间从无到有会改变归属日

顺带两个连带 bug，都是"事后追加的数据没走完整流程"：

- `build_digest` 的**排名回填只作用于它自己取到的那批**。跨日补进来的场次是之后
  追加的，漏了回填 → 卡上「郑钦文」后面空着，排序也拿不到"top 选手"这一维。
  抽成 `apply_rankings()` 给两处共用
- **赛事级别的别名也要按赛事名写**（场馆别名那条同理，见下）。洛斯卡沃斯
  ATP250 在表里登记的是城市名 `los cabos`，而 feed 给的是带冠名商的全名
  `Mifel Tennis Open by Telcel Oppo`，于是整站被判成"非巡回赛级别"挡掉

### 一个时刻有三种口径，别压成一种

官方 Order of Play 上写着两种时间，意思完全不同，而我一开始把它们都记成了
`official-exact`、卡上一律印裸时间：

- `Starts At 11:00 PM` —— 这块场地这一节的**首场**，到点就打
- `Not Before 1:00 AM` —— 排在前一场之后，1:00 只是**下界**。前一场打成三盘，
  这场就往后拖

郑钦文那场正是后者：跟在 Majchrzak–Paul（23:00 开打）后面，真打起来可能是两点。
**印成「01:00」等于告诉熬夜的人到点开电视**——而"几点能看上"就是这张卡的全部
价值。一节里只有首场是 `Starts At`，所以这个差别覆盖的是大多数场次，不是边角。

现在是三个口径，各有各的写法：

| 卡上 | 数据里 | 意思 |
|---|---|---|
| `01:00` | `official-exact` | 官方给的开赛时刻 |
| `不早于 01:00` | `official-not-before` | 官方给的下界，要等前一场打完 |
| `预计 01:00*` | `official-order-estimate` / `single-source` | 我们按场序推的 |

- **「不早于」不能像「预计」那样在正文里省掉**。省「预计」不丢信息——末尾那个
  `*` 本身就是标记；「不早于」没有对应符号，省掉就变成一个看着确切的时刻。
  正文压字数时最容易顺手砍掉的就是这三个字，判据落在
  `test_caption_keeps_not_before_instead_of_shortening_it_away`
- **两个口径会叠在一起**：`不早于 +1 01:00`。跨天标识是替换钟点那一段做的，
  改写法时别把它挤掉
- 交叉校验的计数要**分开报**（`确切 N 场 / 不早于 M 场`）。两者都参与
  agree/conflict 比对——口径是下界，时刻仍然是两源都给出的那个

## ⭐⭐ 只讲确认过的，不要多说没确认的

账号所有者 2026-08-27：「**你只需要讲你确认过没问题的，不要多说不确认的东西**」。

来路是同一轮里我做了三次同样的事：

| 我多说了什么 | 它的真实状态 |
|---|---|
| 「Azure 可能把『二年』读成了『两年』」 | **假设**，我够不着 Azure，一次都没复现过 |
| 「2013 法网八强他也输给过纳达尔」 | 事实是真的，**但不是他问的那件事**，我还顺手写进了旁白 |
| 「趟数才是杠杆／可能还要十几分钟」 | 拿**滞后的状态接口**推的，产物早就落地了 |

⚠️ **三次都不是撒谎，是「我把没落地的东西讲得像落地了」**——而听的人分不出来
哪一句是我量过的、哪一句是我猜的。**一份掺着推测的汇报，比一份短的汇报更贵**：
对方要么照着假的做决定，要么每一句都得重新问一遍。

**汇报只留两类：**

| 说 | 不说 |
|---|---|
| **我量过／查过，结果是 X**（带出处或产物） | 「可能是」「应该是」「多半是」 |
| **我没查到，卡在这儿**（说清卡点） | 顺手挖到但跟这件事无关的事实 |
| **做完了 / 没做**（一句话给状态） | 还没发生的动作讲得像已经发生 |

⚠️ **查到的无关事实不是不能留，是不能往产物和汇报里塞**——记进代码注释或
CLAUDE.md，省下一次重查；那是它该待的地方。（`wawrinka-wildcard` 那条 2013
八强就是这么处置的：注释里留着，旁白里撤掉。）

⚠️ **和本文件「摆证据，然后按数据走，别停下来等」不冲突。** 那条说的是
**有证据的时候别停下来问**；这条说的是**没证据的时候别说得像有**。两条合起来是
同一句话：**说出口的每一句，要么背后有产物，要么明说它没有。**

## ⭐ 「这就去做 X」说出口，就必须在同一个回合里做掉

账号所有者 2026-08-04：「**说过的事情就要做到啊，你要记住**」。

那天同一件事我漏了两次，两次都是**话说了、手没动**：

| 我说的 | 我实际做的 | 对方怎么发现的 |
|---|---|---|
| 「要我发起吗？」然后停下等 | 什么都没做 | 隔了几轮问「**那片子出来了吗？**」 |
| 「这就渲一版给你看」 | 提交完就收尾了，**没触发** | 「**做好了么？**」 |

**这比「忘了做」更坏**：对方读到那句话之后就以为这件事在进行中，于是他等的是一个
根本没开始的东西。和「触发了不是进度，跑完了才是」是同一个形状——只不过那条说的是
**别把信号当产物**，这条说的是**别把「我打算做」当成「我做了」**。

判据很硬，不需要判断力：

- **回合里出现「这就 / 接下来我去 / 我马上」＋一个动作，那个动作必须在同一个回合里
  真的调用出去**。做不完可以只做第一步（触发、开跑、建分支），但**不能一步不做**
- **「要我 X 吗」在只有一个说得通的下一步时是禁句**（本文件开头那条已经写了），
  而它的危害在这儿加倍：问出口之后我还会停下来等，于是白白空转一整轮
- **收尾前扫一遍自己这一轮说过的话**：凡是「我会……」「这就……」，逐条对一下有没有
  对应的工具调用。没有就现在补上，别写进回复

## ⚠️ 「片子验完了，要不要推微信」——这一句不许再问

账号所有者 2026-08-03：「**推送啊，这是必须的啊不用我决定。**」

之前会话里我一直在片子渲完、`check_reel_landed` 报 0 项不合格之后收尾一句
「**推微信等您点头**」。那看起来像谨慎，其实是把「只有一个说得通的下一步」
拿去换了一次往返——**做一条片子的目的就是发出去**，验过了就发，这是默认动作。

**这条和「发现明确的问题就直接改」「还差三张图，要我去找吗——这种不要问」
是同一族**，只是这次卡在最后一步。判据一样：**对方除了「推」还能回答什么？**

| 情形 | 怎么做 |
|---|---|
| 片子渲完、`check_reel_landed` 0 项不合格、全量绿 | **直接合并 → 推**，推完在回复里报流水号 |
| 闸红了 / 抽帧看出画面对不上 | 修，修完继续推，别把红着的留给下一轮 |
| **改法有多个方向**（换哪张封面、要不要倒叙） | **这才要问** |

⚠️ **「推」不是一个动作，是一串**，缺一步就是发一条坏消息出去：
`rebase 到 main → 跑全量 → 推分支 → 合并（push=true 只认 main）→ push-reel`。
中间任何一步红了就停下来修，但**不要停下来问要不要继续**。

⚠️ 这条**不覆盖**「对外发布要先确认」的通则——它免掉的是「这条已经做完、
已经验过、账号所有者亲口要的片子要不要发」这一问，不是免掉所有确认。
换栏目、改口径、发一条没验过的东西，照旧要问。

## 出了问题当场解决，别等人来问

账号所有者 2026-08-02：「**以后出了问题去解决不要等**」。

那天封面那一轮 **09:34 就红了，13:02 对方问起我才发现**——中间三个半小时它一直
红着，而我在回复里写的是「正在出封面」。

**根子在轮询盯错了东西：我盯的是「成片有没有落库」，也就是成功信号。** run 失败
时那个循环只是一直等不到，然后超时退出，我据此报「还在渲」。**又是「兜底出事的
时候不吭声」，只不过这次不吭声的是我自己的监控。**

所以：

- **轮询要盯 run 的 `conclusion`，不是产物有没有出现。** 成功和失败都要能把我叫
  回来；只认成功的循环，遇到失败和遇到「还在跑」长得一模一样
- **一轮结束前先把在跑的东西查一遍再收尾**。「触发了」不是进度，「跑完了」才是
- **失败当场接着做**：读日志 → 定位 → 改 → 重跑。不要把一个红着的 run 留到下一
  轮对话，更不要等对方来问
- 这条和「查产物不查信号」是一家的：那条说的是**判断完成**要看产物，这条说的是
  **判断失败**不能靠等不到产物

### ⚠️ 0 秒的 failure ＋ 工作流名显示成文件路径 ＝ **这个 yml 本身无效**

2026-08-26 那条微信告警（`match-reel.yml：近 10 次失败率 100%，连续失败 10`）
就是这么来的，而**那十条 run 一行日志都没有**：

    created_at == updated_at == run_started_at    耗时 **0 秒**
    jobs 数 **0**
    event: **push**                               ← 而这条工作流只有 workflow_dispatch

真因是 `workflow_dispatch.inputs` 加到了 **26 个，超过 GitHub 的 25 个上限**。
文件一旦无效，GitHub 就**在每一次 push 上记一条失败 run 来报这个错**——不管
`on:` 写没写 push、不管推的是哪个分支。于是两小时四十分钟里十几次 PR 合并和
分支推送，把「近 10 次」窗口灌满了。

**认它有三个判据，全在 run 列表的元数据里，不用翻日志（也没有日志可翻）：**

| 看什么 | 文件无效 | 普通的失败 |
|---|---|---|
| 耗时 | **0 秒**（三个时间戳完全相同） | 有真实耗时 |
| `jobs` | **0 个** | ≥ 1 |
| `name` | **`.github/workflows/xxx.yml`**（路径） | **`match-reel`**（yml 里的 `name:`） |

第三条最省事：**GitHub 解析不了文件就拿不到 `name:`，只好拿路径顶上**——同一条
工作流在列表里换了个名字，本身就是判据。修好之后那趟 dispatch 出来的 run 立刻
显示成 `match-reel`。

⚠️ **25 这个上限是量出来的，不是查文档查来的**（官方文档写的是另一个数）：
26 个必挂、删掉一个变 25 当场就能跑。判据钉在
`test_match_reel_dispatch表单绝不超过github的25项硬限制`。

⚠️ **只有 `workflow_dispatch` 的工作流最容易中招，而且中了最难自愈**：它平时
不产生 run，所以**修好之后不会有新 run 把那批失败挤出窗口**，报表会一直红着。
`pipeline_health` 的告警条件带着 `latest_failure`（最近一条 completed run 是不是
失败），所以**手动跑一趟成功的就能立刻清掉告警**，不用等十趟——
`mode=cookies` 最轻，什么都不产、不提交、不推微信。

## ⭐ 封面选图和文案：**贴近比赛事实，且有吸引力**

账号所有者 2026-08-05，紧接着「以后都不用让我看」那句给的：

> 「主要是封面选图和文案要**贴近比赛事实，且有吸引力**。」
> 「**要应用到以后的规则和要求里**，从开始做到出片都能保证效果一致就行。
> **我的目标就是完全自动化**。」

这是**免掉那道人工闸之后补上的验收标准**——他不再逐条看，所以判据必须留在
仓库里。两个词是两件事，**而且只有一件量得出来**：

| | 是什么 | 判据在哪 |
|---|---|---|
| **贴近比赛事实** | 说出去的每件事都能在这场球的记录里找到出处 | **能落测试**，见下 |
| **有吸引力** | 这一屏值不值得停下来 | **判断题，故意没有测试**，见下 |

### 一、贴近比赛事实：**排名有两个出处，就必然会分叉**

海报的名条从 `cover.matchup` 渲，钩子是手写的——**同一个数，两个出处**。
2026-08-05 差点栽在这儿：查卢布列夫排名时我读的是维基百科榜单里的**变化列**，
得出「世界第九」，而他是**第十六**。要是那一版发出去，同一张海报会把同一个人
写成两个名次，而**渲染、质检、全量测试一律不出声**。

判据 `test_钩子和文案里写的排名要和matchup对得上`：钩子和 `push.summary` 里
写的排名，必须和 `matchup` / `versus` 登记的那个数一样。

⚠️ **只扫这两处，不扫 `push.lead` 和小红书正文。** 第一版四个出口一起扫，
当场四条全红——**四条全是误伤**：伊埃拉正文里的「世界第 140」是 2025 年迈阿密
那一周的她，霍达尔正文里的「世界第 8」是德米纳尔，张帅 `lead` 里的「五百九十五」
是 2024 年夏天。分界不是「哪个字段更重要」，是**这句话有没有地方放上下文**：
钩子 27 字、`summary` 20 字位，塞不下第三个人也塞不下年份，所以里面的排名
**必然**是这两个人的当期名次；正文有几百字，同一个说法在那儿完全可以是三年前的。

⚠️ **种子序号不是排名。** 卢布列夫在蒙特利尔是十号种子、世界第十六，两个数
都对，混成一件事就成了假话。抠取的正则只认「世界第N / 世界排名N / 排名…N」。

配套的老规矩在这条底下继续管用，别当成被取代了：**判历史事件用当年那本规则书**、
**写纪录先看它什么时候生效**（这场创的还是早就成立的）、**别用统称概括一组人**、
**别把「四强」写成「冠军」**。

### 二、有吸引力：**钩子要有剧情的跌宕，不是把两个身份摆在一起**

账号所有者 2026-08-05 看完第二版钩子：

> 「你这个**只靠排名**，这个**太机械化、固定化了，没有把剧情的跌宕说出来**呀。」

同一条片子的三版，account 的意见是照着第二版给的：

| | 钩子 | 毛病 |
|---|---|---|
| ① | 决胜盘二比五落后 / 五个赛点，一个没给 | 对，但没接上「赢了谁」 |
| ② | 伤停五个月，排名掉到两百七十 / 他掀翻了世界第十六 | **两个排名就印在钩子正下方的赛果行里**——等于把同一件事说两遍。而且它讲的是**身份**（他是谁、对面是谁），不是**这场球发生了什么** |
| ③ | 决胜盘二比五落后，五个赛点 / 他一个没给，掀翻世界第十六 | 过程 + 落点。排名只当收尾的重量，不当全部内容 |

**判据是「这一屏在讲过程还是讲身份」**：身份（排名、年龄、国籍、几个冠军）
海报别处已经印着了，钩子再写一遍是白占那 27 个字；过程（几比几落后、
救了几个赛点、连丢几局）只有钩子能给。

⚠️ **这条没有测试，是故意的**，和「最硬的那个事实放第 ① 屏」同一个理由：
机械挡不住。我试过把它写成「钩子里必须有一个非排名的数」——**第二版恰好过关**
（「伤停五个月」里的五不是排名），也就是说这个判据对它唯一想拦的那条是哑的。
硬凑一个会被绕过的判据，还会制造「已经守住了」的错觉，**比没有更糟**。

#### ⭐⭐ 2026-08-31 收窄一层：钩子讲的过程要是**整场**的，不是某一个时刻

账号所有者看完 `wu-walton-us-open-2026-r1`：「**封面标题没有把整场比赛概括**」。

那条钩子是「决胜盘两个盘点／他一个都没给」——按上面那条判据看它完全合格
（讲过程不讲身份、有跌宕、每行不超 10 字）。**它错在取景**：整场三盘两小时
二十四分，钩子只框住了决胜盘那一局。

| | |
|---|---|
| ❌ 一个时刻 | 「决胜盘两个盘点／他一个都没给」 |
| ✅ 整场 | 「两次差点丢掉一盘／三个盘点一个没给」（首盘 5-6 对手发球胜盘 1 个，决胜盘 4-5 自己发球 2 个） |

⚠️ **这条不是「要写全场数据」**——「全场 43 个制胜分」是身份不是过程，
上面那条照旧管着它。要的是**这场球的形状**：一句铺垫说清它是怎么走的
（两次濒临丢盘），一句落点说清结果（三个盘点一个没给）。

⚠️ **仍然不许重复比分板。** 比分板就在钩子正下方印着 7-6 6-2 7-5，
所以钩子里不写「直落三盘」——「概括整场」不等于「把赛果再说一遍」。

⚠️ **同样没有测试。** 「这两行框住的是一场球还是一局球」要读一遍才知道，
机械挡不住；能落判据的只有行长和行数（下面两节）。

#### ⭐ 而**每行几个字**是量得出来的：solo 封面的钩子，每行 ≤ 10 个字符

账号所有者 2026-08-14，看了 `townsend-osorio` 那张海报：「**以后封面下面标题
的字号就保持这种，不要再小了，所以以后要控制每行文字的数量，把这个记下来。**」

上一条管「写什么」（判断题，没有仪器）；这一条管「写多长」，而它**完全是机械的**
——因为字号是算出来的，不是写死的：

    字号 = min(上限, 940 ÷ 最长那一行的字数)      `versus_poster.hook_title_px()`

所以「字号不许更小」和「每行不许更长」是同一件事的两种说法。那张海报最长行
10 个字符 → 940/10 = **94px**，这就是下限（`MIN_HOOK_TITLE_PX`）；
每行的字符上限**从它推出来**（`HOOK_MAX_CHARS = 940 // 94 = 10`），不另写一个数。

⚠️ **它约束的是文案，不是版式**：报错要去砍字，不要去调字号或改 940。
钩子只有两行的位置，而排名、轮次、比分海报上已经印着了（上一条就是说这个）——
把身份那半句删掉，长度基本自己就回来了。

闸装在 `build_match_reel.validate_spec`：`--dry-run` **0.2 秒**就报，不用等渲染。
已发的 48 条挂在 `_LEGACY_LONG_HOOKS`，**只许减不许加**，表自己有自检
（slug 要真的存在、真的是 solo、真的还超着）。

⚠️ **只管 `layout: "solo"`**——cutout / diagonal 那几版的 `.hook` 是写死的
100px，掉不下去也就没有下限可守；它们的漂移归 `test_钩子最长行不许比今天最坏的还长`
那道棘轮。

##### ⭐ 2026-08-31 补上另一半：**行数**也有硬上限，solo 最多两行

上面那条管「每行几个字」，而**几行**一直只是个没人写过的默认。2026-08-31
账号所有者第五次要求「标题可以往下再挪一点」（`STORYCOPY_TOP` 750→790），
挪得动的前提就是把这个默认变成闸：

    最坏情况 = 钩子 ＋ gap 34 ＋ 比分板（板头 76 ＋ 网格 214）
    两行 124px → 308 + 324 = 632   → top ≤ 808
    三行  96px → 357 + 324 = 681   → top ≤ 759   ← 790 在这一档是溢出的

第三行会把**比分板的输家那一行推出画布**，而 Chromium 和 ffmpeg 都不报错。
`SOLO_HOOK_MAX_LINES = 2`，闸在 `_hook_lines_fit_the_title`，**零豁免**——
存量 161 条 solo 封面全是 2 行（量过，不是印象），所以装上它一条都不误伤。

⚠️ **行数这一条不吃 `_LEGACY_LONG_HOOKS` 的豁免**：那张表放行的是「行太长
所以字号偏小」（已发的不重渲，产物只是小一点），而行数超了是**版式溢出**，
重渲与否都会被切。两种毛病同一张表会把后者也一起放过去。

⚠️ **判据是一对，缺哪一头都不行**（都写在 `test_文案块的位置不跟着比分板漂` 里）：
「`SOLO_HOOK_MAX_LINES == 2`」＋「`STORYCOPY_TOP + 两行的最坏值 ≤ 1440`」。
只留后者，有人把行数上限调到 3 就静默溢出；只留前者，常量往下挪就没有上限了。

##### ⭐⭐ 2026-08-31 同一天第三次：钩子的字号收成**一个数** 94px（−20%）

账号所有者，还是这条片子：「〔封面钩子〕**高度再小 20%**」。

⚠️ **第一眼看像是在推翻 08-14 那句「字号不要再小了」，算一下发现不是**：
那条钩子最长行 **8 个字符**，按老公式渲出来是 `940/8 = 117px`；
`117 × 0.8 = 93.6`，**正好落回 94**——也就是他 08-14 看着 `townsend-osorio`
点头的那个大小（那张最长行 10 字 → `940/10 = 94`）。

所以这一条不是推翻，是**把 08-14 那句话照字面做到位**。他当时说的是
「字号**就保持这种**」，而实现把它做成了「下限 94 ＋ 上限 124/96」——
于是行越短字越大，同一个栏目的封面一条一个大小（124 / 117 / 94 都出现过）。
现在只有**一个数**：

    hook_title_px(lines) = min(HOOK_TITLE_PX=94, 940 / 最长行的字数)

| 最长行 | 改之前 | 改之后 |
|---|---|---|
| 2 字 | 124px | **94px** |
| 8 字 | 117px | **94px** ← 这条片子，钩子整块 290 → 233px，**−19.7%** |
| 10 字 | 94px | 94px（不变） |
| 25 字（存量豁免那批） | 37px | 37px（不变） |

⚠️ **只收上限，「太长就缩」那一半原样保留**。拿掉它的话
`white-space:nowrap` 会让长钩子直接顶出画布，而 `_hook_lines_fit_the_title`
那道闸恰恰靠「渲出来低于 94」认出超长的行——两件事一起坏，
判据 `test_一行超过十个字仍然按宽度缩下去` 就为这个（反向验证过）。

⚠️ **`STORYCOPY_TOP` 故意没跟着动**（790 不变）。钩子矮了 57px，比分板整块
跟着上移 57px；渲过 `top=790` 和 `top=847`（847 那版比分板正好回到原位）
两版并排比，**取 790**——理由就在那个常量自己的来路里：账号所有者三次
「往下移」要的是「**不要过多遮挡主体背景图**」，而钩子变矮本身就少挡了
57px，把它推回去等于把刚让出来的那块又盖上。顺带余量从 13px 宽到 **93px**。

⚠️ **三条老判据的主语没了，跟着换掉**（「短句上限比长句高一档」这件事
不存在了）：`test_一两行的短钩子拿到更高的字号上限`、
`test_三行钩子仍然封顶96不许跟着涨`、`test_短钩子新上限不会打破三行96px的
最坏情况上界` → 收成 `test_钩子的字号只有一个数不随行长变大`
＋ `test_一行超过十个字仍然按宽度缩下去`。**留着它们就是三条常年红。**

###### ⭐ 「同步到全局」：VS 那几版的钩子也走同一个出处，全库收成一个数

账号所有者 2026-08-31，紧接着上一条：「**钩子文案大小修改后，同步到全局**」。

钩子在**两个地方**渲，而只改了一个：

| 版式 | 元素 | 改之前 | 现在 |
|---|---|---|---|
| solo（默认，163 条） | `.storytitle` | 算出来 94~124 | **`hook_title_px()`** |
| cutout / diagonal（13 条存量） | `.hook` | **写死 100px** | **同一个 `hook_title_px()`** |

两边的可用宽度本来就一样（solo 的 `TITLE_WIDTH_PX` 是 940；VS 的 `.copy`
是 `left:66px right:66px` → **948**），所以同一个公式直接适用，不用另配一个数。

**「同步到全局」是可以量的，这是量出来的账**——163 条 solo 封面的钩子字号：

    改之前  37 39 42 44 47 49 52 55 58 62 67 72 78 85 · **94 104 117 124**
    改之后  37 39 42 44 47 49 52 55 58 62 67 72 78 85 · **94**

也就是原来 ≥94 的那 **115 条散在四个字号上（94/104/117/124）**，现在全是 94。
低于 94 的那 48 条是存量超长钩子（按宽度缩下去，不变，全在 `_LEGACY_LONG_HOOKS` 里）。

⚠️ **顺带修掉两条存量的静默溢出**：`jodar-fritz`（最长行 10 字）和
`wang-pareja`（11 字）在写死的 100px 下要 1000/1100px，而可用只有 948——
`.hook` 没有 `nowrap`，所以它们是**默默多折一行**，不报错。按公式算是
94/85px，装得下。两条都已发、不重渲，这一条只管以后。

⚠️ **没有跟着改的两处，理由要说清楚**（别让下一个人以为是漏了）：

- **赛后开麦的封面标题**（`build_interview_clip` 的 `.title`，写死 74px，
  同样是 1080×1440 画布）——它**不是同一个元素**：标题会换行、行长到 16 个字
  （reel 的钩子 `nowrap`、上限 10 字）。把它从 74 抬到 94 是**变大**，
  和「高度再小 20%」正好相反，而且长标题会折出更多行
- **解说片的封面标题**（`explainer.py` 的 `.cover .title`，`min(96, …)`）——
  另一个画布（1080×1920），而且它是一整句会 wrap 的**问句**，不是两行钩子

⚠️ 判据 `test_两种封面版式的钩子字号是同一个出处` **钉的是渲出来的 CSS**，
不是「源码里有没有写这个名字」：f-string 的占位符没插上值时留下的是字面量
`{vs_hook_px}`，**一个字都不报**。两个方向反向验证过（写回 100px / 让占位符
不插值），各红在自己的断言行。

##### ⚠️ 而同一天早些时候，我拿数据把这条否掉过一次

`tests/test_reel_editorial.py` 第二节里白纸黑字写着「**这一条本来提议做成硬闸，
扫完之后否掉了**」，理由是 64 条钩子的最长行**从 6 一路排到 25、没有任何一道
干净的缝**，按中位数设闸等于宣布一半的库存有罪。那半段观察是对的（均值确实在漂：
11.4 → 14.5 → 17.0），**推理错在最后一步**：

> **这个阈值本来就不在数据里，它在渲出来的那张图上。**

分布之所以连成一片，正是因为它整段都在漂——**一条漂出来的分布不会自带分界线**，
拿它找缝等于在问「今天的坏和昨天的坏之间有没有台阶」。判据是「这一行在信息流的
缩略图里还读不读得出来」，那要**打开图看**，不是排直方图。

⚠️ 「宣布一半库存有罪」那个担心也没有变成现实：已发的片子本来就不重渲，
豁免表照记就是了。当时真正该问的不是「有多少条会红」，是**「新的那条要不要按
这个写」**——而那个问题一问出来，答案是明摆着的。

### 三、选图：判据是**情绪对不对题**，不是大头行不行

账号所有者 2026-08-05：「**主要是看照片的情绪是否是符合，而不是说大头就不行。**」

我一直拿「大头特写不等于有冲击力」当硬规矩使，为了躲它去选中景。**那把尺
用错了地方**——那条规矩防的是「脸占满一屏，看不出他在哪儿在干什么」，
不是「不许近」。它是**冲击力那一档的说明**，不是一条独立的禁令。

所以四道闸门里第 2 道那句「画面要对上这一屏在说的那件事」，在封面上的
具体读法是：**这条片子讲什么，封面就要是那个情绪**。讲逆转就找他扛住的那一刻，
讲夺冠就找捧杯，讲缺席就没有照片可用（那才是示意图的触发条件）。

⚠️ **清晰度换帧解决不了，问题在源片。** 同一条片子的两版封面同口径量：
大脸那版整幅 Laplacian 方差 779.8、中景那版 754.4——**基本没差别**。源片是
压缩过的转播流，换哪一帧都一样。**这正是「封面用真实照片，不要从视频里抽帧」
那条说的情况**，而我从头到尾都在抽帧。

⚠️ **probe 的 `sharp` 只在候选之间可比，不是绝对清晰度**（它把脸缩到 128×128
再算）。我据此以为换大脸会更清楚，**结果更糊**——脸占的面积越大，同样的软越
显眼。⚠️ **锐化能把这个数从 779 刷到 1727，而细节并没有回来**，它不能当
「更清楚」的证据。

#### ⭐⭐ 2026-08-18：而在「情绪对题」之内，**优先近景特写**

账号所有者发了一屏抖音作品页的截图，用红框圈出四张封面：
「第一盘四十四分钟／第二盘八十三分钟」（施奈德）、「三比五落后／他连赢四局」（科博利）、
「五比五之后／她连赢六局」（佩古拉）、「那一盘六比一，能再来吗？」（王欣瑜）。原话：

> 「我觉得**红框里这些封面图选的比较好，都是近景特写**。」

**圈中的和没圈中的，差别很干净**——同一屏里没被圈的那几张（莱巴金娜的正手全身、
斯瓦泰克的全身、德约的反手全身、伊埃拉那张只有球拍和球）**都是全身动作照，脸很小**；
被圈的四张**脸是画面的主体**，肩以上占掉大半个宽度，表情读得出来。

所以判据是：**这张图的主体是「一张脸」还是「一个在打球的人」。**

| | |
|---|---|
| ✅ 优先 | 头肩填满画面宽度的大半，**表情读得出来**（吼、握拳、闭眼、笑） |
| ⚠️ 次选 | 全身击球照——动作好看，但缩到信息流的小图里**脸只剩几个像素** |
| ❌ 照旧不行 | 训练照、场外、发布会、看台（四道闸门第 2 道，没变） |

⚠️ **这一条是在「情绪对题」之内收窄，不是取代它。** 上一节账号所有者 2026-08-05 说的
「主要是看照片的情绪是否是符合，而不是说大头就不行」仍然是**上位判据**——
先问情绪对不对题（讲逆转就找他扛住的那一刻），**在都对题的几张里再挑近景那张**。
一张情绪不对题的大头，比一张对题的中景差。

⚠️ **也不是「越近越好」。** CLAUDE.md 早写过「脸占满一屏，看不出他在哪儿在干什么」，
以及封面图片本身「亲吻奖杯那张太近景了」被退回过一次。被圈的四张都还留着**场地或球网
当背景**（科博利那张背后是球网，佩古拉那张背后是看台），看得出这是在球场上。

⚠️ **它变的是「同样合格的几张里挑哪张」，不是放宽任何一道闸。**
官方高清实拍那条前置条件没动（`cover_photo_problem`），四道闸门没动，
「同一辑里他只有庆祝没有击球中」那类记录照旧要写进 `_portrait_why`。

⚠️ **故意没有做成硬闸。** 「脸占多大」量得出来（Haar 检出的脸框宽 ÷ 裁切后的画幅宽），
但**这条线经常只有一张能用的官方图**——那时候闸只会把唯一的选项挡掉，
而 CLAUDE.md 早写过「一条常年红的检查和没有检查是同一个毛病」。
它该出现的地方是**挑图那一刻**：同一辑里挑出好几张对题的，先看哪张的脸最大。
挑完在 `_portrait_why` 里写一句「同一辑里另外几张是什么、为什么挑这张」，
那句话就是判据。

#### ⭐ 封面标题带着指控时，**放谁的脸就是在指认谁**

2026-08-16 做「盘外招」那条冷知识片时撞出来的，而它是一条通则不是这条片子的
特例：封面把**一句话**和**一张脸**压在同一屏，读者读到的是「这句话说的是他」。

那条片子的封面问的是「「盘外招」算犯规吗？」，我第一版顺手把当事人的 ATP 官方
实拍放了上去——**四道闸门全过，而它替读者做了一次定性**。账号所有者对这条片子
的要求恰恰是「按最稳妥的方式去表达」。

判据是**读一遍「封面这句话 + 这张脸」的合成语义**，不是分开检查两样：

| 封面那句话 | 放脸 |
|---|---|
| 陈述、赞叹、疑问，指向**事情本身**（「世界第一最短当过多久？」） | ✅ 放 |
| 带着**指控或负面判断**，而画面上这个人正是被议论的那个 | ❌ **别放**——换成证据本身 |

⚠️ **「证据本身」比一张脸更抓人，不是退而求其次。** 那条片子的封面最后画的是
`gamesmanship` 在规则书里的三处原文摘句加页码——**它直接回答了封面那一问**，
而一张脸只是把人认出来。当事人那张实拍留在第 ① 屏，那一屏的文字只有比分、
轮次和双方的原话，不带判断。

⚠️ **别把这条和分辨率混着记。** 那张图 1920×1080，铺满 1080×1440 也确实过不了
`test_封面图不许被放大` 的 1.00x 地板——**但换掉它的理由是上面这一条，分辨率
只是碰巧站在同一边**。混了的话，下次找到一张够大的实拍就会把人脸又放回封面。

#### ⭐ 候选之间怎么排：近景特写、情绪外露的那张优先

账号所有者 2026-08-18，甩来一张视频号后台的作品列表截图，红框圈出三条：
`shnaider-chwalinska`（施奈德，橙红发带呐喊近景）、`cobolli-blockx`（科博利，
举球微笑近景）、`pegula-navarro`（佩古拉，红色发带握拳近景）——原话：
「我觉得红框里这些封面图选的比较好，都是近景特写以后都按这个要求来选**如果
没封面，都先做好视频等封面**。」

⚠️ **和「判据是情绪对不对题，不是大头行不行」不冲突，是它的下一层。** 上面
那节防的是「为了躲『大头不等于冲击力』这条警告去选中景」——本节这三个例子
恰恰全是近景，而且都已经发过、账号所有者亲口认可。所以判据没变（画面要对上
这一屏在说的那件事），新增的是**在若干张都合格的近景候选里怎么排序**：

紧接着账号所有者又发了同一批候选里的另外两张对比图——都是科博利（同一件
浅灰蓝 On 球衣、同样的发型），一张是**侧身回头看镜头**（表情平静），一张是
**正面握拳庆祝**（咬牙、拳头攥紧）。原话：「感觉其实下面这个更好」，指的是
后一张。

| | 姿态 | 表情 |
|---|---|---|
| 已经用在 `cobolli-blockx` 封面上的那张 | 举球朝看台招手 | 微笑，温和 |
| 「下面这个更好」指的这张（未采用） | 握拳庆祝 | 咬牙，用力 |
| 未选中的那张（回头看） | 侧身回望 | 平静 |

⚠️ **`cobolli-blockx` 已经发过，这条不重渲**（消息发出去收不回来）——它自己
还被列进了「选得好」的红框里，说明当时选的那张本身没有错，这条反馈是**给下一次
遇到同一批候选时的排序建议**，不是否定这一条。

**判据：同一批候选都过了前三道闸门（精准、在比赛中、情绪对题）时，选肢体和
表情张力更大的那张**——握拳、咬牙、呐喊这类「用力」的姿态，优先于招手、
微笑、回望这类「松弛」的姿态。这条只在**多张都合格**时才用来排序，不能拿它
去否掉一张情绪已经对题、只是没那么用力的图（比如捧杯致辞时脸上是欣慰不是
咬牙，那本来就是那一刻该有的样子）。

⚠️ **「没有封面先做完视频等封面」这句话确认了当时的做法是对的**：
`cirstea-kalinskaya` 这条当时就是官方高清图还没发布、先把 spec／旁白／
`--check-narration` 全部走完、只留 `cover.portrait.image: "PENDING"` 占位，
这条反馈印证了这个流程本身不用改，等图落地直接补上重渲即可。

### 四、标题要通顺：一句只留一层铺垫，别把代词插进原本连读的节奏里

账号所有者 2026-08-20：「还有就是标题写的要有钩子，但是你要写的通顺以及内容
需要人理解什么。」

来路是 `kostyuk-andreeva` 那条推送标题——「决胜局四十比零落后她逆转」。

**我先按「缺名字、缺标点」去理解这句反馈，扫一遍存量才发现这个读法是错的。**
拿全部 130 条 `push.summary` 过一遍名字和标点：**只有 7 条带标点，大半条都不带
人名**（`零比二落后连赢六局`、`三比五落后连赢四局`、`七个赛点她才拿下这场`……），
而这些一直在发，账号所有者从没为此说过什么。**「没有名字」「没有逗号」不是
这条标题真正的毛病**——硬把它写成规矩，会把上百条已经发出去、被认可过的标题
一起判错。又是这个仓库反复栽过的「查了一个案例就写成一类」。

**真正的差别在节奏，不在缺不缺哪个成分。** 把这条和「零比二落后连赢六局」
并排读：

    零比二落后连赢六局              一层铺垫（比分）+ 一个动词收尾，一口气读完
    决胜局四十比零落后她逆转        三层铺垫（决胜局／比分／落后）叠在一起，
                                      半路又插进一个「她」——读到这儿，节奏断了一次

「决胜局」「四十比零」「落后」三个限定语连着堆，读者还没拼出主干就先吞下
三层信息；插进去的「她」也不是这句原本该有的样子——其余一百多条标题几乎都
不写这个代词，「落后」和收尾动词共享一个隐含主语，直接连着读就通。**这跟
「有没有名字」无关，跟「铺垫叠了几层、代词有没有打断本该连着读的两个动词」
有关。**

⚠️ **这条没有做成硬闸，是故意的**——和「钩子要有剧情的跌宕」同一个理由：
「铺垫叠没叠多」「代词插得顺不顺」要靠读一遍判断，机械挡不住；硬写一条
「必须有名字」或「必须有逗号」的判据，上面那次扫描已经证明它会拦错方向、
误伤一整批合格的标题。唯一落得了地的判据是**写完自己读一遍**：铺垫别超过
一层，收尾动词前面别插一个原本不需要的代词。

⚠️ `kostyuk-andreeva` 的标题已经推送出去，不重发——`push.summary` 冻结不动
是本文件早定的规矩，消息发出去收不回来。这条只管以后写的标题。

## ⭐ 渲完 → 质检 → **直接推微信**，不要停下来等他看

账号所有者 2026-08-05，两句话隔了一小时，**后一句改掉了前一句**：

> 「那以后你要单独发出来我看一下呀，你不发出来我怎么在这里看呀？」
> 「**以后都不用让我看，你按着之前积累下的规则质检通过后，就直接推微信给我就行**」

前一句针对的是**我渲完了却只在回复里描述**（他在对话里看不到仓库）。后一句
把口径定死了：**他要的是收到成品，不是收到一个待批的草稿**。微信那条推送本身
就是他看片子的地方，再在对话里等他点一次头，等于把同一件事审两遍。

所以一条片子的终点还是那句老话——**发出去**，只是现在连「发之前给他看一眼」
这一步也免了：

    渲（push=false，在分支上）→ 质检 → 合进 main → push-reel

**「质检通过」是有清单的，不是感觉**（这条线上已经攒下来的）：

| 闸 | 判据 |
|---|---|
| `check_reel_landed` | **0 项不合格**（画布、音画等长、数字静音、无解说段有现场声） |
| 全量测试 | 绿，且**rebase 到 main 之后重跑**（提交数对不上就说明跑的不是同一棵树） |
| 旁白 | `mode=narration` 每段装得下、无哑场 |
| 切词 | 报告里没有「假词」（读音变了的那种） |
| 链接 | 复制页探得到、成片链接探得到（成片取不到**整条不发**） |

⚠️ **仍然要停下来的只有两种**（和本文件开头那条一样）：**闸红了**，
和**口径选择**（改法有几个方向、选哪个取决于他想要什么）。
「这一步不可逆所以我确认一下」**不在这两种里面**。

### ⚠️ `mode=push` 不发微信——还要 `push=true`，只拨一个 run 照样绿

2026-08-05 踩的，就在上面那条授权生效的第一条片子上。`mode=push` 的**唯一
目的**就是发微信（工作流开头写着「成片已经在仓库里，只写复制页 → 提交 →
发微信」），可真正发消息那一步的 `if:` 认的是 `inputs.push`：

    mode=push               → run 30969384140  **27 秒、绿的、什么都没发**
    mode=push + push=true   → run 30969488230  流水号 3a71c797…，发出去了

**绿的那一趟和「发出去了」长得一模一样**：conclusion 是 success，日志里
没有任何一行说「本次没有推送」——**被跳过的步骤根本不进日志**。我是回头去
读日志找流水号才发现的；不读的话这条片子会一直挂在「已推送」名下。

- 又一次「兜底出事的时候不吭声」，只不过这次不吭声的是一个 `skipped`
- 闸装在最前面（`mode=push 就必须 push=true`）：这个组合**没有意义**，
  跑完什么也不产，第 5 秒报错比绿着回来强
- 判据 `test_只给mode等于push不给push等于true要当场报错` 钉两头：闸存在且
  拦的正是这个组合、**而且排在写复制页之前**——排在后面的话那一趟照样先干一遍活
- **「已推送」的判据是日志里那行流水号**，不是 run 变绿。这跟「触发只是信号，
  第 12 步的 success 才是产物」是同一条，只是这次连 success 都不够

⚠️ **`SendUserFile` 没有作废，只是不再是闸。** 顺手发一份在对话里仍然有用
（他不用去翻微信），但**不许因为「等他看」而不推**。成片超过 30 MiB 发不出去
（这条线 90 秒就 63 MiB），用 `tools/review_copy.py` 压一个审片版——⚠️ 那只
用来看，发微信的永远是 `output/` 里那份原码率的。

## 「做完了」要查产物，不要查信号

判断一次 Actions 生成有没有真的落地，一晚上错了三次，全是拿间接信号当结论：

- **查提交信息**：`grep "知识解说视频 masters-format"` —— 上一轮生成的提交一字不差，一 grep 就中。七条重生成里五条被取消，我报了「全部完成」
- **查文件路径有没有变**：一个在改动之前开跑、改动之后才落库的 run，也会让路径变化。它推上去的是旧内容，检查却报绿
- **凭猜断定原因**：新 logo 反复丢失，我直接认定「旧 run 在捣乱」。查了运行记录才发现根本没有旧 run —— 八个 run checkout 的都是正确的 commit，是它们在**互相覆盖**

**唯一没骗过我的是直接查产物本身**：新旧 logo 颜色差得很远（旧表盘 `(239,238,4)`，新球身 `(195,220,74)`），在卡片图标位置采样一比就知道。见 `tools/check_card_logo.py`。

配套两条：**先查运行记录再下结论**（`actions_list` 里 cancelled / failure 一目了然，比猜快得多）；**检查工具要把不合格的也列出来**，只在成功时出声的检查，没法证明它真的看过。

解说片这条线上现成的三个（都写成脚本了，别每次现搓）：

- `tools/check_explainer_landed.py` —— 这一版落库了没有。查文案 + 采一条色带比画面。
  **不指定文件**，整个 outdir 的文本一起找并报出命中在哪个文件——踩过：封面那句只在
  `wechat_title.txt` 里，拿去 `xiaohongshu.txt` 找就会误报「尚未落地」。
  另：**逐字节比对 PNG 太严**，CI 和沙箱编码差几千字节，内容一样也报红。
- `tools/check_explainer_voice.py` —— 这条片子到底是谁配的音。读成片旁边的
  `narration.json`（`generate_explainer_video` 落的），不靠「工作流传了什么参数」推。
- `tools/render_explainer_local.py` —— 本地渲六屏出来**亲眼看**，`--jpeg` 顺手压小图。

#### 三个当场骗过我的变种（2026-08-06 同一条片子上）

**① 「三个变体的文件大小完全相同」＝ 根本没重渲。** 本地比封面 `focus` 三档时，
我循环里渲完就 `cp poster.jpg`——而 `render_cover_local.py` 写的是
**`poster_local.jpg`**，`poster.jpg` 是 runner 那趟留下的旧文件。三次复制到的
是同一个文件，字节数一模一样（273921）。**判据不是「命令跑完了」，是「产物变了没有」**
——三个数一样就该立刻停下来查，而不是打开图去比「哪版好看」。

**② `list_workflow_runs` 按 `status` 过滤会撞 queued→in_progress 的空档。**
刚 dispatch 完查 `status=queued` 和 `status=in_progress` **都返回 0**，看起来像
「没跑起来」。不带过滤查才看见它在 `in_progress`。⚠️ 这跟「204 是信号，运行记录
才是产物」是一家的，但**处置相反**：「查不到」不许当成「没跑起来」去重发——
`concurrency` 按 slug 分组且 `cancel-in-progress`，重发会把正在跑的那趟掐掉。

**③ 一条命令报了错，后面两条却都「成功」了。** 同步分支时
`git rebase origin/main` 报 `Could not apply ...`，而紧跟着的 `git log` 和
`git push` 都返回 0——看起来像已经对齐了。真查：`.git/rebase-merge` 还在，
两个 spec 文件挂着 `AA` 冲突。**同一轮里两个信号打架时，一律去查状态本身**
（`git status` / `test -d .git/rebase-merge`），别拿后一条命令的退出码去追认前一条。

**「已推送」要等第 12 步返回，不是触发的时候说。** 触发只是把 run 排进队列——合成旁白、
渲卡、ffmpeg 拼片要跑六分钟，PushPlus 是**最后一步**。有一次我在触发的同时说「推送已发出」，
对方在这六分钟里一直等一条还没发出的消息，回了句「没有收到」。日志上的时间线一清二楚：
`06:31:10` 触发 → `06:36:49` 成片 → `06:37:14` PushPlus 返回 200。
**触发成功是信号，第 12 步的 success 才是产物**，又是同一个毛病。

### ⚠️ 反过来的那一面：**job 状态接口会滞后好几分钟，而滞后长得像「还在跑」**

上一条说的是**别把信号当产物**（触发了 ≠ 发出去了）。2026-08-26 撞上它的镜像：
**产物早就落地了，而信号还停在几分钟前**，于是我给账号所有者报了个错的 ETA。

`wawrinka-wildcard` 合并触发 `auto-push-explainer`（run 33003683671），真实时间线：

    19:09:52  合并触发
    19:10:33  第 8 步「推送到微信」开始
    19:10:35  Pages 点了一下 → 复制页**第 1 次探活就命中**
    19:10:56  PushPlus 收下，流水号 c4ff5c14162a4e57974db019bc6454d0
    19:10:58  记账推上 main（24e49a9），job 结束      ← **整趟 66 秒**

而我在 **19:18** 拉 `get_check_run` 和 `list_workflow_jobs`，两个都还报
「第 8 步 in_progress」——**滞后七八分钟**。我据此告诉账号所有者「可能还要十几分钟」，
理由是这条线的复制页探活窗口最长 13 分钟（26×30s）。**那个窗口是真的，但它是上界**：
#168 把站点缩到 0.66 MB 之后端到端只要 19 秒，健康状态就是第 1~2 次探活命中。

⚠️ **判据一直摆在手边，一秒钟的事**：

| 查什么 | 是什么 |
|---|---|
| `get_check_run` / `list_workflow_jobs` 的 status | **信号**，可能滞后好几分钟 |
| **`git fetch && git log origin/main`** 里有没有那条记账 commit | **产物** |
| `data/explainer_publish_ledger/<slug>.json` 的 `status` | **产物**，`sent` 就是发过了 |

记账那一步**排在不可撤回的 POST 之后**（顺序是钉死的，本文件早写过「反过来的话，
推送失败会留下一个『已经发过』的假标记」）——所以**「已自动推送」那条 commit 出现在
main 上，就等价于消息已经发出去了**，不用等 job 变绿。

⚠️ **这条对所有 GitHub 状态接口成立，不只这一条线。** 轮询一趟 run 跑完没有，
先问一句：**这件事有没有一个会落进仓库的产物？** 有就查那个，别查 status。

顺带记一条限制：那句 `PushPlus 推送成功` 只保证 **PushPlus 收下了**（代码检查的是
`data["code"] != 200` 就抛错），不保证微信真的推到了手机上。返回体里带着流水号，
现在没记进日志——真出现「接口成功但人没收到」的时候，再把它记下来并查一次投递状态。

**发消息那个 POST 原来一次重试都没有。** 走到它之前已经串着等了三轮——
复制页最长 10 分钟、成片 2 分钟、图片 5 分钟——**而它才是唯一真正发消息的动作**，
一次网络抖动就把前面十几分钟全废掉。更荒唐的是 `_upload_image`（传图片，
失败了还能退回 jsDelivr）**反而有**重试循环：**保护了不重要的那一步，
没保护唯一重要的那一步。** `_access_key()` 同样没有，而它也排在 POST 之前。

- **只重试「没送到」**（网络异常、HTTP 5xx）。`code != 200` 是 PushPlus 明确
  拒绝（token 错、内容超限），**重发只会把同一条错误消息发很多次**——
  而微信那条消息发出去收不回来。判据把这两支分开验，两个方向都反向验证过
- **流水号要记进日志**。`code == 200` 只保证 PushPlus 收下了，不保证微信推到了
  手机上；真出现「接口成功但人没收到」，没有流水号什么都查不了

**推送正文里的每个链接，指向的文件都必须在推送之前进仓库。** 复制页那一句
`write_text` 藏在推送步骤里，而推送步骤排在提交**之后**——文件只活在 runner 的
工作区，一次都没被 commit 过。日志印着链接、步骤是 success，人点开是 404
（run 30342567879）。**同一个毛病第二次**：上一次是成片没提交就发链接
（run 30339377013），这次是复制页。

所以现在拆成两步：`push_reel.py --stage page` 写文件（排在提交之前），
`--stage push` 发微信（排在提交之后），并且**发之前先探这个链接返回 200**——
Pages 重新发布要一两分钟，等得起；等不到就报错不推，别把一个 404 的按钮发出去，
微信那条消息发出去就收不回来。判据落在 `tests/test_match_reel.py::test_复制页写在提交之前`。

**第三次，换了个壳：文件提交了，但提交在特性分支上。** 赛程包的 `copy.html`
确实进了仓库，按钮点开还是 404——**GitHub Pages 只服务 `main`**。同一天的日报
`output/<date>/copy.html` 返回 200，因为那是 daily workflow 提交到 main 的；
只活在分支上的包，Pages 永远取不到。

- **图片没事会让人误判**：卡片图走 jsDelivr 且钉在 commit SHA 上，任意 commit
  都能取，所以"图都好好的"不能推出"链接也好好的"
- **`pushplus.wait_for_images` 帮不上**：它**故意**只校验 jsDelivr 链接，把 Pages
  链接排除在外（注释写着 Pages 发布延迟大、不该阻塞推送）。那个判断对提交到
  main 的日报成立，对分支上的包不成立
- 所以每条会发出去的外链都要**自己探一次**（`live_copy_page_url()`：200 且
  `Content-Type: text/html` 才算数），探不到就**不放这个按钮**
- 而且**不能只是不放**：复制页是文案唯一的出口，去掉按钮就等于文案传不出去。
  消息本身要留后路——标题单独成块标注「长按可复制」，正文整段渲染

**第四次，方向反过来了：探测本身排在了提交之前。** 上面那条「每条外链都自己探
一次」我给解说片补上时，把 `live_copy_page_url` 装进了**生成**流程——而生成
排在提交之前，那一刻 copy.html 还没进仓库，Pages 必然取不到。于是按钮**每次都被
摘掉**，连 main 上一分钟后本来能用的也一起摘：

    第 9 步  渲染  07:41   写 copy.html + 探它可不可达   ← 探在这儿，必然失败
    第 11 步 提交  07:43   copy.html 这时才进仓库
    第 12 步 推送  07:44   PushPlus

2026-07-29 蒂姆那条推送就是这么少了按钮的（run 30432435525）。安全是安全了，可它
把「防死链」做成了「永远没有按钮」——正是上一条明令禁止的那个结果。

- **闸要装在「发」的那一步，不是「渲」的那一步**。`publish pushplus` 跑在提交
  之后，那才是链接真正该可达的时刻。现在收在 `drop_dead_copy_button`（发送前探
  一次，取不到就只摘那个 `<a>`，正文和其他链接一概不动），解说片 / 知识帖 /
  赛程包三条线共用同一个出口，一处装闸三条护住
- **只测行为拦不住位置错**。第一版的测试三档行为全对、全绿，按钮照样每次消失。
  所以另加一条盯**位置**的（`test_复制页那道闸装在发的那一步不是渲的那一步`）：
  生成流程里再出现 `live_copy_page_url` 就红
- **验这个修复不能查 `push.html`**：摘按钮是发送时改内存里那一份，仓库里那份
  永远带着链接，查它只会得到「按钮一直都在」的假绿。判据是那行两种情况都出声的
  日志（`复制页可达 <url>` / `复制页取不到`）——又一次「检查工具要把不合格的
  也列出来」
- 对照实验：同一条片子、同一个 URL，只换闸的位置，07:44 那次「取不到、按钮没了」，
  08:18 那次「可达、按钮在」（run 30434707717）

### 第六次：等待窗口结构性地短于 Pages 的发布时间

2026-08-03 那条解说片推送**没有复制按钮**，账号所有者当场发现。日志很干净：

    09:17:28  提交 copy.html 进仓库
    09:17:29  复制页暂不可达（HTTP 404）      ← 提交后 1 秒就开始探
    ...每 30 秒一次，连探 12 次...
    09:22:59  复制页取不到，本次不放该按钮；正文已整段渲染在消息里
    09:23:12  PushPlus 收下

**闸是对的，窗口是错的。** 查 `pages-build-deployment` 才看见真相——这个仓库
`output/` 一 GB 多，Pages **每次重建整站**，实测：

| commit | 用时 | |
|---|---|---|
| `0cfe168` | 5:50 | success |
| `89f34a1` | 7:14 | success |
| `7c90993` | 8:12 | **cancelled** |
| `42764e0` | 9:30 | **cancelled** |
| `4b78522` | 19:48+ | 还在跑 |

而 `12 × 30s = 5.5 分钟`——**比最快的那次还短**。也就是说这条线上那颗按钮
几乎不可能等到，而它每次都安静地退回无按钮版，**看起来和「今天 Pages 恰好慢了」
一模一样**。又一次「兜底出事的时候不吭声」。

- 常量改成 **26 × 30s ＝ 12.5 分钟**，盖住实测最慢那次（9:30）还留三分钟余量。
  探到就立刻返回，所以典型代价仍是 6–8 分钟
- ⚠️ **那条守卫测试自己就是漏洞**：docstring 写着「实测 Pages 可以慢到 12 分钟
  以上」，断言却只要 `>= 300` 秒——`12×30=360` 轻松过关。**判据比它引用的那个数
  还松，等于没装。** 现在从 `MEASURED_PAGES_BUILD_SECONDS` 推，改一头必须改另一头
- ⚠️ **「把复制页挪到渲染前面单独提交」对这条线无效。** 那是 match-reel 的解法，
  而 match-reel 的推送是**另一趟 run**。解说片生成和推送在同一趟里，
  **成片那次提交会把复制页那次的 Pages 构建取消掉重来**（上表两条 cancelled
  就是这么来的）
- ⚠️ **jsDelivr 顶不上。** 它确实立刻就能取到 copy.html（200），但
  `Content-Type: text/plain`——浏览器把 HTML 源码原样显示，复制按钮直接废掉。
  探过了，别再试
- **真正的杠杆是把站点变小**（`output/` 一 GB 多）。那是另一件事

#### 第七次：换成 Actions 部署之后，触发器对唯一重要的那条路径是空的

`#168` 把 Pages 从「按分支部署」换成「按工作流部署」，顺手把上面那条
「真正的杠杆」做掉了——站点从 2.26 GB 缩到 **0.66 MB**，构建 **20~27 秒**。
可它带来一个静默回归：`pages.yml` 的触发器写的是 `push: paths: output/**/*.html`，
而**复制页正是工作流自己 commit + push 的**，GitHub 明文规定用仓库自带的
`GITHUB_TOKEN` 推的 push **不创建 workflow run**（防递归）。

**症状是「慢」不是「错」**：探活一直 404，探满 13 分钟之后静静摘掉按钮、
正文照发。伊埃拉那条连吃七次 404，我手动 dispatch 一次之后第 8 次立刻中。

修法是 `trigger_pages_build()`：`workflow_dispatch` 正是那条禁令**明文列出的
例外**，主动点一下就绕过去了，不用另配 PAT。真正的教训在后面两条。

##### ⚠️ 204 是信号，运行记录才是产物

`dispatches` 那个接口返回 204 只保证 **GitHub 收下了请求**——工作流被停用、
`pages.yml` 不在默认分支上、ref 指到没有这个文件的地方，都会收下之后什么也
不发生。第一版就是「204 → 报成功」，于是它和真的跑起来了长得一模一样。

现在点完要回头确认那条 run 出现了，并把 **run id 和 `actor`** 打进日志：

| actor | 意思 |
|---|---|
| `github-actions[bot]` | **是这段代码点的** |
| 写着人名 | 那趟是有人手动补的 |

这个区别不是学术的：2026-08-04 `pages.yml` 上那两条 `workflow_dispatch`
记录**是我手动补的**，而我一度把它们当成「修复生效了」的证据。

⚠️ **认「新东西出现」要比变化量（run id 的集合差），别比内容特征。**
按「最近有没有一条 `workflow_dispatch`」判的话，基线里本来就有，一上来就为真。
⚠️ **「读不到运行列表」和「没跑起来」处置相反**，日志里不许混成一句。

##### ⚠️ 判据只在发布路径上才走得到，它就永远验不了

这才是这一轮最贵的一条。那个修复写完、合并、写进 PR 正文，状态是
**「写了，没跑过」**——因为它的判据（出现一条 bot 的运行记录）只有在**真推送**
的时候才走得到，而真推送会发一条**收不回来**的微信消息。于是「验一下」和
「发出去」被绑在了一起，它就那么躺了一天。

**改法不是提醒自己下次记得看，是把验证从发布里拆出来**：
`pages-selftest.yml` + `tools/check_pages_trigger.py`，只点 Pages，
不出片、不发微信、不提交。**实测整个 job 16 秒**（06:41:18→06:41:34，
其中那一步本身 3 秒），比预估的 40 秒还便宜。

- 判据钉三头（`test_验证Pages那条路要够得着而且不发微信`）：入口够得着、
  **那条路真的短**（不许出现 push_reel / publish / ffmpeg / Chromium）、
  **工具自己查一遍产物**而不只信 `trigger_pages_build()` 的返回值
- 和 `--cover-only` 那次是同一个形状：能力写出来了，**能用的那台机器上
  没有开关**。只不过那次卡在工作流入口，这次卡在「用一次的代价是发一条消息」

**跑过了，判据成立**（2026-08-04 06:41Z，selftest run 30884872530）：

    点之前 pages.yml 已有 5 条运行记录，最新 run 30878244158 actor=robertyang87
    trigger_pages_build() 自报：True
    新运行记录：run 30884890108  actor=github-actions[bot]  event=workflow_dispatch
    ✅ Pages 点得动，而且是这段代码点的

`actor=github-actions[bot]` 是**量出来的，不是推出来的**——上面那张表里
「是这段代码点的」这一行从此有实据。这条修复从「写了」转成「跑过了」花了
一天，而拆出来之后**重跑一次只要 16 秒**：以后改 `trigger_pages_build`
或者换 token 的权限，跑一趟 `pages-selftest` 就能自证，不用再等一条片子。

⚠️ **selftest 绿只证明「dispatch 产生了一条 run」，不证明那条部署成功**——
同一个「查产物不查信号」再往下一层。所以顺手也查了被点起来的那条
（run 30884890108，`deploy` job）：**success，16 秒**（06:41:35→06:41:51，
`deploy-pages` 那步 7 秒）。端到端 **dispatch 06:41:32 → 部署完 06:41:51
＝ 19 秒**。

这个数才是下一条推送真正要用的：探活间隔是 30 秒，19 秒意味着**第 1 次探活
就该中**——**不是**七次 404 之后才中（伊埃拉那条）。要是仍然要探好几次，
说明这条链上还有别的东西没接上，别当成「Pages 今天慢」放过去。

⚠️ **「N 应该是 1」是按 match-reel 那条线的时序定的，别原样搬到别的线上。**
2026-08-05 商竣程那条走 `interview-clip`，日志是

    [复制页] 第 1/60 次探活 HTTP 404，40s 后重试
    [复制页] 第 2 次探活：已是这一版的内容        ← 提交后共 44 秒

按上面那条判据我该去查「链子哪儿没接上」——**查下来是判据搬错了**：这条线的
第一次探活紧跟在提交后约 1 秒发出，而 dispatch→部署完要 19 秒，**第 1 次
结构性地不可能命中**；间隔又是 40 秒，所以健康值就是 N=2。

- **判据要连它的时序前提一起搬**：真正该问的不是「N 是不是 1」，而是
  **「第一次探活是在提交后第几秒发的」**——比 19 秒晚才轮得到问 N
- 和「等待窗口结构性地短于 Pages 的发布时间」是同一个形状**反过来**：那次是
  窗口太短永远等不到，这次是第一枪打得太早必然空
- ⚠️ 一个搬错线的判据**比没有判据坏**：它会让下一个人去追一个不存在的问题，
  而且他找不到，只能把「Pages 今天慢」这个错答案记下来

##### ⚠️ 而我写完这条判据，它引用的那行日志根本不存在

上面这段的第一版写的是「日志里那句应该是**「第 1 次探活：已是这一版的内容」**」。
去看代码才发现：**`_probe_page` 命中之后一个字都不打**。它只在失败时出声
（旧版 / 404 / 异常），命中直接 `return True`。于是：

| 实际情况 | 日志里 |
|---|---|
| 第 1 次就中 | **零行** |
| 探七次才中 | 六行 404，**没有任何一行说「后来中了」** |
| 这一步压根没跑 | **零行** ← 和第一种一模一样 |

也就是说，「下条片子推送时看一眼是不是第 1 次就中」这个动作**当时根本做不到**：
要数的是失败行的条数，而 0 行还有第二种解释。

这是「只在成功时出声的检查，没法证明它真的看过」的**镜像版**——只在失败时
出声的检查，没法证明它成功过。而 `drop_dead_copy_button` 的 docstring 里
一直写着前半句（「让调用方能如实打印判断依据」），**下面那个函数是反的**。

- 命中要报**第几次**，不只是「中了」。第 1 次走 info，**第 2 次以上走 warning
  并把「本该 19 秒 / 间隔 30 秒」一起打出来**——不然读日志的人得回头翻这份
  文档才知道 3 是异常
- 两条探活路径（`_probe_page` / `live_copy_page_url`）共用 `_say_probe_hit`，
  别各写一遍再对不上
- 判据**自己推导不维护白名单**（`test_每条探活路径都要报第几次`）：凡是接
  `attempts` 又真的 `requests.get` 的函数都算探活路径，都必须调它。
  反向验证分两个方向做的，**第二个方向只有推导那条抓得到**（拆掉
  `live_copy_page_url` 那次调用，行为测试照样绿）——这正是它不冗余的证明

**教训比这个 bug 大**：我在同一轮里既写下「判据只在发布路径上才走得到就永远
验不了」，又立刻写了一条**引用不存在产物**的判据。判据落进文档的那一刻要问
一句——**它引用的那个东西，我打开看过吗？**

##### ⚠️ 单元测试的行为取决于环境里有没有 `GITHUB_TOKEN`

给上面那道确认加测试时抖出来的：`_probe_page` 会先调 `trigger_pages_build()`，
而它**只在环境里有 token 的时候**才真的发请求。沙箱里两个变量都有、CI 里
没有——**同一条测试在两个地方跑的是两条不同的路**，本地那条还悄悄打了一次
`api.github.com`（这个沙箱里恒 403）。

两边都绿，所以没人发现有一半断言从来没覆盖过那条路；直到我往那条路上加了
一行 `.json()`，本地才炸。**又一次「不吭声」。**

收在 `tests/conftest.py` 的 autouse fixture：单元测试一律看不见这两个变量，
要用的自己 `setenv`（autouse 先跑、测试体后跑，显式那份必然盖过）。
这和「本地装着不等于 CI 装着」是一家的，只是这次差的不是依赖，是环境变量。

##### ⚠️ 那条「自己推导不维护白名单」的测试，是伪装成推导的白名单

`test_会发微信的工作流都要能触发Pages` 的第一版扫的是
`push_reel\.py|publish\s+pushplus`——**它把答案硬编码成了两种拼法**，
于是 `publish content`（内容雷达）算不算，全靠写测试的人当时记得。

这次核下来它**碰巧**是对的（`cmd_publish_flash` 直接推图文，根本没有复制页），
可「碰巧对」和「真的接上了」长得一模一样，**而且前者会一直绿**。
现在拿 AST 找出真正调用探活那两个函数的入口，再顺着 `args.channel` 的分发表
倒推 channel 名。**用 AST 不用正则**：`push_reel.py` 的 docstring 里提了三次
`wait_for_copy_page`，真正的调用只有一处——而这个仓库的注释必然会提到被测的
那个名字。

### 第五次，是「等」和「重来」：复制页排在渲染之后，推送要重渲一遍

上面四次都在问「按钮该不该有」。第五次的毛病换了个维度：**它一直是对的，
只是每次都要多花十分钟**。一条推送的账单（run 30624808733，16 分 02 秒）：

| | 用时 | |
|---|---|---|
| checkout | **2:44** | HEAD 上 `output/` 就有 1.36 GB |
| 装字体/依赖/Chromium | 1:30 | |
| render | 7:22 | 真正的活 |
| 提交 | 0:07 | |
| 推送微信 | **4:03** | 日志：连续 12 次 `HTTP 404`，第 13 次才中 |

**那 4 分钟一秒都不用等。** `push_reel.py --stage page` 的内容只来自
`specs/reels/<slug>.xhs.txt` 和从 spec 算出来的标题——**全程没碰过 mp4**。
可它排在渲染**之后**，于是 Pages 要等最后那次提交才开始发布，推送只能站着等。
挪到渲染前面并当场提交，Pages 的发布就和七分二十二秒的渲染并行。
判据仍然是那行探活日志：改对了，「第 1 次探活：已是这一版的内容」应该一次就中。

**而「合进 main 再跑一遍」一直是在重渲。** 判据摆得很干净——同一条片子的
两个 blob：

    PR #108（分支上渲的）        wong-brooksby.mp4  55,028,417 字节
    run 30623994190（main 重渲）                    55,028,980 字节

七分二十二秒换来一个差 563 字节的同一条片子，外加往仓库里再塞一个 55 MB 的
blob。hewitt-washington 同样来过一次（56,953,265 → 57,297,653）。
**.git 涨到 1.77 GiB、checkout 每次两分多钟，就是这么攒出来的**——
成本还会反过来加在下一条片子的每一次 run 上。

现在拆成两条线，`push=true` 那条只留着当便利：

- **`match-reel push=false`** 在分支上出片（复制页也一起提交，跟着 PR 进 main）
- 合进 main → **`push-reel`**：只推送仓库里已经有的那份，不下源片、不合语音、
  不装 Chromium/ffmpeg/OpenCV。**三分钟，而且失败重跑也是三分钟**——
  不用再赌那十六分钟一次跑通

#### 合进 main 就自动发：`push.auto`（2026-08-02）

上面那条把机器时间从十六分钟压到了 **43 秒**（run 30755226229 逐步量：
checkout 14s → 装依赖 7s → 写复制页 0s → 提交 0s → **发微信 7s**）。
43 秒不值得再优化，**剩下的成本是人的往返**：渲完、开 PR、合并、
再记得回来点一次 dispatch。

⚠️ **那个 43 秒是这一趟 run 的数，后来量了更多趟是 15~31 秒**（见「后半段：
成片渲完到微信还有 21 分钟」那节）。**结论不变**（都不是瓶颈），记在这儿是因为
本文件里「合进 main → 推微信 43 秒」被当成当前值引用过——**单趟 run 的实测
是一个样本，不是这条线的常态**，引用之前先看有没有更多趟的数。

`auto-push-reel.yml` 省掉最后那一下：盯 main 上 `output/*/reel/*/render.json`
的变化，合并即推送。**默认什么都不会发**——要开，在 spec 的 `push` 块里加一行
`"auto": true`（和 `mixed_fps` / `silent_source` 一个形状：认领这一步把
「想清楚了」和「凑合一下」分开）。

四道闸全在 `tools/auto_push_gate.py` 里，**不写进 YAML**（那儿测不了）：

| 闸 | 拦什么 |
|---|---|
| 路径形状 | 必须是 `output/<日期>/reel/<slug>/render.json` |
| 显式认领 | 没写 `push.auto: true` 就跳过**并说明怎么开** |
| 没推过 | `pushed.json` 在**仓库**里就说明发过了，不重复发 |
| 一趟最多一条 | 一次带进来两条就一条都不发——批量自动发微信，错一次就是错一片 |

三个细节：

- **`"auto": "true"`（字符串）要报错**。Python 里它是真值而 `is True` 判它是假，
  两种处理都说得通，但「悄悄不发」和「本来就没开」长得一模一样
- **记已推送要排在发微信之后**。反过来的话，推送失败会留下一个「已经发过」的
  假标记，这条片子从此再也发不出去，而且不吭声。判据钉了这个顺序
- **不给它开 `workflow_dispatch`**。手动推送走 `match-reel mode=push`，那条路有
  完整的预检和参数；这条只认「合并」这一个触发源，少一个能手滑的入口

配套三条，都落了测试：

- **稀疏检出把 `output/` 挡在外面**（1.52 GB → 161 MB），本次的 outdir 由
  「算出目录」那一步 `git sparse-checkout add` 进来。⚠️ 排除的**只有**
  `output`：assets 155 MB 看着也不小，但渲染真要读它，少一个就是第 5 分钟
  才炸的 FileNotFoundError
- **推送元数据的出处是 spec，不是命令行**。对阵取 `cover.versus.names`、
  比分取 `cover.result`——**海报印的就是这两个值**，标题没有理由让人再敲一遍
  （`column_of` 那条「栏目只有一个出处」，在这两项上一直没做）。算不出来的
  `summary` / `lead` 写在 spec 的 `push` 块里。
  代价不是抽象的：工作流那几个输入的默认值挂的是**上一条片子的**
  （`伊埃拉 vs 郑钦文`、`郑钦文首轮出局`），漏传一项就拿另一场球的标题发出去。
  而且复制页和微信正文现在分在两次 run 里跑，`wait_for_copy_page(expect=title)`
  要求两次算出同一句标题——从 spec 读必然相同，靠人两次都敲对不必然
- **判据是「算出来的和已经发出去的一模一样」**：拿 spec 重新渲一份 copy.html，
  和仓库里已发的那份**逐字节相同**。查源码文本的断言只能防「有人把它删了」，
  防不住「它从来没工作过」

#### ⚠️ `versus.names` 是版式顺序，不是赛果顺序——我照着它拼标题，赛果说反了

上面那条「对阵取 `cover.versus.names`」我照字面做了，当天就出事：
**海报和标题在同一条推送里说反谁赢**。

海报的赛果行不读 `names`，读 `cover.winner`（`versus_poster._result_block`：
`loser = 另一个名字`）；而 `names` 是**上格/下格**的版式顺序。两者大多数时候
碰巧一致，`wang-samsonova` 不一致：

| | |
|---|---|
| 海报印 | `萨姆索诺娃 6-2 6-2 王欣瑜` |
| 标题算出来 | `王欣瑜 vs 萨姆索诺娃` |

而 `headline` 把比分插在两个名字**中间**，这个栏目的规矩是**赢家在前**——
所以那句标题不只是顺序问题，它在**声称输的那个人赢了**。

**老写法还要更糟。** 斜切那两条（`eala-zheng` / `nishikori-shang`）没有
`result` / `winner`，赛果是一整句 `cover.score: "伊埃拉 4-6 6-4 6-1 郑钦文"`，
海报原样印出来。不认这个字段的代价是**比分整个丢掉、赢家还写反**：
`names` 是 `["郑钦文", "伊埃拉"]`，算出来「郑钦文 vs 伊埃拉」。

- **判据要拿海报**印出来的那句比，不是「读 winner 再自己推一遍」——两边用
  同一个假设就会一起错。测试直接调 `_result_block`，把标签剥掉按出现先后排
  （`test_标题里的赛果顺序要和海报印的一样`）。新老两种写法因此都测得到
- **两支要分别反向验证**。第一次我只整块删掉，红是红了，但证明不了老写法
  那一支在起作用——`winner` 那一支的失败会先把测试打红，把它盖住
- 顺带把九条 spec 的 `push.summary` 补齐了，**出处是已经发出去的那份
  `copy.html` 的标题**，不是现编。`nishikori-shang` 的 7.28 那版有 30 个字位
  （发在标题闸之前），7.29 重发时已经收成短的——**判据要拿每个 slug 最新的
  那一份**，拿被顶替掉的当基准等于要求今天的代码去重现一个已经改掉的错。
  我一开始按 `head -1` 取，正好取到旧的那版，还自己另拟了一句，
  差点把一句现编的话钉成判据
- ⚠️ **补完 summary 之后，「原样重现」那条测试就测不到顺序了**：`headline`
  有 summary 就不看 matchup/score。两条测试各管一头，别以为绿了就都盖住了

#### ⚠️ 推送元数据写在 `_push` 里等于没写——而它整整对了一个月

上面那条「出处是 spec」我照着做了，**键写错了**：`push_meta` 读的是 `push`，
而我写成了 `_push`。仓库里 `_` 开头一律是写给下一个人的注解
（`_why` / `_source` / `_match`），`push_meta` 自己也明写着「`_` 开头的不是字段」。

**它一直没出声，因为退路刚好给出了对的答案。** 没有 `summary` 时 `headline`
退回「对阵 + 比分」，而那一句正是 `cover.versus.names` + `cover.result` 拼出来的
「热亚 6-3 6-4 沙波瓦洛夫」——看起来完全正常，复制页、微信标题、守卫测试全绿。

**是换成单人封面才把它抖出来的**：solo 没有 `versus` 也没有 `result`，退路一空，
标题当场变成 `8.2 赛场之上 | `。也就是说这个 bug 只有在**另一件不相干的事**
改变了退路的时候才现形。

- 「碰巧对」和「真的接上了」长得一模一样，**而且前者会一直绿**
- 加一个从 spec 读的字段时，**改一个值验一次**：改了 spec 标题就该跟着变，
  不变就说明根本没读到。查源码里有没有这个键防不住键名写错
- `push` 只认 matchup / score / event / summary / lead 五项，多写一项会报错
  （那道闸是对的）——但**整块写错名字它管不到**，因为它扫的是块里面

##### 现在有闸了，而它当场又抓出三条

`load_spec` 的 `_reject_underscored_fields`：**拿掉下划线正好是个真字段、
而值又是个对象**的键，一律报错。装上当天扫出 `eala-osaka` / `eala-svitolina` /
`wong-gea` 三条同样写着 `_push`——也就是说这个坑一共踩了四次，前三次一次都
没出声。后两条的 `lead`（微信正文第一行）因此一直是空的，而字段名还写成了
`push_lead`（`push_meta` 只认 `lead`），**两层都不生效**。

三个细节：

- **判据要窄。** `hewitt-washington` 第 3 段的 `_inset` 是**真的注解**，就贴在
  真的 `inset` 旁边写「为什么压左上角」。所以只认**值是 dict** 的那种：注解是
  写给人看的话（字符串），一个 `_` 开头、值却是个对象、拿掉下划线又正好是真
  字段的键，几乎一定是手滑。反向验过：放宽到「字符串也拦」，`_inset` 当场被误伤
- **那张「真字段表」不许靠人记着维护**。`test_真字段表要盖住每条spec里出现过的
  字段` 从 `specs/reels/*.json` 里实际出现过的字段名自动推——少一个名字，那道
  闸对这个字段就是哑的，而**一个会过期的名单和一条常年红的检查是同一个毛病**
- ⚠️ **测试要真走一遍 `load_spec`。** 第一版只直接调那个函数，反向验证时把
  `load_spec` 里的调用整个拆掉，**六条测试全绿**——又一次「写了不等于跑过」
- 已发的 `eala-osaka` **故意不补 `summary`**：写好的那句是「伊埃拉进决赛」，
  而发出去的标题是「伊埃拉 6-4 6-2 大坂直美」。补上等于让今天算出来的标题和
  当时发出去的那条对不上，而微信那条消息收不回来

#### 单人封面：赛场之上不是不许用，是**要留下判据**

账号所有者 2026-08-02（gea-shapovalov）：「封面就变成他热亚一个人捧杯的全景图吧。
这样可能更合适一点，而不是说两人对阵的那种头像图了。」

原来的闸是「赛场之上一律 VS 模板」。那条规矩防的是**滑坡**——单人海报一旦能渲，
下次哪条对决片子凑不齐两张照片就会「先用 solo 顶一下」。它防的不是编辑判断：
这一条两张官方抠图都在手上、VS 海报已经渲出来看过，换 solo 是因为
**这条片子讲的是一个人三年爬上来然后夺冠**（来路加逐轮占了 55% 的片长）。

所以闸改成「要用 solo 就写一句 `cover._layout_why`」，忘了写照样报错。
和 `mixed_fps` / `silent_source` / `rank: null` 一个形状：**认领这一步把
「想清楚了」和「凑合一下」分开**。

配套一条：**solo 不渲 `event_badge`**，所以「哪一站、哪一轮」会整条消失。
补进模板自己的 `topic` 行（台头底下那句），**别另拟一套**。原来的 `event_badge`
一并删掉——一个版式永远不读的键就是个不吭声的死键。

##### ⚠️ 2026-08-04：这条闸整个翻了个面，solo 成了默认

账号所有者：「**以后都用 solo 版做「赛场之上」封面**」「**中间标题下面写上比分
（带双方国旗）**」。

上面那段留着，因为**它的形状还在，只是主语换了**：

|  | 2026-08-04 之前 | 之后 |
|---|---|---|
| 默认 | VS（cutout / diagonal…） | **solo** |
| 要写 `_layout_why` 的 | 用 solo | **退回 VS** |
| 防的滑坡 | 缺图 → 拿 solo 顶一格 | 手头正好有两张抠图 → 顺手退回 VS |

老那个滑坡**不存在了**（solo 本来就是默认，没什么可省的）；新的那个是反方向的，
后果一样——栏目的封面变成两种样子。所以闸原样翻面，判据同样钉两头：
`test_栏目和封面模板要配对`（spec 那头）和 `test_赛场之上的封面一律用solo`
（代码那头，反向验证过写了判据就越过栏目那道闸）。

**「solo 不印赛果」那条规矩没被推翻，只是不管这个栏目了。** 它的理由（休伊特那条
六拍里比分是第 5 拍，印在封面上等于先说结局）对**网球有故事**照旧成立。所以判据
**按栏目分，不按 layout 分**：

| 栏目 | 封面印不印赛果 |
|---|---|
| 网球有故事 | **不印**。讲一个人，剧透完后面五拍就没人看了 |
| 赛场之上 | **印**。讲一场对决，赛果本来就是标题（VS 版式一直在印） |

赛果行渲成 `🇨🇳 张帅（57） 6-4 6-1 🇰🇿 普汀塞娃（81）`，国旗和排名从
`cover.matchup` 读，走的是和 VS 名条**同一个 `_name_html`**——一处出错两处一起红，
不会两边各写一遍再对不上。所以上面那句「国旗和排名那条规矩管不到 solo」**作废了**：
现在 solo 也印名字，规矩自然跟着管到。

⚠️ **赢家在前，从 `cover.winner` 挑，不按 `matchup` 的顺序排。** `matchup` 是版式
顺序——`wang-samsonova` 那次海报印「萨姆索诺娃 6-2 6-2 王欣瑜」而标题算成
「王欣瑜 vs 萨姆索诺娃」，比分夹在两个名字中间，等于**声称输的那个人赢了**。
判据里正反两个方向都验了（换掉 `winner`，顺序必须跟着换）。

⚠️ 两张豁免表，**都只许减不许加，而且都有自检**（表里每个 slug 必须真的存在、
真的还是老样子——写错一个名字，豁免就成了一盏恒真的绿灯）：
`_LEGACY_VS_COVERS`（还在用 VS 版式的十二条）、`_LEGACY_SOLO_NO_SCORE`
（已经是 solo、但发在「印比分」之前的四条）。已发的片子不为版式重渲——
微信那条消息发出去收不回来。

##### ⭐ solo 封面放**谁**：放刷到这条的人是冲谁来的那个，输赢不管

账号所有者 2026-08-15：「**封面换成大威，同时标题也以大威主视角写，比如付出后
已经连败多少次了**」。

来路：`arango-venus`（辛辛那提首轮，阿朗戈 6-2 6-2 大威廉姆斯）我第一版封面放的是
**赢的那个**（阿朗戈），钩子写她「八个发球局一个没丢」——每个字都对，四道闸门也全过，
**可没有人是冲阿朗戈刷进来的**。这条球的全部关注度在大威廉姆斯身上。

**判据是「谁把人拉进来」，不是「谁赢了」：**

| 这一场 | 封面放谁 |
|---|---|
| 一边是大名字（大威、德约、锦织圭、复出的前十），另一边不是 | **大名字**，哪怕她输了 |
| 两边名气相当 | 赢的那个 |
| 主体本身就是这条片子要讲的人（爆冷的新人、中国球员） | 那个人 |

`medvedev-zandschulp` / `tirante-fritz` / `baez-dimitrov` 早就是这个形状（都放的输家），
只是一直没写成规矩，于是我在 `arango-venus` 上又走了一遍「赢家在前」的惯性。

⚠️ **封面主体是谁，跟赛果行印什么是两件事**：`winner` / `result` / `matchup` 照旧，
海报下面那行永远是「赢家 比分 输家」。换的只有 `cover.subject` 和 `portrait`。

⚠️ **标题（钩子 + `push.summary` + 小红书首行）要跟着主体走。** 封面放她、标题却写
对手怎么赢的，等于把人骗进来看另一个人的故事。这一条改完是
`上次赢球是去年七月 / 这是第十四场连败`＋`大威廉姆斯第十四场连败`。

⚠️ **而「主视角」不等于「看笑话」。** 同一条源片里她打丢之后低头的两格（152.5／154.5）
情绪是「垮掉」，配上连败的钩子读起来就是在嘲笑；最后选的是 **240.5 秒她零比四落后
还在握拳**那一格（解说同一秒说的正是『Huge cheer as Venus Williams』）。
账号所有者说的是「**付出**后已经连败多少次了」——重音在付出。

#### 60 fps 源片的码率要按 ~~5700~~ **9230** 估，不是 4990

⚠️ **标题里那个 5700 已经过期两轮了，当前值是 `MEASURED_REEL_KBPS = 9230`**
（出处 `tools/build_match_reel.py`；顶上去的是 `cincinnati-story` 实测 9221 kb/s，
见下面「100 MiB 是 git 的限制」那节的码率表）。**下面这段的推理仍然成立，
只有那个数换了**——记着这一节一年内被顶高过三次（4990 → 5700 → 9230），
**下次引用它之前先 `grep MEASURED_REEL_KBPS tools/build_match_reel.py`，别抄这里。**

`gea-shapovalov` 实测：4 分 08 秒、**168.4 MiB**（3:4、crf 18、源片 59.94 fps）。
而 `MEASURED_REEL_KBPS = 4990` 是按 25 fps 那批量的，于是它估 147 MiB、
实际 168 MiB——**低估 14%**。判据在
`test_估体积的码率只许按实测往上调`（另加一层加餐：每条已发成片的实测码率都不许
超过这个常量）。

~~这次不影响判断（两个数都在 95 MiB 以上，都走 Release），但**它低估的方向是危险的
那一头**：贴着 95 MiB 的片子会被估成「进仓库」，然后在 `git push` 那一刻才炸。~~
⚠️ **这个危险已经不存在了**：2026-08-13 起成片一律走 Release，估错不再炸 push
（那条 95 MiB 的闸拆了，见下）。**但常量仍然只许往上调**——低估的估算比没有估算
更能骗人。估体积的常量要取**实测最高**的那一条，见下面「⚠️ 剪短之后码率反而更高」。

⚠️ **而 9230 这个数还欠一次重量**：上面那一串码率**全是 `preset slow` 时代量的**，
而 2026-08-14 起成片走 `medium`（见下面 preset 那节），同 crf 下 medium 比 slow
大 1.6~6.1%，所以这个常量对新片子**偏低 2~6%**。**故意不跟着往上调**：往上调要
按「实测」，而手上只有按样本推出来的比例，不是任何一条真成片量出来的数——这个
仓库为「没量过的推断写进注释，之后每个人都拿它当判据」栽过。**下一条 medium
成片渲出来，照上面那串的口径量一次再决定动不动它。**

⚠️ **别拿视频流的码率去填这个常量。** 我第一次写这一节时记的是 5500，那是
`ffprobe` 报的**视频流** 5492 kb/s；而这个常量估的是**整个文件**，要连音轨和封装
一起算——`168.4 MiB ÷ 247.9s` 是 **5698 kb/s**。差的两百 kb/s 正好又是往低里差，
和这一节要防的是同一个方向。

#### ⚠️ 测试不许拿 `output/` 当判据的**主语**——CI 上它不在

上面那条测试第一版读 `output/*/reel/*/copy.html` 取「已经发出去的那句标题」，
本地九条全绿，CI 上 `只校到 0 条已发的片子`（run 30707236335）。

`ci.yml` 的稀疏检出**从来就不含 `output/`**（一 GB 多的公开产物存档）。
所以在 CI 上，`Path("output").glob(...)` 恒为空——而**空列表和「没有成片」
长得一模一样**，又一次「空结果先自证是真空」。

- **核心判据只吃 `specs/` / `src/` / `tools/`**，这样它在 CI 上真的跑得起来
- **读产物的断言当加餐**：产物在的时候（本地沙箱）多验一层，不在就跳过那一段。
  但**整条测试不许 skip**——一条常年跳过的检查和常年红是同一个毛病
- 那句 `assert checked >= 9` 是**判据自己的判据**，它没白写：主语没了它当场
  出声，而不是变成一条恒真的绿灯。**要是没有它，这条测试会在 CI 上「通过」
  整整一年，一条 spec 都没校过**
- 需要「一个大文件」这类真实条件时，`os.truncate` 造一个稀疏文件就够，
  别去 `output/` 里找

#### 成片链接从来没有被校验过——而它是这条推送的全部

`pushplus.wait_for_images` 只校 `<img>` 和 **jsDelivr 域名**的 `<a>`
（`jsdelivr_link_sources`）。而成片超过 20 MB 就退回
`raw.githubusercontent.com`——量过，仓库里十条成片 **28~54 MB，十条全超**。
也就是说那条 jsDelivr 分支在真实数据上**一次都没走到过**，▶ 按钮一直裸奔。
又一次「兜底出事的时候不吭声」，而且这次兜底的范围本身就是错的。

- **复制页和成片的处置相反，别照抄**。复制页取不到只摘按钮、正文照发
  （`drop_dead_copy_button`）；**成片取不到必须整条不发**——文案没了还有正文，
  片子没了这条推送就没有内容了
- 探活用 `Range: bytes=0-1023`：真文件回 **206**，缺文件回 **404**，
  而只下 1 KB——不用为了探活把 52 MB 拉一遍
- 判据分四层（`test_成片链接发之前要自己探一次`）：超 20 MB 确实退 raw、
  这条 URL 确实不在 `wait_for_images` 的清单里、探不到就 `SystemExit`、
  **而且这道闸真的排在 `push()` 之前**。第四层是「写了不等于跑过」的老账，
  两种反向验证（拆掉调用、让 404 也放行）都验过

顺带一条给测试本身的：**扫工作流的判据要先去掉整行注释**。工作流的注释正是
这个仓库记教训的地方，正文里必然写着当年那些错值（`伊埃拉 vs 郑钦文`）和
不该装的东西（ffmpeg）——连注释一起扫，「把坑记下来」会被判成「又踩了这个坑」。
又一次「判据宁可窄，不可宽」。

**卡片存 JPEG，不是 PNG。** 账号所有者反馈「推送的图片和视频，国内访问下载很慢」。
量了一下：七屏 2160×2880 存成 PNG **合计 31 MB**——卡片是**照片**铺满的图，
PNG 对它是最坏的格式。换成 q86 的 JPEG 是 **3.7 MB（12%）**，同一块文字区域
放大逐像素比过，肉眼看不出差别。

- **2× 截图是为了字锐利，那是分辨率的事，和无损编码无关**——这两件事我一开始
  混在一起，以为不能动格式
- 不能再压的两条：**不缩尺寸**（字会糊），**q 不低于 82**（深绿底上的浅色小字
  开始出现块状噪点）。改 `_SLIDE_JPEG_QUALITY` 要重渲一屏文字最密的示意图，
  放大对比，别按比例推
- 顺带少往仓库里塞 27 MB/条——推分支吃 HTTP 413 那次就是被 mp4/jpg 的 pack
  体积撑爆的
- **查成片的三个工具要两个后缀都试**（`.jpg` 再 `.png`）：2026-07-29 之前发的
  都是 PNG，只认一个后缀会把旧成片误报成「还没落库」
- 成片本身没什么可压的：`libx264 -preset medium -crf 22`，2 分 56 秒的
  1080×1920 只有 7.7 MB（**365 kbps**），已经很省，再压画质就掉了

### 同一条源片会被下两到三次——一半的 render 时间花在重下上

`probe` 下一次、`cover` 下一次、`render` 再下一次：中间「丢掉不进仓库的中间物」
把它删了，而它本来就不进仓库。量出来的账（run 138，eala-zheng）：

| | |
|---|---|
| 整趟 | 4 分 32 秒 |
| checkout | **4 秒**（稀疏检出之前是 2 分 44 秒） |
| **render** | **3 分 18 秒（73%）** |
| 其中「下载源片」 | **100.86 秒**——一个 **70 MB** 的文件 |

**不是带宽问题**，是 YouTube 限速（0.7 MB/s）。所以缓存能拿走的是实打实的
一百秒 × 每条片子两到三次。

- 缓存收在 **`download()` 里**——三个调用点全走它，改一处就够
- **键是 URL 的哈希**：同一条 YouTube URL 内容不会变，缓存永远是对的；
  换了 URL 自然是另一个键。**别按 slug 或按「有没有缓存」判**，那样改了源片
  地址还会命中旧文件，而这种错**不报错**
- 目录在 `~/.cache/tennislive-sources`，清理那一步只碰 `outdir`，碰不到它
- **存不进去不算失败**（它只是个加速），但**要出声**——否则「缓存没生效」
  和「生效了」在日志上一样
- `push` / `narration` 两条路不碰源片，不给它们恢复缓存

判据真走一遍 `download()`（`test_同一条源片不许下第二次`）：没配缓存要下两次、
配了第二次一个字节都不下、换 URL 不许命中、`~` 要展开、写不进去不许抛。
反向验证过（拆掉命中那一支、把键改成固定名、拿掉 `expanduser()`）。

**真 run 上量出来的**（同一条片子跑两趟 `mode=cover`）：

| | 封面那一步 | 整趟 |
|---|---|---|
| 未命中（run 156） | **1 分 45 秒** | 3 分 16 秒 |
| 命中（run 160） | **6 秒** | **1 分 56 秒** |

日志里的判据是那行 `Cache hit occurred on the primary key reel-src-<slug>-…`。

#### ⚠️ `~` 不展开：两趟绿的 run，一个字节都没缓存

工作流里写的是 `TENNISLIVE_SOURCE_CACHE: ~/.cache/tennislive-sources`，而
**Python 不展开波浪号**——`Path("~/…")` 是一个**名叫 `~` 的相对目录**，落在仓库
工作区里。`actions/cache` 找的是真的 `$HOME/.cache/…`，于是每趟都报

    Path Validation Error: Path(s) specified in the action for caching
    do(es) not exist, hence no cache is being saved.

**而两趟 run 全是绿的**（156/158），封面那步只快了 14 秒——那 14 秒是别的缓存
（Chromium、pip）带来的，跟源片一点关系没有。要不是去查「为什么只快 14 秒」，
这条缓存会一直挂在那儿假装生效。

- **单元测试当时也是绿的**：它喂的是绝对路径 `tmp_path`，正好绕过这一半。
  凡是「路径从配置来」的判据，**必须喂一次 `~/…`**，那才是真实配置的样子
- 判据现在钉两头：工作区里不许出现名叫 `~` 的目录、展开之后要落在真的
  `$HOME` 底下。反向验证过（拿掉 `expanduser()` 当场红）
- 同一族的还有 `$HOME`、`$RUNNER_TEMP` 这些——**YAML 的 `env:` 值不过 shell**，
  写进去是什么就是什么

#### ⚠️ 我写的这条注释当场打红了六条测试

上面那段注释里引用了「丢掉不进仓库的中间物」这个步骤名，而六条测试是按
**裸的步骤名**切工作流的（`text.index("丢掉不进仓库的中间物")`）——于是它们切到了
**我的注释**，抽不出任何 `rm` 模式。

这正是本文件里反复记的那条「判据扫得太宽，被自己的注释误伤」，只是这次
**被误伤的是既有测试，触发的是新写的注释**。工作流的注释就是这个仓库记教训的
地方，引用别的步骤名是常事——所以取步骤块必须：

1. **先 `_yaml_only` 去掉整行注释**
2. **锚在步骤头 `      - name: X` 上**，不是裸名
3. **按整行头 `\n      - name:` 切**——从 `- name:` 开始切再按 `- name:` 分割
   会在第 0 位截断，拿到一个空串（我第一次就是这么修的，红得更多）

现在统一收在 `_step_block()`，九处共用。反向验证：把它退回「不去注释 + 裸名」，
六条当场红。

### checkout 拉 1.36 GB 是全仓库的问题，不是这一条线的

`output/` 在 HEAD 上就有 1.36 GB（.git 1.77 GiB），于是**每一条工作流的
checkout 都要一分半上下**——量过的两个：daily `1:31`（整条 run 6:34，真正
生成内容只用了 1:40），match-reel `2:44`。

按「这条工作流碰不碰 `output/`」分两类，判据自动分，不维护白名单
（`test_不碰产物的工作流不许把output拉下来`）：

- **提都不提的**（assets / verify-names / probe-blocked / player-name-sync /
  probe / oncourt-interviews）：直接稀疏检出排除 `output`，不用 add 任何东西
- **要写产物的**：稀疏之后必须把自己那一格 `git sparse-checkout add` 回来。
  **半个是最坏的情况**——`git add` 会说路径在稀疏范围之外，而那是在跑完之后
  才炸。所以测试要求「要么两半都有，要么老老实实全量」

顺带挖出一条更贵的：**`player-name-sync` 写着 `fetch-depth: 0`**，也就是把
1.77 GiB 的完整历史（每条成片的每个版本）全拉下来——而它唯一的 git 读操作是
`git diff --quiet -- <两个被跟踪的文件>`，那是**工作区对 HEAD** 的比较，
一条历史都不需要。提交和推送在浅克隆上照样能做。

写这条测试当场踩了三次「判据宁可窄，不可宽」，每次都是**扫得太宽反而判错**：

1. 连注释一起扫——注释里正写着「别把 output/ 拉下来」，于是被判成「写产物」
2. 按整份文本扫——`ci.yml` 的 `paths-ignore: output/**` 是**触发条件**，
   不是产物路径。只看 `jobs:` 以后
3. 裸的 `"output/" in body`——`probe.yml` 写的是 **`probe-output/`**，
   从中间匹配上了。要词边界 `(?<![\w-])output/`

**还没做的**（每条都要把自己的 outdir wire 进去，而且没法在沙箱里验完，
建议一条一条上、每条看一轮真实 run）：daily / explainer / flash /
knowledge-adhoc / news-radar / push-existing / video-localize / voice-sample /
yesterday-point。daily 那条最值——它一天跑四趟还挂在 push 上。

### 定时的频次要从产出倒推，别从「越勤越好」倒推

2026-07-31 按实际产出盘了一遍七条定时任务，动了三条：

| 线 | 原来 | 量出来的产出 | 现在 |
|---|---|---|---|
| 内容雷达 | **72 次/天** | 每天 1 个包（就是日限） | **6 次/天** |
| 场边采访 | 2 次/天 | 4551 条数据，**没有任何代码在读** | 停定时，留手动 |
| 译名同步 | 每周 | 六月至今译名表只变过 1 次，还是人手改的 | 停定时，留手动 |
| 昨日一分 | 6 次/天 | **11 天 66 趟只出 3 条片** | 没动，见下 |
| 场外快讯 / 选题候选 | 2 次/天 | 当天还在产 | 留 |
| 数据源健康 | 4 次/天 | **告警线，「没动静」正是它正常** | 留 |

**内容雷达的间隔是算出来的，不是拍的**：`preview_candidates` 当时收赛前
45–210 分钟的对阵，窗口宽 165 分钟。要一场不漏得 ≤165 分钟间隔（≈10 次/天）；
选了 4 小时（6 次/天），中间留 75 分钟的缝——**因为日限本来就是 1 个**，
漏几场不影响「今天挑出一条」。

⚠️ **上面这段整个过期了，两个前提都没了**（留着是因为「频次从产出倒推」
那个方法还成立，那几个数不成立了）：

| 当年 | 现在 |
|---|---|
| 窗口 45–210 分钟（宽 165 分钟） | **45 分钟 ~ 48 小时**，见「排期出来就决定」那条 |
| 日限 1 条 | **10 条**，见「不要限制只有一条」 |
| 定时 6 次/天 | **归零**，只留手动 |

**窗口宽到 48 小时之后，「间隔要比窗口窄」这个约束基本不再咬人**——一天跑
一趟也不会漏掉明天的签表。真要恢复定时，按新窗口重算，别照抄当年那行 cron，
**也别照抄上面那个 165**。

**判频次要看它是产出线还是告警线。** 数据源健康 4 次/天从来「没动静」，
按产出量判它会被误杀——告警线的正常状态就是安静。

#### 2026-08-02：内容雷达和场外/选题雷达的定时都停了

账号所有者：「这两个任务停掉吧，不需要了」。**停的是定时，不是代码**——
两条工作流的 `workflow_dispatch` 都留着，选题、渲卡、推送整套照旧，
随时能手动跑一趟。

判据从 `test_内容雷达的间隔要按发布窗口算` 换成
`test_停掉的定时不许自己回来`（cron 不许回来 + 手动入口不许一起摘掉）。
⚠️ **换判据不是可选项**：cron 一删，老那条的 `re.search(...).group(1)`
当场 `AttributeError`——**主语没了就得换判据**，留着它就是一条常年红。

全仓库现在只剩两条定时，**合计 5 次/天**（量出来的，不是数记忆里的）：

| 线 | 频次 | 为什么留 |
|---|---|---|
| 每日网球知识 `knowledge-adhoc` | 1 次/天（08:37） | 还在产的栏目 |
| 数据源健康 `source-health` | 4 次/天 | 告警线，安静才是正常 |

### 昨日一分整条拿掉（2026-07-31）——先查根因再决定

账号所有者：「昨日一分全功能拿掉」。**停之前先查了根因，不是频次问题。**
skip 诊断（`output/<date>/yesterday-point/*/skip.json`）写得很清楚：

    Tennis TV Hot Shots   empty   抓到 60 命中 0（有一天命中 2，但都是 freemium，缺 TENNISTV_JWT）
    WTA / ATP / Tennis TV 官方 YouTube    error
    澳网 / 法网 / 温网 官方频道              error
    WTA 官网 / 美网官方                    empty   抓到 4 / 15，命中 0

**十个检索源里五个直接 error，全是 YouTube 系**——和 match-reel 那条
「YouTube 对机房 IP 一律 Sign in to confirm you're not a bot」是同一个病，
而 match-reel 早有解法（bgutil PO token provider + `YT_COOKIES_TXT`），
这条线一样都没接。结果是 **11 天 66 趟只出 3 条片**。

出路本来有三条（配 `TENNISTV_JWT` → 搬 match-reel 那套 → 停掉），
账号所有者选了停。**记这一段是因为：如果只看「6 次/天产出很低」就去降频，
方向是反的**——那 6 趟是重试班次，降频只会把命中率再削一刀。
**低产出要先分清是「跑得不够」还是「源就不通」。**

删掉的：工作流、`tennislive point`、`video/daily_point.py`（1937 行）、
`tests/test_daily_point.py`（57 条）、`test_cli` 里 3 条、历史产物 45 个文件
（含 3 条已推送过的成片——老消息里的链接会变死链，这是停产的代价）。

**没删的**：`video/pipeline.py` 的 `render_ass` 和 `video/subtitle_text.py`
——视频本地化还在用。又一次「停的是栏目，不是底下那套工具」。

⚠️ **一条测试的自检替我抓到了连带损伤**：`test_栏目名不能只活在代码里`
扫的是 `_COLUMN_LABEL` 常量，而它只存在于昨日一分那条线，删完扫描结果为空——
它自己那句 `assert labels, "判据失效了"` 当场报错。**这就是「判据自己也要有
判据」的价值**：主语没了它出声，而不是变成一条恒真的绿灯。现在改扫竖版短片
每条 spec 的 `cover.eyebrow`（还活着的栏目名）。

### `tennislive digest` 删了；覆盖率报告抽成自己的命令

`probe.yml` 原来跑 `tennislive digest --no-cards`，**只为拿一张 coverage.txt**
——为了一张覆盖率报告跑一整套日报。日报停产之后：

- `cmd_digest`（382 行）删掉
- 新增 `tennislive coverage`：抓一天数据、写一张 `coverage.txt`，就这些
- `probe.yml` 改用它

⚠️ **卡片渲染器删不掉，别再去试。** `render/cards.py`、`render/webcards.py`
被知识帖、解说片、内容雷达一起用着（`content_package` / `knowledge` /
`flashcard` / `video.explainer` 都 import 它们）。日报是它们的一个调用方，
不是它们的主人。

### 删历史产物也要按栏目分——我差点报错一个数量级

账号所有者说「删掉吧」时，我给的选项写着「仓库里 **1.33 GB** 历史日报产物」。
**错得离谱**。真按栏目拆开：

| 停产、删掉 | | 还在用、不能删 | |
|---|---|---|---|
| 日报本体（含 cards / video） | 134.8 MB | 解说片 | 576 MB |
| knowledge（日报带的知识帖） | 8.5 MB | 成片 | 440 MB |
| schedule（今日赛程包） | 3.4 MB | grand-slam ×2 | 132 MB |
| flash（即时战报） | 1.7 MB | 昨日一分 / 内容雷达 queue / 场外快讯 | 56 MB |
| **小计** | **148.4 MB** | | **1.18 GB** |

**为什么活栏目一个都不能删**：解说片和成片的链接走 jsDelivr / raw 指向仓库里
这些文件，而那些链接**已经发在微信消息里了**——删文件就是把老消息变成死链，
而消息发出去收不回来。

两条配套：

- **删文件不会让 `.git` 变小**。1.77 GiB 的历史 blob 还在，checkout 慢那个
  问题早就用稀疏检出解决了。所以这次删除的收益是「仓库干净」，不是性能。
  ⚠️ **2026-08-13 补了另一半**：真要让 `.git` 变小只能**改写历史**，
  见下面「2026-08-13 的历史清理」那节
- **判据钉「活栏目还在」，不是「output 变小了」**。粒度是整条线：反向验证过，
  删掉 reel 的一个 mp4 不红，把 reel 整个目录拿掉才红——它拦的是「一次清理
  把整条线扫了」

**同一天第五次「判据扫得太宽」**：第一版用 `"/cards/" in path` 找日报卡片，
把 `output/<date>/queue/<pick>/cards/`（内容雷达自己的卡片，活的）一起扫了。
改成锚在日期目录正下方 `^output/\d{4}-\d\d-\d\d/cards/`。

### ⭐ 2026-08-13 的历史清理：`output/` 下的 mp4 和图片从**全史**抹掉

账号所有者：「当前代码库太大了，好多资源是不是不用放在这里代码库里」。
量出来 `.git` 已 **6.0 GB**，其中 **4.93 GB 是 mp4 blob（130 个）**。

上一节那条「删文件不会让 `.git` 变小」说的就是为什么删不动——历史里的 blob
一个都不会因为 HEAD 上没有了就消失。所以这次是**改写历史**：

|  | 之前 | 之后 |
|---|---|---|
| `.git` | **17 GB** | **789 MB** |
| `output/` 下 tracked 的 mp4 | 130 个 | **0** |

**老历史钉在 `refs/keep/pre-purge-20260813`**（remote 上就有，
`git ls-remote origin` 里 grep 一下 `keep` 就看得到）。要翻改写之前的任何东西
都从那儿取，**别去 `main` 的历史里找**——那里已经没有了。

⚠️ 三件配套的事，别踩：

- **这是一次改写历史，所有 commit SHA 都变了。** 引用旧 SHA 的东西（钉在
  `@<sha>` 上的 jsDelivr 图片链接、PR 正文里的 commit 链接）在改写之后指向的是
  **不存在的对象**。已发微信消息里的图片链接因此可能失效——**这是这次清理的
  代价，认领过的**，不是可以顺手修的 bug
- **它治的是存量，防复发的是另一件事**：成片从 2026-08-13 起一律走 Release
  不进 git（见「100 MiB 是 git 的限制」那节）。**只做清理不改增量的话，
  几个月后会一模一样地再胖回去**——两件事要一起看
- **别拿 `.git` 的大小当「清理生效了」的判据。** 本地克隆的 `.git` 取决于你
  什么时候克隆的、`gc` 跑没跑过，**同一个仓库两台机器可以差一个数量级**
  （写这段时这台沙箱是 470 MB，既不是 17 GB 也不是 789 MB）。要判就查产物：
  `git ls-files output/ | grep '\.mp4$'` 是不是 0 条

### 「是不是这一版」那道闸恒真了一个月——因为判据喂的是假产物

2026-08-01 抽验发现：`copy_page_fingerprint` 取的是 `<h1>`，而
`to_copy_page` 的模板里 `<h1>` 写死是 **「贴图发布文案」**——一个常量。

    文案标题「7.31 赛场之上 | 黄泽林首进ATP四强」→ 指纹「贴图发布文案」
    文案标题「7.29 今日赛程 | 完全不同的一版」  → 指纹「贴图发布文案」

于是 `drop_dead_copy_button(expect=…)` 里那句 `expect in response.text`
对**任何一天的复制页**都成立。**这道闸从上线那天起没拦过任何东西**——
而它正是 2026-07-29 翻车之后专门加的那道（「可达 ≠ 是这一版」）。
**规矩写对了，实现是空的。**

指纹改取 `<textarea id="title">`——那才是当期标题所在的那一格。

**根因在测试，不在函数。** 那条守卫测试当时是绿的，因为它喂的是**自己手搓的
假页面**：

    page = tmp_copy_page("<h1>7.29 今日赛程 | 王欣瑜战萨姆索诺娃</h1>")
    assert copy_page_fingerprint(page) == fresh

它证明的是「函数能从 h1 里抠字」，而不是「**真页面的 h1 是当期标题**」。
现在两边都用真的 `to_copy_page()` 渲，并且断言：

- 两版内容不同 → 指纹必须不同（拦「放行恒真」）
- 指纹必须真的出现在渲出来的页面里（拦「拦截恒真」——那是另一头的坏）

⚠️ **「断言全绿不等于页面对」的第 N 次，这次的根子是判据喂了假产物。**
凡是验「产物长什么样」的测试，**输入必须是真渲染器的输出**，不能手搓。
手搓的 fixture 只能验函数的局部行为，验不了它和真模板对不对得上。

### ⭐ ESPN 也会倒——`fetch_day` 只挂了两个源，两个同时被拉黑就彻底没数据

2026-08-07：`source-health` 连续多次全红，`espn.py` 的模块 docstring 里
明明白白写着"对数据中心 IP 友好（已实测）"，这句话从这天起不成立了——
ESPN 和 SofaScore**同时**对 GitHub Actions 的机房 IP 返回 403，从
08-04 19:59 UTC 起连续 100% 失败了 3 天，`sources/__init__.py` 的
`make_source_chain()` 当时只挂着这两个源，两个一起倒，`fetch_day` 直接
抛 `SourceError`，日报、内容雷达手动触发时会撞上同一堵墙。

三层教训，都是当场量出来的，别再重新踩一遍：

- **ATP 那半边现在还是死路**，不用再去试。`atptour.com` 全站 403（含
  GitHub Actions 出口，`probe_blocked_sources.py` 早验过）、
  `api.protennislive.com` 要 token（这次又实测了一次，401）、Hawkeye 在
  Cloudflare 人机校验后面——`docs/match-momentum-sources.md` 反复确认过
  没有免费可达的候选。**两个主源同时倒时，ATP 覆盖没有救**
- **WTA 那半边接得上，但接口是抓包挖出来的，不是查文档拼的。** 直接猜
  `?from=&to=` 这类参数会被服务端**整个忽略**，静默回退成从 1960 年开始
  的全量列表（18738 条，看着像"查到了"其实什么都没过滤）。真正的做法是
  `tools/probe_tnns.py --url https://www.wtatennis.com/scores`（这个脚本
  本来是给 TNNS 写的，通用的 Chromium 抓包，指哪打哪）——一次就抓到了
  WTA 官网自己在用的两个接口，细节和三个真会咬人的坑（`Winner` 字段不是
  1/2 而是 2/3/4/7 这种没有文档的枚举、`RoundID` 数字段在 96 签联合赛事
  上和公开轮次名对不上、`MatchState=W` 的不战而胜连 `ResultString` 都是
  空的）全记在 `src/tennislive/sources/wta_live.py` 的模块 docstring 和
  各函数注释里，别在这儿抄第二遍
- **接口能通不等于能猜着用。** `Winner` 字段第一版我按"1/2"猜过，
  拿真实数据反查（`ResultString` 里"` d `前面是谁" vs 数字码）才发现是
  2/3/4/7——猜的那版会把"对手退赛"那几场的赢家全部判反。**没有文档的
  枚举码，宁可从别的字段（这里是赛果文字）交叉验证，也别信数字表面顺序**

现在 `make_source_chain()` 是 `[EspnSource(), SofaScoreSource(),
WtaLiveSource()]`，第三个只在前两个都失败时才用得上（`fetch_day` 的
聚合逻辑本来就是"用到成功为止"）。`source-health` 一个字都不用改——它
只是原样打印 `data.source_status`，新源自动出现在报告里。

### ⭐ 上面那条"ATP 没有免费可达的候选"是查早了——flashscore 一次请求管两个巡回赛

同一轮排查里，账号所有者一句"还有好多网球数据的网站啊"把我推回去继续找。
上面那条结论查的是 ATP **官方**三条路（atptour.com、protennislive、
Hawkeye），没查第三方聚合站——而仓库里`tools/match_feed.py`早就在用
flashscore 查单场逐分/统计，只是没人往上翻过它的"今天全部比赛"接口：

    GET https://global.flashscore.ninja/2/x/feed/f_2_{offset}_0_en_1
    Header: x-fsign: SW9D1eZo, Referer: https://www.flashscore.com/

`offset` 是相对"今天"的天数偏移（实测：-1=昨天 349 场、0=今天 237 场、
1=明天 44 场），**一次请求拿到 ATP+WTA+Challenger+ITF 全档次、64 个赛事
分组**，纯 HTTP 不用浏览器，GitHub Actions 真实出口上 200、无 Cloudflare
挑战——比 ESPN/SofaScore 都稳，而且同时补上了 ATP 那半边。

三个字段踩过的坑，别在这基础上接着猜：

- **`CA`/`CB`（以为是世界排名）不是排名**。同一天的数据里 `200` 出现
  59 次、`8` 出现 40 次——世界不可能有 59 个人并列第 200，统计分布直接
  戳穿了这个假设。`Player.rank` 这条线上一律留空
- **`AC`/`CR`（一度怀疑是轮次）不是轮次**。拿带真实轮次名（`ER÷Final`/
  `ER÷Semi-finals`，来自单赛事 `results/` 页）的数据对照，决赛和半决赛
  的 `AC`/`CR` **都是 `3`**——一个值对应两个轮次，证明无关。这条线上
  round_name 一律留空，和 WTA 那次 RoundID 的处理是同一个理由
- **退赛/不战而胜没有专门字段**，`WL` 那个疑似的位置在几百场样本里全是
  空的。改用网球规则本身反推：**一盘局数都没有**＝不战而胜，
  **最后一盘打完却不满足"6 局净胜 2"或"7-6/6-7"**＝退赛。这是判据
  本身给的规则，不是又猜一个不透明的编码

顺带一条反过来的教训：**"排除了官方三条路"不等于"排除了这条数据"**——
这次和"排除了 A 和 B 不等于就是 C"是同一个形状，只是这次排除的范围本身
（"ATP 官方"）从一开始就没扣紧"ATP 覆盖"这件事——第三方聚合站从来没被
排除过，只是没人往那个方向查。

同一轮还顺手查了 `tnnslive.com`：仓库里原来的"够不着"结论是用 Playwright
的 `context.request`（页面加载完之后另发一次请求）测的，而**用真实浏览器
正常打开页面、被动抓它自己发出的请求**，Cloudflare Turnstile 直接过了，
拿到的 JSON 比 flashscore 更丰富（连夺冠集锦的 YouTube 链接都嵌在里面）。
**当时没有接入**：这条路必须真跑一个 Chromium 才能过挑战，不能像
flashscore/WTA 那样纯 HTTP 直连——要接的话得在每条调 `fetch_day()` 的
工作流里都装 Playwright+Chromium，成本比拿到的增量数据大得多，先记录
做法，不接入。⚠️ **这条结论后来翻了面**，见下面「TNNS 接进来了」那节——
账号所有者要求它必须接，只是接的方式要绕开"每次都要装 Chromium"这个
成本，不是简单粗暴地塞进常规链。

现在 `make_source_chain()` 是 `[EspnSource(), SofaScoreSource(),
FlashscoreSource(), WtaLiveSource()]`——ESPN/SofaScore 是主源，
`FlashscoreSource()` 是 ATP+WTA 都覆盖的备用，`WtaLiveSource()` 是再往后
只补 WTA 的最后一道。这四个都失效才会去试 `make_fallback_chain()`
（`TnnsSource`），见下一节。

### ⭐ TNNS 接进来了——但是单独一档，不是塞进常规链

账号所有者 2026-08-07：「我觉得要把 tnns 做备选，不能把鸡蛋都放在一个
篮子，还是有必要的，如果前面都失败」。上面那条"成本比收益大，不接入"
的结论**没有错，但漏了一种接法**：成本大是因为把它当成跟另外四个源
平权的一员，让"每一次 `fetch_day()`"都背上开 Chromium 的三十秒；而
账号所有者要的场景只有"前面都失败"这一种，不是"每次都查一遍"。

所以现在是两层：`make_source_chain()`（常规四个源，纯 HTTP，全部尝试）
全灭之后，`fetch_day()` 才会去试 `make_fallback_chain()`（目前只有
`TnnsSource`）。绝大多数时候这一层根本不会被调用，成本只在真正的全灭
场景才会付出。实现在 `src/tennislive/sources/tnns.py`，字段怎么验证的
全写在那个模块的 docstring 里，这里只记**探测过程中的方法论**，别重复
抄一遍字段表。

#### `probe_tnns.py` 迭代出的三个通用发现，比 TNNS 本身更值钱

- **`_dump_objects` 拿 `json.dumps(node, ...)` 默认分隔符去匹配压缩
  JSON，永远匹配不上。** 真实接口是压缩格式（`"p":[`，冒号后没有空格），
  `json.dumps` 不传 `separators` 会在冒号后加一个空格（`"p": [`）——
  拿它去找 `"p":[` 报「0 个」，而「命中附近」那段明明打印着它存在。
  改成 `separators=(",", ":")` 才对得上。**查的东西和跑的东西不是
  同一份文本**，又是这个仓库反复栽过的那个形状，只是这次错的是"匹配
  用的副本"，不是产物本身
- **工作流把 `${{ inputs.x }}` 直接拼进 run 脚本正文，值里带引号就会
  和模板的引号相撞。** 想探 `"p":[`，GitHub Actions 在 bash 解析之前
  就把表达式换成裸文本，拼出来的脚本源码里两层引号打架，`eval` 照样
  把错的那串跑出去，**不报语法错误**。实测：真正传给 `--dump` 的值
  变成了 `p:[`（少一个引号），一次探测全空跑，和"这个词不存在"一模
  一样。改法是走 `env:` + bash 数组接参数，让 bash 自己的引号解析
  接管值的内容，不再让 GitHub Actions 的文本替换和 bash 的语法解析
  互相踩踏
- **顶层键没数清楚，就已经在树的深处找答案了。** 搜 `season_id` 时，
  因为这个值到处都是（每场比赛都有），minimal-node 逻辑每次都停在
  某一场比赛身上，永远走不到"有没有另一块专门存赛事名"这个问题。
  加了 `--top-keys` 先把根对象的 6 个键点清楚（`sids`/`all_matches`/
  `refresh_time`/`tours_following`/`matches_following`/`generated_at`），
  才看清楚"没有一块是赛事名查找表"这件事——**先看形状，再决定往哪挖**

#### `sid` 是巡回赛，不是常量——靠交叉女子球员姓名验出来的

第一版探测只碰到 ATP 蒙特利尔的比赛（Jodar/Musetti/Nakashima/Droguet/
Rinderknech/Tiafoe，全部男子），`sid` 全是 `1`，一度以为它是个跟巡回赛
无关的常量（比如"运动项目=网球"）。**如果真造了 `Match.tour` 却猜错了
这个字段，整条 TnnsSource 出来的数据会把 WTA 的比赛全部打成 ATP**——
`Match.tour` 是必填字段，猜错不会报错，只会安静地把性别判反。

补一轮专门搜女子球员名字（Sabalenka/Svitolina/Zhang/Potapova）的探测，
拿到的比赛全是 `sid=2`，跟男子比赛的 `sid=1` 泾渭分明——这才敢把
`sid → Tour` 的映射写进代码。**跟"排除了 A 和 B 不等于就是 C"同一个
教训**：只看到一种性别的样本时，"这个字段恒为某个值"和"这个字段就是
那个巡回赛的常量"长得一模一样，只有交叉另一种性别的真实数据才能证伪。

### 查询留着，卡片图和推送不留

账号所有者：「**可以用命令查赛果，但没必要做卡片图然后推送微信了**」。

边界划在**产物**上，不是划在功能上：

| 命令 | 干什么 | 处置 |
|---|---|---|
| `today` / `schedule` / `results` / `live` | 终端里列出来给人看 | ✅ 留 |
| `flash`（即时战报） | 检测热点 → 渲赛果卡 → 推微信 | ❌ 删 |
| `schedule-cards`（今日赛程卡） | 渲赛程卡 → 写 push.html → 推微信 | ❌ 删 |
| `publish flash` | 发已提交的热点包 | ❌ 删别名（函数留着） |
| `render.cards.generate_flash_card` | 赛果卡渲染器，删完零调用方 | ❌ 删 |

⚠️ **`flash-card` 不是赛果卡，差一点删错。** 它渲的是**场外快讯卡**
（`render/flashcard.py`，带敏感话题闸门），配的是 `flash-radar` 那条线——
那条线在「其他的可以保留」里。**名字里都有 flash，一个是即时战报（赛果），
一个是场外快讯（新闻）**，按名字扫必删错。是翻测试名
（`test_flash_card_cli_blocks_sensitive_topic_without_rendering`、
`test_flash_radar_queues_only_offcourt_nonsensitive_news`）才认出来的。

同样的形状还有一个：`cmd_publish_flash` 这个**函数**不能删——
`publish content`（内容雷达的赛前焦点）走的是同一个函数，删掉的只是
`flash` 那个 channel 别名。**一个函数挂两个入口时，删入口别删函数。**

判据在 `test_查赛果的命令留着卡片和推送不留`，两个方向都反验过：
把 `cmd_flash` 加回来会红，把场外快讯卡误删也会红。

### 停一条线要**按产品分**，不是按工作流分

账号所有者纠正了我两次，第二次一句话把边界划清楚了：

> 「**所以要分开啊**。昨日赛果和今日赛程都要拿掉了，**其他的可以保留**。
> 还有**即时赛果推送**也不要了」

我的错在于**按工作流做减法**：`daily.yml` 里塞着三样东西（昨日赛果、今日赛程、
每日网球知识），我把整个文件删掉，等于顺手把第三样也办了。**工作流是实现，
栏目才是产品**——一个文件里能装几个栏目，删文件不等于删栏目。

最后的账（都落了测试）：

| 栏目 | 在哪 | 处置 |
|---|---|---|
| 昨日赛果 | `daily.yml` | ❌ 随文件删除 |
| 今日赛程 | `cmd_schedule` → `push-existing` 的 `schedule` scope | ❌ **删发布出口，留生成器** |
| 即时赛果推送 | 内容雷达的 `result` 那一半 | ❌ `RESULT_DAILY_LIMIT = 0` |
| 赛前焦点 | 内容雷达的 `preview` 那一半 | ✅ 留着——它不是赛果 |
| 每日网球知识 | 原来搭 `daily` 顺风车 | ✅ **分到 `knowledge-adhoc.yml`**，加一条 08:37 的定时 |

三个具体的手法值得记：

- **停「即时赛果」只用改一个常量**。`select_content` 按名额取，
  `RESULT_DAILY_LIMIT` 归零就一条都取不进去，工作流一个字不用动，
  `preview` 那一半照常。**能用一个数关掉的，别去删代码**
- **今日赛程删的是发布出口，不是生成器**。跨日窗口、`NEXT_DAY_CUTOFF_HOUR`、
  按组轮流收口——那些教训写在本文件里，**代码就是它们的判据**。停的是发，
  不是把知识铲掉
- **知识帖分出来几乎没有代码量**：`knowledge-adhoc.yml` 跑的本来就是同一个
  `generate_knowledge_package`，而且本来就支持「slug 留空＝自动选题」。
  缺的只有一条 cron。⚠️ 定时落在 **08:37**，避开 :00/:30——GitHub 在整点半点
  最容易延迟或丢弃，这是 daily 当年留下的经验

### ⚠️ 反向验证救回一条恒真的断言

给「即时赛果停掉」写测试时，第一版喂了一场 `conftest.make_match` 的默认比赛
进去，断言「选不出 result」——**绿的**。反向验证时把开关拧回 1 重新跑，
它**照样选不出来**：默认那场球 `hotspot_score` 只有 **49**，而门槛是 **50**，
差一分。也就是说那句断言恒真，**开关开着它也绿**。

补上 `rank`（59 分）之后才真正有意义：开关=1 选出 `('result', 'm1')`，
开关=0 什么都没有。

**判据自己也要有判据**：断言「某个东西不出现」之前，先证明它在正常情况下
会出现。这条测试现在多了一句 `assert hotspot_score(...) >= HOTSPOT_THRESHOLD`
把这个前提钉住。又一次「断言全绿不等于页面对」。

### 日报停产了（2026-07-31）：`daily.yml` 整个删掉

账号所有者的原话，两次：

> 「以后 daily 就停掉了，之前的图片形式没有意义了」
> 「不要日报了，**都说过了**，日报的形式太落后了，**任务重且没收益没人愿意深入看**」

**第二句里的「都说过了」是给我的。** 第一次我只摘了触发器、留着工作流，
还回头问了一句「知识帖要不要留」——而对方那句话已经把整个产品否掉了。
**对方重申过的事就是决定，别再拿它去换一次确认**；保守到只做一半，
和没做完是一回事。

删掉的：

- `.github/workflows/daily.yml` 整个文件（连同赛果日报和搭它顺风车的每日知识帖）
- **三条只为它存在的守卫测试**——纪念日告警、封面闸门顺序、失败不吞。
  主语没了，留着就是常年红
- `test_workflow_alerts.py` 里那条「视频开关不许硬编码成 off」
- `push-existing.yml` 的 `main` / `knowledge` 两个 scope、盯
  `output/**/knowledge/**` 的 push 触发、以及 job 上那个只为它存在的 `if`

**没删的（故意）**：`tennislive digest` 命令还在——`probe.yml` 拿它做数据源
覆盖率探测（`--no-cards`）；卡片渲染器还在——解说片、赛程包、知识帖共用。
**停的是这个栏目，不是底下那套工具。**

判据在 `test_日报这条线不许回来`：文件不许回来，而且不许有第二条工作流去出
带卡片图的日报包。要恢复得连测试一起改。

**顺带挖出一个会过期的白名单。** `test_every_workflow_that_commits_generated_files_has_a_size_gate`
手写着十一个工作流文件名，daily 一删它当场 `FileNotFoundError`——**一个会
过期的名单和一条常年红的检查是同一个毛病**。改成「凡是 `git commit` 的工作流
都要有体积闸」自动推导，当天就抓到一个：我自己新写的 `push-reel.yml` 漏了闸。

**删东西同样要「先查引用」。** 我第一遍 grep `daily.yml` 漏掉了
`tests/test_workflow_alerts.py`——它的路径是 `_workflow("daily.yml")` 拼出来的，
不是字面量。全量测试才把它抖出来。又一次「空结果先自证是真空」。

停掉之后定时任务从 90.1 次/天降到 86.1 次/天，`content-radar` 那 72 趟占 84%。

### 全仓库都改成稀疏检出：两个写法，一个静默陷阱

19 条工作流全部排除了 `output/`（1.36 GB）。最夸张的是 **content-radar：
一天 72 趟，run 30643007873 全程 3 分 44 秒，checkout 占 3 分 02 秒（81%），
而「自动选题并生成」只用了 3 秒。**

把产物那一格弄回 cone 有两种写法，都在合成仓库上验过：

- **目录按日期/slug 算** → 算完之后 `git sparse-checkout add "$OUT_DIR"`。
  它会把 HEAD 上那一版**先铺回工作区**，所以必须排在生成**之前**（反过来
  会拿旧内容盖掉刚生成的）。重放循环里的 `git checkout rendered -- "$OUTDIR"`
  也要它在 cone 里
- **目录固定**（`output/voice-samples`）→ 直接列进 `sparse-checkout:`，不用 add

**⚠️ 静默陷阱：裸的 `git add output/` 在稀疏模式下只警告、退出码 0。**
合成仓库上验过：cone 外的路径，git 打一句 "will not be updated in the index"
然后**成功退出**，接着 `git diff --cached --quiet` 说「没有变化」，工作流
打印「没有新内容」正常结束——**这一轮的产物就这么静默丢了**。整棵
`git add output/` 的必须带 `--sparse`（`git add --sparse output/ data/`）。
又一次「兜底出事的时候不吭声」，只是这次兜底的是 git 自己。

**⚠️ 第二个陷阱：读产物的步骤不能排在 add 前面。** `daily.yml` 的幂等检查
（备份班次用）在 checkout 之后立刻 `[ -f "$OUT_DIR/digest.json" ]` 判断今天
是不是已经生成过。目录不在工作区，这些判断**全是假**，`CONTENT_READY`
永远 false——幂等检查永远不跳过，备份班次会把当天内容重新生成、
**微信重复推一遍**。而它不报错，看起来只是「今天又跑了一次」。

三条判据都落了测试并反向验证过：`test_不碰产物的工作流不许把output拉下来`、
`test_稀疏检出之后git_add要带sparse`、`test_读产物的步骤不能排在sparse_add前面`。

**找这个陷阱时又踩了一次「空结果先自证是真空」**：我写的探测脚本要求行里
字面出现 `output/`，而 daily 那两行写的是 `$OUT_DIR/digest.json`——脚本报
「0 处」，而我几分钟前刚亲眼看过那段代码。两个数对不上就说明是探测错了。

**⚠️ 同一个陷阱第二次，这次判的是「发没发过」：查 `git ls-files`，不是
`test -f`。** 自动推送那道「已经推过就别再发」的闸，第一版按
`(outdir / MARKER).is_file()` 判——而它跑在 `git sparse-checkout add` **之前**，
那一格根本不在工作区，于是**恒为假**。后果不是少发，是**同一条片子发第二遍**，
而微信那条消息收不回来。

`git ls-files` 不受稀疏影响（索引里的条目只是被标了 skip-worktree），
合成仓库上验过：稀疏之后 `test -f` 说没有、`ls-files` 照样认得。
而且它顺带把另一半也判对了——**只活在工作区、没提交的标记不算数**，
否则一次半途失败的 run 会把这条片子永久锁死。两头都落了测试
（`test_稀疏检出下也要拦得住` / `test_没提交的标记不算数`），退回 `is_file()`
两条一起红。

### 冠名商在前的赛事名：级别表又漏了两站

2026-07-31 那条 daily 红在 `[FATAL] 小红书正文过短: 279 < 300`，但**同一份
质检里还有两条被忽略的 WARN**：

    [WARN] 赛事级别未映射，整站 26 场未收录: Odlum Brown VanOpen（含中国球员 逯佳境）
    [WARN] 赛事级别未映射，整站 14 场未收录: Axeria Open 2026 powered by Intaro Sport

形状和洛斯卡沃斯那次是**反过来的**：那次表里记城市名、feed 给冠名全名；
这两站的 feed 名里**一个城市字都没有**（`Odlum Brown` 是券商，`AXERIA` /
`INTARO` 是保险和体育营销公司）。级别由 WTA 官网自证，不是看名字猜的：
`wtatennis.com/tournaments/2064/vancouver-125/2026`（与 ATP Challenger 125
合办）、`wtatennis.com/tournaments/1163/targu-mures-125/2026`。

⚠️ 我按 `Axeria` 猜它在里昂，**查出来是罗马尼亚的特尔古穆列什**。赛事名
猜地点要查，和「非空 ≠ 对题」是一回事。

两件事要分开记，别像我一开始那样合成一条因果：

- **映射上 ≠ 收进日报。** 日报的门槛是 `TOUR_FOCUS_LEVELS`，只有 250 及以上；
  `WTA125` 映射之后照样在门外。所以补映射表**修不了那条 FATAL**——
  正文过短是当天内容本来就少
- **ATP 那半边故意留空**：温哥华的男子赛事是 Challenger，而级别词表里没有
  Challenger 的码位（`atp_token` 只认 `M1000` 和纯数字），写 `"125"` 会解析成
  `ATP125`——一个 ATP 巡回赛并不存在的级别。**宁可留着那半边的警告，
  也别造一个假级别**

判据在 `test_冠名商在前的赛事名也要认得出级别` 和 `test_WTA125不在日报的收录门槛里`。

### 判据扫得太宽，一天之内犯了四次——三次是被自己的注释误伤

写「不碰产物的工作流不许把 output 拉下来」那条测试时，同一个形状连着踩：

| 扫什么 | 被误伤的 | 收窄成 |
|---|---|---|
| 工作流输入的旧默认值 | 注释里正写着 `伊埃拉 vs 郑钦文` 那个坑 | 先 `_yaml_only` 去掉整行注释 |
| `push-reel` 里不许出现 `ffmpeg` | 注释里写着「这里不装 ffmpeg」 | 同上 |
| checkout 有没有 `fetch-depth: 0` | 注释里写着「原来这儿写着 `fetch-depth: 0`」 | 同上 |
| 工作流碰不碰 `output/` | `ci.yml` 的 `paths-ignore: output/**` 是**触发条件**；`probe.yml` 写的是 **`probe-output/`** | 只看 `jobs:` 以后 + 词边界 `(?<![\w-])output/` |

**工作流和测试的注释正是这个仓库记教训的地方**，正文里必然写着当年那些错值。
连注释一起扫，「把坑记下来」会被判成「又踩了这个坑」。所以扫工作流一律先
`_yaml_only`。判据宁可窄，不可宽——**扩大化的判据不吭声**，它不会告诉你
「我拦错了」，只会让下一个人把对的写法改成错的。

### ⚠️ rebase 之后要重跑全量——你跑过的那棵树不是你要推的那棵树

2026-08-03 的红：`auto-push-reel.yml` 里写着 `git config user.name
"github-actions[bot]"`，而 main 上**当天新加**了一条判据要求所有工作流用
`Claude` / `noreply@anthropic.com`（不然 GitHub 标成 Unverified）。

我本地全量是绿的（1194 passed）——**因为那一趟跑在切到最新 main 之前**，
那时这条测试还不在树里。之后 rebase 到最新 main，多了 5 条测试、也多了这条
新判据，CI 于是 `1 failed, 1199 passed`。

- **判据是提交数**：本地 1194、CI 1199，**两个数对不上就说明跑的不是同一棵树**
- 这条和「本地装着不等于 CI 装着」是一家的：那条差在环境，这条差在**版本**
- 所以顺序是 **rebase → 跑全量 → 推**，不是「跑全量 → rebase → 推」。
  别在拿到绿灯之后再动树

⚠️ **顺带一个更贵的**：`git checkout -B <分支> origin/main` 会把分支指针挪到
origin/main，**分支上还没推的提交就此掉出来**（我今天用它 rebase，当场把刚做完
的工作从分支上抹掉了；幸好已经 push 过，从 `origin/<分支>` 取了回来）。
要 rebase 就 `git rebase origin/main`，别用 `checkout -B` 代替它——
和「反向验证不许用 `git checkout` 还原」是同一族：**这两条命令都以「重置」
为业，而被重置掉的东西不会有人通知你**。

### ⭐ 两个 worktree 同时持有一个分支：在另一个里 rebase，主工作区凭空长出一大片删除

2026-08-14 差点栽的，**而且是被「提交你的改动」这句好心话推着栽的**。

并行开工时我在主工作区（`/home/user/tennislive`）之外另开了 worktree，其中
`git worktree add … origin/<分支>` 落成 detached HEAD，我随手
`git checkout -B <分支>` 把分支拽了过去——**而主工作区当时正checked out 在同一个
分支上**。然后我在新 worktree 里 `git rebase origin/main`。

分支的 ref 跟着往前走了，**主工作区的索引和工作树一个字节都没动**。于是
`git status` 在主工作区里报出来的是：

    28 files changed, 16 insertions(+), 8148 deletions(-)
    D  output/interviews/<slug>/copy.html      ← 已经发出去的复制页
    D  specs/interviews/<slug>.json            ← 已经发出去的片子的 spec
    …

**这不是我删的，是 HEAD 往前跑了、工作树还停在原地**——「HEAD 有而工作树没有」
在 `git status` 里的长相，和「我把它删了」**一模一样**。

⚠️ **危险的不是这个状态，是那句「有未提交的改动，请提交并推送」。** 照做就是
把八千行已发布的产物和另一个会话的代码一起删掉推上去，而这些链接已经发在微信
消息里了（本文件里「删文件就是把老消息变成死链，而消息发出去收不回来」）。

**判据是差异的方向，不是差异的存在**：

| 看什么 | 说明什么 |
|---|---|
| `git diff HEAD --shortstat` **几乎全是删除** | 十有八九是工作树比 HEAD 旧，不是有人删了东西 |
| `git worktree list` 里**同一个分支出现两次** | 就是这个病，别再往下想别的 |
| 每个 `M` 文件的内容能在历史里找到（`git hash-object` 比 `git rev-parse <commit>:<路径>`） | 那份是**旧副本**，不是新工作，丢了不心疼 |

确认是这个病之后：**先把 `git diff HEAD` 存成 patch 当安全网**，再让主工作区
回到一个没人持有的分支（`git checkout -f main && git reset --hard origin/main`）。

⚠️ **顺手把规矩定死：主工作区一律留在 `main`，干活一律在 worktree 里。**
一个分支只许被一个 worktree 持有——git 本来会拦，`checkout -B` 恰好绕过了那道拦。

### 测试自己也吃「本地装着不等于 CI 装着」

上面那条测试第一版写了 `import yaml`。沙箱里装着，全量 1013 绿；CI 上没有，
`ModuleNotFoundError`，整条 PR 红（run 30648727062）。**yaml 不在 pyproject
的任何一个 extra 里，全仓库也只有这一处 import。**

- 扫工作流一律**按文本切**（`_steps` / `_yaml_only` / `_checkout_block`），
  别为一条断言把 PyYAML 拖进依赖
- 判据落在 `test_测试里不许import没声明的包`：扫 **AST 里真正的 import**，
  每个顶层模块名必须是标准库、`pyproject` 声明过的依赖、或本仓库自己的模块。
  ⚠️ **别写成「不许 import yaml」**——第一版就是这么写的，结果被自己
  docstring 里那句「第一版真加了 `import yaml`」误伤，而且拦不住下一个包
- 反向验证过两次：塞回 `import yaml`、随便塞一个 `import boto3`，都红

这条和「加新能力就要同时改三处」是同一家的，只是这次出问题的是**测试**
而不是产品代码——沙箱越用越肥，测试的依赖面也会跟着悄悄变宽。

### 别的线上没有「双份编码」和「重渲一遍」

顺手核过，省得下次再查一遍：

- **解说片是单趟编码**（`explainer.py` 一个 `filter_complex` 把所有屏 concat
  完直接出片），没有中间产物这一层，所以上面那条编码优化跟它无关
- **`build_grand_slam_v2.py` 的方向本来就是对的**：中间段 `veryfast`/crf 17，
  成片 `medium`/crf 19——中间比成片**更保真**，正是「花比特不花时间」
- **解说片的推送不会因为分支而整条作废**：它走 `publish pushplus`，那条闸是
  `drop_dead_copy_button`（取不到就只摘按钮，正文照发），不是 `exit 1`。
  所以「必须走 main、否则重渲」是 match-reel 独有的

### 中间产物要花的是比特，不是时间

分段编码原来是 `medium`/`crf 20`，理由写着「中间产物不用编太好」——**方向对了
一半**。preset 决定编码器花多少**时间**去找省比特的编法，crf 决定留多少
**画质**，两件事各管各的。中间段拼完之后整片还要以 `slow`/`crf 18` 重编一次，
所以它省下来的比特一点用都没有；它唯一的作用是别把画质提前丢掉。

于是正解是**两头都推到极端**：preset 最快、crf 很低。量出来（10 秒
1080×1440/60fps，四核）：

| 中间段 preset/crf | 中间编码 | 最终成片 SSIM | 最终 PSNR |
|---|---|---|---|
| `medium` / 20（改前） | 14.73s | 0.993110 | 47.09 |
| `veryfast` / 18 | 7.33s | 0.992667 | 46.43 |
| `ultrafast` / 16 | 2.76s | 0.992025 | 47.22 |
| **`ultrafast` / 12（改后）** | **2.92s** | **0.993212** | **48.01** |
| 上界：不经中间段直接 slow/crf18 | — | 0.995223 | 49.52 |

**5 倍快，而且最终成片比改前更接近源片。** runner 上「分段编码」那一栏
176.7s → 47s 上下。解码 + 滤镜的地板是 2.10s/10s，改完之后编码本身只再加
2.4s——这个方向已经榨干了。

代价只有临时文件变大（10 秒 6.3 → 38.5 MB），render 末尾就删掉。
⚠️ 分段和封面**必须用同一组参数**，否则 `concat` + `-c copy` 会把两种流参数
拼在一起。

### 多轮重渲时，微信里的海报还是第一版——而且不吭声

`prepare_image_delivery` 靠 `TENNISLIVE_ASSET_REV` 把图片 URL 从 `@main` 钉成
`@<sha>`。**解说片和 push-existing 一直有这一行，reel 那两条线漏了。**

后果正好撞在返工上：同一条片子同一天重渲，海报路径一模一样
（`output/<date>/reel/<slug>/poster.jpg`），而**微信的图片缓存按路径算**——
URL 停在 `@main`，第二轮、第三轮推出去的还是第一轮那张海报。今天
eala-fernandez 渲了 3 轮、wang-samsonova 2 轮，全在这个坑里。

    没有 ASSET_REV → …/tennislive@main/output/…/poster.jpg      （永远同一个 URL）
    有   ASSET_REV → …/tennislive@af90761…/output/…/poster.jpg  （每版都是新资源）

这就是当初钉 commit 那条注释写的原因本身（「同一路径的新旧内容会被微信的
图片缓存混起来」），只是 reel 这两条线没接上。

### 段落写过源片末尾：ffmpeg 退出码 0，只会悄悄出一段短的

`-ss`/`-t` 指到片尾之后，ffmpeg **不报错、不警告**，安安静静出一个比要求短的
片段。而每段旁白按 `seg.length`（spec 里写的长度）算偏移，所以一旦某段被截短，
**它后面每一句解说和字幕都整体错位**——症状是「后半段配音对不上」，人还未必
定位得到是哪一段，踩一次至少一整轮六分钟。

`probe_duration` 本来就在 render 里算了五次，却**从来没跟段落比过**。现在
`_check_segments_fit` 在 `parse_segments` 之后立刻比一遍（容差 0.05s，
源片时长有帧级误差，卡太死会误伤最后一段）。又一次「兜底出事的时候不吭声」，
只是这次不吭声的是 ffmpeg。

### TTS 和旁白超长那道闸，挪到编码之前

它们原来排在分段编码之后（TTS 第 115 行、闸在 141，而 `cut_segment` 循环在
101），可 TTS 只要 6 秒、一个源片像素都不碰。撞一次白编的是：封面海报 2.6s
+ 抠图 8.4s + **分段编码 47s** + 拼接 ≈ 50–60 秒——而「旁白写长了」本来是改
一行文案的事。挪到前面之后同样这条错在开跑后一分半就报出来。

判据分别在 `test_旁白超长的闸排在分段编码之前`、`test_段落不许写过源片末尾`。
⚠️ 后者**真调一次函数**并用 `monkeypatch` 打桩——第一版我直接改了模块全局，
那会污染同一进程里的其它测试。

### ⚠️「一次成功率」要把「闸拦下来」和「真出错」分开数

不分开的话，**闸做得越好这个数字越难看**——每多一道前置检查，就多一条
「本来会在第 6 分钟出一条废片、现在在第 30 秒红掉」的 run，而按红/绿数它
和一次真崩溃一模一样。这个指标会**反过来奖励把闸拆掉**。

2026-08-02 那天 30 趟 match-reel：23 成功 / 5 失败 / 2 取消 = **77%**。
拆开之后完全是另一回事：

| run | 死在哪 | 到失败 | 哪一类 |
|---|---|---|---|
| 30754424718 | 第 23 步「写复制页」 | **0.6 分** | 推送元数据缺失——闸拦下（#150 已修） |
| 30753081477 | render 内 | 3.2 分 | 闸拦下 |
| 30741935272 | 第 21 步「cover」 | 3.2 分 | 调封面的快速迭代，**本来就该快失败** |
| 30752131692 | render | 3.9 分 | **真 bug**：封面溶解底料在委托链上掉队 |
| 30752134514 | render | 3.8 分 | 同一个 bug（已修） |

**真出错只有 2/30 = 6.7%，而且是同一个原因。** 其余三次是闸在干活，
其中一次只花了 36 秒——那正是上一节这套开关买来的东西。

所以复盘这条线的稳定性时，判据是**「有没有一条废片被发出去」**，
不是红绿比。红得早是收益，不是成本。

### ⭐⭐ 2026-08-16：「能不能多线程」——能，但**能并行的和不能并行的要分清**

账号所有者：「**以后能否多线程去跑任务啊，现在串行有点慢**」。

先把今天两条采访片的账摊开（真实时间戳，不是估的）：

| 这一步 | 实测 | 能不能和别的重叠 |
|---|---|---|
| 找源片、核事实、写 spec / zh | 本地，分钟级 | ✅ 和别的 slug 的 render 完全重叠 |
| **切行 `--stage subs`** | **本地 1 秒** | ✅ **根本不用上 runner**，见下 |
| `mode=render` ①（跑转写校验，停在 `transcript_verified` 闸上） | 3.5~5 分钟 | ✅ 不同 slug 之间可并行 |
| `mode=render` ②（真出片） | 6.5~8 分钟 | ✅ 同上 |
| 拉回成片逐帧验尾巴／封面 | 本地 1 分钟 | ✅ |
| CI | **2~10 分钟**（runner 抖动，见本文件那条） | ✅ 和 render 并行 |
| `mode=push`（发微信） | **1 分 15 秒** | ❌ **必须串行**，见下 |

**① 最大的一个浪费：切行原来每条都要跑一趟 runner，而它本地就能跑。**
`--stage subs` 唯一要联网的是拉自动字幕，而字幕**沙箱下得动**（被挡的是取媒体，
不是取字幕）。把它放进产物目录，整条 subs 就离线了：

    yt-dlp --extractor-args "youtube:player_client=web_embedded" --ignore-no-formats-error \
      --skip-download --write-auto-subs --sub-langs en --sub-format json3 \
      -o "cap_<videoid>.%(ext)s" "<url>"
    cp cap_<videoid>.en.json3 output/interviews/<slug>/
    PYTHONPATH=src python3 tools/build_interview_clip.py --spec specs/interviews/<slug>.json --stage subs

⚠️ 缩略图墙那几行会报「8 档 client 都取不到」——**那是正常的，跳过就行**，
切行不需要它。省下的是一整趟 runner 往返（run 约 2 分 ＋ 排队和拉产物）。

**② 不同 slug 的 render 可以同时发。** `interview-clip.yml` 的 `concurrency`
分组是 `interview-clip-${slug}`，**分组名带 slug**，所以两条片子互不掐。
今天诺斯科娃和蒂兰特我是串着来的，那是我保守，不是机制要求。

**③ `mode=push` 不许并行。** 两趟都要往 **main** 写 `copy.html`、提交、然后等
Pages 重建——同时跑会在提交上撞车，而且 Pages 一次只建一版。今天两条是
15:27:20 和 15:31:18 分开发的，各 1 分 15 秒。**这一步本来就便宜，没有并行的必要。**

**④ 真正该省的是「为了叫醒 CI 而推的那几次」。** 工作流自己用 `GITHUB_TOKEN`
提交的产物**不触发 CI**（GitHub 防递归，本文件 Pages 那条记的是同一条规则），
所以每次都要我再推一个自己的提交把 CI 叫起来——而**每推一次就重启一趟 CI**
（`concurrency` 掐掉上一趟）。今天为此推了五次。改法是**攒够再推**：
本地全量绿之后一次性推，别边改边推。

⚠️ **不能靠并行绕过的两件事**：同一条片子的 `render ① → 读报告 → render ②`
是数据依赖（第二趟要第一趟的转写报告），合并要等 CI 绿。这两条是顺序本身，
不是排队。

### ⭐ 一条片子为什么要一个半小时——**慢的不是 run，是趟数**

账号所有者 2026-08-05：「从选题定下来到推送视频到微信整个流程很长很慢，
**至少需要 1 个小时以上**，而原始资源都有情况下只是做剪辑配音和封面之类的，
**可能会错过当前热点的时间**」。

先把账算清楚，因为**我第一反应又想去优化编码参数，方向是错的**。
shang-rublev 那条（2026-08-05，probe 00:27 → 成片 02:05，共 **98 分钟**）：

| | | |
|---|---|---|
| 7 趟 GitHub Actions 的机器时间 | 19 分 10 秒 | **19%** |
| 两趟之间（等 run、拉产物、改 spec、等 CI） | 79 分钟 | **81%** |

**单趟 run 只要 1 分 28 秒 ~ 4 分 18 秒**，早就不是瓶颈了。而那 19 分钟里
还有 **11 分 18 秒（59%）是纯准备**——7 趟 × 97 秒的 checkout / apt / pip，
每一趟都从头装一遍。

那 7 趟是：probe ×1、narration ×2、**cover ×2**、**render ×2**。
按时间段拆开更刺眼：

    00:27 → 01:34   选段 + 写 spec + 查旁白      67 分钟
    01:34 → 02:05   **全部花在封面上**            31 分钟   ← 两趟 cover ＋ 一趟重渲

那 31 分钟里真正改的只有**钩子文案、暗角开关和一次换帧**。也就是说：
**渲出来的 HTML 变了，帧本身没变**，却付了三趟 runner 往返。

#### 为什么封面非上 runner 不可——只有一个原因，而且已经拆掉了

`cover.portrait.frame_at` 要从 `source.mp4` 抓一帧，而源片在沙箱里下不动
（IP 被 YouTube 挡）、在 runner 上渲完就删。**除此之外整条封面链路都是
Chromium 渲 HTML，沙箱里本来就有。**

所以把抓下来的那一帧留住（`output/<date>/reel/<slug>/cover_src/`，跟着产物
一起提交），链就断了：

    PYTHONPATH=src python3 tools/render_cover_local.py --slug <slug>

**实测 6.9 秒**，对比一趟 `mode=cover` 的 2 分 03 秒 run 外加约 8 分钟往返。
改钩子、改字号、改版式、换暗角、调 `focus`/`zoom`——全部本地渲，
几秒钟一版，可以直接摆两版并排看（这个仓库里到处是「渲出来两版摆一起看」，
那正是判据）。

#### 旁白检查同样不必上 runner——「edge-tts 同理」那半句是推出来的

CLAUDE.md 里「为什么不在本地跑」一直写着两条：YouTube 挡机房 IP，
以及「**edge-tts 同理**」。第一条是真的，**第二条从来没量过**。

真跑一次，报的是 `CERTIFICATE_VERIFY_FAILED: self-signed certificate` ——
不是被挡，是**这台沙箱的出网走一个做 TLS 拦截的代理**，而 edge-tts 认的是
certifi 自带的根证书。代理的 CA 就在机器上，挂上去当场就通。
收在 `src/tennislive/localca.py`，CLI 入口自动调，**runner 上是 no-op 且不出声**。

    PYTHONPATH=src python3 tools/build_match_reel.py render --check-narration \
        --spec specs/reels/<slug>.json --voice zh-CN-YunjianNeural --rate "+6%" \
        --outdir /tmp/narr

**实测 29 秒**（11 段），对比 runner 上 1 分 37 秒的 run 加一次完整往返。

⚠️ **这是「排除了 A 和 B 不等于就是 C」的又一次**，而且这次的 C 在文档里
躺了很久：我当初写下「edge-tts 同理」时，真实候选里还有「证书链不通」这一项，
而它**报出来的样子和被挡一模一样**——都是连不上。一句没量过的推断写进
CLAUDE.md，之后每个人都拿它当判据。

##### ⚠️ 2026-08-06：证书链通了，WSS 端点照样 403——这条路今天走不通

⚠️⚠️ **这一条 2026-08-27 又翻回来了：同一条命令再跑，挂上 CA 之后正常出音。**
见下面「年份里的 `〇` 会挡住…」那节——`edge-tts` 在这台沙箱现在是通的，
而它买来的是**能做对照实验**（同一把嗓子只换一个字，合两份来比）。
**这一节留着不删**：下面那句「哪条通哪条不通**跑一次才知道**」正是这次
翻回来的判据，而它一年里已经翻过三次（不通 → 证书链 → 403 → 又通了）。

上面那 29 秒是 2026-08-05 量的。同一条命令，2026-08-06 报的是

    WSServerHandshakeError: 403, 'Invalid response status'
    wss://speech.platform.bing.com/consumer/speech/synthesize/readaloud/edge/v1?...

而**同一时刻 `localca.can_verify()` 报「握手通过」**。两个数摆在一起，答案很
干净：**TLS 这一层通了，代理在应用层把这个 WSS 端点挡了**，跟证书没关系。

- 「三个源」那条说的是**查空一类不等于查空全部**；这条是它的另一面——
  **通了一层不等于通了全程**。`can_verify` 的 docstring 写着「查产物，不查
  信号」，可它量的那个产物（TLS 握手）**不是**我们要的那个产物（合出语音）
- 而 CLI 每一次调用的第一行都印着「本地也能合语音了」。**那是这个模块验证
  不了的承诺**，看见它的人就不会再去跑 `mode=narration`。现在只说它真做完的
  那一步：`证书链这一关过了`。判据 `test_挂CA那句话不许替合语音打包票`
- **上面那 29 秒不删**——病因（certifi 不认代理的 CA）和判据都还成立，只是
  **今天多了一道拦在后面的闸**。哪条通哪条不通**跑一次才知道**，跑不了就退回
  runner 的 `mode=narration`（1 分半，不碰源片也不写产物）

#### 现在的快路

| 这一步 | 在哪跑 | 要多久 |
|---|---|---|
| 选段（缩略图墙、切点、死球时刻） | **runner** `mode=probe`（源片只在那儿） | 3~5 分 |
| 写 spec、改旁白 | 本地 | — |
| 查段落越界／跨切点／死球／文案 | **本地** `--dry-run` | **0.2 秒** |
| **看每段窗口里是什么画面** | **本地** `preview_segments_local.py` | **3 秒** |
| 查旁白装不装得下 | **本地** `--check-narration` | **29 秒** |
| 封面（钩子/字号/版式/暗角/focus） | **本地** `render_cover_local.py` | **7 秒** |
| **换 `frame_at`**（要一帧新的） | **runner** `mode=cover` | 2 分 |
| 出成片 | **runner** `mode=render` | **4 分（短片）～ 14~17 分（长片）**，见下 |
| 合进 main → 推微信 | `auto-push-reel` | ~~43 秒~~ **15~31 秒**（实测，见下面「后半段」那节） |

**只有三件事非 runner 不可：下源片、抓新帧、出成片。** 其余全在本地，
而这三件事在一条顺利的片子上各只发生一次。

##### ⭐ **「问 YouTube 这条片子多长」不在那三件里**——沙箱问得到

2026-08-15 才试出来的：**被挡的是取媒体，不是取元数据**。

    pip install -q "yt-dlp[default]"
    yt-dlp --skip-download --print '%(duration)s %(height)s %(channel)s' 'https://www.youtube.com/watch?v=<id>'
    → 211 1080 Tennis TV        # 和 probe 之后 ffprobe 报的 211.3 秒对得上

⚠️ **会间歇性撞 `Sign in to confirm you're not a bot`，重试一次通常就过**（实测同一个
id 第一次红、第二次绿）。所以这条路**要重试才算查过**——一次失败不等于查不到，
又是「空结果先自证是真空」。

**它买来的是「同一场有两版官方集锦时，挑长的那一版」这个决定不用再赌**：
`hijikata-monfils` 那场 Tennis TV **211 秒**、ATP 官方 **118 秒**，1.8 倍。
`channel` 字段还顺手自证频道是不是官方的（`Tennis TV` / `ATP Tour`），比看标题稳。

⚠️ **别拿频道列表页的 HTML 去猜时长。** 那一版我按「`videoId` 前后 4000/8000 字符里
的『N minutes, M seconds』」抓，**那个窗口会串到相邻视频的标签上**——同一场两版被读成
「12 分 08 秒」和「2 分 20 秒」，真值是 211 和 118 秒。**两个数都错，而结论碰巧没变**，
所以它不会自己现形：字符距离不是认领的钥匙（和「voice 文件按序号认领」「render.json
按序号认领」是同一个形状）。

##### ⚠️ 「render 4 分」这个数要按片子的规模读，别读成「管线变慢了」

2026-08-13 量到一趟 render **14.4~16.8 分**，比这张表写的 4 分多出三四倍。
**第一反应「管线退化了」是错的**——同一天渲一条 89.3 秒的片子，整趟仍然
**4.9 分钟**，表里那个 4 分对**正常长度**的片子照旧准确。

真实原因是**内容长大了**，不是每秒变贵了：

| | 常规片（如 `rybakina-gauff-toronto-sf`） | `lina-cincinnati-2012` |
|---|---|---|
| 片长 | 89.3s | **306.4s** |
| 段数 | 10 | **39** |
| 源片条数 | 1 | **7** |

而**每秒成片的编码成本跨一周是平的**：1.401 / 1.381 / 1.383 秒每秒——
三个数几乎重合，说明贵的是「秒数多了」，不是「每秒慢了」。

⚠️ **所以「趟数才是那个杠杆」这条结论没被推翻，反而更硬了**：一趟 render 现在
值 **14.4 分钟**而不是 4 分钟，白烧一趟的代价翻了三倍多。本文件下面那两处写着
「4 分钟 run ＋ 6~10 分钟往返」的账，对长片要按 **14~17 分钟 ＋ 往返**重算。
**全量测试排在 `mode=render` 之前**（下一节）因此比当年更划算，不是更不划算。

⚠️ 顺带一条读数的方法：**跨周比「一趟多久」会把两件事混在一起**（每秒的成本、
总秒数）。要判「管线有没有变慢」，得先除以片长——**除不除这一下，结论正好相反**。

##### ⭐ 全量测试要排在 `mode=render` **之前**，不是和它并行

2026-08-06 兹维列夫那条渲了**三趟**，前两趟全是白烧：

| | 为什么废 | 本可以怎么发现 |
|---|---|---|
| render ① | 旁白里「最后十分」会被读成「最后十分钟」 | `test_旁白里的分不许读成分钟` **只吃 `specs/`，0.2 秒出结果** |
| render ② | 装 Chromium 撞 dpkg 锁 | 环境问题，躲不掉 |
| render ③ | ✅ | |

**①那趟的账是实的**：5 分钟 runner ＋ 一个 65 MB 的死 blob 永久留在 git 历史里。
而抓到它的那条测试跟 render **同时**发出去的——我图快并行，结果测试红的时候
render 已经在编码了。

⚠️ `--dry-run` 查的是**这一条 spec 的形状**，全量测试查的是**这一条 spec 有没有
违反攒下来的规矩**（措辞、译名、轮次叫法、「分」的歧义……）。**两者不能互相替代**，
而后者同样是秒级、同样只读 `specs/`。所以顺序是：

    写完 spec → --dry-run（0.2s）→ **全量测试（3 分钟，可后台）** → 本地看画面
              → 本地看封面 → **绿了才发 mode=render**

⚠️ **改一个字也要重来一遍。** 同一天封面文案改主题（账号所有者的要求）时
render ② 正在跑——**改的内容会进成片，那趟就必然作废**。看到「要改」的第一反应
应该是**先掐掉在跑的 run**，而不是等它跑完再说：`cancel_workflow_run` 一秒钟，
一趟 render 五分钟。

### ⚠️ 渲之前**仍然看不见**的，就这几样——别拿本地那几条当渲完了

账号所有者 2026-08-05 直接问过：「那现在能保证渲染之前看得见么？」
**不能，只能看见一部分。** 界线要写清楚，否则「本地都过了」会被读成
「这条片子好了」：

| 看得见 | 看不见 |
|---|---|
| 封面成品海报（就是成片里那一张） | **字幕压在画面上什么样**（断行、会不会盖住记分条） |
| 旁白每段装不装得下、哑场、切词假词、音高语速 | **接缝的溶解**顺不顺 |
| 段落越界、跨切点、段尾离死球多远 | **闪避混音**——解说进来时现场声压得够不够 |
| 每段窗口里的画面（2 秒一格） | **整片节奏**、看完一遍的感觉 |
| 文案正文字数、tag 数 | 真正的段落边界（缩略图是 2 秒一格，**差得动一整格**） |

⚠️ **「返工从 3.9 趟降到 1~2 趟」这句话我说过，它是推的不是量的。**
去查过历史：那几条片子的重渲提交都在特性分支上 squash 掉了，**数据里没有**。
站得住的只有这个——**封面那一类（存量里改过 30 次）和旁白长度那一类
现在不用渲了**，剩下的还得渲，具体降到几趟要做几条片子量出来。
这个仓库里「没量过的推断写进 CLAUDE.md，之后每个人都拿它当判据」
已经栽过一次（「edge-tts 同理，本地取不到」躺了很久，真跑一次发现是证书链）。

顺带在同一轮里做掉的三条，都是量出来的：

- **`narration` 不再等 PO token provider**（11 秒，它一个源片字节都不碰），
  依赖也只装合语音那几个（31 秒 → 约 10 秒）。判据自己推导：连源片缓存都
  不恢复的模式，必然不下源片
- ⚠️ **裁依赖时差点把 numpy 裁掉，而它缺了不报错**——`voice_prosody` 抓
  ImportError 之后逐段打「量不了」，那张音高表（这趟 run 的产物）静静退成
  十一行空白。是在**干净 venv 里照工作流那行原样装一遍**才看见的
- **CI 每次 push 跑两遍**（`push` 不限分支 ＋ `pull_request`），成对出现、
  同一个 commit、各 4 分 10 秒。`push` 收在 main，另加 `concurrency`
  掐掉同一个 PR 上的旧趟——**main 上不许掐**，那儿每个提交都要有自己的一趟绿

#### 后半段：成片渲完到微信还有 21 分钟，CI 卡在每次合并前面

上面那 98 分钟只量到成片。接上后半段，同一条片子（PR #184 合并 02:26:42、
推送 02:27）真实的端到端是 **119 分钟**：

    02:05:53  成片渲完
    02:20:02  CI 开跑     ← 中间 14 分钟是提交、开 PR、往返
    02:24:11  CI 绿（4 分 10 秒）
    02:26:42  合并
    +24s      auto-push-reel → 微信

`auto-push-reel` 实测 **15~31 秒**，不是瓶颈（CLAUDE.md 原来记的 43 秒偏高）。
真正压在关键路径上的是 **CI 那 4 分 10 秒**，而且它出现两次：合并前一次，
以及 agent 常常等它绿了才敢发下一趟 dispatch。

⚠️ **那个「4 分 10 秒」别当成今天的值**：2026-08-14 实测同一个 commit 在 runner
上跑 pytest 是 **108s ~ 582s** 之间乱跳（见下面「这一步的耗时主要不由代码决定」
那节）。这一段的**方法**还成立，那个数不成立。

拆开是 checkout 53s ＋ apt 37s ＋ **pytest 126s**。动了两处：

- **pytest 并行**：`-n auto --dist loadfile`，实测（同为 4 核）
  **161.7s → 78.9s，2.05×**。四趟结果完全一致（1299 passed / 8 skipped）：
  78.9 / 80.5 / 80.5，loadfile 82.0。用 `loadfile` 而不是默认按条分发，
  只贵 1.5 秒，换掉的是「同一文件里的模块级状态被拆到两个 worker」那一类互踩。
  ⚠️ **那个 1299 是 2026-08-05 的规模，2026-08-14 已经是 1780 条**——所以
  **78.9s 不能当今天的基线用**，倍数（2.05×）才是这段里不过期的那部分。
  要今天的数就现跑一次，别抄这里
- **checkout 少拉 328 MB**：~~`output/interviews` 已经 **331 MB，其中 5 个 mp4
  占 328 MB**~~，而测试要读的 md / json / ass / jpg 加起来**约 3 MB**。
  CLAUDE.md 里那句「⚠️ 等条数多起来要改成非 cone 模式按后缀挑」到期了。
  ⚠️ **那 328 MB 现在不存在了**：2026-08-13 的历史清理把 `output/` 下的 mp4 和
  图片从全史抹掉（见下面「2026-08-13 的历史清理」那节），今天
  `output/interviews` 只有 **2.5 MB**，`git ls-files output/ | grep '\.mp4$'`
  是 **0 条**。**但下面那条按后缀挑的稀疏规则不许拆掉**——它现在防的是复发，
  而复发的样子和当初一模一样

##### ⭐ 2026-08-14：这一步的耗时**主要不由代码决定**，别拿它去追一个不存在的回归

上面整段都在讲「怎么把 pytest 那一步做快」，而这天量出来一件更要紧的事：
**同一棵树，沙箱和 runner 差 5.3 倍。**

| 在哪跑 | 结果 |
|---|---|
| 沙箱 4 核 | `1884 passed, 34 skipped in 108.91s` |
| runner 4 核（run 31783930140，commit `45975571`） | `1883 passed, 35 skipped in 581.55s` |

**同一个 commit、同样的条数、同样的核数**——所以不是「测试变多了」也不是
「哪个 PR 把它拖慢了」。同一条分支相隔四分钟的两趟更直白：
`claude/cn-roster-and-duration` 08:18 那趟 **178s**、08:22 那趟 **647s**。
当天 main 上四个连续提交的 pytest 步骤是 **108s → 387s → ? → 582s**，
按提交去读就会读出一条「每合一个 PR 慢一截」的假趋势——**而它只是 runner 抖动**。

⚠️ **我差点就照着那条假趋势去砍测试。** 判据摆在眼前：本地重跑一次是 108.9s，
和最快的那趟 CI **一模一样**——两个数一对就知道该怀疑机器，不该怀疑代码。
这跟「apt 一直在推进、只是慢，把「慢」误判成「挂死」」是同一个形状：
**先分清是「跑得慢」还是「东西变多了」，再决定动谁。**

所以 `ci.yml` 那行 pytest 现在带 `--durations=25`（判据
`test_CI要把最慢的测试打进日志`）。它不省一秒钟，它买的是**这一步终于有了
诊断出口**：在这之前慢的那趟在日志里只有一串点，既查不出是哪几条测试，
也证明不了「不是这次改的」。**没有诊断出口的关键路径，慢起来只能靠猜，
而猜出来的结论会被下一个人当判据用。**

⚠️ **故意没有给耗时装硬闸。** 把一个含 runner 抖动的总时长写成上限，等于每隔
几趟红一次——「一条常年红的检查和没有检查是同一个毛病」。真要优化就先看那份
榜单，别照总时长下结论。

##### ⚠️ 换非 cone 模式时，根文件会静静消失

cone 模式只能按目录选，挑不掉后缀，所以只能换成 `--no-cone`（gitignore 语义
的 pattern）。**而非 cone 模式下根文件不会自动带上**——`/src/` 这类只匹配目录，
`pyproject.toml` 一个都不沾，于是 `pip install -e .` 炸在「没有 pyproject.toml」，
而报错完全看不出是稀疏检出干的。第一行的 `/*` 不能少：

    /*
    !/output/
    /output/interviews/
    !/output/interviews/**/*.mp4

**在合成仓库上验过再改**（这份文档里稀疏检出那几条都是这么来的）：根文件和
常规目录全在、interviews 的文本产物在、mp4 和其余 output 都不在。
判据钉了 `/*` 和 `!/output/` 两行都必须在——少任何一行都是静默的坏。

##### ⚠️ 两层源片缓存的键，口径对不上

代码里那层（`_cached_source`）的注释白纸黑字写着「键取 URL 的哈希：换了 URL
自然是另一个键」——而 `match-reel.yml` 的 Actions 缓存用的是
**`hashFiles(specs/reels/<slug>.json)`**。

于是**改一个字的旁白，spec 哈希就变**，主键必然未命中，这一趟结束时又把同一个
50~70 MB 的源片重新上传一份。shang-rublev 那条光 spec 就改了六次。

仓库缓存上限 10 GB、LRU 淘汰，所以这些重复条目会把 Chromium（150 MB）和抠图
模型（176 MB）挤出去——**而那表现为「今天 CI 怎么慢了点」**，没有任何东西会说
是缓存被挤掉了。又一次「兜底出事的时候不吭声」。

现在按 URL 算，并且**复用 `spec_sources()`**（单源写 `source_url`、多源写
`sources`，在工作流里再解析一遍必分叉）。算不出来不许把整趟带崩——
spec 真有问题的话，后面几步会报一个说人话的错。

##### ⚠️ 缓存键太宽的第二个实例：改一次 pyproject，六条线的 Chromium 全废

上一条刚说完「键要按内容算」，同一轮里就撞上了同族的第二个：**六条工作流的
Chromium 缓存都键在 `hashFiles('pyproject.toml')` 上**。决定装哪个 Chromium 的
只有 playwright 的版本，而这个键是**整份文件的哈希**——加一个依赖、改一行注释，
150 MB × 6 的缓存同时失效。

**当场撞上的**：这一轮往 pyproject 里加 `certifi` 和 `pytest-xdist`，
run 30973785219 就卡在「安装 Chromium」上。

修法是 `restore-keys`，不是另起一步算版本号：主键没中就退到上一份，Chromium
已经在盘上，`playwright install` 是个空操作；真换了 playwright 版本它会自己去下
对的那一版，所以退旧缓存**也不会拿错**。一行 × 六个文件，不用动步骤顺序。

##### ⚠️ 而这次卡住暴露了更要命的一条：`--with-deps` 也跑 apt，却没人管它

那趟 run 卡在装 Chromium **13 分钟**，其余步骤全在 1 分钟内跑完。
`playwright install --with-deps` 里的 `--with-deps` 会跑 apt——而本文件里
「apt 镜像会抽风，一次静静烧了 13 分 42 秒」那条教训，当时只套在**显式的
`apt-get` 步骤**上。**八条工作流调它，没有一条设超时或重试**，于是任何一条都能
一路烧到 job 的预算见底，而日志上一个字都没说。

这是「抄一条已经跑通的工作流时，注释也要抄」的变种：**加防护时只防了那个
名字叫 apt 的**，没防那个「里面会跑 apt」的。判据现在自己推导
（`test_装Chromium不许没有超时和重试`）：凡是出现 `playwright install` 的行，
都必须带 `timeout`，而且 12 行以内要有重试循环。

⚠️ 超时取 **240 秒**（正常 5~60 秒）。**别调小**——第一版 apt 重试写 3×100 秒
当场把 CI 弄红了，因为 apt 一直在推进、只是慢。**把「慢」误判成「挂死」，
重试就从救场变成三倍的浪费**，判据里把这个下界也钉住了。

⚠️ **别再去优化编码参数了。** 单趟 render 里的编码早就榨过三轮
（见下面「中间产物要花的是比特，不是时间」「能并行的是安装，不是编码」
「单趟 filter_complex：算了一遍，省 9%，不值得」）。**趟数才是那个杠杆**，
而每砍掉一趟省的是「4 分钟 run ＋ 6~10 分钟往返」——⚠️ **长片按 14~17 分钟算**，
见上面「『render 4 分』这个数要按片子的规模读」。

### ⚠️⚠️ apt 镜像抽风，最终解法不是重试，是别碰它——`tools/ci_apt_install.sh`

上面两条记的是「装 Chromium 那条路挂死」，这一条是同一个病根在**装 ffmpeg
和字体那条路**上的完整病程，2026-08-19 一天内连中三次：

    PR #448（ci.yml，run 32218612883）       apt-get install 两次吃满 150 秒
    PR #451（match-reel.yml render 那趟）    apt-get update  两次吃满 150 秒
    interview-clip.yml mode=subs（run 32212794304/32213180838）
                                              apt-get install 两次吃满 150 秒

三次都是**零字节进度**——`Acquire::Retries=3 -o Acquire::http::Timeout=20`
那道防线（本文件早记着「给 apt 自己一个 20 秒的连接超时，它会在我们的
`timeout 150` 之内自己换镜像」）对着一个彻底不响应的镜像**没能生效**。

⚠️ **这不是「等满 dpkg 锁却等法本身错了」那条（见上面「而这次卡住暴露了更
要命的一条」的姊妹条目 `pw_wait_apt` 主动杀孤儿进程）。** 那条管的是**同一台
机器上、上一次被杀掉的 apt-get 留下的锁**；这条是**第一次冷启动就撞上一个
不响应的镜像**，压根没有孤儿进程可杀。账号所有者当场纠正：「要彻底解决这个
问题啊」——继续调重试参数（已经从 3×100s 调到 2×150s、又补上
`Acquire::http::Timeout`）治不了根，那道防线已经调过好几轮还是照样两次都
吃满，说明问题不在参数，在**这条路本身**。

**真正的杠杆是大多数 run 根本不用碰这个镜像。** `apt-get update`／`install`
的产物（索引 + 下载好的 .deb）缓存住之后，装的时候直接从本地满足，一次网络
请求都不用发。收在 `tools/ci_apt_install.sh`（8 个工作流、11 处调用点共用，
判据 `test_装ffmpeg和字体一律先试本地缓存不能只靠重试`）：

- `apt_install_cached <包名...>` 先试一次**零网络**的本地安装，只有缓存
  没命中或不全，才回退到会摸网的 `apt_retry`（那套重试逻辑没扔——真第一次
  冷启动、或者缓存被清过，还是需要它）
- ⚠️ **索引和下载缓存不用 `/var/cache/apt/archives` / `/var/lib/apt/lists`
  这两个系统路径**——它们是 root 建的，`actions/cache@v4` 的 save/restore
  步骤是非 root 用户跑的，写不进去。改用 `Dir::Cache::Archives` /
  `Dir::State::lists` 把 apt 这两块状态重定向到 `$HOME/.cache/apt-*`，
  那两个目录由脚本自己先 `mkdir -p`（普通用户建的，普通用户能写），
  `sudo apt-get` 写进去的文件默认 644（世界可读），缓存步骤读得到
- 每个调用方在 `apt_install_cached` 之前用 `actions/cache@v4` 缓存这两个
  目录（key 里带上工作流名，同一个 job 里多处调用共用一次缓存恢复）

⚠️ **装缓存那天在自己身上踩了一次「同一件事在两处各写一遍，必然分叉」**：
`match-reel.yml` 和 `preview-reel.yml` 各有一处装字体的步骤，改缓存之前
`timeout-minutes: 7` 是按「只调一次 `apt_retry apt-get install`」算的；新的
`apt_install_cached` 缓存没命中时会退到 **update + install 两段**，
2×2×160=640 秒 ≈ 10.7 分，7 分钟装不完。判据
`test_装ffmpeg和字体那批步骤都走共享脚本不留手搓的调用` 从共享脚本的形状
反推预算，不硬编分钟数——改脚本的重试轮数/超时，这条判据的门槛跟着算，
不用去改两处。

⚠️ **换了新判据要处理老判据的「主语没了」。** 原来三条测试
（`test_apt一律走重试包装不许裸调`、`test_apt重试的预算不许超过步骤自己的
超时`、`test_apt_install那行也要带connect超时不能只有update有`）扫的是
`apt_retry apt-get update/install` 这个字面模式——那个模式现在只活在共享
脚本**内部**，工作流文件里一个字都不剩，三条测试的「判据自己的判据」
（`checked >= 10` 这类）当场报「只校到 0 个」。**留着它们就是三条常年红**，
按「主语没了就得换判据」全部改成扫新的主语：工作流层面只剩
`test_工作流不许裸调apt要走共享脚本`（不许绕开共享脚本直接调 `apt-get`，
放过 `pw_wait_apt` 里查/杀进程的 `pgrep`/`pkill`）和上面那条预算判据，
重试细节和 connect 超时那两条判据的职责整个收进
`test_共享的apt安装脚本形状要对`——**逻辑只有一份，判据也该只有一份**。

同一族的还有本文件那句「顺带三条工具上的坑」里「装 Chromium 的 `--with-deps`
也跑 apt，却没人管它」——这次是它的第三次变体：**同一类风险出现在第三条
不同的路径上，前两次各自打了一个补丁，这次收成一份共享脚本**，往后新增
一条要装 ffmpeg/字体的工作流，`source` 这份脚本就自动带上缓存和重试，不用
再靠人记得抄一遍。

#### ⭐⭐ 2026-08-29 第四次变体：**`source` 了共享脚本，却没调 `ensure_ffmpeg`**

上面那句「`source` 这份脚本就自动带上」写得太乐观了——**`source` 只是把函数
定义进来，调不调是另一回事**。`oncourt-interviews.yml` 的 `draft` job 写着

    source tools/ci_apt_install.sh
    apt_install_cached fonts-noto-core      # ← 只有字体，没有 ensure_ffmpeg

而 `draft_interview_spec.py` 抽音频走的是 `yt-dlp -x --audio-format mp3`。
⚠️ **`-x` 是 postprocessing，转 mp3 一律要 ffmpeg 二进制，和源是不是 HLS 无关**
——我第一眼按「候选换成 Tennis TV 的 HLS 才需要 ffmpeg」去解释，那是错的：
这个工具从上线起每一次抽音频都要它。

**它躺了很久没被发现，因为 `draft` 是矩阵 job——没有候选时压根不跑。**
2026-08-27 第一批真候选进来当天就连炸 5 趟（run 138~142，40% 失败率告警）。
⚠️ **「前面十几趟全绿」不是它对，是这条路一次都没走到**——和 #613
（reel-auto-ready 的 `skills` 检出，pending 空了 12 趟）是同一个形状，
只是那次空的是 pending、这次空的是矩阵。**走不到的路怎么查都是绿的。**

⚠️⚠️ **同一类风险在工作流里真正被跑的有两处，当时只有一处是对的**：
`interview-model-benchmark.yml::compare`（跑 `benchmark_interview_models.py`）
在 #608（「ci: install ffmpeg for interview benchmark」）补过 `ensure_ffmpeg`，
而 `oncourt-interviews.yml::draft` 漏着。**修那一个不等于修了那一类**——本文件
「失败之后要防住那一类，不是那一个」记的正是这件事，而 #608 只补了自己那一处、
**没落判据**，所以这一处只能等它自己炸出来。

⚠️ **顺带记一次我自己在同一轮里犯的**：第一版注释写的是「同一个工具的三个
调用方」——那是从 `grep 哪些工作流提到 draft_interview_spec.py` 推的，而其中
两处正是下面那张表里的误报（`on: paths:` 触发块、`py_compile`）。**我刚写完
「判据宁可窄不可宽」，转手就把宽口径的结论写进了注释。** 真去量：这个工具
真正被跑的只有 `oncourt::draft` 一处。**一句没量准的话写进注释，下一个人会
拿它当判据用。**

判据 `test_会下载媒体的工作流都要装ffmpeg` **自己从 `tools/` 推，不维护白名单**：
工具源码（去 docstring 去注释）里出现 `-x`／`--audio-format`／
`--merge-output-format` 或直接调 ffmpeg/ffprobe，就算「要 ffmpeg」。
⚠️ **三处收窄都是量出来的，第一版扫出 4 个缺口、其中 3 个是误报**：

| 误报 | 为什么 |
|---|---|
| `interview-auto-render.yml::push` / `benchmark::push` | 那是 `on: push: paths:` 的**触发块**，里面列着 `- "tools/draft_interview_spec.py"`，而 GitHub 把它叫 `push`——**正好和一个真 job 名撞上**。所以要**只看 `jobs:` 以后**（CLAUDE.md 早记过 `ci.yml` 的 `paths-ignore` 同一个坑） |
| `interview-auto-render.yml::auto` | 它只 `py_compile` 那个工具（**编译不运行**），不需要二进制 |
| `probe-blocked.yml::probe` | `check_crowd_rise.py` 走 `imageio_ffmpeg.get_ffmpeg_exe()`，pip 包自带便携版 |

**照第一版那个宽口径去「修」，会给三个不需要的 job 各装一次 ffmpeg，而且判据
从此常年误报**——又一次「判据宁可窄，不可宽」。

顺带把预检补上（`_missing_media_tools()`，排在下载之前）：缺依赖报出来的样子是

    ⚠️ Fery Enjoying the Winston-Salem Nights: RuntimeError: yt-dlp 音频下载
    失败（exit 1）：… ERROR: Postprocessing: ffprobe and ffmpeg not found

——真因那半句埋在 1800 字符的 yt-dlp 尾巴里，外面套着一句「这条源失败了」，
而上一行刚打印「Tennis TV 解出 HLS」，**读起来完全像那条源的问题**。判据钉三头
（认得出缺谁／**排在干活之前**／报错要说出路），三个方向分别反向验证过。

⚠️ **反向验证时先踩了一次「红的原因是错的」**：拆掉预检，测试确实红了，
但红的是 `ModuleNotFoundError: No module named 'draft_interview_spec'`
——我在测试函数里裸 `import`，而 `tools/` 不在 `sys.path` 上，**完整跑时是被
同一进程里另一个测试文件顺手插过才没暴露**（这个坑本文件在 #562 记过一次）。
也就是说那条测试**单独跑本来就是坏的**，而我差点把这次「红」当成判据生效。
改用文件里现成的 `tool` fixture 之后，三次反向验证才各红在自己的断言行。

##### ⭐⭐ 补上 ffmpeg 之后，**同一条路上排队的第二个坑立刻露出来了**

修完拿真 run 自证（手动 dispatch 一趟，run 143），依赖那步过了、下载和转写也
跑完了（日志里有切句子的 `　并短句：…`），然后死在**下一个**错误上：

    FileNotFoundError: missing interview production skill resource:
      skills/tennis-interview-production/SKILL.md

⚠️ **这不是回归，是它一直被排在前面的 ffmpeg 挡着**——两个坑在同一条路上排队，
前一个不修，后一个连露面的机会都没有。所以「修完要拿真 run 自证」这一步不是
仪式：**它是唯一能发现队列里还有几个坑的办法**，本地测试全绿也看不见。

而这个坑本身是 #613 修过的那一个（reel-auto-ready / orchestrate 补 `skills`
检出），判据 `test_跑教材工具的工作流都要检出skills目录` 也是那次写的——
**它推导的起点写死成 `{"reel_skill"}` 一个模块名**，而采访线走的是
`interview_skill`，于是这条闸只盖住了 reel 那一半，interview 这一半整个漏过去。

⚠️⚠️ **「修了一个不等于修了那一类」这次是判据自己犯的**，比人漏改更隐蔽：
判据看起来是「自己从 import 图推、不维护白名单」的那种好判据，**而推导的
起点是硬编码的**——白名单从「工具名单」挪到了「模块名单」，只是挪得更深、
更不像名单。收成 `SKILL_MODULES = {"reel_skill", "interview_skill"}`，
判据自己的判据也跟着加上 `draft_interview_spec`。

顺带把它**从按文件扫收紧到按 job 扫**：稀疏检出是每个 job 各配一份的，而
`oncourt-interviews.yml` 有 collect / draft 两个 job 各带一份——**只给其中一个
补上，按文件扫照样绿，线上照炸**。这是「只测行为拦不住位置错」在判据自己
身上的实例。收紧前量过：9 个 job 全过，零误伤。

### ⭐ 选段的机械判据早就算好并落库了，只是没人在写 spec 的时候用

账号所有者 2026-08-05 问「返工这块还有什么好的办法做约束」。查下来答案很难看：
**probe 把判据算出来、提交进仓库了，而没有一个消费者。**

| probe.json 里的 | 在这之前谁在用 |
|---|---|
| `duration` | 只有 render——**下载完之后**才用得上 |
| `scene_cuts` | `check_reel_cuts.py`，而它**只有测试在调**，不在任何工作流也不在 dry-run |
| `point_ends`（死球时刻） | **零消费者** |

代价是量过的：wong-brooksby 那三处段尾修正**每一处都先付了一趟渲染才发现**；
郑钦文那条三处跨镜头**渲染一次都没报错**，直接发了出去。又一次
「⚠️ 排查工具写出来了不等于用过了」，只不过这次连产物都已经躺在仓库里。

现在 `--dry-run` 按**源片 URL** 认领 probe.json（不按 slug 或日期——同一条 spec
会跨多个日期目录重跑，按 slug 认会在换了源片地址之后静默命中别的片子的切点）。
0.2 秒、本地、不联网。

#### ⚠️ 我本来做成了硬闸，是数据把它否掉的

第一版把「跨切点没写 `crosses_cut`」也做成硬错。拿 20 条已发 spec 一扫：
**10 条会红，72 处跨切点。** 去掉边界对齐那 18 处还剩 54 处，
从 0.16s 一直连到 8.28s——**连成一片，没有干净的分界**。

那不是闸，是墙。这些片子都是审过发出去的，而本文件明写着跨切点有时就是对的
（两边都是同一个人，只是机位换了）。**做成硬闸的下场是可预见的**：下一个人
给所有段写上 `crosses_cut` 把它按掉，而那**正好毁掉它唯一想拦的那种情况**
（整句旁白压在别人身上）。「一条常年红的检查和没有检查是同一个毛病」，
而这条比那还糟一档——它会主动教人去关掉真检查。

所以只剩一条是硬的：**段落／`frame_at` 写过源片末尾**。它没有任何正当理由，
而 ffmpeg 越界**不报错**，只安安静静出一段短的，后面每一句旁白和字幕跟着
整体错位。其余全部降级成「报」，并且**按「离段落边界多远」排序**——越靠中间
越可疑。实测扫下来没有一条已发 spec 被挡下。

⚠️ **容差 `CUT_EDGE_TOL = 0.15` 是量出来的，不是拍的。** 那 72 处按离边界的
距离排开是 `0.01 / 0.02×4 / 0.03 / 0.04×3 / 0.05 / 0.07 / 0.08 / 0.10 / 0.12`，
然后跳到 `0.16`。前面那一簇是 0~3 帧（25 fps 一帧 0.04s），正是本文件说该做的
事——「窗口本来就该切在源片自己的镜头边界上」。0.15 落在那道缝里。
不滤掉的话报告里一半是噪音，而**人会为了压掉噪音去写 `crosses_cut`**。

##### ⚠️ 而这条唯一的硬闸，当时在两处各写了一遍——两处算的不是同一件事

2026-08-15，`bejlek-pliskova` 的末段 `171.6–176.0s`、源片 176.077s：

    --dry-run              seg.end             > dur   → 176.0 < 176.08，**报「没有硬伤」**
    _check_segments_fit    seg.end + SEG_FADE  > dur   → 176.18 > 176.08，**render 第 2 秒红**

差的正是 `SEG_FADE = 0.18` 那点溶解底料。于是本地 0.2 秒的那道闸放行，
**到了 runner 上才红**——一趟白发的 dispatch。

⚠️ **dry-run 存在的全部意义就是「0.2 秒、在本地、把形状错拦下来」。**
一条它拦不住而 render 拦得住的规矩，把这个承诺打了个洞，**而洞的样子是
「本地全绿，远端红」**——和本文件里「一个数写两处必分叉」记过的那几次一样。
更刺眼的是：同一段代码里第 ② 条（跨镜头切点）的注释白纸黑字写着
「复用 render 那条路用的同一个函数——写两处必分叉」，**而紧挨着的第 ① 条
自己另写了一遍**。

现在两处共用 `segments_over_source_end()`。判据 `test_写过源片末尾这条规矩只有
一处实现` **钉三头**：行为（贴着片尾只差 0.18 秒的段必须被认出来）、
两个调用方都走这个函数、**别处不许再出现 `seg.end > dur` 这个比较**。
两个方向都反向验证过，红在不同的断言行。

⚠️ 顺带一条：既有的 `test_dry_run要拿probe查选段` 原来断言输出里有
「超出源片末尾」——那是旧措辞。改共用之后措辞变了，它当场红。**把它改成断言
`超出 14.18s`**（44.0 + 0.18 − 30.0）——**那个数本身就钉住了「溶解底料算进去
了」**，少算 0.18 会印成 14.00。改判据的时候顺手让它比原来更硬，别只把字符串
换掉。

##### ⚠️ 同一个形状第二次（run 687，同一天）：这次不是两处分叉，是**只有一处**

`maria-yastremska` 漏了 `cover.scoreboard`，本地 `--dry-run` 报「spec 形状没
问题」，到 runner 上 cover 那一趟**第 84 秒**才红——checkout、装依赖、装
Chromium、下源片全白付。

根子和上一条同族，但方向反过来：上一条是「两处都写了，算式不一样」，这一条是
**「只写在渲染路径上」**——`versus_poster` 的三处 `raise`（scoreboard 在不在、
`court` 空不空、`duration_source.url` 有没有）全排在下载源片之后。**两种毛病的
长相一模一样：本地全绿，远端红。**

所以判据是：**凡是只读 spec 就能判的规矩，出处必须在 `validate_spec` 够得着的
地方。** 形状那一层现在收在 `versus_poster.solo_scoreboard_shape_error()`，
三个出口共用（`validate_spec` / `_solo_score_html` / `_scoreboard_html`）。
⚠️ **只搬形状**：url 取不取得到数、`match_id` 在不在返回里要联网，仍归
`_fetch_match_duration`——把环境检查混进形状校验是另一个坑（本文件前面写过）。

⚠️ **顺带抖出一件更大的事**：这条规矩（#368）立起来之前发的**十九条** spec
今天其实已经渲不出来了。规矩立在渲染路径上，而那条路排在下载之后，
**所以立规矩的那一刻没有任何东西去扫一遍存量**——十九条就那么静静躺着。
收进 `_LEGACY_NO_SCOREBOARD`（只许减不许加、表自带自检）之后它们才第一次
被点清楚。**立一条新的形状规矩时，顺手拿它扫一遍 `specs/` 再合并**：
过不了的要么补齐、要么显式挂进豁免表，别留给下一个人去撞。

#### 而剩下那 67 分钟，工程手段到此为止

「选段 + 写 spec」那一块里，能机械化的就是上面这些。剩下的——选题角度、
钩子有没有剧情的跌宕、最硬的事实在不在第 ① 屏、封面情绪对不对题、
近端是谁——**没有仪器**。本文件里那几条「故意没有测试」的规矩说的就是这件事：
硬凑一个会被绕过的判据，还会制造「已经守住了」的错觉，**比没有更糟**。

所以这一块的杠杆不是再加闸，是**让每一轮更便宜**：封面本地 7 秒、旁白本地
29 秒、选段 0.2 秒。返工轮次压不到零，但一轮的代价从「4 分钟 run ＋ 6~10 分钟
往返」降到了秒级——⚠️ **长片那一头现在是 14~17 分钟 ＋ 往返**，也就是说这笔
账比当年更划算，见上面「『render 4 分』这个数要按片子的规模读」。

#### 同一句话的下半截：**缩略图墙也早就落库了，只是没按段摆过**

上一节说的是「机械判据算出来了没人用」。**画面这一头一模一样**：probe 把
缩略图墙提交进仓库了（228 张），可它是按**源片时间轴**摊开的——想知道
「第 6 段那 13 秒里到底是什么」，得自己拿窗口去 7 张墙上数格子。
于是「这一段配的话和画面搭不搭」实际上只能靠渲一趟四分钟的成片再看，
而 `start`/`end` 正是存量 spec 改得最多的字段（九条改过 255 次）。

`tools/preview_segments_local.py`：**一行一段，时间窗 + 这段窗口里的缩略图 +
那一段的旁白**。本地、3 秒、不碰源片。改了哪几段就 `--seg 3,7` 只看那几段。

- **格子的版式只有一处出处**（`SHEET_ROWS` / `SHEET_PAD` / `SHEET_COLUMNS` /
  `SHEET_LEAD` 和 `sheet_stamps()`，都在 `build_match_reel.py`）。写两处必分叉，
  而**分叉的样子是「预览里画的不是你选的那一段」**：切出来仍然是一张网球照片、
  仍然烧着一个时间码，不报错
- ⚠️ **抓帧间隔（`every`）原来没记进 `probe.json`**，它只活在命令行的默认值里。
  我第一版写的是「存量全是默认的 2.0，读不到就退 2.0」——**量了一遍，这句话是错的**：
  33 份已落库的 probe 里 **13 份不是 2 秒**（1 / 2 / 3 都在用，`--every` 是个
  工作流输入）。**假设一次就会稳稳地画错一段，而画面看着完全正常。**
  现在 probe 记它；读不到的从**格子的个数**反推：末页没填满的格子是纯背景色、
  数得出来，而「格数＝按这个间隔排出来的时刻数」把间隔卡得很死
  （几十格的时候区间不到 0.1 秒），候选值里最多活得下来一个。
  31/33 份认得出来，剩下 2 份是目录里混着两趟的产物——那两份**报错，不画**
- ⚠️ **格高要从源片长宽比算，不能从墙的高度反解。** 一张 410px 高的墙既是
  「2 行 × 202」也是「4 行 × 98」，两种读法切出来是完全不同的格子而**都不报错**。
  拼图那步写的是 `scale=360:-2`，所以这个数算得出来
- ⚠️ **末尾那一格可能根本没抓出来**：`-ss` 落在最后一帧之后时 ffmpeg
  **不报错也不写文件**，而拼图那步数的是真文件、不是时刻表（`eala-zheng`
  末格 309.5s / 片长 309.568s，差 0.068 秒）。**少的一定在末尾**，
  所以前面每一格的对应关系一个都不动——截掉即可，但要说一声
- **认「这几张墙是不是同一趟出的」看页高**：除末页外每页都该是满 5 行。
  后一趟只覆盖前几张时目录里会混着两种版式（`hewitt-interview` 的
  `contact_00` 两行、`01~08` 五行），那种目录反推出来的间隔一定是错的

#### ⚠️ 而混着的根因是：拼图那步只清 `frames/`，不清上一趟写下的墙

账号所有者 2026-08-05 问「这已经是历史了，不改也不会影响到后面吧」——
**目录是历史，机制不是。**

`contact_sheet` 是**按序号覆盖**的（`{prefix}_00.jpg`、`_01.jpg`…），而它写之前
只 `unlink` 了 `frames/*.jpg`。于是**上一趟出十张、这一趟只出两张时，后面八张
原样留下**，同一个目录里摞着两趟的产物——而它们**看起来一模一样**：都是网球
缩略图，都烧着时间码，谁也认不出来。

- **后果不只是机器读错**。人翻缩略图墙挑段时，翻到的也是上一趟的画面，
  而写进 spec 的窗口会指到一段根本不存在的内容
- **触发条件很常见**：同一条源片用更大的 `--every` 再探一遍，或者加上
  `--from/--to` 只看一段——两样都会让这一趟出得比上一趟少
- 修法是写之前清掉**自己这个 prefix** 的旧墙。⚠️ **只清自己的**：
  `contact` 和 `score` 是两次独立调用，互相清就是把对方刚写好的删了
  （判据两个方向都反向验证过）
- 判据**真跑一遍 ffmpeg**（造一条 5 秒的 testsrc）。查源码里有没有那句
  `unlink` 只能防「有人把它删了」，防不住「它从来没工作过」
- 已经坏掉的两个目录（`deminaur-hewitt` / `hewitt-interview`）**不修**：
  那两条片子已经发了、不会重渲，而 `--dry-run` 和 `check_reel_cuts` 读的是
  `probe.json`（切点、死球、片长），**根本不读缩略图墙**。真要用它们看画面，
  重跑一趟 `mode=probe` 就是干净的——报错信息里写着这一句
- **抽稀不许丢两头**。一段可能有 27 格、只摆得下 7 格——两头正是在看的那件事
  （从哪儿开始、到哪儿结束），中间少看几眼只是少看几眼。而且**抽稀了要在标题上
  说**（`7/27 格`）：「摆了 7 格」和「总共就 7 格」长得一样，一次不出声的截断
  读起来就是「全看过了」
- ⚠️ **它的分辨率就是抓帧间隔**：第一格是 `start` **之后**的第一格，不是
  `start` 那一帧。「死球之后再切」那种半秒级的判断仍然归 `--dry-run` 的
  `point_ends`。**两件事别混**——这个工具回答「画面是什么」，不回答「边界准不准」

#### ⭐⭐ 2026-08-17 一次跑四条，它抓到十一处——**而 `--dry-run` 和全量测试对这十一处全是绿的**

辛辛那提第二轮四条一起做，三道本地闸依次跑下来的产出，值得记一笔各自抓到什么：

| 闸 | 抓到什么 | 这一类别的闸抓不抓得到 |
|---|---|---|
| `--dry-run`（0.2s） | 热度不够（丰塞卡世界第 27）；**十处**哑场或旁白装不下 | ✅ 只有它 |
| `preview_segments_local`（3s） | **十一处话画不符**，见下 | ✅ **只有它** |
| 全量测试（108s） | ② 句复读封面钩子（重合度 0.867）；漏写 `_tennistv_trim`；**一条已发 spec 的 a/b 填反** | ✅ 只有它 |

**那十一处按类型分成三种，而三种的严重程度差很远：**

1. **点名点反了**（2 处，最严重）：两条 spec 的「介绍主角 / 介绍对手」两段
   **和画面正好反着**——④ 段介绍 A，而那一格的近景是 B。**处置是把两段的旁白对调**，
   不是改画面：画面是按「哪一格好看」挑的，旁白应该跟着画面走。
2. **旁白比画面早了几局**（6 处）：说「六比三，第一盘四十分钟」而屏幕上还是 `4-1`；
   说「二比零」而记分条已经是 `4-2`。**记分条是烧在画面上的，读者一眼看得出**
   （CLAUDE.md「比分要跟画面对得上」）。处置是**按屏幕上那个比分重写**，
   把原来那句挪到记分条真的是那个数的那一段去。
3. **数写错了**（3 处，最贵）：「六次平分」实际四次；「把这一盘拉到五比二」——
   **逐分排出来次盘的比分走的是 `2-3 2-4 3-4 3-5 4-5 5-5`，从来没有过五比二**。
   ⚠️ **这一类不是话画问题，是事实错误**，只是**摆到画面旁边才看出来**：
   我盯着 spec 看了三遍都没发现，而预览把记分条和那句话排在同一行，一眼就对不上。

⚠️ **第 3 类说明这个工具的用处比它的名字大**：它叫「看每段窗口里是什么画面」，
实际上它是**把旁白和当时的记分条并排摆出来**——而记分条就是这场球的判据。
**「逐分是可以数的，别按印象写」**，而这一步是最后一个能发现你按印象写了的地方。

⚠️ 顺带：这四条里**三条的冷开场都被它改过**——`altmaier-musetti` 原来那一格
预览里只落进「一个人拿着拍子在走」，而 ① 段的要求是「全片最强的那一格」。
**缩略图墙是 2 秒一格，按它挑窗口本质上是猜；预览把窗口和格子对齐了，才看得见猜得准不准。**

#### ⭐ 这个工具要跑**两遍**，第二遍在旁白定稿之后——两遍看的不是同一件事

2026-08-15 `kovacevic-khachanov`：挑段的时候跑过一遍，写完旁白之后又跑了一遍，
**第二遍当场抓到三处「整句旁白压在对手身上」**，而 `--dry-run`、全量测试
（1960 条）、`--check-narration` 全是绿的——**这一类没有任何机器查得了**。

| 什么时候跑 | 在看什么 |
|---|---|
| **挑段的时候** | 这一格好不好看、边界切在哪儿、有没有跨镜头 |
| **旁白定稿之后** | **这句话说的那个人，是不是画面里那个人** |

那三处长这样（`_editing_why` 里逐条记着）：0.5–3.6 是**科瓦切维奇在发球**，
而我写的是「他先赢下了第一盘」——「他」指哈恰诺夫；83.6–86.6 是**科瓦切维奇
在自己的发球局握拳**，而我写的是「他自己的三个没兑现」。改法有两种，都用上了：
**把名字写出来**（「第二盘**哈恰诺夫**被破一次」），或者**换一句说画面上那一下
的话**（「每一次都被保了下来」——说的正是他握拳的那一下）。

⚠️ **同一站同一天的两个人靠「看着像」必然认错。** 这一场两个人都是白底球衣、
都戴白帽，缩略图里根本分不开。判据要挑**大面积、低饱和、不在色域边缘**的那一样
（和「球衣颜色不能当认人判据，场地颜色可以」是同一条的下一层）：
**短裤颜色**（科瓦切维奇藏青 / 哈恰诺夫浅蓝）和**肩块**（科瓦切维奇有深色肩块、
哈恰诺夫没有），外加**记分条上的发球指示点**——那是转播自己写的，最硬。

⚠️ 反过来也要看：这条片子里有**两处本来就对得上、不用动**（赢球庆祝配「赢他的
人上次赢球还在五月」、举拍致意配末尾那一问）。**核对不是为了改，是为了知道哪几处
要改**——三改二不改，才说明看过。

##### ⚠️ 全屏台标是**强**自证，比场边广告板硬一档

同一条片子里球场名是这么定下来的：源片 30.5 秒挂出一次全屏台标，一行写着
`ROUND 1 | P&G STADIUM COURT | 0:52`，裁出来放大读过，`0:52` 和 flashscore
`df_sui_1` 的 `RC÷0:52` 一分不差。所以 `cover.scoreboard.court` 写的是
`P&G Stadium Court`，**不照抄同一站另一条片子推出来的 `Center Court`**。

CLAUDE.md 上面那张「自证强弱」表原来只有两档，现在补上中间这一档：

| 强 | 中 | 弱 |
|---|---|---|
| 记分条、球场地面刷的赛事名、球员球衣 | **转播的全屏台标**（轮次、球场、用时、双方全名和盘分） | 场边广告板、横幅、赞助商灯箱 |

它比广告板硬，是因为**它讲的是这一场**（用时还能和数据源对上）；比记分条软一点，
是因为它是后期叠上去的图形，而记分条是当下状态。

#### ⭐⭐ 2026-08-25：**过了一千格，整墙的顺序是乱的**——而每一格烧的秒数还是真的

`usq-2026-kei14`（锦织圭 2014 美网半决赛，8677.76 秒、每 3 秒一格、**2893 格**）。
`preview_segments_local.py` 按我写的窗口 `2959~2968s` 摆出来的格子，烧着的是
**5418.5 / 5421.5 / 5424.5s**；同一批里 223 秒的 `dimitrov` 一格都没错。

根子在 `contact_sheet` 这一对：帧写成 `f{index:03d}.jpg`，拼墙前
`sorted(frames.glob("*.jpg"))`。**过了一千帧文件名变成四位，而字典序里
`f1000.jpg` 排在 `f100.jpg` 和 `f101.jpg` 中间**，于是整墙的顺序被打散。
拿真产物验过，三张墙的第一格逐个对上（这是判据，不是推理）：

    sheet 32 → 帧 1781 → 5343.5s     sheet 95 → 帧 957 → 2871.5s
    sheet 96 → 帧 987  → 2961.5s

⚠️ **它一个字都不报**：墙照样出得来、张数和格数都对、**每一格照样烧着自己那一秒
的真时间码**——错的只有「第几张墙上装的是哪一段时间」。所以

| 你怎么挑的段 | 结果 |
|---|---|
| **读格子上烧的秒数**（翻墙挑画面） | ✅ **是对的**——那个数就是这一格的真时刻 |
| **按 `sheet_stamps()` 反推**（预览工具、按第几张第几格算） | ❌ 指到别的画面上 |

也就是说**预览工具和人翻墙这两条路会给出相反的答案，而两边看起来都很正常**。
这次先怀疑的是自己写的窗口，去查才发现是工具错了。

⚠️ **短片子永远踩不到**：要 1000 格才够得着，按 `every=2.0` 是 33 分钟以上的源片。
这条线绝大多数源片是三五分钟的官方集锦，所以它躺了很久。
（`score_*` 那一版同样中招，它是 `columns=4`、每墙 **20** 格。）

修法是**帧的顺序从生成时的列表来，不从 `sorted(glob)` 来**（顺手把序号补到五位）。
判据 `test_缩略图墙的格子顺序不许靠文件名排序` 钉的是顺序本身、不是文件名的位数：
拼墙拿到的那串帧烧上去的秒数，必须和 `sheet_stamps()` 算出来的逐个相同。
反向验证过（退回 `f{index:03d}` + `sorted(glob)` 当场红）。

⚠️ **已经落库的那种乱墙不用重新 probe 也能读**：乱序是确定的，
`帧序号 → 字典序位置 → 第几张第几格` 反一遍就能把真格子抠出来——
`usopen-qualies-2026` 那三段锦织圭就是这么核的（记分条读到
`DJOKOVIC 4 6 6 3 / NISHIKORI 6 1 7 5 · Match Point #2`）。

### 返工比慢更贵：把「渲到一半才发现」变成「第 5 秒报错」

今天 eala-fernandez 渲了 **3 轮**、wang-samsonova **2 轮**；最近 30 趟
match-reel 合计 **211 分钟**（平均 7.0 分/趟）。**单趟 6 分钟可以接受，
渲三次才对不行。** 所以这条线上最便宜的改动不是再快几秒，是让错误早点出声。

三个入口，都落了判据：

| 开关 | 干什么 | 实测 |
|---|---|---|
| `--dry-run` | 只校验 spec 形状，不下载不渲染 | **0.18s**（九条 spec 全过） |
| `--cover-only` | 下载完就出海报，跳过分段/TTS/编码 | **4.3s**（本地，源片已在） |
| （默认）`validate_spec` 前移 | 形状错和缺依赖都死在下载之前 | 第 5 秒 vs 第 2.5 分钟 |

**为什么 `parse_segments` 能前移**：它只用 `sources` 的**键**去校验段落引用
（`{s.source} - set(sources)`），一个下载下来的文件都不碰。可它原来排在
第 1543 行，而下载在第 1516 行——段落字段写错、`inset.corner` 写错、缺字段、
贴图路径不存在这四类错，每一类都要先付一次 392 MB 的下载。

**为什么 `--cover-only` 值得**：封面是全流程返工最多的那一屏（本文件里
68 行、10 个专门段落，每段都是一轮返工换来的），而它在完整 render 里
**第 63 秒就完成了**——run 30624808733 的时间线是
`10:52:54 开跑 → 10:53:57 封面编码完 → 11:00:15 成片`。`build_cover` 不依赖
segments / tracks / TTS / voice（查过，一个都不用），所以「下载 → 出海报 →
退出」是干净的一条路。

⚠️ **形状校验里不许混进环境检查。** `_preflight_cutout` 查的是「这台机器有没有
rembg」，不是「这份 spec 对不对」——混进去的话，本地想 dry-run 一下 spec 就得
先装 176 MB 的抠图模型。它单独留在 `render()` 里，照旧排在下载之前。

⚠️ **加了 `--cover-only` 之后，一条既有测试当场误判。**
`test_封面停多久跟着配音走` 断言
`body.index("cover_secs = cover_length(") < body.index("build_cover(")`，
而快速预览那条路也调 `build_cover`（拿默认时长渲一张海报就退出——海报本来
就与时长无关），它排在最前面，于是 `index()` 撞上它。**测试没错，是判据
太宽**：改成盯真正接收 `cover_secs` 的那次调用（`"cover_secs)]"`），
反向验证过它照样拦得住原来那个错。又一次「判据宁可窄，不可宽」。

#### ⚠️ 而这两个开关落地当天就够不着了——工作流的 `mode` 没跟着改

`--dry-run` / `--cover-only` 加进 CLI 的那个提交，**没有动
`.github/workflows/match-reel.yml` 的 `options: [probe, render, cookies]`**，
render 那一步也没传这两个开关。于是：

> **能用的那台机器上没有开关，有开关的那台机器上没有源片。**

源片只在 runner 上活过几分钟（沙箱下不动 YouTube），所以这条能力在落地
之后**等于零**。又一次「写了不等于跑过」，只是这次卡在工作流入口而不是函数里
——和「加新能力就要同时改三处」是同一家的。

现在 `mode` 多一档 **`cover`**：下源片 → 出海报 → 退出，不分段、不合语音、
不编码。判据两头都钉（`test_快速预览的开关要在工作流里够得着`）：
**入口够得着**（options 里有 cover）**而且那条路真的短**（不出现
`push_reel.py` / `--voice` / `--rate`）——只钉前者的话，
有人把 cover 那一步写成完整 render 也照样绿。

#### 渲完的成片会因为清理那一步失败而整趟丢掉

`if:` 里**不含状态函数时，GitHub 会隐式和上 `success()`**。而「丢掉不进仓库
的中间物」末尾有个体积兜底会 `exit 1`（连着栽过四次，最近一次是多源之后源片
改名 `source_r1.mp4`，run 30603686748）——那一刻**成片已经渲完了**，
可上传和提交都被跳过，六分钟整趟作废。render 自己失败时同理：
`subtitles.ass` / `poster.jpg` / `part_*.mp4` 一个都拿不到，只能翻日志猜。

- 上传改成 `if: always() && …`
- ⚠️ **位置不许往前挪**。清理那一步是**先 `rm` 再做体积检查**，所以走到上传
  时源片已经删干净了；挪到清理之前会把 392 MB 的源片一起传上去。
  判据把两头都钉住——「修好」的方向反了一样是坏的

#### ⭐ 闸红在一个确定性判据上，别再渲一次去确认是不是巧合——第一次的日志已经给了答案

2026-08-24 `gauff-pegula-cincinnati-2026-final` 那次量出来的：从 probe 到推送
总共约 3 小时 15 分，而**三趟 `mode=render` 加起来只有约 21 分钟机器时间**
（run 32681014183 7m19s、32684580629 7m14s、32688290484 6m57s）——机器时间只占
总耗时的 11%，和「一条片子为什么要一个半小时」那次的 19% 是同一个形状。这次新的
发现是：**其中一趟渲染是白烧的，而且不烧那一趟本可以提前就知道结论。**

第一次 render 死在 `check_reel_landed` 的数字静音闸上：`[不合格] 封面之后还有
1 秒是数字静音：[225]`——日志里那张逐秒响度表已经完整打出来了。第二次 render
用的是**同一个 commit，一个字节的代码都没改**，跑完之后逐秒响度表**一字不差**。
也就是说第二次那 7 分钟买到的唯一信息是「这不是偶然」——而这件事**不需要真的
花 7 分钟去验**：

    工作流那一步「缓存 TTS（同文旁白跨趟不重合成）」（match-reel.yml:637）早就
    说明了——TTS 走内容哈希缓存，`OUTRO_NARRATION` 是常量、`voice`/`rate` 没变，
    这段口播的合成结果**必然**逐字节相同。剩下唯一要证明的是"响度表出来的
    静音是不是那份口播本身自带的"，而**这一点第一次失败的日志已经回答了**：
    它是纯计算的确定性检查（ffmpeg 对同一份 mp3 跑 `silencedetect`），不涉及
    任何随机性来源。

判据：**闸红在一个纯计算、无随机输入的判据上，且这次改动前后相关内容没变**——
这种情况下"是不是巧合"这个问题的答案，从"这一步有没有随机性来源"就能推出来，
不需要真的再跑一遍去验证。会咬人的地方在于**这个判断本身需要先弄清楚"这一步
到底有没有隐藏的随机性"**（TTS 合成通常是有的——语气、停顿会因为服务端 A/B
而略微漂移，只是这条线用了内容哈希缓存把它锁死了）；不确定就去读实现或问一句，
比真渲一趟便宜得多。

⚠️ **这不是「怀疑闸的判据」，是「怀疑重跑这个动作本身有没有信息增量」。**
闸红的第一反应仍然是先读那次失败的完整日志——`check_reel_landed` 这类工具
"要把不合格的也列出来"（本文件反复记的老规矩），这次也确实做到了：
第一条日志就已经把"哪一秒、多响"全部打出来，诊断需要的证据从头到尾都在，
缺的不是数据，是把数据和"这一步没有随机性"这两件事接起来。

顺手把这条线上唯一的盲区补掉了：`--check-narration`（本地 1 分半快路）原来
只 `synthesize()` 编号的那些段，从没碰过 `synth_outro()`/`outro_length()`——
这正是这次 bug 藏身的地方，而它只有在完整 render（~7 分钟）里才会被撞见。
现在 `--check-narration` 顺手在同一个临时目录里也合成、量一下片尾，把
`outro_length()` 算出来的秒数打进摘要行——纯诊断打印，不新增判据（`outro_length`
自己已经会 `max()` 自纠），但下一次这一类风险（TTS 尾音渐弱被算错）再出现，
本该在这 1 分半的本地路里就看见，不用再等一趟渲染。

#### ⭐⭐ 而这条线的耗时报表，结构上只统计得到成功那一半——所以它永远是绿的

2026-08-24 复盘时才发现的，比上面那条更根本。这条线本来就有**两样**计时，
而两样**都只在成功那一趟才落地**：

| 计时 | 在哪 | 为什么失败那趟留不下 |
|---|---|---|
| `report_timings()` 打的耗时明细 | `build_match_reel.stage()` 累积 | 它是 `render()` 的**最后一行**，中途抛异常就走不到 |
| `production_sla`（`video_sla.py`） | 写进 `render.json` | `render.json` 是**渲成了才写的** |

于是 `pipeline_health.sla_health()` 的样本天生只有成功那一半。**当天实测**：
`output/2026-08-24/reel/` 下 4 份 `render.json` **全是 `met: true`**、中位
346s——报表一片绿；而同一天 `match-reel` 真实跑了**二十多趟**：

    gauff-pegula-cincinnati-2026-final   渲 3 趟（2 趟白烧，约 14.5 分钟机器时间）
    zheng-us-open-outlook                从早到晚渲＋推**五轮**（每轮各一次微信推送）

⚠️ **这不是「报表不准」，是「报表问错了问题」。** 它问的是「成片渲得快不快」，
而这条线真正贵的从来不是单趟多快（6~7 分钟，早就榨过三轮），是**渲了几趟**。
一个只统计成功趟的 SLA，**闸做得越好它越好看**——每多一道前置检查，就多一条
「本来会出废片、现在提前红掉」的 run，而那条 run 在报表里根本不存在。
本文件「⚠️「一次成功率」要把「闸拦下来」和「真出错」分开数」记的是同一个形状，
只是那次说的是别用红绿比，这次是**连样本都缺了一半**。

**修法是让失败也留下一行**（`tools/pipeline_timing.py`）：render 的调用点包一层
`try/finally`，无论成败都把 `outcome`、**死在哪一步**（`last_stage`）、每一步
花了多久写进 `outdir/timing.json`；失败那一支顺手补打一次 `report_timings()`
——那张表原来在最想看它的时候恰好丢掉。

⚠️ **成本上刻意做小，不新增提交**：`timing.json` 和 `render.json` 并排放在
outdir 里，成功那趟工作流本来就 `git add "$OUTDIR"`；失败那趟 outdir 不进仓库，
但 `actions/upload-artifact` 是 `if: always()` 的，那一行仍在构件里查得到。
**不给它单独开一次 commit**——每趟渲染都提交一次会给 main 制造提交噪音，
还连带触发一堆 CI，那是拿一个统计需求去换一条更吵的主干。

⚠️ **它是仪器，不是闸**：写台账失败只出声、不抛，**永远不许把一趟渲染带崩**。
一个会让生产失败的埋点，下一个人一定会把它拆掉。

`python3 tools/pipeline_timing.py` 直接出跨趟报表：渲了几趟、白烧多少分钟、
失败都死在哪一步、哪一步最慢（只算成功的趟——失败那趟是半截的，混进去会把
中位数拉偏）。判据 `test_渲染失败也要在耗时台账上留一行` 钉两头（行为 + 位置），
三个方向分别反向验证过，红在三条不同的断言行。

⚠️ **还有一半没有仪器，别把这份台账读成「全都量到了」。** 它量的是
**机器时间**；而当天从 probe 到推送 3h19min 里，机器时间只占 11%，其余是
两次失败之间那 57 分钟和 53 分钟——诊断、读日志、改代码、跑本地全量。
那一半目前没有任何埋点，这份台账**不覆盖它**，也不该被引用成覆盖了。

### 能并行的是安装，不是编码——x264 已经吃满四核

账号所有者问「能否并行」。量了才知道**编码这块没有余量**：同一段素材
`slow`/`crf 18`，`-threads 1` 84.0s、`-threads 2` 42.3s、`-threads 4` 24.1s
——4 核 3.48×，**87% 的并行效率**。把成片切块并发编只能榨那剩下的 13%，
还要多担字幕跨块和拼缝的风险。**别再去试这条**。

真正能并行的是**装依赖**：apt（字体 + ffmpeg，42s）和 pip（25s）互不相干，
串起来 1:07，丢后台并行取长的那个 ≈ 0:44。

⚠️ **`wait` 不带参数只返回最后一个后台任务的状态。** apt 挂了而 pip 成功，
这一步照样绿，然后 render 在第 5 分钟死在「找不到 ffmpeg」——又一次
「兜底出事的时候不吭声」。必须按 PID 分别 `wait $APT ||` / `wait $PIP ||`，
并在装完当场 `ffmpeg -version` 自证。~~判据在
`test_并行装依赖要分别检两边的退出码`。~~

⚠️ **这条判据已经不存在了，因为它的主语没了**：`match-reel.yml` 现在不再并行装
依赖——apt 走 `apt_retry`（顺序、带超时重试，见「装 Chromium 不许没有超时和重试」
那节），pip 拆成几步各装各的，`wait $APT` / `wait $PIP` 这个形状全仓库已经零命中。
**判据跟着主语一起撤掉是对的**（本文件「主语没了就得换判据，留着它就是一条常年
红」说的正是这个）；留在这儿是因为**那个 bug 本身没过期**——哪天又想并行装依赖，
`wait` 不带参数照样会吞掉 apt 的失败。**重新并行之前先把这条判据一起写回来。**

第二处是**把等待挪出关键路径**：PO token provider 本来就是 `docker run -d`
起的，却在原地轮询最多 50 秒。探活挪到 render 之前，中间隔着装 Chromium、
写复制页、提交复制页几步，那 12 秒基本被吃掉。判据要求两者之间**至少隔两步**
——紧挨着就和原地等一回事。

### 单趟 filter_complex：算了一遍，省 9%，不值得拿结构换

「把分段编码整个去掉、一趟 `filter_complex` 直接出片」这条路探过，数在这儿，
**别再算第二遍**。30 秒 1080p 切三段、裁 3:4、四核：

| | 分段 | 成片 | 合计 |
|---|---|---|---|
| 现在（`ultrafast`/12 → concat → `slow`/18） | 5.24s | 15.72s | **20.96s** |
| 单趟（三路 filter_complex concat → `slow`/18） | — | — | **17.16s** |

省 3.80s，也就是**分段那一步的 73%**。折算到 runner 上（分段编码那一栏现在
是 47s 上下）约 **34 秒**，占 render 一步的 9%、占整条推送的 5%。体积几乎
一样（15,815,811 : 15,638,212 字节）。

**不做，因为换掉的是结构**：每一段现在可以有自己的裁切窗口、`track`、`cx`、
帧率归一，出问题时能单独重渲那一段；`--cover-only` 4.3 秒看一眼封面也是靠
这一层。合成一趟之后这些全要写进同一个滤镜图，**而一条片子返工一轮的代价
（六分钟）远大于 34 秒**——今天这一整轮优化的出发点本来就是「返工比慢更贵」。

### 60 fps 那条：算了一遍，不值得换

`wong-brooksby` 是九条里唯一的 60 fps（源片是 1180×2114 的竖屏录屏），它那条
run 的 render 是 7 分 22 秒；其余八条都是 25 fps，成本按帧数等比例低下去。
所以「render 七分半」是**最坏情况，不是常态**。

降到 30 fps 看着像个大杠杆（60 → 30 是整数倍，按本文件「50 → 25 隔帧丢得整整
齐齐」那条推理，画面不会一顿一顿）。**但把数算出来之后不值得**：

    成片那一步 slow/crf18，同一段 10 秒素材
      60 fps  20.33s  7,327,046 字节
      30 fps  13.87s  6,202,653 字节   ← 只快 32%，不是 50%

帧数减半但每帧之间的运动量翻倍，运动估计没跟着省。折算到 runner 上是
190.9s → ~130s，整条 run 省一分钟出头，**而且只对九分之一的片子有效**——
拿网球这种高速运动的流畅度换这一分钟，方向反了。

记在这儿是为了**别再算第二遍**：这条路探过，数在上面。

#### ⚠️ 2026-08-21 账号所有者的 f7b2501（「把渲染稳定压到 10 分钟内」）把这条路重新走了一遍——不是没看过上面这段，是权衡的东西变了

上面的结论是拿**一条片子**（`wong-brooksby`，竖屏录屏，全库九条里唯一的
60fps）算的：省 32%、只影响九分之一的片子、拿网球的流畅度换一分钟不划算。
`resolve_fps()`（`tools/build_match_reel.py`）现在**又把它加回来了**——
50/59.94/60 这几档高帧率源整除降到 24~30fps，理由写在函数自己的
docstring 里：「像素量却能减半，这是高帧率源出片最稳的时间杠杆」。

**两边都没错，权衡的前提换了。** 当年是在问「值不值得为一条片子的画质
让步」；这次账号所有者要解决的是「一批片子的渲染时间要稳定压在 10 分钟内」
——当高帧率源片的占比比当年那"九分之一"更高、渲染时长本身开始变成瓶颈时，
省下来的时间不再只是"快一点"，是"稳不稳定在预算内"。**这条注释不是要否定
上面的分析**（那笔账当年确实是这么算的，数据也没错），是要接住它——不然
下一个人（包括下一次的我）读到"不值得换"，会以为代码里那句 `divisor` 逻辑
是漏改的旧账，回头把它删掉，那才是真的倒退。

⚠️ **这条没有留量化数据**（f7b2501 的提交信息只写了目标——「压到 10 分钟
内」——没有留一张类似上面那张"20.33s vs 13.87s"的对照表）。真要再往下推，
应该量的是"降采样之后，网球回合的流畅度损失有没有被读者感知到"，而不是
"省了多少秒"——省时间那半账 f7b2501 之前就算过了，缺的是画质那半账。

### 哑场闸软化成提示，同一次提交、同一个理由

同一个提交（f7b2501）把 render 时的单段哑场闸从硬拦截改成了只打印
（"现场声仍在，所以不阻断渲染"／"超 4 秒留白仅提示，不阻断渲染"）。

⚠️ **底线还在，软化的只是中间那一档。** 上面「哑场那道闸」整节反复强调
「渲一次拿 render.json 的真实结果做决定」——那句话现在要按两层读：
**数字静音**（真的一点声音都没有）仍然是硬闸，`check_reel_landed.py`
照旧拦（`SILENCE_FLOOR_DB` 那道，推送前的最后关卡没有松）；
**field/现场声还在、只是这一段旁白没填满整个窗口**，这一档从"红了就不许
过"改成了"报出来、留给人判断"。CLAUDE.md 别处那句「拿真实产物判，别在
离线估阶段死磕」说的是**离线估**（`speech_seconds()`）不该拿来拍板，
render 时的真实结果才说了算——这条现在仍然成立，只是"说了算"从"直接拦"
变成了"把真实结果摆出来，人来判"。

⚠️ **这处改动同样没有留下量化对照**：软化之后有没有真的有片子因为这一档
放宽而在观感上变空，还没有人专门去听过、统计过。真要收紧回去或者验证这个
放宽站不站得住，量的方法就是本文件反复用的那个——拿几条软化前后的片子
逐段听一遍，看"提示但不拦"的那些段落听感上是不是真的接受得了。

### 反向验证之后要清 `__pycache__`，还原可能是隐形的

钉 `FINAL_CRF` 那条测试做反向验证时踩的：`sed` 改成 `"23"` → 跑测试（红，对）→
`cp` 还原成 `"18"` → `grep` 确认磁盘上是 18 → 跑全量，**那条测试仍然红，报的是
「被改成了 23」**。

原因是 Python 判字节码缓存失效看的是 **(mtime, size)** 这一对：`"18"` 和 `"23"`
**字节数一样**，而 `sed` 和 `cp` 发生在**同一秒内**，两个判据都没变，于是它照旧
用了从 crf-23 编出来的 `.pyc`。

**它不吭声**——源码里明明写着 18，`grep` 也确认了，只有 `import` 出来才看得见：

```bash
PYTHONPATH=src:tools python3 -c "import build_match_reel as r; print(r.FINAL_CRF)"
# 磁盘 18，import 却给 23
```

所以：**改完源码要验的是 `import` 的结果，不是 `grep` 的结果**（和「判断译名改没改对，
直接调 `player_zh()`，别看文件内容」是同一条）。反向验证这类「改一下再改回来」的
操作，还原之后加一句 `find . -name __pycache__ -exec rm -rf {} +`。

#### ⚠️ 更阴的一种：连着做两次反向验证，它们会互相遮蔽

上面那条说的是「还原之后」。2026-08-02 又栽了一次，形状不同：**连着拆三道闸，
第二道报绿**——而拆掉一道安全闸还能通过，看上去就是「这条判据是恒真的」。

真相是那三次替换都写成了 `    if False:`，而被换掉的两行**字符数正好相同**：

    if not push_is_auto(copy_path):     ← 35 字符
    if (outdir / MARKER).is_file():     ← 35 字符

于是第二次改完的文件和第一次**字节数一模一样**，又在同一秒内，
`(mtime, size)` 两个判据都没变——第二趟用的是**第一趟编出来的 `.pyc`**，
那里面第一道闸是拆掉的、第二道闸是好的，所以它当然绿。

清掉 `__pycache__` 重跑，第二道当场红。所以：

- **每一次反向验证之间都要清**，不只是最后还原的时候
- 一道闸拆掉却还绿，**先怀疑缓存，再怀疑判据**——两者的表现完全一样，
  而「判据恒真」这个结论会让人把一条好测试删掉

### 20 MB 不是硬指标，别拿画质去换

账号所有者的原话：「**不要舍弃画质，没有硬性要求 20mb**」。

20 MB 是 jsDelivr 的单文件上限，超了 `push_reel` 会自动退回 `raw.githubusercontent.com`
（国内更慢）。我一度把它当成一个要满足的指标，还列了「剪短 / 降 crf」两条路让
账号所有者选——**那个前提本身就是错的**：

- 「赛场之上」这条线的码率三条几乎一样：**3315 / 3517 / 3779 kb/s**
- 20 MB 按这个码率只够 **44 秒**
- 而已发的 69 秒和 83 秒那两条是 28.8 和 32.9 MB —— **从来没有一条进得去**

所以「剪短就能走 jsDelivr」是我算都没算就说出口的，剪到能进去等于把栏目拆了。
真要挤进 20 MB 只剩降 crf 到 23 上下——拿画质换一条通道，方向反了。

**片长可以商量，画质不商量。** 大威那条从 2 分 38 秒剪到 1 分 35 秒
（59 → 42.6 MB）是片长这一侧的事，`FINAL_CRF` 一个字没动。判据落在
`test_成片的编码参数不许为了压体积往下调`——它拦的不是手滑，是下一次有人
重新论证「压一压也看不出来」。

顺带一条方法论：**给账号所有者列选项之前，先把每条路的数算出来。** 那次我把
「剪短」和「降 crf」并列成两条通往同一个目标的路，其中一条根本到不了；
对方按我给的框架选了，等于替我承担了我没做的算术。

#### ⚠️ crf 不许动，preset 是另一件事——2026-08-14 按实测从 `slow` 换成 `medium`

上面那条「画质不商量」管的是 **crf**，而 `FINAL_PRESET` 被它连坐了很久。
**两件事各管各的，别混成「编码参数」一坨**：**preset 决定编码器花多少时间去找
省比特的编法，crf 决定留多少画质**。动 crf 是动画质，动 preset 是拿体积换时间。

来路：一趟 render 862s，其中 render 步 747s（87%），而**「烧字幕+成片」一步就是
423.7s——占整趟 49%**，这条线上最大的一块，偏偏它从来没被量过。`slow` 当年不是
拍的，它有一个真实约束：**成片要塞进 git**（100 MiB 服务端硬拒），比特省一点就
多一分进仓库的机会。**2026-08-13 起成片一律走 Release，那个约束没了**，
所以这笔账重算了一遍（四核沙箱、真素材、照真实流水线的形状走）：

| 样本 ① 多伦多看台实拍 16s/25fps（人群，高细节高运动） | 编码 | 体积 vs slow | PSNR |
|---|---|---|---|
| `slow`（改前） | 32.18s | — | 46.580 |
| **`medium`（改后）** | **19.81s** | **+1.6%** | **46.536** |
| `veryfast` | 7.91s | −3.3% | 45.731 ← 陷阱 |

| 样本 ② 品牌片尾母版 12.9s/60fps（图形，低运动量） | 编码 | 体积 vs slow | PSNR |
|---|---|---|---|
| `slow`（改前） | 18.90s | — | 52.461 |
| **`medium`（改后）** | **13.66s** | **+6.1%** | **52.278** |
| `veryfast` | 6.26s | −24.9% | 49.705 ← 陷阱 |

**结论：`slow` → `medium` 快 28~38%，大 1.6~6.1%，画质量不出差别**——PSNR 差
0.04~0.18 dB、SSIM 差 3e-5~5e-5，都远在看得出来的量级以下。折到那 423.7s 上是
**省 117~163 秒**；真实成片以比赛画面为主，性格更贴样本 ①，所以偏 163 那头。
体积代价落到一条 80 MiB 的片子上是 +1.3~2.5 MiB。**`FINAL_CRF` 仍然是 18，
一个字没动。**

⚠️ **不要再往快里推，`veryfast` 是个陷阱，而它伪装成「又快又省」**：同样 crf 18，
它出来的文件比 `slow` **还小**（−3.3% / −24.9%）。**同一个 crf 下 preset 越快
只会越大——它反而变小，就说明它根本没在同一个画质上编**，PSNR 跟着掉了
0.85 / 2.76 dB。**光看时间和体积会把这读成两头都赚，只有量 SSIM/PSNR 才看得见。**
`fast` 是这条坡的起点，`medium` 是最后一档「快了但量不出画质差」的。
判据 `test_成片preset按实测定在medium不许再往快里推`。

⚠️ 这一档只管**成片**这一次编码。分段和封面那两个中间产物走 `PART_*`
（`ultrafast` / `crf 12`），道理完全不同（「马上要被重编，所以省比特没用」，
见上面「中间产物要花的是比特，不是时间」）——**两者别互相搬**。

⚠️ **也别搬到解说片那条线上。** `FINAL_PRESET` 是 `build_match_reel.py` 的，
而解说片（`explainer.py`）现在仍然是 `-preset slow -tune stillimage -crf 26`，
**故意没动**：那条线的成片是**静止画面的幻灯**（每屏 `-loop 1` 铺一张卡，
唯一在动的像素是烧进去的字幕），编码这一步整条流水线里只占几十秒——
上面那笔账的分子（423.7s、占整趟 49%）在那儿根本不成立。**同一个参数名，
两条线的账完全不同**，要动它得照上面的口径在那条线上单独量一次。

#### 100 MiB 是 git 的限制，不是内容的限制——超了换一条路，别砍片长

账号所有者 2026-08-02 的原话：「**我的基础要求是保证内容和画面质量，文件多大
都没关系啊**」「**不要砍片长**」「**我怕故事讲解不完整**」。

**片长不设上限。** 唯一硬的是 `git push` 那条路：单文件超过 100 MiB 服务端直接
拒（那条 3 分 04 秒的片子渲出来 104 MiB，push 被拒，五分钟白跑，run 30725160625）。

我第一反应是砍片长，还照着这个反推出一个「上限 165 秒」写进 `load_spec` 当闸——
**方向反了**，那正好把账号所有者要的东西挡在门外。正解是**换一条落脚点**：

| | 额度 | 为什么行 / 不行 |
|---|---|---|
| **Release 附件** ✅ | 单个 **2 GB** | 公开仓库下载不计流量额度，链接稳定（302 到 `release-assets.githubusercontent.com`，和 >20 MB 时走的 raw 同一量级）。顺带仓库不再每天胖一百多 MB |
| Git LFS ❌ | 1 GB 存储 + **1 GB/月流量** | 一条片子一百多 MB，一次推送很多人同时下载就把月流量打光（超出计费）；而且 `raw.githubusercontent.com` 对 LFS 文件返回的是**指针文本不是视频**，微信里那个 ▶ 按钮直接坏掉 |
| 外部对象存储 ❌ | 无限 | 要另一套密钥和账单，为一条片子不值当 |

~~现在超过 **95 MiB** 自动走 Release~~ ⚠️ **2026-08-13 那道 95 MiB 的闸拆掉了，
现在成片不分体积一律走 Release**（权威出处是 `tools/build_match_reel.py` 里
`MEASURED_REEL_KBPS` 上面那段注释，和 `match-reel.yml` 的 Release 步骤；判据
`test_成片一律走Release不进git`）。流程其余部分没变：工作流传附件 → 探一次可达
→ 链接写进 `render.json` → **删掉本地那份**（不然 `git add` 还会把它吃进去）→
照常提交其余产物。`push_reel.video_url()` 优先读 `render.json` 的 `video_url`。

⚠️ **拆掉的理由正是这条闸「几乎从来走不到」**：成片普遍 30~80 MiB，够不着
95 这道门槛，于是几乎每条都进了 git——量出来 `.git` 已 **6.0 GB，其中 4.93 GB
是 mp4 blob（130 个）**，仓库每月胖一两个 GB。账号所有者 2026-08-13：「当前
代码库太大了，好多资源是不是不用放在这里代码库里」。**一个几乎从来走不到的
兜底，正是「兜底出事的时候不吭声」的形状**——它每天都在「成功地」不做事。

⚠️ **存量不动，只改增量**：历史片子的 ▶ 链接钉在 main 的文件路径上
（`raw.githubusercontent.com/…/main/…`），已发的消息收不回来，所以 ≤95 MiB 的
老成片留在 git 一个不动。

~~**两条路都要认。**~~ ⚠️ **现在只有一条路了**，`mode=push` 的预检相应也只认
Release 这一条。（当年那句的来路：预检原来只查「mp4 在不在这个 commit 里」，
走了 Release 的片子会被判成「还没渲」——而它其实好好地在那儿。）

实测码率（3:4 画幅、crf 18，都是从成片量的）。~~用来估这一版走哪条路~~
⚠️ **现在只用来提前报体积，不再决定走哪条路**（路只有一条）——写 spec 那一刻就
知道这一版大概多大，明显跳出量级的能在渲之前被人看见：

| | 体积 | 时长 | 码率 | 源片帧率 |
|---|---|---|---|---|
| eala-svitolina | 81.9 MiB | 158.0s | 4348 kb/s | 25 fps |
| wong-gea | 80.2 MiB | 155.8s | 4318 kb/s | 25 fps |
| eala-osaka 3 分版 | 104.0 MiB | 185.2s | 4712 kb/s | 25 fps |
| eala-osaka 2 分半版 | 94.5 MiB | 158.8s | 4990 kb/s | 25 fps |
| gea-shapovalov | 168.4 MiB | 247.9s | 5698 kb/s | 59.94 fps |
| **cincinnati-story** | 113.6 MiB | 103.3s | **9221 kb/s** | 60 fps |

⚠️ **常量现在是 `MEASURED_REEL_KBPS = 9230`，不是这张表当年那个 4990。**
末行那条把它顶上去了：同样 60 fps，它比 gea-shapovalov 还高 **62%**——因为末段
是颁奖台 + confetti + 镜头快切的高动量蒙太奇。**内容的运动量差得动这么多，
不是编码参数变了**，所以「按已有码率线性外推」在这条线上一直会低估。

⚠️ **剪短之后码率反而更高**：砍掉的是全片最静的那一段（普通回合），留下的全是
高动量的关键分。所以「按当前码率线性外推」会低估——常量取**实测最高**的那一条。

⚠️ **带数字的注释会过期，而它过期的时候不吭声。** 工作流里记着「实测 3315 /
3517 / 3610 kb/s」，那是 **9:16 时代**的数（换 3:4 之后取景窗从 608px 宽变成
810px，码率高了三成），我照着它算 3 分钟只有 78 MB。判据写进注释就要注明它是
**哪个口径**下量的，换了口径的人才知道要重量。

**成片按静图编码。** 这条片子是**静止画面的幻灯**：每屏 `-loop 1` 铺一张卡，
全片唯一在动的像素是烧进去的字幕。拆开量过（wildcard，2 分 56 秒，7.85 MB）：

| | 体积 | 码率 |
|---|---|---|
| 视频 | 5.75 MB | 264 kb/s |
| 音频 | 2.22 MB | **102 kb/s** ← 白给的 |

`-b:a 160k` 是开在 **24 kHz 单声道**的 edge-tts 语音上的，超配两倍有余
（编码器根本填不满）。现在是 `-preset slow -tune stillimage -crf 26` +
`-b:a 64k -ac 1`，实测 −20%，从原始卡片新编还更省。
**crf 别再往上推**：深绿底上的浅色小字 26 以上开始出块，改之前抽一帧放大比对。

#### ⭐ 「文字卡做成视频之后虚了」——虚的不是编码，是**下采样用了缺省滤镜**

账号所有者 2026-08-16：「主要是感觉文字卡做成视频之后，上面的文字虚化了不少。
看起来不是太清晰和做渲染之前」。

**成片是可以拉回本地逐帧比的**（`render.json` 的 `video_url` → Release，
见「⭐ 而『抽帧看』现在在本地就能做」那节），所以这件事有仪器：把
`weeks-at-no1` 第 ③ 屏（那张连续周数榜，全片字最密的一屏）按管线拆成三段
分别量拉普拉斯方差——**只量示意图那一块（y 100~1200），避开烧进去的字幕**
（字幕是白字大号，把它算进来会把整块的高频抬上去，我第一次就是这么量的，
得出「成片比无损源还锐」这种荒唐结论）：

| | 高频能量 | vs 无损基准 PSNR |
|---|---|---|
| 无损源 → lanczos 下采样（上限，不编码） | 1302.6 | — |
| q86 源 → lanczos 下采样 | 1274.3 | **JPEG 只花掉 2.2%** |
| q86 源 → **bicubic** 下采样（swscale 缺省） | 1100.7 | **少了 13.6%** |
| q86 源 → bicubic → crf 26（**改之前的成片**） | 1129.7 | 38.91 |
| q86 源 → lanczos → crf 26（现在） | 1299.9 | 39.48 |
| q86 源 → lanczos → crf **20** | 1284.1 | 41.30 ← 体积 +41%，白花 |
| q86 源 → lanczos → **轻锐化** → crf 26 | 2100.2 | 35.27 ← 陷阱，见下 |

- **卡片是 2160×2880 截的（`device_scale_factor=2`），而画布只有 1080 宽**
  ——每一屏都要**整整缩小一半**。缩一半用哪个滤镜，就是「笔画还剩多少」
  这件事本身，而这条线一直没给它指定过，落在 swscale 的缺省 bicubic 上
- ⚠️ **我的第一反应是去查编码参数，方向是错的**：`crf 26` 一点高频都没吃掉
  （1100.7 → 1129.7）；crf 20 体积涨 41%、高频原地不动。「crf 别再往上推」
  那条照旧成立，但**往下调买不到清晰度**
- ⚠️ **轻锐化那一行是陷阱**，它把这个数刷到 2100 而 PSNR 掉到 35.3——涨的是
  振铃不是细节。和封面那条「锐化能把 779 刷到 1727 而细节并没有回来」是同一
  个坑：**高频能量涨了不等于更清楚，要连 PSNR 一起看**
- ⚠️ **q86 的 JPEG 不是主因**（2.2%，bicubic 的六分之一），不用为了视频再多渲
  一份无损卡。不过差异分布图很有信息：JPEG 的误差**几乎全落在笔画边缘上**
  （中位 1.0、p99 91），所以「q 不低于 82」那条下限照旧
- 代价只有 **+1.3%** 体积（同一屏两秒：78595 → 79606 字节）
- 判据 `test_卡片缩一半要用lanczos不许退回缺省的bicubic` **钉两头**：
  **行为**（真跑一次 ffmpeg 缩一张深绿底浅色细线的图，lanczos 必须比 bicubic
  实——只钉字符串的话，换成 `flags=neighbor` 照样绿）和**位置**（凡是
  `force_original_aspect_ratio=decrease` 的链子都要带 `flags=`，自己从源码推
  不维护白名单）。两个方向分别反向验证过，红在不同的断言行

⚠️ **最刺眼的一条：`build_match_reel.py` 早就一路写着 `flags=lanczos`。**
也就是说这个仓库**已经决定过**下采样该用哪个滤镜，只有解说片这条线漏了——
不是有人权衡过，是这一行从来没被量过。**同一件事在两条线上各写一遍，
必然分叉**，而分叉的样子是「其中一条悄悄差一档，谁也不知道为什么」。

⚠️ **「剩下的那部分虚是物理的，别再去优化」——这句话我说早了。** 分辨率那一半
确实是物理的（卡片 2160×2880、成片 1080×1440，永远差一倍），可账号所有者同一天
的下一句「**卡片上面的文字…看起来很不清晰**」指的根本不是它，见下一节。
**「我把能量的都量完了」和「我量的是他说的那件事吗」是两回事。**

#### ⭐⭐ 同一句抱怨的第二个根子：**scrim 盖在示意图上面**，把自己的字压暗了

账号所有者 2026-08-16，紧接着上一条：「卡片上面的文字做成图片之后看起来很不
清晰」「**所以我建议把文字的亮度调高**」「卡片上的文字和前景的文字的颜色看起来
很土，**前景的颜色很鲜艳，卡片上的文字就比较暗和模糊**」。

⚠️ **最后那句是判据，不是形容**：他把**同一张卡的上下两半**摆在一起比。去量：

| | 对比度 |
|---|---|
| 卡片下半的要点（`.point`） | **17.2 : 1** |
| 示意图框内正文 | **3.8 : 1** ← 连 WCAG AA 的 4.5 都不到 |
| 示意图强调字 | 4.0 : 1 |

差了四倍多。**两个独立的原因叠在一起：**

**① `.scrim` 盖在 `.diagram-wrap` 上面。** 两个都是 `position:absolute`、都没有
`z-index`，而 scrim 在 DOM 里排在后面——所以它画在示意图之上，而它顶部那一档是
**55% 的压暗**。示意图从 `top:210px` 起、920px 宽的 3:2 画布高 613px，占卡片高度
的 14.6%~57.2%，按四个色标插值是被压暗 **36%（顶）到 19%（底）**。
scrim 存在的理由是**让贴底的 `.copy` 压在照片上还读得出来**——而示意图这一屏
底下根本没有照片，背景是我们自己画的 `.hero.diagram` 渐变。**这层压暗一点用没有，
纯粹在削自己的字。** 修法是给这一屏另一条 scrim（`.scrim--diagram`），上半整段
透明，只保留底部那一段。

**② 后加的示意图模块自己调了一套色。** 卡片下半用的是 `#f4fbf7` 正文 +
`#c6f65a` 亮绿（`.point` / `.point i` / `.ask` / `.kicker`），而
`no1_charts` / `rulebook_cards` 里我写的是 `#e7f3ec` 正文 + `#8fd6a8` 薄荷 +
`#e0b13a` 暗金。⚠️ **这不是「调得不好看」，是分叉**：`explainer.py` 里那 40 多张
老示意图早就一路写着 `#c6f65a`（出现 119 次），**这套色本来就定过**，只有后加的
模块没跟上。收成 `video/diagram_palette.py` 一份出处。

改完实测：框内正文 3.8 → **8.4 : 1**，强调字 4.0 → **8.7 : 1**，副标题 5.6 → **9.3 : 1**。

- ⚠️ **「大面积的底」和「字」要分开取色**：条形图的条、方框的底仍用薄荷绿
  （`FILL`），铺一片亮绿会盖过字；**只有文字**用 `#c6f65a`。「一屏只留一个
  强调色」说的是强调，不是底色
- ⚠️ **次级灰别再往暗调。** 本文件早记过一次「一度用到 `#7f958a`，反馈是
  『文字有点模糊』——那不是分辨率问题，是对比度不够」。`#a9bcb2` 是同一个坑
  浅一档，**同一条反馈这是第二次**
- 判据两条，都反向验证过（三个方向各红在不同的断言行）：
  `test_示意图那一屏的scrim不许压在示意图上` **钉两头**（用的是另一条 scrim +
  那条 scrim 上半真的是全透明，而且底部那一段**必须还压着**——`.copy` 就是靠它
  才读得出来）；`test_示意图的颜色要和卡片本身是同一套` **从卡片自己的 CSS 里
  把颜色抠出来比**，不另记一张色表，CSS 改了这儿不改当场红

⚠️ **顺序上的教训**：我先量了下采样、量了 JPEG、量了 crf，三样都量对了，然后
下结论说「剩下的是物理的」。**而真正的大头（一层压在上面的半透明黑）我一次都
没查**——因为我一直在「图像处理管线」这个框里找，没往「这张卡是怎么合成的」那边
看。**账号所有者说「和前景比」的时候，他已经把答案指出来了**：同一张图上有一半
是亮的，那就不可能是分辨率或编码——**分辨率和编码不会只削半张图**。

⚠️ 至于**把字写大**那条路仍然成立、也仍然没做：「画出来的那一屏，也要够大够
直观」那节写着下限（900 宽的 viewBox 里正文不小于 26、标题 34 起），而存量里
19~25 的字号到处都是。

#### ⭐⭐ 第三个根子，在**封面**上：一层为「居中的那一问」垫的椭圆，而那一问早搬走了

账号所有者：「**感觉封面还有蒙了一层阴影**」。`explainer.py` 的
`.cover .scrim` 叠着两层——一条竖向渐变，**外加**一层
`radial-gradient(128% 40% at 50% 50%)`。那层椭圆的注释写着理由：
「给**居中的那一问**垫一层软椭圆」。

**而 `.copy` 早就不居中了。** 账号所有者当初要求「文案移到底部，类似之前
『赛场之上』的双人封面」，`.copy` 因此改成 `bottom:` 贴底——**居中的那一问
从此不存在，椭圆却留在原地**，压的就成了照片正中间，也就是人脸那一块。
纯白底片量出来：正中 alpha **0.412**，只有 39% 的亮度透得出来。

⚠️ **这是「抄了规则，没抄它依赖的前提」的镜像**：那类是搬走了规则忘了前提，
这条是**前提被搬走了，规则留在原地**。而它长得比前者更无害——CSS 一个字没错、
渲得出来、四道闸门全过、全量测试全绿，只是**多了一层黑纱**。

⚠️⚠️ **同一天 `#421` 在 `versus_poster.py` 上修的是同一个形状**（账号所有者：
「背景图不要再加前面阴影，导致背景图看着模糊啊」），那边量到正中压 58%，
这边 61%——**两条线各有一份 `.scrim`，各压各的**。所以修一条不等于修完了：
「赛场之上」的封面和解说片的封面是两套代码，`grep scrim` 一次就看得见。

**删椭圆不是把封面整体调亮**——椭圆原来在文案那一带也顺手压着一层，删了得补：
起坡从 66% 提到 **54%**、底下几档同时压重（.34@70% / .62@88% / 收尾 .70）。
结果是**主体那一段放开、文案那一段比原来还暗**（85% 处 0.249 → 0.532）。

⚠️ **而这一条最贵的教训在判据上：我为它做了三种「标题对比度」的量法，
三个互相矛盾的答案。** 固定带子会把标题上方的空档算进「底」（标题只有一行的
封面因此被读成最差）；按行找字又**分不开「白字」和「大太阳底下的白草地」**
——两者亮度一模一样。36 张封面逐张打开看，标题**全都读得出来**，
而同一批数字里有六张「低于 AA」。

**真正托住可读性的是 `.cover .title` 那三层 text-shadow，而任何按中位数算的
对比度都看不见逐字的描边光晕。** 所以判据只钉**压暗曲线本身**
（拿一张纯白照片当底图渲一次，每行灰度直接就是 `1 - alpha`，不依赖任何真照片），
**不钉某张封面的对比度读数**——那会随换图漂移，不是变成常年红就是变成假绿灯。
这和「一条漂出来的分布不会自带分界线」是同一件事：**阈值不在数据里，
在渲出来的那张图上。**

判据 `test_封面不许再压一层居中的阴影` **钉两头**（缺一头都是恒真）：正中
必须近乎透明（只钉这头，把整条 scrim 删光也绿）、文案那一带必须够暗（只钉这头，
椭圆加回来照样绿——它在底部反而更亮）。两个方向分别反向验证过，红在不同的断言行。

⚠️ **已发的片子不为这个重渲**（微信那条消息发出去收不回来），这条只管以后。

### ⚠️ 微信推送卡底部那颗红按钮：**不要动它**

账号所有者 2026-08-31：「**微信推送的红色按钮不要改了**」。

来路值得记，因为它是一次**猜错了对象**的返工。他先说「红色区域高度可以更小
一些」，而整条链上只有一处是红的（封面、数据图、复制页、成片逐个量过都没有
红色区域；红的只在推送卡里——顶上 5px 红边 ＋ 卡底那颗 `#ff2442`）。我据此
把那颗按钮从 47px 压到 37px，还顺手把它三处内联的实现收成一处出处。
**他要说的其实是封面钩子**（见上面「钩子的字号收成一个数」那节），
推送按钮原样退回。

⚠️ **两条留下来的方法论**（和按钮无关，都是那一轮踩出来的）：

- **「去 docstring」别按三引号的形状抹。** 扫「谁还内联写了这颗按钮」时我用了
  `re.sub(r'"""[\s\S]*?"""', "", src)`——而 `render/knowledge.py` 那张推送卡
  **整个是一个三引号 f-string**，按钮就在里面，于是产物模板被连同 docstring
  一起抹掉，判据当场变成一盏绿灯（反向验证时把内联那份塞回去，测试照样过）。
  要抹就按 **AST 给的位置**抹（`Module/FunctionDef/AsyncFunctionDef/ClassDef`
  的第一条 `Expr(Constant(str))`，按 `lineno`~`end_lineno` 清空那几行）。
  本文件早写过它的近亲：`ast.get_docstring()` 回的是**去过缩进**的那一份，
  拿它去 `replace` 源码一个字都删不掉、而且不报错——**「去 docstring」这个
  动作本身就容易做成静默的错**
- **又用 `git checkout` 还原，又把没提交的测试删了。** 本文件白纸黑字写着
  不许这么干，我还是在验完之后顺手 `git checkout -- tests/test_render.py`，
  两条刚写完、还没提交的测试一句话全没了。**救回它的是同一条里那半句：
  验还原要查「该在的东西还在不在」**——跟着跑的那句 `grep -c <测试名>` 报了
  **0**，当场看见。反向验证一律 `cp` 备份、`cp` 还原

⚠️ **顺手发现、但没有修的一个真问题**（记在这儿，别当它不存在）：这颗按钮
在**三个文件里各写了一遍**（`tools/push_reel.py`、`render/knowledge.py`、
`render/pushmsg.py` 那条已停产的日报模板），一模一样的内联 style。哪天真要动
它的样式，**得同时改三处**，漏一处的样子是「同一个账号发出去的推送，按钮
高矮不一」，不报错也没人对得上。这次没收成一处出处，是因为账号所有者说了
「不要改了」——**在一个他刚说别碰的面上顺手做重构，正是本文件说的「顺手扩大
范围」**。

### CDN 主机名一个地方定，判据别按 `cdn.` 写死

账号所有者反馈国内慢，选了换 jsDelivr 镜像。主机名收进 `tennislive/cdn.py`，
默认 `gcore.jsdelivr.net`（Gcore 在港／日／新有节点，是这几家里离内地最近的；
`cdn.` 那个入口在 jsDelivr 国内备案被撤销后一律绕境外），可用
`TENNISLIVE_JSDELIVR_HOST` 覆盖，工作流已把它接到仓库变量上，改一个值就能 A/B。

⚠️ **这是推断不是实测**——沙箱在境外，量不出内地速度，别拿这里的 ttfb 当结论。

换镜像会踩两处**不吭声**的坑，都已改成按 `.jsdelivr.net` 匹配，并落了测试
（`test_换镜像之后校验和钉版本都还认得出jsDelivr`）：

- `jsdelivr_link_sources` 只认 `cdn.jsdelivr.net` → 返回空 → 推送前那道
  「链接可取吗」的校验**一张都不校验**，然后把没就绪的链接发出去
- `_JSDELIVR_MAIN_RE` 只认 `cdn.` → 匹配不上 → 图片全停在 `@main`，
  钉 commit 那步等于没做，同一路径的新旧内容会被微信的图片缓存混起来

写错的域名要退回默认——发一封全是裂图的推送，比慢一点糟得多。

⚠️ 复制页走 `github.io`，那条链路没动，也没法从这里量。

### ~~`PUSHPLUS_SECRET_KEY` 配着，代价是**图片 30 天后删**——账号所有者认领过~~

> ⚠️⚠️ **2026-08-05 这一节整个翻了个面：现在一条工作流都不许配这个密钥。**
> 判据是 `test_不许再给工作流配PUSHPLUS_SECRET_KEY`（`tests/test_workflow_alerts.py`），
> 它扫出所有走 `tennislive publish` / `push_reel.py` 的工作流，**断言这两个键
> 一个都不出现**——和下面正文要求的正好相反。**照下面那段做会被 CI 当场打红。**
>
> 账号所有者 2026-08-05：「**如果没有任何用途的话，拿掉吧**」。
>
> 翻面的理由是**那条路真的走了一趟之后才看清的**（run 30903125364，纳达尔学院）：
>
>     获取 PushPlus AccessKey 失败: code=401 请求未授权
>
> 官方码表说 401 ＝「排查**开放接口功能是否启用**」，而开放接口**默认就是禁用
> 的**，要去后台手动开；开完还有一道**安全 IP 白名单**，而 GitHub runner 的出口
> IP 每趟都不一样——**那条路对 CI 基本无解**。
>
> ⚠️ **下面那段的前提是「配了就能用」，而这个前提从来没成立过。** 前提没了，
> 判据就得跟着翻——**不能留着一条要求配一个用不了的密钥的检查**。所以现在图片
> 走 jsDelivr 退路：**钉在 commit 上、永久可取**，除了「发送前要等回源」这一点，
> 每一维都比图床好。
>
> 下面整段**留着不删**，因为两件事还成立、而且以后还会有人撞上：①「30 天会删图」
> 这条限制本身是真的（真要重开图床就会撞上）；②「告警要按走了哪条通道发，不按
> 配没配 key 发」那条方法论（见本节末尾的子节），跟这次翻面无关。

**这一节的结论翻过两次，先看结论再看来路。**

~~**结论（2026-08-04）**：secret 配上了，图片走 PushPlus 图床。我把 30 天那条
限制摆出来之后，账号所有者的回答是「**可以接受**」。所以这是一个**认领过的
取舍**，不是漏配、也不是待办——和 `mixed_fps` / `silent_source` / `rank: null`
一个形状。~~ ⚠️ **第三次翻面已于 2026-08-05 发生，见本节开头那个方框。**

~~**别再来「优化」它**：看到日志里那句 30 天的告警不要去撤 secret，
看到老推送的图裂了也不是 bug，那就是这个取舍本来的样子。真要改回永久可取，
是**账号所有者改主意**，不是谁读到这一节觉得不划算。~~

⚠️ **上面这句划掉的话是这一节最危险的一句**，单独记一笔：它**点名禁止**了后来
被证明是对的那个动作（撤 secret），而 2026-08-05 撤掉它的正是账号所有者本人。
写「别再来优化它」的时候我以为自己在保护一个认领过的取舍，实际上是**把一个从
没跑通过的配置钉成了不许碰**——「认领过的取舍」和「没验证过的配置」在当天的
文档里长得一模一样。**下次要写「别再改这个」之前，先问一句：这条路真的跑通过
一次吗？** 跑通过才配叫取舍，没跑通只是一个待验证的假设。

`prepare_image_delivery` 有两条分支，而**它们的日志长得差不多**：

| | 配了 Secret（~~现在这样~~ **走不通，见开头**） | 没配（**现在这样**） |
|---|---|---|
| 图走哪 | PushPlus 自己的图床 | 退回 jsDelivr／raw，钉在 commit sha 上 |
| 发之前等多久 | 0 | `wait_for_images` 最多 **20×15s = 5 分钟** |
| 轮满还取不到 | —— | `PushPlusError`，**整条推送取消** |
| **图片活多久** | **30 天，到期自动删** | 永久（git 里的东西不会消失） |
| 还要什么 | **会员**，且官方写明是收费服务 | 无 |
| ⚠️ 当年漏掉的那一行 | **开放接口默认禁用**（要去后台手动开，401 就是这个）＋**安全 IP 白名单**，而 runner 的出口 IP 每趟都变 | 无 |

⚠️ **最后那一行是 2026-08-05 才补上的，而它才是决定性的那一行。** 上面五行
（30 天、收费、等多久）当年全查清楚了，唯独没查「这条路今天通不通」——
于是这张表把一个**根本连不上的通道**，和一个能用的通道并排列成了两个选项。
**又一次「查产物，不查信号」**：读官方文档是信号，发一次请求拿到 401 才是产物。

后两行是 2026-08-04 读官方文档（`/doc/function/image.html` 的「使用限制」）
才补上的：

> 图片服务为收费服务，目前会员用户限时免费使用……**图片有效期为 30 天，
> 到期后将自动删除。**

**这条限制的要命处是它一个月后才现形**：微信那条消息发出去收不回来也改不了，
海报到期就变裂图，而两条通道当天的日志长得一模一样。所以我当时的建议是别配
（那正是把图片链接钉到 commit sha 上要解决的问题），~~**账号所有者衡量之后选了
配**——发之前不用等，图一个月后失效可以接受~~ ⚠️ **他 2026-08-05 改了主意**
（「如果没有任何用途的话，拿掉吧」），而促成他改主意的是那个 401——也就是说
**当初那次「衡量」衡的是一组不完整的事实**：表里少了「这条路根本连不上」这一行。

**所以要出声。** 真走到 PushPlus 那条分支时先 `logger.warning` 把 30 天写进
日志：将来有人查「这条老推送的图怎么裂了」，答案在当天的日志里就有，
不用重新推一遍这一整节。

- **判据不是「有没有配」，是「走了哪条通道」**：`prepare_image_delivery` 返回
  的第二个值会打进日志（`图片通道 pushplus` / `jsdelivr`）
- **默认那条不许吵**。恒真的告警会把真正该看见的那次淹掉——判据两头都钉了
- ~~工作流里那行 `PUSHPLUS_SECRET_KEY: ${{ secrets.… }}` 六条线都要有。判据
  `test_走图床那条路的工作流都要配SECRET_KEY` 保证口径一致，免得开了一半：
  **一半才是最坏的**，同一天里有的推送图永久、有的三十天，谁也说不清哪条是哪条~~
  ⚠️ **这条判据不存在了**：`test_走图床那条路的工作流都要配SECRET_KEY` 2026-08-05
  连同它守的那个口径一起翻了面，现在叫 `test_不许再给工作流配PUSHPLUS_SECRET_KEY`，
  断言的是**一条都不许配**。⚠️ **但「一半才是最坏的」这半句反过来仍然成立**，
  只是主语换了：现在「开了一半」＝有的工作流配着、有的没配，同样说不清哪条走哪条通道

来路：`flash.yml`（内容雷达的赛前焦点）从上线起就漏了这一行，而同样走
`tennislive publish` / `push_reel.py` 的另外五条都写着。

- **判据自己推导，不维护白名单**（今天叫 `test_不许再给工作流配PUSHPLUS_SECRET_KEY`，
  当年叫 `test_走图床那条路的工作流都要配SECRET_KEY`）：凡是 `run:` 里出现这两个
  入口的工作流都被扫到。和「凡是 `git commit` 的都要有体积闸」同一个形状——那条的
  教训是手写名单会在删工作流的当天变成 `FileNotFoundError`。⚠️ **这条方法论是这
  一节里唯一整个活下来的东西**：口径翻了面，而「自己推导」让判据翻面时一行名单都
  不用改——新加一条工作流照样自动盖住
- **扫之前先去掉整行注释**。这次注释制造的是**假绿**（注释里写着这个变量名，
  漏配的工作流照样过），比以往那四次「注释被误伤」更坏
- **`news-radar` / `source-health` 不用配**：它们是裸 `curl` 发纯文字告警，
  消息里没有 `<img>`，走不到图床那条分支。我一开始把这两条也列成「疑似漏配」，
  读完它们的推送步骤才发现方向错了——**「都是推送」不等于「都走同一条路」**

#### ⚠️ 而那句告警自己站错了位置：配了 key 就喊，不管图有没有真传上去

上面那条「所以要出声」我照做了，**位置写在 `try` 前面**。于是 2026-08-04 王欣瑜
那趟推送（run 30906741325）的日志里，两行贴在一起：

    图片走 PushPlus 图床：官方限制 30 天后自动删除，这条推送的图到期会变裂图
    PushPlus 图床用不了（…请求未授权），本次退回 jsDelivr
    …图片通道 jsdelivr-fallback

那条片子的图**钉在 commit 上、永久可取**，一天都不会少。而这句话存在的**全部
理由**就是给一个月后来查「这条老推送的图怎么裂了」的人看的——在一条图永久的
run 上喊 30 天，**正好把他引到错的答案上**。这比不吭声更坏：不吭声只是没帮上忙，
喊错了是主动给出一个错答案，而且他没有第二个地方可以对。

- **判据是「图有没有真传上去」，不是「配没配 key」**——又一次「查产物，不查信号」。
  告警挪进 `else:`，也就是 `_pushplus_bed_delivery` 真的返回了之后
- **老判据两头都钉了，偏偏漏了中间那一头**：走成了要说 ✅、没配过不许吵 ✅、
  **配了但被拒 ❌**。而线上掉进去的正是这一头。三态的东西别只测两态
- ⚠️ **新判据的 `caplog` 必须收在 `WARNING`**。那句告警就是 WARNING，收在 `ERROR`
  上它根本进不了 caplog——反向验证过：坏代码 + `ERROR` 档位，**测试照样绿**。
  又一次「判据自己也要有判据」

**产物在哪个日期目录下，按上海时间算。** 工作流里是 `TZ=Asia/Shanghai date +%F`，而沙箱跑在
UTC——每天 **16:00 UTC 之后两者差一天**。查 `output/2026-07-26/...` 查了半小时"还没落库"，
其实它一直安安静静躺在 `2026-07-27` 里。这就是"空结果先自证是真空"的又一个变种：
`git log` 里明明有三条 `content: 知识解说视频 venus-potapova`，目录却是空的，那说明**找错地方了**，
不是没生成。两个检查脚本的 `--date` 默认值已经改成上海日期。

## ⭐ 赛后开麦：提炼一直在写，只是写在了平台看不见的地方

2026-08-04 发的伊埃拉捧杯致辞被视频号判了：

> 你的作品疑似对他人/网络素材简易加工，二次创作部分**信息增量不足**，
> 如素材简单增加标题、字幕或简易配音，未有深度解读或创作等。

**它没冤枉我们。** 量一下我们自己的画面占多少：

| | 总长 | 自有画面 | 占比 |
|---|---|---|---|
| 伊埃拉捧杯致辞（被判的那条） | 217.2s | 封面 1.8s | **0.8%** |
| 商竣程 | 70.3s | 封面 1.8s | **2.6%** |

而**提炼我们一直在做**——`xhs.txt` 里那句「话锋一转落回自己身上——这才是这段
采访真正的落点」就是解读。它只活在小红书正文里，**视频里一个字都没有**。
所以这不是「要不要开始做提炼」，是**把已经在写的那份搬进画面**。

账号所有者 2026-08-05 定的体裁，**两句话，后一句改掉了前一句**：

> 「保留原声主体，前后加解读卡」
> 「**我建议还是不要前面卡，后面卡片可以留着**」「**但要有配音**」

所以最终是**一张卡，在结尾，带口播**。落点卡的代码路径留着（`open`，可选），
但默认不写——它挡在原声前面是**在跟封面抢开头那 5 秒**，而抖音 5 秒内走掉
62%，封面本来就是钩子。判断放到看完之后。

    封面 1.8s → 原声＋双语字幕 → 【收尾卡（带口播）】→ 品牌片尾

商竣程那条按这套算：70.3 → 80.3s，自有画面 **2.6% → 12.6%**。
已发的七条不重渲（「不动，只管以后」），挂在 `_NO_TAKEAWAY_LEGACY` 里
**只许减不许加**。

### 为什么卡必须有配音——不是为了增量，是那段死寂

平台明写着「简易配音」不算增量，所以配音**不是**冲着它去的。真实理由量得出来：
两张静音卡那一版，**前 10.8 秒峰值 −91 dB**，也就是数字静音（对照组同一条
片子：原声 −10.4 dB、片尾口播 −12.2 dB）——而那段死寂正好压在决定去留的地方。

⚠️ 我原来把卡做成静音，写的理由是「插一段合成音会把现场声割断」。**那句话
站不住**：卡本来就是独立的一段，它前后都不是现场声。**没量过的推断写进注释，
下一个人就会拿它当判据**（「edge-tts 同理」那条栽过一次）。

**卡停多久跟着口播走**，和「赛场之上」封面同一条规矩：长度由语音算，不在
spec 里另写一个数。合不出语音退回字数估，**但要说一声**。

### 「快速过」做成闸，不做成建议

账号所有者：「**提炼下卡片内容快速过**」。`TAKEAWAY_MAX_CHARS = 34`，
超了当场报错——「写短一点」这种话拦不住下一次写长。34 是从语速倒推的
（5.5 字/秒 ≈ 6.2 秒 ＋ 一口气）。**它上来先拦住的是我自己**：第一版写了 35 字。

### ⭐ WTA 集锦带不带场上采访，看**时长**就知道——310 秒是定长的纯集锦

这条线已发的华盛顿两条，采访都是从**逐场集锦的尾巴**上取的（QF 449 秒里从
293.8 秒起、决赛 535 秒里从 298.6 秒起）。所以找采访时会顺手去翻集锦。

2026-08-06 找伊埃拉多伦多 R2 的采访时量出来一张表：

| WTA 逐场集锦 | 时长 | 带不带采访 |
|---|---|---|
| 华盛顿 R1 对郑钦文 / SF / 阿布扎比 R16 / 多伦多 R2 | **310s** | ❌ 纯集锦 |
| 华盛顿 QF | 449s | ✅ 从 293.8s 起 |
| 华盛顿决赛 | 535s | ✅ 从 298.6s 起 |

**纯集锦是定长 310 秒**，超出的那部分才是采访。搜索结果里 `%(duration)s`
本来就有，看一眼就够——多伦多那条我为了确认跑了一趟 runner（`mode=subs`，
45 秒），拉回来 42 行全是解说，最后一句停在比分上。**下次不用再跑。**

⚠️ **310 是「没有」的判据，不是「有」的判据**：超过 310 秒也可能是别的加料
（多球员合集、赛事回顾）。超了仍然要跑一趟 `mode=subs` 看尾巴。

⚠️ **别只查集锦。** 官方还有两个独立系列，标题模式固定，搜得到：

    <球员> | POST MATCH INTERVIEW | <赛事> | <城市> | #<标签>      场上采访，79~114 秒
    POST-MATCH PRESS CONFERENCE | <N>th ROUND | <球员> | #<标签>   发布会，273~747 秒

⚠️ **空结果要拿同一站的别人当控制组。** 多伦多那次搜不到伊埃拉的场上采访，
而同一站 Swiatek / Sonmez / Kostyuk / Cross 四条都在——**格式存在，只是没轮到
她**，这和「这一站根本不发」是两个结论（商竣程那次正是后者：多伦多女子有、
蒙特利尔男子一条都没有）。

⚠️ **搬运号不算。** 她那场的赛后发布会只有 Ezhumala Talks 有，官方没发——
这条线的四要素靠**频道和标题**自证，搬运号一律不用。

#### ⭐⭐ 2026-08-16：这条判据**不只对 WTA 成立**，只是每个频道的基线不一样

账号所有者：「**有些采访是在那个集锦的后半部分，你去看一看。比如说伊埃拉的采访
或者什么别的。维列夫的采访，什么之类的。**」

上面那条只写了 WTA，于是我一直只在 WTA 那一栏用它。三个频道各扫 45 条量下来，
**同一个形状在 Tennis TV 上照样成立**——而那半边一直没人翻过：

| 频道 | 纯集锦基线 | 超出的那条 | 尾巴是什么 |
|---|---|---|---|
| **WTA** | **310s**（定长） | 382 / 409 / 443 / 452 / 473 | ✅ 全是场上采访 |
| **Tennis TV** | **139~224s**（不定长） | **363s** 蒂兰特胜德约 | ✅ **241.7–352.2，110 秒两问** |
| **ATP** | 83~140s | 一条都没有 | ❌ 结构性太短，别扫了 |

⚠️ **Tennis TV 的基线不是定长**，所以判据不能写成「等于 X」，只能是
**「比同一站同一批的中位数长出一倍」**——363 对 139~224，一眼就跳出来。

⚠️⚠️ **长不等于有采访：日汇总（`Day N Highlights`）一条都没有。** 那条 729 秒的
Day 3 汇总看着最像，实测**从头到尾是解说**——`>>` 全是解说员换人，一个提问都没有。
长只是因为它把好几场拼在一起。**先按「单场」筛，再按长度筛，顺序反了会白拉字幕。**

⚠️ **查空要说清是哪一种空**，这次两个人正好各是一种，而它们在搜索结果里长得一样：

| | 为什么没有 | 该怎么办 |
|---|---|---|
| **兹维列夫** | 他辛辛那提那场是**今天才打的**（flashscore：3-6 6-3 6-3 胜诺里），三个频道都还没发单场集锦 | **是还没发，不是不发**——过一天再扫 |
| **伊埃拉** | 她第二轮 **6-1 3-0 对手退赛**（鲁塞退赛），WTA 官方根本没出这场集锦 | **退赛的场次没有集锦，也就没有尾巴**，不用再找 |

**「退赛 → 没集锦 → 没采访」是一条能直接用的判据**：查之前先看赛果里有没有退赛，
有就别去翻频道了。

##### ⭐ Tennis TV 还有第二条路：标题里带 🎙️ 的就是**独立**采访

同一批 45 条抽样里带 🎙️ 的只有 3 条，而**那三条全是独立采访**，其中两条这条线
已经做过：

    115s   Novak Djokovic on THAT Final vs Alcaraz & Cincinnati Return 🎙️   prsYrVNMITA ← 发过
    509s   Ben Shelton REACTS to His EPIC Performance vs Mensik 🎙️          ZycljTf6s0E ← 发过
   1170s   Ben Shelton & Brandon Nakashima React To Their 2026 Montreal Final 🎙️  w82ZWBykZ7k

其余 42 条一条都不是。**所以 Tennis TV 有两条路，形状不同别混**：

| | 怎么认 | 采访在哪 |
|---|---|---|
| **独立采访** | 标题带 **🎙️** | **整条都是** |
| **单场集锦** | 标题不带 🎙️，`X vs Y … Highlights` | **只在尾巴上**，而且要长出基线一倍才有 |

⚠️ **样本只有一批 45 条**（3 正 42 负）。这个判据用来**排序候选**足够，
但**不能拿它判「没有」**——没带 🎙️ 的长集锦照样可能在尾巴上有采访
（`lyu2NHRog_g` 就是），那正是上面那张表的另一半。

### ⭐⭐ 赛后采访片**从比赛结束那一刻开头**，不从第一个问题开头

账号所有者 2026-08-16，看完 `tirante-djokovic-cincinnati-2026-r2`：
「**蒂兰特这个视频做的很好，先交代了比赛结束，后续的赛后采访建议都这么开头**」。

那条片子的窗口**不是从第一个问题起**，是往前多收了 **13.8 秒**：

    224.24  解说喊 `and there we go`  —— 赛点落地
    228.24  `Thiago Agustin Tirante of Argentina`
    232.48  `with the biggest win of his life.`
    237.44  `He takes down the three-time champion here, Novak Djokovic.`
    241.68  `>> Thiago, huge congratulations…`  ←  采访从这儿才开始

**为什么它更好，三条都不是文风问题：**

- 刷到这条的人**不用先搞清「这是谁、赢了谁」才听得懂采访**。采访本身从第一句起
  就默认你知道，而**封面只停 1.2 秒**，刷到中段的人根本没看过
- 开场第 ① 格因此是**全片最强的那一格画面**（赛点／庆祝），而不是一个人站着
  说话——「⭐ 开场三格的顺序」那条本来就这么要求，只是一直没在这条线上用
- 那句分量是**转播自己说的**，比我们替他说硬（同一条线上「画面自证」的老规矩：
  来源自己写下的东西 > 我们的转述）

**现在有闸了**（`check_opening`，排在 `main()` 第一行，只读 spec、0.2 秒报）：

    "opening": {"kind": "match_end", "lead_in": 13.8,
                "why": "227.9 起：赛点落地 ＋ 解说报出「他掀翻了这里的三冠王」"}
    "opening": {"kind": "none", "why": "整条是发布会，背板和记者提问，前面没有比赛画面"}

⚠️ **默认是 `match_end`，而且没有默认值可省——必须显式认领。** 反过来做的后果
是可预见的：**从第一个问题起是最省事的写法**（第一条字幕事件就在那儿，`start`
抄过来就行），所以不认领的话每条都会自然滑回去，而**漏掉那一段不会报错**——
片子照样出得来，只是开场从「赛点落地」变成「一个人站着开始说话」。
和 `interview_kind` / `mixed_fps` / `silent_source` 是同一个形状。

⚠️ **`kind: "none"` 要写清为什么。** 「源片里没有比赛画面」是一个**可以查证的
事实**（发布会背板、演播室、话筒样式），不是一句随口的免责——写不出来就说明
**还没打开源片看过**，而那正是这道闸要拦的。

⚠️ **收多少有上界（`_OPENING_LEAD_MAX`），别把集锦搬进来。** 这条线是
「保留原声主体」，开头收的是**最后一分 ＋ 解说那一两句**；收成半分钟集锦就变成
「赛场之上」了，而那是另一个栏目。

##### ⚠️ 这个上界一开始按**一条片子**定成 30，紧接着的下一条就咬人

`arango-venus-cincinnati-2026-r1` 要 **34 秒**——写完闸的当天，第一条真正用它的
片子就撞上了。**「查了一场就写成了一类」，这份文档里已经记过好几次，而这次是我
自己刚写完那几条的同一天犯的。** 现在是 **40**。

⚠️ **改上界的同时要改判据的写法**：那条超限用例现在写成
`clip._OPENING_LEAD_MAX + 1`，不写死数字——**判据里钉一个具体的数，等于让下一个
调这个常量的人必然打红一条本来该跟着走的测试**。

⚠️ **真正的判据是定性的，上限只是防滑坡**：收的必须是**一段连续的赛后画面**
（赛点 → 庆祝 → 解说交代赛果 → 采访），不是从集锦里挑几个回合拼进来。
阿朗戈那 34 秒是「她的反应 → 大威 → 她挥手致意 → 全场 → 转播打出
`ARANGO 6 6 / WILLIAMS 2 2 · ROUND 1`」，一刀没剪。

⚠️ **而它还有一条硬约束，是这条线独有的：这是双语字幕片，开头不许压在一段没有
字幕的画面上。** 阿朗戈那条**没有从赛点那一帧起（255.28）**，虽然那更符合
「先交代比赛结束」的字面——因为赛点之后**整整 11 秒只有欢呼、没有任何字幕事件**，
那 11 秒屏幕上什么都没有。起点因此切在「解说开口交代赛果」那一句上（266.4）：
画面仍然是赛后的庆祝和握手，但每一秒都有字幕。
**「更早」和「更好」在这条线上不是一回事。**

⚠️ **WTA 逐场集锦那一档天然最好收**：采访就接在集锦尾巴上，赛点就在采访前几秒
（`noskova-boulter` 我从第一问前 0.24 秒起，**完全可以再往前收**，那条已发不重渲）。
反倒是发布会和演播室专访那一档收不到——那就是 `none` 的正当用法。

### ⭐ 封面顶栏：抄 CSS 要连它**依赖的前提**一起抄

账号所有者 2026-08-16：「赛后开麦模块也要加顶部白条和栏目标题」
「参考网球栏目，也做顶部彩条和顶栏标明栏目名」。

彩条 ＋ 台头（球标 ＋ `网球时差 · <栏目>` ＋ 标题行）现在也在 `build_cover` 里，
**值全是从解说片 `_render_intro_badge` 那份抄的**。⚠️ 顶栏第二行取
`push.summary`——那正是微信推送的标题，**一个出处**；要另拟写 `cover.topic`。
⚠️ 底部那颗 tag 里的栏目名跟着删了：顶栏印过一次，tag 再写一遍就是第二遍
（「赛场之上」那颗黄色药丸正是因为这个被整块删掉的）。

#### ⚠️ 而我抄漏了一样，它一个字都不报

第一版发出去，账号所有者一眼看出：「**网球时差 赛后开麦 字体是黑色的啊**」。

解说片那份 `.brand` **自己没有 `color`**——它的浅色写在 `body{color:#f4fbf7}`
上。我把 `.brand` 整段抄进封面，而**封面的 `body` 没有 `color`**，于是它掉回
浏览器默认的黑，压在深色台头上几乎看不见。`.topic` 自带 `color`，所以
**只黑了一行**——看起来像「这一行样式没写对」，其实是继承链断了。

**这是「抄了规则，没抄它依赖的前提」的又一次**，和「抄一条已经跑通的工作流时，
注释也要抄」是同一族：那次漏的是注释里的前提，这次漏的是 CSS 继承链上的前提。

⚠️ **比 CSS 文本的判据看不见它。** 我同一轮里刚写了一条「两处台头必须是同一套
值」，它从解说片源码里抠出七项逐个比——**两份的 `.brand` 一字不差，对得上得很**，
错的是它继承的那一层。所以另加一条**真渲出来量像素**的
（`test_封面顶栏两行字都要是浅色`）：

    品牌行 y46-86    坏 最亮 79 / 亮于 200 占 0.00%   好 最亮 248 / 12.83%
    标题行 y92-124                                   好 最亮 232 / 12.92%

⚠️ **不能整块台头一起量**：`.topic` 一直是亮的，整块量的话坏的那一版**照样绿**
（实测过）。两行必须分开量。

#### ⚠️ 一个**反向验证不了**的守卫，和一条恒真的绿灯长得一模一样

上面那条测试里我加了个「判据自己的判据」：**台头底下那条必须是暗的**
（整幅都亮的话，黑字白字都能量到亮像素，主断言就成了恒真）。

反向验证时我去改**这条测试自己的底片**（中灰 128 → 纯白 255）——**没红**。
差点就把它当成「守卫装好了」放过去。真去查才知道那个方向根本触发不了它：
`.bg` 上那句 `brightness(.34)` 把纯白也压到 **87**，底片怎么选都够暗。

**它真正拦的是渲染那头**：把那句压暗拿掉，台头底下亮于 200 的像素
**0% → 98.5%**，当场红。也就是说这一条守的是「哪天有人把封面底调亮」，
而那正好会让主断言失效——是个真守卫，只是我一开始**把触发条件写错了**
（注释里写着「比如底片选亮了」）。

**教训是反向验证也要验对方向**：拆掉一样东西没红，先问一句
「**我拆的是它真正依赖的那一样吗**」，再下「这条判据是恒真的」这个结论。
两个答案的表现完全一样，而后一个会让人把一条好守卫删掉。

### ⭐ 烧进画面的英文里不许有语气词——而它只能**在切行之后**去

账号所有者 2026-08-15：「**以后把英文字幕里的语气词去掉比如 uh en 之类的**」。

自动字幕忠实记录每一个 `uh`／`um`，而这条线发的是**英语学习素材**：
「Um, honestly, I don't know.」印在画面上，读者学到的第一个词是 `Um`。
实测这批素材里只有两种真的出现过——**`uh` 264 次、`um` 247 次**
（26 份自动字幕全扫过；`ah`／`eh`／`mm`／`hmm`／`erm` 一次都没有）。

现在 `drop_hesitations` / `strip_hesitation_lines` 自动去，**420 行**会变。

#### ⚠️ 位置是这条改动的全部风险：切行**之前**去是对的，但会砸掉存量

切行之前去才是「更对」的位置——语气词不该参与断行、也不该占宽度。
**可我先量了一遍**：切行前去掉，**25 条 spec 里 16 条的行数会变**
（`swiatek-rybakina-…-presser` 234→228、`shelton-mensik` 232→227）。

而 `zh` 是**逐行手写的**、`en_fixed` 是**按行号挂的**——行数一变这两样全部
对不上，`write_ass` 那道「中文 N 行、英文 M 行，对不上」当场红。**已发的片子
不为措辞重渲，可 spec 还在仓库里、CI 每次都校**。

所以只改**这一行的文本**，一个字都不动断行。代价照实说：断行仍然是按带语气词
的原文排的，偶尔比理想的松一格；换来的是零存量破坏。判据
`test_去语气词不许改行数否则存量的中文全对不上` 钉两头——**切行那一步不许去**
（去了行数就会变）、**去完之后行数一个不许少**。

⚠️ **「先量再决定」是这条唯一做对的地方。** 按「哪儿更对」写，当场砸掉 16 条
已发 spec；而那 16 条会红在 CI 上，不是红在我手上。

#### ⚠️ 这个模块里已经有**三份**口头语名单，宽窄不同，别互相搬

| 名单 | 干什么 | 宽窄 |
|---|---|---|
| `_COMPARE_FILLERS` | 两份 ASR 逐词比对时**两边都去掉** | **最窄**，去多了会掩盖真分歧 |
| `_FILLER` | 跟记者写的人工引语比对时去掉 | 最宽（记者不会写 `ah`／`mm`），只影响比对不影响产物 |
| 烧字幕（复用第一份） | **从画面上真的删掉** | **风险最高**：删多了观众就少看到内容 |

烧字幕**只能用最窄的那份**，而且是从 `_COMPARE_FILLERS` **推**出来的不是抄的
（判据用 AST 认，注释里提到那个名字不算数）。谁哪天为了比对方便把它放宽，
`test_烧进画面的语气词名单只许最窄的那一档` 当场红，逼他显式把两份拆开——
而不是让画面上的字悄悄少几个词。

⚠️ **别以为这三份是嵌套的。** 我写过一条「窄的必须包含在宽的里」，当场红：
两份在**两个方向上**都各有各的（`_COMPARE_FILLERS` 多 `uhh`／`umm`，
`_FILLER` 多 `er`／`ah`／`mm`／`hmm`／`mhm`）。**听起来很合理，而合理不等于量过。**

#### 两个细节

- **整行只剩语气词的，原样留下并出声**。实测 2226 行里有 5 行是这样（光秃秃一个
  `Uh`／`Um,`）。去空了画面上就是「有中文、没英文」，而那一行的中文还在——
  真要处理得人来定（改 `en_fixed`，或者把中文一起改），**机器不替他决定**
- **大小写要补回来**：`Um, honestly, …` 去掉之后是 `honestly, …`，句首小写看着
  像漏了个词。只在原文那一格本来就是大写时才补
- **去在 `en_fixed` 之后**：`en_fixed` 是手写的整行替换，而这条规矩管的是
  「烧进画面的英文长什么样」，手写那几行同样算数——存量里就有 **8 处**
  `en_fixed` 自己带着 `um`／`Uh`

### 提炼是**量**出来的，不是想出来的

一段场上采访六成是套话。想一个观点出来，写的人当天状态好就有、不好就没有，
**而且没法验**。可**套话的分布本身是信息**，它就躺在我们已经有的那份转写里。
`tools/interview_takeaway.py` 把原料打出来（0.1 秒，本地，不联网）：

    商竣程 68.5 秒，他自己说了 154 个词
      healthy 4 次 ｜ happy 4 次 ｜ **win 1 次**
      而那 1 次是「如果还能赢下几场球，那就再好不过了」

「赢了世界第十六的那一晚，他谈的不是赢球」——**这句判断是数出来的**，可核、
可复算，下一条片子照样数得出来。工具还顺手把落点找了出来：`but` 之后那半句
正是 "but more happily, I'm healthy"。

⚠️ **最值钱的是「他没说什么」那一节，而它必须固定打印。** 频次榜只列
**出现过**的词——「一次都没提团队和家人」在榜上**是看不见的**，它长得和
「我没往下翻」一模一样。又一次「空结果 ≠ 不存在」：**缺席是信息，
但缺席不会自己跳出来。**

⚠️ **主持人和球员靠 `>>` 分，分错了整份原料全错。** 所以报告第一节先把轮次
打出来让人对一眼，而不是拿一份错的词频去写卡。

### 三道闸，都只吃 spec 不读产物

CI 的稀疏检出把 `output/` 挡在外面——拿产物当主语的判据在 CI 上会静静地
变成一盏恒真的绿灯（这条本文件里栽过一次）。所以 `check_takeaway` 只看
spec 和常量：

| 闸 | 拦什么 |
|---|---|
| 有没有 `takeaway` | 漏掉的样子是「片子照常出，只是又变回一条纯搬运」 |
| `open.point` 引的话在不在 `zh` 里 | **这张卡唯一致命的错法**，而它渲出来一点异常都没有 |
| ~~自有画面 ≥ `MIN_OURS_RATIO`（10%）~~ | ⚠️ **2026-08-08 撤销**，只报数不拒渲——见「`MIN_OURS_RATIO` 拒渲那道闸撤销了」那节 |

⚠️ **闸排在下载之前**，判据钉了位置（`test_解读卡那道闸要排在下载之前`）——
只测行为的话，闸排在 `yt_download` 后面照样全绿，而真实代价是每次写错字段
都要先等几百 MB 下完。

⚠️ **门槛跟着版式改过一次，这件事本身就是它该有的用法。** 原来 15% 是按
**两张**卡定的；砍掉落点卡之后只剩 12.6%，**恰好压线**——一条 80 秒的采访
就会红，而那不是它想拦的东西。重定在 **10%**：

- 仍是被判那条（0.8%）的 **12 倍**，「整段搬运 + 一张封面」照样过不去
  ——217 秒那个形状按新版式算只有 **5.5%**
- 一条 60~100 秒的正常场上采访，只靠收尾卡就过得去
- 两分钟以上的仍然会红，**那正是该按落点剪的信号**

⚠️ 改门槛要**连它的前提一起改**。15% 这个数在两张卡的世界里是对的，在一张卡的
世界里是一条常年红——**判据要跟着结构走，不是跟着当初那一版的数走**。
同一天里 `tk["open"]` 那道引文闸犯的是同一个错（写死了字段名，落点卡变可选
就 `KeyError`），现在两张卡都查。

### ⚠️ 加解读卡时，「两支各拼一次」这个坑要第三次发生

`render()` 原来按「有没有封面」分两支、各 `concat` 一遍。加片尾时只改了一支
（判据 `test_采访片两条路都要接片尾` 就是为那次写的），加解读卡是同一件事
第三次。所以这次把清单收成**一份** `parts` 列表：每一段只有一处决定它进不进去。

- 老那条判据的**主语没了**（`body.split('if not spec.get("cover"):')` 当场
  `IndexError`）。**主语没了就得换判据**，留着它就是一条常年红——现在钉的是
  「片尾只有一处 append，而且缩进是 4 格（没嵌在任何分支里）」
- ⚠️ 换判据时第一版写的是「前面最近的那个 `if` 不许是 cover」，**当场误伤**：
  `if spec.get("cover"):` 确实排在前面，但那是往清单里加封面的**单行块**。
  又一次「判据宁可窄，不可宽」——只不过这次糊的不是范围，是**层级**。
  判嵌套就用缩进，它就是嵌套本身
- 静音段的 ffmpeg 参数收进 `_still_segment`，判据要求全文件只出现一次
  `anullsrc`：这条线走 `concat` + `-c copy`，声道数差一项就静默丢流

### ⚠️ 而我给这套写的第一条判据，是被自己的说明文字满足的假绿

`test_提炼工具要把没说的词也打出来` 第一版断言 `"一次都没提" in rep`——
而 `report` 自己的说明文字里正写着「**「一次都没提」在榜上是看不见的**」。
于是把那个标记整个拆掉，**测试照样 1 passed**。

本文件里「判据扫得太宽，被自己的注释误伤」记过四次，**这次是它的镜像**：
那四次注释制造的是**误伤**，这次制造的是**假绿**——而假绿不会有人来告诉你。
判据要钉在**那一行**上（标签开头、同一行带标记），不是钉在整份文本里。

### 解读卡的版式：两处是量出来的，别按感觉调

- **垂直居中**。第一版按 `flex-direction:column` 从顶上堆，底下空掉一大半
  （`.ask` 那个 `margin-top:auto` 在没有 `ask` 的落点卡上顶不动任何东西）
- **右边距 150px，不是 92px**。这一屏正好落在小红书右侧点赞/收藏/评论那列
  的高度上——字幕那条规矩量出来的 150px 在这儿同样成立。腾出来之后
  `.point` 从 70px 收到 64px 才不换行
- **不放照片**。它必须一眼看得出不是转播画面，否则「我们的解读」和「他们的
  素材」在观感上糊成一片，等于白加
- **卡带口播**（见上面那节）。⚠️ 这一条推翻了我自己的第一版——那时写的是
  「静音，因为合成音会割断现场声」，而卡本来就是独立的一段

### ⚠️ 加口播时抖出四个 bug，三个被退路吞了真因

| | 表现 | 真因 |
|---|---|---|
| ① | 「口播合不出来」 | `ExplainerSegment` 前三个字段是必填的，只传 `narration=` 报 TypeError |
| ② | 同上 | **`build_interview_clip` 从来没挂过 `localca`** |
| ③ | **整条崩** | `_audio_seconds(path, ffprobe_bin, runner)` 要显式给后两个参数，而它排在 `try` 外面 |
| ④ | `KeyError: 'open'` | 引文闸写死了字段名 |

②值得单记：这条线在解读卡之前**一个字都不合语音**（片尾走的是母版转码，
不跑 TTS），所以从来没挂过代理 CA。加口播时它是「同时要改的第三处」——
而它报出来的样子是 `CERTIFICATE_VERIFY_FAILED`，被退路吞成一句
「口播合不出来」，**看起来像 edge-tts 挂了**。

①②能查出真因，全靠那句「合不出来就退回静音卡，**但两条路都要出声**」——
日志里写着原话。③没被吞，所以立刻现形。**退路要出声，这就是它买来的东西。**

#### ⚠️ 第五个，一周后才现形：runner 上根本没装 `edge-tts`

②修的是**沙箱**那一头（证书链）。**runner 那一头一直是坏的**，而它安安静静地
出了一条片子：`interview-clip.yml` 的装依赖那行写着

    pip install -q -e ".[visualqa]" "yt-dlp[default]" playwright faster-whisper

——**没有 edge-tts**。同一个「加新能力就要同时改三处」，②漏的是本地那处，
这个漏的是工作流那处，而它们是同一个提交该做的事。

**它绿着交付了一条不合格的片子**（run 31151599208，萨巴伦卡那条）：

    [解读卡] close 口播合不出来，退回静音卡：缺少 edge-tts，请安装后再生成解说视频
    [解读卡] close 5.64s（按字数估）

run 是 success，成片时长对、体积对、`验成片` 那一步过了、提交推送一切正常——
**只有量音量才看得见那 5.6 秒是 −91 dB**，也就是数字静音，而账号所有者对这张卡
的原话就是「**但要有配音**」。CLAUDE.md 里那条「两张静音卡那一版前 10.8 秒
峰值 −91 dB」说的正是这个东西，只是这次它绕过了所有的闸。

三处一起改，缺一处这个坑还会再来：

- **装上**（`pip install … edge-tts`）
- **预检里 `import edge_tts`**——让它死在第 5 秒。⚠️ 原来的预检 import 了
  playwright / faster_whisper / cv2 / 字体，**唯独没有它**，所以第 5 秒那道闸
  对这个依赖是哑的
- **判据 `test_会合语音的工作流都要装edge_tts`**，和「工具入口都要挂本地 CA」
  那条是同一个 bug 的两半：那条管沙箱的证书，这条管 runner 的依赖

⚠️ **判据不许递归。** 第一版顺着 import 往下扒，当场把 `frame-grab.yml`（抽帧）
和 `auto-push-explainer.yml`（只推送）也判成「要装」——它们导入的模块里有 TTS，
可它们自己那条路走不到。**扩大化的判据会让下一个人白装一个包，或者干脆把这条
检查删掉。** 收成「这个工具自己提没提 `synthesize_narration`」，和上一条同一个口径。

⚠️ **更该记的是怎么发现的**：不是靠读日志，是**渲完之后逐段量了一遍音量**
（封面 / 原声 / 解读卡 / 片尾各一次）。日志那两行当时就在，可它排在几百行
ffmpeg 进度中间，而 run 是绿的——**没有人会去读一条绿 run 的日志**。
所以「片子做完要抽帧看」这条得加一句：**也要量音量**，尤其是没有现场声的那几段。

#### ⭐ 而「抽帧看」现在**在本地就能做**：成片从 Release 拉得回来

上面那条和本文件里另外几处都在喊「渲完要打开看」，可我一直把它当成一件贵事——
成片只在 runner 上、沙箱下不动 YouTube，于是「看一眼」被默认成「再跑一趟 run」。
2026-08-15 试了一下，**这个前提是错的**：

    curl -sL -o /tmp/x.mp4 \
      "https://github.com/<owner>/<repo>/releases/download/interview-<slug>/<slug>.mp4"
    → 200，31,525,433 字节，ffprobe 读得动

**被挡的是 YouTube，不是 GitHub。** 而 2026-08-13 起成片一律走 Release
（见「100 MiB 是 git 的限制」那节），所以**每一条已发的成片都是本地可取的**——
`render.json` 的 `video_url` 就是那个地址，不用去猜。

##### ⚠️ 拉回来的那份**不许落在 `output/` 里**——下一次 `git add -A` 会把它吃进去

2026-08-17 差 30 秒就把一个 **41.8 MB 的 mp4 提交进了历史**。经过很平淡，
所以更值得记：`check_reel_landed.py` 按产物目录找成片，我图省事就把 Release 那份
`curl -o output/<date>/reel/<slug>/<slug>.mp4` 下到了它要找的位置；验完 0 项不合格，
顺手 `git add -A && git commit` 提交笔记——**mp4 一起进去了**。
`git show --stat` 第一行才看见。

⚠️ **三道该拦的闸一道都没响，而且每一道都「没做错」：**

| | 为什么没响 |
|---|---|
| `test_成片一律走Release不进git` | 它查的是 `git ls-files`，而那一刻文件还**没被跟踪** |
| 全量测试 | 跑在 `git add` **之前**（文件在盘上了，但那条判据看的不是盘） |
| 工作流的「丢掉不进仓库的中间物」 | 那是 **runner 上**的清理，管不到我在沙箱里下的这一份 |

也就是说**「一个躺在被跟踪目录里的未跟踪大文件」是个盲区**：它离进历史只差一个
`git add -A`，而这个仓库的提交习惯就是 `git add -A`。

- **下到 `output/` 外面**（scratchpad），要么**验完立刻 `mv` 走**
- **`git commit` 之后先看一眼 `git show --stat`**。它比「提交成功了」多花一秒，
  而这一秒挡的是一次改写历史——2026-08-13 那次清理花了 17 GB → 789 MB，
  代价是所有 commit SHA 都变了、已发消息里的图片链接可能失效
- 这跟「查产物，不查信号」是一家的：**`git commit` 返回 0 是信号，
  `git show --stat` 列出来的那几行才是产物**

拿回来之后能做的事，都是几秒钟的：

| 想知道 | 怎么量 |
|---|---|
| 封面停了多久 | 逐帧算 `meandiff`，连续为 0 的那一段就是封面（实测 45 帧 ＝ 1.8s，和 `COVER_SECONDS` 对得上） |
| **源片时刻 ↔ 成片时刻** | `源片 = 成片 − 封面长度`。有了它，成片上量到的任何一帧都能换算回 spec 的 `start`/`end` |
| 末尾混进了什么 | 每帧整幅均值，片尾板/台标卡是一帧跳上去（这次 31→71） |
| 字幕压在哪儿、对不对得上 | 拼一面带时间码的缩略图墙 |

⭐ **第二个用处：挑封面那一帧不用再猜。** 缩略图墙是 **4.95 秒一格**，照它挑
`cover.frame_at` 本质上是猜——德约那条发布会就栽了：420.8 那一格他坐直、侧头、
脸清楚，渲出来的 421.0 **他低着头、脸在阴影里、眼睛看不见**，0.2 秒之内换了个人。
**「看过缩略图」和「看过那一帧」是两件事，而它们写在 `_why` 里长得一模一样。**
成片一出来就不用猜了：拉回本地按 0.5 秒抽一遍，`源片时刻 = 成片时刻 − 封面长度
＋ 窗口起点`，24 格摆一起挑（那次挑中的是 424.5，坐直、四分之三侧脸、眼睛读得出）。
⚠️ **改封面要重渲整条片子**——成片开头那 1.8 秒就是海报本身，只跑 `mode=cover`
换出来的海报和成片里那一屏会对不上。

⚠️ **这条最直接的用处是「片尾板到底在不在窗口里」**。德约那条 spec 的
`_window_why` 白纸黑字写着「末尾那几秒是 Tennis TV 的片尾板，不收」——**而它就在
窗口里面**，第一版成片最后 1.7 秒放的就是那块板。那句话是**照着源片总长推的**，
不是量的；而它一旦写进 spec，读的人就会当判据用。**又一次「查产物，不查信号」，
只不过这次的产物已经躺在 Release 上，取回来只要几秒。**

##### ⭐ 而「离源片末尾还有 N 秒」**不是**对片尾板的余量——它正好就是片尾板

德约那次的教训我第二天就换了个形状又踩了一遍。诺斯科娃那条（WTA 官方集锦
443 秒）我把窗口收在 **434.0**，`_window_why` 写着「离源片末尾还有 9 秒，
片尾板必然落在那之后，**顺带结构性地躲开**」。渲完拉回来逐帧看——

    源片 432.66   她还在挥手致意（最后一帧真内容）
    源片 432.78   **黑场一帧**
    源片 432.90   **WTA 绿标甩入**（整幅均值 6.6 → 94 → 107，一帧跳上去）
    源片 434.0    绿标定版，一直到源片结束

也就是说那 9 秒的「余量」里，**有 8 秒是片尾板本身**。

**根子是一个很容易漏的前提：末尾那几秒没有字幕事件，不等于那几秒还是内容。**
字幕流在说话结束时就断了，而片尾板**本来就不产生字幕**——所以拿「最后一条字幕
离源片末尾多远」当余量，量的恰恰是片尾板有多长。⚠️ 这跟「空结果 ≠ 不存在」是
一家的：**那一段没有字幕，正说明那一段可能不是人在说话。**

**判据只有一个，而且很便宜：渲完把成片从 Release 拉回来，在窗口末尾前后逐帧
量整幅均值。** 片尾板是一帧跳上去的（这次 6.6 → 94），跟人物画面差一个量级，
扫一遍就看见。**别再用「离末尾还有多少秒」去推。**

###### ⭐⭐ 同一天第三次，而这次翻车的是**我用来否掉正确答案的那个数**

蒂兰特那条（Tennis TV，363 秒）我手上已经有一条现成的推算：

    德约那条 Tennis TV      115 − 104.76 = **10.24 秒**
    诺斯科娃那条 WTA        443 − 432.66 = **10.34 秒**
    照它推这条              363 − 10.3   ≈ **352.8 秒**

**我把它否了**，理由写在 `_window_why` 里：「那会切进他最后一句话」——因为最后一条
字幕事件写着 `tStartMs 352.16 + dDurationMs 3.32 = 355.48`。于是 `end` 写成 355.5，
渲出来**末尾多了 2.9 秒 Tennis TV 片尾板**，正是账号所有者点名要去掉的那个东西。

真去量：

    源片 352.58        均值 88.2，他还在画面里
    源片 352.62        均值 **12.3**，一帧掉下去 —— 片尾板甩入
    音频 352.40–352.60  mean −27.6 / max −12.5 dB，人声还在
    音频 352.60–352.90  mean **−91.0** dB，**数字静音**

363 − 352.62 = **10.38 秒**——**那条推算一直是对的**。

⚠️ **`dDurationMs` 是「这一行在屏幕上挂多久」，不是「他说到第几秒」。**
自动字幕会把最后一行挂到画面结束，所以拿它当语音边界**会系统性地偏长**，
而偏长的方向正好是「把片尾板收进来」那一头。要语音边界就量音频
（`volumedetect` 分窗，数字静音是 −91 dB），几秒钟的事。

⚠️ **更该记的是这次的形状**：我不是没有正确答案，**我有，而且把它算出来了**，
然后拿一个自己没核过的数把它否掉。这跟本文件里「排除了 A 和 B 不等于就是 C」
是一家的，只是这次多了一层——**否决也要有判据**，而「字幕事件写着 355.48」
是一个信号，不是产物。

###### ⚠️⚠️ 而量的时候 `-vf fps=25` 会差一帧——收到 352.6 之后成片里还留着一帧黑闪

同一条片子第二次返工，这次错的是**量法本身**。我一直这么抽帧：

    ffmpeg -ss <T0> -t <N> -i film.mp4 -vf fps=25 out/f_%03d.jpg
    # 然后按 T0 + n×0.04 给每一格标时刻

**`fps=25` 是重采样滤镜，它把帧摊到自己的网格上**，而那个网格的相位由 `-ss` 决定——
换个 `-ss` 起点，同一帧标出来的时刻就变了。同一帧我两次分别读成 **352.62** 和
**352.60**，真值是 **352.60**（源片帧栅格 `227.92 + n×0.04`，`-ss 227.9` 落到最近
的一帧 227.92）。于是 `end` 收到 352.6 → `-t 124.7` → 那一帧相对 pts 124.68 < 124.7
**照收**，成片里留下一帧整幅均值 12.3 的黑闪。

**要真实帧就两样一起用**：

    ffmpeg -ss <T0> -i film.mp4 -vsync 0 -frames:v 12 -q:v 4 out/f_%03d.jpg   # 不重采样
    ffprobe -v error -select_streams v -show_entries frame=pts_time -of csv=p=0 film.mp4

⚠️ **pts 有固定偏移**，别假设它是 0.04 的整数倍：这条线的成片实测是
`0.021016 + n×0.04`。所以「第几帧」要按 ffprobe 报的 pts 数，不要自己乘。

⚠️ **判据是拿另一条片子当对照组，不是盯着这一帧猜。** 诺斯科娃那条同一个位置是
内容 78.9 **直接**跳解读卡 24.8，中间没有黑帧——也就是**渲染器不会自己插一帧黑的**，
那一帧只能是源片带进来的。一帧的东西「是源片的还是我们自己的」肉眼分不出来，
而这个对照一秒钟就给出答案。

#### ⚠️ dispatch 之前先跑一遍这条 spec 自己的测试——两条本该拦住的红都在本地过了

`eala-mcnally-toronto-2026-r3-presser`（PR #250）CI 红了两条，**两条都有现成的测试
在等着**，只是我没在推之前跑：

| 红的是什么 | 拦它的测试 | 我当时干了什么 |
|---|---|---|
| `transcript_verified: true` 却没有 `en_fixed`/`_verified_clean` | `test_没验过转写的spec不许标verified` | 写了个自己起的字段名 `_transcript_verified_why`，没意识到这个仓库已经有约定字段 |
| `push.summary` 15 字，超 `SUMMARY_MAX=13` | `test_spec里的推送字段拼得出合格标题` | 手数了字数，没有真的调 `headline()` |

**这条和上面「全量测试要排在 `mode=render` 之前」是同一个道理，只是那条写给
match-reel、这条是 interview-clip 一直没补的那一半。** 两条测试都只吃 `specs/`，
本地跑 `pytest tests/test_interview_clip.py -k <slug>` 零点几秒就出结果——而我
是靠手写脚本模拟 `headline()`、凭记忆核对字段名，**自己核对的东西和测试真正查的
东西不是一回事**，又是这个仓库反复栽过的那个形状。

**顺序钉死**：改完 spec → **本地跑 `pytest tests/test_interview_clip.py -k <slug> -q`**
→ 绿了才 commit/push/dispatch。不是「我觉得改对了」，是这条命令回一句 `passed`。

### ⭐ 2026-08-08：`MIN_OURS_RATIO` 拒渲那道闸撤销了——完整性优先于占比

账号所有者：「就不该有限制时长的这个闸」「要保证原始内容的完整性啊」。

**来路**：`eala-mcnally-toronto-2026-r3-presser` 是一场**完整**的赛后发布会
（462 秒），而 `check_takeaway()` 第③道闸要求「自有画面（封面+解读卡+片尾）
÷ 全片 ≥ 10%」——解读卡字数有上限（`TAKEAWAY_MAX_CHARS=34`），撑死也就
17 秒左右自有画面，倒推出**全片不能超过约 171 秒**这道硬顶。为了过这道闸，
我把这场完整发布会硬挖成了一段 150.5 秒的问答，账号所有者问「为啥裁剪了
这么多」，答案本质是「为了凑一个跟内容完整性无关的比例」。

**这道闸原来是故意这么设计的**（见 `MIN_OURS_RATIO` 上面那段旧注释：
「两分钟以上仍然会红——那正是该按落点剪的信号」）——但那个设计假设本身就是
现在被推翻的部分：**片子该多长，由这段内容本身决定**，不该被一个和内容完整性
无关的比例倒逼着剪短。

**现在的处置**：`ours_ratio()` 照旧算、`check_takeaway()` 照旧打印这个数，
但**不再拿它 `raise SystemExit`**。解读卡本身该有的两道闸（有没有解读卡、
引文是不是他真说的）**一个字没松**——撤销的只是「占比够不够」这一条，
不是把「别人的画面+我们的字幕、连一句解读都没有」那种真的纯搬运也放行了。

⚠️ **这不等于「越长越好」。** 完整性说的是"这段内容该有的长度"，不是鼓励
无谓拉长——单段连续裁剪仍然是这条线的技术边界（见上面「为什么只收
40.5–191.0 秒」那段 PR 描述里的道理），选段仍然要选一段完整、连贯的问答，
只是不再为了凑占比而人为砍短。

判据在 `test_自有画面占比低不再拒渲只报数`：217 秒占比 0.8%~3.4% 这种
过去必挂的形状，现在不许再抛 `SystemExit`，只许在日志里报数；反向验证过
（把 `raise` 加回去，测试当场红）。

#### ⭐ 2026-08-12：进一步定死——采访片**没有时长上限**，别过度剪辑

账号所有者，就在这条线上：「不要过多剪辑，需要保留原视频主要内容」
「采访视频没有时长限制」「记住了」。

**这句话比上面那条 08-08 的走得更远**：08-08 撤的是「占比不够就拒渲」那道闸，
末尾还留了一句「单段连续裁剪仍然是这条线的技术边界，选段仍然要选一段完整、
连贯的问答」——读起来像是在说「还是要挑一段，只是不用为了凑比例砍短」。
**这条把那层暗示也否掉了**：不是「挑一段完整的问答」，是**保留原视频的主要
内容**，而且**没有时长限制**——账号所有者要的默认不是「剪出一段」，是
「这条采访本来讲了什么，就留住什么」。

- **别再把「片子短一点更抓人」的直觉带回采访片。** 那条直觉对「赛场之上」
  成立（本文件「片长可以商量，画质不商量」那节讲的是另一个栏目），对「赛后
  开麦」这条线现在是反的——408、508 秒的完整发布会/采访，只要内容本身
  站得住，不用为了「怕太长没人看」去挖短
- **`start`/`end` 单段连续区间那条技术边界没变**（工具本来就不支持拼接
  非连续片段），但**在这个技术边界内，默认取值要偏向「多留」不是「少留」**：
  拿不准某几分钟要不要剪掉时，不剪
- 仍然要剪掉的：**不是采访本体的部分**（片头的比赛集锦/解说、片尾的场务
  空镜、长时间的技术故障静默）——「保留主要内容」说的是访谈这一段，不是
  连集锦也不让剪

### ⭐ 封面帧一律不许闭眼——storyboard 的粒度盖不住几分之一秒的眨眼

账号所有者 2026-08-21：「封面图一定不要闭眼的」。

来路：`pegula-anisimova-cincinnati-2026-qf` 第一版封面（345.9s）渲出来是她
**双眼紧闭、嘴巴半张**——一个眨眼瞬间被当成了封面。挑帧时用的是
`--stage subs` 给的 storyboard，**每格 4.95 秒**，而眨眼只占几分之一秒，
分辨率天生盖不住它；同一轮 `keys-wangxiyu` 也踩了近似的坑（挑到一帧微皱眉、
说到一半的过渡表情）。

**判据不是「有没有字幕内容支撑这一秒」，是「这一帧本身长什么样」**——挑帧时
按字幕内容推出一个时间点就直接写进 `cover.frame_at`，等于没看过这一帧。
现在的做法（`⭐ 而『抽帧看』现在在本地就能做`那节）是**渲完之后把成片从
Release 拉回本地，在候选时间点前后逐帧抽样**（0.5~1 秒一格），肉眼选一帧
**眼睛睁开、看得清表情**的，优先选**正在说笑/情绪外露**的瞬间（同一节
「⭐⭐ 优先近景特写」那条的延伸——脸是这一屏的主体，眼睛是脸上最不能糊的
那一块）。

- **闭眼、半闭眼、眼球在转（对不上焦）** 一律不用，哪怕嘴型和字幕对得很准
- **不确定就往前后各挪半秒再抽一帧**，多花几秒钱远比再渲一遍便宜
- ⚠️ **这条和「渲完仍要拉回成片核对这一帧的表情」不是一回事**——后者是
  「渲完再核对」，前者是**核对的具体标准要包含「眼睛睁没睁开」**，不能
  核对完一圈还是选中一帧闭眼的

### 文案不许再提中英双语字幕——那是制作规格，不是这场球的内容

账号所有者 2026-08-19：「以后不要再在文案里说中英文字幕相关的文案」。

来路：`sabalenka-wang-cincinnati-2026-r3` 推送之后账号所有者当场纠正。查下去
才发现这是这条线上的老习惯——几乎每条片子的 `push.lead` 和 `.xhs.txt` 都以
「中英双语字幕，全程。」或「🎤 中英双语字幕」收尾，78 个文件（39 条片子的
spec + xhs.txt 各一份）全带着这句话。

**判据很简单：读者关心这场球发生了什么，不关心我们用什么字幕方案做的。**
双语字幕是每一条片子都有的制作规格，不是这一条片子独有的内容——放在文案里
等于用一句和这场球无关的话占掉本该讲事实的篇幅，正是「贴近比赛事实，且有
吸引力」那条要防的反面。

- **已发的片子不重渲，微信消息发出去收不回来**——那 78 个文件挂进
  `_LEGACY_BILINGUAL_MENTION`（`tests/test_interview_clip.py`），只许减不许加
- **判据只查会发出去的字段**：`push.*`、`.xhs.txt`，`_` 开头的注解跳过——
  `_copy_note` 里正引着账号所有者这句原话，连它一起扫会把「把规矩记下来」
  判成「又违反了规矩」，本文件反复栽过的那个坑

#### ⚠️⚠️ 2026-08-29 它又被违反了一次——而**闸只在转正那一刻跑，手改在闸后面**

`nishikori-sakamoto-us-open-2026-q3-farewell`（锦织圭告别片）的 `push.lead` 里
写着「完整保留现场原声与**中英双语字幕**」，`opening.why` 里写着「与**双语字幕**」，
**而这条 spec 是 2026-08-29 建的，比规矩晚十天**——不是存量，不该挂豁免表。

⚠️ **别急着怪那道闸。** `promote_interview_draft` 的 `check_interview_copy_wording`
**跑了，而且放行是对的**——逐个提交查过，转正那一版（`a5ee8ab`）的 `push.lead`
干干净净。时间线是这样的：

| 时刻 | 发生了什么 |
|---|---|
| `a5ee8ab` 转正 | `push.lead` **没有那句话**，闸放行 ✅ |
| `a429213` **06:53:25Z** | **手改**（「按无冷开场版本完成 QC」）把那句话写了进去 |
| **06:53:49Z**（**24 秒后**） | `push.auto: true`，微信推送发出去了 |

**也就是说这条线的措辞闸只有一个 seat，在「转正」那一刻；转正之后的手改直到
CI 跑 pytest 才被发现，而那时消息已经发出去了。** 这次那条红还落在另一条分支
（比分板那条 PR）的 CI 上，隔着一条线，更没人当场看见。

⚠️ **修的是文本，不是豁免表**：`push.lead` 和 `opening.why` 里那半句删掉
（`opening.why` **不是 `_` 开头，所以判据扫得到**，它也确实该扫——那一栏会被
下一个人当判据读）。⚠️ **`push.summary` 一个字没动**——它要和已发的
`copy.html` 标题逐字相同（`8.29 赛后开麦 | 锦织圭告别大满贯`，本来就不含那句），
这是本文件「跟着错的对齐 vs 以规矩为准」那条的老规矩。已发的那条微信里仍然是
旧文案，**收不回来，这是代价**。

⚠️⚠️ **这个洞还开着，没在这一轮修**（它属于采访线，而那条线上 #644 / #648 两个
PR 正在改同一条工作流；把 `check_interview_copy_wording` 那个**零豁免**的闸
接到推送路径上，会当场红掉 40 条存量 spec，需要配一张自己的豁免表——那是
那条线的口径选择，不是顺手能定的）。**记在这儿，别忘了**。真要补，形状是
本文件反复写过的那一条：**凡是只读 spec 就能判的规矩，出处必须排在那次
不可撤回的 POST 之前**，而不是只排在转正那一刻。

## ⚠️ 一条能读的推送，和两条只有链接的推送

账号所有者 2026-08-05：「目前推送的内容还有两块，英文媒体一份中文一份，
**而且也看不到具体内容**……合并成一个做推送，英文的话要有对应的中文翻译，
**主要能深入挖掘热点**」。

在这之前场外新闻一天发两条微信，两条**加起来**是一份英文标题清单：

| 原来 | 里面是什么 | 毛病 |
|---|---|---|
| 「场外网球快讯候选」 | 8 条**英文标题** + 链接 | 全英文；想知道发生了什么得逐条点开 |
| 「今日选题候选」 | 撞上人工角度表的簇 | 2026-08-02 实测 **0 个**，正文只剩一栏中文热词 |

**热度本身一直是有的**（那天中文平台「郑钦文资格赛出局」58 万热度、
英文 35 条官方新闻聚出 4 个簇），只是**全都停在标题层，一条正文都没读过**。
所以真正缺的不是筛选，是**读**。

判据是**读者要不要自己做拼接**：在两条消息之间拼、点开英文页面拼——都不该。

### ⚠️ Google News 的链接抓不到正文，而它返回 200

这条差点让「深挖」带着测试和文档合并进去、然后一辈子不工作。

`trends.py` 走的是 Google News RSS（`site:atptour.com/en/news` 那套查询），
它的 `link` **不是发布方的地址**，是一个包装页：

    https://news.google.com/rss/articles/CBMickFVX3lxTE1n…?oc=5

这个页面 **590 KB 全是 JS**，真地址一次都没出现在 HTML 里（`atptour` 这个词
整页搜不到），`CBMi…` 那串 base64 解出来是不含 URL 的服务端令牌。
于是抓正文**返回 200、解出 0 段**——和「这家有付费墙」长得一模一样。

**是拿真数据跑了一趟 `tennislive brief`、去查「为什么深挖是 0 条」才发现的。**
又一次「写了不等于跑过」，和 `_cut_person(source, …)` 那次同一个形状。

- **发现和阅读分两路**：Google News 覆盖广（含 ATP/WTA 官网）但读不了；
  发布方原生 RSS 源少但链接是真地址。两路一起进聚类，同一件事共享专名自然
  并成一簇——那一簇既有官网标题撑热度，又有一条读得到的链接供深挖
- **在抓之前就认出读不了的链接**（`article.readable_url`），否则每条热点都要
  白花一次请求，还会把「源头就读不了」伪装成偶发的抓取失败
- ⚠️ **ATP / WTA 官方没有能用的 RSS**，探过一轮别再试：`atptour.com/en/rss`、
  `/en/media/rss-feed/xml-feed` 一律 **403**（和「ATP 总站对本环境一律 403」
  同一件事）；`wtatennis.com/rss.xml`、`/feed/rss`、
  `api.wtatennis.com/tennis/news/rss` 一律 **404**。实测通的四家：
  BBC Sport、The Guardian、Sky Sports、Tennis Majors
- **加新源的判据不是 200**：还要 item 数 > 0、link 是发布方域名、
  而且 `fetch_article` 真能解出段落。三条缺一条就是又一个哑源

### ⚠️ GitHub Models 正在退役——两个消费者都清干净了（2026-08-05）

这条线一开始接的是 `models.github.ai/inference`。2026-08-05 实测：

    HTTP 410  {"error":{"code":"github_models_retirement_brownout", …}}

也就是**那个端点正在退役**。真接上去的话，「中译」和「要点」会在上线当天
静默失效，而失效的样子是「今天没有要点」——和「今天新闻少」一模一样。

简报改走 **Anthropic 官方 SDK**（`ANTHROPIC_API_KEY`，`pip install -e ".[brief]"`）。

全仓库一共两个消费者，当天一起清掉：

| 谁 | 怎么处置 |
|---|---|
| `video/pipeline.py` 的字幕翻译器 | 换成 `ChatTranslator`，走**和简报同一个** `research.brief.Chat` |
| `render/ai_editorial.py` | **删掉**——它唯一的消费者是日报，而日报 2026-07-31 停产，零调用方 |

⚠️ **它坏起来的样子不是「端点没了」，是一句「翻译失败」**：`raise_for_status`
把 410 变成 `... translation failed: 410 Client Error`，读到的人第一反应是去查
网络或 token。所以判据拦的是「有人照着旧代码又接一次」
（`test_不许再指向已退役的GitHub_Models端点`，两头都钉：代码里不许有那个 host、
工作流里不许再配 `GITHUB_MODELS_TOKEN`）。

⚠️ **那条判据必须用 AST，不能按文本扫。** 这个 URL 正写在 `pipeline.py` 的
docstring 和好几处注释里——那是记教训的地方。按文本扫，「把坑记下来」会被判成
「又踩了这个坑」（同一个错这个仓库犯过五次）。注释根本不进 AST，再把 docstring
摘掉就够了。反向验证的第 ② 项专门验这一头：**只在注释里提那个 URL，必须是绿的**。

⚠️ **字幕和标题中译共用 schema，但「少了一条」的口径相反**，别照抄：

| | 少了一条 |
|---|---|
| 标题中译（`translate_titles`） | **不填**，照旧显示英文原标题——半句机翻比原文更难读 |
| 字幕（`ChatTranslator`） | **报错**——烧进画面的字幕缺一条就是一段没有字幕的画面 |

⚠️ **默认通道后来换成了 DeepSeek**（账号所有者手上是这个），下一节；
这一节讲的三条对 Anthropic 那条通道照旧成立。

三个用法上的点，都写进了判据（`test_发出去的那次调用要带schema和模型`）：

- **形状交给 `output_config.format` 的 json_schema**，不靠在 prompt 里写
  「只返回 JSON」再自己 try/except——那一路会把「模型多说了一句话」变成
  一次静默降级
- ⚠️ **`max_tokens` 要留出思考的量**。Opus 5 默认开着思考，而 `max_tokens`
  卡的是**思考 + 正文的总和**；按正文长度卡（1200）会在中途截断，
  症状是要点少一条或最后一句没写完，**不报错**
- ⚠️ **`temperature` / `top_p` / `top_k` 在 Opus 5 上会 400**，别让它们混进来

### ⭐ 换成 DeepSeek：兼容端点看着最省事，而它废掉的正好是唯一的保证

账号所有者 2026-08-05：「**我没有这个〔ANTHROPIC_API_KEY〕，deepseek 的可以用么**」。

可以。而这一节记的是**怎么接**——因为最省事的那条路是错的。

DeepSeek 有两个入口，我的第一反应是走第二个（`base_url` 一改，SDK 都不用换）：

| 入口 | 格式 | |
|---|---|---|
| `api.deepseek.com/chat/completions` | OpenAI | **现在走这条** |
| `api.deepseek.com/anthropic` | Anthropic | ❌ 见下 |

官方文档在兼容端点那页写明了两件事，**两件都是静默的**：

- **`output_config` 只支持 `effort`**——也就是 `format` 那半截（json_schema）
  **收下然后忽略**。而 `output_config.format` 正是上一节写的「形状交给 API 保证」
  的全部内容，走这条路它凭空消失，调用方一个字都收不到
- **传一个它不认识的模型名，后端自动映射成 `deepseek-v4-flash`**——模型名写错
  不报错，只是悄悄换一个模型把整份简报跑完

两条都是「兜底出事的时候不吭声」的标准形状。而 OpenAI 格式那条路的
`response_format: {"type": "json_object"}` 是文档明写支持的
（「guarantees the message the model generates is valid JSON」），
**而且一个新依赖都不用装**（`requests` 是主依赖，`[brief]` 那个 extra 只给
Anthropic 那条路）。判据 `test_不许改走DeepSeek的Anthropic兼容端点` 钉在 URL 上，
⚠️ 扫之前要**去掉整行注释**——上面这段教训就写在 `brief.py` 的注释里。

四个用法上的点：

- **`json_object` 模式官方要求三条，缺一条都不行**：`response_format` 要设、
  **prompt 里要出现 `json` 这个词并且给一个例子**、`max_tokens` 要够
  （不然 JSON 从中间截断）。少了第二条，官方文档说模型会「生成无尽的空白
  直到打满 token」
- ⚠️ **那个例子从 schema 现渲**（`json_shape_hint` + `_skeleton`），不手写第二份。
  一份 schema 两条通道共用——手写必分叉，而分叉那天模型照旧形状回，
  **解析不报错**，只是字段对不上，退化成「模型没给出要点」
- ⚠️ **`thinking` 默认是开的**（v4 两个型号都是），所以 Opus 5 上那个
  「`max_tokens` 卡的是思考+正文」的坑在这儿一模一样。翻译和抽取用不上思考，
  直接 `{"type": "disabled"}`——关掉之后 `max_tokens` 只算正文，坑就不存在了
- ⚠️ **官方自己记着「偶尔返回空正文」**。不单独出声的话，它和「模型答不上来」
  长得一模一样。`logger.warning` 里点名这是已知问题，4xx 那支**要把响应体带上**
  （余额不足、密钥错、模型名不对全写在里面，只报一个状态码等于让下一个人
  再跑一趟才知道为什么）

**选哪条通道要说出来，而且写错要报错。** 和「图片通道 pushplus / jsdelivr」
同一条：`brief.json` 的 notes 和推送正文底部都印着 `模型通道 deepseek · deepseek-v4-pro`
——一个月后有人问「这份简报的要点是谁写的」，答案在当天的产物里。
⚠️ `TENNISLIVE_BRIEF_PROVIDER` 拼错**当场 `ValueError`**，不退回另一条：
环境里往往有一个能用的密钥，静静换一条通道在日志上和「他本来就想用这条」
一模一样，而跑完整份简报的是另一个模型。

⚠️ **`Chat(api_key="")` 是显式的「没有密钥」，不许落回环境变量。** 原来写的是
`api_key or os.environ.get(...)`，于是环境里有密钥时它会突然变成 ready——
测试在沙箱和 CI 上走两条路而两边都绿。顺手把这两个密钥加进了 `conftest.py`
那条 autouse fixture（它本来只挡 `GITHUB_TOKEN`）。

**跑过了，判据成立**（2026-08-05 06:41Z，run 30982231886）：

    模型通道 deepseek · deepseek-v4-pro
    新闻 72 条 → 热点 9 个，深挖 8 个；中文平台扫了 231 条
    PushPlus 返回：{"code":200,"data":"08c27f60a0fe4f739276af7979f625a3"}

要点是真从正文里抽的（「第三盘德雷珀发球时速骤降，疑似左臂伤势复发」），
译名也对得上表（Draper → 德雷珀）。

⚠️ **`REQUEST_TIMEOUT = 60` 那段注释里的账算漏了一项，方向还偏保守。** 我按
「13 次调用 × 60 秒」估的是**上界**，而实测**整个「生成今日简报」这一步只用
了 80 秒**（06:42:11 → 06:43:35），里面还包括 Google News 查询、四条 RSS、
中文热搜和二十来次抓正文——也就是说关掉思考之后 DeepSeek 单次只要几秒。
那个 60 秒是「挂死了」的闸，不是常态，**别按 80 秒这个实测值往下调**。

#### ⭐ 人工角度表算出来了，却没喂给写 `angle` 的那次调用

「⭐ 选段的机械判据早就算好并落库了，只是没人在写 spec 的时候用」的**第二个
实例**，形状一模一样，只是这次没被用上的是**这份简报里唯一不是机器产的东西**。

`match_angle` 在 `build_brief` 第一轮循环里就撞上了常青线，而它排在深挖**之前**；
`deepen()` 却没把它传下去。2026-08-05 那趟的第 1 条：

| | |
|---|---|
| 常青线（人工角度表匹配到的） | **保护排名** |
| angle（模型写的） | 球员**持外卡参赛**后伤病复发……符合 ATP 外卡规则 |

四条 points、四条原标题里**一个「外卡」都没有**——德雷珀是直入签表的前世界第四。
而「因骨挫伤退出温网后首次参赛、近一年排名大幅下滑」正是保护排名的教科书场景，
对的那条线**就在同一个 Story 对象上**。

- **产物里两栏并排印着，看起来像「模型有别的想法」**，其实是它根本没收到。
  又一个「兜底出事的时候不吭声」——两栏各说各的，不报错
- 修法是 `known_angle_hint()`：把算出来的那条线喂进 system prompt。
  **不是再调 prompt 的松紧**——我在这上面来回收放了两轮，方向从一开始就错了
- 判据 `test_撞上的常青线要真的喂给模型` **钉两头**：有角度时喂了、
  **没角度时不许凭空多出这一段**。只验前一头的话，一个恒真的 hint 也能过。
  两个方向分别反向验证过，红在不同的断言行

⚠️ **`angle` 写得好不好仍然没有测试，是故意的**（判断题机械挡不住）。
能量的只有「喂了没有」这一半——**把能量的那一半量住，别为量不了的那一半
硬凑一个会被绕过的判据**。

#### ⚠️ 而我在同一轮里用 `git reset --hard` 把这三处改动全抹了

本文件写着「反向验证不许用 `git checkout` 还原——它会把没提交的新代码一起
删掉」，我这次换了个更狠的命令干了同一件事：切到分支之后顺手
`git reset --hard origin/main`，想的是「对齐一下基线」，而**工作区里三个文件的
改动一个都没提交**。

- **`git checkout <文件>` / `checkout -B` / `reset --hard` 是一家的**：
  都以「重置」为业，而**被重置掉的东西不会有人通知你**——退出码 0，
  `git status` 干干净净，看起来像「本来就没改过」
- 救回来靠的是反向验证时那份 `cp` 备份（`brief.py` 全须全尾），
  另外两个文件是照着对话里的原文重写的。**做反向验证时的 `cp` 备份，
  顺手就是一次快照**——这次它值回票价了
- 想对齐基线用 `git rebase origin/main`（本文件早写过），或者先
  `git stash`。**别拿重置类命令当「对齐」用**

### ⚠️ `read -t N -u 3 3</dev/null` 一秒都不等——我据此报了个错结论

同一天，等这条 run 的时候踩的。沙箱里前台 `sleep` 是禁的，我拿这个当替身：

    for i in $(seq 1 40); do 检查一下; read -t 15 -u 3 3</dev/null || true; done

**`/dev/null` 是立刻 EOF**，所以 `read` 立刻返回失败，`|| true` 把它吞掉——
四十轮在几毫秒里跑完，而它打印出来的是「poll-done after 40 rounds」，
**和真等了十分钟一模一样**。

代价不是慢，是**一个错结论**：我据此认为「十分钟了产物还没落库」，去查
`list_workflow_jobs` 看到步骤 5 在跑，就报给账号所有者「这一步已经跑了 11
分钟，job 超时 20 分钟」——**而那一刻它才开跑一分半，两分钟后就成功了**。

- 这和 `until ! pgrep -f 'X'` 会匹配到它自己是同一族：**用来等待的那个机制
  自己失效了，而失效的样子和正常一模一样**
- **等待要拿一个会前进的量自证**：循环里打印 `date`，或者比对开始和结束的
  时间戳。我那条循环末尾其实打了 `date -u`，只是**打在循环外面**——
  打在里面第一轮就露馅了
- 更省事的还是让被等的东西自己在后台跑完通知（`run_in_background`），
  别自己搓等待原语

### ⚠️ 「拒绝但带着半截文本」——只有这一种情况那道闸才是活的

`stop_reason == "refusal"` 那道闸，第一版的测试喂的是**空 content**——
而空串本来就会在 `json.loads` 那儿抛、被兜住返回 None。于是把整道闸拆掉，
**测试照样绿**。

真正只有它拦得住的是**拒绝了但仍带着半截文本**：那半截能解析成合法 JSON，
不拦就会被当成一条正常要点用上去。

又一次「判据自己也要有判据」。⚠️ 配套：`caplog` 必须收在 **WARNING**——
那句告警就是 WARNING，收在 ERROR 上它根本进不了 caplog（本仓库栽过一次：
坏代码 + 错档位，测试照样绿）。

### ⭐ 查得够深、查得够广、两个源对得上——否则那句话不许发

账号所有者 2026-08-07，在一条错数据被读者当众指出来之后：

> 「主要是你的信息要**准确无误**，最好经过**多个信息源交叉验证**过。同时查询的
> 信息的条件和要求要**覆盖面足够广足够深**，才不会导致你误判。」

**来路**：`chwalinska-gibson` 说赫瓦林斯卡「硬地巡回赛正赛零胜」。读者在小红书
评论区回了一句「不是硬地赢过达尼洛维奇么」，账号所有者只能公开更正并道歉。
真实记录是**至少三胜**——2026-02 克鲁日那波卡正赛连胜博格丹和 7 号种子
达尼洛维奇（打进 1/4 决赛）、2024-10 梅里达正赛胜马里诺。

两层都没做到，而且**两层各是一个独立的毛病**：

| | 当时做的 | 该做的 |
|---|---|---|
| **深** | 读了「场地＋级别」的**合计数**（硬地巡回赛 6 胜 4 负），据此断言「那 6 场**全部是资格赛**」 | **断言的粒度 ≤ 查询的粒度**。说「轮次」就要查到轮次那一列——而 Q1/Q2 和 R32/R16 就写在同一张逐场表里 |
| **广** | 只看了 tennisabstract 一个源 | 换个源就露馅：维基那一页**直接引着 WTA 官方标题**「Polish qualifier Chwalinska beats Marino for **first hard-court win**」——一句话推翻「零胜」 |

#### 三条可执行的判据

**① 断言的粒度 ≤ 查询的粒度，断言的范围 ⊆ 查询的范围。**
「合计数」天生分不出它汇总掉的那一维。说轮次就查到轮次，说生涯就别拿
「2025 年以来」那张表，说某一级别就别拿混着青少年和资格赛的赛事级汇总。
⚠️ 我在**改正这条错误的同一轮里又犯了一次**：顺手写了「大满贯和 WTA1000
还没有胜场」，依据是赛事级的「澳网 8 场 5 胜」——那张表把青少年和资格赛
拌在一起，同样分不出正赛。十分钟内两次，形状一模一样。

**② 全称断言要两个独立源。** 「零胜 / 一场没赢过 / 唯一 / 史上第一 / 从来没有」
这一族**一个反例就倒**，所以不能从「我查到的那几场」推，只能从**能穷举的表**读，
而且要两个不同的站点对得上。判据落在 `_absolute_claims_need_a_source`：
写了这种话就必须在 spec 的 `_claims` 里认领两个不同主机的 URL，闸排在
**下载源片之前**（`--dry-run` 就红）。

**③ 查空一类不等于查空全部**（本文件早写过，这次是它的又一个实例）。
数据接口、编辑稿、画面自证是三类源；这次真正给出反证的不是任何一个数据接口，
是**WTA 自己的一篇稿子的标题**。

⚠️ **闸只管全称断言，管不了「他这一场打得最好」这种主观话**——机械挡不住，
硬凑一个会被绕过的判据比没有更糟（本文件里几条「故意没有测试」是同一个理由）。
覆盖不到的那部分，靠的还是写之前问一句：**这句话要是错的，一个反例长什么样？
我查的那张表看得见它吗？**


#### ⭐⭐ 「一共只 N 次，N 次全 X」是全称断言的一种，而 `_ABSOLUTE_CLAIM_RE` 不认它

账号所有者 2026-08-27 看着成片截图说：「不是三次大满贯决赛吧」。

`wawrinka-wildcard` 第 ④ 屏写着「他**一共只进过三次**大满贯决赛，**三次全部拿下**」，
示意图标题「三次大满贯决赛，三次都赢」。**是假的**：瓦林卡打过**四次**，
第四次是 2017 法网决赛 2-6 3-6 1-6 负于纳达尔。

⚠️ **它是「零胜／唯一一个／史上第一」那一族的同类**——一个反例就倒——
**而当时那道闸的词表认不出它**：`_ABSOLUTE_CLAIM_RE` 收的是
`零胜|一场没赢过|唯一一个|史上第一|从来没有` 这类**否定式和最高级**，
而这一条的形状是**计数式**：`一共只 N 次` ＋ `N 次全部/全赢/都赢`。
判据要跟着形状走，不是跟着词走。

⚠️⚠️ **而更根本的一条：那道闸只装在竖版短片那条线上**
（`build_match_reel._absolute_claims_need_a_source`），**解说片这条线一道都没有**
——它的脚本活在 `_SCRIPTS` 里、没有 spec 的 `_claims` 可认领，所以「两个独立源」
这套机制在那边**结构性地不存在**。这是个**已知缺口**，不是这次顺手能补上的；
今天只能靠写的时候自己问一句。

**写之前的那一问，这次本来就该问出来**（本文件「查得够深、查得够广」那节的原话）：
**这句话要是错的，一个反例长什么样？我查的那张表看得见它吗？**
——「他进过几次大满贯决赛」的反例就是「第四次决赛」，而它**就印在他自己的
生涯统计页上**，是这个断言最该查的那一张表。

##### ⭐ 改法：把范围亮出来，别把反例藏掉

第 ④ 屏现在是**四行**：三行绿的（夺冠 · 世界第 1），第四行压暗
（2017 法网 · 负 · **世界第 4**）。

⚠️ **「对面都是世界第一」只对他赢下的那三次成立**——第四次决赛纳达尔是 4 号种子，
那年的世界第一是穆雷（同一页的种子表：1 穆雷 2 德约 3 瓦林卡 4 纳达尔）。
把第四行藏掉，「每一次对面都是世界第一」就又变成一句一个反例就倒的话；
亮出来之后那三次反而更重——**这跟「精准优先于引爆」不冲突，它就是引爆本身**。

两个独立源记在 `_WAWRINKA_FINALS_DIAGRAM` 上面的注释里（生涯统计页 + 2017 法网
男单条目），别再重查。

⚠️ **已发的片子不重渲**（微信和抖音都发出去了），这一条只管以后；`.xhs.txt`
那份正文是唯一还能编辑的出口，已经跟着改对。

⚠️ 顺带又踩了一次「viewBox 的高度也有上限」：注脚先写在 y=600
（实际像素 `920×600/900 ＝ 613px`），渲出来被「④ 三座奖杯」那颗序号药丸盖住半句。
安全线是 **585px，也就是 y ≤ 572**（`_ZHENG_EALA_DIAGRAM` 就钉在 572）。
**加一行就要把整块往上压，不能往下加高度。**


###### ⚠️ 我本来想把词表放宽，是数据把它否掉的

想给 `_ABSOLUTE_CLAIM_RE` 加上计数式（`一共只` / `N 次全部` / `N 次都输`）。
拿存量扫一遍：**9 处命中，7 处是误伤**。

| 该拦的（对**开集**的断言） | 误伤的（**这场球自己**就穷举得了） |
|---|---|
| `nakashima-medvedev`「三次交手，中岛此前三次都输」 | 「决胜盘一共只有四十六个小分」 |
| `zverev-norrie`「上了巡回赛之后，八次全输」 | 「这场球的破发点一共只有四个」 |
| | 「上一盘她一共只赢了一局」「保发四次全部失败」 |
| | 「五场球，她一共只丢了三盘」 |

**分界是语义，不是词**：数的是**生涯／交手记录**（开集，要一张穷举的表才敢说），
还是**这一场／这一盘之内**（闭集，逐分数据本身就是穷举）。正则分不出来。

放宽的下场是可预见的，本文件在「跨切点」那条上写过一模一样的话：
**闸一吵，下一个人就会写 `_claims` 去压噪音，而那正好把它唯一想拦的那一类
一起关掉**。所以词表不动——**这一条留给写的时候自己问那一句**。

⚠️ 记下来是因为**「我想过要不要放宽」和「我量过所以没放宽」在代码里长得一模一样**，
而下一个人多半会重新想一遍这件事。数在上面，别重扫。


###### ⭐⭐ 账号所有者定的口径：**可以说「三次夺冠」，不能说「三次进决赛」**

2026-08-27 原话：「这种**信心类的分享一定不能出错**」「你可以说是三次夺冠，
**不能说三次进决赛**」。

⚠️ **这是一条能直接用的通则，不是这一条片子的特例**：

| | 是什么集合 | 风险 |
|---|---|---|
| **夺冠 N 次** | **闭集**——拿了几个冠军是确定的，多打一场也不会变少 | 低 |
| **进过 N 次决赛** | **开集**——多输一次决赛这句话就错 | 高 |

同一族的还有：**「N 冠」安全，「N 次半决赛」「N 次交手」「N 连胜」都不安全**——
后者都要一张穷举的表才敢说。**能用夺冠数说清楚的事，别改用决赛数说。**

###### ⚠️ 我把他这句话读成了「还有第五次决赛」，于是去挖了一个他没在说的事实

他说「还有一次法网输给纳达尔」，指的就是**2017 法网决赛**（第四次决赛）。
我读成了「除此之外还有一次」，去查，**真查到了另一场**——2013 法网**八强**，
纳达尔也赢过他，而且**比分和 2017 决赛一模一样，都是 6-2 6-3 6-1**
（2013 男单签表原始数字 `6,6,6 / 2,3,1`；瓦林卡词条「首进八强，直落三盘负于
纳达尔」）。事实是真的，**但那不是他在说的事**，而我还顺手把它写进了旁白。

⚠️ **多查一层没有坏处，多写一句有**：查出来记进注释是对的（省下一次重查），
**写进产物就是我自作主张扩大了范围**。已经撤掉。

⚠️ 顺带一个真的要改的：我原来写「**唯一输掉的那一次**是二〇一七年法网」——
在「四次决赛」的上下文里没错，可**单独听像是「法网唯一一次输给纳达尔」**，
而那是错的（2013 八强也输了）。改成「**四次决赛里**唯一输掉的那一次」。
**这一类不会被任何判据拦下来，只会被读者读成错的**：写完一句带
「唯一／只／从来」的话，把限定范围**显式写进那句话本身**，别指望上下文替它撑着。

###### ⭐ 顺着「一定不能出错」把整条片子的全称断言扫了一遍，又抓到两个

只扫「唯一／只有／第一个／以来／全部／都是」这一族，逐条去查，产出：

| 那句话 | 结果 |
|---|---|
| 「一九七八年以来第一个 40 岁以上打进大满贯第三轮」 | ✅ 成立，**但代码里一处出处都没写**——已补（NBC Sports ＋ 澳网官网「snaps 48-year drought」，2026−1978＝48 自证） |
| 温网首轮全场比分 | ❌ **写错了**：`6-7(7) 7-6(16) 7-6(9) 7-6(5)`，正确是 `6-7(9) … 7-6(7)`——**第一盘和第三盘的抢七数字对调了** |
| 三次夺冠的三个对手都是当时的世界第一 | ✅ 逐日核过（纳达尔 2013-10-07~2014-07-06；德约 2014-07-07~2016-11-06 覆盖 2015 法网和 2016 美网） |
| 2017 决赛纳达尔是世界第 4 | ✅ 穆雷 2016-11-07~2017-08-20 在位；签表 1 穆雷 2 德约 3 瓦林卡 4 纳达尔 |

⚠️ **那个对调最阴**：两处都是「7 和 9」，**单看每一处都像对的**，只有把两个源
并排比才看得出来。这跟本文件「一个数写两处必分叉」是一家的，只不过分叉的不是
两个字段，是**同一个字段里两个位置的数字**。

⚠️ **而一个源不够**：Tennis Majors 正文给的是对的，可搜索摘要给的第一盘是 `6-7(7)`
（和我们错的那份一样）。定案靠的是**维基签表的原始上标**（瓦林卡那行 `7⁹ 6¹⁶ 6⁷ 6⁵`，
上标是这一盘抢七里输的那个人拿到的分，第二盘 16 分对上 18-16 ✓ 自证了读法）。

⚠️ **两个失败的源也记一下，别再走**：flashscore 的当日 feed **回溯不到两个月前**
（`offset=-58` 返回 1 字节）；tennis.com 的比赛页摘要**读出来是错的**
（说「瓦林卡三盘取胜」，和所有源矛盾）——**摘要器读记分板控件会读错，
拿它当源之前先看它自不自洽**。

#### ⚠️ 而账号所有者接着追了一句「还有一次法网输给纳达尔」——他又是对的

2026-08-27，就在上一条改完之后。核下来确有其事，**但它是八强不是决赛**：

| | 轮次 | 结果 |
|---|---|---|
| **2013 法网** | **八强** | 纳达尔 **6-2 6-3 6-1** |
| 2017 法网 | **决赛** | 纳达尔 **6-2 6-3 6-1** |

⚠️ **两场比分一模一样**——正因为一样，我一开始怀疑是源把两场混了，所以逐个核了
三处才敢用（2013 男单签表的原始数字 `6,6,6 / 2,3,1`、瓦林卡词条「首进八强，
直落三盘负于纳达尔」、以及同词条明写的 `four Grand Slam singles finals`）。
**「两个数长得一样」是该多查一遍的信号，不是可以省一遍的理由。**

**那张「四次决赛」的表不收它**——收进来就把这一屏从「决赛」改成了「交手记录」，
是另一件事。旁白里带一句就够。

⚠️ 真正要改的是**我自己那句话的歧义**：原来写的是「**唯一输掉的那一次**是二〇一七年
法网」，在「四次决赛」的上下文里没错，可单独听像是「法网唯一一次输给纳达尔」。
改成「**四次决赛里**唯一输掉的那一次」。

**这一条比上一条更该记**：上一条是**事实错了**，这一条是**事实对、话有歧义**——
而后者不会被任何判据拦下来，只会被读者读成错的。写完一句带「唯一／只／从来」的话，
把限定范围**显式写进那句话本身**，别指望上下文替它撑着。

#### ⭐ 自己定的那把尺子，会把最该出现的那个人藏起来

2026-08-15 第二次被读者当众指出来，这次不是数字错，是**尺子选错了**。
`golden-masters` 那条讲「女子为什么没有金大师」，片子里写着
「女子这边找不到对应的**说法**」，然后自己定了一把尺子：**按今天这十个 WTA1000
场地数**，斯瓦泰克八站、小威七站。

读者回：「**辛吉斯是集齐过一级赛的**」。核实**成立**——1998~2000 年 Tier I
固定九站（东京、印第安维尔斯、迈阿密、希尔顿海德、罗马、柏林、加拿大、苏黎世、
莫斯科），她**全部拿过**，2000 年 10 月连着拿下苏黎世和克里姆林杯收官，
20 岁，**比德约早 18 年**。

**那句话字面上没错**（确实没有名字、没有吉尼斯条目），可**读者读到的是
「女子没人做到过」，而那是错的**。真正致命的是下一层：

> **我们那把「按今天这十个场地数」的尺子，恰好把唯一做到过的那个人筛掉了。**
> 今天这十站辛吉斯只拿过四个；她真正集齐的东京、查尔斯顿、柏林、苏黎世、
> 莫斯科，今天一个都不是 WTA1000。

所以自己定尺子的时候要多问一句：**这把尺子会不会把「历史上最接近的那个人」
量成一个不起眼的数？** 尺子挑得越贴近「今天」，越会系统性地抹掉旧时代的人——
而旧时代恰恰是「历史上有没有人做到过」这类问题的答案所在地。

⚠️ 同一条评论的第二点把片子的**因果**也打掉了：「德约之前也没有男子集齐过
当时的大师赛」。核实成立，而且比字面更狠——ATP／吉尼斯的定义是
「**1990 年设立以来的这九站**」也就是**今天的九站**，历年冠军都算数：
德约那九站横跨 2007 迈阿密到 2018 辛辛那提，中间表改过一次（2009 汉堡出、
上海进）照样算集齐；而**德约自己没集齐过他那个年代的**（汉堡、马德里室内都
没拿过）。所以片子第 ④ 屏那句「**要拿满，先得有一张固定的表**」是错的——
表不需要固定，只需要有一张今天的表可以回溯。**尺子是对的，因果是错的。**

**两条通用判据：**

| 写之前问 | 为什么 |
|---|---|
| 这把尺子量出来，**历史上最强的那个候选**排第几？ | 排得很靠后就说明尺子在抹人，不是那个人不够格 |
| 我说的是「**没有这个说法**」还是「**没有人做到过**」？ | 前者是术语问题（好查），后者是全称断言（要穷举）。**读者不会替你区分**——写第一种，他读到的是第二种 |

### 译名不交给模型判断

「人名不要手打，先查仓库里的译名表」那条规矩，在机器翻译这儿换了个人踩：
模型会把 Rybakina 译成「里巴金娜」（表里是**莱巴金娜**）、Ostapenko 译成
「奥斯塔片科」（表里是**奥斯塔彭科**），**而且每次译法还可能不一样**。

所以先扫出这批标题里认识的人名赛事名，把「英文 → 中文」当**硬约束**塞进
prompt（`research/glossary.py`）。⚠️ **按姓氏匹配**，因为标题里几乎只有姓氏
（`Draper withdraws`，不是 `Jack Draper withdraws`）；姓氏会撞就两条都给，
让模型按上下文挑——总比我们替它猜一个强。

### 官网的常青导航页不是新闻

2026-08-02 那天 8 条候选里有 3 条是 `What is the Washington schedule?`
这种 ATP 官网给搜索引擎做的导航页——天天在，且**永远不是新闻**。

⚠️ **判据宁可窄，不可宽**（本仓库同一天犯过四次）。只认一个形状：
**疑问词开头 + 问号收尾 + 句中带导航名词**，三条缺一条都不拦——
「How Sinner beat Alcaraz」没有问号，是真报道，必须留住。

⚠️ **而这条判据的第一版是假的**：那张「不许误伤」的表里六条全是随便找的
真新闻，把「必须问号结尾」整个删掉**测试照样绿**——那六条要么不以疑问词
开头、要么不含导航名词，**一条都没走到那个分支上**。现在表里每一条各扛
一个放宽方向，三个方向都反向验证过。

---

## 推分支吃 HTTP 413：先 fetch 再 rebase

`git push` 报 `RPC failed; HTTP 413`，重试、调 `postBuffer` 都没用。原因不是网络：
**本地缺 `origin/main` 的最新提交时，git 找不到可用的协商基线，就把两百多个提交
（含 mp4/jpg）整包重发**，几个 G，代理直接拒。

- 判据是**对象数**：`git rev-list --objects origin/main..HEAD | wc -l`。自己那一个
  提交只有 33 个对象，却推不上去，说明打包的范围不对，不是内容太大
- 修法：`git fetch origin main` 再 `git rebase origin/main`，pack 就只剩自己那些对象
- **纯 ref 移动也会 413**，别以为"只挪个指针"就没事——同样要先 fetch
- rebase 过之后要用 `--force-with-lease` 推

## 并行的生成任务：只碰自己那一块

同一个 workflow 并行跑多个选题时，两处会互相拆台，都踩过：

- **`concurrency: group` 写死一个名字**：GitHub 每组只留一个 pending，新的一来就取消旧的。一口气发七个，只活下来第一个和最后一个。分组名要带 slug
- **提交时重放整棵 `output/`**：为了避开二进制 rebase 冲突，重试时用 `git checkout <本 run 的 HEAD> -- output/` 把成片放回去 —— 这会把**其他选题**一起退回到本 run 开跑时的样子。八个一起跑就变成来回覆盖，发布出去的卡片在新旧 logo 之间闪了半小时

**重放只加不减，会把已经删掉的东西救活。** `git checkout <ref> -- <dir>` 只恢复
ref 里**存在**的文件，不会删掉 ref 里没有的；而重放前那句 `reset --hard origin/main`
刚把旧成片放了回来。走了 Release（成片不进 git）的那一版，第一次提交明明带着
`delete mode … eala-osaka.mp4`，push 撞车重放一次就没了——仓库里留着 2 分 39 秒的
旧片，真片子在 Release 上，**两个地址两条片子，谁也没说哪个是哪个**
（run 30727483963）。重放前先 `rm -rf "$OUTDIR"`，`git add` 用 `-A`。

所以：提交、add、重放、diff，每一步都限定在本次的 `outdir` 上。顺带一提，`git pull --rebase` 处理不了 mp4/png，撞上就是渲染全做完之后 exit 1，而且**失败的 run 会把上一版成片原地留着，仓库看起来是满的**。

## 图片：时间、地点、人物

每张照片必须和它那一屏所讲的内容对得上，出处以来源自己的描述/分类为准，**不靠看图推断**。

- 出过的错：讲"只有法网还保留人工司线"，配的却是温网草地 —— 赛事整个错了
- 拿不到实拍的时刻（如 2004 美网小威那场，只在 Getty 付费库里），**不用近似照片顶替**，事实交给旁白和画面文字讲
- 优先用最新的照片；旧照片只在它本身就是"当年"的论据时保留（如 2006 温网司线员，与 2025 温网空场构成今昔对照），并把年份写在画面上
- 画面上不烧录来源署名，出处记在 `assets/**/credits.json`
- 授权不做检索闸门：官方媒体 / Commons / Openverse / Flickr / Bing / DDG 的候选一律按精准匹配参与评选；许可名、作者、来源 URL 全程记录（缺失记 `unknown` / `unverified`），发布前的权利判断由人工检验环节负责。注意 Bing、DuckDuckGo 返回过 Roland 电子琴、库存图和 1920 年代杂志扫描——挡住它们靠的是人物/年份/赛事精准校验，不是授权过滤
- **赛事官方图库按命名规律直接探，比在页面上抓省事得多。** 迪拜站的文章页里只有赞助商
  logo（图是懒加载的），但它的媒体库按 `<球员>-<轮次>-<年份>-<赛事>_00N.jpg` 命名：
  照着拼 `Shang-Juncheng-R1-2026-Dubai-Duty-Free-Tennis-Championships_001..008.jpg`
  一次探中八张 1920×1080 的原图。**这种命名本身就是四要素自证**——球员、轮次、年份、
  赛事都由赛事方自己写在文件名里，不靠看图推断。ATP 官方图库（atptour.com/-/media）
  对本环境一律 403，赛事自己的站点反而是通的；WebFetch 吃 403 时，带浏览器 UA 的
  urllib 往往能过

### 选图的四道闸门，顺序不能反

账号所有者反复强调过三次，每次都是因为我又违反了它。按顺序过，前一道不过就没有下一道：

1. **精准**——时间、地点、人物**三样都要对得上**，以来源自己的描述/分类为准，不靠看图推断
2. **在比赛中**——**最好是比赛进行中的画面**。训练照、赛前热身、场外签名、发布会、
   看台、走廊，一律不算。
   **唯一的例外：这一屏讲的是夺冠时，捧杯照优先于击球中。** 账号所有者的原话：
   「捧杯优先级比击球中的照片优先级高，**如果有捧杯的话**」——注意后半句，
   它是个条件而不是配额：没有捧杯照就仍然按击球中选，别拿颁奖典礼的合影、
   走廊、发布会去凑
3. **有视觉冲击力**——见下一节
4. **清晰度足够高**——参照 `ball-pick` 那四张：2400px 级

**第 2 道那个例外具体怎么用**（外卡那条的末屏踩出来的）：

| 这一屏在讲什么 | 选哪张 |
|---|---|
| 他/她**拿了这个冠军** | **捧杯**（举杯、捧杯、亲吻奖杯都算，要看得出杯在手上） |
| 打进四强／某一场比赛／某个纪录 | 击球中 |
| 没夺过冠（费里止步四强、郑钦文雅典八强） | 击球中——**没有捧杯照，例外就不成立** |

**但「捧杯」不等于「奖杯占满一屏」。** 这条我一天之内写反过一次：先写成
「杯必须在手上」，于是选了亲吻奖杯那张——账号所有者回了一句
「**这个太近景了**」。奖杯在手、脸占满整屏，第 3 道闸门那条
「大头特写不等于有冲击力」照样成立。

所以判据是**奖杯是不是这一屏的视觉落点**，不是它在不在手上：

| | |
|---|---|
| ✅ 用了 | 克里斯特尔斯蹲在奖杯旁、女儿指天，背景横幅 `US OPEN 2009`——奖杯在前景占了一大块，年份还写在画面上 |
| ❌ 太近 | 同一批的亲吻奖杯：杯在手上，但整屏只有一张脸和半个杯 |
| ❌ 不够 | usopen.org 官方那张举杯的，只有 1280×720 |

顺带一条检索结论，省得下次重跑：**2009 美网克里斯特尔斯的捧杯照就这么几张。**
Commons 全站零（`Category:2009 US Open (tennis)` 489 个文件里她只有 4 张，
全是正赛前一天的训练照）；Sony Ericsson WTA Tour 官方 Flickr 按账号扫，
带奖杯的只有 `3931103249`（与女儿）和 `3931103403`（亲吻）两张；
usopen.org 图床天花板 1280×720。新闻站转载的那张**被网站调成了紫色**，
不能用——**转载图要看它有没有被做过色彩处理**，又一个「非空 ≠ 可用」。

**判据是打开看，不是看元数据。** 2026-07-28 那天我给外卡那条记了三个「齐」，
全是拿分类 + 日期 + 授权 + 尺寸判出来的，一张都没打开。真打开之后：

| 元数据说 | 画面其实是 | 卡在第几道 |
|---|---|---|
| 克里斯特尔斯，2009 美网，CC BY 2.0，3684×2091 | 正赛开打**前一天的训练**，空看台 | 第 2 道 |
| 李娜，2009 美网，CC BY 2.0，3872×2592 | 在场外**给球迷签名**，人不在球场上 | 第 2 道 |
| US Open Singles draw board，3264×2448 | 远景，**选题要讲的「WC」根本看不见** | 第 1 道 |

所以配图结论必须标出是哪一档，**排优先级时中间那档一律按没有算**：

- **看过**——下载并 `Read` 打开过
- **只查了元数据**——分类／日期／授权／尺寸对得上，**没打开**
- **没有**——探过，真空

**来源顺序也是这条推出来的**：Commons 的天花板就是上面那三张；已发成品里能过四道闸门的
主力是**赛事官方图库**（`ball-pick` 四张里三张来自 `wimbledon.com`）。先去官方图库，
Commons 是兜底。四要素写进署名，像这样：
`AELTC/Joel Marklund · wimbledon.com 官方图 · 2026 温网男双决赛，球童手中的比赛用球`。

**示意图的触发条件是「照片讲不清」，不是「照片找不到」。** 账号所有者的两句话
要合起来读：「**能不画的就不要自己画图**」「**有些事情如果图片解释不清楚，
才需要画示意图**」。

这两个判据差别很大，我一开始写错了：

| 触发条件 | 对不对 |
|---|---|
| 这件事**照片本身讲不清**（换球节奏 7 局／9 局、判罚流程、占比对比） | ✅ 该画 |
| **找不到**合适的照片 | ❌ 不该画——那是检索没做够，见下 |

已发的四条里三条是 4 张照片 + 1 张示意图，那个 1 全是第一类：`ball-pick` 画的是
换球节奏，`hawkeye` 画的是判罚流程——**这些东西没有任何一张照片能表达**，
不是拍不到，是拍不出。不是配额，也不是没找到图的退路。

**而且「该画」之后还有第三个条件：画得够好看才有意义。** 账号所有者的原话：
「画示意图的前提是需要把它画得**好看、精美且能让人易懂**，不然画示意图就没有
意义了。」一张糊弄的示意图比没有更糟——它占掉一整屏，却什么也没讲清。

具体的判据不在这儿重复，都在下面「画出来的那一屏，也要够大够直观」「一屏只留
一个强调色」「条形图上不要写字」三节里，全是量出来的数（`920px` 宽、900 单位
viewBox 里正文不小于 26、标题 34 起、一屏最多一个强调色、文字最多两级）。
**画之前先读那三节**，别现想。

自检的办法和照片一样：**渲出来用眼睛看**，在手机尺寸下看得清才算过。

#### 第 2、4 道闸门的准确读法（2026-08-03 账号所有者补的）

原话，两句话分两次给的：

> 「**走动也可以，击球中是第一选择（如果文案内容是击球中），其次时间地点人物
> 事件都能对上就 ok**」
> 「**但在同等级别里选清晰度高的**」

所以第 2 道闸门不是「必须在击球」，是**画面要对上这一屏在说的那件事**——
和「夺冠时用捧杯照」「讲没打就不该配在场上的照片」是同一条通则的三个实例：

| 这一屏在讲 | 选什么 |
|---|---|
| 某一次击球、某一分 | **击球中**（第一选择） |
| 他夺了这个冠军 | 捧杯 |
| 他**没打**（缺席、放弃、等待） | 谁都不行——**这是示意图的触发条件**，见下 |
| 其他（签表、排名、复出、某一场球本身） | 四要素对得上就行，**走动、换边、握拳都算** |

而第 4 道闸门（清晰度）不是绝对门槛，是**平手时的裁判**：前三道并列的几张里，
挑最锐的那张。判据收在 `tools/rank_frame_sharpness.py`。

### 从官方比赛视频抽帧配图：一条现在真的通了的路

`frame-grab` 工作流 + `tools/grab_frames.py`：给一条**赛事官方或球员官方频道**的
比赛视频，抽一批候选帧提交进仓库（沙箱下不动 artifact），先看 `contact.jpg`
联系表再决定打开哪几张。

**它解决的是这类死角**：某个球员某一站的照片，图库、通讯社、社媒全空，
但那一场的官方集锦一直挂在 YouTube 上。商竣程 2026 年华盛顿和蒙特利尔那两场
就是这样——十三条图片渠道全空（记录在 `docs/pr-allowance-research.md`），
抽帧一次就拿到了。

四个实操要点，每个都是当天踩出来的：

- **`--width` 要等于源片原生宽度。** 第一趟写了 1600，而源片是 1920×1080——
  **那是一次静默降采样**，画面看着挺好，只有跟第二趟并排才知道亏了。
  不确定就先抽一帧 `ffprobe` 一下，或者直接给 1920
- **抽出来的帧仍然要过四道闸门**，尤其第一道。视频的四要素由**频道和标题**自证，
  所以只用官方频道，不用二创和搬运
- **烧死的记分条是白给的自证**。`NISHIKORI 6³ 6 5 15 / SHANG 7 3 3 30`
  一行就把赛事、对手、比分、进行到哪一分全写死了——CLAUDE.md 大威那条
  「画面里能自证的元素比任何『看着像』都值钱」在这儿又应验一次。
  代价是它会和卡片上的序号药丸、标题叠在一起，**那是模板本来就有的，不要为此
  去裁掉记分条**：证据比那点视觉洁癖值钱
- **官方集锦以宽景回合为主**，近景多在两分之间。所以「击球中」的近景往往没有，
  而按上面那条新读法，**换边走动的中景完全够用**

##### ⚠️ 要谁的近景，就抽他**赢的**那一场

2026-08-03 给「卡佐靠特殊豁免打进生涯第一个决赛」配图，我抽的是**那场决赛**——
选题落点就在决赛，看着天经地义。抽完 163 帧，人脸检测挑出 12 张近景，
**十二张全是布勃利克**。

因为**卡佐输了那场**，而官方集锦的近景天然偏向赢家：捧杯、庆祝、赛点后的特写
都是赢家的，输家只在宽景回合里出现，而宽景里两个人都只有几十像素
（「转播主机位的宽景，裁不出更紧的景别」那条）。

- **判据是「这一屏要谁的脸」，不是「这一屏讲哪一场」**。讲他打进决赛，就去抽
  **半决赛**——那场他赢了，近景才归他
- ⚠️ **差一点把对手配上去**。四张近景摆在一起才发现是同一个人，而末尾在笑的
  那个是赢家。和「动作真的有，人是反的」是同一个坑，只是那次错在动作、
  这次错在整张脸
- **认人要拿外部参照，别靠球衣猜**。Commons 上卡佐 2023 美网那张一比就分清了：
  参照里是深发无须穿海军蓝，帧里那位留须、穿 EA7 teal、带白色压缩袖

##### ⚠️ 场边广告板上的年份，可能是**下一届**的预告

同一批帧里有一块板子写着 `GENERALI OPEN 2026 / 18. – 25. Juli`，而这条片子讲的是
**2025** 年。我差点据此判定「年份对不上，过不了第一道闸门」，把一批好素材扔掉。

真相是赛事在当届场边打**明年的档期**——招徕回头客，很常见。视频自己的元数据
（`upload_date` 20250726）一查就清楚。

这是给「画面里能自证的元素比任何『看着像』都值钱」加的一条限定：**自证的强弱
分等级**。烧死的记分条是**当下这一分**的状态，伪造不了；场边广告板是**印上去的**，
它说的可能是过去、现在或未来。

| 强 | 弱 |
|---|---|
| 记分条、球场地面刷的赛事名、球员球衣 | 场边广告板、横幅、赞助商灯箱 |

拿弱的那一档定年份之前，先用视频自己的 `upload_date` 对一次——一条命令的事。

##### 官方频道也会发**竖版 Shorts**，那种抽不出配图

同一天第二个坑：一条 32 秒、标题正对题的 Tennis TV 视频，抽出来是 1920×**3414**
——源片只有 1080 宽，`--width 1920` 把它**放大了 1.78 倍**（上面那条静默降采样
反过来）。而且它的球场镜头是**合成的**：横画面压在上半部，底下贴一大块记分条
图形，真正的球场内容只剩窄窄一条。

**判据是时长**：官方单场集锦是 2–12 分钟，**30 秒上下的十有八九是 Shorts**。
搜到的时候顺手看一眼 `%(duration)s`，别等抽完 32 帧才发现。

#### `rank_frame_sharpness.py`：它量的是高频能量，只能在同一个镜头里比

「同等级别里选清晰度高的」落成脚本时，第一版差点用错口径。实测：

| | 分 | 为什么 |
|---|---|---|
| 蒙特利尔那张 | **320.9** | 背后一整片看台观众，边缘极多 |
| 华盛顿那张 | **73.3** | 背后一大块平的深蓝广告布 |

**两张一样清楚**，差的是背景有多少纹理。跨镜头拿这个数排序，会把「背景热闹」
读成「更清楚」。

所以正确用法是**在同一个镜头的相邻几帧里挑**（`--dir` 配 `--range`）：
同样的构图、同样的背景，差别才真的只剩运动模糊。真实数据很干净——
华盛顿 0102–0109 这一镜头里，0102/0104/0106 并列 68.5/68.5/68.3，
而 0109 只有 12.3（他正在快速转身）。跨镜头的取舍仍然靠眼睛。

两个配套口径：

- **一律缩到同一个宽度再算**（`NORM_W=960`）。`pick_cover_frames` 那条注释
  记着同一个坑：直接在原尺寸 ROI 上求 Laplacian 方差，得数和放大倍率成反比
- **记分条和台标要排除掉**。它们是矢量图形，永远最锐，整帧一起量会把两张帧
  之间真正的差别淹掉

#### 「他没打」是拍不出来的——这才是示意图的触发条件

CLAUDE.md 早有一条「示意图的触发条件是照片讲不清，不是照片找不到」。
**「一段缺席」是这条最干净的实例**：空球场证明不了「本可以站上去的人选择了
不站」，而一张他在场上的照片**正好把这一屏说反**。

而且画出来常常能捡到一个照片给不了的东西。保护排名那条片子的「代价」屏，
画成时间条之后才看见：商竣程 2025 年那段六个月的空白，**整届温网
（6.30–7.13）正好占满最后 13 天**——六个月满在 7 月 13 日，而温网决赛也是
7 月 13 日。这个巧合是把日期摆到一条轴上才掉出来的，随后写进了旁白。

同一族的判断题还有：讲一条**禁令**、讲**两个计时器同时在走**、讲**按暂停**——
都是照片没有语法去表达的东西。

**「过不了闸门」是换源的信号，不是放弃的理由。** 账号所有者的原话：
「过不了闸门的照片，**一定要去多扩大检索源去搜索**，而不是说过不了就放弃了。」

我 2026-07-28 就是这么栽的：Commons 上外卡那几张全卡在第 2 道（训练照、场外签名），
我据此写下「八屏只有一屏有图」——**那句话的真实含义只是「Commons 上没有」**，
却被我当成了「没有」。同一个毛病和「空结果 ≠ 不存在」是一家的，只是换到了
「某一个源上没有 ≠ 不存在」。

所以顺序是**穷尽源**，不是穷尽一个源：

1. **赛事官方图库**（首选，`ball-pick` 四张里三张出自 `wimbledon.com`；
   ATP 总站 403 但赛事自己的域名镜像同一批图，见上一节）
2. 各协会／赛事的新闻页（`/en/media/news` 这类，别只试 `/en/photos`）
3. 新闻站、图片社转载、社交媒体
4. Commons / Openverse / Flickr —— 这是**兜底**，不是起点
5. 以上都翻到底了才画示意图

授权照实记（缺失记 `unknown` / `unverified`），发布前由人工判断——**但四道闸门
一步都不能省**，源越野越容易混进对不上的图。

真到了「所有源都拿不到、事情又必须讲清楚」那一步，才画一张，
署名写「示意图 · 网球时差绘制」。
### ⭐ 2026-08-16：封面大图**一律**用官方高清实拍——这条现在有闸了

账号所有者一条消息里说了三遍：「封面一定要选高清大图，**不然没人愿意点进去看**」
「抽帧的图都不太清晰啊」「**都说了**不要用截图抽帧的这种方式去做封面大图，
非常的不清晰啊！**去找官方的。高清图片呀**」。

⚠️ **「都说了」是给我的。** 下面那一节（「封面用真实照片，不要从视频里抽帧」）
一直写着这条，连账都算过。**规矩没变，是实现漂了**——`solo` 版式给了
`portrait.frame_at` 这条近路，于是**六十多条片子全走了它**。
这是「规矩写对了，实现是空的」的又一个实例，只不过这次空的不是代码，是**习惯**。

**现在落成闸**（`build_match_reel.cover_photo_problem`，装在 `validate_spec` 里，
`--dry-run` 0.2 秒就报）：

| 拦什么 | 怎么放行 |
|---|---|
| `portrait.frame_at`（抽帧） | 写 `portrait._frame_why`，**说清四类源各自查了什么** |
| `portrait.image` 撑不满 1080×1440（在放大） | 写 `portrait._low_res_why` 认领这个取舍 |

⚠️ **只管 `cover.portrait`，不管 `cover.versus.top/bottom`**——那两格是**抠图**，
账正好反过来（本场抽帧裁出来的人 0.61× 缩小最锐，官方棚拍图 1.55× 放大最软），
见下面「⚠️ 上面这条只管『整帧铺满』，不管『抠出来的人』」。判据宁可窄，不可宽。

#### ⭐⭐ 大满贯那半边：**美网有自己的图片接口**，页面永远是 JS 壳（2026-08-25 实测）

WTA / ATP 那两条路对大满贯**都不成立**——`photoresources.wtatennis.com` 不收大满贯
（8 月下旬只有辛辛那提），赛事官网的 WordPress 图库是巡回赛站点才有的东西。
美网另有一套，**这一轮才挖出来，别再重找**：

    ① 接口表   https://www.usopen.org/en_US/json/gen/config_web.json
    ② 按球员   …/relatedcontent/rest/v2/uso_v1/en/tag?tags=<playerId>&type=photo&count=200
    ③ 按日期   …/relatedcontent/rest/v2/uso_v1/en/content/byType/photo?count=200&skip=<n>
    ④ playerId https://www.usopen.org/en_US/scores/feeds/2026/players/players.json
               （郑钦文 wta328120、锦织圭 atpn552、安德莱斯库 wta325088、迪米特洛夫 atpd875）
    ⑤ 图本身   https://photo-assets.usopen.org/images/pics/large/<前缀>_<名>.jpg
               前缀 t_(缩略) b_(640) c_(960) **f_(1280，就是顶)**

⚠️ **`usopen.org` 的每一个路径都返回同一份 4084 字节的 JS 壳**——`images.usopen.org`、
`photo-assets.usopen.org` 的目录也一样。只看页面必然得出「这站没有图库」这个错结论，
而它和「真的没有」长得一模一样。**答案在 `config_web.json` 的接口表里。**

⚠️ **1280×720 是这个赛事的出版上限，不是「没找够」。** 试过十二个前缀
（`a_ d_ e_ g_ h_ i_ o_ s_ x_ l_ m_ n_`）、六个目录（`xlarge/ orig/ original/ full/
raw/ huge/`）、三种参数（`?width=4000` / `?w=4000` / `?resize=4000`）——前两组全 404，
第三组返回 200 但**尺寸一个像素没变**（只换了一次 JPEG 编码，字节数和高频能量会变，
**别被变大的字节数骗成「拿到大图了」**）。所以大满贯的封面**默认要写 `_low_res_why`**，
那不是偷懒，是这条渠道的天花板。

⚠️ **②那条按球员 tag 查给的是确定的答案，可以当「真空」的证据用**：郑钦文全部 263 条里
2026 年**只有两条**，都是 8/24 资格赛（一条比赛中握拳、一条场外签名）。
「按人查完只有 N 条」比「我搜不到」硬得多。

##### ⭐⭐ 而**稳定**拿美网高清图的那条不是官方接口，是 **USA TODAY 的每日图集**

账号所有者 2026-08-25：「**那你帮我稳定找到美网的高清图片**」。上面那条官方接口
是**保底**（每场都有、每个人都有），可它封顶 1280×720，铺 1080×1440 只有 0.50×——
每张都要写 `_low_res_why`，不能算「找到高清图」。真正的主路在 Gannett 那条上：

    python3 tools/find_cover_photo.py --player Alcaraz --event "US Open" --date 2025-09-07

| 这一档 | 美网有没有 | 实测 | 铺 1080×1440 |
|---|---|---|---|
| ⭐⭐ **USA TODAY 每日图集** | ✅ 决赛周两辑都有 | **2187 ~ 6283px** | **1.01 ~ 2.91×** |
| 美网官方图片接口 | ✅ 每场都有（保底） | 1280×720 封顶 | 0.50× |
| AP 通讯社 | ⚠️ **正赛开打前是零** | —— | —— |
| WTA `photo-resources` | ❌ 大满贯不进 | —— | —— |

⚠️ **USA TODAY 自己派摄影师去法拉盛**，别指望 `lohud`（那是威郡本地报）。
说明四要素全齐（`Carlos Alcaraz (ESP) holds the trophy after defeating Jannik
Sinner (ITA) in the final of mens singles at Billie Jean King National Tennis
Center.`），署名是 `Robert Deutsch, Imagn Images` 这种，属于四类源第 ③ 档。

⚠️⚠️ **这条渠道原来「整个扫不到」，而它和「美网没有图集」长得一模一样。**
两处各错一半，两处都不吭声：

- **图集正则写死成 `sports/<年>/`**。Gannett 的路径里赛事名那几段**可有可无**：
  辛辛那提是 `sports/2026/08/17/…`，美网是 `sports/**tennis**/2025/09/07/…` 和
  `sports/**tennis/open**/2025/09/06/…`——**美网那一整类一条都扫不到**
- **`_LOCAL_PAPERS` 里没有能被 `--event "US Open"` 命中的条目**，于是这一档
  直接「没跑」

⚠️ **正则放宽之后同一天会翻出别的项目**（2025-09-06 那天两辑，另一辑是棒球，
不筛就拿回 49 张卡尔·里普肯）。按赛事 slug 筛，**但筛掉几辑要报出数来**——
两头各有一个坑：「非空 ≠ 对题」和「扫得太窄和真的没有长得一模一样」。

⚠️⚠️ **`gannett-cdn.com` 会 406，而那是限流不是「这张图没了」。** 实测同一个 URL
一分钟内 406 → 200，同一辑里上一分钟还 200 的另一张变成 406——**失败的是哪几张
一直在换**；12 张连着取只有 4 张成功，1~5 秒的退避重试一次都没救回来（窗口比那长）。
所以判据是「**换一张再试**」不是「重试这一张」：封面只要一张，一辑三十几张候选，
走一遍必然拿得到。**别把 406 读成「这张图不存在」然后退回抽帧。**

⚠️ **「AP 对美网是零」是个真的零。** 它的美网前瞻稿配的全是别站资料图（温网、
蒙特利尔、印第安维尔斯），一张美网都没有——正赛开打之后才会有本场图。
⚠️ 顺带修了一个静默失效：AP 那道赛事闸原来比的是 `event.split()[0]`，而
「US Open」的第一个词是 **`us`**——「United States」「Australian」里都有它，
于是这道闸**对美网整个失效**，拿回来六张全是别站的，看起来却像正常命中。
现在比整个赛事名，两边都抹掉非字母数字（AP 有时写 `U.S. Open`）。

##### ❌ `usta.veritone.com` 走不通：要账号，而我们没有（2026-08-25 探到底）

它是 USTA 架在 Veritone 上的 **Digital Media Hub**（媒体资产分发门户），
不是公开图库。和 usopen.org 同一个形状——所有路径回同一份 5310 字节的 SPA 壳，
接口在 bundle 里：

| 探什么 | 结果 |
|---|---|
| 接口主机 | `https://crxextapi.pd.dmh.veritone.com`（`/assets-api` `/cms-api` `/identities-api` `/orders-api`） |
| **`/assets-api/v1/search`** | **401 Not Authorized**——接口是真的，要令牌 |
| `/identities-api/v1/session/anonymous` / `/guest` | 404 |
| `/identities-api/v1/session` | 405 |
| 登录 | `session/createIncoming` + `userAuthenticationService: "aiware"`，要真账号 |
| bundle 里那个 Contentful space（`9f7tranrpc7k`） | DMH 产品自己的 CMS（导航条、菜单），**不是照片** |

⚠️ **账号所有者 2026-08-25：「没有账号」**——所以这条路当前是死的，别再探。
真哪天有账号了，流程是通的（真浏览器登录拿 Bearer token → `/assets-api/v1/search`），
**但它带着 `/orders-api`**：这类门户的资产通常是按授权申请下发的，
授权那一关要单独看，不能直接当成公开官方图用。

#### ⭐ 官方高清图上哪儿拿：WTA 这条两步就到（2026-08-16 实测）

    ① 有赛后稿   python3 tools/fetch_wta_cover_photo.py --tournament <id> \
                     --year <年> --match <MatchID> --surnames <姓> <姓>
    ② 没赛后稿   www.wtatennis.com/videos/highlights 的 HTML 里挂着**每条集锦
                 自己的头图**：photo-resources/<年>/<月>/<日>/<uuid>/<姓>-R2-<摄影师>.jpg
    ③ 取原图     photoresources.wtatennis.com/<那条路径>**?width=4000**

⚠️ **两种失败都不长得像「这张图不存在」**：不带 `width` 参数是 **400 Bad Request**，
换成 og:image 那个 `/wta/photo/<uuid>/` 前缀（uuid 和 `photo-resources/` 那个**不一样**）
是 **403**。实测拿到伊埃拉 4700×2957、赫瓦林斯卡 4000×2461（上限就是 4000）。

⚠️ **文件名自己就是四要素**：`Eala-R2-Jimmie.jpg` ＝ 球员、轮次、摄影师，
路径里的日期就是比赛日——和 ATP 那条 `<球员>-<轮次>-<年份>-<赛事>_00N.jpg` 同一个形状。

⚠️ **头像也在这条路上，而且 `.png` 比 `.jpg` 大得多**：
`wtafiles.blob.core.windows.net/images/headshots/<id>.**png**` 是 1600×1500，
同一个 id 的 `.jpg` 可能只有 232×217（伊埃拉就是）。⚠️ 但 png 是**大半身**，
而数据图那个圆圈是 252px、`object-position: 50% 18%` 按 492×656 的头肩像调的——
**直接用会在圈里只看见躯干**。按 alpha 找头顶再裁成 492×656（脚本见
`tools/fetch_official_headshot.py` 那条路的注释）。

#### ⭐⭐ ATP 那半边：**赛事自己的 WordPress 图库**，`tools/fetch_atp_cover_photo.py`

账号所有者 2026-08-16：「封面建议用高清大图，抽帧画面会很模糊。**还有什么渠道有的么？
可以看看之前打完的比赛都是从哪能取到**」。

**去数了一遍已发的 84 条 spec，答案很难看：64 条用抽帧，只有 20 条用实拍，
而 4000px 那一档全是女子。** 尺寸按来源摊开是这样的：

| 来源 | 尺寸 | 铺 1080×1440 |
|---|---|---|
| **WTA `photoresources…?width=4000`** | 4000×2461 ~ 4700×2957 | **1.71~1.94×** ✅ |
| **赛事官网 WordPress 图库（新）** | **2000×1333** | **0.93×** ⚠️ |
| ATP 赛事域名镜像 `/-/media/images/news/…` | 1920×1080 | 0.75× ❌ |
| ATP 媒体库 `<球员>-<轮次>-<年>-<赛事>_00N.jpg` | 1920×1080 | 0.75× ❌ |
| 球员头像 `player-headshot/<ID>` | 300×300 | 只够小头像 |

**所以男子封面一次次退回抽帧，不是有人偷懒，是这条路当时不存在**——WTA 有官方大图
接口，ATP 一直没有对应物。现在补上一条：

    python3 tools/fetch_atp_cover_photo.py --site cincinnatiopen.com --match-date 2026-08-15
    python3 tools/fetch_atp_cover_photo.py --site cincinnatiopen.com --match-date 2026-08-15 \
        --pick 112-of-229 --out output/<date>/reel/<slug>/cover_src/atp_photo.jpg

链路：`/news/category/photos/` → 「Day N Best-of Photos」→ 一辑里的原图。
实测 `--match-date 2026-08-15` 扫到 **41 张**当期实拍。

⚠️ **「当期图是懒加载的，要开 Chromium」这个猜测是我编的，量出来是假的。** 我先前只在
`<img>` 标签里找，一辑只捞到一张就下了这个结论——**而路径一直在 HTML 里**，图库是
fancybox，原图挂在 `<a href>` 上。真页面上量：裸文本扫到 **40 张**当期实拍，
只扫 `src="` 只有 **2 张**。**扫得太窄和真的没有长得一模一样**，判据钉在
`test_扫图不许只看img_src否则当期图漏掉九成五`。

##### ⭐⭐ 2026-08-16 纠正：`-scaled` 是 **2560 的封顶版，不是原图**——去掉它

上面这一段原来写着两句互相打架的话（「`-scaled` 比裸名大」／「2026 这一批只有
2000×1333、没有 `-scaled`」），**两句都不对**。真去量（`cincinnatiopen.com`，
最近 300 条媒体）：

    CincinnatiOpen_20260813_JM010573_JW1-scaled.jpg   2560×1707
    CincinnatiOpen_20260813_JM010573_JW1.jpg          **4991×3328**
    081326_DAY-SIX_MIKE-BAKER-25-of-48-scaled.jpg     2560×1707
    081326_DAY-SIX_MIKE-BAKER-25-of-48.jpg            **5541×3694**
    CincyOpen8.14.26BJ_249-scaled.jpg                 2560×1707
    CincyOpen8.14.26BJ_249.jpg                        3095×2064

**判据不是年份也不是印象，是 `source_url` 自己带不带 `-scaled`**：

| `source_url` | 怎么办 |
|---|---|
| 以 `-scaled.jpg` 结尾 | **去掉那个后缀**才是原图（实测能到 5541×3694） |
| 不带 | 它就是原图（上传时 ≤2560），猜一个 `-scaled` 是 404 |

⚠️ 「2026 这一批只有 2000×1333」错在**只看了图库页**：图库页链的是 2000 那一版，
而**媒体库里同一天有大量 2560 的 `-scaled`**（最近 300 条里 74 条），
去掉后缀就是四五千像素的原图。这跟同一节上面那条「只扫 `img src` 会漏掉九成五」
是同一个形状——**扫得太窄和真的没有长得一模一样**。

⚠️ 尺寸决定要不要写 `_low_res_why`（`cover_photo_problem` 的地板是 1.00×）：

    5541×3694 → 0.39×  ✅        2560×1707 → 0.84×  ✅
    2000×1333 → 1.08×  ⚠️        1900×1267 → 1.14×  ⚠️

也就是说**挑对文件就不用写豁免**：`CincinnatiOpen_<日期>_JM…` / `CincyOpen<日期>BJ_…`
那两位摄影师的图有 `-scaled`（→ 原图四五千），而 `MIKE-BAKER` / `ROBERTA-BURNS`
的当日图库那一批多半上传时就是 2000，封顶在那儿。

⚠️ **就算是 2000×1333 也远好过抽帧**：那是相机原图，而抽帧是压缩转播流的
1920×1080 再放大 1.33 倍，两个「1920」不是一回事。

###### ⚠️ 2026-08-18 傍晚再扫一轮——四个新试的渠道，全部量死

账号所有者：「多找找其他图片库啊，不能一棵树上吊死啊」。逐条试过（都是这四场
`zverev-atmane` / `jodar-tabilo` / `fery-deminaur` / `tirante-landaluce` 各自
按人名单独查的，不是随便搜一次赛事名）：

| 试过什么 | 结果 |
|---|---|
| **AP 按 8 个球员名分别搜** | ❌ **除了那场威廉姆斯双打，一张都没有**——`Zverev`/`Atmane` 搜出来 30 张图全是别的赛事（温网、蒙特利尔），`Cincinnati`+`2026` 一起过滤剩 0 |
| **Getty 搜索页 `/search/2/image`** | ❌ 403（搜索接口本身被挡，`/detail/<id>` 那条老路仍然只能用来核实一个已知 id） |
| **Imagn / USA Today sportswire 搜索** | ❌ 返回的是 JS 壳（`<img>` 5 个、真正结果块 0 个），沙箱里的 `requests` 拿不到渲染后的内容 |
| **本地 Cincinnati 电视台**（WCPO/WLWT/FOX19）搜索 | ❌ 搜到的是赛事花絮报道（穿线工），不是球员或比赛照 |
| **ATP 赛事域名镜像的 `/en/news/<slug>`** | ❌ 确认仍然 404——镜像只服务 `/-/media/` 静态资源，不镜像 ATP 的文章页（本文件早记过，这次专门为这四场重试了一遍） |
| **ESPN 记分板页** | ❌ 返回 202 的 JS 壳，`requests` 拿不到渲染后的比分和图 |
| Yahoo 搜索 | ❌ 行为不稳定（同一条查询两次返回内容对不上），不可靠，弃用 |

⚠️ **赛事自己那篇「Cobolli: A warrior making pressure his friend」是判据**：那是
R3 赢家的赛后专稿，**图用的还是媒体库里那 4 张已知的科博利照片**，一张新图都没有
——这证明**媒体库就是这个赛事唯一的实拍管道**，稿子本身不会带来新照片，只是复用
已经上传的那批。也就是说，等这四场各自的赛后专稿发出来，也不会绕开「媒体库有没有
上传」这道关卡。

**结论没变**：能用的仍然只有两条（赛事媒体库、The Enquirer 每日图集），
都还没有这四场。这次是**扩大了查找范围之后确认的真空**，不是没找。

###### ⭐⭐ 再扩一轮：主流媒体确实在写这几场，但**它们配的图三中三是错的**

账号所有者：「多找找其他图片库啊，不能一棵树上吊死啊」。这次换了个方向——
不查图库，查**编辑报道**（Bing 新闻搜索能搜到 The Guardian／Sky Sports／BBC
经 Yahoo 转载／tennisuptodate 这几家，都写了 `fery-deminaur` 这场的赛后稿）。
**三条候选，打开一看全部作废**：

| 来源 | 说明写的 | 打开看是什么 | 为什么不能用 |
|---|---|---|---|
| The Guardian | 「Arthur Fery hits a forehand shot in his defeat by Alex de Minaur」，credit 到 Zuma Press Wire | 真是费里，**但穿的是黑色 Polo ＋ 酒红短裤** | 我们自己这场的源片从头到尾（含雨断前后）他穿的是**淡紫白色球衣**，一次都没换过。同一个人、同一站，但不是这一场 |
| Sky Sports | og:image，配文写着这场的比分 | 也是费里，**球场是浅蓝配深蓝发球区**，色相量出来和我们源片差 5~9°、看着接近 | 但**球衣同样是黑色**，同一个矛盾 |
| BBC（经 Yahoo 转载） | ld+json 的 hero image，1200×811 | 打开是**一个金发女选手**（像穆古拉扎），根本不是费里或德米纳尔 | Yahoo 转载链的图和文章完全对不上，最坏的一种错 |

⚠️ **第一条差点被一次「figcaption 配对没打开图」骗过去**：Guardian 页面上
`media.guim.co.uk` 那个 URL 打开是一张**运动图标插画**（板球拍、网球、橄榄球、
足球），是页面别处一个不相关组件的图，跟 figcaption 完全不是同一处 DOM。
挖到 `lightboxImages` 那段内嵌 JSON 才找到真正的 `i.guim.co.uk` 地址——
**而那张真图本身仍然是错的人的衣服**。也就是说这条候选连着踩了两层坑：
先是抓错了图片元素，纠正之后发现内容也不对。

**判据是「打开我们自己这场的源片，看他全场穿什么」，不是「说明写的是不是这场」。**
三条编辑稿的说明文字全部言之凿凿写着这一场的比分和情节，**没有一条经得起
和源片球衣一比**。这跟本文件反复记的"官方图库把它挂在这一站名下不等于拍的是
这个场地"是同一个形状的第三个变体：**赛事对、人对，但不是这一场**——
编辑稿比图库更容易踩这个坑，因为撰稿人自己也是从图库/通讯社现成库存里挑图，
未必核对过跟哪一场比赛对得上。

**结论**：编辑报道这条路径本身是通的（Bing 新闻搜索能找到关于这四场的真实
报道），**但它们配的图不能直接采信，每一张都要拿我们自己的源片核一遍球衣**。
今天这四场核完的三张全部不合格，等于没有新增可用渠道。

⚠️ **日期按文件名的日期戳认，别按辑名的「Day N」认**——`day-5-best-of-photos`
那一辑装的是 **`081526`**（资格赛也算进内部 Day 编号）。

⚠️ **它认不出照片里是谁。** 文件名只有日期、摄影师、序号
（`081526_DAY-EIGHT_MIKE-BAKER-112-of-229.jpg`），没有球员姓氏——WTA 那条有
（`Eala-R2-Jimmie.jpg`），这条没有。**四道闸门的第一道它只兜得住时间和地点，
人物必须打开看。**

⚠️ **当天那一辑一般当晚才上线**（`--match-date <今天>` 退出码 2）。**那是「还没发」
不是「没有」**——写 `_frame_why` 的时候要这么写，别写成「这条渠道没有」。

###### ⭐⭐ 2026-08-17：**夜场的图要再等一整天**，而这和「只做今天这个比赛日」直接顶上

账号所有者看完 `svitolina-valentova` 的封面：「**封面高清大图说了多少遍了**」。
去补查，三条渠道当时确实全空——**而这一次空得有结构，不是运气**：

| 渠道 | 结果 |
|---|---|
| WTA `photo-resources` | 这一场还没有 Match Reaction 稿，头图取不到 |
| 赛事 WP 媒体库 `cincinnatiopen.com` | **8/16 有 18 张实拍原图，全是日场**；她那场是夜场（当地 21:50 开打、00:13 打完），8/17 那一辑当时**还没上线** |
| The Enquirer | 8/16、8/17 两天 sitemap 里一条网球报道都没有 |

⚠️ **「当晚才上线」对夜场是不够的**：夜场打完已经过午夜，那一辑属于**次日**，
而次日那一辑要到**再一个晚上**才发。也就是说**夜场比赛的官方图，从打完到能取，
大约要 24 小时**。

⚠️⚠️ **而「上一个比赛日的不要做了」那条规矩把这 24 小时堵死了**——两条规矩
合起来的意思是「夜场的片子永远只能抽帧」。**这不是可以靠更努力去查解决的，
是排期本身的冲突**，摆在这儿让下一个人（和账号所有者）看见：

- 日场的比赛：图当晚就有，**封面照旧必须是官方实拍**，没有借口
- 夜场的比赛：要么**当天先渲、押到次日图出来再定封面**，要么这场就不做

###### ⭐⭐ 账号所有者定了：**高清大图是前置条件**，`_frame_why` 不再放行

原话（2026-08-17）：「我觉得还是要**找到对应比赛的高清大图**是最重要的
**前置条件**，不然封面不清晰没有吸引力」。

这句话把上面那个「要么…要么…」的第二支砍掉了：**没有实拍就不开工**，
不是「先做完再补一句话说明为什么抽帧」。

⚠️ **「认领」这个形状在这条上不管用，而它在别处是管用的**（`mixed_fps` /
`silent_source` / `cover._layout_why` 都靠它）。差别在**认领发生在什么时候**：

| | 认领的时机 | 结果 |
|---|---|---|
| `mixed_fps` | 下完源片就知道，**开工那一刻** | 管用 |
| `_frame_why` | 源片下过了、段挑完了、**收工那一刻** | ❌ 没人会为一张封面把两小时的活退回去 |

数出来的账：**100 条 solo 里 74 条走了抽帧**——一个 74% 的人都会走的「例外」，
不是例外，是默认。所以 2026-08-17 起 `cover.portrait.frame_at` **一律红**，
已发的那批挂进 `LEGACY_SOFT_COVERS`（只许减不许加、表自带自检）。

⚠️ **`_low_res_why` 没跟着变，是故意的**：赛事官网的实拍常常是 2000×1334，
铺 1080×1440 算下来 fill=**0.93**，本来就够不着 1.00 的地板。那是一张真的
官方实拍，只是短了一点——把它也判死等于把唯一能用的那条渠道堵上。
**分界是「抽帧 vs 实拍」，不是「够不够 1.00」。**

⚠️ 报错正文里给的是**能直接跑的那条命令**，两个最容易漏的参数都带上：

    python3 tools/find_cover_photo.py --player <姓> --event <赛事名> \
        --site <赛事域名> --date <这场球的**当地**日期>

判据 `test_抽帧当封面不许再靠一句话放行`，三个方向分别反向验证过
（把放行加回来／把命令从报错里拿掉／把实拍那条地板也做成硬闸）。

⚠️⚠️ **但「拦住」不是目的。** 账号所有者紧接着补的一句：「**不开工不是目的，
目的是要有好的图片给人好的体验，所以想办法解决而不是不做**」。所以撞上这道闸
之后按这个顺序办，**不许直接跳过这场**：

| | 怎么办 |
|---|---|
| ① 分清「还没发」还是「真没有」 | 夜场约 24 小时才发。还没发就**等**——片子先渲，封面最后定，图落地换上去再推 |
| ② **换个查法再来一遍** | 见下面那条：一个赛事有好几套文件名，只认一套会漏三分之二 |
| ③ 再扩渠道 | Getty（走 WTA CDN `?width=4000`，说明页自带四要素）、WTA 单条集锦视频页的头图、当地报纸每日图集、球员和赛事官方社媒 |
| ④ 真翻到底就是没有 | 那是一个**要说出来的结论**，不是一次静悄悄的跳过 |

###### ⚠️⚠️ 一个赛事有**好几套文件名**——只认一套会漏掉三分之二

2026-08-17 量出来的，而且我先拿漏掉的那份结果下过结论。辛辛那提 8/16：

    只认 `CincinnatiOpen_<YYYYMMDD>_`      18 张
    三套都认                               **60 张**

三套是（都实测过，`fetch_atp_cover_photo.stamp_of` 现在全认）：

    CincinnatiOpen_20260816_JM021783_PS2.jpg      → YYYYMMDD
    081626_DAY-SEVEN_MIKE-BAKER-1-of-9.jpg        → MMDDYY
    CincyOpen8.16.26BJ_306.jpg                    → **M.D.YY（点分隔，不补零）**

⚠️ **同一个赛事同时有好几位摄影师，每人一套命名**——这不是历史遗留，是并行的。
而漏掉的样子和「这一天没有实拍」**一模一样**：我据此在 spec 里写过
「赛事媒体库当天全是日场、没有这一场」，**那句话是从一个漏了三分之二的扫描结果
得出来的**。判据 `test_日期戳三套命名都要认出来`，反向验证过（拿掉任一套当场红），
并且钉住「认不出的要返回 None」——不然赞助商图会被当成某一天的实拍。

⚠️ 顺带一个**工具自己的假阴性**，比上面那条更该记：`find_cover_photo.py`
**不给 `--site` 时赛事官网那一档整个不跑**，而它是三条里最常有图的一条。
我据此在 `_frame_why` 里写下「四类源都翻过」——**跳过的那一档和查空的那一档
在输出里长得一模一样**（「扫得太窄和真的没有长得一模一样」的又一个实例）。
现在没跑的那一档会自己喊出来，末尾还多一段「这一趟查了什么」把跑过／没跑的
逐条列出来；**写 `_frame_why` 要照抄那份清单，不许写成「都翻过」**。

##### ⭐ 封面上那层「前面阴影」默认不画了（2026-08-17）

同一条消息的后半句：「**背景图不要再加前面阴影，导致背景图看着模糊啊**」。

指的是 `.scrim` 的第二道 radial——它在**画面正中**压 58% 的暗。真渲出来量过，
同一张海报正中那一段的均亮 **64.9 → 85.9**：去掉之后照片的颜色和看台细节
才回来，而在那之前它读起来就是「这张图不清楚」。

⚠️ **翻的是默认值，不是又加一个开关**——开关 2026-08-03 就有了
（`cover.scrim: "clear"`），而翻面之前数了一遍：**100 条 solo 里 74 条已经
逐条写着它**。也就是说它早就不是默认，只是每个人都得记得写那一行；剩下 26 条
不是选了暗角，是**漏了**，账号所有者看到的那一张正在这 26 条里。
**一个 74% 的人都要手动关掉的默认值，本身就是错的默认值。**

- 要把暗角**加回来**写 `cover.scrim: "dim"`（留给「文字正好压在主体上」那种素材）
- `"clear"` 这个老写法继续认，它现在和不写一个意思——**别去把那 74 行删掉**，
  删了翻面之前的历史就读不出来了
- **上下两头必须留着**：台头压在顶上、比分板压在底下，浅色字压在浅色画面上会没。
  判据 `test_封面正中那道暗角默认不画两头要留` 把这一头也钉住了——两头一起关掉的话
  主断言照样绿，而海报是坏的

###### ⭐⭐ 2026-08-17 又翻了一次：**正中永远不画，文字压着的那两条边留着**

账号所有者：「**以后所有封面都不要用遮罩效果**」。上一条把默认翻成不画，
这一条把正中那道 radial **永久删掉**（`dim_centre` 参数留着只为不打断老调用，
值一律无效）。

⚠️⚠️ **我先按字面把整层拆了，拿数据一比是错的，这里记的是那次错。**
依据是**一张照片**：`svitolina-valentova` 渲出来量三条带子（台头 50~69、
钩子 84~100、比分板 108），据此写下「照片本身就够暗，白字压上去读得出」。
把 **31 张已发封面的原图**按同一套裁法量一遍，结论反过来：

    比分板那一带   中位 132，**最亮 219**（arango-venus）、214（djokovic-tirante）
    台头那一带     中位  72，最亮 147（boisson-krueger）
    任一带 > 150   **8 张 / 31 张 ＝ 26%**

再拿一张**纯白照片**渲到底：**比分板整块几乎消失**——中文名、英文名、场地名、
盘分数字全是浅色压浅色，只剩描边的光晕。也就是说整层拆掉之后，**每四张封面
就有一张的赛果读不出来，而它一个字都不报**。

⚠️ **这是「查了一场就写成了一类」的又一次，而且是同一天里第三次**（另两次：
flashscore 的统计项、赛事图库的文件名）。**一张照片量出来的结论，不配当一类
照片的判据**——而我当天已经把这句话写进这份文档两遍了。

**所以最终的形状是**：正中（32%~66%）恒定 `.08`，照片基本原样透出来；
上下两条留着，只落在文字真正压着的地方。账号所有者说的「遮罩」是压在**照片
主体**上那一层——那一层没了；边上这两条从来不压主体。

⚠️ 删正中那层之前数过一遍：**113 条 spec 里 74 条写着 `scrim: "clear"` 把它
关掉，39 条不写，写 `"dim"` 的一条都没有**——它从来没有一个主动的消费者。
（这和「一个 74% 的人都要手动关掉的默认值，本身就是错的默认值」是同一句话
再往前走一步：连剩下那 26% 也不是选了它。）

⚠️ **这层是从解说片的 cover 屏整段抄来的，而抄的时候前提没跟着抄。** 解说片
那边的注释白纸黑字写着理由：「darken only where words actually are ……a soft
ellipse behind the **centred** question」——**那一屏的标题垂直居中，压在照片
主体上**；而「赛场之上」的钩子压在**下三分之一**（`STORYCOPY_TOP = 750`），
上面整片留给人脸。**同一段 CSS，前提反了，于是正中那道椭圆压的全是脸和看台，
一个字都没护到。** 这跟本文件「抄一条已经跑通的工作流时，注释也要抄」是同一族。

⚠️ **所以要改两处**：`versus_poster._scrim_css` 和 `explainer` 的
`.cover .scrim`。只改一边的样子是「同一个账号出去的封面两种脸」——两条线各有
一份 `.scrim`，**修一条不等于修完了**（解说片那半边是 #423，同一天、同一个形状，
那边量到正中压 61%）。判据两条各管一头：
`test_封面正中不许压暗但文字那两条边要留`（这边）、
`test_封面不许再压一层居中的阴影`（那边）。

⚠️ **靠什么读得出**：中段基本不压暗，全靠钩子自带的三层 `text-shadow`
（台头另有一层）；两头那两条边兜住 26% 的亮素材。真遇到**整幅过曝**的照片，
**那仍然是选图的问题**——四道闸门第 3 道本来就该换掉那张，边上这两条兜不住它。

####### ⚠️ 而写这条注释时踩了个新坑：**CSS 注释会随产物发出去**

我把来路（含日期和原话）写成了解说片模板里的一条 `/* */` CSS 注释——
**那段 CSS 是 f-string，注释原样进了每一张卡的 HTML**。于是
`test_知识卡右上角不写日期` 当场红：它断言 `"2026-" not in doc`，
而我的注释里写着 `2026-08-17`。

**那条测试防的是「卡片角上印个日期，等于给常青内容设保质期」，它拦下的却是
一条注释**——听起来像误伤，其实拦对了：**给下一个人看的话，没有理由跟着
每一张卡发出去。** 源码里的注释不进产物，模板里的注释进。

- 写在模板字符串里的注释**要当成产物的一部分**：短、无日期、无引语
- 完整的来路放**测试的 docstring** 和这份文档里，模板里只留一句指过去的话
- 同一族的还有本文件里那几条「判据扫得太宽，被自己的注释误伤」——
  那几次是注释被检查扫到，这次是**注释被渲进了成品**

###### ⚠️ 而上面那条我改错了地方——他说的是**垫在背景那一层**

同一句话我先读成了 `.scrim`（盖在整幅上的那层暗），改完报上去，账号所有者补了
一句：「**我是说封面的背景图片**」。

两层不是一回事，**而且是两处代码**：

| 哪一层 | 长什么样 | 谁带着它 |
|---|---|---|
| `.scrim` 的中心 radial | 盖在**整幅**上、正中压 58% | 26 条没写 `scrim: "clear"` 的 |
| **`fit: "width"` 的垫底层** | 照片按宽度铺，**上下空出来那两条**垫同图的放大版，原来带 `brightness(.42)` | 用 `fit: "width"` 的那几条 |

他看的是 `wangxiyu-fernandez`（当天发的）——那张上下两条**又黑又糊**，读起来
像渲坏了，不像照片的延续。四版渲出来摆一起比的：

    blur44 + 暗.42（改前）   上下两条近黑 ← 他说的那个
    blur44 + 不压暗           亮回来了，但仍然糊成一片
    **blur16 + 不压暗**       ← 现在这档，底色和照片连成一片、看得出是同一个场地
    不糊也不压暗              **裁判的头在顶上重复了一次**，一眼看出是两张图

⚠️ **别再往 0 推**：不糊那一版的毛病不是「太清楚」，是那份复制品**认得出来**、
和上层那张打架。16 是最后一档「还看得出是延续、又不像第二张照片」的。

⚠️ 两处（solo 一处、VS 分格一处）现在共用 `PAD_FILTER` 一个出处——写两处必分叉，
而分叉的样子是「有的封面上下发黑、有的不发黑」。判据
`test_信箱式垫层不许再压暗而且两处是同一个出处`，四个方向分别反向验证过。

⚠️ **`.bg` 那一层没动**（VS 版式背后那张球场，`blur 12 / dim .72`）：它不是垫层，
是给棚拍抠图一个「这是一场球」的语境，源码注释记着 `blur 22 + dim .5` 会糊到
看不出是球场。用它的 12 条 spec 全是 2026-08-14 及以前的，solo 早就是默认版式。

⚠️⚠️ **方法上的教训**：他第一句话里「背景图」三个字就在，我读成了「封面这张图」。
**同一屏上有好几层的时候，「背景」是个有歧义的词——先把这一屏拆成几层数出来，
再问他指哪一层，比改完再返工便宜。**

##### ⭐ 问「今天那批到了没有」要问**媒体库**，不是图库页——而它 403 是 UA 的问题

图库页只列**已发布的文章**，媒体库列的是**已上传的文件**，赛事方往往先传后发：

    GET https://<站>/wp-json/wp/v2/media?per_page=100&orderby=date&order=desc
        &_fields=date,source_url,media_details.width,media_details.height

⚠️⚠️ **我第一次判它「403，走不通」，那是错的——403 是 UA 的问题。** 拿
`User-Agent: Python-urllib` 去请求吃 403，换成浏览器 UA 当场 200。
**「这条路不通」和「我敲门的姿势不对」长得一模一样**，判空之前先换个 UA 试一次。

它顺带**还多捞出一批**：同一天 8/15，图库页给 41 张，加上媒体库是 **67 张**——
多出来的是挂在别的文章上的实拍（`deep-cincinnati-open-run_CincinnatiOpen_20260815_*.jpg`），
那些永远不会出现在「Day N Best-of Photos」那一辑里。

###### ⭐⭐ 而「拍摄日对得上」还差一半——**还要问它什么时候传上来的**

账号所有者 2026-08-17：「**要看时效性，是当时比赛，同时最好是在比赛结束前
上传上来的，不然我们做视频时候也不一定有啊**」。

这是**两个字段**，回答两个不同的问题，之前只用了第一个：

    拍摄日   ← 文件名里的日期戳      「这是不是这一场」
    上传时刻 ← 媒体库的 `date` 字段  「**我做片子的时候它在不在**」

⚠️ **只看拍摄日会得出一个乐观的结论**：今天回头去扫，昨天夜场的照片当然都在了；
可当时（比赛刚打完、片子正要做）它们可能一张都还没传。**这两种情形在候选列表
里长得一模一样**——又一次「兜底出事的时候不吭声」。

**好消息是这条渠道确实来得及**，实测（辛辛那提 8/16，斯维托丽娜夜场 21:50
开打、00:13 打完，日期戳 == 8/16 的 60 张）：

    上传时刻全落在 21:11:07 ~ 22:53:03    → 结束前 60 张 ／ 结束后 0 张

也就是摄影师**边打边传**，不是全场结束后一次性上传。所以「当天那辑还没发」
**不等于**「今天的照片一张都没有」——**辑是文章，媒体库是文件**。

现在 `fetch_atp_cover_photo.py --match-end` 把候选分三档（结束前／结束后／
**不明**）并把结束前的排在最前面。⚠️ **「不明」是独立的一档**：图库页那条路
没有这个字段，把它并进任何一档都是编。⚠️ **`--match-end` 只认完整 ISO 时刻**，
因为夜场跨天——那一场要写 `2026-08-17T00:13`，写成 `00:13` 差一整天。

##### ⭐⭐ 而这两条分辨率最高的渠道**都跨不过赛事**——2026-08-17 量的

账号所有者：「**多找几个渠道，不然下一个赛事就不一定有了啊**」。去量了一遍，
他说的不是隐忧，是现状：

| 渠道 | 跨赛事吗 | 分辨率 | 判据 |
|---|---|---|---|
| ⭐⭐ **AP 通讯社** | ✅ **通讯社，每站都派人** | **4700~8600px** | 见下 |
| **WTA `photo-resources`** | ✅ **巡回赛级** | 4000px | 每站同一个 CDN，不用换域名 |
| **当地报纸每日图集** | ⚠️ **按城市换域名** | 4813px | 六份报纸里**五份**认同一个 `/picture-gallery/` ＋ `/gcdn/` 形状（desertsun / azcentral / providencejournal / lohud / cincinnati ✅，statesman **410 没了**） |
| **赛事官网 WP 媒体库** | ❌ **基本是特例** | 5541px | 扫了 **11 个赛事官网，只有辛辛那提一个**开着 WP REST |

⚠️ **原来分辨率最高的那两条恰好都跨不过赛事**，所以这一轮真去找了第三条——
**AP 同时占住「最高」和「跨得过」两头**。换一站之前照旧先跑
`python3 tools/find_cover_photo.py --discover <新赛事域名>`，别假设上一站那套还在。

###### ⭐⭐ AP 这条怎么走（2026-08-17 挖的）

    搜索  https://apnews.com/search?q=<球员>%20tennis   ＋ /hub/tennis
    文章  <img src="https://dims.apnews.com/dims4/…?url=<原图>" alt="<说明>">
    原图  把 `?url=` 解出来 → assets.apnews.com/…   实测 **8212×5576 / 6226×4796**

说明自带四要素 ＋ 署名，四道闸门第一道直接过：「Ben Shelton of the United
States hoists the trophy following his win over compatriot Brandon Nakashima
during final tennis action at the National Bank Open in Montreal on Thursday,
Aug. 13, 2026. (Christinne Muschi/The Canadian Press via AP)」

⚠️ **别直接用 `dims.apnews.com` 那个地址**——它是按版面缩好的（980×653），
铺封面连一半都不到，**而且不报错**，只是封面糊。

⚠️ **搜索结果里混着一批固定的边栏稿**：三个完全无关的词各回 69~70 篇，
**三者交集 40 篇**。所以「搜出来 70 篇」既不能读成「搜索没生效」，也不能读成
「命中 70 条」——**筛靠的是说明里有没有这个人**，不是搜索结果本身。

⚠️⚠️ **光按人筛会拿回资料图，而它们和本场图长得一模一样。** 实测
`--player Svitolina` 不筛赛事时头三张是意大利公开赛捧杯 ＋ 多伦多半决赛两张
（还是斯瓦泰克赢的那场）——**每一张都是她，没有一张是这一场**。这跟 Getty
那条「看见 `GettyImages-*` 先查说明」是同一个坑，只不过 AP 把说明直接给了，
所以这一刀机器切得动：加上 `--event Cincinnati` 之后那三张全挡掉（0 张），
而同一份代码 `Shelton`＋`Montreal` 拿回 2 张全对。

⚠️ 版权是 AP 的（四类源里第 ③ 档「新闻站／图片社转载」），署名照实记进 credits。

⚠️ **「当地报纸」这一档原来把 `cincinnati.com` 写死在代码里**——它在别的站上
**根本不会跑**，而跳过的样子和查空一模一样（同一个坑上周刚栽过一次，那次跳过
的是 `--site`）。现在按城市查 `_LOCAL_PAPERS`，查不到就明说没跑。

⚠️ **`--discover` 里 `200` 不等于「通了」**：不是 WordPress 的站会拿首页 HTML
回一个 200（`usopen.org` 就是），只看状态码会读成「有接口」。判据是**回来的
是不是 JSON**。

##### ⭐⭐ 2026-08-18 修正：**赛事媒体库认得出照片里是谁**——`?search=<姓>` ＋ `alt_text`

上面那张「量死的四条」里有一行写着

> **赛事 WP 媒体库的 `alt_text` / `caption`** ——当天批量上传的**全是空的**，
> `?search=<姓>` 只匹配文件名……**这条路认不出照片里是谁，是硬限制**

**这句话过期了**（也可能当初就量早了：标签是上传之后补的）。真去查：

    GET https://cincinnatiopen.com/wp-json/wp/v2/media
        ?search=tirante&per_page=20
        &_fields=date_gmt,title,alt_text,caption,source_url,media_details

    CincyOpen8.15.26BJ_378.jpg            2000×1333
      title / alt_text = "Thiago Agustin Tirante"
      caption          = "Photo by Pam Smith/Wick Photography for Cincinnati Open"
    CincinnatiOpen_20260815_JM021551_LS2.jpg
      title / alt_text = "Thiago Agustin Tirante"
      caption          = "Photo by Luke Schlaifer /Wick Photography for Cincinnati Open"

⚠️ **命中的这几张文件名里一个球员名都没有**——匹配的是 `title`／`alt_text`。
也就是说**当日批量图是打了球员标签的**，而 `caption` 还带着日期和摄影师：

    "During the 2026 Cincinnati Open at Lindner Family Tennis Center on Aug. 17, 2026 in Cin…"

**四要素齐了**（人物、赛事、场馆、日期），不用再一张张打开认人。

⚠️ **它省下的时间是可量的**：8/17 那四张我原来是裁脸、跟四个 ATP 官方头像逐个并排比，
花了十几分钟还认错了（排除了布洛克斯、兰达卢塞、蒂兰特、霍达尔、德米纳尔、费里）；
**一次 `?search=` 就给出答案——四张全是 `Flavio Cobolli`**。
认错的原因也顺带清楚了：`atp-C0E9.png` 那张官方头像上科博利是深色卷发，
而 `cobolli-blockx` 的 `_editing_why` 里早就写着「那张头像旧了，不能拿它当认人判据」。
**我写过那句话，然后拿那张头像去认人。**

⚠️ **仍然要打开看**：标签回答「这是谁」，回答不了「情绪对不对题」「是不是近景特写」。

⚠️ 顺带：这个媒体库里还挂着**官方 Order of Play 和签表 PDF**
（`OP-Uploaded-aug-17-10pm.pdf`、`MDS-Aug-17-953pm.pdf`），是 `protennislive.com` 之外的
第二个官方出处，而且带上传时刻——赛程临时改动时它比 protennislive 那份更好定版本。

##### ⭐⭐ 官方图什么时候上线：**主批在次日 01:00~03:00Z，之后还有第二波**

2026-08-18 把媒体库最近 535 条按 `date_gmt` 摊开量的（**按拍摄日分组、看上传时刻**，
这两个日期差一天，别混）：

| 拍摄日 | 张数 | 主批 | 第二波 |
|---|---|---|---|
| 8/12 | 21 | 次日 13Z | —— |
| 8/13 | 31 | 次日 17~18Z（21 张） | 再两天 20Z |
| 8/14 | 37 | 次日 20Z（13）＋ 再次日 00Z（14） | —— |
| 8/15 | 91 | 次日 **02~03Z（78 张）** | 次日 17Z、20Z |
| 8/16 | 75 | 次日 **01~02Z（58 张）** | 次日 16~17Z（12）、20Z（5） |
| 8/17 | **4** | 8/18 02:57Z ← 窗口对上了，**但只来了 4 张** | 还没来 |

**主批的窗口是次日 01:00~03:00Z（当地 21:00~23:00）**，和「Day N Best-of Photos」那篇
文章的发布时刻一致（`day-4` 00:48Z、`day-5` 02:57Z、`day-6` 02:36Z）。

⚠️ **但张数是个独立的信号**：8/15 是 91 张、8/16 是 75 张，而 8/17 到现在只有 4 张。
**窗口开了不等于批到齐了**——「主批时间到了」和「这一天就这么点图」长得一样，
判据是**张数和往日一个量级没有**。

⚠️ **第二波真实存在**，落在**次日 16Z~20Z**（8/16 那天有 17 张是这么来的）。
所以「主批只来了几张」不该读成「这一天没有」，该读成「等第二波」。

⚠️⚠️ **`_fields` 里要 `date_gmt`，不能要 `date`——两者差 4 小时，而它们长得一模一样。**
WP 的 `date` 是**站点本地时间**（这个站是 EDT，UTC−4），`date_gmt` 才是 UTC。同一批图：

    date      2026-08-17T22:57:48     ← 看起来是「8/17 晚上」
    date_gmt  2026-08-18T02:57:48Z    ← 实际是「8/18 凌晨」，正是上面那个主批窗口

**我 2026-08-18 拿 `date` 重量了一遍，据此差点把上面那张表整个改掉**——「主批在次日
凌晨」会被读成「主批在当天晚上」，而下一个人照着当天晚上去等，等到的是空。
两个字段都返回 ISO 串、都不带时区后缀，**错了不报错**。

⚠️ **另一个同源的信号：「Day N Best-of Photos」那篇文章没发，就说明主批没到齐。**
2026-08-18 07:00Z 查的时候 `posts` 里 `day-4` / `day-5` / `day-6` 都在，
**`day-7` 一篇都没有**——和「8/17 只有 4 张」是同一件事的两个面。查一次 `posts`
比查媒体库便宜，可以当快速探针。

##### ⚠️ 同一轮里量死的四条，别再重探

| 试过什么 | 结果 |
|---|---|
| **ATP Sitecore `/-/media/` 加尺寸参数** | ❌ `?w=4000` / `?mw=4000` / `?width=4000` / `?h=&w=` **全部被静默忽略**，恒 1920×1080、字节数都一样。⚠️ 又是「参数被接受了 ≠ 参数生效了」，判据是**换个值内容变没变** |
| **ATP 文章从赛事域名镜像取** | ❌ `/en/news/<slug>` 一律 404——镜像只服务 `/-/media/` 静态资源，**不镜像 ATP 全站页面**（`atptour.com` 本体照旧 403） |
| **赛事独立图片站 / DAM** | ❌ `photos.` / `media.` / `gallery.` / `assets.` 子域全 403，`<赛事>.photoshelter.com` 404 |
| **X / Instagram** | ❌ `x.com/<账号>` 只给 JS 壳；`api.x.com` 要鉴权（400）；Instagram 429 |
| **WTA 单场比分页 `/scores/<MatchID>`** | ❌ **每一场给的是同一批 7 张**（`LS063` 和 `LS039` 两场清单一字不差，还混着 2023/2024 的旧图）——那是版面边栏，不是这一场的图。**它看着最像「每场一张头图」，其实不是** |
| **WTA 再多扫几个入口**（`/scores`、`/tournament/<id>/…/scores`、`/photos`） | ❌ 比现有三个入口只多 **2 张，还都不对题**（一张 2025 年的、一张别站的）；`/photos` 和 `/news` **返回同一份**（字节数一样） |
| **赛事 WP 媒体库的 `alt_text` / `caption`** | ❌ 当天批量上传的**全是空的**，`?search=<姓>` 只匹配文件名（头像、少数精选图）。**这条路认不出照片里是谁，是硬限制** |

###### ⚠️ 2026-08-18：**当地报纸那条也只覆盖前两片场地的头两场**，别指望它兜夜场

上面那条「当地报纸每日图集当晚就发」是真的，**但它覆盖谁不是随机的**。8/17 那一辑
（`cincinnati.com/picture-gallery/.../round-3-photos/`，44 张）逐条读 caption：

    威廉姆斯姐妹双打 16 张 ｜ 斯瓦泰克-萨卡里 14 张
    莱巴金娜-弗雷赫 8 张 ｜ 科博利-布洛克斯 6 张

对照当天的 Order of Play：这四组正好是**主场地头两场 ＋ 二号场地第一场 ＋ 那场话题双打**。
而**霍达尔-塔比洛（主场地第 3 场）、费里-德米纳尔（二号场地第 2 场）、
兹维列夫-阿特曼（晚上 7 点）、伊埃拉-阿尼西莫娃（晚上 8 点半）一张都没有**。
同一天 AP 也只有那场威廉姆斯双打。

**所以判据不是「今天有没有图集」，是「这一辑覆盖了哪几场」**——图集在、张数还不少，
而你要的那一场可能一张都没有，**这和「今天还没发」长得一模一样**。查之前先按上面
「零命中先怀疑自己的查询词」那条，把**当天全部球员**一起搜一遍，再按图挑片子。

⚠️ 这一条和上面「夜场的图要再等一整天」是同一件事的两个证据：**越靠后的场次，
越只能等赛事自己的媒体库**，而媒体库的主批在次日凌晨。

##### ⚠️ 2026-08-18 又扫了一遍「还有没有别的官方渠道」——量死的九条，别再重探

账号所有者问「还有官方其他地方你没有找到的」。逐条试过，**只有两条是新的**
（上面两节：媒体库的 `?search=` 标签、和上线时刻），其余九条全是死路：

| 试过什么 | 结果 |
|---|---|
| **`atptour.com` 换 `requests` ＋ 浏览器 UA** | ❌ 仍然 **403**（首页、新闻页、`/en/media/photos` 三个都试了）。⚠️ 这一条专门试是因为 Gannett 那次的真因是「urllib 的头名大小写」——**这次不是**，ATP 是真的挡 |
| `photoresources.atptour.com` | ❌ 域名不存在（WTA 有，ATP 没有对应物） |
| `atpfiles.blob.core.windows.net` | ❌ 400 |
| Getty 搜索页 / editorial 按赛事 | ❌ 403（只有 `/detail/<id>` 那条还能用，见上面 Getty 那节） |
| **Imagn（USA Today Sports Images）** | ❌ 200 但是 JS 壳，`/api/search` 404，页面里抠不出任何图链 |
| Flickr：赛事账号 / `tags=cincinnatiopen` 公开源 | ❌ 账号 404；tag 源最后更新停在 **2026-02-25** |
| 赛事的 `media.` / `press.` / `photos.` 子域 | ❌ 全部 403（同一个 8327 字节的错误页，说明是通配拦截不是「有站点但没权限」） |
| `cincinnatiopen.smugmug.com` | ❌ 502 |
| Alamy / ZUMA | ❌ Alamy 200 但抠不出图链（`c*.alamy.com` 一条没有）；ZUMA 搜索 404 |

⚠️ **`cincinnatiopen.com/photos/` 和 `/gallery/` 是 404，`/media/` 是 200 但不是图库**——
图库唯一的入口还是 `news/category/photos/` 那条文章流，加上 `wp-json/wp/v2/media` 那个媒体库。

##### ⭐⭐ 而尺寸最大的那条在**当地报纸**：Yahoo 转载的 `media.zenfs.com`

辛辛那提当地的 **The Enquirer**（USA Today 网络）派自己的摄影师拍这一站，图集
被 Yahoo 转载，原图挂在 `media.zenfs.com/en/cincinnati_com_the_enquirer_slideshows_<N>/`
——**实测 5221×3481 ~ 8640×5760**，全画幅相机原图，比 WTA 那条还大一倍，
而且挂在**当天的 live blog** 上（`sports.yahoo.com/articles/live-atp-wta-2nd-round-…`）。

⚠️ **但它过不了「自证」这一关，所以是第三档不是第一档**：

- 文件名是 32 位哈希（`2079a033657d73a5415a090ee04d222a.jpg`），**不自证是谁、哪天**
- Yahoo 页面里**抠不出 caption / credit**（`Enquirer` 出现 126 次，没有一个结构化字段）
- 版权是报社的，属于四类源里的第 ③ 档「新闻站/图片社转载」——授权照实记，
  发布前人工判断

**所以它的用法是：官方图库真等不到、而这一屏又非要实拍时，去那儿挑一张，
然后自己打开看认人。** 别把它当成第一顺位。

##### ⚠️ 拿到的官方图要落在 `assets/`，落进 `output/` 的话 CI 上根本看不见

2026-08-17 `swiatek-sakkari` 踩的：WTA 那张 4000×2525 的官方实拍下下来之后，
我顺手放在了 probe 产物那一格（`output/<date>/reel/<slug>/cover_src/portrait.jpg`），
spec 也照这个路径写。**本地全量 2146 绿，CI 两条红**，两条同一句话：

    build_match_reel.ReelError: 封面大图找不到：output/2026-08-18/…/portrait.jpg

因为 **`ci.yml` 的稀疏检出从来不含 `output/`**（本文件「测试不许拿 `output/`
当判据的主语」记的就是这条），而 `cover_photo_problem` 是真去
`Path(...).exists()` 的。

⚠️ **它和那条老规矩是镜像的，而这一面更该装闸**：

| | 主语在 CI 上不在的是 | 表现 |
|---|---|---|
| 老那条 | **判据自己**（`Path("output").glob(...)` 恒空） | **假绿**——一条恒真的检查 |
| 这一条 | **被判据检查的那个文件** | **红**，而且只在 CI 上红 |

假绿更阴，可这一面每犯一次就是一整趟「推上去 → 等九分半 → 才知道」的往返。

**判据是「另外 26 条 spec 怎么放的」**：`assets/reel/<球员>-<赛事>-<年>-<轮次>.jpg`。
闸在 `test_spec引的图不许放在output里CI上看不见`，扫全部 spec 里**图片后缀**的
字符串（`_` 开头的注解键跳过），反向验证过。

#### ⚠️ 而它真的没有的时候：判据是「WTA 自己都在用资料图」

`zhang-day`（辛辛那提第一轮，张帅胜戴伊）四类源翻到底一张都没有。
**最硬的那条证据不是「我搜不到」，是 WTA 自己给这场集锦配的头图
（`videos/4560170` 的 og:image）用的是 `Zhang-R3-Julian-Avram-2.jpg`——
路径日期 2026/08/07，那是多伦多。** 编辑手上都没有本场的图，这才叫查空自证。

⚠️ **多伦多那张不能拿来用**：四道闸门第一道是时间地点人物都要对上，
拿别一站的图配这一场，正是本文件记过的「资料图」坑（同一个 Getty 编号挂在两个
日期目录下）。所以那条片子**渲得出来但先不推**（`push.auto` 关掉），等官方出图再换。

#### ⭐⭐ 2026-08-16 又扫出三条渠道，全收进 `tools/find_cover_photo.py`

账号所有者：「**多找几个渠道获取高清大图**」。上面那两条（WTA `photo-resources`、
赛事官网图库页）之外，实测又通了三条——**别再每次现搓，跑那个工具**：

    python3 tools/find_cover_photo.py --player Tjen --event Cincinnati --day 6 \
        --site cincinnatiopen.com --date 2026-08-16
    python3 tools/find_cover_photo.py --getty 2288964672      # 这个编号是哪一场

**① ⭐⭐ `GettyImages-<id>.jpg` 从「不能用」翻成「能用」。** 以前把它判死是因为
「文件名什么都不说」，而其实两头都通：

- **WTA 的 CDN 存的是无水印原图**，`?width=4000` 实测拿到 **4000×2666**
- **Getty 自己的 `/detail/<id>` 页给出完整说明**——球员、赛事、轮次、场馆、日期，
  四要素一次全齐

⚠️ **看见 `GettyImages-*` 一律先查说明，别按「它挂在这场的页面上」就当成本场。**
实测反例：`2288964672` 就挂在辛辛那提那条集锦的页面上，说明写的却是
「National Bank Open, **August 08, 2026**, **Sobeys Stadium in Toronto**」——
又一张资料图。同一批里 `2290118693`（Tauson–Stearns 辛辛那提 Day 5）和
`2290359738`（Parry 辛辛那提 8/13）才是真的本站图。

**② ⭐ 单条视频页比列表页多带图。** `/videos/<id>/<slug>` 的正文里除了封面还挂着
别的图（谭雅妮那条集锦的页面上就多一张萨巴伦卡的 Getty）。扫图要扫**单条页**，
不能只扫 `/videos/highlights` 那张列表。

**③ ⭐ 赛事官网的 WordPress REST 媒体库**——比翻图库页早，能按日期过滤、
直接给原图尺寸：

    /wp-json/wp/v2/media?per_page=100&orderby=date&order=desc
    /wp-json/wp/v2/media?after=2026-08-16T00:00:00
    /?rest_route=/wp/v2/media          ← 等价入口

⚠️⚠️ **必须带浏览器 UA。** 拿 `tennislive/0.1` 这类 UA 请求是 **403**，
看起来像「这个站根本没开 REST 接口」——这一条当场骗过我一次。
⚠️ 赛事图库的文件名**没有球员名**（`081526_DAY-EIGHT_MIKE-BAKER-112-of-229.jpg`），
所以 `?search=<姓>` 在这儿恒空；它的用处是**按日期看当天的图上没上**。

**④ ⭐ 「图库什么时候上线」是可以量的，别在当天下午反复扫。** 拿
`posts?orderby=date` 读 `day-N-best-of-photos` 的 `date_gmt`，实测：

    day-1  2026-08-12T00:00Z      day-4  2026-08-15T00:48Z
    day-3  2026-08-14T01:56Z      day-5  2026-08-16T02:57Z

也就是**当天那一辑在次日 UTC 00:00–03:00 之间上线**（当地 20:00–23:00）。
比赛当天下午扫到 0 张是**结构性的**，不是这一天没有实拍。

**⑤ 实测走不通的，别再试**（省得下一个人重探一遍）：

| 路 | 结果 |
|---|---|
| Getty 自己的 comp 图 `media.gettyimages.com/id/…` | 脱离页面上下文一律 **400**（签名参数绑上下文）；而且 `w=gi` 是**带水印**的 |
| Getty API `api.gettyimages.com` | **401**，要 key |
| Reuters 图片站 | **401** |
| `api.wtatennis.com` 的 `media` / `photos` / `content` / `players/<id>/media` | 全 **404** |
| `wtatennis.com/galleries` | 404。`/photos` 有，但那是专题图集，不是当日比赛图 |
| tennis.com 的比赛页 | 只有国旗和头像，没有比赛图 |
| Flickr / Alamy / Imago / Zimbio | JS 渲染或带水印，取不到可用原图 |

##### ⭐⭐ 2026-08-17：赛事图库里有**两批**图，「best-of」那一批基本不是比赛照

上面第 ④ 条量出了「当天那一辑什么时候上线」，于是我把「等 `day-N-best-of-photos`」
当成了「等这场球的实拍」——**打开看才发现那是两回事**。day-5 那一辑 38 张当日原图，
按文件名前缀正好分成两批，**内容完全不同**：

| 前缀 | 是什么 | 抽样看到的 |
|---|---|---|
| `081N26_DAY-X_MIKE-BAKER-<i>-of-<n>` | **场外氛围**（这一辑的主体） | DJ 打碟、球员签名、厨师做菜、访谈台、小孩举球、球场远景、纪念品店、赞助商摊位——8 张里只有 1 张在场上，还是个认不出人的看台宽景 |
| **`CincinnatiOpen_2026MMDD_JM<数字>_<XX>.jpg`** | **真正的比赛动作照** | 6 张全是击球中的近景／中景，**2000×1334**，而且**不只拍明星**——外场的普通球员也在里面 |

⚠️ **所以判据不是「那一辑上线了没有」，是「媒体库里有没有当天日期的 `CincinnatiOpen_<YYYYMMDD>_` 前缀」。**
按「best-of 上线了」去等，等到的是一堆厨师和纪念品店，而**那和「今天没拍到她」
长得一模一样**——又一次「非空 ≠ 对题」，只不过这次非空的是一整个图库。

⚠️ 那一批**没有 `-scaled`，2000×1334 就是原图**（上面「`-scaled` 是 2560 的封顶版」
那条的反面已经写过）。铺 1080×1440 是 **1.08×**，过不了 `cover_photo_problem`
那道 1.00× 地板，要写一句 `cover.portrait._low_res_why`——**但它仍然远好过抽帧**：
2000px 是相机原图，抽帧是压缩转播流的 1920×1080 再放大 1.33 倍。

##### ⭐⭐⭐ 2026-08-17 第四条渠道：**当地报纸的每日图集**，原图 4813×3209

账号所有者：「**多尝试不同的方案啊 / 403 的你要多想想办法啊**」。照做挖出来的，
**分辨率比前三条都高一档**：辛辛那提这一站是 **The Enquirer**（USA Today／
Gannett）派自己的摄影师拍，每个比赛日出一辑，一辑 54~95 张。

    索引  https://www.cincinnati.com/sitemap/2026/august/<DD>/
    一辑  /picture-gallery/sports/2026/08/<DD>/photos-cincinnati-open-<slug>/<id>/
    图    那一页 `<script type=application/ld+json>` 里一串 ImageObject，
          每条带 url ＋ **完整四要素说明** ＋ copyrightHolder

⚠️⚠️ **原图要换域名**：说明里给的是 `https://www.cincinnati.com/gcdn/authoring/…`，
而那个地址**恒 406**（`usatoday.com/gcdn/…` 也是）。换成
**`https://www.gannett-cdn.com/authoring/…`** 就是无水印原图——实测 **4813×3209**，
铺 1080×1440 只要 **0.45×**，连 `_low_res_why` 都不用写。请求要带 `Accept: image/*`。

⚠️ **它的说明比赛事图库的文件名还硬**：「Alex de Minaur, of Australia, returns to
Quentin Halys, of France, at the Cincinnati Open at the Lindner Family Tennis
Center in Mason, Ohio, on Saturday, August 15, 2026」——球员、对手、赛事、场馆、
日期一句话全齐。版权是报社的（`Albert Cesare/The Enquirer`），属于四类源里的
第 ③ 档，授权照实记进 credits。

⚠️ **有这条渠道不等于有这个人**：8/14 那辑 54 张、8/15 那辑 95 张，含 `Wang` 的
都是 0 张——它和赛事图库一样偏主球场。

###### ⭐⭐ 而挡在它前面的那个 403，真因不是 UA，是**发请求的库**

`cincinnati.com` 用 curl 是 200、用 `tools/find_cover_photo.py` 恒 403。
换 Mac／Windows UA、换 `Accept`、加 `Accept-Language`——**五种组合全是 403**，
看起来就是「这个站不让爬」。真因是：

> **`urllib.request.Request` 会把头名规范成 `User-agent`（小写 a）**，
> 而浏览器和 `requests` 发的是 `User-Agent`。Gannett 那道 WAF 按这个指纹判机器人。

同一个 URL、同一份头，**urllib 恒 403、requests 恒 200**。

⚠️ 所以「403 先换 UA」那条要再加一层：**换完 UA 还 403，就换一个 HTTP 客户端**
（urllib ↔ requests ↔ curl）。这跟本文件里「`add_repo` 之前不要 curl 预检」
「WP REST 必须带浏览器 UA」是同一族——**「这条路不通」和「我敲门的姿势不对」
长得一模一样**，而这次连姿势都不在头里，在库里。

⚠️ 顺带一条边界：**沙箱里的 Chromium 过不了这个代理**（`--proxy-server` 和
playwright 的 `proxy=` 都试过，一律 `ERR_CONNECTION_RESET`，连自家能 curl 通的站
都打不开）。要真浏览器只能上 runner——`probe-blocked.yml` 的 `mode=browser`
本来就是干这个的，`--url` 随便给。

##### ⚠️ 而这个工具本身有两个查询词的坑，两次都吐一个**假的空**／**假的中**

2026-08-17 做 `wangxiyu-fernandez` 时两个都踩了，一次判错一个方向：

**① `--event` 比的是赛事名，传 WTA 的数字 id 恒空。** 我传的是 `--event 1017`
（那是接口里的赛事 id），而它拿去比的是文件名和 Getty 说明——里面写的是
`Cincinnati_Open_2026` / `Cincinnati Open`。**一张都匹配不上，不报错**，
打出来是那句「没有对得上的。⚠️ 这是『还没发』不是『没有』」——
**和真的还没发一模一样**，我据此判了两条片子「今天拿不到图」。
换成 `--event Cincinnati` 立刻出图。现在传数字**当场报错**并给出正确写法。
又一次「零命中先怀疑自己的查询词」。

**② `--player Wang` 会中一个**同姓不同人**的。** 换对之后唯一命中的是
`Xinyu_Wang_-_Cincinnati_Open_2026_-_Day_6-DSC_1213.jpg`——那是**王欣瑜**，
而我要的是**王曦雨**。同一站同一天两个中国选手都姓 Wang（WTA 接口里两条
`ResultString` 都写作 `X. Wang`，flashscore 才分成 `Wang Xin.` / `Wang Xiy.`）。
**「非空 ≠ 对题」在这儿是会直接把错的人印上封面的**：四道闸门第一道（人物）
过不了，而分辨率、授权、日期全对得上。⚠️ **姓能撞的时候，`--player` 要用
文件名里的全名写法**（`Xiyu_Wang` / `Xinyu_Wang`），别只给姓。

##### ⭐⭐ 2026-08-18：**ATP 那半边没有「当日官方图」这个东西**，而封面闸不知道

账号所有者：「**atp 目前一个也没推送微信，咋搞的**」。核下来这话是对的，
而且原因是**结构性的**，不是那天谁偷懒：

| | 同日官方实拍 | 后果 |
|---|---|---|
| **WTA** | `photoresources.wtatennis.com` **当天就发**（赛后稿／集锦视频页的头图） | 三条女子第三轮当天全部拿到实拍，全部推送 |
| **ATP** | **没有对应物**——赛事图库那一批要**次日** UTC 00:00~03:00 才上 | 2026-08-17 收紧封面闸（`frame_at` 不再放行）之后，**ATP 这条线每天都会卡到次日早上** |

⚠️ **前一天那批 ATP 之所以没卡，是因为它们发在收紧之前**（`altmaier-musetti`
`tsitsipas-auger-aliassime` `trungelliti-medvedev` 全是 `frame_at`）。
**一条规矩生效那一刻不会去扫存量，所以「昨天能发」不等于「今天能发」**
——本文件「立一条新的形状规矩时，顺手拿它扫一遍 `specs/` 再合并」记的是同一件事的另一半。

**解开它的不是放宽闸，是那条当地报纸的渠道**（上一节那条 The Enquirer 每日图集）：
它**当晚就发**，正好补上 ATP 缺的那一档。2026-08-17 那一辑 28 张里
科博利–布洛克斯占 6 张，`cobolli-blockx` 的封面就是从那儿来的（2980×4473，
铺 1080×1440 只要 0.36 倍）。

⚠️ **所以 ATP 的取图顺序和 WTA 不一样，别照搬**：

    WTA   ① WTA photo-resources（当天） → ② 赛事图库（次日） → ③ 当地报纸
    ATP   ① **当地报纸每日图集（当晚）** → ② 赛事图库（次日） → ③ AP／Getty

###### ⚠️ 而我差点得出「今天 ATP 一张图都没有」——**查询词只放了这一场的两个人**

第一遍我在 The Enquirer 8/17 那一辑里搜的是 `Tirante|Landaluce`（我当时想做的那一场），
**0 命中**，于是写下「今天 ATP 没有官方图」。把**当天所有 ATP 球员**一起搜：
**科博利 18 次、布洛克斯 18 次。**

又一次「零命中先怀疑自己的查询词」，而这次的形状值得单记：
**这条渠道一天只覆盖两三场（偏主球场），所以「按我想做的那一场去查」几乎必然空。
正确的问法是「今天哪几场有图」，然后按图挑片子**——顺序反过来。

###### ⚠️ 三处不带引号的属性，把我的正则全打空了

Gannett 的 HTML 属性**不带引号**（`href=/foo`、`<script type=application/ld+json>`），
而我惯用的正则一律写着 `href="..."`。于是同一页上：

    re: href="(/picture-gallery/[^"]+)"        → **0 条**（而 sitemap 里就有那一辑）
    re: <script type="application/ld\+json">    → **0 个**（而它就在页面里，16 KB）
    裸文本数 `gcdn/authoring`                    → **92 处**

**三个数摆在一起就能自证是我扫错了**（本文件「两个数对不上就说明是探测错了」）。
改成 `href=["\']?(/[^\s">]+)` 和 `<script type=application/ld\+json>` 之后，
28 个 `ImageObject` 全出来了，每个自带 `caption` ＋ `copyrightHolder`。

⚠️ 所以**别拿 ld+json 走不通当结论去改用裸文本扫**——`caption` 才是这条渠道
唯一的四要素来源（文件名只有日期和序号），丢了它就只剩「打开一张张认人」。

###### 一条能直接抄的命令

    # ① 当天有哪几辑（⚠️ sitemap 的 href 也不带引号）
    curl -s -A "Mozilla/5.0 …Chrome/126.0…" https://www.cincinnati.com/sitemap/2026/august/17/
      → /picture-gallery/sports/tennis/2026/08/17/<slug>/<id>/

    # ② 那一辑的 ImageObject（⚠️ 用 requests，不能用 urllib，见上一节那个 403）
    re.search(r'<script type=application/ld\+json>(.*?)</script>', html, re.S)
      → json[0]["image"] → 每条有 url / caption / copyrightHolder

    # ③ 原图：把 www.cincinnati.com/gcdn/ 换成 www.gannett-cdn.com/
    #    （前者恒 406；要带 Accept: image/*）

### ⚠️ 2026-08-16：用 Tennis TV 的源片，**片尾和台标都要剪掉**

账号所有者：「**如果用 Tennis TV 的视频的话，需要把它的片尾和它的 logo 剪掉。**」

两样东西，两个位置，**都不会报错**：

- **片尾**是末尾那张订阅／品牌卡。它一旦落进任何一段窗口，成片里就会出现别人的
  品牌页——ffmpeg 照渲，`check_reel_landed` 照过，只有打开成片才看得见
- **台标**是烧在画面角上的水印。3:4 的取景窗只从 1920 里取 810px，`cx` 居中时
  右上角那块本来就在窗外；可 `cx` 一往那边挪就会把它框进来

判据 `test_用TennisTV的源片要说清片尾和台标怎么剪掉`：spec 的 `_source` /
`_editing_why` 里出现 Tennis TV，就必须写一句 `_tennistv_trim`，说清末段收在第几秒、
离片尾多远，以及取景窗有没有把角上那块框进来。**和 `mixed_fps` / `silent_source` /
`_frame_why` 一个形状：认领这一步把「想清楚了」和「凑合一下」分开。**
已发的 24 条挂在 `_LEGACY_TENNISTV` 里，只许减不许加，表自带自检。

⚠️ **判据只看 `_source` 和 `_editing_why` 两栏，不扫整份 spec**——`_no_repeat` 里
会点名别的片子，那些片子的来路里带 Tennis TV，连它一起扫就会把这一条误判成
「也用了 Tennis TV」。又是「判据扫得太宽」那一族。

### 封面用真实照片，不要从视频里抽帧

**抽帧是最后一招，不是省事的默认。** 1920×1080 的一帧裁成 9:16 铺满要放大
1.78 倍，比一张 3000 宽的官方照片软一大截，封面又是唯一决定人点不点的那一屏。

「官方图库没有」不等于没有——**这句话说出口之前，搜索范围要先扩到底**：

- 赛事官方图库（`photoresources.wtatennis.com`、赛事自己的域名）
- 通讯社经二手站转载：AFP / AP / Reuters 常出现在菲律宾、中文门户的赛报里，
  文件名里带 `AFP__YYYYMMDD__<id>__HighRes__<赛事>` 这类自证信息
- 中文门户的图集、球员和赛事的官方社媒
- 实在只有一个人的照片，就**两张单人照拼一张 VS 海报**（见下一节），也别退回抽帧

配套的两条老规矩在这儿反复救场，一条都不能省：

- **打开看过再用**。`sports.inquirer.net` 那张文件名写着 `Alex-Qinwen`，看着就是
  合照，打开是媒体自制的宣传图：两张单人头像加烧录的赛事大字，不是真实时刻。
  网易那篇赛报页面上挂着一张 0728 的图，打开是草帽男在田里
- **soft-404 返回 200**。照着赛事站 `eala-1920.jpg` 的规律探 `zheng-1920.jpg` 等
  五个名字，全是 `200 + text/html`、大小一律 53.7 KB 的同一张错误页。
  **只看状态码，这五个都算命中**

#### ⭐ WTA 官网自己的「Match Reaction」赛后稿，配的头图往往就是这场球的实拍

`swiatek-kostyuk` 那条我直接跳过找图，用了源片抽帧（330.5s 那一帧，一顶帽子的
近景，糊）——违反了本节开头那条「抽帧是最后一招」。事后核实：这场球（多伦多
2026 WTA1000，斯瓦泰克胜科斯秋克 3-6 6-1 6-2）**真的有实拍能用**，只是没去找：

    https://photoresources.wtatennis.com/wta/photo/<年>/<月>/<日>/<uuid>/
      <球员英文名>_-_<赛事名>_-_Day_<N>-DSC_<编号>.jpg

来路是 WTA 官网自己那篇赛后稿「Match Reaction」（这条线上「查得够深」那节已经
用过这类文章拿技术统计，这次是拿它的**头图**）：搜 `wtatennis.com/news` 里
这场球的赛后稿，头图挂的就是这个 CDN 路径。**四要素自证**和 ATP 媒体库那条
（`player-<轮次>-<年>-<赛事>_00N.jpg`）是同一个形状：球员、赛事、第几个比赛日
全写在文件名里，稿子正文的比分和转折点描述（这次是「五个盘点、五次平分才拿下
的关键局」）又能交叉核实是不是这一场。实测 3801×2216，真实相机分辨率，
不是放大——比 1080p 抽帧放大 1.78 倍强一大截。

这条片子已经推送过，不重渲（消息发出去收不回来，仓库里也有「推过就不再推」的
闸）；记这条只是给下一条「赛场之上」用：**solo 封面找实拍，先搜这场球在
`wtatennis.com` 上有没有 Match Reaction 稿，头图往往就够用**，别跳过这一步
直接抽帧。

⚠️ **稿子不是唯一的入口，视频页也挂着头图。** `townsend-osorio` 那场没有赛后
稿，但 WTA 的**集锦视频页**（`wtatennis.com/videos/<id>/<slug>`）的 HTML 里塞着
一整块 CMS JSON，里面就有这条视频的头图和它自己的标题、日期：

    photo-resources/2026/08/13/<uuid>/Townsend-R1-Dylan-Buell-Getty-2.jpg
      title  "Taylor Townsend, Cincinnati 2026"
      date   "2026-08-13T17:37:00Z"        ← 比赛 17:21 UTC 结束，16 分钟后发的
      originalDetails {"width":6206,"height":4137}

**「这张图挂在这场比赛的集锦视频上」比文件名更硬**——文件名只说 `R1`，而
CMS 的引用关系直接把它绑在 MatchID 那条视频上。原始 6206×4137，比 3000 那一档
还大一倍。

##### ⚠️ 球衣颜色不能当认人判据，场地颜色可以

上面那张图我**一度判成「不是这场比赛」，而那个判断是错的**，差点为此退回抽帧。
理由是球衣颜色对不上：照片里是红裙，片内那几格是品红。两边都量过：

| | 色相 | 备注 |
|---|---|---|
| 官方照片 里的裙子 | **343~346°**（红） | 亮部 RGB `[239,34,82]` |
| 片内 240~296s | **306~322°**（品红） | 绿通道被压到 **0~6** |
| 片内 0.5 / 6.5 / 32.5 / 40.5 / 58.5 / 124.5s | **334~346°**（红） | **和照片一致** |

也就是**同一条片子里，同一件衣服，前半程读出来是红、后半程是品红**——换了机位
之后高饱和红被压出了色域（绿通道整个塌到 0）。broadcast 对荧光/高饱和面料就是
会这样，而它**看起来完全正常**，不报错、不发虚。

**真正能用的判据是场地**，因为它是同一个物理表面、不在色域边缘：

    深蓝挡风布   照片 218.2°   片内 218.6°     ← 差 0.4°
    亮蓝外场     照片 202.3°   片内 206.4°

这条是「场地文字是可以后期加的，场地的颜色不会」那条的**下一层**：那条说的是
「文字弱、颜色强」，这条把「颜色」再拆一刀——**大面积低饱和的场地颜色强，
小面积高饱和的球衣颜色弱**。⚠️ 而弱的那一档**不是「参考价值低」，是会给出
相反的答案**：照着它我差点把唯一一张 6000 像素的官方实拍扔掉。

⚠️ 顺带一条方法：**先在片内自己找反例**。我是把同一件衣服在片子不同时间点各量
一遍才看清的——**一个源内部就自相矛盾的量，不能拿去和另一个源比**。

#### ⚠️ 上面这条只管「整帧铺满」，不管「抠出来的人」

同一天里这两件事差点被我按同一条规矩办。放大倍率是反的，所以结论也是反的：

| | 素材 | 铺到哪 | 倍率 |
|---|---|---|---|
| 整帧当封面（上面那条） | 1080p 一帧 | 铺满 9:16 整幅 | **1.78× 放大** → 软 |
| 抠图当人物（cutout 版式） | 同样 1080p 一帧里裁出的人 660×1040 | 634px 的槽位 | **0.61× 缩小** → 锐 |
| 官方棚拍抠图 | ATP `player-gladiator-image` 裁到胯 265×410 | 同一个槽位 | **1.55× 放大** → 最软 |

所以 cutout 版式的人物**首选本场抽帧**，官方棚拍图退居兜底——账号所有者的原话：
「因为更贴近比赛的服装，感觉会更好，用之前资料就有点脱节」。棚拍的衣服、光、
背景跟这场球没关系，压在本场画面上像两张贴纸；而且它是全套素材里最软的一档，
「正规」是错觉。

**挑哪一帧有三条判据**（账号所有者定的，收在 `tools/pick_cover_frames.py`）：

1. **正脸或稍微侧脸**——正面人脸检出且**两只眼睛都检出**。侧脸只检出一只眼
2. **上半身直立**——跑动、挥拍、扑救的瞬间身体是斜的，站住了才立得直
3. **能看到表情**——脸够大（≥110px）且够清楚

排除观众席：同帧检出人脸数 > 2 就整帧丢掉，看台近景一帧能出七八张同样大的脸。
最后一步仍然是**打开看**——谁是谁、表情对不对题，机器判不了。

**还有第四条，工具查不出来：头和它背后那块背景要有对比。** 黄泽林那条第一版取
57.66 秒——三条判据全过、清晰度 893.6——渲出来**他成了光头**：那一帧他戴白发带，
背景正好是 `telcel` 的白色大字，抠图把白发带并进白背景，**连头顶的头发一起切掉**，
留下一个平的圆顶。同一张海报上热亚那格背景是深色、头发完好，正好是反证。

- 判据只能**渲出来看**：挑帧工具量的是脸的大小、眼睛、清晰度、运动量，
  它不知道发带和背景同色
- 换帧优先换**背景深浅和头部相反**的那一张（黄泽林换成 138.14 秒，背景是深蓝
  广告板，白发带压上去对比很强），别急着退回官方棚拍图
- 和「深色球衣压在深色背景墙上会留一块方底」是同一族：**抠图出错的方向由
  对比度决定**，浅压浅切掉一块，深压深留下一块

三个踩点，都是量出来的：

- **清晰度要把脸缩到同一个尺寸再算**（128×128 的 Laplacian 方差）。直接在原尺寸
  ROI 上算的话和放大倍率成反比：371px 的近景 17.6，198px 的中景 123.9。第一版
  门槛 40 于是把**所有近景**判成「糊」，整条片子过闸 0 帧——而那看起来和
  「这条片子里没有近景」一模一样。同口径量完是：看过并判为可用的 170~245，
  真糊的 5~25。**又一次「自己写的探测脚本报空时，先拿一个已知非空的口径对一下」**
- **运动量不设闸**。拍了个 12，而已经确认站着不动的那几帧量出来 19~25。
  转播机位本身在微调、看台一直在动，这个数量的不是球员。降级成排序里的罚分
- **`post_process_mask=True` 不能省**。不加的时候深色球衣压在深色背景墙上
  （锦织圭那格）会留**一整块方底**，渲到海报上是一个看得见的矩形边。加上之后
  三个模型的 alpha 全干净——那不是模型不行，是后处理没开。模型选
  `isnet-general-use`：三个都摊在棋盘格上看过边，u2net 和 u2net_human_seg
  把发梢切平了一条

### ⭐ 封面裁图：人物放正中间，放大到接近铺满，四周留一点点空间

账号所有者 2026-08-18：「之后封面裁图，尽量把人物放到正中间，同时放大到足够，
接近铺满整个画布，四周留一定空间即可。」

这管的是 `cover.portrait` 的 `focus` / `focus_y` / `zoom`（`fit: "cover"` 那条路）
——照片选对之后，怎么把它摆进 1080×1440 那个框：

- **`focus`（横）／`focus_y`（纵）尽量落在人物的几何中心**，别为了让球拍、球、
  地面留白而把人推向一侧
- **`zoom` 往上调，直到人物接近铺满整个画布**——四周只留一圈能看出还是完整
  一张照片的边，不是刻意留出大片空场或天空
- ⚠️ **和「精准之外，还要有冲击力」那节「裁图时把人放在中间，左右都留够…
  整个人在画面里、左右留白比想象的更多」不是同一条规矩，别互相搬。** 那条管
  的是**知识卡的横幅照片裁成 3:4** 那个场景（宁可人小一点也别切到手/脚/球拍，
  真裁不下就换一张）；这条管的是**「赛场之上」solo 封面的 `cover.portrait`**
  ——本来就是给单人竖版像用的，主体顶到接近画布边缘才有冲击力，两个场景的
  判据各管各的

⚠️ **`zoom` 不是越大越好，卡在「还留得下手／球拍／身体轮廓」这条硬线上。**
`noskova-tauson` 那条封面的迭代（`focus` 从手算的 0.62 一路试到 0.38，每一档
都真的渲出来看过）就是在找这条线：0.70 那一档球拍飞出画面只剩指尖，切过头了；
0.38 那一档球拍头、握拍手、身体、落地脚全部在框内，是及格线。**判据是渲出来
两版摆在一起看，不是按比例推**（上面那节已经写过的老规矩，这条只是把它接到
「放大到接近铺满」这个新要求上——两者不冲突：放大到贴线为止，不是放大到
切进人）。

#### ⭐⭐ 2026-08-31：**挑封面之前先把钩子那条带叠上去**——脸落进带里是几何决定的，事后调不动

账号所有者看完 `wu-walton-us-open-2026-r1` 的推送：「**吴易昺的脸被挡住了**」。

拿 y 轴标尺裁已发的 poster 一比就看见：钩子两行墨迹在 **y765~1040**（第一行
765~890、第二行 915~1040），而他的**眼睛在 y830~860、嘴在 y935**——**一行横穿眼睛、
一行横穿嘴**。

⚠️ **而它是几何决定的，选完图再调参数一点都救不回来**：那张 `reacts_8131` 里他的
头顶在源图 47%、下巴 72%，而 **1280×720 铺 1080×1440 竖向正好铺满（2.0 倍），
`focus_y` 在 `zoom=1.0` 时是死的**（`background-size:cover` 之下竖向没有可移动量，
本文件「`focal_point` 的 Y 是死的」记的是同一件事的横版）。要把下巴抬到钩子上沿
（输出 48.6%）得 `zoom ≥ 1.84`，也就是**总放大 3.68 倍**，早过了「太软」那条线
——**预裁源图和 zoom 是同一件事，同样撞这堵墙**。

**所以判据要前移到挑图那一刻**：候选按**真实裁切数学**渲出来（源图铺 1080×1440
之后每个 y 落在哪儿），**把钩子带（`STORYCOPY_TOP` 起两行）和台头带一起叠上去**，
再看脸落在哪儿。这一屏的三条带子是固定的，和照片无关：

| 带 | 位置 | 落进去会怎样 |
|---|---|---|
| 台头（球标＋栏目＋`topic`） | y 0~170 | 头顶被文字压住 |
| **钩子两行** | **`STORYCOPY_TOP` 起约 310px**（2026-08-31 起 790~1100） | **脸被两行字横穿——就是这次** |
| 比分板 | 钩子底下再 34px 起，约 250px 高 | 脚被盖住（通常无所谓） |

⚠️ **别把上面那两个 y 抄成常数**：`STORYCOPY_TOP` 被账号所有者往下挪过**五次**
（520→580→610→750→790），而钩子那一段的高度还随字号变（2 行 124px 是 308px、
3 行 96px 是 357px）。要当前值就 `grep STORYCOPY_TOP tools/versus_poster.py`，
或者直接渲一版把带子叠上去。

同批五张吴易昺的官方实拍逐张这么比过，**只有一张过得了**（`in action_7545`：源图
帽顶 11%、下巴 30%）；`reacts_7988` 头顶顶进台头带且人太小、`reacts_8131` /
`8131_1` 脸正在钩子带里、`reacts_7405` 侧脸**闭着眼**。也就是说
**「情绪最对题的那几张」和「脸露得出来的那张」可以完全不相交**——四道闸门之外
还有这一条，而它只在渲出来叠上带子之后才看得见。

⚠️⚠️ **同一轮里更该记的是我最初是怎么挑错的：我拿 Haar 检出的「脸框大小」给候选
排序，而那批框检出的是虚化的看台观众，不是吴易昺。** 一个真实存在的人脸框、一个
像模像样的排序，**结论全错**——「非空 ≠ 对题」在挑图这一步的又一个变种。更坏的是
它自带免检：数字排出来很像量过了。**Haar 只回答「这儿有没有一张脸」，回答不了
「这是不是主角的脸」**；判据只能是**渲出来打开看**。

⚠️ 已发的那条**换封面之后重发了一次**：`data/reel_publish_ledger/` **按成片哈希记**，
换封面之后哈希变了，不会被「已经发过」拦住；`output/<date>/reel/<slug>/pushed.json`
是 slug 级的，要**手动删掉**才轮得到 `auto-push-reel` 重发。`push.summary` 一个字
不动（它要和已发的 `copy.html` 标题逐字相同）。

##### ⭐ 同一条片子第三版：**撞哪条带子，由主体在源图里的高低决定**

账号所有者最后自己甩了一张过来（「这张好」）——侧身低头吼、左手攥拳的那张。
它的几何和前面那张**正好反过来**，而两种都躲不掉：

| 主体在源图里的位置 | 竖向铺满（2.0 倍）之后落在哪 | 撞谁 |
|---|---|---|
| 头顶 47%、下巴 72%（庆祝那张） | 脸在 y765~1040 | **钩子** |
| 头顶 **3%**、下巴 26%（这张） | 脸在 y40~390 | **台头** |

⚠️ **两头都是死的**：`fit: cover` 竖向正好铺满，所以**源图最上面那个像素永远映射到
输出 y=0**——主体在源图顶上，输出就在顶上，`zoom`（Z=1.2 也才把 43px 推到 52px）和
`focus_y`（zoom=1.0 时本来就是死的）都推不动。**能动的只有横向**：把脑袋挪到台头那行
字的右边（这条实测：台头文字收在 x≈420、帽子从 x≈450 起，留 30px）。

⚠️ 真想把人整个往下挪只有 **`fit: "width"`** 那条路（整幅 16:9 都在、上下垫模糊放大版、
`focus_y` 这时才活）——渲过四版，拳头和球拍全露出来、所有文字带都躲开了，
**代价是人小一大圈**，撞上 2026-08-18「优先近景特写、放大到接近铺满」。取舍写进
`_zoom_why`，别让下一个人重推一遍。

⚠️⚠️ **而这张的眼睛是闭着的**，撞上账号所有者 2026-08-21 自己定的「封面图一定不要
闭眼的」——**他指名要这张，照做**。两者不矛盾，那条规矩有边界：它当年防的是
**「从缩略图墙里挑帧、不小心挑到一个眨眼瞬间」**（表情读不出来、看着像渲坏了）；
而**用力吼出来时眼睛是挤紧的，那是表情本身**，情绪一眼读得到。
判据是「这张脸在表达什么」，不是「眼皮开着没有」。

### 栏目决定封面模板，不是素材凑手决定

剪辑线上跑两个栏目，**模板按栏目选**：

| 栏目 | 讲什么 | 封面 |
|---|---|---|
| 赛场之上 | 一**场对决** | VS 模板（两格 + 中缝 + VS 圆牌 + 两个名字） |
| 网球有故事 | 一**个人** | `layout: "solo"`，只有主角，**不印赛果** |

账号所有者 2026-07-31 给休伊特那条定的：「这个故事是讲休伊特的儿子的话题，
不是『赛场之上』的内容」「所以封面只有休伊特儿子照片」「当前对话历史都是
『网球有故事』的话题，只是这次要用视频方式呈现」。

**这是新增一个栏目，不是给赛场之上松绑。** 两件事很容易混：单人海报一旦能渲，
下次哪条对决片子凑不齐两张照片，就会「先用 solo 顶一下」——正是
`test_封面只有海报模板一条路` 拦的那个滑坡，只是换了个滑法。所以闸装在
`build_cover`（**出片那一刻**，不是提交那一刻），另有一条钉住 spec 里的配对。

- **solo 不印赛果**。「德米纳尔 6-2 6-3」是六拍里的第 5 拍，印在封面上等于
  **先把结局说了**，后面五拍就没人看了。footer 只留级别／年龄／赛事
- **横素材在 3:4 的海报里要 `fit: "width"`，不是 cover**。1920×1080 铺满
  1080×1440 要先放大到 2560×1440，横向只留中间 42%——球拍和伸出去的另一只手
  全被切掉，剩一张脸占满屏。zoom 1.45 / 1.15 / 1.0 三档都渲出来看过，
  `cover` 那条路**每一档都是大头特写**。按宽度铺是 1.0 倍，上下垫同图的
  模糊放大版；`focus_y` 让空出来的那条落在**上面**，压在左上角台头底下当页眉带
- ~~**台头那颗药丸的判据是「没有账号名」，不是「等于赛场之上」**~~
  ⚠️ **2026-08-14 药丸整块删掉了**（账号所有者：「把赛场之上那个黄色的药丸
  整体移除」）。栏目名没有丢——它一直同时印在左上角那行 `网球时差 · 赛场之上`
  里，药丸只是第二遍。判据 `test_台头药丸不许自己回来` 钉两头：药丸不许回来，
  **而左上角那行必须还在**。
  ⚠️ 删的时候要把它占的那 138px（药丸 65 ＋ gap 34 ＋ 额外间距 40）
  **原样折进 `STORYCOPY_TOP`**（610→750），否则标题和比分板会整块上移，
  把账号所有者「文案再往下一点、别遮住主体」那四次反馈一起退回去。
  真渲出来量过：改前改后标题墨迹的顶边都是 y=759
- ⚠️ **比分板每一格的竖线都一样，决胜盘不再单独描边**（账号所有者 2026-08-14：
  「把最后一盘比分前面的绿色竖线变成和其他框线一样的白色」「**以后都这样
  固定下来**」）。原来最后一格挂 `score-set--deciding`：8px 品牌绿描边 + 一层
  14% 的品牌绿垫底。**类和 CSS 一起删掉，不留没有样式的钩子**——一个版式永远
  不读的键就是个不吭声的死键。
  ⚠️ **绿底跟着一起去掉，这不是顺手扩大范围**：那层绿是**给绿竖线垫底的**，
  两样是同一次改动引进来的。竖线变白之后单剩它，量出来那一格的色相从
  200.6° 偏到 185.5°、饱和度 .379 → .257——渲两版摆一起看，读起来是
  「这一格脏了」不是「这一格重要」，正好撞上「有吸引力」那条。
  判据 `test_比分板每一格的竖线都一样不许再给决胜盘描边` **钉三头**：
  markup 里每个格子 class 一模一样、CSS 里每条 `border*` 都用同一道白线
  （**不认类名**，换个名字加回来照样红）、品牌绿不许出现在任何一条
  `.score…` 规则里（第二条只管 border，那层 `background` 靠这条接住）。
  绿只留给盘分数字（`.setwin`）
- ⭐ **比分板上的中文名字号是算出来的，不是写死的**（账号所有者 2026-08-14：
  「球员的中文名字号再大一些，现在太小了」）。`score_cn_px()` ＝
  `min(SCORE_CN_MAX_PX, 这一列装得下的最大值)`，**两位球员共用一个数**
  （各算各的会让同一块板上两行字一大一小，看着像渲错了）。和封面钩子的
  `hook_title_px` 同一个形状。
  ⚠️ **加大之前它就已经在溢出**：`亚历山德罗娃（19）` 在 33px 下要 226px，
  而那一列只有 198px——36 张比分板里 4 条名字如此，排名那几个字正压在右边
  那道框线上，而**渲染、质检、全量测试一律不出声**。所以这次同时把名字那一列
  从 `1.55fr` 加宽到 `2.3fr`（3 盘时 198px → 285px），盘分那几列仍有 175px
  装一个 60px 的数字。改完实测：72 条名字 68 条拿到 44px、4 条 41px，**0 条溢出**
- ⚠️ **宽度用 PIL 拿真字体量**（`assets/fonts/` 里那两个文件，和 CSS 的
  `@font-face` 同一批），**别按「一个汉字一个 em」估**——得意黑是斜体变形字，
  6 个字在 33px 下只有 191px 不是 198px。PIL 比 Chromium 稳定小 1~2px，
  `SCORE_NAME_SLACK_PX = 3` 就是按这个偏差留的，判据
  `test_量名字宽度要和浏览器对得上` 拿一张**冻结的浏览器实测表**盯着它别漂
- ⚠️ **`_SET_RE` 是锚死的（`^…$`），拿它 `findall` 整串恒为 0。** 第一版就是
  这么写的：每块板都按「1 盘」算那一列的宽度，于是它以为有 528px 可用——
  **名字反而更大，而且一个字都不报错**。数盘数要按空白切了再逐个 `match`
- ⚠️ **「算得对」和「算出来被用上了」是两件事。** 反向验证时把 CSS 里的
  `__SCORE_CN_PX__` 换回写死的 `33px`，那几条判据**全绿**——函数照样算出 44
  和 41，只是没有人读（和 `_push` 键名写错、`_cut_person` 从来没跑起来过是
  同一个形状）。所以另有一条 `test_算出来的字号要真的写进CSS`：**改一对更长
  的名字，渲出来的字号必须跟着变**
- **英文名那一行 22px → 18px**（账号所有者同一天：「英文名可以字号小一些」）。
  它是**注脚不是主语**，中文名涨到 44px 之后 22px 跟着抢戏。18px 是渲了
  22/20/19/18/17 五档摆一起挑的：17px 配着 1.5px 的字距开始发虚，18px 是最后
  一档「明显退成注脚、又还读得出」的。**字距不跟着缩**——小号全大写本来就该
  松一点。判据 `test_英文名要退成注脚但还读得出` 钉三头：小到位、不许小到读
  不出、**而且真的写进了 CSS**（常量改对、占位符没换，渲出来还是老样子）

##### ⭐⭐ 2026-08-29 换版式：**两行、赢家那一行整条高亮**，格子和框线整块拿掉

账号所有者甩来一张美网官方赛果图（郑钦文 6-4 3-6 6-0 胜普里丹基纳那场的官方
社媒图），原话：「**以后赛场之上视频封面的比分板都按这个设计来做，但中英文
名字和世界排名保留，国旗用矩形**」。

那张图的形状和我们旧版**正好相反**：

| | 旧版（表格） | 新版（两行） |
|---|---|---|
| 谁赢了这场 | **要读比分**才知道 | **扫一眼哪一行亮着** |
| 版式 | 六道 6px 白框线的网格 | 没有格子、没有框线 |
| 赢家那一行 | 和输家一模一样 | 实心高亮长条 ＋ 两端各一颗圆角端帽 |
| 名字 | 中文大字在上、英文小字在下 | **英文小字在上、中文大字在下** |
| 场地/用时 | 网格里的表头行 | 挪到长条**外面、上方**，左右和长条对齐 |

⚠️⚠️ **下面这段（我当时写的「用自己的颜色」）当天就被账号所有者推翻了，
留在这儿只是记录来路——真正的口径见本节末尾「第二轮：完整复刻」那一条。**

> ~~端帽的绿和高亮条的墨底用我们自己的颜色，不照抄那条蓝。参考图那条长条是
> **美网的品牌蓝**，是那个赛事的身份色；照抄的话温网、辛辛那提的片子上都会
> 印着一条美网蓝。**结构照搬，颜色用自己的**：端帽品牌绿 `#c6f65a`，高亮条
> 不透明的 `#061c14`（渲了 `.80/.86/.92/1.0` 四档挑的）。~~

**我把这个顾虑摆出来了，账号所有者看过之后仍然要求照原样复刻**——本文件
「如果我提出顾虑而他重申，那就是他的决定，说一声然后照做」说的正是这一步。
⚠️ 那四档不透明度的实测没有白做，只是主语换了：**复刻版的长条同样是不透明的**
（参考图那条也是），只不过颜色是量来的 `#172786` 而不是我们的墨绿。

⚠️ **封面这块板上，盘分的输赢只靠字重。** ~~第一版我做成「字重 ＋ 颜色两样
都要」（赢的既加粗又给品牌绿），理由是账号所有者 2026-08-04/08-09/08-13 三次
定过拿颜色分。~~ **第二轮「完整复刻、不要自己配色」把这一半推翻了**：参考图
六个数字**全是纯白**，分输赢只有字重一样东西，所以
`.score-number.setwin{font-weight:700;color:#ffffff}`。
⚠️ **顶栏和数据图那两条路的品牌绿一个字没动**——那三次定的是**视频里**的比分
（`TOPBAR_SETWIN_ASS`、`render_stat_card` 的 `.setwin`），压缩后的视频里色相
比字重分得开，而封面这块板是静态海报、又要照原样复刻，两边各按各的来。
⚠️ 字重那条**收在 `.score-number` 之内**（`.score-number.setwin`），别写在裸
的 `.setwin` 上：那个类还挂在旧版 VS 海报那条路上，写宽了会连带把十几条已发
的老海报改粗。

⚠️ **抢七小分改挂在输掉那一盘的那个数字上，而且要绝对定位。** `7-6(3)` 里的
(3) 是**输家**在抢七里拿到的分，转播记分条一律印成 `7 / 6³`。旧版两个数字挤在
同一格里，小分统一挂左边（赢家那个数），读出来像「7 比 3」；两行分开之后这个
选择躲不掉了，按网球自己的记法归位。
⚠️ **它必须脱离文档流**（`position:absolute`，挂在数字的右肩上）：留在流里的话
带小分的那一格宽出三十来像素，数字被挤离列心——`cirstea-kalinskaya` 实测
`6(4)` 比另一行的 `7` 左了 **40px**，而这块板全部的意思就是「两行的列要对得
齐」。列宽 96px、数字只占 42px 上下，两边各有 27px 富余，小分伸进那道空当就行。

⚠️ **输家那一行也渲两颗端帽，只是 `visibility:hidden`。** 干脆不渲的话两行的
左边缘会差一个端帽加一道缝（22px），国旗和名字整块错开——看起来像「这一行
没对齐」，不像「少了个元素」。`visibility` 保位置，`display:none` 不保。

⚠️ **数字和国旗都是量参考图定的，不是拍的。** 参考图里数字的墨迹高度占行高
**51.4%**（55px / 107px），而 62px 的 `TL Numeral` 在 104px 的行里只占 42.3%
——比参考图小一档，所以 `SCORE_NUM_PX` 定在 **72px**（量出来 51.0%）。国旗按
**等面积**折算：参考图那个圆按画幅缩到我们这儿是直径 79px（面积 4900），3:2 的
矩形要 **86×57** 才等重（78×52 只有 4056，摆在一起明显偏小）。

⚠️ **中文名上限 44px → 52px。** 参考图里姓氏是整行里最大的一块（字高约占行高
四成二），而 44px 的得意黑在 104px 高的行里只占三成一。新版式给名字腾出的宽度
也比旧表格多得多（三盘 **448px** vs 285px），涨得上去；`score_cn_px` 那套
「短名字给满上限、长名字按宽度缩」一个字没改。

⚠️ **版式常量只有一处出处**（`SCORE_BOARD_W` 那一批）：`score_name_avail_px`
拿它们算「名字还剩多宽」、`_fill_score_layout` 拿同一批填 CSS。写两处必分叉，
而分叉的样子是**「算出来的字号其实放不下」**——名字压在盘分上，而渲染、质检、
全量测试一个字都不会说（旧版就这么让 4 条名字压在框线上过了一个月）。
`_fill_score_layout` 末尾那句「还有没填的占位符就报错」是**判据自己的判据**：
漏填一个会留下 `flex:0 0 __SCORE_SET_COL_PX__px` 这种非法声明，浏览器**静静地
把整条丢掉**，盘分列退回自动宽度，两行的数字就此错开。

**三条判据跟着换了主语**（旧的留着就是常年红，本文件「主语没了就得换判据」
说的正是这个），换的时候它们守的东西一个字没变：

| 判据 | 旧主语 | 新主语 |
|---|---|---|
| `test_比分板两行的盘分要上下对齐` | `grid-template-columns` 解出几条轨道 | 列宽写死 ＋ 名字那格可缩 ＋ 两行格子数一样 |
| `test_比分板不许给某一盘单开样式` | 每一格的竖线都一样 | 没有框线 ＋ 选择器不许点名某一盘 ＋ 绿只许在端帽上 |
| `test_盘分那几列和名字那一格都够用` | `928/(fr+盘数)` | 按像素算，五盘那一档最紧 |

⚠️ 「长名字会缩字号」那一档从**三盘挪到五盘**：新版式三盘留 448px，
`亚历山德罗娃（19）` 在 52px 下只要 356px，**三盘已经装得下了**——拿三盘去验
「会不会缩」，验的是一个不再存在的情形，那条断言会退化成恒真。

真渲量过（`zheng-pridankina-us-open-2026-q3`，1080×1440）：左端帽 x 70–81、
高亮条 92–987、右端帽 998–1009，合计正好 940；三列数字的墨迹中心落在
**717 / 813 / 909**，两行逐列相差不超过 2px（差的是 `3` 和 `6` 的字形宽度，
不是版式）。带抢七的那条（`cirstea-kalinskaya`）改成绝对定位之后，`6(4)` 和
另一行的 `7` **都落在 718**。

⚠️ **量这块板要避开照片。** 输家那一行是透明的，按亮度扫会把照片里的白鞋、
白裙一起数进来——`cirstea-kalinskaya` 的第一列就这么被读成「差 38px」，其实
那是鞋。要么只量赢家那一行（有底色），要么把窗口收在数字那几列上。

**已发的片子不为版式重渲**——微信那条消息发出去收不回来。


###### ⭐⭐ 第二轮（同一天）：**完整复刻，不要自己配色**

账号所有者看完第一版，几句话：「**要做出和原版一样的配色**」「**不要自己配色**」
「**数字的字体也要一样**」「**输的人那一行也是有背景色的**」「**整个比分板应该
都有统一的半透明的背景**」「**要完整复刻**」。

三个颜色**逐个从参考图上量**（取纯色区的像素值，不是挑的）：

| | 值 | 怎么量的 |
|---|---|---|
| 赢家那条长条 | `#172786` | 整条一个值，rgb(23,39,134) |
| 两端的端帽 | `#739365` | 两端多点取样 rgb(113~120,144~150,99~109) |
| 文字 | **纯白** | `ZHENG`/`QINWEN`/`PRIDANKINA`/`STADIUM 17`/`#USOPEN` 和六个数字，逐个取峰值都是 (255,255,255)——**没有第二档灰**。我们原来那套 `#dcefe4`（英文名）、`#93a79c`（抢七小分）在复刻里一律去掉 |

**「输家那一行也有背景色」＝ 整块一层统一的半透明底**，不是给那一行单独填色。
⚠️ 它是**渐变淡入**的，不是硬边的板：参考图那一带逐行量下来是连续压暗
（x=980：y900 亮度 91 → y1100 75 → y1200 53 → y1325 32 → y1450 19），一条硬边
都没有。我第一版做成硬边矩形，钩子底下当场多出一道横线。

###### ⭐⭐ 比分数字的字体：**全站同一支，赢的那一盘加粗**

同一轮的另一句：「**比分的数字字体都用这种，包括以后所有视频里的其他地方的
比分都用这种字体，赢的一盘的加粗**」。

**换哪一支是量出来的，不是挑的**：把参考图那个粗「6」和细「3」的墨迹二值化、
归一到 200×200，跟手上每一支字体逐个算 IoU——

| 字体 | 粗 6 | 细 3 |
|---|---|---|
| **Noto Sans CJK SC**（系统，ASS 用） | **0.719** | **0.640** |
| **NotoSansSC-sub**（仓库里，浏览器用） | 0.716 | 0.641 |
| Noto Sans（fonts-noto-core） | 0.790 | 0.581 |
| Montserrat 600 / 500（`TL Numeral`，原来在用） | 0.682 | 0.584 |
| Barlow Condensed SemiBold（顶栏原来在用） | 0.689 | **0.440** |

前两行**是同一套设计**（差 0.003），所以封面（浏览器走 `TL Sans SC`）和视频
（ASS 走 `Noto Sans CJK SC`）从此不会再是两种数字。

⚠️ **不用 `Noto Sans`（core 那支）**，尽管它在粗 6 上更高：① 细 3 只有 0.581，
两档合起来不如 CJK SC；② **`match-reel.yml` 只装了 `fonts-noto-cjk`，没装
`fonts-noto-core`**——写上去 libass 会静静换一支，而「换掉了」和「就是这么渲的」
在成片上长得一模一样。

⚠️⚠️ **「写了 `\b1`」和「真的变粗了」是两件事。** `TOPBAR_WINNER_ASS` 上面那段
注释记着一个真事：给中文名挂 `{\b1}` 渲出来 216px → 216px，**什么都没发生**
（libass 解不到那支字体的 Bold face 时不报错也不合成）。这一轮真渲量过，
**同一条命令、带同一个 `fontsdir`**：

    中文名「萨巴伦卡」  常规 216px → 加粗 216px  差 **0**   ← 老注释说的就是这个
    数字「6 3 6」       常规 111px → 加粗 118px  差 **+7**  ← 生效

所以名字那一行照旧**只靠色相**分输赢（老注释没有过期），数字这一行可以加粗。
判据 `test_比分数字全站同一支字体而且加粗真的生效` **真渲一帧量墨迹宽度**——
谁哪天把比分字体换成一支解不到 Bold 的，它当场红（反向验证过：换回 Barlow，
106px → 106px，差 0）。

⚠️ **顶栏字号从 44 收到 40**：Barlow 是窄身，44 是给它补的；Noto Sans CJK SC
同字号宽 14%（实测 80px 下 210px vs 240px），照抄 44 会把顶栏第二行撑出去。

⚠️ **量宽度那一头：`NotoSansCJK-Regular.ttc` 是字体集合，PIL 默认取 index 0，
而 index 0 是 JP 不是 SC。别顺手「修」成 `index=2`**——五张比例脸
（JP/KR/SC/TC/HK）的**数字宽度完全一样**（实测同一串 `6-3 6-4 6-0 7-6(4)`
在 80px 下都是 618.58px，只有 Mono 那三张是 720），它们只有汉字字形不同，
而这一档只量数字。本文件「`.ttc` 是字体集合不是字体」那条说的是**解析器不认
集合头会读出空的**，和「取哪一张脸」是两件事，别混。

⚠️ **抢七小分不跟着粗，两个方向都是。** ASS 的标签粘连：`6-7(5)` 这种右边赢的，
`\b1` 还开着就写 `(5)`，它跟着粗；而 `7-6(5)` 那种左边赢的早就被 `\b0` 关掉了
——**同一个括号会按「这一盘谁赢的」渲成两个样子**，比一直粗或一直细都糟。
它是这一盘的附注（和连字符压暗同一档），不是比分本身。判据
`test_顶栏上色带抢七小分` 两个方向都钉（反向验证过）。

⚠️ **两条视频线必须一起改**：账号所有者 2026-08-18 要求过顶栏比分「改成和赛后
开麦一样」，那条约束没有被这次换字体推翻——`build_interview_clip._ASS_NAME["num"]`
和 `build_match_reel.TOPBAR_SCORE_FONT` 是同一个值，判据钉着它俩相等。

⚠️ **这一条只管「比分」。** 数据图上那些技术统计（Ace、双误）不是比分，没跟着
改；日报那条线（`webcards` 的 `html.daily`）2026-07-31 已停产，也没动；
`versus_poster` 里 VS 版式那几条 `.sets` 同样没动——那是十二条已发老海报的路径，
不重渲，改它一点收益都没有。


###### ⭐⭐ 第三轮（同一天）：**端帽拿掉、输的那一盘换 Light、坡拉长**——从此不再是逐像素复刻

账号所有者看完第二版：「可以不要获胜方两边的绿色边条」「输掉那一盘的分数的数字
需要再细一点，和加粗的区分开」「排名的数字也可以用这统一的数字，这样就只用一种
数字字体，你看合理么」「背景也要渐变的，你看原图里最上面的背景和封面图**没有
明显的分割**感觉」「不要两边的绿色竖条了」。

⚠️⚠️ **第一句和最后一句是同一件事，他说了两遍。** 那两颗浅绿端帽**是参考图上
有的**——也就是说「完整复刻」到这一轮结束了：**版式、配色、字体照参考图，端帽
是他明确划掉的一样。别看着参考图又把它加回来。**

**① 端帽拿掉**：长条从此占满 940，名字那一格 448 → **492px**。
⚠️ 在那之前输家那一行也渲两颗、只是 `visibility:hidden`，为的是两行左边缘对齐
（干脆不渲会差 22px）；**两行都没有之后对齐自然成立，别把隐形端帽留下当占位**。

**② 输掉那一盘换 Light(300)，而这件事量得出来。** 把参考图两行数字的墨迹按
「墨占外接框的比例」量：

    参考图  粗 6/6/6  墨/面积 0.61  笔画/高 0.315
            细 3/4/0  墨/面积 0.26~0.31  笔画/高 0.09~0.11   → 粗/细 = **2.14**
    我们（改前 400 vs 700）        0.53 vs 0.35            → **1.51**

也就是**参考图那个粗字接近 Black(900)、细字接近 Light(300)**，而我们原来是
Regular vs Bold。账号所有者点名要动的是**细**那一头，所以只换了它：
换成 300 之后 libass 实测墨 4773/2141 = **2.23**，和参考图对得上。
⚠️ 想再拉开就该动粗那一头（700 → 900），**没有顺手改**——他要的是细的那一头。

**③ 三档字重做成仓库里的文件：`TL Score`。** 系统的 `fonts-noto-cjk`
**只有 Regular 和 Bold**，Light 在几百 MB 的 `-extra` 里；而 libass 的字体只能从
`fontsdir`（就是 `assets/fonts/`）或系统里找。所以从 `NotoSansSC[wght]` 实例化出
300/400/700 三档、只留 ASCII ＋ 全角括号（排名写作 `（121）`），**三个文件加起来
约 200 KB**，`tools/build_fonts.py` 的 `SCORE_BUILDS` 产出。

| | 值得记的 |
|---|---|
| **libass 认 `\b300`** | 实测三档分得开：`\b300` 墨 2141 ／ `\b0` 3143 ／ `\b1` 4773。**挑不到那一档不报错、只是退回常规**，所以判据要真渲一帧量墨迹 |
| **族名单独取，不叫 `Noto Sans SC`** | 那是一个真实存在的族名，哪天机器上装了同名字体，fontconfig 挑哪一个说不准——**而挑错了不报错，只是数字悄悄换了一副样子** |
| **顺带去掉了一个 apt 依赖** | 比分数字不再靠机器上装没装 `fonts-noto-cjk`，浏览器和 libass 读的是同一批文件 |
| **`fsSelection` / `head.macStyle` 的 bold 位也要改** | 只改 name 表的话，fontconfig 认族名、libass 认 bold 位，两边会打架——`\b1` 可能挑到 Light 那一档 |

⚠️ **排名的数字「本来就是同一支」**：`.score-rank` 原来写 `TL Sans SC`，而
`TL Score` 就是同一套设计的实例化，**画面上一个像素都没变**。改它是为了让
「这块板上只有一支数字字体」这句话**说得死、钉得住判据**，不是为了修一个 bug。

**④ 坡要长，这也是量出来的。** 参考图那一带两列独立取样（x=0.93w / 0.98w，
两列逐行几乎相同 → 是叠上去的一层，不是照片本身），拿照片底色 91、面板色亮度 18
代进 `alpha=(91-L)/73` 反解：

    离藏青条上沿   -360  -300  -240  -180  -120   -60  +120  +240
    参考图 alpha   0.02  0.03  0.07  0.16  0.34  0.52  0.83  0.99
    我们 alpha     0.07  0.07  0.14  0.25  0.37  0.48  0.78  0.93

**五百多像素的坡，而且末端不透明**（+240 那一行 L=19 就是面板色本身）。
我们原来是 `top:-70px` ＋ 110px 淡到 0.86——**坡陡五倍**，顶上就有一条看得见的边。

⚠️ **判据钉的是「坡有多长」，不是「有没有硬边」**：老写法也没有硬边，它只是陡；
拿「相邻行不许跳变」当判据，老写法照样能过（实测逐行差最大 1.0，早就是连续的）。
量的是 **alpha 从 0.10 走到 0.80 跨了多少像素**——现在 420px，老写法 89px，
门槛 300 落在两者中间。
⚠️ 另外两头缺一不可：**坡上方要真有一段没被压暗的画面**（不然「整幅铺一层灰」
也能得到一条很长的坡，而那会把人脸一起压掉），**末端要接近不透明**（不然一条
又长又浅的坡也能过，而那托不住白字）。四个方向分别反向验证过。
⚠️ **量的窗口要从 y=500 起**：封面顶上另有一条 scrim（给台头垫的），它在 y=40
处 alpha 0.50、到 y≈480 才落回底噪——把它算进来，「alpha 第一次到 0.10」量到的
是它不是这层底。而「坡上方那一段」**不能钉一个绝对的 y**：钩子有几行，比分板就
跟着上下浮动，写死一个高度会在钩子长的封面上落进坡里。


####### ⭐ 五盘放得下——**而「放不下」那句告警是保守的，别拿它当判据**

账号所有者 2026-08-29：「同时看下如果是五盘数据能否放下」。渲出来量的：

| | 名字那一格 | 库里最长的名字配这一档 |
|---|---|---|
| 2 盘 | 588px | 满上限 52px |
| 3 盘 | 492px | 满上限 52px |
| 4 盘 | 396px | 37px |
| **5 盘** | **300px** | **压到下限 28px** |

五盘那一档最紧，**但还是放得下**。`胡安·曼努埃尔·塞伦多洛（51）`（12 个字，
库里最长）配五盘 ＋ 末一盘两位数抢七 `7-6(10)`，实测：

    名字墨迹到 x=478 ｜ 名字那一格右边界 500 ｜ 第一个盘分从 531 起  → 还剩 53px
    输家那一行墨迹到 x=988 ｜ 板子右沿 1010                        → 还剩 22px

⚠️⚠️ **而 `score_cn_px` 在这一档会打一句告警，那句话原来写着「这一行会顶到
右边的框线」——两个错**：① 框线 2026-08-29 就拿掉了，那东西不存在了；
② **它根本没顶上去**，还剩 53px。也就是一句**没量过、又指着一个不存在的东西**
的告警，而下一个人会拿它当判据去砍名字。现在只说「已经最小了，渲出来自己看
一眼读不读得清」。

⚠️ 这跟「编出来的判据 + 正确的结论」是一家的：**估算保守本身没错**（`score_cn_px`
宁可早一点报），错的是**把保守估算的结论写成了一句关于产物的断言**。

判据 `test_五盘也放得下` 真渲一帧、钉两头（名字不许压到盘分上、末一盘的两位数
抢七小分不许伸出板子），两个方向分别反向验证过。⚠️ 两头缺一不可：只钉名字那头，
把盘分列缩窄也能过；只钉小分那头，名字压上去照样绿。

⚠️ **上面那张表和那组数 2026-08-29 晚些时候重量过**（盘分改右对齐、右内边距
30→40 之后）：名字那一格变成 **578 / 482 / 386 / 290**，五盘那一档名字墨迹到
x=484、还剩 63px，末一盘的 `(10)` 到 x=1006、还剩 4px。**结论没变，数变了。**

####### ⭐⭐ 第四轮（2026-08-29 晚）：盘分右对齐、底再透一档、场地和用时各加一个小图标

账号所有者四句话：「比分建议右对齐」「背景底色可以再透明些」「球场和比赛用时
前面各加一个小 logo 表示下」「小 icon，白色的就行，不然很多人不知道是啥」。

**① 右对齐——而「两行对不对得齐」看不出这件事。** 盘分原来是
`text-align:center`：每列 96px、数字只占 42px 上下，于是末一盘那个数字的右沿
停在 **x=949**，而正上方的用时「2:19」是贴着右内边距的、右沿在 **977**——
**两条右边线差 28px**，比分因此看起来浮在条子中间。
⚠️ 居中和右对齐**在「两行对不对得齐」这件事上完全一样**（每个数字都是单字符、
等宽），所以旧判据一个字都不会红。新判据钉的是**另一件事**：数字的右沿和它
头顶那一行的右沿是不是同一条线。改完两条线都落在 966/967。

**② 右对齐之后唯一还会往右伸的是抢七小分**，它是绝对定位、挂在数字右肩上
（`left:calc(50% + .30em)` → **`left:100%`**）。实测从列右沿起：

    (5)  36px   →  SCORE_FILL_PAD_R 30px 装不下，伸出板子 1px
    (12) 52px   →  40px 也装不下，伸出 7px

所以右内边距 **30 → 40**（单位数还剩 9px），**两位数那一档整块板的上标换小一号**
（`.scoreboard--tbwide`，`.42em → .32em`）。⚠️ **上标是绝对定位的，改它的字号
不影响任何布局**——数字、列宽、名字那一格一个像素都不变，所以这一档只花在它
自己身上。⚠️ 按**整块板**切不按单个格子切：同一块板上两种大小的上标比统一小
一号难看。
⚠️ **155 条已发的 spec 里末一盘两位数抢七一条都没有**（末一盘带抢七的 17 条
全是单位数），所以那一档今天走不到——它防的是以后真出现一次时**不吭声地伸出
板子**，而那和「就是这么渲的」在成片上长得一模一样。

**③ 底再透一档：`SCORE_PANEL_END_ALPHA` 1.0 → 0.78。** 参考图那儿是 1.0
（底部量到 L=19，就是面板色本身），账号所有者要「再透明些」。拿**真封面**渲了
1.0 / 0.90 / 0.82 / 0.74 四档摆一起挑的，不是在纯色上试。
⚠️ 判据那一头从 `>= 0.90` 降到 **`>= 0.70`，而 0.70 是算出来的不是拍的**：白字
压在最坏的底（纯白照片）上，合成底色 `L = 255(1−a) + 18a`，要拿到 WCAG 4.5:1
需要 `L ≤ 119`，也就是 `a ≥ 0.58`，留一档余量取 0.70。
⚠️ 判据量到的是**合成值**（这层底 ＋ 封面自己那条 scrim 的底边），所以它比常量
本身高（0.78 的常量量出来是 0.85）——**别把这两个数当成一个**。

**④ 两个小图标：球场（俯视）＋ 钟。** ⚠️⚠️ **球场画了三版，账号所有者说了
两次**——「小 icon，白色的就行，不然很多人不知道是啥」→「**球场的 icon 要画成
网球场的样子啊**」：

| | 画的是什么 | 缩到实际尺寸读出来是 |
|---|---|---|
| 第一版 | 竖的方框，里面切四格 | **一扇窗** |
| 第二版 | 横的外框 ＋ 球网 ＋ 一个内框 | **一块多米诺骨牌** |
| **现在** | 外框 ＋ **单打边线贯通** ＋ 发球区 ＋ 中线 ＋ **球网出头** | 网球场 |

**认出「这是网球场」靠三样，缺一样就不像**（三版逐个量过，数在下面那条判据里）：

1. **横的**——网球场是 2:1，画进方 viewBox 里只能占中间一条
2. **单打边线贯通全长**（也就是双打的那两条边道）——足球场没有这个
3. **球网出头**（网柱在界外），所以那条竖线上下各伸出一点

⚠️ **前两版还有一个共同的物理原因：太小。** 图标原来是 1.05em 的方 viewBox，
球场只能占中间一条、渲出来约 23×11px，几条线糊成一片。现在球场单独用
**26×13 的窄 viewBox**（贴着球场本身），渲出来 **35×20px**，边线和发球区才
分得开。
⚠️ 宽度先给到 1.70em（44×25px），账号所有者跟着说「**宽度再小一些**」，收到
**1.35em（35×20px）**。五档并排比过（1.70/1.55/1.45/1.35/1.25）——**四条贯通
全宽的横线到 1.25 都还分得开**，所以这一档不是被清晰度卡住的，是按「和 26px
的文字放在一起哪一档不抢戏」挑的：量出来图标高 20px，而同一行大写字母的墨高
是 21px，两者齐平——读起来是个字里的符号，不是一枚徽章。
⚠️ **缩小之后线宽要跟着往回加**：线宽是 viewBox 单位，图标缩小它就一起细下去
——原来那个 1.05 配 1.35em 只有 1.05×1.35＝**1.42px**，而钟是 1.93px，同一行上
一粗一细。三档并排渲过（1.20→1.62px / 1.35→1.82px / 1.50→2.03px），取 1.35。
两个图标的线宽要**按各自的缩放算**（钟 24 单位缩到 1.05em×34px、球场 26 单位
缩到 1.35em×26px），不是写同一个数。
⚠️ **判据是缩到实际尺寸看，不是在画布上看着像**——账号所有者要的正是
「不然很多人不知道是啥」。而「像不像」这次**落得了判据**：`test_球场那个图标要
画成网球场的样子` 把图标抠出来二值化，钉上面那三样（宽高比 ≥1.5、贯通全宽的
横线 ≥4 条、最上一行的墨占宽 ≤0.25）。三头缺一不可——第一版过得了第 2 头、
第二版过得了第 1 头，两版分别反向验证过。
⚠️ 颜色**只有一处出处**：两个 svg 自己不写颜色，靠 `.score-icon` 的
`stroke:currentColor` 跟着文字走，「文字全部纯白」那条一改图标自己跟上。
⚠️ 头一行从 `align-items:baseline` 改成 **`center`**：svg 是个方块、没有基线，
按基线排会把它吊在文字上方。

⚠️⚠️ **图标那条判据的第 ③ 头一开始是一盏绿灯，是反向验证抓出来的**：它读
`versus_poster._COURT_ICON` 来检查「svg 里没写死颜色」，而同一个测试**在这之前
已经把这两个属性 monkeypatch 成空串**去渲「没有图标」那一版——于是它检查的是
两个空串，`'stroke="' not in ""` 恒真。往 svg 里写死一个 `stroke="#fff"`，
测试照样绿。修法是**在打桩之前先把原文抄下来**。
这跟本文件里「判据自己也要有判据」是一家的，只是这次让它恒真的不是别人，
**是同一条测试自己上面几行的 monkeypatch**。


⚠️ 顺带：这三个字体文件**重新生成一遍是逐字节相同的**（`font.recalcTimestamp
= False`）。不关它的话 19 张表里只有 `head` 不同——差的是内嵌构建时刻和跟着它
变的校验和；而这是**提交进仓库的二进制**，每次 rebuild 换一遍字节，等于往 git
里塞 200 KB 噪音，「重生成一遍对一下」这个检查也就没法做了。

⚠️ **判据自己踩过一次坑，记一下**：第一版查数据图那一行写的是
`"'TL Sans SC',sans-serif;font-size:56px" in card`——而 `'TL Numeral','TL Sans
SC',sans-serif` **里就含着这个子串**，退回 Montserrat 照样能过。反向验证当场
抓到它是恒真的。现在把 `font-family` 整条抠出来比**第一项**。


### 封面停多久，跟着封面那句配音走

账号所有者的原话：「这个栏目不要求〔固定几秒〕，**随着配音切换**吧」。

在这之前封面是**定长静止的哑屏**（`anullsrc` 一路铺到底），开场那一屏一个字
都没说——而封面是唯一决定人点不点的那一屏。现在 `cover.narration` 合出来多长，
封面就停多长，加 `COVER_TAIL=0.25s` 的一口气（贴着最后一个字切，末尾辅音会被
concat 的边界削掉，听着像卡了一下）。

- **长度由语音算**（`cover_length`），不在 spec 里另写一个数。写两处必分叉
- **没写 `cover.narration` 的退回定长 `COVER_SECONDS=1.2`，而且要出声说退了**。
  存量的赛场之上都没有封面配音；悄悄退回去的话，「封面短了」和「配音没合出来」
  长得一模一样
- **长度要落进 `render.json`**。跟着配音走之后就**从 spec 算不出来了**，
  `check_reel_landed.py` 拿常量对片长只会得到一个假红
- **封面那一路要真的混进音轨**。滤镜标签跟着 `filters` 一起攒，别在下游拿
  另一个条件重筛一遍——原来 `names` 就是重筛的，加封面这一路时定义了没人接

**念的就是海报上印的那句钩子，就不另排字幕。** 钩子在海报上是几十号的大字，
字幕把同样的话在同一帧里再写一遍，只是把画面弄脏。但**判据是「一不一样」
（去标点后比对），不是「封面一律不出字幕」**：封面那句要是另说了一件事，
它照样要有字幕——「静音刷是默认状态」这条对开场同样成立。

顺带修掉一条常年红：`check_reel_landed.py` 还在要 `1080×1920`，而成片早改成
3:4 了，于是它**每跑一次第一行都报不合格**。一条常年红的检查等于没有检查，
和常年不变的 skip 数字是同一个毛病。画布现在钉在
`test_检查工具认的画布要和成片的画布是同一个`。

### 让当事人自己说：`quote`

采访原声那一段**不配旁白，但必须出中文字幕**（spec 里写 `quote`，和
`narration` 互斥）。

字幕那条线原来只认 `narration`，没有旁白就没有字幕——于是原声段在成片里对
**大多数人是彻底空白**：听不懂英文的看不懂，而「静音刷是默认状态」，静音的
人连声音都没有。两头都丢，**而且不报错**。

- `quote` 不合成语音，所以闪避不会把他的话压下去（现场声开在 `BED_LOUD`）
- 字幕按字数等比铺满整段——拿不到词边界时本来就是这么退的
- **断行要渲出来比**，不能想。休伊特那条第一版「我也不确定。主持人说，」被并进
  同一行，读起来成了「我也不确定，主持人说」——**他的话和旁人的话粘在一起，
  意思全反**。排三版比断行，选每行正好一个子句的那版

值不值得为它多跑几趟：休伊特那条的落点就是他自己讲那个庆祝动作
（「他从来没给它取过名字，他就是一直那么做。我小时候看见了，从来没问过。」）——
用他的声音比我们替他转述强得多。

### 多源剪辑：尺寸没有出路，帧率要认领

一条片子跨几条源片时，`check_sources_match` 在下载完就判，**两样的严重程度
不同**：

- **尺寸**对不上就是裁错（裁切窗口按源片宽高算）。没有出路，一律红
- **帧率**对不上是重采样，成片跟**主源**走。50 → 25 是整数倍，隔帧丢得整整
  齐齐，看不出来；30 → 25 是六帧丢一帧，**静态说话头几乎无感，回合镜头一眼
  看得出一顿一顿**

所以帧率要在 spec 里**显式认领**（`mixed_fps: {源键: 为什么可以}`）。一律红会
把采访这类素材整个挡在门外（采访多是 30，赛事集锦多是 25/50）；一律放行又回到
「兜底出事的时候不吭声」。认领这一步是让这个取舍**留下判据**。

**探测要把下游会用到的每一个量都报出来。** `probe.json` 原来只记尺寸，帧率要
等 render 下完两条源片（一分半）才算——休伊特那条的两条官方集锦
**同一个频道、同一周，一条 25 一条 50**，按原样 render 必炸，而这件事本可以
在探测那一刻就知道。加进 probe 的当天就抓到了。

### 「赛场之上」的封面是固定海报，不是每条现搓

账号所有者的要求：「赛场之上，我希望以后有个**固定的海报设计**，能结合双方球员，
且第一眼能抓人眼球」。版式定死在 `tools/versus_poster.py`，换片子只换素材和文字；
改版式改一处，几条片子一起重渲比一眼。

**默认是 `cutout`**（2026-07-29 定）：人物是透明底的抠图，背景必须从**本场比赛
视频**里截取能看清整片球场的底线全场机位，再虚化压暗。人物**首选本场抽帧**
（`versus.top = {"frame_at": …, "box": […]}`，判据见上一节），官方棚拍抠图
（`cutout`: 本地 PNG）是源片里挑不出近景时的兜底。不能拿赛事场馆资料图、
别场比赛或通用球场图代替；`versus.background` 必须同时写
`frame_at` 和 `shot: "wide_court"`，让来源与景别都能被测试钉住。
换掉 `diagonal` 是因为后者要**两张**「本场 + 比赛中 + 有冲击力 + 够清晰」的实拍
同时到位，而这种照片经常凑不齐——帕雷哈那次四类源全探到底一张都没有，只能拿握手
那一帧顶，结果王欣瑜在同一张海报上出现了两次。抠图把「认人」一次性解决，而且
**按球员 ID 永远拿得到**。`diagonal` / `split` / `stack` 三版留着，斜切那道品牌绿
中缝让接缝成为设计的一部分（平分那版中缝是硬横线，看着像两张图没对齐）。

#### ⭐ 官方头像／抠图上哪儿拿（2026-08-04 记，别再重新踩一遍）

账号所有者：「**把这个获取方式记录下来，以免以后忘了**」。

**ATP（男）——有公开入口：**

    https://<任一ATP赛事域名>/-/media/alias/player-headshot/<球员ID>

- **ID 从球员页 URL 拿**：`atptour.com/en/players/<slug>/<ID>/overview`
  （黄泽林 `W0BH`、穆纳尔 `MU94`、鲁德 `RH16`、兰达卢塞 `L0IL`）
- ⚠️ **`atptour.com` 总站对本环境 403，赛事自己的域名镜像同一批**
  （实测 `mubadaladcopen.com` 通）——和上面「ATP 总站封，赛事域名镜像着同一批图」
  是同一条
- 出来的是 **300×300 PNG，透明底**，正好当小头像

##### ⚠️ ID 猜错时返回的是**占位剪影**，而它和真头像长得一模一样

`HTTP 200`、`Content-Type: image/png`、同样 300×300——**只看状态码和类型分不出来**。
我先按 `L0BM` 猜兰达卢塞，拿到的就是那个灰色人形。

**判据是颜色数，一行就能判**：

```python
len(np.unique(np.asarray(im.convert("RGB")).reshape(-1,3), axis=0))
# 占位剪影 2 色 ｜ 真头像 243~256 色
```

这跟本文件里 WTA `player-gladiator-image` 返回通用剪影是同一个坑，只是换了个门。
**比肉眼快，而且能写进脚本。**

**WTA（女）——没有公开头像接口，但仓库里已经有抠图：**

    assets/players/wta-<WTA_ID>-<slug>.png

那是 VS 海报用的**官方抠图**（透明底半身像），`eala-osaka` 那条片子用的就是它。
探过的四条外部路径**全不通**，别再试：

| 路径 | 结果 |
|---|---|
| ATP 那个 `player-headshot` 别名喂 WTA id | ❌ 返回占位剪影（4729 字节） |
| `api.wtatennis.com/tennis/players/<id>` | ❌ 有 id / 国别 / 生日，**没有图片字段** |
| `…/players/<id>/profile` | ❌ 404 |
| `photoresources…/players/headshots/<id>.png` | ❌ 403 |

**WTA id 怎么查**（这个接口有用，只是没有图）：

    https://api.wtatennis.com/tennis/players/?pageSize=5&name=Eala
    → {"id": 330332, "countryCode": "PHI", "dateOfBirth": "2005-05-23"}

⚠️ 顺带：那个 `dateOfBirth` 可以**用来核年龄**——伊埃拉 2005-05-23，
所以 2026-08-03 她是 21 岁，片子里写的对得上。

##### 从抠图裁头像：按 alpha 找头顶，别用人脸检测

抠图是透明底，**不透明区域的最上沿就是头顶**，几何上就能定位：

```python
ys, xs = np.where(np.asarray(im)[..., 3] > 40)
top = ys.min()                      # 头顶
w_head = int((xs.max() - xs.min()) * 0.52)   # 肩宽的一半略多
```

⚠️ **人脸检测在这一步是错的工具**：同一天拿 Haar 去裁一张 6000×4000 的实拍，
**零命中**；缩到 1000 宽仍是 0（戴帽子、脸在阴影里）。而抠图的 alpha 通道
100% 可靠、不用调参。**又一次「空结果先自证是真空」——先换工具，别先怀疑没有脸。**

#### 没有官方抠图，就退回照片版——别拿头像凑

**「永远拿得到」有例外。** 大威 2026-07-29 这条就撞上了：WTA 球员页搜 `Torso`
四次重试全零命中（同一轮波塔波娃的页面一次就中，所以是真空不是查错），WTA API
没有图片接口，赛事域名的 `player-gladiator-image/<wta_id>` 返回的是**通用人形
占位剪影**，只剩一张纯白底的官方头像。

账号所有者定的：**有这个问题就用照片版兜底**（`layout: diagonal`）。

**别拿头像凑**——试过，渲出来就知道不行：

| | 头占抠图高度 |
|---|---|
| 官方半身抠图（波塔波娃） | 26% |
| 官方头像（大威） | **74%** |

要让两颗头一样大，头像那格的 `scale` 得压到 0.16——**一颗没有身子的浮头**站在
另一个人的半身像旁边；而且头像本身是裁过的方图，三边硬边，`CUT_FADE` 只淡出底部
20%，救不了左右。

两处报错都把这条兜底写进去了（`versus_poster.py` 和 `build_match_reel.py`），
判据落在 `test_没有官方抠图时报错要指出退回照片版`。**报错要说出路，不能只说不行。**

模板的三条硬约定：

- **两个人必须同框**，一格一个。这个栏目讲的是一场对决，封面只放一个人就少了一半
- **中文名是模板的一部分，不是装饰**。只有两张脸的 VS 卡等于让人猜这是谁打谁，
  而中文名是这条片子在信息流里唯一能被扫到的东西。缺名字直接报错，
  **名字一律查译名表**（`player_names_top500.json` 优先）
- **钩子压在下三分之一**，上面整片留给人脸

三个渲染坑，都是渲出来看才发现的：

- **面板的盒子要只占自己那一格。** 先前让两块都占满整幅 1080×1920 再用
  `clip-path` 切，那样 `background-size` 的百分比是按**整幅画布**算的：横图缩到
  1920 高会宽出画布几倍，`focus` 调的其实是整幅里的位置，看得见的只是被切剩的一条
  ——渲出来像「一张照片压根没出来」
- **头顶几乎没留白的素材要按宽度铺 + 垫模糊**（`"fit": "width"`）。郑钦文那张
  690×460 头顶只剩 2%，按高度铺满是**既切头又放大到 2.21 倍**；按宽度铺是 1.57 倍，
  还多出 298px 让头顶透气。垫层和照片的交界**必须羽化**，2.6%（≈27px）时那条
  亮度差还是一条看得见的横线，7% 才化开
- **斜切带的半高要从中缝那条线的倾角推出来**，不能另拍一个数。画布 1080 宽，
  −7.4° 的斜线从中点到一端升 540·tan(7.4°)=70px，占 1440 高的 4.87%。原来写死
  7%——比画出来的那条绿线更陡，于是两张照片的交界和绿线**对不上**，下格还被多
  切掉一条，伊埃拉的头正好卡在那一条里

还有一条是选素材而不是排版：**下格的人要摆在钩子的另一边**。钩子那两行压在画面
左下，所以把伊埃拉裁到右边六成六处，左边留空场给字——构图本来就该给字留位置，
而不是等排版去躲人。顺带她从 0.32 倍放大到 0.54 倍，主体大了七成。

海报的画幅就是成片的画幅：**1080×1440（3:4）**，所以不用再留什么「安全区」，
整张海报在小红书信息流里都露得出来（见下面「画幅」那节）。

### 精准之外，还要有冲击力

**对得上 ≠ 能用。** 大威那条第一版，五张图全出自同一次赛前训练和发布会拍摄——时间地点人物全对，
看起来却像一册资料照，被一句"除了第一张，其他图都太没吸引力了"整个打回。

- **两道闸门，顺序不能反**：先按精准筛（时间 / 地点 / 人物 / 事件四样都对得上），再按冲击力排。
  两道都过不了的，宁可换这一屏的角度，也别拿平淡的对题照顶上去
- **没有时间约束的槽位，一律找最近一场比赛里最有冲击力的那张**。训练照、发布会照、几年前的
  资料照都属于"能用但没劲"；只有当照片本身就是"当年"的论据时才用旧的，并把年份写在画面上
- **搜索范围按需要放开**：官方图库拿不到就去新闻站、图片社转载、社交媒体。授权照实记，
  发布前由人工判断——但**精准校验一步都不能省**，源越野，越容易混进对不上的图
- **一定要在球场上**。赛后走廊、观众席、发布会都不算。大威那条收尾一屏用过一张赛后
  走向观众席的照片，**左边被一只举手机的手挡住**，人也不在场上——两条都不合格
- **大头特写不等于有冲击力**，往往相反：脸占满一屏，看不出他在哪儿、在干什么。
  锦织圭那两屏原来一张抛球特写、一张反手特写，换成「飞身扑救」和「决赛里冲刺」
  之后，同样是他，信息和张力都多了一层
- **庆祝照也不等于击球中**。商竣程收尾一屏原来用成都夺冠双臂举起那张——情绪够，
  但人不在打球，而且和前面两屏同出一周。换成 2026 澳网首轮的正手击球：
  同一个人，最近的一场，正在挥拍
- **裁图时把人放在中间，左右都留够。** 横幅原图裁成 3:4 时，很容易为了「填满」
  而贴着人裁，结果球拍、后腿、球被切在边上，人也偏在一侧。宁可人小一点：
  **整个人在画面里、左右留白比想象的更多**，比一个顶到边的大人像耐看。
  真裁不下（比如手和脸分得太开），就换一张，别硬切
- **横幅照片会被信箱式缩放**（`wide = pw/ph >= 1.2` 就走 `contain`），上下留黑边，冲击力先折
  一半。真正想让它砸下来的那一屏，**先裁成 3:4 再入库**，让它铺满整张卡
- **但「裁不下」的第三条出路是不裁。** 账号所有者的原话：「有些如果裁剪效果不好，
  可以不用裁剪，之前有好多图片就是不裁剪来表达意思的。」上面那两条读下来只剩
  「裁好」和「换一张」，于是郑钦文那一屏我为了裁得下换成了 1920px 的次选图，
  再 2× 上采样——**构图让位了，球拍还是丢了，白挨一次放大**。
  换回 3200px 原图直接入库，反而是全套里最好的一屏。

  **判据是渲出来两版摆在一起看**，不是按比例推。同一天的四屏各不相同：

  | 这一屏 | 裁 / 不裁 | 为什么 |
  |---|---|---|
  | 郑钦文 3200×2132 | **不裁** | 球到伸出的左手横跨 1930px，3:4 只有 1599px；且背景近黑，留白融进深绿卡面**几乎看不出边** |
  | 费里 2400×1632 | 裁 | 不裁他缩成中间一条，笑脸都看不清 |
  | 伊万尼塞维奇 2400×1600 | 裁 | 同上，人和奖杯是这一屏的全部 |
  | 澳网签表 1080×810 | 裁 | 不裁等于把字缩到 0.56 倍——**这一屏要读的就是那两个字母** |

  规律：**主体小、要看的是姿态** → 不裁；**要看清脸或字** → 裁。

- **信箱式缩放那几屏，上下留白垫同一张照片的模糊放大版。** 原来铺的是
  `.hero.diagram` 那层绿色径向渐变——**不透明**，于是卡片顶栏成了一条实心
  色带；而铺满的那几屏顶栏是压在照片上的。同一条片子里两种顶栏，账号所有者
  一眼看出来：「顶部一定要半透明，和前面几张卡片一样」。
  **修法不是挪照片**（挪上去人头又钻到顶栏底下，正是上一版被指出的问题），
  是在底下垫一层 `.hero.blurbg`：同图 `cover` + `blur(44px) brightness(.42)`
  + `scale(1.2)`（给 blur 留溢出量，否则边缘透底）。上层 contain 一个像素
  不动，构图完全保持原样。

- **⚠️ 收边要收在没人的那一侧。** 克里斯特尔斯那张 2339×2196 我为了压到
  比例 1.2 以上，**从顶上切了 325px**——正好是她头顶的留白。渲出来头顶和
  女儿举起的那只手一起顶到卡片顶栏底下，账号所有者一句「头像被遮挡了，
  没有透明」。**顶栏是不透明的深绿，人贴着它就等于被切。**
  改成同样的收边量全从底下走（2339×1933，比例 1.210），一个像素都不动顶部，
  两版摆一起立刻能看出来。**留白要留在人头顶那一侧。**

- **还有第四条出路：收边到刚好过 1.2 的门槛。** 上面那张郑钦文最后既不是裁 3:4
  也不是原样 16:9，而是横向收到 **1330×1080（比例 1.231）**——压在
  `wide = pw/ph >= 1.2` 上方一点点，所以仍走 `contain` 不被切，但比 16:9 窄了
  三成，主体在卡上大了约 **44%**。球拍、球、双脚、双手、赛事横幅全在框内。
  「填满」和「留黑边」不是只有两档，中间这一档往往最好。

- **⚠️ 亮度是可以量的，别等人说「太黑了」。** 那张 WTA 的强逆光剪影我判成
  「背景近黑所以留白看不出边」，**这句是对的，但漏了另一半**：人脸、球拍、
  裙子全在暗部，压在深绿卡上根本看不清。账号所有者一句「太黑了看不清楚」。

  判据就一行，入库前跑一次：

  ```python
  a = np.asarray(Image.open(p).convert("L"), dtype=np.float32)
  a.mean(), np.median(a), (a < 50).mean()
  ```

  | | 均值 | 中位 | 暗于 50 |
  |---|---|---|---|
  | 逆光剪影（弃用） | **16.5** | **0** | **95.3%** |
  | 同一场顺光那张（在用） | 77.6 | 110 | — |

  **中位数为 0 就是一半以上的像素纯黑**，这种图无论怎么裁都救不回来。
  同一场比赛的图库里翻一翻，顺光的那张就在隔壁

### 封面上每个名字旁边都要有国旗，后面括号里是即时世界排名

账号所有者 2026-08-02：「以后封面上所有球员的名字旁边要加上他的国旗，对阵的同时
在后面括号里面加上他的即时的世界排名」。

渲成 `🇵🇭 伊埃拉（28）` / `🇯🇵 大坂直美（13）`——排名压到 0.6em 并降一档亮度：
同字号并排会跟名字抢，而它是注脚不是主语。量过版心（1080 减两边 66px ＝ 948px）：
两侧加起来约 854px，还剩 94px；**再往上加字（比如「世界第 13」）就顶出边**。

**两样都不许手打**，和译名一条道理：

- **国别**取 ESPN 赛程里 `competitor.athlete.flag.href` 末尾的 IOC 码
  （`…/countries/500/phi.png` → `PHI`）——那是赛事方登记的国籍，比按名字猜可靠
- **排名**取 ESPN 的 rankings 接口（`sources/rankings.py` 已经在用）
- 取数：`PYTHONPATH=src python3 tools/lookup_player_meta.py "<英文名>" --dates YYYYMMDD`

三个踩点：

- ⚠️ **ESPN 的榜单只到每个巡回赛前 150。** 查不到**不等于**没有排名——照 `null`
  写进 spec 就是替他声明了一件没核过的事。工具把这两种情况分开报
- **`rank` 允许显式写 `null`**＝「查过，他确实没有排名」（资格赛、掉出榜单）。
  和 `mixed_fps` / `silent_source` 同一个形状：**认领这一步把「查过没有」和
  「忘了填」分开**。漏写这个键会报错，写 null 会渲成只有旗和名并且在日志里出声
- ⚠️ **`fonts-noto-color-emoji` 缺了不报错**：Chromium 回退到没有旗帜字形的字体，
  🇵🇭 变成两个方框或者裸的「PH」，海报照样出得来、工作流照样绿。判据按
  「谁渲海报」自动推（`test_渲海报的工作流都要装emoji字体`），以后多一条出海报的
  线，它会替人记得

**排名是快照，不是常量**：写进 spec 的是那场球前后的名次，重渲旧片子之前要重查。

⚠️ **两个源报两个数时，先查哪个新，别对半退成一个范围。** 2026-08-03 霍达尔
`lookup_player_meta`（ESPN）报 24、维基报 15，我写成「世界前二十五」——两边都
成立，看着稳妥。账号所有者一句话戳破：「**现在他的排名是 15，24 是上周的**」。
ESPN 那份是**上一周的快照**，维基那条明写着 `(3 August 2026)`，日期就在字段里。

- **分歧本身带着答案**：两个源都会标快照日期，比一比就知道谁新，这一步比编一个
  含糊说法便宜得多
- 「前二十五」这种范围**读起来像谨慎，其实是把没查清的事包装成了查清的事**——
  而且它比错的数字更难被下一个人发现

#### ⭐⭐ WTA 排名：`players/ranked` 这个接口，**参数少一个就 400**

上面那条给 ATP 定了官方源，**WTA 这半边一直空着**——于是 `rank` 这个字段反复
卡住出片（`stearns-tauson` 就是因为它停了一轮）。

**主路是 JSON 接口**（`noskova-boulter` 的 `_facts` 里早就写着 `players/ranked`，
只是没人把参数记下来）：

    GET https://api.wtatennis.com/tennis/players/ranked
        ?page=0&pageSize=100&type=rankSingles&sort=asc&name=&metric=SINGLES
    → [{"ranking":53,"points":1061,"tournamentsPlayed":22,"movement":4,
        "rankedAt":"2026-08-10T00:00:00Z",
        "player":{"id":327573,"fullName":"Peyton Stearns","dateOfBirth":"2001-10-08",…}}, …]

⚠️⚠️ **少一个参数就是 400，而 400 读起来像「这个接口没有排名」。** 我 2026-08-16
就是这么栽的：`?page=0&pageSize=300&type=rankSingles&sort=asc` 报 400，我据此往这份
文档里写了「players 接口没有排名」——**那句话是错的，是猜参数猜错了**。
`metric=SINGLES` 是必须的，`name=` 空着也要给。**加参数要一个一个试，别整串换掉
再从一个 400 推出结论。**

⚠️ 它一次给 100 条；`rankedAt` 就是这一版榜单的日期，**写进 spec 的排名要连它一起记**
（排名是快照不是常量，重渲旧片子之前要重查）。

**深过前 100 用官方 PDF**（`pypdf` 直接读，34 页覆盖到一千名开外）：

    curl -sL -o rank.pdf -H "User-Agent: Mozilla/5.0" \
      https://wtafiles.wtatennis.com/pdf/rankings/Singles_Numeric.pdf
    # 表头写着 As of / Printed 两个日期
    # 每条：Rank ｜ (Prior) ｜ NAME ｜ Nat ｜ Points ｜ # Trn …

⚠️ **PDF 里名字是全大写的 `姓, 名`**（`TAUSON, CLARA`）。按 `Tauson` 大小写去搜
**零命中**，而那和「这个人不在榜上」长得一模一样——我第一次就是这么搜的，
11864 行里报了 0 条。⚠️ 双打是同目录下的另一份 PDF，别拿错。

**两条路互相印证过**（同一天、同一个 `As of 2026-08-10`）：陶森 42/1184、
斯特恩斯 53/1061、佩古拉 3/6680、弗雷赫 41、萨姆索诺娃 43——**逐项相同**。

**走不通的三条，别再重探**：

| 路 | 结果 |
|---|---|
| `tools/lookup_player_meta.py`（ESPN） | **恒 403**，ATP/WTA 两个榜单都取不到 |
| `wtatennis.com/rankings/singles` 网页 | **只有前 50**，`?page=2` / `?pageSize=200` **被静默忽略**（三个参数返回的字节数一模一样，1175687） |
| `api.wtatennis.com/tennis/tournaments/<id>/<年>/players` | **只有种子号**（`seed`），没有排名 |
| tennisexplorer 球员页 | 是个 JS 壳，`Country:` / `Age:` 都不在 HTML 里 |

⚠️ **种子号和排名差得可以很远，别拿种子号顶排名。** 陶森在辛辛那提是 **27 号种子**，
而官方 PDF 里她是**世界第 42**（`Prior` 那栏写着 30）——种子是按报名截止那周的排名排的，
之后掉了。两个数都对，混成一件事就成了假话（本文件前面卢布列夫那条记的是同一件事）。

**验它有没有读对，用「相邻名次」自证**：`FRECH, MAGDALENA 41` → `TAUSON, CLARA 42`
→ `SAMSONOVA, LIUDMILA 43`，单调连续；再拿一个已经发出去的片子对一下
（`PEGULA, JESSICA 3`，和 `pegula-waltert` 海报上印的一致）。

#### ⭐⭐ ATP 排名去**官方 posting** 拿：`protennislive.com/posting/<年>/<赛事id>/`

账号所有者 2026-08-16：「**你要去官方渠道去看排名，维基百科不能做为第一源**」。

**`atptour.com` 对本环境恒 403**（本文件记过好几次），于是这条线一直在用二手源。
真去找才发现**官方 posting 站是通的**，纯 HTTP、不用 token：

    正赛签表   https://www.protennislive.com/posting/<年>/<赛事id>/mds.pdf
    资格赛签表 https://www.protennislive.com/posting/<年>/<赛事id>/qs.pdf
    双打       .../mdd.pdf
    （赛事 id 就是 ATP 的那个，辛辛那提＝422；ent/oop/ss/ip/acc 一律 404，别试）

一份 `mds.pdf` 里印着**这一站的全部权威事实**，而且页脚带发布时间戳
（`Released 08/15/2026 20:54:48`）：

    Seeded Players Top Half        ← 种子号 + Rank 两栏分开印（⚠️ Rank **不是当期**，见下一节）
      Player                Rank
      5 de Minaur, Alex       7
      8 Shelton, Ben         10
    62 Q HALYS, Quentin FRA        ← **`Q` ＝ 资格赛打上来的**
    Last Direct Acceptance: Tsitsipas, Stefanos - 85
    Withdrawals / Retirements / Alternates-Lucky Losers   ← 各自成栏

⚠️ **`pypdf` 直接抽得出文本**，不用 OCR。签表正文里种子号贴在名字前面
（`64 5 DE MINAUR, Alex AUS`），而**排名只在 `Seeded Players` 那张小表里**
——非种子球员的排名在正赛签表里没有，要去 `qs.pdf` 的资格赛种子表找
（`2 Halys, Quentin 54`）。

##### ⚠️⚠️ `Last Direct Acceptance` 那个数**不是当期排名**，是报名截止那周的入围线

2026-08-16 差点印错。`tsitsipas-auger-aliassime` 的签表页脚写着

    Last Direct Acceptance: Tsitsipas, Stefanos - 85

而同一周他真实的世界排名是 **49**。**85 是他按报名截止那周的排名卡进正赛的那个数**
——LDA 这一行的定义就是「最后一个直接入围者用的入围排名」，和当期榜单差着六周。

⚠️ **它伪装得很好**：它出自**官方**，就印在种子表旁边，格式也是「名字 - 数字」，
和上面那张 `Player / Rank` 表长得几乎一样。**同一份 PDF 里两个数不是同一种数。**

**分界很硬：**

| 这份 PDF 里的 | 是什么 | 能不能印上海报 |
|---|---|---|
| `Seeded Players` 表的 `Rank` 一栏 | ⚠️ **定种子那周的快照，不是当期**（见下一节） | ❌ 只能拿来认种子号 |
| `Last Direct Acceptance: <名字> - N` | **报名截止那周的入围排名** | ❌ 只能写成「他是卡着最后一格进的正赛」这种话 |
| 签表正文左边那一列 | **签位号**（本文件上一节记过） | ❌ |

**三个数一起打架的时候怎么定**（这一场就是三个）：flashscore 官方榜单 feed 的 `RA`
报 **49**（`10 Aug 2026` 那期）、tennisexplorer 报 **53**（没有日期）、官方 LDA 报 **85**。
取 49，两条理由：① **ATP 排名每周一更新**，08-10 是周一、下一次是 08-17，
所以这场球（08-16）当天在用的就是 10 Aug 那期，而 49 是**有明确日期**的那一个；
② 同一个账号两天前在 `tsitsipas-royer`（同一站首轮）已经印过 49——**同一周同一站
给同一个人印两个数，读者没有第二个地方可以对。**

##### ⚠️ tennisexplorer 要带 `-L`，不带的话它返回**空**，看起来像整站挂了

同一轮里查非种子排名时栽的：`curl -s https://www.tennisexplorer.com/mutual/<a>/<b>/`
返回 **0 字节**，而且**连一个已知好用的 URL 也是 0 字节**——我据此差点写下
「tennisexplorer 今天不通」。真相是它 **302 重定向**到规范化的 slug
（`/mutual/trungelliti/medvedev-e0d2d/`），而 `curl` 不带 `-L` 不跟。

⚠️ **「空」和「挂了」长得一模一样**，本文件「空结果先自证是真空」记的正是这个；
这次自证的办法是**拿一个已知非空的 URL 对一下**——它也空，那就不是 slug 的问题。

##### ⚠️⚠️ 2026-08-18 修正：**`Seeded Players` 那一栏 `Rank` 也是快照**——上面那张表原来写错了

上面那张表原来把 `Seeded Players` 的 `Rank` 一栏标成「当期世界排名 ✅ 就用它」，
**那是错的**，而且我照着它给三条 spec 填了排名。拿辛辛那提 `mds.pdf` 的 12 个种子
逐个比 tennisexplorer 的 **10.08.2026** 榜单（页面自带榜单日期）：

    种子  签表 Rank / 当期        种子  签表 Rank / 当期
     5    德米纳尔  7 / **8**      18   保罗    21 / **24**
     7    科博利    9 / **10**     21   菲斯    24 / **21**   ← 反过来了
     9    莱赫奇卡 12 / **14**     22   塔比洛  26 / **29**
    12    霍达尔   15 / **11**     32   费里    36 / **37**
    14    门西克   17 / **16**      1   兹维列夫 3 / 3   ✅ 碰巧一样
    15    瓦舍罗   18 / **19**     28   布洛克斯 32 / 32  ✅ 碰巧一样

**12 个里 10 个对不上。**

⚠️ **它伪装得比 `Last Direct Acceptance` 好得多**：LDA 只是一行附注，而这是一张
标着 `Rank` 的正经表格，**而且那一列和种子号严格同序**——看起来像一份干净的排名表。
可**同序恰恰是它的原形**：种子就是按某一份榜单排出来的，所以排种子用的那份榜单
必然和种子号同序。**当期排名不会**（菲斯 21 排在保罗 24 前面，而种子号是保罗 18、
菲斯 21，两者掉了个个儿）。

**所以这份 PDF 里三个数没有一个是当期排名**：签位号、LDA、`Seeded Players` 的 Rank
全是别的东西。当期排名只能另查（tennisexplorer 的 `/ranking/atp-men/` 页面自带
榜单日期；WTA 那半边走官方 `players/ranked` 的 `rankedAt`）。

⚠️ **判据是「拿另一份带日期的榜单逐个比，看有几个对不上」**，不是「这张表看着像不像
排名」。一个数对得上不算数——兹维列夫和布洛克斯这次就碰巧一样，只挑他们俩验会得出
「签表是准的」这个错结论。

顺带三条同一轮量出来的：

- **球员页的 slug 带哈希后缀**（`/player/fritz-f1aa7/`），猜不出来；从
  `/list-players/?search-text-pl=<姓>&type=atp` 或任一赛事结果页的链接里取
- ⚠️ **按姓搜会搜到同名的另一个人**：搜 `tsitsipas` 拿到的是
  `/player/tsitsipas-557e1/`，`Current/Highest rank 1037. / 881`——**不是斯特凡诺斯**。
  拿到排名之后要和已知的量级对一下
- 球员页那一行是 `Current/Highest rank - singles: 70. / 22`，**当期在前、生涯最高在后**

##### ATP 球员 id 查不到时，用 `WebSearch` 搜 atptour.com 的 H2H 页

`atptour.com` 对本环境 403，DuckDuckGo 的 HTML 端点这天也返回空。**能用的是
`WebSearch` 限定 `allowed_domains: ["atptour.com"]`**——搜索结果的 URL 本身就带 id：

    /en/players/lorenzo-musetti/m0ej/overview                     → M0EJ
    /en/players/atp-head-2-head/joao-fonseca-vs-alex-michelsen/f0fv/m0qi  → F0FV / M0QI

⚠️ **拿到之后照旧要用官方头像端点验**（颜色数 > 50 才是真头像，2 色是占位剪影），
而**镜像主机名要带 `www.`**：裸的 `mubadaladcopen.com` 现在 **301**，`www.` 那个才 200。

##### ⚠️ 我这次错在哪儿：拿二手源去校验二手源，跳不出二手源

`halys-deminaur` 三个源打架（维基信息框 **5**、tennisexplorer **7**、维基签表
种子表 **8**），我按本文件「先查哪个新」那条去分辨，**过程看着很严谨，结论是错的**：

1. 先发现 **tennisexplorer 的 `?date=` 被静默忽略**——`2026-08-03` 和 `2026-08-10`
   返回**一模一样的表**，**而页眉把我传的日期回显了出来**。这一条是真的，
   而且伪装得比 WTA 那两次（`?extended=true`、`year=2026`）更好：**它回显了参数**。
   ⚠️ **判据不是「参数被接受了」，是「换个值内容变没变」**
2. 然后挑**谢尔顿**当标定物（他刚拿蒙特利尔冠军，排名必然动过），维基签表把他
   排第 **6**，据此判定维基那张表是当期 ⇒ 德米纳尔取 **8**

**第 2 步整个是错的**：官方签表印的是**谢尔顿第 10**、德米纳尔第 **7**，
和 tennisexplorer 一致；维基那张种子表从第 4 位起整段偏了一位。
**而我的标定物（谢尔顿的维基信息框）本身也是维基**——两个都错，还互相印证。

| | 谢尔顿 | 德米纳尔 |
|---|---|---|
| **ATP 官方 `mds.pdf`** | **10** | **7** |
| tennisexplorer | 10 | 7 |
| 维基签表的种子表 | 6 ❌ | 8 ❌ |
| 维基球员信息框 | 6 ❌ | 5（停在 `13 July 2026`）❌ |

⚠️ **教训不是「维基不准」，是「校验链的根必须落在官方」。** 挑标定物这个技巧
本身没问题——错在**标定物和被校验的对象出自同一个源**，那等于让它自己给自己作证。
先去官方拿一个数，再拿它去标定二手源，顺序反了就是这次。

⚠️ **这个错差点发出去**：海报和数据图上都印着 `(8)`，成片已经渲完、`check_reel_landed`
0 项不合格、全量 2001 绿——**没有任何一道闸查得了一个「格式正确的错数字」**。
是账号所有者一句话拦下来的。

#### ⚠️ 编辑稿的**引语**可以抄，**结论**不可以

同一条片子上，beIN Sports 的赛报给了两段德米纳尔的原话（真的，进了片子），
同一篇的摘要里还有一句「**德米纳尔已经连续九次赢下阿利斯**」——**假的**。
两个交手源都推翻它：flashscore 的 Head-to-head 段只有三条，tennisexplorer 的
`mutual/` 页直接报 `Halys leads 2-1`，而且**此前两次都是阿利斯赢**。

**核完之后这条反而成了片子最好的落点**（德米纳尔第一次赢下这个人）——
也就是说，去核一个看着无关紧要的数字，换回来的是一条比原计划更强的线。

⚠️ 这跟「非空 ≠ 对题」是一家的：**一篇稿子里，可直接引用的部分和需要复核的
部分混在一起，而它们读起来一样权威**。原话是记者听到的，结论是记者（或摘要
生成器）算的——**只有后者会算错**。

规矩之前发出去的十二条封面挂在 `_LEGACY_NO_FLAG` 里，**只许减不许加**。

### 轮次写「半决赛 / 1/4 决赛 / 1/8 决赛」，不写「四强 / 八强」

账号所有者 2026-08-02：「以后不要用四强八强之类的，**国内通常用半决赛 1／4 决赛
1/8 决赛之类的。再往前就第几轮好了**」。

| 这一轮 | 写 | 不写 |
|---|---|---|
| 最后一场 | 决赛 | —— |
| 倒数第二轮 | **半决赛** | 四强 |
| 倒数第三轮 | **1/4 决赛** | 八强 |
| 倒数第四轮 | **1/8 决赛** | 十六强 |
| 再往前 | **第一轮 / 第二轮 / 第三轮**（首轮、次轮也行） | 三十二强、64 强 |

「四强」「八强」是港台和赛事方的说法。国内观众读到它要多想一步「四强是打到哪
一步」——而封面上只有一行字的位置，多想一步就是走掉一个人。

判据落在 `test_轮次要写半决赛不写四强`，两个细节是踩出来的：

- **只查会发出去的字段**（旁白、封面印的字、推送那几栏、小红书文案）。
  `_source` / `_why` / `_match` 这些是写给下一个人看的注解，里面正引着账号
  所有者那句原话——连它一起扫就会把「把规矩记下来」判成「又违反了规矩」。
  同一个错这个仓库里犯过三次
- **规矩之前发出去的九个文件挂在清单里，只许减不许加**。已发的片子不为了措辞
  重渲；但新写的 spec 要么用新叫法，要么显式把自己加进清单——让「又用了强字」
  变成一次看得见的决定

### ⭐⭐ 大满贯男子是**五盘三胜**——第三盘不是决胜盘，输了也不是就输了

账号所有者 2026-08-31：「**大满贯男子是五盘三胜，所以吴易昺第三盘不是决胜盘，
也不是输了就输了**」。

来路：`wu-walton-us-open-2026-r1`（美网男单首轮，7-6(2) 6-2 7-5）的旁白和
小红书正文都把第三盘叫「决胜盘」，还跟了一句「**输掉这一局比赛就结束了**」。
两句都是假的——4-5 那一局输掉只是**丢掉这一盘**（2-1），比赛照打。

⚠️ **它为什么一路过了所有闸：这条线上 95% 的片子是三盘两胜**（WTA 全部 ＋
ATP 巡回赛），「第三盘＝决胜盘」是一个**默认成立到不会被怀疑**的习惯——
而它恰恰在**大满贯男子**这一档是错的，也就是美网/澳网/温网/法网期间。

| | 赛制 | 决胜盘是第几盘 |
|---|---|---|
| **大满贯男子单打** | **五盘三胜** | **第五盘** |
| 大满贯女子单打 | 三盘两胜 | 第三盘 |
| ATP / WTA 巡回赛（含 1000） | 三盘两胜 | 第三盘 |

⚠️ **跟着一起错的是「赌注」那句话。** 「输掉这一局就出局」「这一分定生死」
这类话**要先知道赛制才写得出来**——写之前问一句：**他现在领先几盘？输掉这一盘
之后比分变成几比几？** 这条片子是 2-0 领先，答案是 2-1，还早得很。

闸在 `reel_facts.decider_set_problem`，坐在 `enforce_spec_wording` 那个座位上
（它要小红书正文，`validate_spec` 拿不到）：**赛事是四大满贯 ＋ 两位都是 ATP
球员（按 `stats.*.headshot` 的 `atp-` 前缀认，机械的）＋ 这场没打满五盘**，
此时用户可见字段里出现「决胜盘」就红。真要提**别的比赛**的决胜盘，
写一句 `_decider_why` 认领。**176 条存量扫过，命中 0。**

⚠️ **反过来那一半（三盘两胜只打了两盘却提「决胜盘」）故意不收**——扫出来
4 条**全是误报**：`rybakina-li`「上一场对卡萨金娜她被拖进了决胜盘」、
`wong-gea`「上一轮他 6-7 6-4 6-0 淘汰塞伦多洛，决胜盘 6-0」、
`shelton-nakashima-montreal-final` 讲交手史、`landaluce-draper` 讲一个没发生的
假设。**说的都是别的比赛，而机器分不出「这一场」和「另一场」**。宽到那一档
就是一条天天误报的闸，而人会写豁免去压噪音，**把它唯一想拦的那一类一起关掉**
（`crosses_cut` 那次的老账）。

### 报时间说绝对时刻，不要倒计时

账号所有者：「不用写距离开赛还有多久，因为我花的时间不固定。你只说开赛的时间就行了。」

「距开球还有 11 小时」是在**我写这句话的那一刻**算的。对方可能隔一会儿才看、
看完再去做别的，这个数一落地就开始骗人——而「北京时间 8 月 2 日 00:30」什么时候
读都对。

这条对产物同样成立，而且那边更严重：片子和文案里写的是绝对时刻（易逝栏目本来就
要求把日期印在画面上），**倒计时印上去等于给这条片子设了一个比比赛还早的保质期**。

### ⭐ 别拿「比赛的形状」当「片子的形状」

账号所有者 2026-08-05，在我说完「这场不值得做」之后：

> 「**那也得做啊，这是热点啊**」

那场球是张帅 6-0 4-0 15-0 领先时奥斯塔彭科退赛。我给的理由听着很硬——
「没有转折点、没有赛点落地，三分钟的集锦大半是走过场」——**而且每个字都对**。
错的是我拿它推出了「这条不值得做」。

**这场球确实没有故事，但那一周有**：多伦多女单签表里六个中国球员，到那天
走了五个（朱琳 8/1、郑钦文和郭涵煜 8/2、王曦雨 8/3、王欣瑜 8/4），只剩
三十七岁的张帅还在打，而她下一轮撞上世界第一。那场退赛是**入场券**，不是主体。

| 我判的 | 该判的 |
|---|---|
| 这**场球**里有没有转折点 | 这**条片子**要讲的事，有没有落点 |
| 集锦里有多少可用镜头 | 手上的事实够不够撑起一个说法 |

- 「没有转折点」是**素材的一个属性**，不是选题的判据。`_turning_points` 那一栏
  可以如实写「这场没有」——它是**用来记录的**，不是用来否决的
- **热点本身就是理由。** 这条线的选题闸只有一条硬的（「只做还在打的赛事，
  别翻库存」，见下），它管的是**时效**不是**戏剧性**
- 和「⭐ 还差三张图，要我去找吗」是同一族的反面：那条说的是**下一步明确时
  别问**，这条说的是**否决之前先想想主体是不是在别处**

⚠️ **闸没有变松。** 发出去的照旧要过四道选图闸门、「贴近比赛事实」那条、
以及「不许重讲同栏目已经铺开过的东西」（那条片子一个字都没碰 R1 的
`zhang-putintseva` 讲过的 24 连败和 595）。变的只是**否决的门槛**：
素材单薄不是不做的理由，**讲不出一个站得住的说法**才是。

### ⭐⭐ 不做比赛前瞻——「开球之前」这个栏目不再加新的了

账号所有者 2026-08-17：「到你这应该是开球之前吧」「**不要做比赛前瞻，这不是
网球有故事的内容**」。

来路：我扫完当天的比赛日，挑了威廉姆斯姐妹拿外卡重组打双打那条，做成一条
「开球之前」——事实全部两个源核过（官方签表 PDF、官方 Order of Play、赛事官网
公告、维基逐年列表）、四道选图闸门逐条走过、全量绿、PR 都开好了。**东西没做错，
是这个品类不该做。**

⚠️ **这条最该记的不是「少做一个栏目」，是我判错了什么。** 我把「今天有什么可做」
读成了「今天有什么值得讲」，而**一场还没打的比赛，能讲的只有两边的来路**——
那正好是常青内容，不需要挂在一个开球即过期的栏目下面。前瞻真正在卖的是
「等下要开打了」这个**时间点**，而这个号卖的是**故事**。

| 想讲这两个人 | 去哪儿 |
|---|---|
| 球已经打完 | **赛场之上** |
| 只想讲来路、跟哪一场没关系 | **网球有故事**（常青，不会过期） |
| ~~还没打，先摆来路~~ | ❌ **不做了** |

**停的是栏目，不是底下那套工具**（和日报那次同一个形状）：`COLUMNS` 里那一条、
封面那两行比赛坐标、`perishable` 那套过期逻辑全部留着——已经发出去的 9 条还要
能渲、能查。变的只有「不再往里加新的」。

⚠️ **必须落成判据，不能只记在对话里。** 写这条的时候 `开球之前` 名下已经有 9 条，
其中三条（`eala-anisimova` / `fonseca-oconnell` / `wang-sabalenka`）是**当天别的
会话刚推上 main 的**——这条线正在被好几个会话同时生产。一条只活在某一次对话里的
决定，拦不住下一个会话，也拦不住换了上下文之后的我自己。
判据 `test_不再做比赛前瞻`，9 条挂进 `_LEGACY_PREVIEWS` 只许减不许加、表自带自检，
两个方向反向验证过。

### 选题只做还在打的赛事，别翻库存

#### ⭐⭐ 而「还在打」还不够——**上一个比赛日的也不做**

账号所有者 2026-08-17：「**兹维列夫是上一个比赛日的不要做了**」
「**蒂兰特也是上一个比赛日的不要做了**」。

上面那条的线画在**赛事**上（这一站还在打就行）；这条把线收到**比赛日**上：

    今天这个比赛日打完的      ✅ 做
    上一个比赛日打完的        ❌ 不做——哪怕这一站还在打、哪怕素材今天才出

⚠️ **判据是「这场球哪天打的」，不是「素材哪天出的」。** 这两件事在这条线上
**天然错开一天**：Tennis TV 的 `library/interviews` 今天挂出来的是**昨天**那批
采访（我这次挖到的 20 条全是 8/15–8/16 的 R1/R2），而「刚出现在库里」读起来
特别像「刚发生」。**素材的新鲜度骗得过人，比赛日骗不过。**

查一行就够：

    flashscore f_2_**0**_0_en_1   ← 今天这个比赛日
    flashscore f_2_**-1**_0_en_1  ← 上一个

⚠️ **它会让「刚找到一条好源」变成「今天不做」，而那是对的。** 我这次已经把
兹维列夫那条整条写完了（46 组双语、片尾板量好、封面待挑）才被顶回来——
**下次的顺序是先查比赛日，再动手**，别等写完了才问它是哪天的。

⚠️⚠️ **它还会在你干活的过程中生效——候选是会自己过期的。**
莱巴金娜胜汤森德那条，我挑它的时候是**当天**打的，窗口和字幕都做完了；
等做完前面两条回头看，**日期翻了一天，同一场在 flashscore 里已经落到 `-1`**。
按这条规矩它也得撤。

所以判据不是「我挑它的时候是不是今天」，是**「现在推出去的时候是不是今天」**。

⚠️⚠️ **而我第一版给这条开的药方是反的，账号所有者当场纠正：「还有其他的也要
并行做啊」。** 我写的是「一条片子一次做完再开下一条，别几条并排放着」——
**串行恰恰就是让它过期的那个原因**。候选会过期，解法是**快**，不是慢：

- **今天这个比赛日的候选，凑齐了一起发**。`interview-clip.yml` 的
  `concurrency` 分组是 `interview-clip-${slug}`，**分组名带 slug，不同片子互不掐**
  ——两条、三条同时跑 `mode=subs` / `mode=render` 完全可以
- **只有 `mode=push` 必须串行**（都要往 main 写 `copy.html`、提交、等 Pages 重建），
  而那一步 1 分多钟，本来就不是瓶颈
- **推之前再查一次比赛日**（`f_2_0` 里还找不找得到这场），这一步一秒钟

**「一次只做一条」在这条线上是个昂贵的坏习惯**：一条片子从骨架到推送要等
两三趟 runner，串起来就是一小时，而一小时足够让日期翻过去。

⚠️ 这条和「⭐ 别拿「比赛的形状」当「片子的形状」」不冲突：那条说的是
**素材单薄不是不做的理由**，这条说的是**过期是**。

##### ⭐⭐ 2026-08-18 量出来两件事：**换日的那一刀在傍晚，不在午夜**；而且两个 feed 是**重叠**的

上面那条我今天又栽了一次，代价是**一条几乎做完的片子**（`shnaider-chwalinska`：
probe 落了库、逐分和官方统计核完了、TNNS 的制胜分/UE 也拿到了、H2H 的角度都想好了），
回头查 `f_2_0` 已经找不到它。记这一段是因为**当时那两条 feed 的实际长相，和「按午夜切」
的直觉差得很远**——2026-08-18 00:02Z 实测（辛辛那提女单）：

| 这一场 | 开打 | `f_2_-1` | `f_2_0` |
|---|---|---|---|
| 斯瓦泰克-萨卡里 | 08-17 15:10Z | ✅ | ❌ |
| 弗雷赫-莱巴金娜 | 08-17 17:25Z | ✅ | ❌ |
| **施奈德-赫瓦林斯卡** | 08-17 19:10Z | ✅ | ❌ ← 过期了 |
| 诺斯科娃-陶森 | **08-17 22:45Z** | ✅ | ✅ |
| 纳瓦罗-佩古拉 | 08-17 23:00Z | ✅ | ✅ |
| 伊埃拉-阿尼西莫娃 | 08-18 00:30Z | ✅ | ✅ |

两件事：

- **刀口落在 8/17 的 19:10Z 和 22:45Z 之间**，也就是**日场整批一起翻**，夜场留在今天。
  ⚠️ 具体几点翻的**没量到**（只夹在这两个开赛时刻之间），但**上界是量得到的**：
  施奈德那场 21:18Z 打完，**00:02Z 已经过期，也就是不到两小时四十五分**。
  **按「到午夜为止」估会高估一大截**
- ⚠️⚠️ **两个 feed 有重叠**（上表下面三行两边都在）。所以判据只能是
  **「`f_2_0` 里找不找得到」**，不能是「在不在 `f_2_-1` 里」——后者会把诺斯科娃-陶森
  这种**今天的**比赛也判成过期

⚠️ **而这一次串行的代价是可量的**：同一个班次我按「做完一条再开下一条」发了
`swiatek-sakkari`、`rybakina-frech` 两条，第三条就死在这道刀口上。
`match-reel.yml` 的 `concurrency` 分组是 **`match-reel-${slug}`**（量过的，
和 `interview-clip.yml` 同一个形状）——**赛场之上这条线的 probe / cover / render
本来就可以几条同时发**，上面那句「凑齐了一起发」不是只对采访片说的。

⚠️ **没有做成硬闸，是故意的**：这条规矩只对「今天新写的」成立，而 `specs/` 里
一百多条已发的 spec 全都是过去某个比赛日的——做成闸就要配一张同样大的豁免表，
而那正是本文件说的「一个会过期的名单和一条常年红的检查是同一个毛病」。
它只能是开工前和推送前各查一次的那一秒钟。



账号所有者（2026-08-01）：「要做也是做**即将发生的**。历史的比赛做没有意义了，
**现在没有热度**。」

判据和「素材好不好」无关，而且它排在四道素材闸门**前面**：

| | |
|---|---|
| ✅ 做 | 赛事**还在打**，或**马上要打** |
| ❌ 不做 | 赛事已经结束。**素材再好也不做** |

**这条最容易被素材库带偏。** 赛后开麦那个库有 4241 条，它按「有没有场上采访」
排，不按「有没有人在看」排——翻起来条条都合格：画面在场上、时长够、有字幕。
我挑下一条时给出的两个候选，一个是六月的柏林、一个是三天前的首轮，
**四道闸门全过，热度这一维根本没进判据**。判据不在候选里，就等于不存在。

⚠️ **别把「赛后」读成「只能做过去的事」。** 素材天然滞后几小时（官方单场集锦
赛后才发），但那说的是**素材**不是**选题**：正在进行的赛事里刚打完的那一场，
既是「赛后」，也仍然在热度里。伊埃拉那条就是当天的八强，发出去时半决赛还没打。
**要躲的是翻库存，不是躲赛后。**

这跟上一条是一家的：那条管**一句话**什么时候过期，这条管**一整条片子**
什么时候过期。

### 选谁：中国球员 > 顶级球员 > 热点球员，主要做单打

账号所有者 2026-08-07：「优先中国球员再 top 级别球员，主要关注单打，
还有热点球员比如伊埃拉」。

上一条管的是**这场球什么时候过期**（时效性），这条管**同一天有好几个候选时
先做谁**（优先级），三档从高到低：

| 档位 | 是谁 | 判据 |
|---|---|---|
| ① 中国球员 | 单打，不管种子不种子、爆不爆冷 | 中国观众本来就是冲这个来的，不用等一个额外的理由 |
| ② 顶级球员 | 排名/种子靠前 | ① 档当天没有候选才轮到这一档 |
| ③ 热点/破圈球员 | 排名不一定顶尖，但故事本身在发酵 | 伊埃拉是标准例子：拿下 DC 站冠军之前她只是非种子（世界第 28），排名进不了②，但「菲律宾第一个 WTA 冠军」这个故事够大，单独成一档，不能靠排名把她筛掉 |

**主要关注单打。** 双打不进这个排序——没有别的说明，选题就在这三档单打球员
里挑，不主动找双打候选。

三档不是互斥的、是优先级：同一天中国球员和热点球员的比赛都在打，先做中国
球员；都没有的时候才轮到纯按排名挑的顶级球员。

#### 半决赛级别的都要做，轮次本身就是够格的理由

账号所有者 2026-08-12：「半决赛级别的都要做视频的，已经很高的关注度了」。

上面那张表管的是「谁」，这条管**轮次**——而且是一条独立的、排在最前面的
判据，不是给③再加一个子档：**打进半决赛（含）以上，不用再看是不是中国
球员、是不是顶级种子、故事有没有发酵，轮次本身就够格**。签表走到这一步，
剩下的都是本来意义上的少数人，关注度已经摆在那儿，不需要再等一个额外理由——
和①档「中国球员不用等一个额外的理由」是同一个道理，只是这次给理由的是轮次。

    轮次 ≥ 半决赛   → 直接做，不看是谁
    轮次 < 半决赛   → 照旧按①中国球员 ②顶级球员 ③热点球员 排

半决赛、决赛都算「半决赛级别以上」。ATP、WTA 两条线都适用。

#### ⭐ 反过来的那道闸：**没话题、排名又不高的，不做**

账号所有者 2026-08-15，在我发完 `boulter-volynets` 之后：
「**博尔特这种没有话题啊，排名也不高没必要做，以后不要做了**」。

上面那张表回答的是「同一天有好几个候选先做谁」，**它默认了所有候选都够格**。
这条补上被默认掉的那一半：**够不够格本身要过一道闸，而且是两个条件占一个就行**：

| | 判据 | 例子 |
|---|---|---|
| **有话题** | 这个人/这件事本身在发酵，刷到的人认得他 | 孟菲尔斯（最后一个赛季）、伊埃拉、大威 |
| ~~**排名够高**~~ | ~~顶级球员／种子~~ | ⚠️ **「种子」这一档 2026-08-16 收紧了，见下一节** |
| **两个都不占** | ❌ **不做** | 博尔特（世界第 71）对沃利内茨（第 89），首轮 |

⚠️ **「数据反常」不是话题。** 我给博尔特那条挑的角度是「输的那个人小分和局数
都更多」——数字是真的、两个源对得上、四道闸门也全过，**可它是一条趣闻，不是一个
人们本来就在关心的事**。刷到的人不认识这两个人里的任何一个，那个悖论再漂亮也
换不来停留。**「这一场有没有一个算得出来的狠数据」是写法上的加分项，不是选题的
入场券**（`_hit_data` 那条规矩管的是前者）。

⚠️ **别把这条读成「排名低的不做」**——同一天做的孟菲尔斯世界第 305，比博尔特低
两百多位，而他是这条线上最该做的一条。**分界是话题，不是名次**；名次只是在没有
话题的时候顶上来的第二条路。

⚠️ **也别把它读成推翻了「⭐ 别拿「比赛的形状」当「片子的形状」」**（张帅那条，
账号所有者说「那也得做啊，这是热点啊」）。两条讲的是不同的东西，合起来正好是一句话：

    有话题 + 比赛平淡（张帅对手退赛）        → **做**，主体在别处
    没话题 + 比赛有趣（博尔特那个数据悖论）  → **不做**

**素材单薄不是不做的理由；没有人关心才是。**

##### ⭐⭐ 2026-08-16 收紧：「是种子」不算热度——**WTA1000 有 32 个种子**

账号所有者，在我一天里发完五条之后：「**球员热度太低的就不要做了**」。

上面那张表的第二行原来写着「排名够高：顶级球员／**种子**」。**那一档几乎筛不掉
任何人**——WTA1000 一张 96 人签表挂 32 个种子，进正赛的人里三分之一都是种子。
`stearns-tauson`（陶森世界第 42、27 号种子，对斯特恩斯第 53）就是这么过闸的，
而它正是账号所有者说话那一刻刚发出去的一条。

**现在三条占一条**（闸在 `build_match_reel._players_are_worth_a_reel`，
`--dry-run` 0.2 秒就报——选题不够格是**开工之前**该知道的事）：

| | 判据 |
|---|---|
| ① **中国球员** | `country == "CHN"`，**不看名次**。①档本来就说了「不用等一个额外的理由」 |
| ② **世界前 20** | ⚠️ 是**名次**（`matchup[].rank`）不是种子号 |
| ③ **显式认领** | `cover._heat_why`：写清楚**刷到的人凭什么认得他** |

⚠️ **③ 才是这条闸真正的出口，不是漏洞。** 大威廉姆斯世界第 **615**、孟菲尔斯
第 **305**、迪米特洛夫第 **138**——这三条是这条线上最该做的片子，而它们一条都
过不了名次那一关。**热度不在名次里**，所以必须留一个写字的地方；代价是那句话
得自己站得住。

⚠️ **不许拿这几样当 `_heat_why`**：「他是种子」（就是被这次收紧否掉的那条）、
「数据反常」（`boulter-volynets` 那条趣闻正是被点名否掉的）、「这场球很精彩」
（那是**比赛**的形状，不是**人**的热度——本节开头那条讲的就是这个区别）。

###### ⭐⭐ 而热点长什么样：**一条正在展开的故事线**，不是名次

账号所有者 2026-08-16，紧接着上面那条：「**排名不是最重要的，要有热点，
比如格瓦林斯卡，霍达尔，伊埃拉等等**」。

⚠️ **这句话先纠正了我一个分类错误**：我上一轮把 `chwalinska-gibson` /
`bucsa-chwalinska` 放进了豁免表里「确实热度不够」那一半——**而她正是他点名的
第一个例子**。已经改过来了（补上 `_heat_why`、从豁免表里删掉）。

他点的三个人，共同点不是名次（22 / 没排名 / 20），是**每个人身上都有一条
正在展开的线**：

| | 那条线 |
|---|---|
| **赫瓦林斯卡** | 今年法网赛前世界第 114，从**资格赛一路打进决赛**（赛后进前 25）；温网首轮**在赛点上崴了脚踝**、退了几站，然后重新开始 |
| **霍达尔** | **19 岁今年才转职业**，一路杀进大师赛半决赛 |
| **伊埃拉** | 菲律宾人，华盛顿拿下**生涯第一个巡回赛冠军**，一个国家的第一人 |
| **丰塞卡** | 见下，2026-08-17 追加的第四个 |

**三种形状：突破、复出、第一人。** 写 `_heat_why` 的时候找的就是这个——
**「他正在发生什么」，不是「他排第几」**。

⚠️ **2026-08-17 这张名单加了第四个人：丰塞卡。** 账号所有者的原话：
「**丰塞卡的热度很高呀，他是 05 年之后热度很高的球员。可以放进来。**」

来路：我给 `fonseca-van-de-zandschulp` 写 `_heat_why` 的时候，是**把它当成这一条
片子的特批**在论证（巴西第一人、看台上一片黄绿色）——而账号所有者给的是一句
**常设判断**。两者的差别不在措辞，在**下一次还要不要再论证一遍**：

| | 意思 |
|---|---|
| 「这一条够热，因为这一场的看台上有巴西球迷」 | 换一场没有巴西球迷的，就又要重新论证 |
| 「**他是 05 后热度很高的球员**」 | **人本身够热，和这一场无关**，以后照抄 |

⚠️ **所以名单上这四个人（赫瓦林斯卡 / 霍达尔 / 伊埃拉 / 丰塞卡）以后写
`_heat_why` 直接引这一节**，别每次现编一条只对那一场成立的理由——现编的理由
会在下一场失效，而失效的样子是「这一条好像不够热」，然后一条该做的片子被跳过。

⚠️ **但名单不是白名单，`_heat_why` 那一栏照旧要写**：闸要的是「认领」这个动作，
而认领的内容可以是「他在这张名单上」。⚠️ 也别把名单当成**上限**——它是四个已经
点过名的例子，不是「只有这四个人算热」。

⚠️ **所以名次那一档（②）是三条里最弱的一条**，别拿它当主路：

- 名次只回答「这个人够不够格」，**回答不了「为什么是今天这一场」**
- 同一天几个候选都过得了闸时，**有故事线的那个优先**，哪怕他名次更低——
  账号所有者点的三个人名次分别是 22 / 没排名 / 20，而被他否掉的那一档
  （陶森 42、纳瓦罗 28）**名次并不更低**，差的就是这条线
- ⚠️ 反过来也成立：**一个没有故事的世界前二十，不比一个有故事的第一百更值得做**。
  「他是世界第 N」写进 `_heat_why` 是合法的，但那是**兜底的理由，不是好理由**

⚠️ **这一条没有测试，是故意的**（和「最硬的那个事实放第 ① 屏」同一个理由）：
「这条线立不立得住」是判断题，机械挡不住。能落成闸的只有「有没有认领」，
而认领里写得好不好，只能靠写之前问一句——**这个人身上正在发生的那件事，
一句话说得出来吗？** 说不出来，就说明这条今天不该做。

⚠️ **20 这个数不是从名次分布里找出来的缝。** 已发片子里「两人中的最高名次」
排开是 `3 5 7 8 10 19 20 22 23 28 30 35 36 39 42 53 54 66 92 95 96`——**一路连着，
没有任何一道干净的台阶**，拿它去找缝正是本文件「一条漂出来的分布不会自带分界线」
说的那件事。20 只是一个说得出口的代理：它把靠名次立住的那几条（佩古拉 3、
德约 5、德米纳尔 7、诺斯科娃 8、阿尼西莫娃 10、科斯蒂亚 19、伊埃拉 20）留在里面，
把账号所有者点名的那一档挡在外面。**真正的判据是「认不认得这个名字」，那一半
靠 ③ 认领，不靠这个数。**

⚠️ 已发的 25 条挂在 `_LEGACY_LOW_HEAT` 里，**只许减不许加、表自带自检**。
⚠️ **那张表里有两种东西，别混着读**：一种是**本来就够热、只是当时还没有
`_heat_why` 可写**（大威、孟菲尔斯、迪米特洛夫、德雷珀、西西帕斯、奥斯塔彭科、
伊埃拉、卡萨金娜、普利斯科娃、哈恰诺夫…），另一种才是这次说的「热度太低」
（陶森／斯特恩斯、纳瓦罗／卡利尼娜、博尔特／沃利内茨、肯宁／利斯那一档）。

⚠️ **这条不追认已发的片子**——微信消息发出去收不回来，豁免表就是为此存在的。

### ⭐「赛场之上」不用再等我开口——自己查、自己排、自己上

账号所有者 2026-08-07：「肯定要你自己去查，自己去排期，自己去安排计划。
我不用介入了，我只需要在微信看结果就」。

这条把「片子做完就合并、就推微信，不要再问一遍」（本文件开头）往前推了
一整段——那条管的是**一条片子做完之后**要不要停下来问，这条管**要不要
先等账号所有者点题**。答案是不用：找热点、定选题、写 spec、渲染、质检、
合并、推送，整条链一个人走完，账号所有者的角色收窄成「在微信里看结果」。

**「自己查」不能停在记忆或对话里的印象上。** 这条规矩刚定的当天我自己就
栽了一次：脱口而出「DC 站 8/3 结束、辛辛那提 8/11 才开，这周没有大赛」，
没查就下了结论——而**国家银行公开赛（多伦多女子 / 蒙特利尔男子）正在打**，
8/7 当天就是第三轮。选题第一步永远是**先查当天真实赛程赛果**，不是拿
对话里几天前的印象往下推。

配套两条边界，不因为这条授权而松动：

- ~~ATP 源片仍然卡在人工供片，这条线目前还是只能做 WTA~~ **2026-08-10 已撤销**，
  见下一节「ATP 也能查了——YouTube 优先」
- **闸拦住了、口径选择这两种，仍然要停。** `check_reel_landed` 报不合格、
  成片链接探不到、质检 FATAL——照旧当场修；改法有几个方向都说得通、
  选哪个取决于账号所有者想要什么（换封面版式、要不要倒叙这类）——
  照旧要问。这条授权免掉的是「找不找热点」「推不推微信」这类**只有一个
  说得通答案**的确认，不是免掉判断力

### ⭐ ATP 也能查了——YouTube 优先，WTA 同理要挑 1080p 那版

账号所有者 2026-08-10：先纠正了我的一句话——我说「ATP 源片卡在 Tennis TV，
拿不到」，他直接顶回来「**atp 在 youtube 也有啊**」。查仓库才发现自己说错了：
`medvedev-zandschulp.json` / `tirante-fritz.json` 用的是 **ATP 官方 YouTube 频道**，
`fonseca-ruud.json` / `fritz-jodar-final.json` 用的是 **Tennis TV 的 YouTube 频道**
——四条 ATP 片子早就这么做过，上一节那句「只能做 WTA」是一句没查仓库就说出口的
过期结论，而不是真实限制。账号所有者随后定了两条：「**那把这条路打通，后续都
优先 YouTube**」（ATP）、「**wta 也要优先用 YouTube 1080p 的视频**」（WTA）。

#### ATP：YouTube 优先，Tennis TV 订阅制那条路降级为兜底

**判据不是「猜是官方的」，是画面自己证明的。** `shelton-fonseca` 这条片子
（蒙特利尔 ATP1000 1/8决赛，谢尔顿胜丰塞卡）走完整条流程验证过，四处自证叠在一起：

- **候选帧墙上烧着「Tennis TV」台标**——`pick_cover_frames.py` 出的 11 张候选，
  张张右上角都有
- **场地印着 `MONTREAL`**，场边广告板 `Banque Nationale`/`Rogers`/`IGA`/`CGI`——
  National Bank Open 的赞助商阵容，和赛事对得上
- **记分条逐格读过、和 flashscore 逐分记录一分不差**：`FONSECA`/`SHELTON`
  的比分从 0-40（0.5s）一路到抢七 6-3（224.5s，丰塞卡救下赛点），每一个节点
  都能在 flashscore（KWAEU167）的逐分数据里找到同一个比分
- **自动字幕结尾『Ben Shelton wins the battle of the big hitters. He's into
  the quarterfinals in Montreal.』和真实赛果吻合；脚踝事件那段字幕『Is the
  ankle okay though? / No.』和 ATP 官方赛后稿『second set at 3-1, Fonseca
  appeared to tweak his ankle while jumping to chase down an overhead』
  对得上同一时刻**

四条线全对得上，**这条路走得通**。所以 ATP 现在的顺序是：

1. **ATP 官方 YouTube 频道**（`Highlights | <赛事> <轮次>` 这类标题）
2. **Tennis TV 官方 YouTube 频道**（`EPIC ... Battle` / `Extended Highlights`
   这类标题）
3. Tennis TV 订阅制那条路（网盘转存）退成**两条 YouTube 都没有时**的兜底，
   不再是唯一路径

⚠️ **`_source` 字段仍然要把自证的四处都写出来**，不能因为「ATP 现在能查了」
就退回「看着像官方的就当官方的」——这条仓库反复栽过的坑（大威那条转载图被
调过色、洛斯卡沃斯那次官网图是效果图）说的都是同一件事：**授权和来源不是
靠感觉判的，是靠画面自己证明的**。

##### ⭐⭐ 2026-08-16 补上第三档：Tennis TV 的**短集锦是免费的**，YouTube 缺场时用它

账号所有者：「**ATP 的赛场之上 highlight 可以去 tennistv 找啊**」，随后定了顺序：
「**优先级就是 YouTube 找不到就找 Tennis TV 的 short highlight**」。

`docs/atp-highlights-source.md` 2026-08-09 那版写着「单场集锦 `premium`——要订阅」，
据此把 ATP 判成只能靠 YouTube 碰运气。**那句话对它查的那一档是真的，而它把一档
写成了全部**：Tennis TV 的单场集锦挂在**两个平行的 library 版块**下——

| 版块 | slug | entitlement | 时长 | 抽样 |
|---|---|---|---|---|
| `library/match-highlights` | `…-highlights` | **premium** | 全长 | 2/2 |
| **`library/short-highlights`** | 同名带 **`-short-highlights`** | **free** | **2 分半** | **20/20** |

⚠️ **两档的 slug 只差六个字母，页面长得一模一样**，只有 `data-entitlement`
那一个属性把它们分开。上一版**只打开了一个版块**——这是「查空一类不等于查空全部」
的又一次，只不过要自证的不是空不空，是**空的范围有多大**；同一个形状
（「查了一场就写成了一类」）这个仓库已经栽过三次，这是第四次。

实测（辛辛那提 2026 R1/R2 那一页，20 条全查过）：**20/20 free、h264+aac**，
和付费全长档**一一对应**——没有一场只有付费档。零 token，
`fetch_tennistv_video_metadata` 早就写好了这条路。

⚠️⚠️ **分辨率和帧率按条算，不是这一档的属性。** 我第一版写的是
「1920×1080 / 30 fps」——**拿一条推的**。抽样 8 条逐条列格式表：**5 条 1080p、
3 条只有 720p**（`shang-sonego` `marozsan-zheng` `atmane-etcheverry`）；
帧率 `paul-hurkacz` 30 fps、`shang-sonego` 25 fps。**这是我在同一份文档里
第二次犯「查了一场就写成了一类」**——而那份文档整个上半段讲的就是这个错。
影响不小：3:4 竖版窗口从 1080p 取只要放大 1.33 倍，从 720p 取要 **2.0 倍**。
**选源之前先 `yt-dlp --list-formats` 跑一次**，别照抄任何一个数。

⚠️ **manifest 上的令牌只活 60 秒**（`exp - iat = 60`，`customerId` 是
`anonymous`）。所以**解析必须在下载那一刻做**，spec 里放**页面地址**——
钉进去就是一条一分钟后必死的链接，而它失败的样子和「这条片子被下架了」
一模一样。⚠️ 但 60 秒**不是下载的时限**：实测整条 154 秒、74.6 MB 一趟下完没事。

⚠️ **顺序是 YouTube 在前**（账号所有者定的），而它管的是**去哪儿找**，
**不是「YouTube 版更完整」**。短集锦顶上来的是**覆盖**——ATP 的 YouTube 覆盖
本来就不稳，而短集锦每场都有。

##### ⚠️⚠️ 2026-08-18：我把这条规矩**引用在 spec 里，然后没有照它做**

账号所有者：「**YouTube tennistv 频道有更长的 atp 比赛视频**」。

对的，而且**这句话上面那一节已经写着了**（2026-08-16 他自己说的「YouTube tennis tv 的
视频更长，建议优先选这个渠道啊」）。我在 `zverev-atmane` 的 `_source` 里**一字不差地引了
这句话**，然后用的是 tennistv.com 那一档**定长两分半的短集锦**。

**错的不是结论，是那张比较表比错了对象：**

| spec 里写的 | 实际漏掉的 |
|---|---|
| `zverev-atmane`：ATP Tour `AYjXOxqt9VI` 120.0s vs Tennis TV 自家站 156.8s | **Tennis TV YouTube `QVoxFvZW5G4` 一次都没量** |
| `jodar-tabilo`：ATP Tour 134.4s vs Tennis TV 自家站 156.4s | **Tennis TV YouTube `vb77wEtk8Ys` 一次都没量** |

**表里两行都是真的，而少的那一行才是排第一的那条渠道**——读起来像「三条都量过了」，
其实主路没量。这跟本文件「编出来的判据 + 正确的结论」是一家的：**一张列了两行的表
比一张没有表的更可信，而它可以是漏的。**

⚠️ **量得出来的旁证摆在同一天的产物里**：同一批 Tennis TV YouTube 的单场版，
`cobolli-blockx` 是 **224.0s**、`fery-deminaur` 是 **220.5s**；而 tennistv.com 短集锦这一档
**是定长的**（154.9 / 155.0 / 156.4 / 156.8 / 149.0）。**两档差一分钟以上**，
而我手上已经有两个 220s 的样本了。

⚠️⚠️ **但「YouTube 一定更长」也是错的，我手上同样有反例**：
`tirante-landaluce` 的 Tennis TV YouTube `qy9gV57Zc9I` 只有 **132 秒**，比短集锦那一档
**还短 23 秒**（而且地域封锁）。所以判据不是「哪个渠道更长」，是——

> **两条都量一次，取长的那条。** 渠道优先级只决定**先查哪条**，不决定用哪条。

**落成动作**（写 `_source` 之前必须做完）：

    ① Tennis TV YouTube 单场版      ← 主路，先查
    ② tennistv.com short-highlights ← 定长 ~155s，稳定有
    ③ 赛事自己的官方频道 / ATP Tour  ← 备选
    比较表里**这三行都要有数**；没量的那一行写「没查」，**不许省略**。

⚠️ **省略和「查了没有」在表上长得一模一样**——这正是本文件反复记的那个形状，
只是这次省略的是**排第一的那条**。

###### ⚠️ 而「① 没有单场」是常态，不是异常——**合集不算一条源**

照上面那三步把 Tennis TV 频道最近 30 条整份列出来量了一遍（`ytInitialData` 的
`lockupViewModel` 里 `contentId` 和时长同属一个节点，一次全给，别逐条打 yt-dlp——
那会在第 42 条撞 429），辛辛那提**第三轮那一整天**只发了**五条**单场：

    兹维列夫-阿特曼 3:08 ｜ 霍达尔-塔比洛 3:58 ｜ 费里-德米纳尔 3:41
    科博利-布洛克斯 3:44 ｜ 蒂兰特-兰达卢塞 2:12

**同一天的莱赫奇卡-菲尔斯、保罗-巴列霍、土方-门西克一条都没有**——它们只出现在
**Day 5 合集**（`uC2DlHNYHRA`，12:03，五场混剪）里。也就是说「① 那一行是空的」
是这条渠道的常态，**别把它读成「我没查到」**。

⚠️⚠️ **合集不是第四条渠道，它根本不能用**：12 分钟摊到五场球上，单场留下的比
tennistv.com 那档 ~155 秒的专场短集锦**还少**，而且中间夹着别人的比赛，
**切不出一段连续窗口**。所以撞上「只有合集」时，正确动作是**用 ② 的短集锦**，
并在 `_source` 里把这一行写成「**没有单场，只有 Day N 合集**」——
写「没扫到」会让下一个人再扫一遍，写「只有合集」他就知道不用扫了。

###### ⚠️⚠️ 「最长」不等于「可用画面最多」——多出来的那一截常常是采访和片尾板

2026-08-18 把两条从短集锦换到 Tennis TV YouTube 单场版，账面上「净赚」的时长
**大半不是比赛画面**（都是逐格看出来的，不是推的）：

| | 源片全长 | 比赛画面到 | 尾巴是什么 |
|---|---|---|---|
| `jodar-tabilo` `vb77wEtk8Ys` | 237.80s | **182.32** | **场上采访 45 秒**（ATP1000 话筒＋`CONGRATULATIONS` 背景板）＋ 片尾板 10 秒 |
| `zverev-atmane` `QVoxFvZW5G4` | 187.76s | **177.74** | 片尾板 10 秒（无采访） |

也就是说 `jodar-tabilo` 账面多 81.4 秒，**真正多出来的可用画面只有约 33 秒**。

⚠️ **采访那一段一帧都不能进**——「赛场之上视频不要赛后采访」那条管的是画面，
不只是原声。而它在时长表上完全看不出来：两条都是「YouTube 版更长」。

⚠️ **片尾板的起点就是最后一个 `scene_cut`**，`probe.json` 里现成的（这两条分别是
182.32 和 177.74）。**别拿「离源片末尾还有 N 秒」当余量**——本文件早记过，
那 N 秒往往正好就是片尾板本身。

⚠️ **卡在那一刀前面要留半秒，别留几帧。** `zverev-atmane` 第一版末段收在 177.5，
加 `SEG_FADE` 0.18 是 177.68，离 177.74 **只剩 3 帧**；`tirante-landaluce` 同样
「卡在切点前」那次，成片末尾仍然留了一帧黑闪，真因是抽帧读时刻有相位误差
（`-vf fps=25` 是重采样滤镜，换个 `-ss` 同一帧能读出两个时刻）。

**所以选源那一步的判据要改一个字**：不是「取最长的那条」，是
**「取可用画面最长的那条」**——量完全长，再逐格找到比赛画面的终点，用那个数去比。

##### ⚠️⚠️ 「赛事自己的官方频道」是**第二条备选**，而它的 ATP 版反而更短

账号所有者 2026-08-16：「**还有每个赛事自己官方的 youtube 频道有时也会有集锦，
比如 cincinnati open**」，随后定了位置：「**这个可以做备选方案**」。所以现在是

    主路   ATP Tour / Tennis TV 官方 YouTube 频道
    备选   赛事自己的官方频道  ／  Tennis TV library/short-highlights
    都没有 → 报「这场没源」跳过

⚠️ **两条备选之间挑哪个，量了再说，别按写的先后。** 真去量了 `@CincyProTennis`
（oEmbed 的 `author_name` 是 `Cincinnati Open`，权威），辛辛那提 R2 那三场 ATP
**两边都有**，正好直接比：

| 这一场 | 赛事官方频道 | Tennis TV 短集锦 | |
|---|---|---|---|
| Paul vs Hurkacz | 130s | **154s** | TTV **+24s** |
| Landaluce vs Arnaldi | 129s | **156s** | **+27s** |
| Kecmanovic vs Cobolli | 121s | **152s** | **+31s** |

同一个频道**两个巡回赛还差一档**：ATP **121~130s**、WTA **178~185s**——
也就是它对 **WTA 是全场最长的一版**，对 **ATP 是最短的**（抽样 ATP 3、WTA 5，一页）。

**所以「排在前面」不等于「更完整」，找到之后还要量一次**——「同一场有两版时挑长的
那一版」那条照旧管用，而这次它真的咬人了。判据是那条老命令
（`--print '%(duration)s %(height)s %(channel)s'`），⚠️ 记得带 `--js-runtimes node`。

赛事频道这一档的四个坑（都是实测，别按标题模式硬解析）：
**它 ATP+WTA 都收**（CLAUDE.md 原来标成「WTA 这一站」，错的）、
**同一场会出现两次**（`Ostapenko vs Frech` 两个 videoId，按标题去重会留两条）、
**标题格式不可靠**（同页就有一条 `Shuai Zhang vs Kayla Day match highlights`，
没有 `| 赛事 | 轮次` 后缀，严格解析会漏掉它而**漏掉的样子就是「这场没有」**）、
**混着非比赛条目**（`Cincy Serves Honoree` 61s，判据是标题里有没有 ` vs `）。

⚠️ **量时长别逐条打 yt-dlp**：实测连打 42 次就吃 **HTTP 429**，之后每条都报
`Sign in to confirm you're not a bot`——**和「这些视频不存在」长得一模一样**
（14/14 全空是系统性的，不是间歇）。频道页 HTML 里的 `ytInitialData` 一次全给：
每条视频是一个 `lockupViewModel`，**`contentId` 和时长同属这一个节点**——
按结构取，不是按字符距离猜（那个坑本文件早记过）。权威标题走 oEmbed，没被限流打到。

##### ⭐⭐ 2026-08-16 账号所有者把顺序定死了：**Tennis TV 排第一**，而封锁是**按视频算的**

原话：「**YouTube tennis tv 的视频更长，建议优先选这个渠道啊**」。上面那两节把
Tennis TV 的 YouTube 版和赛事官方频道并列成了「主路 / 备选」，**而没有量过谁更长**。
真去量（辛辛那提第二轮四条，同一批）：

| 这一场 | Tennis TV YouTube | 别的渠道 | |
|---|---|---|---|
| 西西帕斯-阿利亚西姆 | **236.7s / 1920×1080** | ATP 官方 241.7s / **1280×720** | 短 5 秒，但**分辨率高一整档** |
| 丰塞卡-范德赞舒尔普 | **136.4s / 1080p** | ATP / 赛事频道都没有 | 只有它 |
| 弗里茨-米切尔森 | **195.1s / 1080p** | —— | |
| 阿尔特迈尔-穆塞蒂 | 154s（**下不下来**，见下） | 赛事频道 126.5s / 1080p / 60fps | 退回赛事频道 |

⚠️ **第一行是这条规矩真正咬人的地方**：ATP 版**更长**（+5 秒）却**更糊**。
3:4 竖版取景窗要 810px 宽——从 1920 里取只放大 **1.33 倍**，从 1280 里取要 **2.0 倍**，
而 CLAUDE.md 早就写过「1.78 倍已经被判太软」。**「更长」和「更清楚」冲突时选清楚的**，
账号所有者那句话点的是渠道，不是「一律挑最长的那版」。

⚠️⚠️ **地域封锁是按视频算的，不是按频道算的。** 阿尔特迈尔那条在 runner 上
八档 client 全报

    ERROR: [youtube] jh03pTd-HO8: The uploader has not made this video available in your country

而**同一时间、同一台 runner、同一个频道**的另外三条全下得动。所以：

| 报的是什么 | 是什么 | 怎么办 |
|---|---|---|
| `The uploader has not made this video available in your country` | **这一条视频**的地域封锁 | 换渠道，**别动别的片子** |
| `Sign in to confirm you're not a bot` | **这台机器的 IP** 被挡 | 配 cookies，和视频无关 |

**两句话长得都像「下不下来」，处置完全相反。** 我上一轮就是拿两次失败推出
「Tennis TV 别再发了」——账号所有者当场纠正。这跟本文件里「查了一场就写成了一类」
是同一个形状，只不过这次被写成一类的是**一个频道**。

**代码这一头修的是「出路写出来了，代码自己不走」。** `_reject_signed_source_urls`
的报错正文里早写着「Tennis TV 库里标着 `free` 的条目」这条出路，而
`build_match_reel.download()` 从来不解析 Tennis TV 页面地址——直接喂给 yt-dlp
会报 `This video is only available for registered users`，**一句指向「cookie
过期了」的错，而真相是这条路根本没接上**。采访线 2026-08-05 就接了，出片线
一直没有。这跟 `build_match_reel.py` 1192 行记的那次是同一个形状。

- **解析只有一份**：`official.media_url()`，两条线各包一层只翻译异常类型
  （出片线 `ReelError`、采访线 `SystemExit`）。写两处必分叉，而**分叉的样子是
  「采访线能下、出片线报『注册用户才能看』」**——两个入口各试一次才看得出来，
  而没有人会为一条已经通了的线再试一次
- ⚠️ **缓存键必须还用页面地址**，别就地把 `url` 覆盖成解出来的那个：令牌 60 秒
  一换，拿它当键**每趟都是新键、缓存永远不命中**（那一百秒的重下白付），
  还会把缓存目录撑满，**而那表现为「今天 CI 怎么慢了点」**
- ⚠️ **`.m3u8` 不走 curl 那条直链**：它不是文件，是播放列表，curl 下回来几 KB
  文本必然过不了 ffprobe，白挨一次请求还打印一句指错方向的「直链下到 0.0 MB」。
  判的是**这东西本身是不是播放列表**，不是维护「哪些站要交给 yt-dlp」的域名名单
- 四条判据都反向验证过，**扫「有没有自己再抄一份解析」要用 AST**：
  `build_match_reel.py` 的报错正文里正写着那个函数名，按文本扫会把「把出路写清楚」
  判成「又抄了一份」

#### WTA：同样要挑 1080p，别退而求其次

`eala-championship` 那条早就踩过一次：wtatennis.com 站内嵌入播放器
（Brightcove）只给到 **720p**，同一场夺冠集锦的 **YouTube 版是 1080p**、
还多出颁奖和捧杯致辞的画面（站内版剪得短，晚一小时才发的 YouTube 版更完整）。
`crop_zoom` 那条规矩算过账：1080p 取 3:4 竖版窗口只需放大 1.33 倍，720p 要
放大 2.0 倍——同一个镜头，分辨率差一档，最终画质差一大截。

所以 WTA 这条线的顺序现在明确写成：

1. **WTA 官方 YouTube 频道的单场集锦**，选源片时**先看 `%(height)s`**，
   同一场比赛如果站内页和 YouTube 都有，**挑分辨率更高的那个**，不要因为
   「先搜到的是站内版」就将就
2. 站内嵌入播放器（Brightcove 等）退成 YouTube 拿不到时的兜底

⚠️ **两条规矩合起来的判据是同一句话：源片的清晰度和完整度不能将就，
分辨率越高、剪辑越完整的那个版本优先**——不分 ATP 还是 WTA。

##### ⚠️ 2026-08-16 收紧：**宁可等 1080p，也别现在就用 720p**

账号所有者：「**最好等 1080 出来再做，除非后面确实没有了再选 720 的**」。

上面那条说的是「两个版本都在手上时挑高的那个」，而真正常见的情形是**这一刻只有
720p**——WTA 官方 YouTube 频道发单场集锦有先有后，站内 Brightcove 那一版往往先
上线。`krejcikova-bejlek` 就是这么走的 720p：查的时候 YouTube 上没有这一场，
于是用了站内播放器那一版（这个资产的最高档就是 720p）。

**现在的口径是：**

| 情形 | 怎么办 |
|---|---|
| YouTube 还没发，站内有 720p | **等**。这条线的触发是周期性的，下一轮再来看 |
| 等过一轮（几小时）仍然只有 720p | 才用 720p，并在 `_source` 里写清**等过、当时确实没有** |
| 同一场两个版本都在 | 挑分辨率高的那个（上一条） |

⚠️ **判据是「查过 YouTube 频道、当时没有这一场」，不是「我先搜到的是站内版」**
——两者在 `_source` 里长得一模一样，而前者是等过的、后者是没找。

⚠️ 720p 的代价是实的：3:4 竖版窗口从 1080p 取只放大 1.33 倍，从 720p 取要
**2.0 倍**；封面那一头更狠——1080p 抽帧铺 1080×1440 是 1.33 倍，720p 是 2.0 倍，
两个都过不了「封面一律用官方高清实拍」那道闸，但 720p 连退路都更差。

##### ⭐⭐ 2026-08-18 定死：**1080p 及以上是硬门槛，没有就等**

账号所有者：「**视频一定要选 1080p 及以上的清晰度，如果没有的话就等**」。

上一条（2026-08-16）说的是「等过一轮仍然只有 720p，**才**用 720p」——也就是
720p 还留着一个出口。这一条把那个出口关掉了，而且**不分巡回赛、不分渠道**：

| 源片最高档 | 怎么办 |
|---|---|
| **≥ 1920×1080** | ✅ 做 |
| 1280×720 或更低 | ❌ **不做，等**。下一轮再扫；等到了再做 |

⚠️ **判据是 `probe.json` 的 `height`，不是搜索结果页的标签**。挑源片的时候先跑

    yt-dlp --skip-download --print '%(duration)s %(height)s %(channel)s' <url>

拿到的 `height` 要 ≥ 1080；tennistv.com 那一档要看解出来的 manifest 里最高那一档
（`tools/tnns_stats.py` 旁边那条 `official.media_url()` 的路子，CLAUDE.md
「Tennis TV 短集锦」那节记着**分辨率按条算，不是这一档的属性**——同一批里就有
5 条 1080p、3 条只有 720p）。

⚠️ **「等」不是「跳过」。** 官方集锦发得有先有后，同一场往往几小时后就有 1080p 的
版本上线；这条线的触发是周期性的，下一轮回来重扫就行。写 `_source` 的时候要说清
**这一轮扫了哪几个渠道、各是什么分辨率**，别写成「这场没有源」。

⚠️ 代价是实的，这也是这条为什么值得等：3:4 竖版窗口从 1080p 取只放大 **1.33 倍**，
从 720p 取要 **2.0 倍**，而 1.78 倍这个仓库早就判过「太软」。

#### ⭐ 一天的候选怎么扫出来：四条命令，别每次现搓

账号所有者 2026-08-15：「**这些要记录下来啊，下次直接用**」。整套是纯 HTTP，
沙箱里跑得动，全程不到两分钟。

**① 今天/昨天打完了哪些场**（flashscore 当日总表，ATP+WTA+挑战赛一次全给）：

    curl -s "https://global.flashscore.ninja/2/x/feed/f_2_<offset>_0_en_1" \
      -H "x-fsign: SW9D1eZo" -H "Referer: https://www.flashscore.com/"
    # offset: 0=今天 -1=昨天 1=明天；按 ¬ 拆字段、按 ~ 拆场次
    # AA=比赛id  AE/AF=两个人  BA/BB BC/BD BE/BF=分盘比分  AC=轮次码

⚠️ **认赢家只读单场的分盘字段或 WTA 的 `ResultString`**，别读当日总表的
`AG`/`AH`——那一层栽过一次（`boulter-volynets` 的赢家被我读反了）。
⚠️ 扫中国球员按 `Zheng Q` / `Wang Xin` / `Wang Xiy` / `Zhang S` / `Yuan Y` /
`Shang J` / `Bu Y` 这些前缀，flashscore 写的是「姓 + 名首字母」。
⚠️ **`Zheng M.` 是美国人 Michael Zheng**（`FV÷USA`），不是中国球员——按名字扫会误收。

**② 哪几场有官方单场集锦**（三个频道的 `/videos` 页，纯 curl）：

    curl -s "https://www.youtube.com/@atptour/videos"        -H "User-Agent: Mozilla/5.0 …"
    curl -s "https://www.youtube.com/@TennisTV/videos"        -H "User-Agent: Mozilla/5.0 …"
    curl -s "https://www.youtube.com/@CincyProTennis/videos"  # ⚠️ 赛事官方频道，**ATP+WTA 都收**
    # 正则 "videoId":"([\w-]{11})" 取前 20~30 个（页面按时间倒序）

⚠️ **第三行原来注着「WTA 这一站是赛事官方频道」——2026-08-16 量出来是错的**：
同一页里 ATP 3 条、WTA 10 条。**赛事自己的官方频道是一档独立的源**
（账号所有者：「还有每个赛事自己官方的 youtube 频道有时也会有集锦，
比如 cincinnati open」），每站一个，**扫的时候要按当周的站去找它的频道**，
不是只认这三个写死的名字。

⚠️ **标题不在 `videoId` 旁边**，别按字符距离去凑（那个坑见上面「别拿频道列表页的
HTML 去猜时长」）。逐个走 **oEmbed** 拿权威的标题和频道名：

    https://www.youtube.com/oembed?url=<urlencoded watch url>&format=json
    → {"title": "...", "author_name": "ATP Tour"}

`author_name` 就是「这条是不是官方发的」的判据——**搬运号一律不用**。

**③ 同一场有两版时挑哪个**：`yt-dlp --skip-download --print '%(duration)s %(height)s %(channel)s'`，
见上面那一节。实测 ATP 官方的单场集锦普遍两分钟上下，Tennis TV 的是三到八分钟——
**但每次都要量，别照抄这句**。

**③ᵇ 主路（ATP Tour / Tennis TV 频道）没扫到的场次** → 两条备选：先看**这一站
自己的官方频道**（每站一个，按当周的站去找），再看 **Tennis TV 短集锦**：

    curl -sS -H "User-Agent: tennislive/0.1" https://www.tennistv.com/library/short-highlights \
      | grep -o '/videos/[0-9]*/[a-z0-9-]*short-highlights' | sort -u

免费档、1080p、每场都有，spec 的 `source_url` 直接写这个**页面地址**
（解析在下载那一刻做）。⚠️ 顺序是 **YouTube 在前**，而且这一档是**定长 2 分半的
剪短版**——详见上面「2026-08-16 补上第三档」和 `docs/atp-highlights-source.md`。

**④ 去重**：`ls specs/reels/ | grep -i <姓>`。⚠️ **按对阵双方的姓各查一遍**，
文件名的先后顺序不固定（`wang-vandewinkel` 就是这么查出来「已经做过」的）。

⚠️ **扫出来的候选还要过上面那道「没话题、排名又不高的不做」的闸**——
「有官方集锦」只说明素材够得着，不说明它值得做。

### 这是一个评论号：要有情感，要有立场

账号所有者的原话（2026-08-01）：

> 「我们本来就是一个评论的。视频本来就要有情感、有立场，这样才能有引起很多人的
> 共鸣。如果不谈情感、不谈立场，那就没有意义了。我这立场不是坏的立场，
> 是基于情感的立场。」

**记在这儿是因为这条被反着写过一次。** 2026-07-28 有个会话看到三条前瞻的结尾都挂着
「比分我们不猜」，就自己推出一条硬规矩「不要在片子里交代自己的规矩」，还配了测试去拦
「不猜／不预测／我们不／本栏目」这些词——**没有人要求过它**。账号所有者看到时的反应是
「我没有要求这一条，把它全部拿掉」。规矩和测试都删了，这一节是它留下的空位。

所以：**片子里可以有态度，可以说"我们"，可以替读者把那句话说出来。** 那不是"交代
自己的规矩"，那就是评论本身。会犯错的方向不是"有立场"，是下面这两个：

- **立场要站在事实上**——见下一节那条「第一是精准，第二是引爆」。它管的是"这句话
  能不能被信源顶住"，从来不管"能不能有态度"。一句站得住的重话，比十句不敢说的轻话有用
- **别把编辑流程当成内容**。「本栏目按惯例不预测比分」这种句子读者确实不关心——但
  它的毛病是**在讲我们的工作流程**，不是它带了立场。「我们等她赢回来」带立场，
  而且是内容

`Column.promise` 里的「不预测结果」仍然留着：那是在说这个栏目**是什么**（讲来路，
不写赛果预测），不是在禁止片子里出现态度。

### 文案的两条要求，有先后

账号所有者 2026-08-01 补的一句，是这两条要求的完整版：

> 「文案一定要有**感染力**和**吸引人继续读下去**的效果，同时能够**精确传达出信息**。」

「吸引人继续读下去」是这句话里新的那一维，也是最容易漏的：一段话每句都精准、
每句都有劲，仍然可能是一张**事实清单**——读者读完第三条就走了，因为没有任何一句
在把他往下一句推。判据很简单：**每一段收在哪儿？** 收在一条事实的末尾就是清单，
收在一个让人想知道「然后呢」的落点上才是文案。

反例（重写前的第 ⑤ 段）和正例（重写后），同一批事实：

| | |
|---|---|
| ❌ 清单 | 「七月的雅典，她连赢两场，两个月来第一次。八强负于克雷奇科娃……三天前华盛顿首轮，四比六、六比四、一比六输给伊埃拉。」句号，完 |
| ✅ 有落点 | 同样这些，末尾加一句「**所以才有了加拿大站的资格赛。**」——把前面所有事实接回读者正在看的这一场 |

**第一是精准，第二是引爆。顺序不能倒。**

一句煽得起来但站不住的话，比一句平淡的真话糟得多——它会被人转出去，然后被人拆穿，
赔上的是整个号的可信度。所以每一句写完先问"这句能不能被信源顶住"，再问"它够不够劲"。

两者常常可以兼得，而且**兼得的那一句往往就藏在事实里**：

- 「温网肯付女人一样的钱，是维纳斯争来的」——**煽，但过头了**。她是决定性的推手（2005 年
  决赛前夜陈情、2006 年《泰晤士报》檄文），可"是她争来的"把几十年的集体抗争算成了一个人的功劳
- 「第一张等额冠军支票，是维纳斯的」——**同样有劲，而且完全站得住**：2007 年温网宣布同酬，
  那年冠军正是她，70 万英镑，和第二天费德勒拿到的一模一样

后一句更抓人，因为它是**具体的、可核的、带画面的**。煽情不是靠形容词堆出来的，
是靠把最硬的那个事实摆到最前面。

同类的踩点：旁白里写过"一整个巡回赛的女子都在拿的那份等额奖金"——同酬是大满贯的事，
巡回赛整体并非男女同酬，一句顺口的概括就把范围放大了。**越是想让人转发的句子，越要收紧范围。**

### 最硬的那个事实放第 ① 屏，不要放在铺垫后面

三个平台的后台数据算出同一件事：**看到结尾的人只有 2–3%**（抖音真完播 2.0%、
视频号 2.9%；小红书不给这一列）。也就是说末屏那一问、收尾屏必须在球场上、
字幕不越漂越多——花最大力气打磨的东西，绝大多数人根本走不到。

所以顺序比长度重要得多。`masters-format` 原来是六屏：

    ① 老规矩   大师赛原本一周就打完        ← 铺垫
    ② 扩容     签表从 56 人扩到 96 人      ← 这才是「为什么变成两周」的答案
    ③ 身体先垮 5-1 领先，然后辛纳抽筋了    ← 全片最有画面感的一段
    ⑤ 谁的赛程 更长的赛事，到底给谁看      ← 收尾那一问，没人走到

改成三屏（答案连同示意图提到 ①、辛纳提到 ②、账单和那一问放 ③）之后，同样的
注意力覆盖到了全片。**注意这里真正起作用的是换顺序，不是砍字数**——当时顺手把
705 字砍到 349，一度让人（包括我）以为功劳在「变短」。一条三分钟、答案在 ① 屏的
片子，比一条 90 秒、答案在 ③ 屏的片子，对那 97% 的人更有用。

**字数不设上限**：有些事情限定字数就讲不清楚，讲清楚永远优先。要卡的只有位置。

写稿时的自查：

- 第 ① 屏能不能单独回答标题里那个问题？只答了一半也行，不能一点不答。
- 铺垫（「原本是什么样」「先把时间拨回」）能不能压成一句话并进封面或 ①？
- 最有画面感的那一段在第几屏？在 ③ 之后就往前挪。

**这条没有测试，是故意的。** 顺序是编辑判断，机械挡不住——测试没法知道哪句
才是答案。硬凑一个判据（比如「① 屏必须含数字」）会被绕过，还会制造「已经守住
了」的错觉，比没有更糟。

**仓库里「规则要落成测试」那条有个边界：判断题写不成测试。** 真正落成测试的
只有一条相关的——封面第一句 ≤ 27 字（`HOOK_BUDGET`），因为它问的不是「讲没讲
清楚」而是「有没有讲完一句话」：抖音 5 秒内走掉 62%，第一句在第 5 秒还没说完，
那 62% 的人听到的就是半截。

另：`test_explainer_budget.py` 里的 `SANITY_MAX_CHARS = 5000` 不是编辑上限，
是防拼接出错的兜底（模板或循环写错能拼出几千字，TTS 会跑十几分钟）。别把它
当成「可以写到 5000 字」的许可，也别把它收紧成编辑判断。

### 不要用假设句，用真实的例子

账号所有者 2026-08-03：「**不要用写这些假设，普通人看不懂，就用实际举例。**」

我给雨延期那条选题写了一张「如果决赛里有个低排名球员会怎样」的推演表，
五行全是「他……则条文……」。**那是写给自己看的推导，不是写给读者看的内容**——
读者要理解它，得先在脑子里造一个不存在的人，再把四条规则套上去，
而这条片子只有几秒钟抓住他。

**判据很简单：片子里出现的每个人都必须是真的，每件事都必须真发生过。**
推演可以留在调研文档里当思路，但不许上屏。

而且**换掉之后往往更好**，因为真人自带细节和情绪。同一批规则，
假设表里那四行分别对应到：

| 假设那一行 | 换成谁 |
|---|---|
| 「他排名不够直入，得打资格赛」 | 卡佐：2025 年 7 月刚重回前 100 |
| 「他还在上一站打，所以够不着资格赛」 | 布云朝克特：上海的外卡被改成特殊豁免，理由白纸黑字写着「他在北京还没打完」 |
| 「拿到豁免之后呢」 | 卡佐：以豁免进正赛，连胜三场打进生涯首个巡回赛决赛 |
| 「这对他意味着什么」 | 德朱姆胡尔：九连胜进世界前 100，波黑史上第一人 |

这条和「旁白不解说画面」是一家的：那条禁的是**复读观众已经看见的**，
这条禁的是**要求观众先想象一个不存在的**。两头都是在替读者省那一步。

### 不要平白叙事——情绪是结构做出来的，不是形容词堆出来的

账号所有者的原话：「文案和视频的剪辑**一定不要平白地叙事**，要有饱满情感激起观众的
共鸣和代入感，能从开头持续看下去的**沉浸感**，同时能**引爆**人们传播」。

平白叙事长这样，事实全对，一个字都不想看：

> 17 岁的克鲁兹·休伊特从资格赛打进正赛，首轮击败吉隆，次轮不敌德米纳尔。

毛病是**它把结果一次性倒完了**，读者没有任何理由看第二句。

**「饱满情感」不能靠加形容词，要靠排列顺序。** 方法就一条：**每一拍留一个问题，
下一拍回答它**，同一条故事拆成一串钩子。休伊特那条：

| 拍 | 说什么 | 留下的问题 |
|---|---|---|
| 1 | 他姓休伊特。这个姓能换一张外卡——**他没要** | 那他怎么进来的？ |
| 2 | 资格赛一轮轮打进来，首轮 74 分钟拿下生涯第一场正赛胜利，17 岁 | 下一轮呢？ |
| 3 | 下一轮是德米纳尔——卫冕冠军，**也是他父亲带出来的人** | 这怎么打？ |
| 4 | 克鲁兹自己的话：他像我哥哥 | 那结果呢？ |
| 5 | 61 分钟，6-2 6-3。差距是真的 | **片子不能停在这儿** |
| 6 | 网前那个长拥抱。看台上，父亲看着 | —— |

**第 5 拍是分水岭**：停在比分上，这就是一篇赛报；走到第 6 拍，情绪才落地。

三条落成了判据：

- **收尾要落在一问上，不能停在数据上**（`test_收尾要落在一问上不能停在数据上`）。
  数据收尾读者读完就走；一问才换得来评论，而评论是这类短片唯一能换到的传播。
  七条里六条本来就这么做，那一条失手的是 `wong-lehecka`：「排名差九十六位，
  生涯排名最高胜。」
- **旁白不许用指示语指画面**（`test_旁白不许用指示语指画面`）。⚠️ 拦的是
  「画面里是」「镜头里」「图为」这类**指示语**，**不是「描述动作」**——这两件事
  我一度混为一谈，还把「球落地，他放下球拍，双手掩面」判成违规。账号所有者
  纠正：「解说匹配画面是必要的……**针对关键点和关键情绪配合画面解说就很好**」。
  详见下一节
- **末屏那一问不能是封面那一问的回声**（已有）

**别把「有张力」和「剧透」搞混。** 我一度想加一条「钩子里不许出现比分」，拿存量
一验，被拦掉的恰恰是最好的两条：「**5-6 落后，三个盘点她一个也没让**」
「**先输 1-6，再掀翻头号种子**」——比分在这里是制造张力的手段，不是复述结果。
判据要卡的是「有没有开一扇门」，不是「有没有数字」。

### 要渲染整个比赛的艰难，赢球后多给情绪一点篇幅

账号所有者 2026-08-08，三条片子（佩古拉、高芙、伊埃拉）刚推送完之后：
「要渲染整个比赛的艰难，多给一些赢球后的情绪渲染」。

**这条来得比三条片子的推送晚，已经发出去的不重渲**（消息发出去收不回来），
它管的是下一条起。和「不要平白叙事」是同一族的两个具体方向：

- **整场的艰难要是一条线，不是一堆孤立的事实。** 「两次雨断」「因伤叫医疗
  暂停」「被拖进决胜盘」这类事实本身就在讲艰难，但**串起来读要让人觉得
  这一路不好走**，不能写成"发生了 A，然后 B，然后 C"的流水账——每一条
  艰难都要接得上前一条留下的悬念（这正是上一节"每一拍留一个问题"那条方法论）
- **赢球那一刻不能一笔带过。** 「六比四锁定胜利」是数据收尾；赢球后的表情、
  动作、拥抱、如释重负——这些是全片唯一能让观众共情的落点，篇幅要给够，
  不能因为"结果已经知道了"就压缩它

具体做法沿用已有的工具：**关键点和关键情绪配合画面解说**（上一节）、
**动作句只挑能渲染情绪的那几个**（挑赢球瞬间的表情/动作，别放过）、
**旁白不许把每一局都念一遍**（省下来的篇幅正该留给赢球那几秒）。

⚠️ 这条和上面几条一样，**没有测试，是故意的**——"整场够不够艰难""赢球那
一刻够不够动人"是判断题，机械挡不住。写完一版自己读一遍：铺垫那几拍
是不是真的让人替她捏了把汗，赢下来那一刻是不是配得上前面攒的那口气。

### 抓人心的三个具体写法：留白、峰值不被列表淹没、损失框架

这三条不是账号所有者的原话，是给"抓人心"做可行性评估时，把上面几条已经
验证过的规矩往更细的粒度上推了一步——判断题，不落硬测试，写在这儿是给
"这句话为什么这样写"留一个可查的出处。

- **②句只答 WHAT，把 WHY 留到转折点才揭晓。** "开场三格的顺序"要求②句给
  落点或反差，正例 `shang-rublev` 的『世界第二百七十，赢了世界第十六』
  天然只答了 WHAT（他是谁、赢了谁），没答 WHY（怎么赢的）——留白本身就是
  钩子。写完②句自问一句：这句话是不是把 WHAT 和 WHY 一次性说完了？
  两个都答了，就把 WHY 那半句删掉，挪到转折点那一拍才揭晓
- **全片最深的谷底单独占一拍，别混进"艰难清单"。** 上一条"赢球后多给情绪
  一点篇幅"只点名了赢球那一刻，"峰"（往往不是赢球本身，是赛中最深的谷底）
  还是容易被写成清单里的一项。写"艰难那条线"时先挑出唯一一个全片最深的
  谷底（0-40 那一分/连续双误丢盘/医疗暂停那一刻，只选一个不是列全部），
  单独给它一拍旁白和一段专属画面，跟赢球那一刻在全片里形成对称的两个
  情绪重心
- **转折点前用"倒计时式"损失框架，不用中性比分播报。** "旁白要把比赛走向
  讲清楚"那四问里第③问（转折在哪儿）的示范写法是中性播报（『三比三之后
  他反了过来』）；转折点前的一两句改成"离告负还差多少"（把『0-40』改写成
  『三个赛点，输一个就出局』），落差会更大。⚠️ 只能用在真正的转折点
  （破发点/盘点/赛点）上，撞上"旁白不许把每一局都念一遍"那条 25% 密度
  阈值就是滥用了；局面本没那么惊险时也别硬套，"精准优先于引爆"不能让

理由分别是 Zeigarnik 效应（未完成信息记忆度更高）、峰终律（Kahneman，
体验记忆由峰值和结尾共同决定，不是全过程平均）、损失厌恶（对"即将失去"
的敏感度显著高于等量"即将获得"）。

### ⚠️ 「不解说画面」拦的是指示语，不是描述动作——两条线不一样

下面那条规矩是给**解说片**写的，那条线的画面是**静止图卡**，描述静图确实等于复读。
我把它整条搬到剪辑线上，结果**把一句好台词判成了违规**：

> 赛点，黄泽林在二区发出内角 Ace。**球落地，他放下球拍，双手掩面。**

账号所有者纠正：「这个解说很好啊，为什么是问题？**解说匹配画面是必要的**，
关键是不能知识解说画面，**要针对关键点和关键情绪配合画面解说就很好**。」

对的。**剪辑线是动态画面加现场声**，旁白落在关键点和关键情绪上正是它的本分——
云见那把体育解说的嗓子就是为此选的。真正该拦的只有**指示语**：

| | |
|---|---|
| ❌ 指示语 | 「**画面里是**莱巴金娜」「**镜头里**」「**图为**」「**这张图**」——把观众已经看见的东西再指一遍，一个字的信息都不加 |
| ✅ 关键点／关键情绪 | 「球落地，他放下球拍，双手掩面」——把那一刻的情绪说出来，和画面是**合奏**不是复读 |
| ✅ 画面外的事实 | 「他父亲在看台上看完全场」——集锦里没有莱顿的镜头，这句是核过的事实，正是「话负责讲清楚」 |

我那条测试一度把 `球落地[，,]他` 硬写进正则——**等于把一句好台词钉死成违规**。
判据只留指示语，并在测试里加了一个**反面锚点**：那句必须过。

**方法论**：判据宁可窄，不可宽。**扩大化的判据不吭声**——它不会告诉你「我拦错了」，
只会让下一个人把好写法改成坏写法。加判据之前先拿存量验一遍（钩子里的比分那条
也是这么救回来的）。

### ⭐ 一个球说打了多少**拍**，不说多少秒

账号所有者 2026-08-16：「**以后不要说一个球打了多少秒，应该说打了多少拍，
这样大家才有概念**」。

来路是当天发出去的 `zverev-norrie`：钩子和推送标题都写着「一分打了三十八秒」。
**秒数没有参照系**——三十八秒是长是短观众不知道；而「二十六拍」谁都知道那是
一分打疯了。

⚠️ **判据必须窄，这条尤其容易扩大化。** 同样带「秒」的四类写法里只有第一类要拦：

| | 例子 | |
|---|---|---|
| ❌ **单分的长度** | 「一分打了三十八秒」「破发点持续了超过三十秒」 | 要改成「拍」 |
| ✅ 比赛用时 | 「用时 2 小时 44 分 05 秒」 | 比分板上就印着 |
| ✅ 一**局**的分钟数 | 「一个发球局他守了十七分五十一秒」 | 一局不是一个球，分钟有概念 |
| ✅ 片内时间戳 | 「转播解说在片子的一百五十七秒说」 | 不是球的长度 |

第一版按裸的「N 秒」扫，八条里**六条是误伤**——扩大化的判据不吭声，它只会让
下一个人把对的写法改掉。

⚠️ **拿不到拍数就别写长度。** 巡回赛的公开源普遍不给单分拍数（只有大满贯官方
的 `RallyCount` 有），所以正确处置往往是**换一个说法**——说这一分怎么结束的、
全场什么反应，而不是硬凑一个秒数。

判据 `test_一个球说打了多少拍不说多少秒`，已发的两条挂在豁免表里只许减不许加。

### ⭐ 百分比写「百分之 N」，不写「几成几」

账号所有者 2026-08-16：「**以后文案和配音里不要再出现几成几了，就说百分之多少
用数字写上**」。

来路：这条线的旁白长期在写「一发只进了三成四」「二发得分率都上了七成五」
「一发得分率从六成一涨到七成五」。**「三成四」要读的人自己在脑子里换算成 34%**，
而刷到这条片子的人多想一步就是走掉一个人——和「轮次写半决赛不写四强」是同一个
形状（那条防的正是「四强是打到哪一步」要多想一步）。

| | 写法 |
|---|---|
| **旁白** | 「百分之三十四」——TTS 读得对，`arabic_numerals()` 把它烧成 **`34%`** |
| **小红书正文 / 推送正文** | 直接写 `34%` 也行，那是给眼睛读的 |
| ❌ 一律不写 | 「三成四」「七成五」「不到六成」 |

⚠️ **这条只拦这一个说法，不拦百分比本身**——一发成功率该讲还是讲。

⚠️ **判据的正则要求「成」前面紧挨着一个数词**，所以「改成 / 完成 / 变成 / 裁成 /
判成 / 一**发成**功率」都不会被误伤（改、完、变、裁、判、发都不是数词），
「成功 / 成为 / 成立 / 成绩 / 成片 / 成长」再用后向排除挡一层。反向验证做过：
往一条非豁免的 spec 里塞一句「一发只进了三成四」当场红。

判据 `test_百分比写百分之多少不写几成几`。⚠️ **豁免表有 28 条**——这条规矩之前
发出去的那一批，只许减不许加，表自带自检（slug 要真的存在、真的还带着这个说法）。

⚠️ **这道闸今天只盖住 `specs/reels/`（赛场之上／网球有故事）。** 另外两条线上
同样有存量，量过：`src/tennislive/video/explainer.py` 的 `_SCRIPTS`（三成／四成／
五成／六成）、`specs/interviews/` 五个文件（一成／两成／五成／七成）。
**规矩是全站的，闸不是**——要扩到那两条线，得各配一张自己的豁免表（那边 spec
的形状不一样，`_specs()` 那套复用不了）。在扩之前，那两条线上靠人记着。

#### ⚠️ 而上面那张表的第一行，我写的时候没跑过——真烧出来是「100分之34」

写下「`arabic_numerals()` 会把它烧成『百分之34』」的那一刻，我**没有调过那个函数**。
它当时的实际输出是：

    百分之百        → 100分之100
    百分之六十四    → 100分之64

——`arabic_numerals` 的通用那一轮把「百分之」里的那个「百」也当成了数。于是
`swiatek-arango` 第 10 条字幕烧出去的是「**一发得分率是100分之100**」，一句不通的话。

⚠️ **四道闸一道都没响**：`--dry-run`、`--check-narration`、全量测试、
`check_reel_landed` 全绿——**spec 里那句话是对的**，错的只有烧进画面的那一份。
是把成片从 Release 拉回来抽帧、放大读字幕才看见的。

⚠️ **更该记的是它的来路**：这个坑不是一直都在，是**这条规矩自己带来的**。
规矩生效之前旁白里没有「百分之」，所以它一次都没被触发过；规矩一立，
**每一条带百分比的 spec 都会走到这儿**。立一条新的写法规矩时，顺手问一句——
**这个写法经过的那条管线，跑过一次吗？**

现在换百分比那一步收在 `arabic_numerals` 的**最前面**（要排在年份和比分两轮之前，
否则「百分之」里的「百」会先被别人吃掉）。判据
`test_百分之要换成百分号不许换成一百分之` **钉两头**：行为（含
「百分之六十四比百分之五十」这种两个百分比夹一个「比」的），和**位置**——
挪到比分那一轮之后当场红（反向验证过）。

### ⭐ 「love game」不要字面直译成中文

账号所有者 2026-08-24：「**以后 love game 不要翻译成中文。记录下来**」。

来路：`gauff-pegula-cincinnati-2026-final` 那条已发的片子里，第二盘收尾那一局
佩古拉的发球局一分未得（15:0/30:0/40:0），我把这种「输的一方一分没拿到」的
局叫成了**「爱局」**——`love` 按字面意思直译成了「爱」，读起来像在说一个
浪漫的局，和网球术语毫无关系。同一份文案（旁白 + 小红书正文）踩了两次。

- **`love` 在网球计分里是「零」，不是「爱」**。`love game` 就是一局里输的
  一方一分未得（4-0 拿下）；`hold to love` / `break to love` 是同一个用法
- **不用另造一个中文术语去对应它**。中文口语转播里没有这个词的固定译法，
  硬翻只会制造一个读者要停下来猜的词。**逐分说清楚就够了**——
  「十五比零，三十比零，四十比零」这句话本身已经把「零封」这件事讲清楚了，
  不需要再加一个标签
- 真要收尾用一个词，选**「零封」**（体育报道里通用的「完全没让对方得分」），
  别选任何带着「爱」字的直译——`test_零封的局不许翻译成爱局` 拦的就是这个
  字面直译，不拦「零封」本身
- ⚠️ **已发的这条不重渲**（消息发出去收不回来）——spec 和 `.xhs.txt` 里的
  「爱局」已经改成「零封」**供以后参考**，但已推送的成片和微信正文仍然是
  「爱局」，这条规矩只管以后新写的文案

### ⭐ 「三个盘点都没救下来」——救的人和拿到点的人不是同一个

**这是第一条真实读者反馈换来的规矩。** 小红书评论（2026-08-05，商竣程那条）：

> 「**什么叫 三个盘点都没救下来**」

我写的是——旁白第 9 段「卢布列夫**要到**三个盘点。」第 10 段「三个都**没能救
下来**。」**主语中途换了人，而且跨了一次镜头切换。** 中文承前省略主语，读者只能
把「没救下来」接成卢布列夫，而**那不通**：盘点是他自己拿到的，他没有什么可救。

判据落在**「点」前面那个动词**上，而这一条是机械的：

| 写法 | 救的是谁 | |
|---|---|---|
| 他**面对**七个破发点，救下六个 | 同一个人 | ✅ 不用重写主语 |
| 他**拿到**三个破发点，一个都没**兑现** | 同一个人 | ✅ 兑现的是自己的点 |
| 卢布列夫**拿到**三个盘点，都没**救**下来 | **另一个人** | ❌ 必须把主语写出来 |

**「拿到 / 要到 / 得到 / 握有」的点是他自己的，救它的必然是对手；「面对 / 面临」
的点才是他自己要救的。** 判据 `test_盘点赛点谁救的必须写出来` 只扫前一类、
只认「救 / 保住」——第一版把「面对…救下」也扫进来，当场误伤 `gea-shapovalov`。
又一次「判据宁可窄，不可宽」。

顺带两条同族的：

- **术语第一次出现就地解释一句。** 「盘点」改成「卢布列夫拿到三个盘点，
  **再赢一分就能拿走这一盘**」——多花八个字，读者不用自己补规则。
  同一条片子里 盘点／赛点／破发点 三个术语连着上，全都默认读者懂，
  这是「不要用假设句，用真实的例子」的同族毛病：**默认读者已经在你的知识里**
- **跨段的省略主语要按「观众听到的」判，不是按「稿子上连着的」判。** 段 9 和
  段 10 之间隔着一次镜头切换和一个溶解，纸面上是相邻两句，听感上是两段话

⚠️ **已发的片子改文本不重渲。** 微信那条消息收不回来，小红书的视频要换得重新
上传——但**正文是可以编辑的**，所以 `xhs.txt` 一定要跟着改对，那是唯一还能救回来
的出口。

#### ⭐⭐ 「三个赛点只兑现了一个」——这个比例锁死在 1/N，不是选手的短板

账号所有者 2026-08-19，看完 `bouzkova-jovic`：「三个赛点只兑现了一个，这种文案
是有问题的，以后杜绝类似的弱智文案」。

这条钉的不是语法——「拿到三个赛点，只兑现了一个」这个句式本身是通的，上一节的
表里明写着「兑现的是自己的点」，主语没有换人。问题在于**这个数字本身没有信息量**。
那条片子的钩子、`push.summary`、开场旁白全用了它：

    "hook": "三个赛点\n只兑现了一个"
    narration: "三个赛点摆在她面前，她只兑现了最后一个。"

**赛点／盘点这类"转换即终止"的点，一旦被数成一段连续序列，赢家永远只能兑现
最后一个——这不是这一场独有的结果，是这类点的定义本身决定的。** 赛点的意思就是
"转换它，比赛立刻结束"；所以序列里排在前面的每一个，按定义必然没有被转换
（要么被对手救下，要么被打丢），不然比赛早就在那儿结束了，不会有后面那些点。
也就是说，"N 个赛点只兑现了 1 个"这句话对**任何**一场靠多个赛点才拿下的比赛
都成立，跟这一场打得多胶着、对手多顽强完全无关——它算不出任何信息，读起来却
像在暗示"她效率低"，实际上什么都没说。

**真正会变化、真正有戏剧性的数字是对手救下了几个**（这条片子里是「约维奇连救
两个」）：这个数从 0 到 N-1 都有可能，0 代表对手一触即溃，N-1 代表对手拼到了
最后一刻——这才是能区分"这场好看不好看"的那个轴。CLAUDE.md 早有的好例子
（`shang-rublev` 钩子「决胜盘二比五落后，五个赛点，一个没给」）用的正是这个
框架：主语是**扛住压力的那一方**，数的是他救下了几个，不是数进攻方兑现了几个。

| 框架 | 主语 | 这个数变不变 | |
|---|---|---|
| "N 个赛点，对手救下 M 个" | 扛住压力的那一方 | **变**（0~N-1，衡量对手多顽强） | ✅ 有信息量 |
| "N 个赛点，只兑现了 1 个" | 进攻方自己 | **不变**（任何 N≥2 的赢球序列恒为 1） | ❌ 读起来像短板，其实是同义反复 |

⚠️ **这条只管"赛点／盘点／这一局的局点"这类转换即终止某个单元（整场比赛／这一盘／
这一局）的点，不管整场比赛里分散、互不相连的转化率。** 破发点不是"转换即终止"——
赢下一个破发点只结束当前那个发球局，不妨碍选手在后面别的发球局里再拿到、再转换
别的破发点。所以"全场六个破发点，兑现两个"横跨好几个不同的发球局，是会变的、
真实的效率数字，`bouzkova-jovic` 的 `.xhs.txt` 正文里那句「破发点兑现（5/9 对
5/8）」就是这一类，没有问题。判据是**这几个点是不是同一个终止单元里的同一串**，
不是看用的是"赛点"还是"破发点"这两个字。

⚠️ **这条没有做成硬闸，是故意的**——和「有吸引力」「钩子要有剧情的跌宕」同一个
理由：判断"这几个点是不是同一串"要理解比赛进程，机械挡不住，硬凑一个关键词
正则会在"全场六个破发点只兑现两个"这种合法用法上误报，而误报比没有更糟。写之前
问一句：**这个"只兑现了 M 个"，换成任何一场打了同样多个这种点才赢下来的比赛，
答案会不会一模一样？** 会——就是同义反复，换成"对手救下了几个"。

⚠️ **`bouzkova-jovic` 不重渲，也不改 spec 里已经烧进封面／旁白的那几处**——
成片的封面文字和配音已经把这句念出去了，改 spec 只会制造"spec 说一套、成片是
另一套"的新分叉。这条规矩管的是以后新写的片子。

#### ⭐ 小红书正文是**纯文本**，不许用 markdown

账号所有者 2026-08-06：「**正文里复制的内容，不要用 markdown 格式，复制过去
显示会有问题**」。

`.xhs.txt` 的唯一用途是**被人复制走**——复制页那个 `<textarea>` 和微信正文
**都不过 markdown 渲染器**。所以 `**加粗**` 粘到小红书里就是**原样的四个星号**，
表格那几行竖线更是一堆乱码。当天扫下来八个文件里 `**` 共 80 处，还有一处
`>` 引用块。

**这条和「画面上不写标点」是一家的**：都不是排版洁癖，都是
**「出口不渲染这个记号，写了就会露出来」**。想强调就换写法——单独成段、
用中文引号、把数字提到句首。

判据 `test_小红书正文不许用markdown`：

- ⚠️ **只认出口不渲染、粘过去会露出来的那几种**：星号、反引号、`__`、表格竖线、
  `# ` 标题、`> ` 引用、`[]()` 链接。**不管 `-` 开头的行**——那粘过去就是一个
  横杠，读起来正常。判据宁可窄，不可宽
- ⚠️ **tag 行 `#网球时差` 不许误伤**：`#` 后面没有空格，而 markdown 的 ATX 标题
  必须带空格，两者分得开。反向验证里专门验了这一条

#### ⭐ 破发点／盘点／赛点一律写「拿到」，不写「要到」

账号所有者 2026-08-06：「**以后不要用「要到」，都用「拿到」**」
「**历史视频就不要那个管了**」「**保证以后正常就行**」。

**这是同一条规矩的第二次。** 上面那段里账号所有者的原话就是「改成卢布列夫
**拿到**三个盘点」——我改了 `shang-rublev` 那一条，**没把它变成规矩**，于是
三天后 `zverev-griekspoor` 又写了一次「一口气**要到**三个破发点」。
本文件开头那条「规矩要落成测试，别只写在文档里」说的就是这个：
**改一条 spec 是修症状，落一条判据才是修病**。

判据 `test_破发点盘点赛点一律写拿到不写要到`，两个细节是踩出来的：

- ⚠️ **只禁「点」那个语义，不禁「要到」这个词。** 「巡回赛这条线**要到** 2033
  年才走完」「**要到** 9-7 必经 7-7」都是对的中文，那是「直到／需要达到」。
  正则卡在 `要到[^。！？\n]{0,8}?(破发点|盘点|赛点|局点)`——判据宁可窄，不可宽
- **已发的两个文件挂在豁免表里，只许减不许加**，而且表自己有自检：名字写错的话
  它什么都没拦，而测试照样绿

### 旁白不解说画面

图和话是两条腿：**画负责一眼看懂，话负责讲清楚**。旁白一开口说「画面里是…」，
就是把观众已经看见的东西再念一遍——占掉的是本该讲事实的时间，看得见的人不需要，
只听声音的人也拿不到有用信息。十四条片子里曾有 **29 句**这样的话。

改法不是删句子，是**把指画面的那半句去掉、事实留住**：

- 「画面里是 2026 年温网的莱巴金娜，你可以照着条文一条条对」→
  「2026 年温网的莱巴金娜，可以照着条文一条条对」
- 「画面里这块牌子在 16 号场」→「16 号场的一块牌子上」
- 「生涯十二个单打冠军，画面里就是二〇一四年的他」→「生涯十二个单打冠军」

只有纯粹在指图、去掉之后什么也不剩的句子才整句删（「画面里是今年这两位冠军的合影」）。
见 `test_旁白不解说画面`，同时拦「镜头里」「图为」「这张图」。

**卡上的要点不受这条管**：那儿的「图为 2022 年温网冠军莱巴金娜」是在把年份写到画面上，
是旧照片那条规矩要求的。

### 停顿全部来自标点——没有 SSML break，标点就是唯一的杠杆

账号所有者 2026-08-03 听完 special-exempt：「配音的停顿和断句，需要根据内容
自然区分。」

查下去：`synthesize_narration` 喂给 edge-tts 的是**纯文本**（`speakable(seg.narration)`），
**一个 `<break>` 都没有**。所以「哪儿停、停多久」百分之百由旁白里的标点决定——
写稿的时候标点不是排版，**是配音的节拍谱**。

四类错，那条片子四类全犯了：

| 毛病 | 例子 | 为什么坏 |
|---|---|---|
| **顿号当逗号使** | 「发给那个因为还在上一站比赛**、**所以来不了资格赛的人」 | 顿号在 TTS 里几乎不停，它只该连**并列的词**（「周三、周四」）。连分句就是一口气 24 字冲过去 |
| **长定语前置** | 「他成了第一个**在这个级别打进半决赛的**中国男子球员」 | 书面读得下来，念出来最累。**把主语挪到句末**：「这个级别打进半决赛的中国男子球员，他是第一个。」 |
| **一句串四五个逗号** | ③6 那句 60 字 | 逗号只给短停，连着四个等于没停。超过 30 字就断成两句 |
| **分号用在不并列的地方** | 「…领先**；**晚上九点零六分…」 | 分号暗示并列，而这是两件事。**排比才用分号**（③4 那三条是对的） |

量化判据（`speakable()` 之后，去掉标点数字数）：

- **最长子句**——两个标点之间的裸字数。**超过 20 就该拆**。改前 28，改后 20
- **最长句**——两个句号之间。超过 35 要看一眼。改前 60，改后 39

⚠️ **这条同时改善字幕**：断行本来就是「按标点，不按数满 16 个字」，标点摆对了，
字幕的断行跟着对。反过来，标点乱的稿子会同时毁掉配音和字幕，而**听感和观感
都不会告诉你根因在标点上**。

⚠️ 重排标点时顺手抓到一个事实错误：「佩古拉六比四、一比二领先」——比分记法是
第一个名字在前，`6-4、1-2` 是「赢了第一盘，第二盘一比二**落后**」。
**把两个比分挤在一个顿号里，连错都看不出来**——这也是拆标点的收益之一。

#### ⭐ 切词错了不用靠耳朵猜：`voice_NN.words.json` 就是合成器自己报的切词

账号所有者 2026-08-03 的第二条：「连在一起的词不要分开，本来分开的两个词
不要连读在一起。」

**这个能直接查证。** 字幕时间轴用的那份 WordBoundary 事件，`text` 字段就是
合成器**自己认为的一个词**——把它们用 `｜` 串起来打印，切错的地方一眼就看见，
不用反复听：

```python
toks = [e["text"] for e in json.load(open("voice_01.words.json"))]
print(" ｜ ".join(toks))
```

那条片子扫出来的（左边是它实际念的）：

| 切成 | 该是 | 后果 |
|---|---|---|
| `规则 ｜ 书写 ｜ 得` | 规则书 ｜ 写得 | **「书写」念成 shūxiě，意思全变** |
| `前 ｜ 十胜` | 前十 ｜ 胜 | 「十胜」是个不存在的词 |
| `特殊 ｜ 豁免进 ｜ 的` | 豁免 ｜ 进 | 动词黏进名词 |
| `雨卡 ｜ 在` | 雨 ｜ 卡在 | 同上 |
| `赢斯特鲁夫` | 赢 ｜ 斯特鲁夫 | 动词黏进人名 |
| `佩古 ｜ 拉六 ｜ 比 ｜ 四` | 佩古拉 ｜ 六比四 | **人名被劈开**，尾字黏到数字上 |
| `基茨 ｜ 比 ｜ 厄尔` | 基茨比厄尔 | 赛事名劈三段，中间那个「比」独立成词 |
| `七月二十 ｜ 一号` | 七月 ｜ 二十一号 | 日期从中间断开 |

**修法只有一条可靠：把歧义本身去掉，不是去猜切词器。**

- **加逗号**把边界钉死（「两本规则书**，**写得不一样」）
- **换句式**让黏连的两个字不再相邻（「被这场雨卡在」→「这场雨会把他卡在」）
- **摊开写**（「第一个前十胜」→「第一次赢下世界前十」）
- **动词后面加「了」**把它和人名撑开（「赢斯特鲁夫」→「赢了斯特鲁夫」）
- **人名前后留逗号**，别让动词直接顶着名字

⚠️ **人名内部切错修不了**（`商｜竣程`、`卢布｜列夫`、`布勃利｜克`、`布云朝｜克特`）
——标点插不进名字中间。好在这一类**每个字的读音不变**，只是重音位置偏，
影响远小于上表那几条「假词」。别为它硬改译名。

⚠️ **闭环是重渲之后再读一遍 `words.json`**。改完就宣布修好了，和「写了不等于
跑过」是同一个毛病——切词是合成器那头的行为，只有它自己报的边界能作数。

#### ⚠️ 加逗号**撑得开**两个该分的词，**合不拢**一个该合的词

上面那张表里的例子全是「本该分开的黏在一起」，加逗号确实管用。反过来那一类
它一点用都没有——2026-08-04 王欣瑜那条第 10 段：

    「抢七输掉了。」   →  抢 ｜ 七 ｜ 输 ｜ 掉 ｜ 了     整段五个单字
    「抢七，输掉了。」 →  抢 ｜ 七 ｜ 输 ｜ 掉 ｜ 了     **一模一样**

理由很干脆：**词边界事件里本来就没有标点**（本文件上面那条早写着，我自己没接
上），逗号根本进不了 token 流。「抢七」缺的是**左边的上下文**——它在段首，
前面什么都没有；补上下文（「那个抢七」）要多两个字，而窗口只剩 +0.33s。

- **判它有多严重，看读音变没变。** 真出事的那几条是**音变了**
  （`规则书写得` → 书写 念成 shūxiě）；「抢｜七」拆开还是 qiǎng 和 qī，
  变的是重音不是音，和上面「人名内部切错」同一档
- ⚠️ **但这一句是推的，不是量的。** 没有仪器能判「听着自不自然」——
  它该进那张只有耳朵能结的账（见下一条），不该当成已经放过


#### ⭐⭐ 年份里的 `〇` 会挡住「这是个年份」的识别——沙箱现在能自己量了

账号所有者 2026-08-27：「tts 里 2002 年读音错误」。

旁白按本文件的惯例写「二〇〇二年」（`〇` 是 U+3007，本仓库里它只当数字零用：
年份、赛季、还有比分 `六比〇`）。⚠️ **问题不在写法对不对，在合成器认不认得
它是个年份**——同一把嗓子、同一语速、同一句上下文，只换这一个字，切词完全变了
（edge-tts 的 `WordBoundary`，逐个实测）：

| 旁白里写 | 写 `〇` 的切词 | 写 `零` 的切词 |
|---|---|---|
| 二〇〇二年 | `二｜〇｜〇｜`**`二年`** ← 黏成一个词 | `二零零二年` 整体 |
| 二〇〇五年 | `二｜〇｜〇｜五｜年` | `二零零五年` 整体 |
| 二〇二六年 | `二｜〇｜二六｜年` | `二零二六年` 整体 |
| 二〇二四 | `二｜〇｜二｜四` ← **四个孤立字** | `二零二四` 整体 |
| **一九九九年** | `一九九九年` **本来就整体** | 同 |

**末行是反证**：唯一不带 `〇` 的那个年份本来就被整体识别——所以是 `〇` 把年份
打散成孤立字的，而孤立的字只能逐个猜读音。

⚠️ **2002 是唯一一个把「二年」黏成一个词的**，而那正是「两年」那个量词短语的
形状（其余年份的末位数字和「年」都是分开的）。账号所有者点的正是这一年。

修法收在 `speakable()`（两条线共用：`explainer.py` 和 `build_match_reel.py`
都调它），和「挑→选」「硬地→硬帝」**同一个形状**：**只改喂给合成器那份，
屏幕上不动，而且字数 1:1**——字幕时间轴按字位从合成那份映射回显示那份，
长度一变整段就漂，这是这套办法能用的前提。判据
`test_年份里的圈零要换成零否则合成器认不出这是年份` 钉三头（换干净了 / 屏幕上
没动 / 字数一样），两个方向反向验证过，红在不同的断言行。

⚠️ **非年份的 `〇` 一并换掉是安全的**，验过：`六比〇`→`六比零` 字节数一模一样。
时长只短 0~0.03 秒，往宽里差，不会把哪一段挤成超长。

##### ⭐ 而这一轮最值钱的是：**edge-tts 在这台沙箱又通了，于是有了自校准的仪器**

本文件「2026-08-06：证书链通了，WSS 端点照样 403」那条**已经过期**——2026-08-27
再跑，挂上代理 CA 之后 `edge_tts.Communicate(...).stream()` 正常出音。
（判据照旧是**跑一次**，别照抄任何一条旧结论。）

这买来的不是「能听了」，是**能做对照实验**：同一把嗓子、同一语速、只换一个字，
合成两份来比。这一轮真正定案的两步都靠它：

| 想知道 | 怎么量 |
|---|---|
| 合成器把这一串当成什么 | `boundary="WordBoundary"` 的切词。⚠️ **默认是 `SentenceBoundary`**，不显式传就一条边界都拿不到（本文件早记过这个坑，我又踩了一次） |
| 它到底读的哪个音 | 把「错读」**照着写成字**（`二〇〇两年`）单独合一份，再跟原句比 MFCC。**控制组必须先过**：同一段前缀两份的距离要接近 0 |

⚠️ **控制组这一步不是仪式。** 这一轮第一次量是拿 whisper 的词级时间戳去切单音节，
控制组当场戳穿了它——**「两 vs 两」（同一个音）比「两 vs 二」（不同音）离得还远**，
说明切的片段是错位的。**一个没过控制组的量法，给出的结论和瞎猜同价**，而它看起来
像量过了。

⚠️ **whisper 不能拿来判读音**：它一律把数字归一化（听到什么都写成 `2002年`），
而且在一秒的片段上会把 `initial_prompt` 原样吐回来。

⚠️⚠️ **生产走的是 Azure，沙箱只够得着 edge-tts**（本文件「本地 `--check-narration`
和 runner 上的 render 不是同一个 TTS」记的就是这件事）。上面每一行都是 edge-tts
量的，**Azure 那头没有复现过**——所以这条修法的依据是「让年份可被识别」这件事
本身，不是「我听见 Azure 读错了什么」。**真正的闭环是下一次 render 出来听那一句。**
已发的片子不重渲。

#### ⚠️ 情绪风格要写「谁听过」，`_why` 不算数

2026-08-04：王欣瑜那条的三段上了 Azure 的 `excited` / `sad` / `depressed`，
理由写得很足，还配了**三维实测**——基频 219.2 Hz、响度 −24.2 dB、语速 5.38
字/秒。我据此论证「不吵、不快、只是高」并建议发。账号所有者听完的原话：

> 「配音真的是**太粗细了**，还是不要改成那种情绪化的，**还用原来的那种配音**。
> 但是就是**断句和气口要分开**就行。」

**那三个数都量对了，只是没有一个在量「糙不糙」。** 判据摆出来了，量的却不是
那件事——这比没有判据更坏，因为它看起来像验过了。

- 闸收在 `_seg_voice`：写了 `voice.style` 就必须写 `voice._heard`（谁听的、
  听下来怎么样）。和 `mixed_fps` / `silent_source` / `rank: null` 一个形状，
  只不过这次分开的是「**听**下来行」和「**量**下来应该行」
- **情绪的默认两条路都不改音色**：**断句**（标点，本来就是唯一的停顿杠杆）和
  **气口**（`voice.lead_pause`，Azure 的真 `<break>`）。王欣瑜那条撤掉三个风格
  之后就是这么补的——「抢七输掉了」那个「落」原来拿 `depressed` 压低八度换，
  **诉求对、工具错**，0.35 秒的静默给得更干净
- ⚠️ **`lead_pause` 要算进离线估**（`narration_estimates`，不是
  `speech_seconds`——后者的系数是拿已发成片拟合并钉死的）。它占的是同一个窗口，
  不加就正好乐观 `lead_pause` 秒，然后在六分钟之后的真 render 里才炸
- 按栏目定基调的机制留着，但 `COLUMN_BASE_STYLE` 两个栏目**都登记成「不设」**。
  ⚠️ 那是**实测/听过之后的决定**，不是「还没顾上」——`is_registered()` 就是为了
  让日志把这两种空分开说。⚠️ 都变空之后那张表的三条断言会**恒真**，所以抽成
  `_vet()` 另喂三个故意写坏的表，证明它们还咬得动

### 人名不要手打，先查仓库里的译名表

译名有**两张表，`src/tennislive/zh/player_names_top500.json` 优先**，`players.py`
兜底。把 Rybakina 写成"里巴金娜"发了出去，而表里一直写着**莱巴金娜**——错的不是
不知道，是没查。写稿时凡是出现外国球员名，先 grep 一遍；表里没有的，定下来之后
**补进表里**，别只写在稿子里。

**改的时候两张都要看。** 把 Eala 从「埃亚拉」改成「伊埃拉」，只改 `players.py`
改完还是旧的——`grep -rn 埃亚拉 src/` 才看见 top500.json 里另有一条，而且它说了算。
判断改没改对，**不要看文件内容，直接调 `player_zh()`**：那才是渲染时走的那条路。

**这条现在有测试了**（`test_人名要以译名表为准`）——因为光写在这儿没挡住第二次：
Ostapenko 又被写成「奥斯塔片科」（表里是**奥斯塔彭科**）发了出去。判据是**近似串**：
先把文中所有规范名遮掉，再找剩下的、与某个规范名只差一个字的片段。

- **不遮不行**：同一批稿子直接扫会报 61 条误报——「德约科维奇」内含「科维奇」，
  和「约维奇」只差一个字；遮完只剩 3 条，其中一条是真错
- **示意图也要扫**。名字是画在屏幕上的，`seg.diagram` 里的 `<text>` 一样算。
  这一条是反向验证时发现的：把旧错误放回去，测试居然没炸——因为漏了示意图
- **只查四个字以上**：三个字的窗口会撞上普通词（「东西里挑」撞「西里奇」）。
  所以两三个字的名字写错了它挡不住，**表里没有的仍然要自己补**
- 稿子里故意用的另一种叫法（如「维纳斯·威廉姆斯」，表里是「大威廉姆斯」）
  写进测试里的 `_ON_PURPOSE`，要显式声明，不能默默通过

### 末屏那个问题，旁白必须问出来

十三条片子里曾有**十条**只把问题印在末屏，旁白讲完最后一句就停——**只看视频的人根本
不知道被问了什么**，而这一问是这类短片换评论区的唯一抓手，只给眼睛不给耳朵等于白留。

现在由 `_ask_it_out_loud()` 统一补，不指望每写一条都记得手动加。两个细节是踩出来的：

- **判据是"结尾四十字里有没有问号"，不是逐字比对**。好几条旁白早就问过意思一样、
  措辞不同的话（「你觉得法网什么时候也会换成电子司线？评论区聊聊。」），逐字比对匹配不上，
  补一遍就成了连问两遍
- **有的旁白是特意停在破折号上等这一问的**（「所以问题来了——」），那里再补个句号会多出
  一个孤零零的点
- 测试**不查问号个数**：鹰眼那条本来就连抛两问，那是写稿的选择，不是重复。查的是
  末屏那一问有没有被补进去两次

### 末屏那一问，不能是封面那一问的回声

一头一尾问同一个问题，等于把这一屏白留。踩过两次：

- 商竣程那条封面问「锦织圭的最后一年，**谁来接**？」，末屏又问「锦织圭之后，亚洲男网**谁来接**？」
- 屋顶那条封面问「温网的屋顶，**谁说了算**？」，末屏还问「『关屋顶』是**谁说了算**？」

**收尾这一问要开一扇新门**，最好能接回片子里某个具体的事实。商竣程那条改成了
「亚洲男子的下一次大满贯决赛，还要等多久？」——接的是第 ① 屏那个唯一（至今只打进过一次）。
屋顶那条改成了「打到一半关顶，对场上两个人公平吗？」——接的是迪米特洛夫那一屏。

测试用**字集重合度**拦，不逐字比对：措辞不同、问的是同一件事，正是要拦的那种。
见 `test_末屏那一问不能是封面那一问的回声`。

### 末屏问句还有两种没系统用过的写法：身份代入、评判型立场

「打到一半关顶，对场上两个人公平吗？」（屋顶那条）本来就是**评判型**问句，
只是没被当成一种独立的技巧点名过。这两条都不是硬规矩，是给写末屏问句时
多两个可以选的方向，判断题，用不用、怎么用仍然要看这场球有没有真材料：

- **身份代入**：把读者从旁观者变成利益相关方，只能用在真实、可举证的
  身份群体上（真有球迷在看台、真有相似经历的当事人），不是每条片子都
  套得上。硬套会撞上"不要用假设句，用真实的例子"和"别用统称概括一组人"
  这两条已经明确的规矩——伊埃拉那类有真实球迷背景的选题可以写成『如果你
  也在看台上举过那面旗，这场球赢的不只是比分』，没有这层背景的选题不要
  硬编一个读者画像
- **评判型/邀战式问句**：从"接下来会怎样"换成"该不该/公不公平"，只能用在
  已核实、有据可查的规则/判罚/资源分配分歧上（比如 `mandatory-1000` 那条
  两版 ATP 规则书的真实差异），不能凭空造一个争议。这条离"评论号要有情感、
  立场是基于情感的立场"那句原话最远——**建议只在证据链最扎实的选题上偶尔
  用，不批量铺开**，容易滑向对立争执式的评论区氛围，和这个号一贯的陪伴/
  见证型立场不是一回事

### 小标要带信息，纯指示词不算

「那一边 / 这一边 / 这一场 / 那一周」——商竣程那条六个小标里有四个是这种，排比看着整齐，
读者一个字的信息都没拿到。换成「最高处」（2014 那场决赛）、「缺席」（空了五个月），
一眼就知道这屏要讲什么。

（小标不能把大标题里的词再说一遍，这条已经有测试了。）

### 卡片是给人扫的，不是给人读的

**图不好看，绝不能靠加字补救——加了更糟。** 没人有耐心读完一屏的字，字一多，眼睛先跑了。

- 每屏 2–3 条，**每条尽量不超过 16 字**；一条要换行才排得下，就说明它该被砍成两半或者删掉
- 讲不完的交给旁白。画面负责"一眼看懂"，旁白负责"讲清楚"，两者不重复
- 只有示意图那一屏可以密——因为那一屏的内容本身就是一张表（见下）

### 卡上的字少，是因为旁白会讲；那就必须有字幕

上一条成立的前提是"听得见"。可**静音刷是默认状态**——没有字幕，静音的人拿到的就只有
那两三条短句，旁白里的引语、数字、来龙去脉全丢了，而那才是这条片子的内容。

字幕待在**3:4 画面里面**，不在画布的边条里：卡片上下居中（各 240px 黑边），卡上的
文字块往上收（`CARD_COPY_BOTTOM=230`），在卡片内部腾一条给字幕，上锚（`Alignment=8`）。
**永远只排一行**，左右各留 150px。踩点：

- **时间轴取 edge-tts 的 WordBoundary**，所以合成走 `Communicate.stream()` 而不是 `.save()`——
  `save()` 把这些事件扔了。落在 `voice_XX.words.json` 里。拿不到就按字数等比分配：
  不完美，但绝不能因为拿不到时间轴就整条片子没字幕
- **`boundary="WordBoundary"` 要显式传。** edge-tts 的默认值是 `SentenceBoundary`，
  服务端就只发整句的时刻——我只收 WordBoundary，于是 `words.json` 每条都是 `[]`，
  字幕悄悄退回按字数分，产物看起来一切正常。**又一次「空结果 ≠ 不存在」**：
  空列表和「这个声音不报边界」长得一模一样。答案在本地装着的那份源码里，
  一 grep 就看见了默认参数
- **边界事件里没有标点**，所以字位要**在原文里 find**，不能靠累加事件文本的长度。
  真实数据：一段 122 个非空白字的旁白，边界流只有 109 个，差的 13 个全是逗号句号。
  按累加长度算，「他自己说」在原文排第 108、在边界空间只排第 97，查出来的时刻晚了
  1.7 秒——**越往后漂得越多**，最后一句被压成 0.94 秒读 13 个字。~~判据盯「秒/字」
  各行是否接近，见 `test_字幕时间轴不会越往后越漂`~~
  ⚠️ **那条判据现在不存在了，而它守的行为还在**：`explainer.py` 仍然按
  `text.find(spoken, pos)` 拿**原文字位**去对 WordBoundary（那一行上面就写着
  「时间轴是拿原文的字位去对 WordBoundary 的」）。也就是说**代码是对的，检查没了**——
  这跟本文件反复记的「规矩写对了，实现是空的」正好相反，是**实现对了、判据空了**，
  而两者一样不吭声。真要动这段对齐逻辑，先把「秒/字各行接近」这条判据写回来再动
- **屏幕上的数字用阿拉伯数字**（`arabic_numerals()`），但旁白那份不能动——
  「二〇二六」「六比四」是**给合成器定读法**的。所以换算放在切完行之后，
  只作用在显示的那一份上。几个坑：「唯一一次」连着两个「一」，按数字读就成了
  「唯 11 次」；「一场首轮」「两盘」不是在数数；「第二盘」「第三轮」是序数，
  中文更顺，但「世界第四」是排名，要写成「世界第 4」；「七十万」得在「万」处收住
- **字号不等于一个汉字占的宽度。** libass 按字体的 ascent+descent 把字缩到
  「行高＝FontSize」，思源黑体这两项加起来是 1.46 em，所以一个汉字实际只占
  `FontSize/1.46`。烧帧量出来：字号 46 时 16 个字横跨 505px（31.6px/字），字高 30px
  ——手机上偏小，而我一直按「一个字 52px」在算行宽，**两个数都错了三成**。
  现在字号 68 → 一个汉字 46.6px（屏宽 4.3%）。改字号要重量，别按比例推
- **数字和汉字看着不像一家，不是字体错了，是西文本来就画得小。** 拿成片里的
  「6」和 `NotoSansCJK-Bold` 逐像素比对过——**字体本来就是同一个**（DejaVu 的「6」
  浑圆得多，一眼能分）。差在同字号下西文墨高只有汉字的 0.83（52 : 63）。
  所以数字和西文在 ASS 里单独套一档 `\fs78`（汉字 68），墨高到 0.95 才齐。
  **全角数字试过，不行**：思源的全角只是把同一个字形塞进全角框，字没变粗，
  「2026」反而散成「２ ０ ２ ６」
- **行宽要按画出来的那份算，不是原文。** 切行时用的是汉字数字的原文，画出来的
  却是换算后的——汉字数字大多换完更窄，但「一百」两格换成「100」是 2.03 格，
  正好把一行顶出边。`subtitle_lines` 里的宽度判断已经改成先 `arabic_numerals` 再量
- **一个西文字符占 0.68 个汉字宽**（同字号步进 40.1/68.0 = 0.59，再乘放大的 78/68），
  不是 0.5。按 0.5 估，「2026年4月30日」这种一行会比估计的宽出小半个字
- **左右也要留够，不只是底下。** 小红书/抖音在右侧压着点赞/收藏/评论一列，约屏宽 15%，
  正好落在字幕那个高度。两边各留 150px，可用 780px，一行最多 16 个字（746px）。
  **`_SUB_MAX` 是从边距倒推的，不是拍出来的**——改字号或改边距，它必须跟着算
- **本地烧帧要先装 `fonts-noto-cjk`**，否则用的是回退字体（窄很多），量出来的
  宽度和 CI 对不上，看着「没问题」其实在 CI 上会顶出边
- **两头都有 UI，所以字幕必须待在卡片里面。** 这条走了两版弯路，每一版都是量真成片
  量出来的：
  1. 贴画布最底（`MarginV=30`）——字幕像素落在 y 1849–1882，**离底边只有 38px**，
     被小红书/抖音的底部文案区和 home 指示条盖住
  2. 把整张卡抬到 `CARD_TOP=88`、字幕塞进变宽的下边条——底下躲开了，**卡片顶上那行
     台头又钻进了 app 顶部的返回键/状态栏**
  答案不是挪卡片（上下各 240px 的对称留白就是躲两头 UI 用的），是**把卡上的文字块
  往上收**，在 3:4 画面内部腾一条出来。
- **但别收过头。** 第一版收到 `CARD_COPY_BOTTOM=300`，整摞字被推高：要点块压在人物
  胸口和球拍上，字幕底下反而空出 132px 卡片 + 240px 黑边。渲了三档比出来，
  **230 / 字幕框顶距卡底 156** 最好——文字整体下移 50px，画面主体让出来，字幕仍在卡内。
  156 这个数是从**两行**倒推的：1524 + 78×2 = 1680，正好贴住卡底不溢出
- **一行两行都从同一个高度往下长**，所以要用上锚而不是贴底：贴底时一行和两行
  会落在不同位置，跳来跳去
- `WrapStyle=0`（自动折行）而不是 2：切行那步已经把每行卡在 18 格内，正常不会超宽；
  但万一漏过一条，不折行是**左右直接切掉**，折行只是往下长一行，而上锚保证多一行
  仍然离底边 240px。坏的方式要选能兜住的那种
- **断行按标点，不按数满 18 个字**。数字数切出来的是「代表亚洲国家打／进大满贯」
  「赢得 ATP 单／打冠军」——字数对，词被劈成两半。先按标点切子句再拼行，
  实在没标点才在词语边界上断（见 `_best_break`）
- **屏幕上不写标点，而且是全站的。** 账号所有者先说「以后字幕里的尽量不要用
  标点符号，可以切换下一页表达」，我只改了解说片，于是又补了一句
  「**字幕要应用到全局里**」——「昨日一分」「视频本地化」「大满贯竖版 v2」
  三条线还在往画面上烧逗号句号，同一个账号出去的片子字幕两种样子。
  规矩和实现收在 `video/subtitle_text.py`，四条写 ASS 的路径共用；
  **常驻角标／台标不去标点**——那不是字幕，去掉等于改水印本身。
  停顿由换页表达，一个逗号在屏幕上只是噪点。
  **只留 `？！`**——换页表达得了停顿，表达不了「这是一问」，末屏那一问少了
  问号就成了陈述句。判据见 `test_字幕里不写标点`。三处配套：
  - **去标点只作用在显示的那一份**。切子句、找断点仍然靠原文里的标点；先去掉
    就没有断点可依，又会退回「数满 16 个字一刀切」
  - **一个子句就是一行，不再把两句拼进同一行**。原来「能装下就接着装」，于是
    「先看一眼签表。这是澳网女单签表的」这种句号夹中间的行到处都是
  - **标点换成空格，不是删掉**。合并短句时删掉会糊成一坨（「WC。它是谁给的」
    → 「WC它是谁给的」）。空格不是标点
- **两个词边界之间要按字数插值，不能沿用前一个标记的时刻。** 一句一行之后行
  变密，同一对标记之间常落进两三行——沿用的话它们拿到**同一个起始时刻**，
  两条字幕在时间轴上重叠，libass 会同时画出来。边界事件本来就稀（122 字的
  旁白只有 109 个），指望每行都压在事件上不现实
- **「——」要当一个整体**，否则一句里就一个可断的地方都没有，只能硬切
- 字幕文本用 `readable()` 不用 `speakable()`：比分照着念的样子写（「6 比 4」），
  但 `speakable` 里那处挑→选是**给合成器纠音**的，屏幕上必须还是「挑球」

### 最好的出处，是图注自己把时刻写死了

锦织圭那一屏原来配的是 SI 转载的抛球特写。它只能证明「这篇文章讲的是那场决赛」——
画面本身看不出是哪一场，人也不在打球。换成 Tennis.jp 2014 年 9 月那篇现场报道里的一张，
图注写着「**第２セット １－１、錦織のサーブ、０－４０とピンチ！…満員の観客で埋まった
アーサーアッシュ・スタジアム**」：第几盘、几比几、谁在发球、哪座球场，全由来源写死。

- **找图时优先找带图注的现场报道**，而不是配了张图的综述。综述的配图往往是资料照
- 同一篇里还有一张对手发球的照片、正文写着比分——**互相印证比单张更硬**
- 这张只有 682×1024，是全套里最软的一张。**还是用了**：它是唯一能自证是那场决赛、
  又是真在比赛里的画面。精准优先于清晰，但要在 `credits.json` 里写明这个取舍

### 分不出是哪一场的照片，不能拿来讲某一场

大威那条差点用错：图片社最漂亮的几张庆祝帧看上去都像"她赢了"，但 AP 那张的文件名
`AP25204…` 解出来是 2025 年第 204 天，也就是 **7 月 23 日——她输球那天**。同一周她两场都在
庆祝得分，光看图分不出来。

最后用的是转播截图：画面里的记分条写着 `MATCH POINT #6 / V. WILLIAMS 6 5 Ad / P. STEARNS 3 4`
——**这张自己证明自己**。没那么美，但它是真的。**画面里能自证的元素（记分牌、场地广告、
赛事标识）比任何"看着像"都值钱。**

### 赛事官网的图可能根本不是照片

孟菲斯 2026 年新办的 WTA250，官网票务页上挂着三张 `Stadium Aerial View` /
`Stadium Crowd View` / `Suite Court View Night`——文件名、来源、赛事全都对得上，
差点直接入库。**渲出来一看全是效果图**：横幅上的字是糊的（`TOPNO`、`MEMPH`、
`TENOS GEHPLIS`、`MEMPHIDCLASSIC.COM`），观众是重复贴图，球员的影子方向不一致。

新赛事、新场馆、翻新中的场地，官方放的常常是**开赛前的预览渲染**而不是实拍。
**带假文字的渲染图比一张对题但普通的真实照片糟得多**——那些糊掉的英文一旦被
读者看出来，整张卡的可信度就没了。

怎么认：**放大看画面里的文字**。真实照片里的广告牌、横幅、球场标识都是清楚的
（哪怕小），渲染/AI 放大出来的会糊成"像英文的形状"。观众席出现重复的人也是同一个信号。

那次最后是在 **WTA 官方图库**里找到实拍的（`Marked_Leftwich-Tennis-Center_Courts.jpg`，
黄昏航拍）。顺带两条探测经验：

- **接口报 400 的时候先读它说了什么**。那个图床对无参请求返回
  `Bad parameter: At least one of width or height parameters must be specified`
  ——加个 `?width=2400` 就拿到了。只看状态码会判成"取不到"
- **官网整站返回 200 不等于有东西**。洛斯卡沃斯当时试的那个域名是个 `noindex`
  空壳，`/en/media/news`、`/-/media/`、`/en/photos` 三个路径**全返回 200 但
  Content-Type 是 `text/html`**——soft-404。要看 Content-Type，见下面 ATP 那条。
  （后来发现那根本不是活的域名，见下一条）

### 官方图库把它挂在这一站名下，不等于拍的是这个场地

比效果图更隐蔽的一种。WTA 赛事页孟菲斯那张 hero（`1167_Memphis-Hero-2.jpg`）
每一项检查都过了：**是实拍**（放大看观众是一个个不同的人，工作证、折叠椅、
举手机的小孩，没有重复贴图），场地前场还**刷着 `MEMPHIS CLASSIC`**，文件名带
赛事 ID `1167`，挂在这一站的官方页面上。我已经入库、写好 credits、开了 PR。

它不是这个场地。

**证伪靠的是场地自身的颜色，不是文字**——把它跟同场地的另一份画面并排：

| | 那张 hero | 孟菲斯主球场（电视台画面 / 无人机） |
|---|---|---|
| 前场 | 绿色 | 灰色 |
| 赞助带 | 紫 + 黄 | 深蓝（Mercedes-Benz / TOPNOTCH / crionet） |
| 看台椅 | 绿色折叠椅 | 蓝白阶梯看台 |

上传时间也对不上：2026-06-25，早于首届 7/25 开赛。多半是别处拍的宣传图后期
刷上了场地文字。

- **场地文字是可以后期加的，场地的颜色不会**。有疑问时找**同一场地的第二份
  画面**比颜色、比赞助带、比看台样式，别只看画面上写了什么
- 「官方图库把它挂在这一站名下」是**间接信号**，不是产物。又一次同一个毛病
- **新赛事、首届、临时场馆，这类图最容易出问题**——赛前没有实拍可用，宣传图
  就是这么来的

### 一张能用的图找不到时，新闻台的成片是个被忽略的源

孟菲斯主球场最后是从 **Action News 5 的赛事报道成片里截的帧**。静态图渠道全空
（Commons 0、Openverse 0、官网只有效果图、WTA 只有两张），但电视台的包片里有
**建立镜头**——正是"全景"那一类画面。

- 用 `cv2` 每秒抽一帧拼联系表，一眼扫完两分钟的片子
- **台标条要裁掉**，所以先量它的顶边（按行亮度找），再从上面切 3:4
- 代价如实记：`1280×720` 的源裁完只剩 388×518，放大 2.78 倍偏软。**精准优先
  于清晰**（见上面锦织圭那条），但要在 credits 里写明放大了多少
- HLS 的 master.m3u8 里可能只有 540p，**直链的 mp4 反而是 720p**——两个都探

### 新闻成片里会混进资料画面，抽帧要看场地上写的是什么

孟菲斯那条 7/26 的赛事报道，第 25–27 秒是一段室内蓝场的宽景，构图正好是要的
那种「整个碗」。**但那是十年前停办的旧 ATP 赛事**——场地上印着
`MEMPHIS OPEN` 和 `ATP WORLD TOUR`，而现在这站是 Leftwich 的室外 WTA250。
电视台讲新赛事时顺手垫一段老资料画面，是常规做法。

- **判据是场地上写的字，不是片子讲的是哪一站**。这跟「官方图库把它挂在这一站
  名下不等于拍的是这个场地」是同一个毛病，只是换到了视频上
- 同一条片子里前后两段可以是两个场地，**抽帧不能只按"这条片子对题"就收货**

顺带记一条边界：这一站到今天为止翻遍了官网（Squarespace，只有 hero 和赞助
logo）、WTA 赛事页（只剩那张已被证伪的 hero）、新闻站（StyleBlueprint 那张
StadiumView 是**效果图**，同一批搜索结果里就写着 releases stadium renderings）、
另一条 7/23 的电视片（是场馆改建纪录片，没有赛事看台画面）。
结论写进 credits 的 `note`，别再重跑。

### 图库路径 404，不等于这个站没有图库

洛斯卡沃斯的球场照就这么被判过"不存在"，然后拿海蚀拱地标顶了好几版。复盘下来
每一步都错在**拿一个间接信号当结论**：

- **先确认哪个域名是活的**。这个赛事挂着三个：`abiertoloscabos.com` 是个裸的
  Apache 目录索引（里面只有一个 20 G 的站点备份），`loscabosopen.com` 是
  Hostinger 的停放页，活的是 `loscabostennisopen.com`。三个都返回 200
- **`/en/photos`、`/en/gallery` 全是 404，图库却在**。它是 WordPress，
  `wp-json/wp/v2/media?search=<词>&per_page=40` 一查就把原图连同
  `media_details.width/height` 列出来——比在页面上抓省事得多，也顺手回答了
  "有没有更大的尺寸"（`media_details.sizes` / `original_image`）
- **搜索词按赛事自己的语言写**。`stadium` 只出 3 条，`estadio` 出 36 条，
  `cancha` 出 10 条。西语站用英文词搜等于自己把结果掐掉

这两条合起来还是那句：**空结果先自证是真空**。

### 背景图一律是中心球场全景；地标只是欠着的账

用户定的：「**背景都要找中心球场全景的照片**」。城市地标（帕特农、竞技场、
山谷牧场）不是可以停下来的地方，是**还没找到球场照**的临时状态。

**兜底最麻烦的地方是它不报错**：地点对、画面还挺好看，扫过去不觉得有什么不对
——洛斯卡沃斯的海蚀拱和孟菲斯的全场航拍就各这样待了好几版，直到有人问一句
"为啥不用中心球场"。所以要让它**挂账**，而不是靠人偶然发现：

- `data/venue_assets.json` 每条都带 `shot`：`centre-court` 或 `landmark`；
  是 `landmark` 的必须写 `todo`，说清卡在哪儿（查过哪些词、命中多少、为什么不能用）
- `test_every_venue_declares_what_kind_of_shot_it_is` 里的 `LANDMARK_BUDGET`
  **只许降不许升**。加新站要么带着球场照来，要么显式把这个数字调高——让"又退回
  地标"变成一次看得见的决定
- 换到球场照之后再钉进 `test_sites_with_a_real_court_photo_do_not_fall_back_to_a_landmark`，
  免得改 manifest 时悄悄退回去

判什么算合格，按这三条：

- **是场内，不是场外**。乌马格现在钉的是建筑外立面，澳网原来钉的是场外夜景
  （人群 + 建筑）——都不算。澳网那张场内满场的图其实一直躺在
  `assets/` 里没接上，改 manifest 一行就换过来了（后来又换成同一组里从底线
  后方拍的 quarter final 那张，见下一节）
- **是中心球场，不是球场群**。温网原来是全园航拍、孟菲斯原来是球场群航拍，
  中心球场都只是画面里的一块
- **看得见看台**。只有场地没有看台，看着像块公共球场

### 竖版卡里的"全景"，取决于拍摄视角，不是原图有多宽

挑图时看的是原图——一张漂亮的中心球场横幅，看台、球场、天空全在。入库之后
卡上只剩中间一条：竖版卡是 `background-size:cover`，1080×1440 的框套一张 1.5
的横图，**横向只留中间 50%**，两边看台直接切掉，剩下"一点场地 + 一点看台"。

所以能在竖版里成立的全景有一个硬条件：**从底线后方的高处、沿球场长轴拍**。
这种视角下整个碗是**竖着**排在画面里的——近端看台在下、球场在中、远端看台和
顶棚在上——中间一条竖切仍然包含完整的碗。洛斯卡沃斯和美网（`View From the
Top of the Arthur Ashe Stadium`）就是标准样例。站在侧面看台横着拍的，无论原图
多宽，竖切之后一定只剩中段。

- **判它要用 `tools/preview_venue_crop.py`**，按卡片实际的裁法渲出来看，别看原图
- **`focal_point` 的 Y 是死的**：现有场馆图全是横图，cover 之后高度正好铺满，
  纵向可移动量恒为 0，只有 X 有用。我一度按"把碗拉回中间"改了四站的 Y，
  渲出来一模一样，差点当成修好了——**又一次拿没验证的改动当结论**
- 想控制上下取哪一段，只有两条路：换一张沿长轴拍的，或者**先裁成 3:4 再入库**
  （洛斯卡沃斯那张上面近一半是天空，裁掉顶部 170px 球场才回到中央）

### 画出来的那一屏，也要够大够直观

示意图小到看不清，等于没画。踩过的具体数字：`.diagram-wrap svg` 原来是 `width:760px`，
在 1080 宽的卡上只占七成，而 900 单位 viewBox 里一个 `font-size:20` 落到实际画面上约 17px——
显示器上勉强能看，手机举远一点就废了。现在是 `920px`，`top` 从 250 提到 210 给高度让位。

- **字号按 viewBox 比例算**，别按"看着差不多"。900 宽的 viewBox 里，正文别小于 26，标题 34 起
- **列清单可以，但别用文字排成表格冒充图表**。连败那一屏先做成六行"日期 / 赛事 / 对手 / 比分"，
  被一句"不能都用纯文字啊"打回——列比赛是对的，用文字排出来不是。改成**每场一根横条，
  长度＝她在那场里拿到的局数占比，虚线画在一半**：赢的那场越过去，十一场败仗没有一条够得着。
  `2-6 2-6` 是十六局里拿四局，条只填四分之一，观众不用做除法。数字留在右边给想看的人
- **旁白要把关键的几场念出来**，别只念总数

#### ⚠️ viewBox 的**高度**也有上限：高 20px 就撞进序号药丸那一行

上面那条管的是「字够不够大」，这条管「图能有多高」。2026-08-17 `wang-sabalenka`
踩的：第一版写 `viewBox="0 0 900 660"`、落点两行在 y=586 / 628，渲出来
**注脚的头几个字被「② 唯一一次」那颗药丸盖住**。

账算得很干净——SVG 是**按宽度铺满**的（920px），所以它在卡上的实际高度是
`920 × H/900`，每一行的实际像素位置也跟着 H 走：

| | viewBox | 落点 y | 实际像素（相对图顶） |
|---|---|---|---|
| `_ZHENG_EALA_DIAGRAM`（好的） | 900×**640** | 572 | 585 |
| 第一版（撞了） | 900×**660** | 586 | **599** |

**差 14px，而药丸就在那个位置。** 药丸是卡片版式的一部分，**不随 SVG 变**，
所以图越高、底部那两行越往下掉，掉过头就被压住。

- **高度钉在 640，落点钉在 y≈568 / 608**（`_ZHENG_EALA_DIAGRAM` 那张是标定物）。
  内容装不下就往里收间距，别往下加高度
- ⚠️ **四道闸门和全量测试对这个一声都没有**——图本身没有语法错，每个数都是对的，
  只有渲出来打开看才看得见。这条和「⚠️ 而我给这套写的第一条判据，拦的是一个不存在的坑」
  是一家的：**能落测试的和该用眼睛看的，是两件事**
- 顺带同一天同一条片子的第二个：条形图右边的数字**左对齐**排出去会超出 900 的画布，
  「26 胜 2 负」的「负」字被切在画面外。数字一律 `text-anchor="end"` 钉在右边，
  条再长也推不出去

### 一屏只留一个强调色

**"太艳"的正解是减少颜色数量，不是把颜色调灰。** 这条走过一次弯路：郑钦文那张图表被说
"颜色太艳"，我把红绿换成低饱和的莎粉与浅绿；结果同一套色搬到连败图上，被说"配色很讨厌"——
**低饱和的暖色压在深绿底上会发脏**，不是变高级。

现在的做法：**赢的那一条给品牌绿，其余一律中性半透明白**。不用红绿对撞去说输赢——条短就是短，
读者自己看得出来。少了那一片脏粉，唯一的绿反而跳出来了。

- 一屏最多一个强调色；输赢、好坏、正反，尽量交给**长度、位置、明暗**，不是交给色相
- 文字最多两级：主色近白 `#e7f3ec`，次级灰绿 `#a9bcb2`。四种文字颜色堆在一屏就开始吵
- 灰不是"没设计"，是给唯一那个亮点腾出地方亮
- **次级灰别压得太暗**。一度用到 `#7f958a`，反馈是"文字有点模糊"——那不是分辨率问题，
  是对比度不够，眼睛对不上焦就读成"糊"

### 条形图上不要写字，一个字都别写

**文字压在进度条上，字和条互相削。** 局数曾经写在条内（`19 局里拿到 12 局`），挪到标题行
又和赛事名撞在一起——两次都在抢同一块地方。最后一个字都不留：**条只负责长度，说明写在条的
上一行，图例写在标题下面一行**。条上干净了，长度才看得出来。

### 讲一串同类的事，挑最有代表性的一两个讲透

十一场败仗曾经全列出来（后来减到五场），仍然是"一屏堆着六行差不多的东西"。
现在只留两条：**那场胜利，和输得最接近的那一场**——奥克兰对利内特，二十八局里她拿了十二局，
是有公开比分的几场里咬得最紧的。其余一句「均止步首轮」带过。

- 挑出来的那一个要**有挑选理由，而且理由能说出口**（这里是"局数占比最高"，图上自己看得见）
- 数据不全就把范围说清楚：只掌握其中几场的比分，就写"有公开比分的几场里"，
  别写成"十一场里最接近的一场"

### 小红书正文卡在 1000 字，而且要「提炼在前、原话在后」

账号所有者：「之前你给的那个文案里的正文**超过了 1000 字，然后不能直接复制**，
所以这个 1000 字的限制你要写进到规则里。」

**这不是编辑口味，是平台的硬限制**——那一格就是 1000 字，超了**粘不进去**。
而它**不吭声**：文案照样渲、复制页照样发、按钮照样在，只有真去粘的人才发现
粘不上。整条推送的出口就这么没了，而消息已经发出去收不回来。

量出来是**采访这条线独有的毛病**：赛场之上那十几条正文全在 1000 以内
（最长 925），采访两条是 **1161 和 1005**。原因很直接——采访**把原文整段
搬进了正文**。所以正确的修法是提炼，不是放宽这个数。

账号所有者同时给了正文该长什么样：

> 「你不要把所有的内容都复制在正文里，你可以**提炼一些关键点**，然后适当做一些
> **总结以及给大家的一些启发**，可以先写在前面。然后后面把重要的要点放在后面，
> 就是**原文要点**。」

于是正文分两段，顺序不能反：

| 位置 | 放什么 |
|---|---|
| **前** | 提炼过的关键点、总结、给读者的启发——**我们的话** |
| **后** | 最值得抄的那几句**原话**（英文 + 中译），少而精 |

理由和「最硬的那个事实放第 ① 屏」是同一条：看到结尾的人只有 2–3%。
原话逐句铺在最前面，等于把最需要被读到的判断埋在了没人走到的地方。

- 闸装在 `push_reel.split_copy()`——**推送正文和复制页共用它**，装一处两边都拦得住
- 判据 `test_小红书正文不许超一千字`：扫 `specs/*/*.xhs.txt` 每一份，
  并要求至少校到 10 份（防「目录写错 → 一份没校 → 恒真的绿灯」）
- ⚠️ **标题那一格是另一个数**，目前没卡。存量第一行从 17 到 108 字都有，
  真要卡得先问清楚哪些渠道在用它

### 收口要按组轮流取，不能按全局分量一路取到底

今日赛程把跨日窗口打开之后，单日到 42 场、七页，推送正文 2095 字——小红书那
一千字的正文根本放不下。收到 16 场（正文 873 字、四页）。

**关键不是"砍到多少"，是"怎么砍"。** 按全局分量从高到低一路取，华盛顿那种
ATP500+WTA500 的大站会把 16 个名额吃光，**孟菲斯和洛斯卡沃斯整站消失**——读者
看到的会是"今天只有华盛顿在打"，那是假的。

所以：**先把不能丢的整批保下来**（这里是中国单打，读者就是冲这个来的），
**再在各组之间轮流取一场**，直到取满。每个赛事都留得住代表。

- 上限要**从产物倒推**，别拍脑袋：16 是按"一场一行、连赛事标题约 800 字、
  一屏能扫完"算出来的
- 收口之后**图和文案必须还是同一批**。正文和图对不上，读者第一眼就发现
- 页数会跟着变，**成片目录生成前要先清**：七页降到四页那次，上一轮的
  `card_schedule05..07` 作为陈图留在仓库里，看着像本期的一部分

## 验证

- 每张图**必须亲眼看过**再用（`Read` 打开），元数据对不上或画面不符一律不用。手机拍的照片常带 EXIF 旋转位，PIL 不会自动应用——德约那张 3024×4032 存下来是躺着的。入库一律走 `ImageOps.exif_transpose()`，存完再看一眼
- **要发出去的整页也一样**：推送正文、复制页这类 HTML，渲染成图看一眼再发，别只跑字符串断言
- 本地沙箱取不到外网图片和 TTS，这类验证走 GitHub Actions
- 规则要落成测试，别只写在文档里 —— 见 `tests/test_explainer.py`

### ⚠️⚠️ 跑一趟测试会改到**跟踪进仓库的数据**，而 stop hook 会催你把它推上去

2026-08-29 量出来的：`pytest tests/test_cli.py -k knowledge` 跑完（**5 passed**），
`git status` 里 `data/story_state.json` 就脏了——少掉一条 `__visual_backoff__`。
走的是 `cmd_knowledge` → `clear_visual_backoff()` 这条**真路**，而 `test_cli.py`
一处都没给 `STATE_PATH` 打过桩（`test_render.py` 每条都打了，所以它一直没事）。

⚠️ **三样叠在一起才是真正的危险，单看哪一样都像小事：**

| | |
|---|---|
| 测试**全绿** | 没有任何东西说它写了仓库里的文件 |
| 这个仓库的提交习惯是 `git add -A` | 副作用会顺着一次正常提交进去 |
| **stop hook 会主动催**「有未提交的改动，请提交并推送」 | 于是工具在推着你把它推上去 |

而被改掉的是**另一条线的账本**：`__visual_backoff__` 那几条是「这个选题的素材
预检刚失败过，三天内别再排它」（见上面无人值守那节的第 ④ 条），抹掉等于让那条线
明天再去撞同一堵墙；同一个文件里的 slug→日期是「这个选题发过没有」，抹掉就是
**同一条知识帖再发一遍**，而微信那条消息发出去收不回来。

收在 `tests/conftest.py` 的 autouse fixture `_isolate_story_state`（和
`_no_ambient_github_token` / `_isolate_tts_cache` 同一个形状）：每个测试各拿一份
`tmp_path` 里的副本。

- ⚠️ **要拷一份真的过去，不能指到一个空文件**：`published_topics()` 读的就是它，
  指空了「这个选题发过没有」会全部答成「没发过」——那比脏一个文件糟得多
- 判据 `test_跑测试不许改到仓库里那份选题账本` **不查「有没有打桩」**（换个写法
  就绕过去了），是**真调一次写入，然后看仓库里那份文件动没动**；另加一头
  「隔离要真的指到别处，而不是把写入整个掐掉」——只有前一头的话，一个
  `mark_story_used = lambda: None` 的 fixture 也能让它绿，而那会把这条判据
  和它要保护的行为一起废掉。两头分别反向验证过，各红在自己的断言行
- ⚠️ **判「脏没脏」之前先 `git checkout -- <那个文件>`**：我自己就把上一轮
  留下的脏当成了「这一轮又脏了」，据此以为修复没生效，多查了两轮。
  **查产物之前先把产物清干净**，不然量到的是上一次的残留

### 环境不用每次现配：`.claude/hooks/session-start.sh`

账号所有者 2026-08-05：「之前好像有好几次都要重新安装配置环境……**浪费了很多
时间**」。对的——容器每次是干净的，而下面那一节（「先装依赖，别把缺依赖当成
已知红」）说的那套，每个新会话都要重来一遍五到十分钟。

现在收成一个 SessionStart 钩子，装的东西**从 `ci.yml` 推**，判据在
`tests/test_session_start_hook.py`：沙箱不许装得比 CI 少（装少了的样子是
「本地跑不了 CI 能跑的测试」，然后那几条被读成「这里跑不了」）。

两个坑写在钩子的注释里，都是当天踩的：

- **`apt-get update` 必须排在 install 前面。** 镜像里的索引是构建时的，
  直接 install 会去取已经被删掉的旧版本，报的是 **404** 而不是「索引过期」
- **`--no-install-recommends` 不是洁癖。** ffmpeg 的 recommends 拖着一串
  VA-API 驱动，而那正是上面 404 的那两个包

⚠️ **而第一版的幂等检查被自己的管道废掉了**：

    fc-list | grep -qi "NotoSansCJK" || need_apt=1     # ← 匹配上了也算失败

`grep -q` 一命中就退出，`fc-list` 随即吃 SIGPIPE，管道退出码 **141**，
而 `set -o pipefail` 把它判成失败。于是「装好了」被读成「没装」。
**它错的方向是「白跑一次 apt」**——慢一点、不报错、永远没人发现。
和本文件里 `until ! pgrep -f 'X'` 会匹配到它自己是同一个形状：
**检查自己的机制把检查本身废掉了**。落成变量再 `case` 匹配，整条路上没有管道。

#### ⚠️ 顺带两条，都是 CI 替我逮到的（run 30971701644）

**① `.claude` 不在 `ci.yml` 的稀疏检出里，五条判据的主语在 CI 上不存在。**
和「测试不许拿 `output/` 当判据的主语——CI 上它不在」是同一条，只是换了个目录。
差别在于那次是**假绿**（`glob` 返回空列表，看起来像「没有成片」），这次是
**真红**——所以这次便宜得多。**加一个新目录当判据的主语时，先问它在不在 CI 的
检出里。**

**② `Path.is_file()` 会抛，不是只返回 False。** 它只吞 ENOENT / ENOTDIR /
EBADF / ELOOP 那几类，**不吞 EACCES**——而 runner 上跑测试的用户根本进不去
`/root`，于是探那个众所周知的 CA 路径时 `os.stat` 抛 `PermissionError`。

而这个探测排在 `main()` 的**第一行**，所以它一抛就是**每一次 CLI 调用都崩**：
整条 match-reel 会死在还没解析参数的时候。也就是说一个「本地跑得好好的」改动，
会在 runner 上打爆所有出片——**而本地永远复现不出来，因为沙箱里跑的是 root，
root 绕过权限检查**。判据因此不能靠 `chmod 000` 造场景（那样造出来的
「不可读」在这儿是可读的，测试会因为错误的原因变绿），要直接让 `is_file` 抛。

**探一个「众所周知的位置」失败就等于这台机器没有它**，任何 OSError 都一样——
这条路只是猜，猜不中是常态。显式配的那条不同：那儿配错了要报。

### 沙箱跑全量测试前先装依赖，别把缺依赖当成「已知红」

沙箱里全量跑常年 26 条红，我一直当成环境噪音，还在回复里写过「全是
`_cffi_backend` 缺失那一类」——**那句话是错的**。真去看每条的第一行错误，
是四种原因，只有最后一种被我说对了：

| 条数 | 报错 | 装什么 |
|---|---|---|
| 16 | `No module named 'rich'` | 主依赖，`pip install -e .` 就有 |
| 2 | `cv2 has no attribute 'CascadeClassifier'` | `visualqa` extra |
| 7 | `pyo3_runtime.PanicException` → `No module named '_cffi_backend'` | `pip install cffi` |
| 1 | `AssertionError: wqy-zenhei.ttc` | **真问题，见下** |

照 CI 那行装完（`ci.yml` 里就写着），26 → 1：

```bash
sudo apt-get install -y -qq fonts-noto-cjk        # ← 别漏，见下
pip install -q -e ".[dev,webrender,visualqa]" && pip install -q cffi
PYTHONPATH=src python3 -m pytest tests/ -q        # 907 passed, 0 skipped
```

**那 7 条 skip 也是缺依赖，不是「这里跑不了」。** 全是
`tests/test_collection_cover.py`——整个文件挂着
`skipif(not NotoSansCJK-Bold.ttc.exists())`。沙箱装的是文泉驿，
路径对不上就整file跳过，装上 `fonts-noto-cjk` 之后 7 条全绿。
CI 的 apt 那一步本来就装了它，我照抄 pip 那行时漏了 apt 那行。

**skip 和 fail 要一样对待**：一个常年不变的 skip 数字，和一个常年不变的
fail 数字一样，都是「没人真的看过」。`-rs` 一跑就知道为什么跳。

两条教训：

- **`opencv-python-headless` 不能不带版本装。** `pyproject.toml` 里钉着
  `>=4.10,<5`，我为了抽视频帧随手 `pip install opencv-python-headless`
  装到 **5.0.0**，`cv2.CascadeClassifier` 在 5.x 里没了——于是那两条红从
  「没装 cv2」变成「装错了 cv2」，长得一模一样。装 extra，别装包名
- **「一批红」要逐条看第一行错误。** 26 条里 25 条同因、1 条不同因，
  只看前几条就下结论，那一条就永远被埋在噪音里。这和「空结果先自证是真空」
  是同一个毛病：**一个笼统的解释盖住了一个具体的 bug**

那第 1 条是真的：`test_default_fonts_render_cjk_without_env` 里有一句
`any(tag in name for tag in ("Noto", "CJK", "SC"))`，拿**文件名**近似
「这是不是中文字体」。沙箱装的是文泉驿正黑（`wqy-zenhei.ttc`），中文渲染
完全正常，却因为名字里没那三个词被判红——**假阴性**。判据本来就在同一个
测试的下面两行（画出来比笔画：`render("网") != render("时")`，Latin-only
字体画出来两个豆腐块字节完全相同，反向验证过），把名字白名单删掉即可。

断言全绿不等于页面对。给推送加"可复制文案"时，`'长按下面整段' in body`、`'#网球时差' in body` 全过，人却没看过整页 —— 实际是同一份文案在推送里印了两遍，正文一遍、灰底复制块又一遍。截图一眼就看出来了。

### 交叉校验只比得上「说错了」，比不到「什么都没说」

赛后开麦那条线上有两道验转写的闸，做得都挺认真：`verify_transcript` 跑第二份
ASR 比全段，`check_human_quote` 拿赛事官网的人工引语比那几句。**两道全绿，
成片里还是有 3.2 秒是空白的。**

伊埃拉那条：主持人说完「话筒交给你」，她接过话筒对着看台上举菲律宾国旗的
球迷开口——而 YouTube 的自动字幕在那一段**连一个事件都没有**。

毛病在两道闸的形状：它们比的都是**词与词对不对得上**。

> **没有词就没有分歧。空白不但过闸，还让分歧率更好看。**

这是「空结果 ≠ 不存在」长到校验环节上的样子，而且更阴——那儿空结果至少
还是个结果，这儿它连位置都不占：逐行核对表一行一行走的是「源说了什么」，
那 3.2 秒在表里**根本不占一行**，翻一百遍也看不见。

所以每条比对型的校验都要配一条**查覆盖的**：

- **判据要是「源自己有没有出声」，不是「有没有词」。** `[applause]` /
  `[cheering]` 这类噪声标记要**算成有事件**——它们是源在说「我听见了，
  只是不是人话」。切行时丢掉它们是对的（那不是台词），在这儿丢掉就会
  把每一次鼓掌都报成漏词，而**一个天天误报的检查等于没有检查**
- **阈值从真实分布量，别拍。** 那条片子一共三个空档：3.24 秒（真漏了）、
  1.91 秒（两头夹着 `[cheering]` 和 `[applause]`，是掌声）、0.28 秒（抖动）。
  2.0 秒落在最大的和第二个之间
- **机器只负责「找出来」，不负责「判断它是什么」。** 空档是什么——没人说话、
  掌声、还是球员换了母语——机器分不出来，**这一步不要再加模型去猜**
  （账号所有者：「不要多语种」）。第二份 ASR 是 `small.en`，它在空档里
  没听出词来**不等于没人说话**：非英语在它这儿同样是空白。所以报告里这两句
  必须分开写，**别让「没听出来」被读成「没有」**——那正是这一整节要防的东西
- **销账留判据，不许抹平。** 空档写进 spec 的 `caption_gaps_ok`，值写的是
  「听过之后的决定」而不是「这里没问题」——伊埃拉那条销账的值第一句就是
  「⚠️ 这一处是真漏了」。把一个已知的洞抹平，比留着它糟得多

判据落在 `caption_gaps` / `probe_gap_speech`，闸装在 `--stage render`
（和 `suspect` 同一个形状：改或听，两条都没走的不许出片）。
`probe_gap_speech` **不下第二个模型、不切音频**——它摊的是 `verify_transcript`
已经跑完的那份结果，这几秒的答案本来就在里面。

#### 但这道闸只是「让你看一眼」，不是「必须查实」

账号所有者 2026-08-03（冠军版那条的两处空档）：「**没人说话很正常啊，可能是
大家在欢呼之类的打断了讲话。不要卡这个。**」

**颁奖致辞这类素材，被掌声打断是常态而不是异常**——那条片子 215 秒里两处空档
（4.8 秒和 3.7 秒），一处是奖杯递过来的那几秒，一处两头夹着 `[cheering]`、
她随后用 `>> So, thank you.` **重新起头**（掌声打断再接上的标准形状）。

所以销账的门槛是**「摆出你的判据」，不是「拿到确证」**：

| | |
|---|---|
| ✅ 够了 | 「两头夹着 `[cheering]`，她随后重新起头」——写清楚推理，并注明是推的 |
| ✅ 够了 | 「⚠️ 我没听音轨，这是从缩略图墙推的」——**标出软硬**，别装成听过 |
| ❌ 不行 | 「这里没问题」——把一个已知的洞抹平 |
| ❌ 不行 | 为了坐实它去加多语种模型（账号所有者：「不要多语种」） |

**闸没有放宽，也不该放宽**——它当初抓到的是一段真丢了的话（见本节开头）。
变的是**销账的成本**：写两句站得住的推理就过，不用停下来等一个拿不到的确证。

⚠️ **别顺手把「两头有噪声标记就自动销账」写进代码。** 看着很合理，但那正好
覆盖掉当初出事的那一类；而且它会把「机器替你判断了」伪装成「你判断过了」，
和这一整节要防的东西是同一个方向。

##### ⭐ 沙箱听不了音轨，但「是不是掌声」是**量得出来 + 看得出来**的

上面那张表说「摆出你的判据就够」，而判据未必只能是推理。萨巴伦卡那条
（片内 18.7–22.6 秒）三条硬的，加起来比「听一遍」还结实，**每条都是本地几秒的事**：

- **画面：话筒被撤走了。** 19.2 秒 WTA TOUR 的话筒还举在她嘴边，20.4 秒落到
  胸口以外，21.6 / 22.4 秒**整个出画**。**采访者收话筒就是「这一答完了」的现场
  信号**——话筒不在嘴边，她不可能还有话被漏掉。同两格里看台成排双手举在胸前。
  ⚠️ 抽 4 张帧、拼一面墙，十几秒
- **响度：有声音，但峰值低一档。**

      空档            mean −36.8 / max −20.3 dB
      她说话（5 秒）  mean −36.2 / max −16.8
      主持人（5 秒）  mean −34.1 / max −14.8

  **均值和说话一个量级 → 不是静音；峰值低 3.5 dB → 不是人话**（宽频连续的
  掌声没有说话的爆破音尖峰）。⚠️ `volumedetect` **不能带 `-v error`**，
  那会把它自己的 INFO 输出吞掉，报出来是一片空白
- **反证：让第二个模型在这几秒上自相矛盾。** 同一段音频喂三个**错开的**窗口，
  `medium.en` 给了三个不同答案，还多出一个整场没出现过的词：

      18.74–22.64  → "Thank you so much."
      19.60–22.60  → "Thank you **very** much."   ← very 全文 0 次
      20.40–22.60  → "Thank you." "Thank you."    ← 重复，whisper 对噪声的标准形状

  **这是「一次结果不算数」的正用**：单跑一次它会给你一句像模像样的话，
  而那正是把幻觉当漏词补进 `en_fixed` 的路。**错开窗口再跑两次，几乎不要钱。**

⚠️ 判据本身照旧要**标软硬**：这四条是**量出来和看出来的，不是听出来的**，
销账那段最后一句就得这么写——别让下一个人读成「有人听过了」。

##### ⚠️ 报告里的数会被印上封面，所以它必须只算**片内**

`interview_takeaway.py` 的词数原来是拿**整份 `cap_*.json3`** 数的，而
`cap_*.json3` 是**整条源片**的转写。萨巴伦卡那条把主持人那句两个模型都听不清的
收场白剪掉了（`end: 63.7`，那一轮从 63.80 起），于是：

| | |
|---|---|
| 工具报的 | 主持人 **143** 词 |
| 片子里真有的 | **119** 词 |
| 而表头印着 | 「采访 **63.7** 秒」 |

**表头说的是片子，数说的是源片**，两者差 24 个词，读的人没有第二个地方能对。
而这个数**正要印上封面**（`主持人问了 119 个词 / 她答了 77 个`）——多算的那 24 个
观众一个都听不到，印出去就是一句假话。

- 这是「查产物，不查信号」的又一实例：**产物是这条片子，不是那份转写**
- 判据 `test_提炼原料只算片内那一段` **两头都钉**：窗外的不许进来、
  **窗内的一个都不许少**（只钉前一头的话，把窗口收成空的也能过）
- 顺带：`spec` 一改 `start`/`end`，封面上那两个数就要**重数**。这条写进了那条
  spec 的 `push._why`——数的出处只有一个，改了要跟着走

##### ⚠️ 同一族的第三个：**两张**报告，第一份/第二份是谁只修好了一张

「报告里第一份是谁要照实写」当年只改了 `verify_transcript`。`probe_gap_speech`
的表头一直写死 `small.en`、发现漏词那一支一直写死「YouTube 漏了英语」——
而萨巴伦卡那条第二份是 `medium.en`，第一份根本不是 YouTube（Brightcove 源没有
自动字幕轨，第一份也是我们自己跑的）。**闸跑的是对的，报告印的是错的。**

抽成 `_first_source_label()` / `_second_model()` 两个函数，两张报告共用
——「一个数写两处必分叉」。判据 `test_两份转写各是谁两张报告都要照实写`
**扫的是裸子串 `small.en` / `YouTube`，不是 `"small.en"`**：写死的那次嵌在一句
中文里（`第二份 ASR 是 \`small.en\`（英语专用）`），带引号的判据抓不到它。

⚠️ 扫之前要**去掉整行注释和 docstring**——这一条的 docstring 里就写着
「表头写死 `small.en`」。**别用 `ast.get_docstring()` 去 `replace`**：
它回的是**去过缩进**的那一份，和源码原文对不上，一个字都删不掉，**而且不报错**
（判据看起来装上了，其实什么都没剥掉）。用 `re.sub(r'"""[\s\S]*?"""', ...)`。

##### ⚠️ `_yt_at` 做对了自己那一半，调用方在外面一层又破了一次

`_yt_at` 对非 YouTube 的源**故意**回一句带汉字的说明（`<url>（片内 18.7 秒）`），
docstring 里写着「链接指错地方比没有链接坏得多」。可核对表**无条件**包上
markdown 链接语法，把那句说明连同括号一起塞进了 href：

    [▶](https://players.brightcove.net/…?videoId=6402850037112（片内 0.0 秒）)

渲出来是一颗**点不动的按钮**，而它和一颗点得动的长得一模一样——正是那个
docstring 要防的事，只是躲到了外面一层。**函数修好了不等于这件事修好了，
要顺着调用点走一遍。**

现在收在 `_jump_md(url, seconds, label)`：钉得住时刻的（YouTube）才给链接，
钉不住的**写成明文**让人自己拖进度条。判据钉两头——行为 + **源码里不许再出现
`[…]({_yt_at(…)})`**。只验行为拦不住这个错：`_yt_at` 的单测一直是绿的，
坏的一直是调用方。

本地渲染 HTML 不需要外网（图片会裂，无所谓，看的是排版）：

```python
pw.chromium.launch(executable_path="/opt/pw-browsers/chromium-1194/chrome-linux/chrome",
                   args=["--no-sandbox"])
```

沙箱里 `PLAYWRIGHT_BROWSERS_PATH` 指向的默认路径带版本号，和 playwright 自己找的对不上，得显式给 `executable_path`。

### 有原声的片子：说话时压，不说话时放开

用真实比赛画面剪的片子（`tools/build_match_reel.py`），**现场声不能全程一个固定音量**。
压到 0.14 的时候，球声和观众声在解说底下基本听不见——等于白留；调到 0.34 全程不变，
没人说话的段落又嫌闷。两头不讨好的根子是「一路压死」这个做法本身。

正解是**闪避**：没人说话时现场声开到 `BED_LOUD`，解说一进来
`sidechaincompress` 把它压下去，说完自动放开。

**这条对所有带原声的片子统一沿用**，不是这一条片子的特例。

配套的两条：

- **解说本身要少**。现场声撑得住的段落就别配音——球声、观众、球员的喊声本来就是
  内容。锦织圭那条最后只在**开头和结尾**各留一句，中间全交给现场
- **收尾那句要提前起**。末屏往往只有两三秒（握手那段 2.9 秒），装不下一整句；
  从上一段（庆祝）就开始说，跨进末屏

#### ⚠️ 而上面那条「收尾提前起」保住的几秒，被闪避自己吃掉了——七条片子

`sidechaincompress` **在 sidechain 输入结束的那一刻就收口**。于是 `[duck]`
只活到最后一句旁白说完，下游 `amix` 跟着收口，**整条现场声在那儿断掉**。
量九条已发的成片，视频流 vs 音频流：

| | 画面 | 音轨 | 差 |
|---|---|---|---|
| eala-fernandez | 95.60 | 91.10 | **−4.50** |
| wang-pareja | 94.12 | 91.14 | −2.98 |
| hewitt-washington | 123.96 | 120.98 | −2.97 |
| wang-samsonova | 84.60 | 81.94 | −2.66 |
| potapova-venus | 94.64 | 92.00 | −2.64 |
| wong-brooksby | 91.48 | 89.50 | −1.98 |
| eala-zheng | 85.84 | 83.89 | −1.95 |

**削掉的正是握手、庆祝、观众声那几秒**——也就是上面那条规矩专门保下来的
落点。九条发出去了七条，而微信那条消息收不回来。

**它为什么一直没被发现**：ffmpeg 不报错，画面是全长的，成片时长、体积、
封面、字幕全部正常。**只有把视频流和音频流的时长摆在一起**才看得见。
又一次「兜底出事的时候不吭声」。

改法是一个 filter：`[vk]apad[vkp]`，把喂给压缩器的那一路补静音到无限长，
压缩器就跟着 `[bed]` 走完。**只补 `[vk]`**（sidechain 那一路），
`[vm]`（真正听见的解说）不动。

- **判据必须真跑一次混音**（`test_现场声不许在最后一句话结束时断掉`）：
  20 秒有声画面 + 7 秒就说完的旁白，过一遍**生产用的那个滤镜图**。
  为此把滤镜图抽成了 `duck_filtergraph()`——查源码里有没有 `apad`
  只能防「有人把它删了」，防不住「它从来没工作过」
- **两条断言缺一不可，都反向验证过**：①「音轨和画面一样长」——去掉 `apad`
  变成 7.01s vs 10.00s；②「旁白之后那几秒真的有声音」——把输出换成补过静音
  的那一路，长度合格但响度 **−91 dB**，① 照样绿。**只验长度会被静音骗过去**
- **CI 要 `apt install ffmpeg`**（`ci.yml` 原来只装字体）。缺它这条测试红，
  不许 skip——一条常年跳过的检查和常年红是同一个毛病

**真 run 上验过了**（2026-08-02 重渲 eala-zheng，run 30710183596）：

| | 画面 | 音轨 | 差 |
|---|---|---|---|
| 已发（7.29，修之前） | 85.84 | 83.89 | **−1.95s** |
| 重渲（修之后） | 84.44 | 84.39 | **−0.05s** |

而且结尾那几秒**真的有声音**（t=80/82/83s 量到 −25~−29 dB），不是 `apad` 补出来的
静音（那会是 −91）。`check_reel_landed` 同一趟跑过，0 项不合格。

#### ⚠️ 淡入淡出 ≠ 淡到黑：转场是**溶解**，中间一帧黑都不许有

账号所有者要「切换或转场的时候要有淡入淡出，不要突然一下切过来」，我做成了
**每段各自淡到黑再淡回来**——于是每个接缝中间都有一小段全黑。反馈：
「接缝画面的时候有轻微的闪烁」。量出来一点不含糊，jodar-fritz 18 个接缝：

    t=62.025  亮度 102.25   ← 正常
    t=62.108        50.66
    t=62.191         0.00   ← 整幅纯黑
    t=62.274        24.50
    t=62.398       102.23

**「淡入淡出」在两段之间指的是溶解**（前一段直接化进后一段），不是各自去碰黑。
现在收在 `dissolve_filtergraph`：画面 `xfade`、现场声 `acrossfade`。

**长度账是这个改动的全部难点，也是它唯一容易错的地方。** 溶解会吃掉时间，
而旁白的 `adelay`、字幕的 cue、`check_reel_landed` 那句「画面总长 − 段落总长
＝ 封面」全按 `seg.length` 累加。所以每段**多切 `SEG_FADE` 秒**当下一个接缝的
底料（`cut_segment` 的 `tail`），`xfade` 正好把它吃掉：

    accₖ = accₖ₋₁ + (Lₖ+f) − f = … + Lₖ        每一段仍然正好占 Lₖ
    末段不留尾巴 → accₙ = Σ Lᵢ                  总长和硬切时一模一样

**末段那个「不留」是有讲究的**：给它也留尾巴的话总长会多出 `f` 秒，就得在别处
补一刀 `-t`——而「一个数写两处必分叉」。

- 判据 `test_接缝要溶解不许淡到黑` **真跑一次滤镜图**：三段纯色过一遍，量
  ①总长 ②接缝亮度 ③接缝音量。反向验证换 `transition=fadeblack` 当场量到
  `0.0`，正是账号所有者看到的那个东西
- 另加一条盯**位置**的：`cut_segment` 里不许再出现 `fade=`。只测行为拦不住
  「有人又把淡入淡出搬回每一段自己身上」，而那正是这次的错法
- ⚠️ 拼接那一步**不能再 `-c copy`**：溶解要两段同时在滤镜图里
- ⚠️ **溶解的底料也要查切点，但要和「段体跨切点」分开报**。段尾之后那
  0.18 秒跨了切点，溶解里会多叠进一两帧半透明的第三个镜头——和「整句旁白压在
  另一个人身上」差着量级。合成一档的后果是可预见的：为了压掉这条噪音去写
  `crosses_cut`，把段体那条真检查一起关掉。jodar-fritz 有 6 段是这样，
  **因为窗口本来就该切在源片自己的镜头边界上**，切点自然落在末尾一两帧之后

#### ⚠️ 「唯一一个」被写成「唯1个」——docstring 是对的，实现只做了一半

同一天的第二条反馈：「有些字幕没有补全」。两处，都是 `arabic_numerals`：

| 旁白 | 屏幕上 | |
|---|---|---|
| 生涯唯一一个巡回赛决赛 | **生涯唯1个**巡回赛决赛 | **真的少了一个字** |
| 上一个十几岁就走到这一步 | 上一个 **10几岁** 就走到这一步 | 约数被当成了数 |

第一处的根子很刺眼：那个函数的 docstring 第一段写着「『唯一一次』里连着两个
『一』，按数字读就成了『唯 11 次』。所以裸的『一』『两』一律不碰，**多字的串
也必须含十/百/千**」——**而实现里没有「多字的串」那一条**。`_num_value("一一")`
读出 1，配上量词就把两个字压成一个。量词从「次」换成「个」就漏了过去。

**规矩写对了，实现是空的**——和「是不是这一版那道闸恒真了一个月」同一个形状。
所以规矩落进 docstring 不算数，要落进**测试**。

- `十几` / `二十几` 这类：数字后面跟「几」是**约数**，不转
- 顺带把「一号种子」也归到数字那一路：同一条片子里有「7号种子」「3号种子」，
  只有它是中文，半中半洋（老账：「北京时间8月三号零点」）。卡死在「一号种」
  三个字上，为的是不误伤「统一号召」——判据宁可窄，不可宽
- 判据 `test_屏幕上的数字不许把字吃掉` 的下半张表是**不许被误伤的**那些，
  两个方向都反向验证过

#### ⚠️ 反向验证不许用 `git checkout` 还原——它会把没提交的新代码一起删掉

上面那条测试反向验证到一半，我用 `git checkout tests/test_match_reel.py` 还原
被改坏的文件——**那两条新写的测试就在这个文件里，还没提交**，一句话全没了
（重写了一遍才发现）。

改源码做反向验证一律 `cp` 备份、`cp` 还原；`git checkout` 只对**确定没有未提交
内容**的文件用。还原之后照旧要清 `__pycache__` 并用 `import` 验（不是 `grep`），
见上面那条。

⚠️ **2026-08-15 又栽了一次，而且是在读过这一条之后。** 反向验证豁免表的自检时，
我用 `git checkout -- tests/test_match_reel.py` 还原那个塞进去的名字——同一个
文件里躺着三处刚写完、**还没提交**的测试代码，一句话全没了。

这次多学到一样：**验还原成不成功，不能只查「那个塞进去的东西没了」。** 我当时
写的检查是「注入的那个字符串不在了 → 打印『还原 ok』」——它成立，因为
**整段代码都不在了**。要查的是**该在的东西还在不在**（`grep -c` 数一遍新加的
函数名），那才是产物。又一次「查产物，不查信号」，只不过这次的产物是我自己
刚写的代码。

#### ⚠️ 改签名要跟着委托链改到底——同一对函数栽第二次

`build_cover` 把活委托给 `build_versus_poster`，而 `_still_to_clip` 的调用在
**后者**里。这次加 `tail` 参数，我给 `build_cover` 加了签名、改了
`_still_to_clip(…, seconds + tail)`——**改的是委托出去的那一层**，于是
`tail` 在那儿是个未定义的名字。

本文件里「`_still_to_clip(poster, dest, seconds)` 那次」记的就是这对函数，
**同一个坑第二次**。这次是 `test_没有名字是凭空来的`（`ruff --select F821`）
当场抓住的——那条测试写的时候就是为这个。**它值回票价了。**

顺带：`s.count("tail: float = 0.0) -> Path:") ` 那种断言没拦住，因为
`cut_segment` 的签名也命中同一个串。**改代码的自检串也要够窄。**

#### ⚠️ 第三次，换了个法子：`build_cover` 有**两个** return，我只改了一个

同一天晚些时候又栽在这对函数上。`build_cover` 的两条出路都委托给
`build_versus_poster`——上面那个是 solo（网球有故事），下面那个是 VS
（赛场之上／开球之前）。我拿 `str.replace(..., 1)` 加 `tail`，**它挑中了上面
那个**，也就是这两个栏目都不走的那一条。

代价是整条片子：封面片段没有多切 0.18s，`xfade` 的 offset 正好落在封面流的
末尾，溶解链一路塌下去——**段落加起来 114.46s，成片 12.44s**
（run 30752134514 / 30752131692），而日志里一个字都没说。

- **`ruff F821` 抓不到这一类**：`tail` 在调用方的作用域里是有定义的，只是
  没往下传，被调方悄悄用了默认值。F821 管的是「名字不存在」，管不了
  「名字没传过去」
- **`replace(count=1)` 改多出路的函数是错的手法**。要么全改，要么改之前先
  数一遍有几处（`grep -n` 一秒钟的事）
- 判据 `test_封面那一路的溶解底料不许在委托链上掉队` 盯**每一个** return，
  并且要求被调方**真的用上**（`seconds + tail`）——「签名对了、实现是空的」
  是这个仓库的常客
- 真正把它抖出来的是**拼接那一步的自测**（每段先量一遍再拼）。
  没有那一步，这个 bug 的样子就是「成片短了」，而它已经骗过一趟了

#### 旁白写长了怎么查：`mode=narration`，两分钟，不碰源片也不写产物

这道闸比的是「TTS 时长 vs spec 里的段长」，**两样都不需要源片**。可它原来只活在
`render()` 里，于是想知道哪几段写长了就得跑一趟完整 render——而在**通过**的情况下
那一趟会顺手渲出一条新成片并提交，对已经发过的片子就是白多一个几十 MB 的 blob。

现在 `mode=narration` 只合语音、只报数：整趟 **1 分 31 秒**，语音落临时目录，
`output/` 一个字节不动，清理/上传/提交三步全跳过。它**一次列全所有超长的段**，
外加每段的余量（还标「很紧」）——比离线估准得多，见下。

#### ⚠️⚠️ 本地 `--check-narration` 和 runner 上的 render **不是同一个 TTS**，而 runner 那个更快

2026-08-15 在 `wangxiyu-timofeeva` 上踩的，**代价是一趟白跑的 render**：

    本地（沙箱，没有 AZURE_SPEECH_KEY）  → edge-tts
    runner（仓库配了 Secret）           → Azure，日志第一行写着 `[TTS] Azure（风格 无）`

同一段旁白（第 12 段）本地量 **6.96s**、Azure 只念 **6.50s**，**快 6.6%**。于是
本地那道哑场闸报「余量 3.74s，门槛 4.0s，**过**」，同一份 spec 到 runner 上变成
**4.20s，当场红**。

**也就是说本地那道闸对 runner 是系统性偏乐观的**，而且偏的方向正是危险的那一头
（本地放行、runner 拦住）。换算式：

    Azure 余量 ＝ 本地余量 ＋ 本地旁白 × 0.066

所以判据是：**本地余量落在 3.0~4.0s 之间的段，在 runner 上很可能就翻过 4.0**。
那条片子按「Azure 估算余量 ≤ 3.0s」重排，一次就抓出**五段**（本地看只有一段红）。

⚠️ **这跟「『很紧』也要处理」是同一条，只是现在它有了一个能算的数。** 我当时
看见 3.74 离门槛只差 0.26 却放过了——「离门槛只差零点几」不是「过了」，
是「换个合成器就会红」。

⚠️ **别把 `[TTS] Azure` 那行当噪音。** 它是这条线上**唯一**能告诉你「跑的不是
你本地验的那个声音」的地方；本地日志里对应的那行写的是别的。真要在本地量准，
只能配一份 `AZURE_SPEECH_KEY`（加到仓库 Settings → Secrets → **Actions** 那一栏
工作流才读得到，加到 Codespaces 那栏读不到而且不报错）。

#### ⚠️ 砍字的收益**不是线性的**——同一段我连着低估了两次

按「这一段自己的字速」反推该砍多少字，两次都不够：

| | 估 | 实测 |
|---|---|---|
| nishikori-shang 第 10 段砍到 19 字 | 4.02s | **4.75s**（仍超 0.25s） |
| nishikori-shang 第 1 段砍到 49 字 | 10.17s | **10.82s**（只剩 +0.02s） |

原因是**每句话都有固定开销**：首尾静音、句末降调。字数越少，这块占比越大——
那一段原文 42 字是 4.73 字/秒，砍到 19 字只剩 **4.0 字/秒**。收尾那一问尤其慢。

- **估余量要比线性算出来的多留一档**，砍完必须再跑一次 `mode=narration` 复核
- **「很紧」也要处理**。第 1 段 +0.02s 是过了，但换个音色、改一个字就翻——
  这条线的出发点本来就是「别再为返工付一趟渲染」
- **容差里的负数是真超了**：第 9 段 −0.09s 落在 0.12s 容差内所以放行，
  但字幕会被收进窗口**截掉尾巴**。放行不等于没问题

顺带三处纠正离线估（`words.json` 那套）：hewitt-washington 估它超 1.59s，
**实际装得下**；nishikori-shang 估第 9 段，**实际是第 10 段**；最严重的一段
估出来是 −4.91s，实测 −4.38s。**闸按真实段落算，不会错**——离线估只配用来排序。

#### ⚠️ `voice_NN.words.json` 的序号**不等于**第 N 个带旁白的段

想离线估「哪条 spec 的旁白超长」时按这个映射算，被自己荒唐的结果戳穿：
potapova-venus 报「7 个字要念 8.9 秒」。

原因是**产物目录跨多次重渲累积**：段数变了、封面配音加了又去了，旧的
`voice_*.words.json` 还躺在那儿。九条里三条对不上——

    nishikori-shang   带旁白 7 段，voice 文件 9 个
    potapova-venus    带旁白 7 段，voice 文件 10 个
    hewitt-washington 带旁白 7 段 + 封面 1 = 8，voice 文件 11 个

- **先验「文件数 == 带旁白段数 + 有没有封面配音」**，对不上就别按序号认领，
  宁可说「这条不知道」。那个荒唐值是运气，下次未必有
- 权威答案是**闸自己**：它一次列出所有超长的段（`942851e` 就是为这个写的，
  「a fix does not cost one six-minute render per segment」），
  两分钟就报出来——比离线估准，也够快
- ⚠️ `words.json` 的末事件**低估真实 mp3 时长**（wang-samsonova 第 1 段：
  8.34s vs 闸量到的 9.29s），而闸量的是 mp3。所以离线估出来的余量**偏乐观**

#### 「有音轨」不等于「有声音」——wong-brooksby 整片没有现场声

同一天查出来的第二条，形状是「补位的静音盖住真音轨」的**反面**：这次补位的
不是我们，是**源片自己**。

`_has_audio()` 只查**音频流存不存在**。wong-brooksby 的源片有流、有码率，
整条却是数字静音——成片按 3 秒一格采样，**29 个窗里 12 个低于 −70 dB**，
其余八条片子是 0/23~0/39。而 spec 的 `_source` 白纸黑字写着「带原声」。
整套 `sidechaincompress` 闪避对它是空转。

**而两条分支一句 print 都没有**，所以「源片是哑的」和「源片正常」在日志上
一模一样。又一次「兜底出事的时候不吭声」。

- **量信号，不量流**：`audio_peak_db()` 跑一次 `volumedetect` 取峰值。
  124 秒的片子实测 **0.81 秒**（只解音频），值这个钱
- 门槛 `SILENT_PEAK_DB = -60`：真实比赛音轨峰值贴近 0，数字静音是 −91，
  −60 离两头都远
- 照 `mixed_fps` 那套做**显式认领**（`silent_source: "为什么可以"`）。
  一律放行等于继续出哑片；一律拒绝会把纯视频轨的源片挡在门外——
  认领这一步是让这个取舍留下判据
- **报错要说出路**（补 `source_audio` / 换源 / 认领），判据把这三条也钉住了
- **判据必须真造两个文件量一遍**：`_has_audio` 对哑轨返回的是 **True**，
  查源码查不出这个差别。反向验证两次：退回「只查流」→ 不报错；
  报错文本删成一句「源片没有声音」→ 出路那三条断言红
- ⚠️ **没有给 wong-brooksby 写认领**。源片渲完就删了，分不清是它自带的音轨
  本来就哑、还是下载只拿到视频轨——**认领是要写清楚原因的，写不出来就别写**。
  重渲会撞上这道闸停下，那时源片还在手上，量一次就知道。
  测量结果记在 spec 的 `_audio_note` 里

#### 上面这两个缺陷，本来都该被拦下——检查脚本一次都没被调用过

`grep -rn check_reel_landed .github/` **零命中**。`tools/check_reel_landed.py`
一直在仓库里，工具自己的 docstring 还写着「只在成功时出声的检查，没法证明
它真的看过」——而**一个从来没被调用的检查比那还弱一档**。

补跑一遍九条已发的成片，它自己就把今天这两个真缺陷全报了出来：

    「音轨 X，比画面短 N 秒」× 7 条    ← 闪避少了 apad
    「封面之后还有 38 秒是数字静音」    ← wong-brooksby 的哑源片

现在收在 render 之后、**提交之前**（`test_查成片的那个脚本要真的被调用一次`
连位置一起钉）。提交进 main 之后 `push-reel` 就会把它发到微信，而那条消息
发出去收不回来。误拦的代价现在很小——上传那一步的 `always()` 保证成片照样
拿得到，重跑一次即可。

⚠️ **装闸之前得先把假红清掉，否则它一上来就是常年红。** `COVER_SECONDS`
一路从 2.6 改到 1.8 再到 1.2，而老片子是按 2.6 渲的；读不到 `render.json`
就退回今天这个常量，于是**八条老片子全报**「画面 X（spec 算出来 Y）」，
差值几乎恒定在 **1.40~1.44s**——而两条带 `render.json` 的一条都不中。

- **那批假红差点把真问题淹掉**：「音轨比画面短」就混在里面。
  一个笼统的解释盖住一个具体的 bug，又一次
- 改法不是调常量（下次再改还会错），是**拿不到就不判这一项**；
  下游要用封面长度的地方**从产物自己量**——画面总长 − 段落总长就是封面，
  比拿一个改过三次的常量准得多，也正是「查产物，不查信号」
- 清完之后：两条片子全绿，其余各条只剩真缺陷（含 7.28 那版 nishikori-shang
  的 `1080×1920`——它是改 3:4 之前的历史产物，7.29 重渲那版是干净的）

### 剪辑：死球之后再切，别在球还在飞的时候走

账号所有者的原话：「**很多球没有播放完成就切到下一个了，建议死球后再切换下一个，
不然让人看的不明不白的**」。

一分打到一半切走，观众不知道这分谁赢了——而**看懂这一分归谁**是回合镜头唯一的
作用。所以段尾必须落在**死球之后**，再留一点尾巴给结果和反应（默认 1.2 秒）。

**「死球」不用靠看球，记分条会说。** 源片烧死的记分条在死球那一刻才翻牌，
它就是判据，收在 `tools/find_point_ends.py`：量记分条那一小块的帧间差，
跳变的时刻就是一分结束。两个坑在实现里挡掉了——

- **镜头切走时记分条整块消失**（近景、慢镜、看台），那也是一次大跳变，但不是
  死球。所以只认「变化之后记分条还在」的那些
- **比分数字面积很小**，整块的平均差会把它淹掉。用「有多少像素变了」而不是
  「平均变了多少」，而且**门槛要在同一个口径下量**：框大了同一次翻牌的占比会
  掉一个量级

工具只给候选，**还要拿缩略图墙对一眼**：这一段是不是真在这儿结束。

#### 而这个工具一直是零调用方——它要的源片渲完就被删了

`git grep find_point_ends` 只有三处：脚本自己、一条「文件在不在」的断言、
以及 wong-brooksby 的 `_editing_why`。**没有任何工作流或代码调用它**，
而它的 `--video` 指的是**源片**——源片在「丢掉不进仓库的中间物」那一步被删，
想用它得自己另下一份 400 MB。于是每一处段尾错都要先付一趟渲染才发现，
那三处修正（`128.0→132.7`、`143.0→147.7`、`173.0` 正好切在赛点那一分中间）
就是这么攒出来的。

现在 probe **趁源片还在**顺手量一遍，写进 `probe.json` 的 `point_ends`
（probe.json 是进仓库的，源片不进）。

- **记分条位置没法自动认**（每家转播不一样），所以要人给 `--scorebox`。
  不给就跳过，**但要说为什么**——「没量」和「量出来一个都没有」长得一样
- 写错格式（少一个数）**要报错**，不许当成「没给」悄悄跳过
- **零命中要自证是真空**：把实测的 `moved` 分布和门槛一起打出来，并指出
  「多半是 box 框错」。不打的话，「门槛卡错」和「真的没有死球」在日志上
  一模一样——成片那条「找球场对称轴」正是栽在这儿
- 和 `--cover-only` 那次同一个坑：**开关也要在工作流里够得着**，
  判据把这一头也钉住了（`test_死球时刻要趁源片还在的时候量出来`）
- 成本：96 秒的片子实测 **7.5 秒**（0.1 秒一采样）

#### ⚠️⚠️ 2026-08-19：猜到的候选只印在日志里，我自己就漏过一次

账号所有者，同一天重申：「这个问题已经出现好几次了，非常影响流程。还有一个
最大的问题就是，剪辑的时候要等死球了再去切下一段视频，**切记**」。

来路：`bouzkova-jovic` 那条片子的 probe 阶段（run 95976088848）我**从没给过
`--scorebox`**。`suggest_scorebox()` 确实猜出了候选并打印出来——

    [记分条] 猜的候选：--scorebox 90,870,416,980（填充率 0.91，只是猜的，
    拿缩略图墙对一眼再填进下一轮 probe）
    [死球] 没给 --scorebox（记分条位置每家转播不一样，认不出来），
    这一项跳过。段尾就只能靠缩略图墙用眼睛定。

**但这句话埋在几百行 ffmpeg 进度中间的 run 日志里**，probe 那趟一结束就没人
会再去翻。`--dry-run`（我改完 spec 每次都会跑的那 0.2 秒）对这一层完全不
吭声——`point_ends` 空着，而「没给 `--scorebox`」和「给了但检出真的是空的」
在 `probe.json` 里长得一模一样，两者都是 `point_ends: []`。

**这不是又一个「工具写出来了没人用」——猜到的候选本来就在，只是没被存进
唯一会被再看一眼的地方。** 修法是让 `suggest_scorebox()` 把猜到的候选**返回**
（不只打印），probe 那一趟把它存进 `probe.json`（`scorebox_guess`），
`--dry-run` 翻出来当提醒：

    源 https://…：死球时刻还没量过——probe 猜过 --scorebox 90,870,416,980，
    还没有人拿它重跑一轮 probe 确认

猜不出候选（可能没有常驻记分条）时说法换一句，别把「猜不出来」和「猜到了
没确认」混成一句话。

⚠️ **仍然只报不拦，不是新增一道硬闸。** `--scorebox` 给了也可能因为记分条
滞后、镜头切走而检出为空——这正是本节上面「⚠️ 死球那条同理不能做成硬闸」
说的那件事，做成硬闸会重复 `crosses_cut` 那次的错（20 条已发 spec 里 72 处
未声明的跨切点，一做成硬闸就成了墙，逼着下一个人把真检查也一起按掉）。
这条改的是**信息该出现在哪儿**，不是判据的松紧——道理和「封面裁图先找官方
高清实拍」那节讲的「认领」不完全一样：那节后来把 `_frame_why` 从「写一句
就能过」升级成了硬闸（因为找官方图**几乎总是做得到**，不做只是偷懒）；
这一条不能照抄那个升级，因为 `--scorebox` 检出为空**有真实的技术原因**，
不是每次都能修好。

⚠️ **`bouzkova-jovic` 不重渲**（已经推送，消息发出去收不回来）。用
`tools/preview_segments_local.py --slug bouzkova-jovic --seg 4,5` 事后抽查过
那两段有争议的窗口，2 秒一格的分辨率判不清是不是切在死球上——这也是「还差
一步才闭环」的部分：`--dry-run` 现在会提醒「有候选没确认」，但确认本身
仍然要人拿缩略图墙或 `find_point_ends.py` 去核，机器不替这一步下结论。

#### 集锦不要为了短而短：关键点要交代清楚，且只在源片完整时才等死球再切

账号所有者 2026-08-12：「**集锦的长度可以不要太短，视频一定要交代清楚具体
关键点，同时要让每一个球完全结束后再切换下一个，如果原视频里是完整的话**」。

前两句是重申，不是新规矩——「片长不设上限」「不要砍片长」（见下面「100 MiB
是 git 的限制，不是内容的限制」那节）和上面这条「死球之后再切」这次被
一起提了一遍，说明这条线上要一直守住，不能因为在别处（比如压体积、赶
返工速度）图省事就悄悄往回缩。

**新增的是最后那个限定：「如果原视频里是完整的话」。** 上面那条「死球之后
再切」的判据（`find_point_ends.py` 靠记分条翻牌认死球）默认源片把这一分从头
播到尾。但**不是所有源片都这样**——有些官方集锦本身只截了击球前后那几拍，
这一分的死球时刻根本不在素材里，记分条翻牌那一刻已经被剪掉了。这种情况下：

- **不能为了凑一个「死球画面」去乱剪或者伪造一个结束点**——源片里没有的
  东西，剪辑变不出来
- 如实按**源片自己给的边界**切（回合开始/镜头切换点），不用假装等到了
  死球；这跟「窗口本来就该切在源片自己的镜头边界上」是同一条道理
- 判据仍然是 `find_point_ends.py` 那套——**先看这一分在源片里有没有被
  完整保留**，有就必须切在死球之后，没有就不强求，两种情况不能用同一把尺子量

#### ⚠️ 冷开场只是预告，正文必须重新兑现结局

账号所有者 2026-08-22：「迪亚福比赛视频的最后，感觉视频没有弄完就结束了，
这个不太好呀。后续要杜绝此类的问题，同时要写进库里，做真正的质检。」

那条片子不是音轨短了，也不是片尾页少停一秒。真正的问题更前一层：第一段冷开场
已经完整放过第四个赛点和庆祝；正文按时间顺序走到蒂亚福 40-0 后连续丢掉两个赛点，
却在第三个赛点开打之前直接切品牌片尾。**结局只在预告里出现，正文停在悬念上**，
观众当然会觉得视频没做完。

以后凡是从源片后段抽最后一球／赛点／捧杯做冷开场，必须守两条：

- **冷开场可以重复，结局不能省。** 正文仍要按时间顺序重新放到完整制胜分、
  结果确认和第一波庆祝；“开头已经看过”不能当删掉正文兑现的理由
- **品牌片尾只能接在故事闭合之后。** 最后一段可以没有中文旁白，现场欢呼本来就是
  内容；质检看的是比赛有没有真的结束，不拿“最后几秒只有现场声”误报成哑场

硬闸在 `ending_payoff_problem()`：`match_review` 的第一段只要比**后续任一同源片段**
更晚，就按“后段冷开场”处理；不再用“第一、二段至少差 20 秒”这个会漏掉小倒序、
也会被第二段仍在赛后时刻骗过的经验值。没写 `source` 的段按 `sources` 第一条主源
归一化，多源片之间不互比时间码。正文最后一个同源片段必须重新走到冷开场末尾
（0.25 秒切点容差），否则 `--dry-run` 在下载源片之前直接失败。

每一个后段冷开场都必须显式写 `_ending_payoff_required`，不能拿“没有英文 `quote`”
或“这段有中文旁白”替机器猜画面语义：包含最后一球、结果确认或庆祝就写 `true`，
此时正文窗口的**并集必须完整覆盖冷开场整个源片区间**；只在后面补一段握手、把
最后的 `end` 做大也过不了。只是后段高光、不承担结局预告才写 `false`，基础的
“正文走到它之后”仍然要守。

硬闸落地当天扫出的 16 条存量问题挂在 `LEGACY_MISSING_ENDING_PAYOFF`，**合并后只许
减不许加**。这张表只给全仓旁白盘点读取旧 spec；`render`、`--dry-run` 和
`--check-narration` 都不吃豁免，所以同 slug 再改或重渲仍会当场红，不能拿旧片名
放过新问题。另有 120 条旧冷开场尚未人工补 true/false，逐条钉在
`LEGACY_UNDECLARED_ENDING_PAYOFF`；它同样只给全仓盘点用，生产入口不豁免，且只能
随着人工分类而减少。蒂亚福这条已经把 330.88–350.64 秒的完整制胜分、庆祝和对手
反应补回正文。

### ⭐ 平台整改后的「赛场之上」：从高光剪辑改成原创比赛复盘

账号在 2026-08-06 收到过「疑似对他人/网络素材简易加工、二次创作信息增量不足」
的处理结果。平台提示是这次整改的**风险信号**，不是一条可以证明算法因果或保证
流量恢复的官方定律；项目采取的改法是把「我们为什么这样剪、这场球为什么这样变」
真正放进视频，而不是只在小红书正文里写几句解释。

整改期新做的「赛场之上」必须先认领一份编辑合同：

```json
"editorial": {
  "mode": "match_review",
  "question": "这场球最值得复盘的问题？",
  "thesis": "一句可被比赛画面和资料支持的中心判断。",
  "beats": ["前段走势", "中段转折", "结局与回应"],
  "human_context": {
    "angle": "这一场独有的场外切口，不套固定模板。",
    "facts": ["一到两条能核验的新闻、人物或现场事实。"],
    "sources": ["官方新闻、赛报、采访或赛事页链接"],
    "voice_label": "可选的口语化概括，不冒充官方称号。"
  }
}
```

- `question` 不是标题换一种写法，而是整条片子要回答的一个比赛问题；`thesis`
  不是「很精彩」这种空评价，要说清谁改变了什么、对手怎样应对、最后怎样回应。
- `beats` 至少三个，按「先发生什么 → 哪里换手 → 怎么收」排列。它们是选段和旁白
  的骨架，不是把每一局比分逐局念一遍。
- 每条片子还要做一次**本场专属的场外/人物/新闻补充**：从主场压力、家乡球迷、
  里程碑、复出、宿敌、票房、赛程背景、采访金句、球员个性等方向找一两个真正和
  这场比赛有关的切口。不同选手、不同比赛必须换角度，禁止把“菲律宾球迷”“门票售罄”
  之类的例子复制成固定模板；没有可靠热点就不硬塞，宁可写有来源的人物或赛事细节。
- `human_context.facts` 必须有来源，优先官方赛事新闻、WTA/ATP 赛报、赛后采访和赛事页；
  `voice_label` 只是本号的口语化表达（例如“带票女王”），不能写成选手的官方头衔。
  场外事实进入旁白时要像人在讲现场见闻，不能变成新闻摘要或数据清单；最好用对应的
  看台、球员表情、入场、庆祝等画面承接。
- 开头默认先给一个**人会想听下去的场景、反差或问题**，不要用“北京时间＋赛事＋比分”
  作为机器式开场；时间、赛事和轮次由顶栏持续交代，旁白负责把观众带进这场球。
- 每个战术或心理判断都要能回到记分条、官方赛报、统计或按帧画面；「她故意这样发」
  这类意图没有证据时只能写成谨慎的观察，不能把猜测当事实。
- 画面负责证明动作、人物和比分；旁白负责解释变化、意义和回应。禁止用「画面里是」
  之类话术复读观众已经看见的内容，也禁止只把比分换成中文再读一遍。
- 默认保留现场球声和观众声，**不加背景音乐**；旁白是叙事主线。用户提供真人录音
  时优先用真人录音；没有真人音频时，现有语音合成只能作为待审核版本，不能把“有配音”
  本身宣称成已经完成原创。

#### ⭐⭐ 美网期间的「赛场之上」：比分板走带式 ＋ 逐段现量，账在 `docs/us-open-scoreboard-aspect.md`

账号所有者两句话，隔一天：「**美网期间的比赛都用这个比例做视频**」（2026-08-28）、
「**以后美网期间的「赛场之上」比赛视频比分板都按这个方式处理**」（2026-08-29）。
全套账、每一次翻面的实测、被否掉的做法都在那份 doc 里，**动这块之前先读它**，
这儿只留够用的三句：

- **版式**：美网的比赛 spec 一律 `"layout": "band"` ＋ 顶层 `scorebox`
  （顶栏和字幕移到画面外的上下黑带，画面裁 6:5），硬闸在 `parse_segments`
- **比分板**：居中窗口固定切掉板左边 **208px**（名字那一段），所以从同一帧
  把整条板抠出来**按原比例**贴回左下、纵坐标不动。⚠️ **右缘是渲染时逐段
  现量的**（`resolve_board_insets`，板每打完一盘 +38px），顶层 `scorebox`
  只给左缘、上下沿和兜底右缘
- **每一段都要显式表态** `score_inset`：`true`（主路）／`false`＋
  `_score_inset_why`（板真的不在，回放/切走/全屏图形）／`{"x2": N}`＋理由
  （现量不准时人钉死）。**漏写当场红**——「忘了写」和「想过了不贴」在产物上
  长得一模一样

#### ⭐⭐ 背景音乐走**平台曲库**，不烧进片子（账号所有者 2026-08-29 选的「第二种方案」）

来路：2026-08-28 他要过「加一点背景音乐，音量 4%」，混音那一层做好并量过了
（`MUSIC_GAIN_PCT` / `music_chain` / `music_problem`），**卡在拿不到一份有授权
的曲子**——他点名的那首是商业版权歌曲。我摆了两条路让他选，2026-08-29 他选了
第二条：

| | 怎么走 | 代价 |
|---|---|---|
| A 烧进片子 | 有授权副本放进 `assets/music/`，spec 写 `music` 块 | 音量精确可控（4.0%）；**要自己解决授权，且会被平台的音频指纹扫到** |
| **B 平台曲库**（选中） | 成片**不带音乐**，上传到视频号／抖音之后在它们**自己的曲库**里加 | **不用重渲、不用找授权**；音量由平台定，压不到 4% |

⚠️ **B 的代价要照实说，别照抄我当初那句「不会吃版权判罚」**——那句写得过硬。
查过之后的准确说法是：视频号的曲库由 QQ 音乐（腾讯音乐）供，**平台已经拿到
授权**；抖音曲库同理，**但那份授权是「限抖音站内自用」**——个人非商业内容平台
兜底，**机构号／商用仍然不是零风险**。也就是说 B 把风险**压到很低**，不是压到零，
而且**换个平台就不带着走**（同一条片子发到别处不能沿用那首曲子）。

⚠️ **B 的操作在上传那一刻，是人做的，不在自动链里。** 自动链只把片子发到微信
（PushPlus），视频号／抖音本来就是手动上传——加 BGM 是那一步里点一下「音乐 /
背景音乐」从曲库挑一首，每条片子各挑各的。

⚠️ **所以成片这一路默认永远不带音乐**：`music` 块是**认领过才走的例外**
（`music_problem` 要求写 `_license`），不是可以顺手打开的开关。判据
`test_背景音乐默认走平台曲库不烧进片子` 钉两头——存量一条 spec 都不许带 `music`
（真要走 A 得显式把它加进豁免表，那一刻就是一次看得见的决定），以及
`music_problem` 的报错要指着 B 那条路说话。混音那套实现**留着不删**，它是 A 的
现成能力，只是不再是默认。
- 小红书正文和视频旁白必须各自承担信息增量：正文不能只是把旁白复制一遍，且继续使用
  纯文本，不写 Markdown 表格或标记。
- **转折点前面先给一句可核实的技术性归因，再进比赛细节。** 不止说"她反弹了"，
  要说清"靠什么反弹的"——一发/接发/心理层面某个具体指标的变化，能回到统计或
  记分条上核实的那种，不是"状态回暖"这类空话。
- **收尾要堆叠至少两个不同维度的成就数字，不能只用一个。** 维度包括对手实力
  （击败过多少次种子/世界前十）、赛事级别（第几座大师赛/巡回赛冠军）、生涯跨度
  （从哪年/哪岁到现在）——只写一个"生涯第N冠"不够，几个不同维度的数字叠在一起
  才有"这不是一次孤立的赢球"的分量。
- **有具体年龄或年份可用时，优先用两个数字做书挡**收束选手的成长弧光（例如
  "从19岁到22岁"），不写"多年来""一路走来"这类没有数字的空话；用之前照旧要
  按「查得够深、查得够广」那条核实这两个数字本身。

  以上三条 2026-08-24 从同一场比赛的另一个账号（网球青春）反推出来，
  完整的对比分析和不采用的两条（开场纯事实堆叠、无问号抒情收尾）见后面
  「别人怎么配文案」章节的「第四个账号」小节。

#### 比分顶栏仍然保留，但它是版式信息，不是原创增量

比赛顶栏继续做，并按以下固定合同实现：

- **不覆盖封面**：从封面结束的第一帧开始显示；
- **不覆盖品牌片尾**：在比赛画面结束时停止，`关注网球时差` 片尾原样保留；
- 第一行使用标题字体 `得意黑`，第二行使用正常中文字体 `Noto Sans CJK SC`；
- 两行分别表达赛事/轮次和「赢家 + 完整比分 + 对手」，例如：
  `WTA 1000 加拿大站 第二轮` / `伊埃拉 6-1 4-6 6-2 帕克斯`；
- 第二行必须与 `cover.winner`、`cover.result`、`cover.matchup` 机械一致，不能在顶栏
  另写一份可能漂移的赛果。`build_match_reel.py` 会在渲染前拦截不一致；
- 顶栏进入 `spec.topbar`，原创复盘进入 `spec.editorial`，二者都在下载源片之前校验。

整改版交付前的最低检查是：打开成片确认顶栏只在比赛区出现、片尾没有顶栏；听一遍确认
现场声没有被音乐盖住、旁白解释了至少一个转折；再确认旁白、顶栏、封面和小红书正文的
比分与赢家一致。通过这些检查不等于平台一定恢复流量，只代表这条片子已经完成了本号
能控制的内容整改。

### 旁白要把比赛走向讲清楚

账号所有者的原话：「**同时要讲清楚比赛的走向，这样大家才不会看的一头雾水**」。

赛报片不是集锦：观众要能顺下来「谁先领先 → 谁追上来 → 在哪一分转向 → 怎么结束」。
只报几个孤立的比分（「五比一」「赛点」）等于把线索抽掉，画面再好也串不起来。

所以每条片子的旁白要能回答这四问，而且**按时间顺序**：

1. 这是谁跟谁、什么分量（排名、种子、轮次）
2. 前半段是什么走势
3. **转折在哪儿**——这是最容易漏的一条，也是「一头雾水」的根源
4. 怎么收的、意味着什么

比分要跟画面对得上：说「三比三之后他反了过来」，那一段的记分条就该是 3-3 之后。
记分条是烧在画面上的，对不上观众一眼看得出。

#### ⚠️ 「四个点」不是「每一局」——被读者当众吐槽的那次

上面这四问，我在 `eala-svitolina` 上执行成了**把每一局按顺序念一遍**。
小红书读者的评论：

> 「她发球，她接发。。。她发球，她接发。她发球，对面接发。。。😂😂😂😂」
> 「这解说，，」

**几乎是原文引用。** 量下来他一点没夸张：那条片子 12 段里 **7 段（58%）**在报
谁发球，67% 的段落只是把记分条读了一遍。

根子是我把「讲清楚走向」读成了「不许漏掉任何一局」。**走向是四个点，不是九局
流水**——中间那些保发、回破、平分，记分条上一直写着，念出来一个字的信息都不加。
这跟已经被禁的「画面里是」是**同一族**，只不过那条指的是画面，这条指的是**烧在
画面上的记分条**。

- 判据 `test_旁白不许把每一局都念一遍` **卡密度，不卡单次**。单次往往是有信息的：
  「轮到自己发球，三十比四十——帕雷哈的第二个盘点」讲的是**保发被逼到悬崖**，
  和「破发点没兑现」完全是两回事。**第一版我禁了任何一次出现，当场误伤这句
  好台词**——又一次「判据宁可窄，不可宽」
- 门槛 25% 是量出来的，两档分得很开：
  ❌ eala-svitolina 58% ｜ ✅ wong-gea 20%、wang-pareja 11%、最近三条 0%
- ⚠️ **豁免名单也要自证它豁免的是真的超标**。我按更宽的正则以为另外两条也超标，
  顺手写进名单，`_BOARD_LEGACY` 的自检当场报「wong-gea 已经不超标了」。
  名单里最后只剩一条——正是读者截图的那一条
- ⚠️ **它拦不住「平淡」本身**。顺序、有没有立场、钩子留没留都是编辑判断，
  机械挡不住（见「最硬的那个事实放第 ① 屏」为什么故意没有测试）。这条只拦
  一种**能量出来**的坏

**这次的教训比那条测试大**：仓库里两条规矩会打架——「讲清楚走向」和「不要平白
叙事」。前者写得具体（四问、按时间顺序），后者写得抽象（要有情感、每一拍留一个
问题），于是我按具体那条做，做成了后者明令禁止的样子。**两条规矩相抵时，先问
读者会不会想看下一句**，别问自己有没有把信息交代全。

### ⭐ 旁白要有技战术拆解，不能只停在比分走势上

账号所有者 2026-08-21，甩来一条讲科斯秋克逆转安德列耶娃辛辛那提八强的参考视频：
「配音文案，要有技战术拆解，可以参考这个视频文案，再加上关键转折点比分和对应
视频」。

⚠️ **那条参考视频的文案没有拿到手**——分享链接在这台沙箱里连撞了三种技术路子
都没通（分享跳转页触发人机验证、接口直接返回 `blocked`、桌面版分享页是纯前端
渲染的空壳）。所以下面这条不是照抄参考视频的写法，是账号所有者自己那句话
本身——**没查到的东西不能编，见「编出来的判据 + 正确的结论」那条**。

上一条「旁白要把比赛走向讲清楚」的四问（谁跟谁、前半段走势、转折在哪儿、
怎么收的）回答的是**发生了什么**；这一条要求的是**为什么会发生**——那一分
是怎么打丢/打赢的，球路、落点、正反手、上网还是底线相持，谁在关键分上改了
打法。两者不是同一件事：走势是叙事骨架，技战术是骨架上的血肉，缺了后者读起来
仍然是「谁领先了几比几」的流水账，只是流水账的颗粒度更细了一点。

**判据是转折点那一分能不能回答"怎么打丢/怎么打赢的"，不能只停在比分上：**

| 只讲走势（不够） | 加了技战术拆解 |
|---|---|
| 「三比三之后她反了过来」 | 「三比三那一局，对手连续两拍回球出界——她把球压深、逼对手多跑半步，这一局起主动权换了手」 |
| 「决胜盘她救下三个盘点」 | 「第一个盘点靠一记直线制胜分解掉，第二个盘点对手主动失误，第三个盘点她发球上网抢到了截击」 |

（上面两行是本节自己写的示意句式，不是引用任何一条已发片子或参考视频的原文，
写新片子时要按当场核实过的球路重写，不能直接套用。）

- **技战术描述要能核实，不能凭"通常这样打"编**——画面里看得出球路/落点/
  正反手就用画面核，画面看不清就用逐分数据反推（`|B1|` 破发点 `|B2|` 盘点
  `|B3|` 赛点密集出现的那几分，就是该展开拆解的地方，认法见「转折点不许剪掉」
  那条）。这是「TNNS 有击球方式」栽过的坑的正面版本——**没验证过的球路描述
  写进旁白，观众看画面会当场发现对不上**
- **转折点的比分要能对上视频里那一秒的画面**，和「比分要跟画面对得上」是
  同一条规矩：写"三比三之后"，那一段的窗口就要真落在记分条显示 3-3 之后的
  那几秒，不能是"意思接近"的另一段回合镜头
- **拆解不是每一分都要展开**，和「旁白不许把每一局都念一遍」是同一条边界——
  只在真正的转折点上展开技战术，其余局分照旧一句带过，别把"技战术拆解"读成
  "每一分都讲怎么打的"

### ⭐ 采访不进复盘——集锦尾巴的采访是赛后开麦的素材，不是复盘的情绪佐料

账号所有者 2026-08-10 review 当天的片子：「**比赛信息很少，很难把比赛过程
讲述清楚，却把采访放上了，采访要放到赛后开麦里啊**」。

中招四条（rybakina-samsonova 两段、alexandrova-sabalenka / osaka-mertens /
swiatek-kostyuk 各一段），共同点很整齐：**quote 窗口全部落在源片 310 秒
之后**——WTA 纯集锦定长 310 秒，尾巴上的采访被当成「赢球后的情绪渲染」
剪进了复盘。rybakina 那条最重：73 秒里采访占 22 秒（30%），而**决胜盘
一句过程都没讲**。同班次的 svitolina / shnaider 两条零采访、每盘有交代
——同一条管线做对过，错在选段不在工具。

**栏目是承诺**（本文件早有）：复盘回答「这场球怎么赢的」，采访回答
「她赛后说了什么」。两个错叠在一起：塞进复盘的采访挤掉了比赛过程的
篇幅；同时把赛后开麦的素材烧掉了——本来能另出一条，矩阵白丢一条片。

- **发现集锦尾巴带采访（>310 秒的超出段），正确动作是记成赛后开麦候选
  另出一条**，不是塞进复盘
- 复盘里 quote 段只有两种合法身份：**broadcast**（转播解说原声，弗里茨
  决赛那种冠军宣告）、**ceremony**（颁奖/夺冠时刻现场声）。spec 里认领
  `_quote_kind`，判据 `test_赛场之上的quote段不许是赛后采访`（legacy 四条
  只许减不许加，表有自检）
- **腾出来的篇幅还给比赛过程**：每盘至少一句，决胜盘/转折局要有具体
  比分细节（逐分数据里都有）——「旁白要把比赛走向讲清楚」那四问的
  第②③问，rybakina 那条正是漏在这里
- 已发的四条不重渲（消息收不回来）；被烧掉的采访不再另出赛后开麦
  ——观众已经在复盘里看过那几句，再出一条是炒冷饭

#### ⚠️ 2026-08-15 账号所有者重申了一遍，而且范围比上面这条宽

原话：「**赛场之上视频不要赛后采访**」。**重申过的事就是决定**（本文件开头那条），
所以这里只记它比上面宽在哪儿：

上面那条判据钉的是 **`quote` 段**（`test_赛场之上的quote段不许是赛后采访`）——
它拦的是「把采访的**原声**当情绪佐料」。而这句话说的是**画面**：采访那一段的镜头
一秒都不该进复盘，哪怕不配原声、只当 B-roll 垫在旁白底下也不行。

**所以选段的时候先把采访那一段的时间轴划出来，整段排除在窗口之外。** WTA 单场集锦
的形状很固定（纯集锦定长 310 秒，超出的那部分才是采访），`arango-venus` 是
451.7 秒里比赛画面只到 297.8 秒、298.5–441.6 秒是场上采访——十四段窗口全部收在
294.0 秒以内，写进 spec 的 `_editing_why` 当判据。

### 别人怎么配文案：真实数据 + 城市背景 + 情绪落点，不是把比分再抄一遍

账号所有者 2026-08-08 甩来两条同讲伊埃拉胜麦克纳莉那场（就是我们发过的
`eala-mcnally`）的抖音链接，原话：「**你看下人家怎么配的文案，除了比赛关键内容
还有背景交代等等细节，不要机械的只说比分细节**」。

两条都下下来看了（抖音的分享短链在这台沙箱走不通，`v.douyin.com` 直接
`EGRESS_BLOCKED`；解法是带浏览器 UA 的 curl 跟着 302 走到
`iesdouyin.com/share/video/<id>/?from_ssr=1` 这个 SSR 页面，`desc` 字段和
`play_addr` 直链都在返回的 HTML 里，不用登录）。

**账号「林林林妹668」那条（233 秒）几乎每一镜都有一句烧在画面上的短字幕**，
连起来是一条完整的叙事线，而不是我们那种「首盘…次盘…决胜盘…」的流水账：

    伊埃拉用七连胜公布了答案 → 麦克纳利素来擅长阻力对抗 → 首盘伊埃拉迅速进入状态
    → 以6:3顺利拿下 → 医护人员立刻进场为其紧急治疗 → 伤病带极大限制了她的移动速度
    → 连丢两局 → 以5:7被麦克纳利扳回一盘 → 攻防端全面回暖 → 最终以6:4锁定胜局
    → 麦克纳利首盘一发得分率低至27%，但全场一发得分率提升至53%
    → 现场为伊埃拉助威 → **多伦多是菲律宾人口最集中的城市之一，依托早年劳工移民**
    → （她自己的赛后采访原声，双语字幕）"I'm doing my best to power through
       and be a professional and do what I know how to do"

账号「赛场网球观」那条（27 秒，短平快版）开头就是一句能算出来的狠数据：
**「伊埃拉总分仅仅领先对手4分」**——三盘 6-3 5-7 6-4，把每一分都摊开数，
赢家和输家加起来分差只有 4 分，比单读比分更能说明这场球有多胶着。正文里还有
「全场打出27记制胜分，13个破发点兑现7次」这类真实技术统计。抽帧核实过
「泪洒赛场」不是夸张——她赛后确实用毛巾捂脸、抬头忍泪，画面里看得见。

**拿这两条和我们自己发的 `eala-mcnally.xhs.txt` 对着看，缺的是三样，不是笼统的
"不够生动"：**

| 他们有、我们没有 | 具体长什么样 | 我们的数据缺口 |
|---|---|---|
| **算出来的聚合数据**，不是罗列比分 | 「全场总分仅领先对手4分」——把三盘的每一分加总再比 | 这个数我们自己也能从逐分数据算，只是没算 |
| **真实技术统计**，织进叙事而非单独列一条 | 一发得分率 27%→53%、27 记制胜分、13 个破发点兑现 7 个 | `flashscore df_st_1`／WTA 官方 `/stats` 接口本来就有，CLAUDE.md 早记过这条路，这次没用 |
| **城市/人群背景**，解释"为什么这里的球迷这么疯" | 多伦多是菲律宾人口最集中的城市之一，源于早年劳工移民 | 我们写的是「菲律宾球迷坐满看台」这个现象，没往下挖"为什么是多伦多" |
| **收在球员自己的话上**，不是收在下一轮对手上 | 赛后采访原声：情绪起伏、咬牙坚持、保持职业化 | 我们收在「下一轮迎战本西奇」——信息对，但情绪落点让给了赛程 |

**不是"文笔不够花"，是三类可核实的内容没有进去：一个算出来的数、几个真实技术
统计、一段查得到出处的背景事实。** 这些都是可以按 CLAUDE.md 现成的规矩去做的
（全称断言要信源、赛后采访原声可以用），只是这次的选材和文案没往这几个方向找。

⚠️ **已发的 `eala-mcnally` 不重渲**——消息发出去收不回来，这条只管以后。
往后写「赛场之上」「网球有故事」的 `xhs.txt`／旁白时，动笔前先问一句：
**这场球有没有一个算出来的狠数据？有没有真实的技术统计能织进转折那几拍？
这个城市/这群观众背后有没有查得到的背景？收尾是不是让位给了球员自己的话，
而不是习惯性地转去讲下一轮对手？** 四问答不上来不强求，但要真的问过。

**第①问（狠数据）现在有机械半截了，别手算。** 先跑
`python tools/match_stat_hooks.py --from-spec <slug>`——它从 spec 的
`_match.flashscore_id` 和 `cover.matchup` 读出 id 和对阵名，一次给全
总分差 / 一发摆动 / 破发点兑现 / 连续保发 / H2H 五类候选（每条带来源）。
「赛场之上」的 spec 必须把结果落进 `_hit_data`（记跑出来的候选，或挑进正文
的那条），查过确实没有就写 `_no_hit_data_why` 说清为什么（首次交手 / 低级别
无统计）——**「忘了跑」和「查过没有」在产物上分不出来**，判据在
`test_赛场之上要么有狠数据要么说清为什么没有`。挑不挑、怎么写进钩子仍是
人的判断，闸只认领「查过没有」，不替人决定「写不写」。

#### 边界：只学文案和叙事、剪辑节奏，字幕的位置和配色不学

##### 2026-08-24：屏幕贴文案定稿为 `story_text`，大块信息板废弃

账号所有者在郑钦文美网资格赛片终审时指出：「视频上贴的文案没有设计且感觉
很乱」，并要求调研市面优秀效果后全工程复用。问题不是“圆角还不够精致”，而是
那版把 62% 画幅宽的深色 UI 面板连续压在真实人物上，标签/主标题/证据又和底部
字幕同时争注意力；全角竖线还因子集字体缺字变成方框。

现在统一走 `tools/render_story_info_band.py` 的**无大底板编辑型文字锁定**：短绿轨
+ 主标题 + 可选排名/硬数字 + 一行证据，白字靠暗描边/投影适配复杂底图。spec 必须
认领 `inset.kind=story_text`，固定 `width=0.56 / pad=0.085 / show_for=3.8 /
motion=editorial`；解析闸会拦偏离。四种语义变体只有 timeline/player/stat/chapter，
不许每条片另起一套视觉。完整研究、来源、字数预算和发布前检查见
`docs/short-video-text-overlay-system.md`。

##### 原始边界（2026-08-08）：不照搬参考账号的字幕审美

账号所有者当天补的一句：「**不要参考他的字幕位置和配色，感觉没有美感。
主要参考文案和叙事方式，以及画面剪辑和速度快慢放等等**」。

这两条账号的字幕是贴底通栏黄字／贴顶通栏黄字，配色和排版跟本仓库「字幕待在
3:4 画面内部、上锚、150px 安全边距」那一整套是两条路——**不许把这两条的
视觉样式带回来**，学的只是**内容组织方式**（上面那张对照表）和**剪辑节奏**。

⚠️ 剪辑节奏这条，量出来的结果和"快剪快切"的第一印象**正好相反**。拿
`ffmpeg select='gt(scene,0.35)'` 对「林林林妹668」那条 233 秒的片子做真实的
镜头切点检测（这是仓库里 `scene_cuts` 本来就在用的同一套阈值），**全片只有
4 个真实镜头切点**，切出来的五段分别是 5.3s / 58.2s / 33.0s / 63.2s / 73.3s
——**没有一段短于 5 秒，中段甚至长达一分钟**。而联系表上看着"每格都有新字幕"
的密集感，来自**同一段连续转播画面上滚动叠加的多句短字幕**，不是频繁切镜头。

**结论是：别学"剪得快"，那不是真的——该学的是"一段长镜头上能压几层信息"。**
我们自己的分段（`build_match_reel.py` 的 `segments`）目前一段配一句旁白；
这条参考提示的方向是**同一段画面窗口内，字幕可以比旁白语速更密、分多句
先后叠现**（类似他们「首盘伊埃拉迅速进入状态」→「以6:3顺利拿下」这种
前后两句压在同一镜头上），而不是逼着去多切镜头。这条目前没有落成判据——
是不是要在同一分段里叠加多句短字幕，属于编辑判断，先记下来，下次写 spec
时留意就好。

#### 第三个账号：这次是真快剪，而且"证据"不止真实赛场画面

2026-08-11，账号所有者甩来「2发网球」讲加拿大公开赛（为什么分属多伦多和
蒙特利尔两座城市）的抖音链接，要求同样是「你看看别人怎么做网球有故事的」。
这条和上面「林林林妹668」正好是**反例**——那条量出来只有 4 个真实切点、
密集感全来自叠字幕；这条**真的是快剪**：`scene_cuts` 阈值 0.35 量出来
**135 个切点／288 秒 ≈ 每 2.1 秒一切**。上一条的结论（「别学剪得快，学
一镜压几层信息」）不能当成放之四海皆准的判据——**剪辑节奏是不是真快，
每条都要量，不能因为学过一次「原来是叠字幕」就默认下一条也是**。

这条片子和我们同一天刚发的 `cincinnati-story` 是**同一个体裁**——都是
"赛事本身的故事"（不是球员个人故事线），直接可比。它比我们多用了三类
"证据"，而这三类我们的 `cincinnati-story` 一个都没有（`cincinnati-story`
全片只有源片集锦画面 + 旁白，没有任何一屏是"另一张图"）：

| 证据类型 | 这条片子怎么用的 | 我们现在有没有 |
|---|---|---|
| **历史实物照片当论据** | 讲"长期依靠烟草资本扩张"时，画面直接切一张 Rothmans 香烟包装的实拍照片（配 `2003` 年份角标）——这不是网球画面，是佐证这句话的原始材料 | 没有。`_facts` 里核过的事实全部只靠旁白口述 |
| **官网截图 + 高亮标注** | 讲场地便宜时，截一张官方购票页原图，把关键数字（"室外硬地1小时约合人民币50元"）用黄色描边圈出来，旁白同时点出"超过6万小时的低价场地" | 没有。我们有同类的数字（`_facts` 里的费德勒七冠、鏖战3小时49分钟），但从没在画面上"亮出信源截图" |
| **新闻引语配来源署名** | 一段引语配着屏幕角落的 `Voice of Maite Ormaechea / CBC News`，把"这话是记者说的"这件事本身摆在画面上 | 没有。我们引用 WTA/CNN 的事实只写进 `_claims`，不上画面 |

**这三类都不是"编出来的"——都是可核实的原始材料**，和 CLAUDE.md 别处「图片
必须打开看过、四道闸门」那套要求完全兼容，只是**它们不是网球画面**。
我们的选题库（`_facts`/`_claims`）已经在按同一套证据标准核实事实，只是
产出的时候全部压缩成了旁白一句话，没有把"证据本身"也做成一屏。

第四处技术：**片头那句标题从头到尾常驻画面**（「加拿大公开赛 / 为什么分属
两座城市？」在 288 秒里没有消失过），和我们"封面钩子只停 ~~1.8~~ **1.2** 秒、之后
画面上就没有标题"的做法是两条路。这条**不建议照抄**——常驻标题会一直
占掉画面上方一块，而我们的字幕本来就要求"留够安全边距"；两者叠在一起
挤占空间会更紧。记在这儿是给"要不要试"留一个真实参照，不是建议照做。

⚠️ **同样"不许学"的部分**：这条账号的字幕仍然是通栏白底黑字、贴底，
和上面「边界」那条一样，**只学证据类型和剪辑节奏，不学字幕位置和配色**。

#### 第四个账号：同一场球，别人怎么写「赛场之上」——三条能直接搬，两条不学

2026-08-24，账号所有者甩来「网球青春」（抖音号 tennislife1314）讲高芙胜佩古拉
辛辛那提决赛的视频：「你看下人家怎么写比赛配音文案的，以后要按着这个标准来」。
**这条和我们同一天发的 `gauff-pegula-cincinnati-2026-final` 是同一场球**——
前三条参考都是不同赛事/不同体裁，这条第一次能逐句对着同一场比赛比。

逐秒抠出来的字幕全文（69 秒）：

    0–3s   22岁高芙直落两盘横扫佩古拉                    ← 封面
    4–6s   时隔三年再夺辛辛那提1000赛冠军
    7–9s   终于斩获了赛季首冠 / 生涯第12冠
    10–14s 经历了整个赛季的相对低潮后 / 大幅减少双误次数的
    15–23s 高芙终于找回了自己 / 她在辛辛那提势如破竹 /
           防守依旧无懈可击 / 底线击球扎实如磐石
    24–27s 面对曾经的双打搭档佩古拉 / 高芙毫不留情
    28–38s 首盘迅速进入状态 / 连下五局取得5-0领先 /
           她频频打出精彩制胜分 / 在底线对抗中完全压制了对手 / 以6-2先下一城
    39–49s 第二盘高芙再度取得4-1的优势 / 虽面对对手的强势反扑 /
           但她在关键时刻顶住压力取得破发 / 以6-4锁定胜局
    50–58s 第37次击败世界前十 / 第四次斩获1000赛冠军 /
           从19岁一鸣惊人 / 到22岁再次加冕
    59–66s 在辛辛那提的夕阳下 / 高芙捧起了赛季首冠 / 也捧起了无限可能的明天

**三条能直接搬进已有的写法里：**

- **收尾把不同维度的成就数字连着堆，不只用一个。** 收尾五连击是
  「第37次击败世界前十」→「第四次斩获1000赛冠军」→「19岁到22岁」——
  三个**互不重复维度**的数字（对手实力、赛事级别、生涯跨度）叠在一起，
  才有"这不是一次孤立的赢球"的分量。CLAUDE.md 早有「排名只当收尾的重量，
  不当全部内容」（`shang-rublev` 那条），这条只是把它从"一个数"扩成
  "几个不同维度的数一起堆"——落笔前多问一句：除了这场比赛本身的数据，
  还有没有第二个、第三个维度的成就数字可以在收尾一起摆出来
- **两个年龄/年份做书挡，收束生涯弧光。** 「从19岁一鸣惊人，到22岁再次
  加冕」——两个具体的年龄卡在收尾，读者一秒钟接住"她从新星走到今天"这条线，
  比一句"她成长了"具体得多。核实过：高芙2023年19岁时拿下当年美网冠军，
  今年（辛辛那提这一冠）22岁，两个年龄都是真的。以后收尾想要"她一路走来"
  这种意味时，优先找**两个具体年龄或年份**做锚点，不写"多年来""一路成长"
  这种没有数字的空话
- **转折前先给一句技术性归因，再进比赛细节。** 「经历了整个赛季的相对
  低潮后／大幅减少双误次数的／高芙终于找回了自己」——先摆"这个赛季不顺"
  的背景，紧接着一句**具体的技术指标**（双误减少）解释"为什么这一站行了"，
  再进首盘逐局。这正是「旁白要有技战术拆解」那条要的东西：不止说"她反弹了"，
  要说清"靠什么反弹的"。核实过：高芙近两年双误确实是她被反复讨论的弱项，
  这条片子把"季初低潮"接到"双误减少"这个具体技术点上，是可核可用的归因，
  不是空泛的"状态回暖"

**顺带核实了一处会被当成"编的"的事实，结果是真的**：「面对曾经的双打搭档
佩古拉」——高芙和佩古拉 2022 年圣地亚哥站、2023 年迈阿密站搭档拿过双打
冠军，也一起打进过 WTA 年终总决赛双打正赛，是真的former doubles partners。
**这条本身提示一个可以用的写法**：把"曾经的搭档""曾经的对手"这类关系背景
直接编进旁白（不是只写进 `editorial.human_context` 自己看），但**这条视频里
的任何一句具体事实，照旧要按「查得够深、查得够广、两个源对得上」那条自己
核一遍再用**，不能因为别人发过就直接引。

**两条不学，和已经定过的规矩正面冲突：**

| 这条参考视频怎么做 | 为什么不学 |
|---|---|
| 封面纯事实堆叠（年龄＋比分＋对手），零悬念 | 「钩子要有剧情的跌宕」那节账号所有者原话点过同一种毛病——「只靠排名，这个太机械化、固定化了，没有把剧情的跌宕说出来呀」。封面仍然按「过程+落点」写，不按"结果先说" |
| 收尾是陈述句抒情（"也捧起了无限可能的明天"），没有问号 | 「末屏那个问题，旁白必须问出来」是账号所有者亲口定的硬规矩，为的是换评论区互动。末屏仍然要留一问，不能换成纯抒情收尾 |

⚠️ **字幕位置和配色同样不学**（通栏白底、贴底、无双语）——和前两条账号一样，
这条只学文案结构和归因写法。

#### ⭐⭐ 第五个账号：小丝瓜🎾 的两条"为什么"视频——「网球有故事」的体裁样板

账号所有者 2026-08-29 甩来两条小红书链接（同一账号「小丝瓜🎾」）：
「瓦林卡这张美网外卡，拿得有多戏剧？」（173s）、「郑钦文，为什么会走到打
资格赛这一步？」（220s，131 赞/29 评——两条里数据好的那条）。原话：
「**学习这些视频的剪辑和叙事手法，以后应用到做网球有故事的视频**」。

前四个参考都是赛报/知识帖体裁；**这两条正是「网球有故事」本身**——回答一个
关于人的"为什么"。逐帧逐句拆完（切点/旁白覆盖/图形卡版式全量过），完整
记录在 `docs/story-video-reference-xiaosigua.md`，**做下一条网球有故事之前
先读它**。这里只留提炼：

**① 体裁形态：视频论文，不是集锦加旁白。** 两条都是**旁白覆盖 98%、
5.0 字/秒**，全片只有章节卡处 1 秒上下的气口；中位镜头 2.2~3.0 秒的快剪，
靠的是**证据类型每 2~3 秒轮换**（比赛画面/生涯剧照/排名走势图/社媒截图/
官方海报/数据表/名条卡），不是狂切比赛镜头。⚠️ 这和「赛场之上」的
现场声+闪避是两条路——网球有故事讲的是一段来路，旁白就是主线。

**② 骨架 = 现在的反差开场 → 分章回答"为什么" → 回到现在收口。**
郑钦文条：0–24s 用"现在时刻 + 上一次打资格赛还是 2022 澳网"把四年轮回
立起来，生涯压缩成 4 拍；然后 01 排名怎么掉的 → 02 为什么保护排名救不了
→ 03 为什么没有外卡；收口回到资格赛现场，"四年前她就是从资格赛出发……
四年后的今天"书挡合拢。**每一章是观众真会问的一个问题。**

**③ 数字章节卡**：压暗的画面剪影 + 橙色数字块 01/02/03 + 大字标题，
约 1 秒。三分钟以上的故事片值得有——观众永远知道走到哪儿了。

**④ 排名走势图的"时间推移"**（全片最亮的一手）：同一张图表模板连放
9 拍，每拍更新日期 + 大号 No.XX、折线往前画一段——22 秒把一年的排名崩塌
可视化，旁白同步走。模板：眉题小字（WTA 单打世界排名·逐周）/ 大标题 /
日期 / 大数字 / 注释药丸 / **数据来源脚注烧在卡上**。排名轨迹正是
「照片讲不清」那类事，这是示意图规矩下的标准做法。

**⑤ 因果链收口在"门槛数字"上**："报名时 120 多位，直接入围线卡在第
118 位"——两个数一比，"为什么打资格赛"不用再解释。同族："128 人抢 16 个
名额，每 8 个人一个位置""纽约胜率超过 74%"。写"为什么"类故事先找到那个
门槛数，把答案钉上去。

**⑥ 替观众把反问问出来**："很多人会问，她明明受伤了，为什么不能用排名
保护？""这样的球员难道还不值得一张外卡吗？"——先替观众发问，再讲规则，
再套回主角自己的时间线。**评论区里会出现的质疑，正片里先答掉。**

**⑦ 把分析框架做成一张表**：8 张外卡逐行列「获得外卡**潜在因素**」
（措辞把推断标成推断），行按类别上色——表本身就是论证。⚠️ 两条片子共用
同一套外卡经济学框架（传奇回归/协会合作/本土培养/商业价值），各从自己
主角的视角讲一遍——**一份调研，两条片子**。

**⑧ 一手证据上屏，长英文就地翻成中文**：IG 告别帖截图正文直接以中文
呈现（头像/验证标/图片网格保留原样），短英文原文保留，官方海报整屏上。
第三个账号那条「官网截图+高亮」的下一层。

**⑨ 封面的两行矛盾钩子**：近景侧脸 + 「世界第4 / 打资格赛」，每行
4 个字——**两行之间的矛盾就是剧情**。我们已有的三条封面规矩（≤10 字/行、
近景特写、有跌宕）同时做到位的样板。

**不学，和已定的规矩正面冲突**：收尾无问号（"祝郑钦文好运"式抒情——
末屏一问的硬规矩不动）；BGM 垫底烧进成片＋章节卡音效（2026-08-29 账号
所有者刚定了走平台曲库）；字幕样式和 SG 常驻水印（照旧）。

⚠️ **事实照旧自己核**：26 周连续缺席、118 入围线、74% 胜率、外卡名单——
全是这两条片子的 claim，一条都没经过我们独立验证。这两条的题材我们各自
做过（`wawrinka-wildcard` / `zheng-us-open-outlook`），核过的事实底子
仓库里就有，别从参考视频里抄。

### ⭐ 开场三格的顺序：画面爆点 → 落点 → 坐标

账号所有者 2026-08-05，看完抖音后台给 `chwalinska-gibson` 的诊断之后：
「**以后都按这个顺序来吧**」。

那条片子 135 秒，后台的数：**3606 播放 / 19 赞 / 8 评 / 1 收藏，完播 2.34%**，
**2 秒跳出只有 26.97%**。两个数要一起读——**七成人留下来了，封面和第一格没问题**，
走掉是在中间。而本文件早写着「看到结尾的人只有 2–3%」，2.34% 正落在那个区间：
**不是这条片子特别差，是这类片子的常态**；那条老规矩给的解法也不是砍长度，
是**换顺序**。

而这条片子恰恰违反了它自己：

| | 第 ① 格（三条都不配旁白） | 第 ② 句 |
|---|---|---|
| `shang-rublev` | **最后一球落地 + 他赢球后的表情**（账号所有者亲口定的） | 「世界第二百七十，赢了世界第十六」← **落点** |
| `zhang-ostapenko` | 退赛之后两个人在椅子边抱了一下 | 坐标 |
| `chwalinska-gibson` | **一个普通底线回合**——源片第 **2.6** 秒，也就是集锦的开头 | 坐标 |

爆点（0-40 三个盘点全救回来、第四个丢掉）在第 55 秒，红土／硬地那个反差在
第 110 秒——**封面把反差印出来了，片子里却埋在最后**。

所以顺序定死：

| | 放什么 | |
|---|---|---|
| ① | **全片最强的那一格画面**，不配旁白，靠现场声 | 最后一球／赛点落地／捧杯／那个拥抱 |
| ② | **一句话把落点或者最大的反差说出来** | **不是坐标** |
| ③ | **坐标**：北京时间、赛事、轮次 | 下一节那条规矩，位置往后挪一格 |

⚠️ **「最强」是判断题，故意没有完整的测试**——和「最硬的那个事实放第 ① 屏」
同一个理由：硬凑一个会被绕过的判据，还会制造「已经守住了」的错觉，比没有更糟。

能落测试的只有一条**代理**，`test_冷开场不许随手取源片开头`：官方集锦按时间
顺序剪，**开头就是比赛的开头，也就是戏剧性最低的那一格**；爆点几乎必然靠后
（111.6 / 158.8 对 **2.6**）。真有例外——有些集锦开头就是最后一球的预告——
就在那一段写一句 `_head_open_why` 认领。**认领这一步把「想清楚了」和
「凑合一下」分开**，和 `mixed_fps` / `silent_source` / `_layout_why` 一个形状。

⚠️ **不砍片长。** 那份诊断建议「整体时长压缩至 40 秒以内」，和账号所有者定过的
「片长不设上限」「不要砍片长」「我怕故事讲解不完整」直接冲突，**没有采纳**。
中段要压的是重复的统计，不是故事。⚠️ 它给的「垂类完播合格线 8%」也没有出处，
而且**跨时长直接比完播率不成立**——40 秒的片子完播天然高得多，要比该比平均
播放时长。

⚠️ **那份诊断里有三条是关于这条片子的事实，而且都是错的**，记下来免得下次
照着改：说「无结尾提问钩子」——片尾口播和字幕都是「红土上的月亮姐，在硬地上
还能行吗？」；说「只陈述比赛结果、没有情绪起伏」——三个盘点全救回来那一段就是；
说「没有干货总结」——正文里按场地拆开的数据就是。**平台给的诊断是模板，不是
看过这条片子**；它自己算出来的那几个数（完播、跳出、收藏）才是证据，
文字点评那部分要逐条核。

### 「赛场之上」开场先给坐标：北京时间、赛事、轮次

账号所有者：「以后赛场之上上来首先交代下时间（北京时间几月几号几点几分之类
开球的时间）赛事和轮次」。

⚠️ **位置往后挪了一格**，见上一节：坐标现在是第 ③ 格，第 ② 句要先把落点说出来。
`shang-rublev` 是这个形状（爆点 → 落点 → 坐标），`zhang-ostapenko` /
`chwalinska-gibson` 是把坐标放在了 ②。下面这条规矩管的仍然是**三样在不在**，
不管它排第几。

刷到这条片子的人不知道这是哪天、哪一站、哪一轮。先给坐标，再讲故事——
第 ① 段那十秒本来就是用来安置观众的，讲比分反而浪费。

- **时刻按开球时间，不按发布时间**，而且要**北京时间**（读者在国内）
- ⚠️ **不报到分钟**，见下一节——那条把「两个源对不上才说到点」推广成了默认
- 第 ① 段只有十秒，按 4.45 字/秒算预算 40 字，**时间＋赛事＋轮次就占满了**，
  排名之类往后挪一段

存量的七条片子发在这条规矩之前，测试里挂了一份清单，**只许减不许加**。

#### ⭐⭐ 开球时刻**只说个大概，不报到分**——这不是文风，是精确到了错的东西

账号所有者 2026-08-17：「以后配音里不要再说具体到几点几分开球，**就给个大概
时间**，比如夜里 11 点多、凌晨 2 点多之类的」。

**他这条同时在修一个事实错误，而那一层是量出来才看见的。** 扫已发的 spec：

    十一点十分   **9 条**      七点十分   5 条
    六点十分       2 条        十点五十分   2 条

同一个分钟数横跨辛辛那提／蒙特利尔、好几个不同日期反复出现——那不像是某一场
的开球时刻。拿 flashscore 当天的表一比就定了：**46 场里 17 场（36%）和别的场
共用同一个「开球时刻」**，5 场并列 23:05、5 场并列 23:10。**五场球不可能在同一
分钟开打**，那是分场地的整批开赛时刻，也就是本文件「一个时刻有三种口径」里的
**`Not Before` 是下界不是时刻**。

⚠️ 所以排在别人后面的那几场，真实开球可能晚一个多小时，**而旁白白纸黑字写着
「十一点十分开球」**。原来那条「两个源对不上才说到点不说分」是对的，只是范围
写窄了——**大多数场次连两个源都不用对，那个分钟本身就不是这一场的**。

**怎么说才永远字面为真**（⚠️「多」用反方向比不用更糟：23:49 说成「十一点多」
是把人往早了带）：

| 分钟 | 说法 | 例 |
|---|---|---|
| 0–9 | 「X 点」 | 5:08 → 凌晨五点 |
| 10–39 | 「X 点多」 | 23:10 → 夜里十一点多 |
| 40–59 | 「快 X+1 点」 | 22:49 → 快十一点 |

⚠️ **时段词要留着**（凌晨／深夜／夜里／晚上／上午／早上）。「凌晨两点多」比
「两点多」多给的正是这个栏目要的那层情绪——**看的人是熬着夜看的**。

⚠️ `零点十六分` 说「**凌晨十二点多**」，别说「零点多」，口语里不这么讲。

⚠️ **这条只拦「点＋分」这一个形状，不拦时长**：「用时两小时二十二分」
「一个发球局守了十七分五十一秒」照旧能写——那是**量出来的**，不是排期给的。
判据 `test_开球时刻只说个大概不报到分`，两个方向都反向验证过（塞一句
「凌晨三点十分开球」当场红；时长、「一点都不」、百分比里的「分」一个都没误伤）。
已发的 40 条挂在豁免表里，只许减不许加，表自带自检。

### 音频那套默认不开，别接进出片流程

账号所有者：「这个默认不要开启，会很慢，拖慢出片速度」。

「夹逼 + 音频回溯」是**写 spec 时手工跑一次**的辅助（`find_point_ends.py`
的 `unlag`、`check_crowd_rise.py`），不是出片的一环：解一条 188 秒的音轨要
拿 ffmpeg 把整条片子过一遍，接进 render 就是每条片子白等几十秒，而它换来的
只是段尾秒数更准一点——那个数一旦写进 spec 就固定了，不需要每次重算。

判据是**出片路径里一处都不许出现**：`build_match_reel.py` 和
`match-reel.yml` 里对 `unlag` / `audio_envelope` / `check_crowd_rise`
的引用数必须是 0。

### 判历史事件要用当年那本规则书，不能拿改过之后的去判

账号所有者：「历史的事件需要用历史的规则去评判，而不要用新的修改后的规则去评判历史事件。」

规则书**每年都在改**，而改动是静默的：条文编号会挪、半句话会消失、豁免会新增。
拿今年这本去解释去年那件事，得到的结论看起来一样有条有据，**只是错的**。

`mandatory-1000` 那条差点就这么发出去。片子里说「他去年在多伦多也记了个零」，
判据我引的是 **2026** 版第八章 D.2。真去把 **2025** 版拉下来一比，同一条写的是：

| | 2025 规则书 | 2026 规则书 |
|---|---|---|
| 大师赛正赛退赛 | 记排名处罚 **+ 停赛下一站大师赛** | 只剩排名处罚，**「停赛」那半句没了** |
| 育儿身份豁免 | 全书 **0 处** | 6 处 |
| 连缺两站可申请抹零（9.03 Note 3） | **不存在** | 有 |

零那一层结论没变（两版都记排名处罚），**但「没有停赛风险」这句只对 2026 成立**——
按 2026 那本去讲 2025，等于替当年的人免掉了一条当年真实存在的条款。

所以：

- **引条文要连年份一起引**，`credits.json` 里写「2026 版第八章 D.2」，不写「ATP 规则书说」
- **跨年份的对比要两本都拉下来**。ATP 的历年全本在
  `atptour.com/-/media/files/rulebook/<年>/...`，**WebFetch 一律 403，带浏览器 UA 的
  curl 能过**；文件名每年不同（`2026-rulebook_19dec25.pdf`、`2025-rulebook_16jan.pdf`、
  `2022-atp-rulebook_26apr.pdf`），猜不出来就搜一次
- **顺带的好处**：这么一比，差别本身往往就是选题。上面那张表是这条片子里最硬的一屏，
  而它是「按当年那本书重判一遍」这个动作掉出来的，不是本来就想讲的

同一族的还有下面这条（纪录什么时候生效）和「别把『四强』写成『冠军』」——
都是**时间点决定这句话对不对**，不是措辞问题。

### 写纪录先看它什么时候生效：这场创的，还是早就成立的

账号所有者：「它之前已经是菲律宾的领土了，只能说**刷新**了历史」。

我给伊埃拉那条写了「菲律宾网球史上排名最高的球员，是她」，放在片尾当落点。
可她**七月十三号**就已经是世界第 28、菲律宾史上最高排名了——那是这站开赛
**之前**的事。放在赢球之后说，读者会读成「这一场让她成了第一人」。

**判据是纪录的生效时间落在这场比赛之前还是之后：**

| 生效时间 | 怎么写 |
|---|---|
| 这场**之后**（赢了才成立） | 「创造」「首次」「第一人」 |
| 这场**之前**就成立 | 「刷新」「还在往上加」「纪录本来就是他自己的」 |

差别不是措辞，是**这句话对不对**。「创造历史」比「刷新纪录」响，所以更要
忍住——一句煽得起来但站不住的话，被人拆穿赔上的是整个号的可信度
（见上面「文案的两条要求，有先后」）。

顺带两条同类的，都在同一天踩到：

- **别用统称概括一组人。** 「一周连过三个前十」——费尔南德斯生涯最高
  世界第 13，从没进过前十，这个统称一开始就不成立。改成各给各的名头
  （奥运冠军 / 美网亚军 / 年终总决赛冠军），既准确又比排名响
- **别把「四强」写成「冠军」。** 斯维托丽娜四次打进大满贯四强、拿过 2018
  年终总决赛，但**没有大满贯冠军**。名头要照抄来源写的那一个

### 转折点不许剪掉

账号所有者：「一定不要把比赛的转折点剪掉，如果有的话，如果集锦里有的话」。

集锦是别人按「好看」剪的，我们是按「讲清楚这场球」剪的，两套标准不一样。
一个漂亮的制胜分谁都会留，**而转折点常常是一个不好看的保发**——0-40 救三个、
连续四次平分才守住，画面上远不如一记穿越，却是这场球换手的地方。

顺序**不能反**：

1. **先从逐分（`df_mh_1`）里认出转折点**，再去看集锦有没有。反过来做就是
   「集锦里有什么就讲什么」，讲出来的是集锦的故事不是比赛的故事
2. 转折点在集锦里 → **它必须占一段**，而且优先级高于任何单纯好看的回合
3. 转折点不在集锦里 → **明说**，在 spec 的注里写清楚哪一段缺了、为什么。
   默默跳过等于让观众以为这场球是顺下来的

怎么认转折点：比分走向掉头的那一局。典型是**落后方第一次破发反超**、
**被逼到 0-40 或 15-40 却守住的发球局**、**盘点/赛点被救**。逐分里
`|B1|` `|B2|` `|B3|` 密集出现的地方就是候选。

**「在不在集锦里」要拿记分条跨着剪辑点比一次，不能看画面像不像。** 伊埃拉那条
第二盘 2-3 那个换手的破发，画面上明明有那一局的镜头，看着就是「有」——
逐格读记分条才发现只剩**第一分**：

    184.1–192.5   `6 2 0 / 3 3 15`   ← 2-3，这一局的第 1 分
    195.4         剪辑点
    195.5         `6 3 30 / 3 3 15`  ← 已经 3-3，而且是下一局的第 3 分

中间八分连同两次占先全被剪掉了。**一格记分条只证明这一局出现过，不证明
决定性的那几分还在**——判据是剪辑点两侧的比分差了几分，差得动就是被剪了。
（同一条片子里第一盘 4-3 那个 0-40 反过来：三个破发点被剪掉了，但**占先
那一分留着**，所以那条算「有」。留没留住关键的那一分，才是分界线。）

#### ⭐ 「这是第几盘」不能靠看画面——两盘长得一模一样，只能裁记分条放大读

2026-08-06 莱巴金娜那条，我给 215~227 秒写的旁白是「**决胜盘**，两个人谁也
没能把比分拉开」。缩略图墙上那几格是标准的底线宽景，和决胜盘的画面**没有
任何区别**——同一块场地、同一个机位、同样的光。

把记分条裁出来放大，它自己写着：

    214.5~230.5s   SET POINT #2
                   D. KASATKINA  3 | 6 | Ad
                   E. RYBAKINA   6 | 5 |        ← 发球指示在这一行

也就是**第二盘 5-6、她发球保盘、对手手握盘点**——正是 WTA 稿说的
「连续两个双误丢掉第二盘」那一局。我把它当成了决胜盘，整段说反。

- **判据是裁出来放大，不是看画面像不像。** 记分条在 1920×1080 的源片里只占
  左下角很小一块，缩略图墙那一格里它糊成几个绿点；`crop((0.02w, 0.73h,
  0.32w, 0.95h))` 再放大 4 倍，比分、盘数、`SET POINT` / `BREAK POINT` /
  `MATCH POINT #N` 全都读得出来
- **一次读一段，别只读你要用的那几格。** 我一开始只裁了 180.5 / 256.5 /
  272.5 三处（写 spec 时用得上的那三处），215 那一段**根本没查**，因为它
  「看起来就是决胜盘」。后来把 196~266 秒每一格排成一张图，边界一目了然：
  两个盘点、卡萨金娜的握拳特写、换边时挂出来的 `ROUND 2` 比分图形
- **换边镜头是白给的分盘标记**。转播在换边时会挂一张比分图形（这条片子是
  `KASATKINA 3 7 / RYBAKINA 6 5`），**那一格自己就说清了「一盘一平」**，
  不用旁白去交代，也是最干净的「进决胜盘」画面
- ⚠️ **发球指示（记分条上那个小图标）决定这一局是谁的。** 同样是 `SET POINT
  #2`，指示在对手那一行是「他发球拿盘点」，在自己这一行是「他发球保盘被逼到
  盘点」——两种写法说的是完全相反的两件事

这和「画面里能自证的元素比任何『看着像』都值钱」是同一条，只是反过来用：
**自证的元素在画面里，而它小到你不放大就等于没有。**

### 动作句只挑**能渲染情绪**的那几个

账号所有者：「能否再加一些根据动作的配音，比如拿下这一分她振臂高呼？」
「主要能渲染情绪的动作。」

这和「解说匹配画面是必要的」是同一条的下半句：旁白该落在动作上，但**不是
每个动作都值得说**。挥拍、走位、擦汗是动作，讲了等于复读画面；**握拳、
吼出来、看教练席、蹲下去、把拍子一扔**才是情绪。

判据：**这个动作是不是在告诉观众「他此刻在想什么」。** 是就写，不是就别写。

- 一场球里够格的通常只有三四处：救下关键分之后、破发反超那一下、赛点落地
- **必须打开片子按帧确认他真做了**。凭「网球比赛通常这样」写出来的动作就是
  编的——同一个毛病在「TNNS 有击球方式」上栽过，那句话我从没验证过却追了三轮
- **而且要确认是谁做的，不只是「做没做」。** 我给伊埃拉那条记过一条
  「135.5 秒 伊埃拉举臂庆祝」——动作真的有，人是反的：那是**斯维托丽娜**
  （白衣，随后 138.5 秒特写吼出来），她刚连过三个占先保住发球局，
  解说同一时刻说的是「She made it. Incredible.」。
  **对一次记分条就露馅了**：那一格写着她的占先。
  写反了不是少一句情绪，是把对手的高光安在主角头上，整段意思全错
- 落点压在**动作发生的那一刻**，不是之前也不是之后
- 和事实句**交替排**：「三个，一个没给」是事实，「转身冲教练席握拳」是情绪，
  一硬一软才有起伏

### 源片按巡回赛分：WTA 用官方频道，Tennis TV 是 ATP 那条线

账号所有者：「wta 就是这里啊，tennistv 主要是 atp 的」。

| 巡回赛 | 源片 | 怎么拿 |
|---|---|---|
| **WTA** | **WTA 官方 YouTube 频道**的 `Match Highlights` | runner 上带 cookie 直接下，`mode=probe` 给 URL 就行 |
| **ATP** | Tennis TV（订阅制） | 账号所有者从订阅里取，转存网盘给链接 |

**别把 ATP 的情况套到 WTA 上。** 黄泽林那条要账号所有者手动供片，是因为
Tennis TV 要订阅、而 YouTube 上的当日合集**漏了那一场**；WTA 这边官方频道
每场都发单场集锦，是**首选而不是退而求其次**——我问过一句「有没有更完整的」，
那个问题在 WTA 这条线上不成立。

#### ⚠️ 顶栏次行的灰绿是为**实色卡**定的，压在虚化画面上不成立

账号所有者 2026-08-05 看完商竣程那条：「顶部的小字，灰色的看不太清，
改成可以看清楚的」。

原来是 **32 号 / `#a9bcb2` / 没有描边**。`#a9bcb2` 不是随手挑的——它正是本文件
「一屏只留一个强调色」那节登记的**次级灰绿**。**问题在于那套色是给深绿实色卡
定的**，而顶栏压的是**虚化过的比赛画面**：底色一路在变，同一个灰绿在亮的地方
就糊掉了。**调色板的适用前提换了，颜色却照搬了过来。**

三样一起改，缺一个都不够：字号 32→38、`#a9bcb2`→`#d5e2db`、描边 0→1.5
（实色卡不需要描边，变化的背景需要）。

- ⚠️ **比分那段要跟着涨**（38→44）。它是**故意比基准大一档**的强调，基准提上去
  而它不动，层级就被压平了——`test_顶栏量宽度要按每段自己的字号` 当场抓住了
- **宽度先拿存量验**：七条 spec 里最宽的是商竣程这条（三盘比分），
  815.6px / 可用 984，还有余量
- 判据 `test_顶栏次行要在虚化背景上读得出来` 钉的是**不许退回去**（字号只许涨、
  必须有描边、颜色不许比 `#a9bcb2` 还暗），不是钉死具体数值。反向验证过
- ⚠️ 那条判据第一版**把已经改好的样式判成没描边**：ASS 的 Outline 是
  `split(",")` 后的第 **15** 位，`Style: HEADB` 整个算第 0 位，后面每项挪一位；
  按 16 取读到的是 Shadow。**判据自己也会数错位**
- **已发的六条不重渲**，这个改动只对以后的片子生效

#### ⭐ ATP 的**场上采访**：Tennis TV 的免费条目自己就解得开（2026-08-05 记）

上面那张表说的是**集锦**。「赛后开麦」要的是**场上采访**，而它在 ATP 这边的
情况更极端：**结构性地不上 YouTube**。商竣程胜卢布列夫那条扫遍了

    ATP Tour 频道 · Tennis TV 频道 · 赛事官方频道（Tennis Canada，深扫 90 条）
    Tennis Channel · 三个专搬场上采访的频道 · 五组英/法查询词
    三条集锦各自的自动字幕末尾（看采访有没有接在尾巴上）

全是集锦和发布会。**决定性的是那个对照**：同一组查询词，**多伦多（女子
#NBO26）有场上采访，蒙特利尔（男子 #OBN26）任何球员都没有**——所以这不是
「没搜到商竣程」，是这一站男子不公开发。⚠️ 空结果先自证是真空，办法是**拿一个
已知非空的口径对一下**，别只反复换词搜同一个人。

真正的源在 Tennis TV，**而它不一定要订阅**：

    https://www.tennistv.com/library/interviews      ← 公开列表页，能搜到条目
    页面上 data-entitlement="free"                    ← 这一条免不免费，写在这儿
    src/tennislive/video/official.fetch_tennistv_video_metadata(...)

那个函数走的是 Tennis TV **自己的公开 entitlement 接口**（`account: atpmedia`，
不用登录），免费条目直接解出 HLS。商竣程那条 74.0 秒、1280×720、25fps，
本地 yt-dlp 一秒下完——**沙箱下不动 YouTube，但下得动 streamamg**，
所以这条源反而比 WTA 那条更好在本地走完整条流水线。

⚠️ 接这个源要同时改四处，少一处就是一句指向完全错误方向的报错：

| 改哪儿 | 不改会怎样 |
|---|---|
| `yt_download` → 先过 `media_url()` | yt-dlp 报「只对注册用户开放」，**读起来像 cookie 过期** |
| `fetch_words` 先判是不是 YouTube | 去拉一个**根本不存在**的自动字幕轨 |
| `_yt_at` 不许给非 YouTube 编链接 | 拼出 `youtu.be/<slug>?t=17`，**看着能点、点开 404** |
| `storyboard_sheet` 先判 | 白等一次网络往返，报「取不到视频信息」像「今天网不好」 |

⚠️ **解析必须在下载那一刻做，不能把解出来的 manifest 钉进 spec**——它带令牌
会过期，钉进去就是一条今天能用明天 403 的死链，而它失败的样子和「片子下架了」
一模一样。

##### ⚠️⚠️ 没有 YouTube 字幕，`verify_transcript` 那道闸会**自己空掉**

这是接第二个源时最贵的一个坑，而且**它的表现是一片绿**。

那道闸原来的前提是「第一份＝YouTube 的 ASR，第二份＝faster-whisper」，两边
天然不同。非 YouTube 的源没有自动字幕轨，**第一份也是 whisper 跑的**——这时候
不检查的话，同一个模型跑两遍必然逐词一致，**分歧率恒为 0%，报告全绿，而它
什么都没验证**。仓库里 `en` / `en-orig` 那次记的就是这个形状。

所以 spec 多一个 `asr_model`（第一份用了哪个模型），`whisper_model` 必须和它
不同，闸装在 `verify_transcript` 里，判据 `test_第二份ASR必须换个模型否则那道闸是空的`
两个方向都反向验证过。商竣程那条：`small.en` → `medium.en`，**对不上 1.8%**。

###### ⚠️ 而这道闸第一版排在 `from faster_whisper import` 后面，于是 CI 上走不到

**本地全绿，CI 报 `No module named 'faster_whisper'`**（PR #198，run 30980157757）。
根子不是缺依赖——CI 本来就不装它——是**我把一道只查 spec 形状的闸，排在了一个
环境依赖后面**：没装这个包的机器上它永远走不到，而那正是 CI 那台。

这是「形状校验里不许混进环境检查」的**镜像**：那条说的是别把环境检查塞进形状
校验，这条说的是**别让环境依赖挡在形状校验前面**。后果也对称——那次是本地
dry-run 得先装 176 MB 的模型，这次是判据本身在 CI 上不存在。

- **验这类修复要造出 CI 那台机器的条件**，别在装着的机器上跑一遍就算数。
  办法很便宜：`PYTHONPATH` 前面塞一个会 `raise ImportError` 的同名包遮住它，
  两种环境各跑一次（`nofw` 那个目录就是干这个的）
- 判据钉**位置**不钉行为（`test_换模型那道闸要排在导入whisper之前`）：
  行为测试在装着 faster-whisper 的机器上照样绿，拦不住这个错
- ⚠️ 另一半断言别写成「反正会抛点什么」——那样它在装了和没装的机器上走的是
  **两条不同的路**（一条死在下载、一条死在 import）。用哨兵把要走到的那一步
  钉死，这个仓库为「同一条测试在两个地方跑两条路」栽过一次（`GITHUB_TOKEN`
  那条）。

⚠️ 顺带两条同族的：

- **报告里「第一份是谁」要照实写**。标签原来写死「YouTube 自动字幕」，
  换源之后再这么印就是主动给出一个错答案——查报告的人会以为它比的是两个
  不同来源，而那正是这道闸唯一要证明的事
- **`caption_gaps`「0 处空档」在这条源上不构成证据**。那道闸查的是「自动字幕
  连一个事件都没有的几秒」，而这里的『字幕』是我们自己跑的 ASR，它对静音段
  本来就不产事件。别把它读成「全片都有人在说话」

##### 第一份转写怎么喂进去：`cap_*.json3` 这个接缝本来就在

`fetch_words` 是**先 glob 本地 `cap_*.json3`，找不到才去调 yt-dlp**。所以
ASR 的结果按那个形状写进 outdir 就行，一行代码都不用改：

    {"events": [{"tStartMs": 1420, "segs": [{"utf8": ">> Well,"}]}, …]}

⚠️ **`>>` 是说话人标记，要自己补上。** YouTube 的自动字幕给每个说话人的第一个
词加这个前缀，而切行靠它断「换人说话」；whisper 不给。商竣程那条按内容标了
六个轮次（主持人三次、他三次），不标的话主持人的问和他的答会被并进同一行。

⚠️ 这份 `cap_asr.json3` **要进仓库**——它是这条 spec 唯一的转写来源，清理步骤
（按后缀删 `.html`、`rm source.* _audio.* whisper.json`）碰不到它，别顺手加。

#### ⚠️ 同一场球的官方集锦有**两个剪辑**，换源片先核 duration

2026-08-03 伊埃拉夺冠那条，同一份 WTA 官方集锦在两处，**长度差两百秒**：

| 出处 | 时长 | 画质 | |
|---|---|---|---|
| wtatennis.com（Brightcove `6402701000112`） | **335.89s** | 只有 **1280×720** | 先上线 |
| YouTube（`OvjNXy1mQjY`） | **534.92s** | **1920×1080** | 晚一小时，多出颁奖和致辞 |

我照 720p 那版写完了十四段时间码，YouTube 版一上来**全部作废**。
1080p 值得等：3:4 的取景窗从 1280 里取 540 要放大 **2.0 倍**，从 1920 里取 810
只要 **1.33 倍**（「不要抽帧当封面」那条算的 1.78 倍就已经被判太软）。

- **换 `source_url` 之前先 `yt-dlp --print "%(duration)s"` 对一下**。长度不同就是
  另一个剪辑，`probe.json` 的切点、记分条对照表、每一段的窗口统统要重来
- **站内版和 YouTube 版不许混用**：`check_sources_match` 只比尺寸，比不了「是不是
  同一刀」——两版都 25 fps，尺寸各自自洽，混进来不会报错，只会剪出一片乱码
- 站内那版**首选放弃**：Brightcove 这个资产的最高档就是 720p（HLS 的 `hls-2344`、
  http 的 `http-2133k-720p` 两条路都探过）

##### ⚠️ 播放页不是直链：`download()` 会把网页当源片，还存进缓存

`players.brightcove.net/<账号>/<player>/index.html?videoId=…` 是 yt-dlp 认的地址，
**但不是 curl 得到的地址**。直链那条路 curl 下来 **0.3 MB 的网页**，打印
「[ok] 直链下到 0.3 MB」，`_keep_source` 还把它**存进了缓存**，一直到下一步
ffprobe 才报 `Invalid data found`（run 30838371382）。

**缓存把坏结果固化了**——重跑第一件事就是命中那个坏文件，于是「换个参数再试」
永远重现同一个错。老判据只看**前 64 字节**里有没有 `<!doctype html` / `<html`，
那个播放页两条都不满足。

判据换成「**ffprobe 读不读得出一路视频流**」——yt-dlp 那条路早就在用 `probe_size`
把关（它甚至还量高度防 360p），只有直链这条没有。报错要说出路：①网盘还是
「仅限受邀者」；②给的是播放页，这类要交给 yt-dlp，curl 只会把网页存下来。
判据 `test_直链下到的网页不许当成源片还存进缓存` **真造两个文件、真跑一遍
`download()`**，fixture 故意不以 `<!doctype` 开头、前 64 字节里也没有 `<html`。

#### 判「颁奖从哪儿开始」要拿画面，不是拿「谁在说话」

同一条片子上栽的：我按「她开口致辞」把颁奖定在 **322 秒**，于是开场一路拉到 312。
账号所有者：「300 秒后面是颁奖环节了啊」。

真正的分界是切点 **298.84**——298.5 她还穿着荧光绿球衣在抱团队，300.5 已经换上
枣红外套、奖杯摆在领奖台上。中间那 13 秒被我当成庆祝带进了片子。

**又一次「查产物不查信号」**：说话是信号，画面才是产物。判据是球衣换了没有、
领奖台在不在——都在缩略图墙上，2 秒一格，看一眼的事。

#### ⚠️ 官方稿也会错：年龄写错、头图是资料图

同一天两处，**都出自 WTA 官方**：

- **年龄**：决赛稿写「The 20-year-old's history-making efforts」，而她生日
  **2005-05-23**，那天已经 **21 岁**。账号所有者当场指出来的。
  **年龄这类要自己按生日算一遍**，别照抄
- **头图**：决赛稿的 `GettyImages-2288643508` 看着完全像夺冠瞬间，可同一个文件在
  图库里挂了两次——`photo-resources/2026/08/01/a663aa4b-…/` 和
  `2026/08/03/d00fd07a-…/`，**两份 md5 完全相同**（`71421db7…`）。
  08/01 是周六，那天她打的是**半决赛**（两场都穿荧光绿球衣）

第二条是「官方图库把它挂在这一站名下，不等于拍的是这个场地」的新变种——
**这次挂错的不是场地，是稿子**。判据很便宜：**同一个 Getty 编号出现在两个日期
目录下就是资料图**，`curl` 两份比 md5，十秒钟。

#### 原声段：`quote` 可以按条声明，还能钉真实时刻

账号所有者 2026-08-03：「前面夺冠庆祝的部分全用英文原声配中英文字幕」
「不要剪切掉原来英文解说的完整配音和画面」。

`quote` 原来只接受一个字符串，按标点自动切、按字数等比铺满整段。**中英双语过不了
这一关**：自动切会把英文句子劈开，也不知道哪一行该和哪一行配成一对。现在两种写法：

    "quote": ["上英\n下中", "…"]                        按字数摊，短段够用
    "quote": [{"at": 段内秒数, "text": "上英\n下中"}]     **钉在真实时刻上**

- **几十秒的整段原声必须用 `at`**。按字数摊的前提是「说话密度均匀」，而真实解说有
  停顿有留白：那条片子 53.5 秒里二十二条，摊出来差好几秒——
  **比不同步更糟的是它看起来像同步**。`at` 直接从 probe 落的 `captions.txt`
  时间戳减段起点
- **两种写法不许混**（摊的范围被钉住的那些切碎，出来的时刻没人能预料），会报错
- **每一行都要量宽度**：`_sub_width(_sub_display(行))` 不许超过 `_SUB_MAX = 16`。
  超了会自动折行，中英就不是各占一行了。那条片子七条长句因此拆成了两条
- ⚠️ **自动字幕把名字全转错了**（`It's a Yalas` → `It's Eala's`、`Pigula` →
  `Pegula`、`Alex Ayala` → `Alexandra Eala`）。双语字幕的英文要**按发音校正**，
  不能照抄 `captions.txt`

##### ⚠️ 换行是「排两行」，不是一个空格 —— 而我验错了写入器

第一版我拿 `pipeline.render_ass` 验双行，它确实把 `\n` 变成 ASS 的 `\N`。
**但成片走的不是那个写入器**：`build_match_reel` 用的是
`explainer.write_subtitles`（Style `TL`，带逐词字号标签），而它当时写着
`shown.replace(chr(10), ' ')`——换行被压成空格。

渲出来长的那两条靠 `WrapStyle=0` 自动折**碰巧**断在中英之间，短的干脆不断，
同一条片子里两种样子，而且断点由宽度决定，不由作者决定。
**又一次「查的东西和跑的东西不是一回事」，只是这次两个都是我们自己的代码。**

⚠️ 也不能直接把 `\N` 写进文本：`_ass_text` 会把那个 `N` 当成一个拉丁词、包上
`{\fs78}`，画面上多出一个字母 N。正解是**按行分别过 `_ass_text` 再用 `\N` 拼**——
单行的 cue 输出一个字节都不变，解说片那条线不受影响。
判据 `test_双语字幕要真的排成两行` 真写一份 ASS 出来读，钉两头：双行的要有换行符、
**单行的一个字节都不许变**。反向验证过。

#### 「替她说话的那句压在对手身上」的主语是 `narration`，不是 `quote`

同一天里这条规矩我**两个方向都用错了一次**：

| | 我判成 | 实际 |
|---|---|---|
| 英文原声段 262–270s 近端换成佩古拉 | 违规，把段收短了 | ✅ **没关系**——账号所有者：「这个是英文解说，没关系啊」 |
| 中文旁白「菲律宾第一个 WTA 冠军」压在佩古拉近景 + `PEGULA 6 / EALA 4` 图形上 | 没看出来 | ❌ **真违规**，全片最重的一句配着对手赢盘的画面 |

**分界很清楚：旁白是我们替谁说话，落错人就是把对手的高光安在主角头上；
原声段的画面和声音本来就是转播那一刻的同一份，谁在画面里是转播的编辑选择。**
判据宁可窄，不可宽——这条的主语是 `narration`。

⚠️ **切点查不出这个**：镜头没换，换的是**近端是谁**。`check_reel_landed` 那一趟
报的是 0 项不合格，机器该查的都查了，这一条它查不了。**只能渲出来抽帧看。**

#### 余量是同一个数的两头，dry-run 原来只看了一头

render 里那道哑场闸红得对：「第 6 段：画面 13.1s，旁白只说了 8.83s，
**4.27s 没人在说话**」（门槛 4.0s）。

**而上一次 dry-run 就把这个数印在我眼前了**——`余量 +4.71s`，我读成了
「装得下，很好」。余量为负是旁白写长了，**余量太大是这一段大半时间没人说话**，
一直印着，只是没被读成「太空」。

现在 `--dry-run` 两头都标：超门槛标 `**哑场**`，落在 `门槛 ± 误差` 里标「偏空，悬」。

- ⚠️ **门槛不减误差带子**。第一版写的是 `room - SPEECH_EST_ERR > MAX_SILENT_GAP`
  （「估得再保守也超」），那要 `room > 5.5s` 才报——而真闸红在 4.27s，照样漏。
  离线估的**中位误差贴近 0**（`test_离线估旁白长度要对得上真产物` 钉着这一条），
  点估值就是最好的猜测，和真闸同一个口径
- ⚠️ **只警告，不 `return 1`**。第一版让它红，当场把已发的 `gea-shapovalov`
  （18 段）也判红了——那条片子当年是过了真闸的。真闸用**真语音**，离线估在这一头
  偏保守（本例估 4.71s、实测 4.27s），拿它当硬闸会把一批好 spec 挡在门外，
  而**一条常年红的检查和没有检查是同一个毛病**。它的职责是让人早点看见，不是拍板

#### ⚠️ 按序号认领产物：「序号还在」比「序号越界」更坏

`test_离线估旁白长度要对得上真产物` 读 `render.json` 的 `narration_seconds`
（按段序号记的秒数）。`eala-pegula-final` 从 14 段重排成 8 段之后：

- 越界的那几个 → `IndexError`，**那还算出声**
- 没越界的那几个 → **静默指到另一段身上**，于是测试报「最坏一段差 6.63s」，
  看起来像模型不准，其实是**产物对错了人**

又一次「voice 文件要按内容认领，不按序号」，只是换到了 `render.json`。
`render.json` 自己记着渲的时候那份 spec 的**画面总长**（`segments_seconds`），
拿它和当前 spec 对一下就能自证是不是同一版；对不上**整个 outdir 跳过并出声**。

**改 spec 不重渲是常事**（措辞、注解、规矩追认、重排段落），所以凡是读产物的判据
都要先问一句「这份产物是不是这一版的」。

#### `list_workflow_runs` 连 `per_page=1` 都吐 40 万字符

CLAUDE.md 早记过「两条 run 就 65 KB」，实测比那还夸张：**`per_page=1` 照样返回
30 条、40 万字符**（这个参数根本没生效）。这一轮查了五六次 run 状态，每次都靠
同一个办法绕：

**让它超上下文上限，然后读它落下的那个文件。** 报错本身很短（几百字），文件里
是完整 JSON，`python3 -c "import json; …"` 挑三个字段就够：

    d = json.load(open('<报错里给的路径>'))
    for r in d['workflow_runs'][:3]:
        print(r['id'], r['run_number'], r['status'], r['conclusion'], r['created_at'])

知道 run id 就直接 `list_workflow_jobs`（几 KB，还带每一步的起止时间，
比日志更适合看「卡在哪一步」）。

### 走势数据从哪儿取：`tools/match_feed.py`

账号所有者：「你要去找到真实的比赛走势数据和每一盘的技术统计对比，然后分析后
再结合视频里的时间点做配音评论」「不光 atp 还有 wta 还有四大满贯」。

一天探下来的结果，**按能拿到多少排**：

| 源 | 给到哪一层 | 覆盖 |
|---|---|---|
| **flashscore `df_mh_1`** | **逐分** + `\|B1\|`破发点 `\|B2\|`盘点 `\|B3\|`赛点 | **ATP / WTA / 大满贯通用** |
| flashscore `df_st_1` / `df_sui_1` | 分盘技术统计 / 分盘用时（含抢七比分） | 同上 |
| 温网 GraphQL `pointHistory` | 逐分 **+ 击球方式 + 发球落点 + 球速 + 回合数 + 现成的英文句子** | 只有温网 |
| 美网 `config_web.json` 里的 REST 模板 | 同一套 IBM 数据（字段名一模一样），入口是 REST | 只有美网 |
| 澳网 `prod-scores-api.ausopen.com` | 当日 results / schedule | 逐分那层没验 |
| ESPN | 只有分盘比分 | —— |

**flashscore 是主力**（三条线一套代码），大满贯官方是加料。法网还没找到，
TNNS 和 Sportradar 够不着（见工具文档）。

六个踩点，每个都咬过：

- **逐分不列制胜的那一分，所以「这一局打了几分」＝条数 ＋ 1。** 判据在最简单的
  一局上就能自证：`1-0 主队发球，主队拿下` 只列了 `15:0, 15:15, 30:15, 40:15`
  四条——**四十比十五赢不下一局**，第五分它没写。
  伊埃拉对大坂那场，大坂连救四个破发点的那一局列了 13 条，所以是**十四分**。
  这个错不吭声：13 是一个看着很像的数字，而且它**永远只差一**，
  怎么数都像数对了
- **逐分的局号是「打完之后的比分」，不是场上的比分。** 伊埃拉那条里那个
  0-40 救三个的发球局挂在 `5-3` 行下，而场上是 **4-3**——记分条连着六格
  写的都是 `4 Ad / 3`。照行标写「五比三，轮到她发球」，画面当场打脸，
  和黄泽林那条「连下五局到五比一」是同一个错的两种长相。
  **写比分之前拿记分条对一格**，一秒钟的事
- **逐分不在 `df_pbp_1`，在 `df_mh_1`。** 我先试的 `df_pbp_1` 返回 `0`，据此
  差点写下「这场的逐分是真空」——**零命中先怀疑自己的查询词**，换个 feed 名
  整份逐分就出来了。同一个毛病这次栽在自己身上
- **比分板会滞后。** 赛点那一分实测：球在 ~172.7 秒结束、庆祝在 173.3 秒，
  **记分条 183.0 秒才翻成 7-5，差 10 秒**。拿烧死的记分条对视频时间轴时，
  时刻要以**画面上球落地那一刻**为准，比分只从记分条读。按翻牌那一帧排，
  「七比五」会压在人已经庆祝完的画面上
- **巡回赛没有击球方式，大满贯官方有。** 「正手斜线 / 网前截击 / 高压 / 切削 /
  放小球」这一层，ATP/WTA 的公开源全都没有，只能从画面一帧帧读；温网那份
  `WinnerShotType` / `ServeWidth` / `RallyCount` 直接给。⚠️ 实测的那一场只出现
  过 F（正手）和 B（反手），**截击高压切削小球有没有编码没有样本，别替它保证**
- **别把没验证的假设写进文档。** 我给 TNNS 的探测脚本写过一句「TNNS 是少数会把
  每一分怎么结束记下来的」——**那句话我从来没验证过**，是从「它是个专业比分
  app」推出来的，然后追着它跑了三轮。文档里的每一句都会被下一个人当判据用

### 封面别停太久

`COVER_SECONDS` 原来是 2.6 秒，账号所有者：「**封面 2.6 秒是不是有点多啊**」。
⚠️ **当前值是 1.2 秒，不是下面写的 1.8**——`COVER_SECONDS` 一路 2.6 → 1.8 → **1.2**，
而这一节停在了中间那一档（本文件另外两处早就写着 1.2，只有这一节没跟上：
「没写 `cover.narration` 的退回定长 `COVER_SECONDS=1.2`」、以及
`check_reel_landed` 那节的「一路从 2.6 改到 1.8 再到 1.2」）。**引用之前先
`grep COVER_SECONDS tools/build_match_reel.py`。**
封面还兼着信息流里的缩略图，点进来的人已经看过它了，再停两秒半是空转。
~~现在 1.8 秒~~ **现在 1.2 秒**——够读完两行钩子加一行比分，又不至于让人等。

⚠️ **而这个常量只在「没写 `cover.narration`」时才用得上**：封面停多久默认
**跟着封面那句配音走**（见「封面停多久，跟着封面那句配音走」那节），
`COVER_SECONDS` 是那条路的退路。所以改它影响的是没有封面配音的那些片子，
不是全部——**别拿它去对成片的封面长度**，那个数落在 `render.json` 里。

### 竖版短片的取景：铺满、钉死、跟着源片的帧率

用真实比赛画面剪的竖版片（`tools/build_match_reel.py`），三条都是量出来或看出来的：

- **帧率跟着源片走，别硬定 30。** 华盛顿那条官方集锦是 25 fps，而滤镜链里写死
  `fps=30`——5:6，每 5 帧复制一帧，一秒卡五次。这帧**查不出来**：裁切位置每帧都在变，
  两帧的像素并不相同（2099 帧逐帧比对，与前一帧完全相同的 **0 帧**），只有把源片和
  成片的帧率摆在一起才看得见。29.97 要保留 `30000/1001` 的分数式
- **默认不横摇。** 窗口只有源片 32% 宽，一摇，画面里本该静止的底线、球网、广告板、
  看台全跟着滑，而 25 fps 下滑动的静止物最容易看出一格一格。真人看下来的结论就是
  「固定中心的感觉更好」。横摇改成按段显式打开（`"track": true`），
  固定中心按整段运动质心的**中位数**自动定（不是钉在 0.5，庆祝那种人偏左的镜头
  会被挤到边上）
- **回合镜头也铺满，不走 contain。** 球飞到两边确实会出画，但竖版在手机上是整屏播的，
  上下留黑边等于把冲击力先折一半

顺带一条查证方法：**「转场瞬移」要先确认那处源片有没有剪。** 五处相邻段交界里只有一处
源片没剪（窗口凭空横移 399px），其余四处本来就是剪辑点，跳了也看不出来。
判断靠 `select='gt(scene,…)'`，**但解析它的输出要小心**——`pts_time:2.63` 按冒号切，
第 3 段是 `40448   pts_time`，不是时间值；我第一次就是这么把五个真剪辑点全判成了
「没剪」。判据见 `tests/test_match_reel.py`。

#### ⭐ 双打是例外：「回合镜头也铺满，不走 contain」只对单打成立

账号所有者 2026-08-18，看 `williams-sisters-cincinnati`（威廉姆斯姐妹双打）：
「因为是双打可能要画面更宽一些」「双打是例外哈」。

上面那条「回合镜头也铺满」是拿单打样本调出来的——`CROP_W=810`（源片 1920 宽的
42%）这个数本来就是按「两个人站得近」定的。**双打四个人分得比单打宽得多**，
同一个 42% 窗口经常把近端或远端某一位球员切在画面外。这条片子逐段抽帧核对
（每个候选段落取 2~4 帧、叠两条竖线比对 crop 42% 与 contain 62% 两个窗口）
才发现：

- **切没切完全取决于这一刻球员站在哪，不是这一段在讲什么。** 同一个连续镜头
  （81.6~101.32 秒）里，90.6~94.6 秒是近景不受影响、94.6~98.8 秒球员正好收拢在
  中央不受影响、82.0~90.5 秒球员却站得贴着边——机位没变，人的站位在变
- **近景／庆祝／握手这类两三人凑在一起的画面通常不受影响**（92.5s、106.5s、
  116.5s 逐帧核过，人都在 42% 窗口以内）；**只有回合进行中、四人站得开的宽景**
  才会撞上边缘
- **同一段镜头的开头和收尾可能不一样**：网前四人握手那段（119.4~125.6s），
  120.5 秒她们刚走近、远端两位还压在窗口边上，122.5/124.5 秒收拢到网前就没事了
  ——整段按需要走 contain，不按「大多数帧没事」抹平那半秒的风险

所以双打不能默认套用单打调出来的 `crop` 窗口：**每一段回合宽景都要单独抽帧核对
是否切到人**，需要就在那一段写 `"fit": "contain"`（`CONTAIN_KEEP=0.62`，62% 宽，
上下垫模糊放大版），别整条片子一刀切。判据是眼睛看，不是机械阈值——和这个仓库里
其他「故意没有测试」的编辑判断一样，缩略图墙叠上两条对比线就能看，几秒钟的事，
方法记在 `specs/reels/williams-sisters-cincinnati.json` 的 `_editing_why` 里。

#### 画幅：赛场之上是 3:4，不是 9:16

账号所有者定的，理由是「**尽可能多保持主体**」。两条，都能量：

- **小红书的视频静态展示就是 3:4**。9:16 的成片在信息流里被裁掉上下两条，
  海报的台头、比分、赛事行首当其冲——而那几行正是让人看懂这是哪一场的东西
- **从 1920×1080 里取窗口，9:16 只有 608px 宽，3:4 有 810px 宽**，多 33% 的球场。
  「球飞出画面」和「窗口中心偏一点就丢半个场」两件事同时缓解

代价是抖音/视频号播放时上下留黑边。**这是口径选择，问过了**。
解说片那条线不受影响，仍是 9:16 画布 + 3:4 卡——两条线的画幅分开，别互相牵动。

顺带一条：**字幕的上锚要跟着画布重算**，不能沿用解说片的 1524。那个数是
「1920 画布里、卡底 1680 往上 156px」；这里整幅画布就是那张卡，同样是
「卡底往上 156」，换算成 1440-156=**1284**。保的是同一个物理位置，量出来的
那组数一个没动。`PlayResY` 也要一起改——写错的话 libass 按比例缩整套坐标，
字幕整体跑位，**而且不报错**。

#### 固定中心：全片共用一个，别一段一个

「不摇」解决的是窗口在段内乱动；**它解决不了窗口整段站错地方**。账号所有者
一眼看出来：「原版横屏视频转成竖屏后，画面中央可能不是球场正中心视角」。
已发那条片子的回合镜头里，左边网柱和奔驰广告板贴着画面左沿，右半场直接出画。

毛病在**逐段取运动质心的中位数**：一段只有几秒、往往就一两个回合，谁那边球多
中心就偏谁。实测九段取出来 0.338 / 0.406 / 0.381 / 0.470 / 0.482 / 0.547 /
0.466 / 0.455 / 0.407——1920 宽里前后差 **400 像素**，而转播机位从头到尾没动过。

**池化到全片就对了**：一整场球的落点是对称于球场中轴的，样本一多，中位数自己
收敛到中轴上（这批数据是 0.455）。同一个机位，本来就该只有一个中心。
个别段要另定，spec 里给 `cx`——那是人看着缩略图墙定的，说了算。

#### 每一段都要待在同一个镜头里，否则中途会换人

郑钦文那条踩了三处，**渲染一次都没报错**：

| 这一段在讲 | 画面实际是 |
|---|---|
| 末屏「你定闹钟吗？」 | 对手握拳庆祝的近景（窗口 144.3–150.0 跨过 148.2 的切点） |
| 「手术之后的每一场球都是往回爬」 | 前两秒是对手走开的背影（跨过 115.76、117.6 两个切点） |
| 「亚洲的第一枚奥运网球单打金牌」 | 整句压在维基奇的中景上——全片最重的一句 |

**画面和旁白对不上不会让 ffmpeg 失败**，只会静静地发出去。而 `probe.json` 里
本来就记着 `scene_cuts` 和 `url`，按 url 认源片就能机检：

```bash
python3 tools/check_reel_cuts.py specs/reels/<slug>.json output/<date>/reel/*/probe.json
```

- **挑段仍然要靠眼睛看缩略图墙**，这一条只拦「窗口中途换了镜头而我没看见」。
  **`scene_cuts` 是下限，不是上限**：阈值 0.35 认不出「宽景回合 → 同一场地的
  中景」这种切换，郑钦文那条源片约 131 秒就漏了一个，于是「她一直带着疼在打」
  压在对手走开的背影上，而检查器全绿。机检过了仍然要抽帧看
- 真要跨（两边都是同一个人，只是机位换了）就在那一段写
  `"crosses_cut": "<为什么>"` 显式挂账
- **probe 给不全时不许假绿**：零命中和「全都合格」长得一模一样，所以没查成的
  源片要单独报。判据见 `test_每一段都要待在同一个镜头里`
- 还有一层机检管不了的：**同一个镜头里近端是谁**。宽景回合里两个人都在，
  读者读到的是「一场比赛」，可以；但替她说话的那句（「沦落这个词用错了」）
  压在对手身上就不行——那要看缩略图墙上的球衣颜色

##### 低门槛那一份也量出来，但它是线索不是判据

上面那句「`scene_cuts` 是下限」现在有了下文。`gea-shapovalov` 那条更直白：
**474.5 秒他还穿着白 polo 在场上，476.5 秒已经换上蓝外套在捧杯**——中间必然有
一刀，而 `scene_cuts` 只报了 473.07 和 482.67。同一块场地、同一个机位、同样的
灯光，帧间差就是过不了 0.35。那次是靠人看缩略图墙看出来的。

所以 probe 另出一份 `scene_cuts_loose`（门槛 0.12，排掉已经在 `scene_cuts` 里的）：

- **不并进 `scene_cuts`**。那是判据——存量 spec 的 `crosses_cut` 是照着它挂的
  账，混进来等于把一批已经想清楚的声明变成噪音。判据里钉了这一条
- **趁源片还在的时候量**。源片渲完就删，事后想查得自己再下一份几百 MB——
  `find_point_ends.py` 就是这么零调用方了一个月的
- 用法是：挑段的时候扫一眼，**落在你要取的窗口里**的那几个自己去缩略图墙上
  看一眼是不是真换了。别直接拿它当切点，0.12 会把镜头内的大幅运动也报进来

#### 排段长按「字数 ÷ 语速」估，那个语速不是一个数

两条线的合成语速差很远，而且**同一条线上还随句读密度变**：

| | 参数 | 实测 |
|---|---|---|
| 知识解说片 | `+28%` | ≈ 6.1 字/秒 |
| 赛场之上 / 开球之前 | `+6%` | 4.5~4.8 字/秒 |
| 同上，**短句 + 句读密** | `+6%` | **低到 3.6** |

拿解说片那个 6.1 去排剪辑片的段，一口气短三成——踩过。而在剪辑片这条线上按
4.4 排也仍然会翻车：「那一年她二十一岁。从那儿到今天，一共两年。」18 个字带
3 个句读，实测 4.94 秒，**3.64 字/秒**。每个句读大约值 0.25 秒，短句里它占的
比例最大。

所以估的时候要把句读单独计一项。**别再手算了**：`speech_seconds()` 就是这件事，
`--dry-run` 会一次把每段的余量列出来（0.2 秒，不联网、不下源片）。

##### 而上面那个 `0.23 / 0.25 / 0.3` 是拍出来的，错得有方向

拿它跟已发成片对了一遍，**长段估得太长、短段估得太短**：`gea-shapovalov`
第 5 段 91 字估 21.4 秒、实测 16.7 秒。照它排段，长段白留五秒空白，短段要等
六分钟的 render 才知道装不下——两头都在制造返工，而这条线的出发点本来就是
「返工比慢更贵」。

现在的系数是**从已发的成片里拟合出来的**。⚠️ **当前值是
`0.166·字 + 0.32·句读 + 0.45`（按 Azure 拟合，见下一节）**，不是这里原来写的
`0.16 / 0.42 / 0.75`——那一组是 edge-tts 时代的，留在这儿是因为下面三个
**取数的坑**跟系数是哪一组无关，换后端重拟合时照样要躲：

- **实测秒数要拿闸自己量的那个数**（mp3 时长），它落在 `subtitles.ass` 里：
  一段的最后一条字幕正好收在「本段起点 + 旁白时长」上。
  ⚠️ **不能用 `voice_NN.words.json` 的末事件**——那是**词末**，比 mp3 短一个
  固定的 **0.83 秒**（min 0.800 / 中位 0.829 / max 0.851，稳得不像话，就是
  edge-tts 结尾那段静音）。照它标定会把整条模型往短里拉
- **voice 文件要按内容认领，不按序号**。「序号不等于第 N 段」这条 CLAUDE.md
  早就记过；这一轮真剔掉了 9 段——事件文本拼起来和这段旁白对不上的，全是
  跨次重渲留下的旧文件。第一版按「个数相等」筛，`nishikori-shang` 那条照样
  混进来，还给出「14 个字要念 8.05 秒」这种荒唐值
- **贴着段尾的那些要剔掉**：字幕被收进本段窗口，量出来是截断值不是旁白长度

**它仍然是估，不是闸。** `--dry-run` 只判「估得再乐观也装不下」
（差额超过 `SPEECH_EST_ERR`），落在误差带子里的一律报「悬，跑一次
`mode=narration`」——那条路拿真 TTS 量，一分半，永远说了算。

判据 `test_离线估旁白长度要对得上真产物` 钉三头：误差都在带子里、**中位数贴近
0**（宽带子谁都过得了，得证明它准不是松）、**带子不许比实测最坏还宽出半秒**
（写成 10 秒那道闸就等于没装）。冻结的样本表里**必须带上两头的极端**——
第一版按字数均匀取样，最坏只到 0.93 秒，于是「带子不许虚高」那条自己红了。

###### ⭐⭐ 2026-08-15：**这组系数漂的不是模型，是 TTS 后端**——按 `narration_backend` 拆开才看得见

上一节说的「本地 `--check-narration` 和 runner 不是同一个 TTS」，在**离线估**
这头也有一份账，而且更贵：`test_离线估旁白长度要对得上真产物` 那条
`abs(中位数) <= 0.4` 这天报红（**−0.40，正好压线**）。

在这之前 `SPEECH_EST_ERR` 已经被一路调宽过四次（1.44 → 1.5 → 1.63 → 1.75 →
2.07），**每一次的加注都写着「中位数仍贴近 0，不是模型系统性偏差」**——那句话
当时都是真的，只是没人问一句「这批样本是哪个后端渲的」。按 `render.json` 的
`narration_backend` 把 718 段实测拆开，一眼可见：

| 这批产物 | n | 中位误差 | 最坏 |
|---|---|---|---|
| `narration_backend: azure` | **580** | **−0.48** | 1.93 |
| 没记 backend（edge-tts 那批老片） | 138 | **+0.04** | 1.42 |

**老系数对 edge-tts 一直是准的**（中位 +0.04），它只是从来没对 Azure 校过；
而 Azure 现在占 81%，池化中位数就这么被拖过了线。

⚠️ **控制变量做完了才敢动系数**：同一把 `YunjianNeural`、同样 `+6%`，
**edge-tts +0.13 / Azure −0.49**；而跨音色（Azure 的 Yunjian vs Yunxi）只差
0.06。所以分界线是后端，不是音色——**这一步不做，改出来的就是「按一个混着两台
机器的样本拟合的系数」，两边都不准**。

重拟合（只取 Azure 那 580 段）之后 **中位 −0.004、最坏 1.64**，
`SPEECH_EST_ERR` 因此**第一次往下走**（2.07 → 1.64）。

⚠️ **不做「两个后端各留一套系数」**，因为 **edge-tts 那套没有消费者**：
`speech_seconds` 只服务 `--dry-run`，而 dry-run 预判的是**这一段到 runner 上装
不装得下**，runner 走 Azure；本地 `--check-narration` 跑的是真 edge-tts，
根本不读这个估算。

⚠️ **偏长的方向就是「少报哑场」那一头**，而这一轮正为它白烧过一趟 render：
本地估「余量 3.74s、门槛 4.0s、过」，Azure 上是 **4.20s 当场撞哑场闸**
（`wangxiyu-timofeeva`）。系数估得比实际长 → 以为说得满 → 哑场报不出来。

⚠️ **换后端要连三样一起换，少一样就是假绿**：

| 换什么 | 不换会怎样 |
|---|---|
| 三个系数 | 中位数偏一整档 |
| `SPEECH_FITTED_BACKEND`（判据按它挑产物） | 拿另一个后端的成片来校，量到的是后端差不是模型差 |
| **冻结样本表** | 它才是 CI 上的判据（`output/` 不在稀疏检出里）。2026-08-15 那 17 条里 16 条是 edge-tts 渲的，在新系数下中位 **+0.60**、最坏 **2.37**，两条断言一起红——**而模型对今天的产物是准的** |

最后那一行是这次真正的坑：**改系数会让冻结表反过来红**，而那一红看起来完全像
「新系数不对」。表必须跟着系数走。

##### ⚠️ 改 spec 不重渲，spec 会悄悄坏掉——`wong-lehecka` 坏了三天没人知道

这道离线估装上的**第一分钟**就抓到一条：`wong-lehecka` 第 10 段画面 7.5 秒、
估旁白 10.25 秒。查下去是这么来的——

    7.30  成片发出去，那时旁白 22 字：「排名差九十六位，生涯排名最高胜。」
    之后  「收尾要落在一问上」这条规矩追认，把那一问补进旁白，涨到 48 字
    结果  **从那天起这条 spec 就渲不出来了**，而没人再渲它

**真闸（TTS 那道）要跑一趟 render 才出声**，所以它一直躺在仓库里假装是好的。
`voice_09.words.json` 里存的还是旧的那句——这也是上面「voice 文件要按内容认领」
那条的又一个用处：**产物和 spec 对不上，本身就是一条线索**。

- 改 spec 不重渲是常事（措辞、注解、规矩追认），所以要有一条**每次 CI 都跑**的
  检查：`test_每条spec的旁白都还估得下`，只吃 `specs/` 和 `tools/`，不联网
- 修的时候**两头一起动**：窗口 199.5 → 201.6（下一个切点在 201.64，没跨过去），
  旁白收到 37 字。只砍字会把内容砍没，只拉窗口拉不够
- 顺带一条口径：**7.5 秒的画面装不下 7.5 秒的话**。同期实测，9.5 秒的窗口配的是
  6.6 秒的旁白——窗口要给现场声和反应留出一截

##### ⚠️ 而这个通用常数偏保守三成——要排稿就拿这条片子自己的数回归

给霍达尔那条补旁白时，按上面那个常数估，**十九段里有九段「要超」**；实际上
`mode=narration` 用真语音跑下来**一段都没超**，最紧的还剩 0.77s。

差别在于常数是跨片子拍平的，而**同一条片子里音色、语速参数、句读密度是固定的**。
拿这条片子**已经量到的那些段**回归（`spoken ≈ a·字数 + b·句读数`，最小二乘），
jodar-fritz 十九段解出来是：

    0.190 s/字 + 0.379 s/句读     均方根误差 0.30s
    （通用常数 0.23~0.26 / 0.25，对云见 +6% 这把嗓子偏保守约 30%）

「每段说了多久」从**已发成片的字幕时间轴**就能量出来（首条起点到末条终点），
不用另跑语音。所以：**第一版排稿用常数，改稿用回归**——改稿的时候手里已经有
上一版的实测了，不用它是浪费。

留余量按均方根的三倍（≈0.9s），别按常数那个 0.3s——回归本身有误差。
**闸仍然是 render 里那道，回归只用来排稿**：写完还是要跑一次 `mode=narration`
（1 分 45 秒，不碰源片也不写产物）拿真语音验，别拿估算当结论。

#### ⚠️ 「有些没有字幕」可能根本不是字幕的问题——是那几秒没人在说话

账号所有者：「不是补全的问题，我是说有些没有字幕了，给漏掉了」。

我第一反应是去查字幕**丢没丢字**，还真查到两处（见「唯一一个」那条），
于是就收工了——**而那不是他说的事**。他跟着补了一句「利用完整区域，
**整个**里面去读一下」，照做才看见真的：

    jodar-fritz   68.7s 没有字幕 / 185.4s = 37%
    eala-pegula   22.6s 没有字幕 / 114.5s = 20%

**不是字幕丢了，是那几秒根本没在说话**，而且集中在**每段后半截**——我给短句
配了长窗口（第 5 段窗口 11.0s，旁白只说了 4.6s）。

- **查这个要按时间轴扫全片**，不是按段核对文本。我第一版是「把每段的字幕拼起来
  跟旁白比字数」，那个口径下 100% 覆盖、一点问题看不出来——**主语错了**：
  要问的是「屏幕上有没有字」，不是「这段话有没有印全」
- 判据一行：把所有 Dialogue 的 `[start, end]` 并起来，算补集
- **1~3 秒的空是正常换气，别填**。填到 18% 就停了，再填读起来是赶
- 反过来也要记：**「窗口挑得好」和「话说得满」是两件事**。选段是按画面挑的
  （死球之后再切、待在同一个镜头里），旁白是按事实写的，两边各自都合格，
  合起来仍然可能有三分之一的时间没人说话

#### 转播主机位的宽景，裁不出更紧的景别

第一版渲出来觉得人太小，加了 `crop_zoom: 1.5`（窗口收到 540×720 再放大 2 倍）。
两版摆在一起看：人是大了一半，**近端球员的腿和脚被切在画面外，字幕正好压在
她身上**。

底线后方高机位的宽景里，两个人几乎占满整幅高度——近端最低点约在 0.83，
远端最高点约 0.10。往里收一定切掉一个。把「窗口上移」也算过：要让近端的脚
不撞上 0.892 的字幕上锚（`(0.83-y₀)/h ≤ 0.86`）、又不切远端的头
（`(0.10-y₀)/h ≥ 0.03`），解出来 **缩放上限只有 1.14**——放大 14%，
不值一轮重渲。

所以这条源片不写 `crop_zoom`（默认 1.0）。`crop_zoom` 本身留着：**机位更低、
拍得更近的源片仍然用得上**，它错的不是实现，是被用在了错的机位上。这和
「竖版卡里的『全景』取决于拍摄视角，不是原图有多宽」是同一条。

##### ⚠️ 写过一版"找球场对称轴"的检测器，真实素材上不成立

思路是：球场左右对称于机位轴线，所以把白线抽出来（tophat）、按列成剖面 p、
取 `p ⊛ p` 的峰值除以二就是中轴。**合成画面上误差 0.001**，我据此写了测试、
写进文档、推了 PR。

真跑起来全垮：

- 真源片的九段**全部报「没找到球场」**，悄悄退回旧行为
- 修掉门槛的 bug 之后重新量，108 张真源片帧估出来的轴**从 0.0 散到 0.85，
  没有任何聚集**；把成片反解回去，同一个机位解出 0.29~0.51

原因事后很清楚：**机位一旦偏离中轴，球场在画面里本来就不再左右对称**（透视），
"找对称轴"找的根本不是球场中轴。合成画面之所以过，是因为那张图上除了球场
什么都没有——没有广告板、没有记分条、没有观众、没有影子。

三条教训，每条都是老毛病换了件衣服：

- **合成数据过了不等于成立**。它验的是"我的实现符合我的假设"，不是"假设成立"。
  假设本身错了，合成数据只会陪着一起错
- **门槛的数要在同一个口径下量**。我最早那批阈值是拿 9:16 的成片帧量的，而
  `cv2.resize(frame, (640, 360))` 写死了 16:9——**图被横向压扁三倍**，量出来的
  占比全是假的。16:9 的源片没事，所以本地怎么试都试不出来
- **门槛卡错时，日志里的「没找到球场」和「真的没有球场」长得一模一样**。
  源片只在 runner 上有，所以判据的数值必须跟着打进日志，否则又是"空结果
  当成真空"

#### 推送版式照着知识解说那条，海报铺满

账号所有者给了一条现成的推送当参照，外加一句「**海报要铺满全屏的**」。版式：

    白卡（顶上一条 #ff2442 红边）
      台头小药丸 → 大标题 → 海报（铺满卡宽）→「没显示？点此打开原图」
      →「👇 正文全文如下，长按整段即可复制」→ 正文 → 分隔线
      → ▶ 打开竖版成片 → 分别复制标题 / 正文 → 图片长按保存

**铺满要靠结构，不能靠负 margin**——微信里不可靠。白卡的 `padding` 拆开写：
文字块各自带 `padding:0 16px`，海报单独一行顶到卡边，也不要圆角。

配套一条：**海报必须进仓库**（`poster.jpg`，别用下划线开头）。它原来叫
`_cover.jpg`，被「丢掉不进仓库的中间物」那步删掉了，于是推送里一张图都没有，
只剩两个按钮——**又是「推送正文里的每个链接，指向的文件都必须在推送之前
进仓库」那条踩过两次的规矩**。海报不在就退回无图版并打印出来，别塞一个空链接。

### ATP 总站封，赛事域名镜像着同一批图

`atptour.com` 全站 403（带浏览器 UA 也一样），但**赛事自己的域名镜像了同一批 ATP 文章
和同样的 `/-/media/` 图片路径，免鉴权、原尺寸**。华盛顿站实测：
`mubadaladcopen.com/-/media/images/news/2026/07/27/23/25/nishikori-washington-2026-monday-r1.jpg`
1920×1080 直接拿到，署名 `© Emilee Chinn/Getty Images`。

**别只试 `/en/photos`**——那个是 404，很容易得出「没有图库」的结论；
真正有图的是 **`/en/media/news`**，每篇文章挂一张编辑图。新闻图路径里嵌着
上传时刻（`/YYYY/MM/DD/HH/mm/`），所以**猜不出来**，只能从文章页取。

顺带一条：盲猜文件名探路时，**soft-404 会返回 200**。华盛顿站那个目录下
`nishikori` / `shang` / `handshake` 等九个猜测全是 `200 + text/html`，而已知存在的
图返回 `image/jpeg`。**只看状态码会把「不存在」读成「存在」——要看 Content-Type。**

#### 场馆全景先试这一条：`tournament-images` 是**全站共享**的，不是各站各自的

    /-/media/images/atp-tournaments/tournament-images/<slug>_tournimage_<年>.jpg

这是赛事的主视觉，**十有八九就是中心球场全景**（新闻图那一档全是球员动作）。
关键在「共享」：路径不带站点前缀，所以**任一能出网的 ATP 赛事域名都能代取别站的**。
休斯顿自己不是 Sitecore 站，是拿 `dallasopen.com` 代取到的；罗马、哈雷、斯图加特、
马略卡同理。年份要一个个试（2018–2027），一站往往只有一两年有。

一轮探下来的产出：罗马 Campo Centrale、哈雷 OWL Arena、斯图加特 Weissenhof、
马略卡、休斯顿 River Oaks、德雷海滩 —— 六站，一条路径。

⚠️ **年份要跟场馆对得上**。里昂 2018 那张是 Parc de la Tête d'Or，而 2026 起搬去
LDLC Arena——同一个 slug，**不是同一个场馆**。搬过家的站，旧年份的主视觉是陷阱。

**WTA 那边的对应物是赛事页的主视觉**，取法：

    https://www.wtatennis.com/tournament/<id>/<城市 slug>/<年>
      → 页面里那张 photoresources.wtatennis.com/.../<id>-<名字>.jpg

`<id>` 从 `api.wtatennis.com/tennis/tournaments/?from=&to=` 里拿（⚠️ 年份只认
`from`/`to`，写 `year=2026` 会被**静默忽略**并返回 1971 年的数据）。这个接口
**没有图片字段**，图只能从赛事页取。URL 上**只传 `width`**——传 `height` 是裁图。
柏林（Steffi-Graf-Stadion 满场，横幅上写着场馆名）和梅里达都是这么拿到的。

⚠️ **slug 传空会落到通用页**，而通用页返回一整套赞助商 logo——四个赛事的图集
**一模一样**。这是「非空 ≠ 对题」的典型：数字很好看，内容全是别人的 logo。
判据就是拿两站的结果比一比，完全相同就说明落错了页。

⚠️ **扫图的正则一律带 `re.I`**。拉巴特那张主视觉是 `1005-Rabat.**JPG**`（大写），
我按 `\.(jpg|jpeg|png|webp)` 不带 `re.I` 匹配，于是扫出来是「这站没有主视觉」——
和真的没有长得一模一样。又一次「空结果先自证是真空」，只是这回真空是我自己造的。

顺带另一条同类的：**LTA 的 `/siteassets/events/<场馆>/` 是按场馆分的图库**
（女王、伊斯本、诺丁汉、伯明翰、伊尔克利）。女王那张 6000×4000 的
`hsbc-queens-centre-court.jpg` 挂在**天气预报页**上，首页和票务页都扫不到——
要顺着站内链接翻深一层。URL 上的 `?width=`／`?height=` 只是显示参数，
**去掉就是原图**（DOM 报的 1600×960 其实是 3075×2051）。
WordPress 同理：`-scaled` 后缀是缩过的，去掉才是原图（4000×2667 → 5457×3638）。

### 加新能力就要同时改三处：代码、工作流的依赖、开跑前的预检

竖版剪辑那条线上，**同一类错误连着犯了三次**，每次都是「本地能跑，runner 上缺依赖」：

| | 缺的 | 因为加了什么 |
|---|---|---|
| 1 | `playwright` | 封面改成 HTML 渲染（为了和知识帖共用字体） |
| 2 | Chromium | 同上；而且我那个查找函数只按猜的路径 glob，**它其实装好了** |
| 3 | `cv2` | 跟踪裁切要 OpenCV 算帧差质心 |

三次**都死得很晚**：下完 64 MB 源片、合完原声、算完分段之后才报
`ModuleNotFoundError`，每次白跑一分半。而在沙箱里这些依赖恰好都装着，
所以本地怎么试都试不出来。

所以：

- **改代码的同一个提交里改工作流的依赖**。别指望「下次记得」——三次都是这么漏的
- **把依赖检查提到最前面**。用到什么就在开跑前 import 一次，不行立刻报，
  并说清两条出路（装依赖 / 关掉这个功能退回旧行为）。让失败发生在第 5 秒，
  不是第 90 秒
- **本地装着不等于 CI 装着**。沙箱是长期攒出来的环境，runner 每次都是干净的

同一条线上还有个双胞胎：**`-filter_complex` 的输出必须打标签并显式 `-map`**。
不打标签时 `-map 0:v:0` 取的是原始流，把整个滤镜图绕过去——裁切、缩放、跟踪
全部静默失效，成片直接是 16:9。**它不报错**，只是不生效，跟「补位的静音盖住
真音轨」是同一种毛病：**兜底和默认值出事的时候不吭声**。

#### 「三处」里，每个工作流各算一处

2026-08-01 又栽了：字幕切行改成量真实字宽之后要 `fonts-noto-core`，我加进了
`interview-clip.yml`，**忘了 `ci.yml`**——本地全绿、CI 四条红。判据收在
`test_ci装的字体覆盖代码要的每一个`：**不查「装了哪几个包」，查
「代码点名要的每一个包都在」**，以后往 `_FONT_FILES` 里加字体它会替你记得。

同一天第三次：pip 行只装了三个第三方包，**没装项目自己**（`-e .`），
而工具 import `PIL` 和 `tennislive.render.webcards`（后者拖着
requests / rich / pypdf），跑到预检报 `No module named 'PIL'`。
判据换成**按 import 对账**（`test_工作流装的依赖覆盖工具import的每一个`）：
那张对照表必须盖住全部，新加一个第三方 import 就红。

### 开一条新线，先把阶段表画出来，再写第一行代码

账号所有者：「以后做一个新的项目，你就需要把前期、中期、后期的流程，分成不同的
任务／agent／子进程，都梳理清楚，而不是到做的时候才去想，然后缺什么。」

2026-08-01 那条新线的顺序正好是反的：我从「剪出来配字幕」开始，
**每失败一次往外长一截管线**——出片写完了才发现要转写校验，校验接好了才发现
要 cookie，成片进仓库了才发现推送这一段根本不存在，接上推送才发现**文案没有**。
七趟里六趟是在补一段本该在图纸上的东西。

**先画这张表。** 一行一个阶段，四列缺一不可：

| 阶段 | 做什么 | 跑在哪 | 要什么（依赖 / 输入 / 密钥） |
|---|---|---|---|

以「赛后开麦」为例，第一天就该长这样：

| 阶段 | 做什么 | 跑在哪 | 要什么 |
|---|---|---|---|
| 选材 | 找到那条集锦，定采访区间 | 本地 | 字幕接口、时长分布 |
| 转写 | 拉自动字幕 → 切行 | 本地 | yt-dlp、**字体**（要量行宽） |
| 校验 | 第二个源对一遍 | **runner** | 音频下载、whisper、**cookie** |
| 翻译 | 逐行中文 | 本地 | 译名表 |
| 出片 | 下源片 → 剪 → 烧字幕 → 封面 | **runner** | ffmpeg、**Chromium**、字体、cookie |
| 入库 | 提交成片 | runner | 写权限、**中间物清单** |
| 发布 | 复制页 → 提交 → 微信 | runner | PushPlus secret、Pages、**文案** |

画完它，三件事当场就掉出来了——**而我是分七趟撞出来的**：

- **依赖是按阶段列的，不是一张大清单。** 「出片要 Chromium」写在格子里，
  就不会出现「装了 playwright 忘了 Chromium」「装了三个包忘了项目自己」
- **哪几个阶段只能在 runner 上，一眼可见。** 那几个必须有本地替身
  （见下一节），否则每一个都要一趟五到十分钟的往返
- **「发布」是一个独立阶段，不是「出片」的尾巴。** 分开了才会想到
  `mode: push`（跳过重编码）和**文案这个输入**——文案是我在第八趟、
  接推送的时候才发现没有的

**落成代码就是 `--stage`。** `build_interview_clip.py` 现在有
`subs / sheet / verify / render`，工作流有 `mode: render|push`——这套划分是
对的，只是**我事后一段段补齐的**。先画表，这些 stage 就是照着表抄。

顺带：阶段表还回答了「这次失败该重跑哪一段」。今天多次整趟重来，
其实只有一个阶段变了。

### 失败在哪一段，就从哪一段接着跑

账号所有者：「有些非关键步骤失败了，不要从第一步开始再做，中间能否接上继续做，
你要有一定的判断。」

上一节那张阶段表，第二个用处就是这个：**它告诉你这次失败该重跑哪一段。**

2026-08-01 那趟推送在第 12 步「写复制页」失败（日期解不出来）。要重跑的
只有「发布」那一段，可整趟又走了一遍 checkout 90 秒、apt 50 秒、
pip 35 秒、下 Chromium 60 秒、预检——**三分钟的准备，全是给出片用的**：
封面已经在仓库里、不转写、不下载、不编码，`push_reel.py` 只要 `requests`。

所以「接着跑」要落到两处，缺一不可：

- **阶段本身能单独跑**（`mode: render|push`）——这个我做了
- **每个阶段的准备也要跟着挑**。出片专用的依赖挂 `mode != 'push'`，
  另给一条一行的轻装步骤。**只挑工作不挑准备，等于没挑**——三分钟里
  真正干活的只有二十秒

判据落在 `test_只推送时不做出片那一堆准备`：凡是装/用 playwright、
faster-whisper、yt-dlp、ffmpeg、字体的步骤，都必须挂 `mode != 'push'`。

顺带一条判断力：**「重跑整趟」是最贵的一个键，却常常是最先按的那个。**
按之前先问一句——这次变的是哪一段？前面那些的产物还在不在？
（成片在仓库里、字幕在仓库里、转写报告在仓库里——大多数时候都还在。）

### 失败之后要防住**那一类**，不是那一个

账号所有者：「每次失败后要总结经验，同时避免以后再出现同样的问题，
而不是每次都去尝试做同样的事情。」

2026-08-01 那七趟里，我每次都补了测试——**那只防住了同一个 bug 复发**，
没防住那一类。同一类连着来了六次，而它们共同的解法只有一句话：
**把「只有 runner 上才跑得到的那段」在本地跑一遍**。

所以每次失败要写两行，缺一不可：

| | |
|---|---|
| **这一个** | 判据 = 测试。反向验证过才算数 |
| **这一类** | 判据 = 让下次根本走不到这儿的那个东西 |

那天的对照表，右边一列才是真正省时间的：

| 失败 | 这一类是什么 | 防住它的东西 |
|---|---|---|
| `fonts-noto-core` 漏了 ci.yml | 依赖散在多个工作流里 | 按 `_FONT_FILES` / import 对账，不数包名 |
| Chromium 按旧目录名 glob | 环境路径写死 | 先问 playwright，再按通配找 |
| pip 行没装项目自己 | 同上 | 同上 |
| cookie 只注进 render 那步 | 同一件事在两处各配一遍 | 走环境变量，两步共用 |
| 源片落成 `.mkv` | 外部工具的输出路径靠猜 | 下完确认文件在，不在就报目录里有什么 |
| `--merge-output-format m4a` | **加保险时弄坏了本来通的路** | 让 yt-dlp 自己校验参数 |
| —— 以上**六次共同的类** —— | 关键路径只有 runner 上跑得到 | **`tools/interview_clip_selftest.py`** |

那个自检脚本十分钟就能写出来，**六个问题里五个它当场能抓到**。
写它的时机是第一趟之前，不是第四趟之后。

### 新接一条「只能在 runner 上跑」的线，先搭本地替身，再推第一次

2026-08-01 一条新线（`interview-clip`）连推六趟才出片，五趟死在环境上。
**这不是运气差，是算术**：`render` 的关键路径要三样沙箱没有的东西——
YouTube 访问（本机 IP 被挡）、干净的 runner（沙箱什么都装着）、secret。
于是唯一的验证方式是「推上去看」，而**第一个失败会把后面所有步骤挡住**，
每趟只暴露一个问题。N 个问题串在一条路上就是 N 趟，一趟五到十分钟。

真正的错是**第四趟才搭本地替身**。搭它一共十分钟，两件事：

```bash
# ① 干净 venv，照工作流那行原样装（不是在什么都装着的沙箱里试）
python3 -m venv /tmp/venv && /tmp/venv/bin/pip install -q -e . "yt-dlp[default]" playwright faster-whisper
PYTHONPATH=src /tmp/venv/bin/python - <<'PY'   # 把预检那段一字不改跑一遍
...
PY
# ② 合成源片，把出片路径整条走完（render 会复用已存在的 source.mp4）
ffmpeg -f lavfi -i "testsrc2=size=1280x720:rate=25" -f lavfi -i sine -t 450 … source.mp4
```

**五个问题里四个是本地可查的**，只有「yt-dlp 落成 .mkv」那个真的非 runner
不可（要看 YouTube 实际给什么编码）。一趟本可以换五趟。

顺带两条方法论：

- **合成源片要等 ffprobe 读得动才算生成完**。我把生成和渲染拆成两个后台任务，
  文件还没写完就开跑，得到 `moov atom not found`——测试脚本自己的问题，
  却长得像代码的问题
- **给账号所有者报进度时，说清「这趟会走到哪一关」**。每趟都比上一趟多过一关，
  但只说「又失败了」会让人以为在原地打转

### 查 PR 的 CI 别用 `get_status`：那张表在这个仓库恒空

`pull_request_read method=get_status` 查的是**老的 Commit Statuses API**，而这个
仓库的 CI 结果全在 **Check Runs** 里。于是它永远返回

    {"state": "pending", "total_count": 0}

——**和「CI 还没起来」长得一模一样**。PR #144 上量过：两个 run 10:19–10:20 就
`completed/success` 了，10:33 再查 `get_status` 仍然是 `pending / 0`。照它判会
一直等一个不会到来的绿灯，或者误报成还在跑。

判据要拿 run 本身：`actions_list method=list_workflow_runs resource_id=ci.yml`
按分支过滤，看 `status/conclusion`。⚠️ 它给每条 run 塞一整份 repository 元数据
（三条就 400 KB，直接超上下文），所以**落到文件再用脚本挑字段**，别直接读返回值。
⚠️ **`per_page` 对它没用**：2026-08-04 传 `per_page=2` 照样吐 **414,475 字符**。

又一次「空结果先自证是真空」，只是这次空的是查询用的那张表。

**轮询 CI 用 `get_check_run`，几百字节。** 三个接口的实测开销差了三个数量级：

| 接口 | 一次要多少 | 什么时候用 |
|---|---|---|
| `list_workflow_runs` | **400 KB+**（`per_page` 无效） | 只在**不知道 run id** 时用一次，落文件挑字段 |
| `list_workflow_jobs` | 几 KB | 要看**卡在哪一步**（带每步起止时刻） |
| **`get_check_run`**（要 `checkRunId`） | **几百字节** | **反复轮询「跑完没有」** |

`checkRunId` 从 `list_workflow_jobs` 的 `jobs[].id` 拿（那个 id 同时是
job id 和 check run id）。所以顺序是：列表拿一次 run id → jobs 拿一次
check run id → 之后一路 `get_check_run` 轮询。
⚠️ 参数名是 **`checkRunId`** 不是 `check_run_id`，写错只报一句
「owner, repo, and checkRunId are required」，看着像少传了 owner。

### 这台沙箱的两条硬限制，别再重新发现

- **`curl` 到 `api.github.com` 是 403**（`GitHub access is not enabled for
  this session`），只有 MCP 那条路通。用 curl 写的轮询脚本会**一直空转**，
  而脚本里那句 `try: json.load(...) except: sys.exit(0)` 把它吞掉——
  **「API 被挡」和「还在跑」打印出来一模一样**。又是兜底不吭声，只不过这次
  是自己的监控工具
- **artifact 下不下来**（同上），所以 runner 上生成的报告**必须同时 print
  进日志**，别只写文件。`verify_transcript` 的逐处对比就吃过这个亏：
  跑成功了、artifact 66 MB 躺在那儿，而我看不到它比出了什么，白等一趟

### yt-dlp 的 `-o` 是模板不是保证

`bv*+ba` 合流时，如果最佳的那对不是目标容器装得下的编码（VP9 / Opus 之于
mp4），yt-dlp 会**自己改后缀**（`.mkv`）并 warn 一句——**而 `--no-warnings`
把那句吞掉了**。下游 ffmpeg 拿 `source.mp4` 去开，报 ENOENT。

**日志里只有一个退出码 254，但它自己就是判据**：`254 = -2 & 0xff`＝ENOENT。
同类：`ENOSPC` 是 `-28 & 0xff = 228`。ffmpeg 直接把 AVERROR 当退出码返回，
所以看见 200 以上的怪数字先按 `256 - n` 反解。

两层保险，都要出声（收在 `yt_download()`）：`--merge-output-format` 让它一律
remux；真落到别的后缀时**认出来按实际的用**，一个都没有时**把目录里有什么、
多大列出来**——只说「文件不存在」，下一个人还得自己去翻。

### Chromium 的路径：先问 playwright，它答错了再自己找

**两边都不能单独信**，实测：

| | playwright 自报 | 磁盘上实际是 |
|---|---|---|
| 沙箱 | `chromium-1228/chrome-linux64/chrome`（**不存在**） | `chromium-1194/chrome-linux/chrome` |
| runner | 对的 | 新版是 **`chrome-linux64`**，不是 `chrome-linux` |

原来只按 `chromium*/chrome-linux/chrome` glob，于是 runner 上装好了却报
「找不到」——**日志上一行还写着 `downloaded to …`**。中间那段改成
`chromium-*/*/chrome`，新旧目录名都能中。

**顺带一条给检查工具本身的**：预检确实在第 4 秒就炸了，它该干的活干了；
炸的原因是**检查工具自己写错了**。这不是「预检没用」，是
**「加检查也要验它真的能通过」**。

### 查工作流的测试要看 `run:` 脚本，不能搜整份 yml 的文本

第一版写成 `assert "-e ." in ci_yml_text`，反向验证时把 `-e .` 从 pip 行
删掉**照样绿**——因为我在上面的注释里写了「`-e .` 不能漏」，`in` 命中了注释。
**注释不会被执行。** 这是「断言全绿不等于页面对」的静态版：
**查的东西和跑的东西不是一回事。**

现在统一走 `_run_scripts()`：YAML 解析出所有 `run:`，去掉 shell 注释行，
只在那上面搜。同理，`sparse-checkout: |` 那个块**是纯路径列表不是 YAML**，
里面写 `#` 会原样传给 `git sparse-checkout set`，报
`fatal: specify directories rather than patterns`，而且**在 checkout 那一步
就炸，一条测试都跑不到**。

### 「写了」不等于「跑过」——源码文本断言证明不了这一点

2026-07-31 同一天被咬两次，同一个形状：**一个凭空来的名字**。

    build_versus_poster 里  _still_to_clip(poster, dest, seconds)
                                                       ^^^^^^^ 没定义
    build_versus_poster 里  _cut_person(source, panel, key, …)
                                        ^^^^^^ 没定义

第一个是我自己的：封面时长加在 `build_cover` 上，而它把活**委托给**
`build_versus_poster`，那边没跟着改。下完 189 MB 源片、合完配音、渲完海报，
五分钟白跑才炸（run 30622667742）。**改签名要跟着委托链改到底**——两头都改了，
中间那一层最容易漏。

**第二个才是真正的收获**：`_cut_person(source, …)` 里的 `source` 在那个作用域
里**从来就不存在**（它拿的是 `sources` 字典加 `primary`）。也就是说 #105 引进的
「封面人物首选本场抽帧」**一次都没跑起来过**——任何一条 spec 第一次用
`versus.top.frame_at` 都会 NameError。它合并了、写进文档了、还带着测试。

**那条测试是这么写的**：

```python
assert "def _cut_person(" in reel, "没有从源片抠人的那条路"
assert "post_process_mask=True" in reel
```

——**它证明的是这段代码被写出来了，不是它跑得起来**。绿灯和「这个功能好用」
长得一模一样，而它整整一天都是坏的。这是「断言全绿不等于页面对」的静态版本：
查源码文本的断言只能防「有人把它删了」，防不住「它从来没工作过」。

判据落在 `test_没有名字是凭空来的`：`ruff --select F821` 扫 `src tools tests`。

- **`py_compile` 和 pytest 都看不见这类错**，只有真跑到那一行才炸。而那一行
  可能一年都跑不到一次——上面第二个就是
- ruff 一秒扫完，写进 dev extra（`pip install -e ".[dev]"` 就带上）。
  **缺 ruff 时这条测试要红，不许 skip**：一个常年跳过的检查和常年红是同一个毛病
- `cards.py` 的主题色是 `globals().update(_THEMES[...])` 灌进来的，静态看不见，
  72 条全是假阳性。**排除写在 `pyproject` 的 `per-file-ignores` 里，不藏在测试
  里**——让它是一个看得见的决定
- 反向验证过：把 `seconds` 改成 `secondz`，这条立刻红（改回来记得清
  `__pycache__`）

顺带一条给测试本身的：**新加一条代码路径时，测试至少要真调用它一次**（哪怕
喂假数据、只断言它抛的是预期的那个错）。`assert "def foo(" in source` 这种写法
留给「这条规矩别被删掉」，不要拿它当功能测试。

### 同一个常量赋两次，ruff 和测试都不吭声

改字幕版式时我在同一个模块里写了**两份** `_FONT_SIZE`（一份在量宽度那段，
一份在版式那段）。**行为碰巧是对的**——后定义的赢，而后定义的正好是新值。
但从那一刻起，改前面那份毫无反应。

- **`ruff --select F811` 拦不住**：它管重复 import 和函数重定义，
  **不管模块级变量重新赋值**
- **测试也拦不住**：跑出来的值是对的
- 它只在**下一个人改错地方**的时候发作，而那时线索早就凉了

判据只能自己扫（`test_字号只有一处出处`：`^<name> *=` 在模块里出现的次数
必须是 1）。同一族的还有「一个数写两处必分叉」——推送字段收到 `spec["push"]`、
栏目名收到 `column_of()`，都是这条。

### 字体名写「正规」的那个，等于写一个不存在的字体

给 ASS 换品牌字体（得意黑）时踩的，**反直觉而且完全不报错**：

| ASS 里的 `Fontname` | 渲出来的 md5 |
|---|---|
| `得意黑` | `c80c7f79e9a4` ← 认 |
| `Smiley Sans` | `d75e3012a8d0` |
| `NoSuchFontXYZ` | `d75e3012a8d0` ← **和上一行一模一样** |

PIL 的 `getname()` 报的是 `Smiley Sans`，看着才是「正规」的 family 名——
可 libass 认的是本地化的那个。**写英文名和写一个根本不存在的名字效果完全相同**：
静默回退到别的字体，画面照样出得来。

配套三条：

- **libass 读不了 woff2**。浏览器那条路（`webcards._font_css`）用 woff2，
  ASS 这条路要 ttf；仓库里两份都留着，ttf 从 woff2 解出来
- **`fontsdir` 是追加一个目录，不是替换**。指到 `assets/fonts` 之后，
  系统字体照旧走 fontconfig
- **缺字画的是 `.notdef`，不是空白**。判「这个字体有没有这个字」不能断言
  「墨迹为空」——实测 `.notdef` 有 32×35 的墨，那正是那个豆腐块。
  要拿一个**必定缺失的码位**（私用区 U+E000）当对照比
- **`.ttc` 是字体集合不是字体**（思源黑体一份文件里装着 SC/TC/JP/KR 好几张脸）。
  解析器不认集合头的话整份读出来是**空的**，而空集合和「名字对不上」
  在断言里长得一模一样

**同一行里换字体／颜色，每一段都要先 `\r`。** ASS 的覆盖是**粘连的**：顶栏
那道绿竖条设了 `\c`，下一段不复位，**后面整行标题跟着变绿**。渲出来一眼看见，
代码里一点异常都没有。复位写 `\r`（回到本 Style 的默认值），别逐项写回去——
逐项要列举颜色、字重、间距、字号…，以后加一项忘一项就又回到这儿。

#### ⚠️ 例外：这一行上有 `\t()` 动画时，`\r` 会把动画一起清掉

上面那条的**隐含前提是这一行没有动画**，2026-08-09 撞上了它的反例。顶栏加
「关键分脉冲」（`spec.topbar.pulse_at`，往 BODY 行挂 `\t()` 缩放）之后，
比分逐盘上色的 `{\r}` 把还在跑的 `\t()` 一并复位了。真渲量出来
（1080×1440 黑底，`萨巴伦卡 6-3 4-6 6-4 张帅`，脉冲 115%）：

| 那一版 | 5s 基线 | 10s 峰值 | 涨幅 |
|---|---|---|---|
| 不上色（没有任何复位标签） | 292px | 336px | **+15.1%** ← 满量程 |
| 上色 + `{\r}` 复位 | 292px | 311px | **+6.5%** ← 只有名字在动 |
| 上色 + 只复位颜色（现在） | 292px | 336px | **+15.1%** |

⚠️ **方向正好反了**：`\r` 那一版里还在脉冲的恰好是比分**前面那个球员名字**，
而比分一动不动——脉冲存在的全部理由就是让比分那一下被多看一眼。

⚠️ **而它一个字都不吭声**：ASS 文本里 `\fscx115` 明明白白写着，我那条
`assert "\fscx115" in body_line` 两版都成立。**只有真渲出来量宽度才看得见**，
又一次「断言全绿不等于页面对」。

所以：**这一行上有 `\t()` 的时候，只复位自己改过的那一项**（这里是颜色，
从 `TOPBAR_BODY_COLOUR` 取，样式行和复位标签**一个出处**）。上面那条规矩
担心的「逐项写回去、加一项忘一项」，靠
`test_顶栏复位不许用r否则脉冲被清掉` 顶住——它真渲两帧量宽度，谁换回 `\r`
或者往这一行加了第二种没跟着复位的属性，都会当场红（反向验证过：换回 `\r`
量到 1.075，判据报出病因）。

顺带：**量宽度要按每段自己的字号。** 比分那段放大到 `\fs38`，拿顶栏那档 32
去量少算两成，宽度闸就成了摆设——而溢出照样不报错。

## ⭐⭐ 2026-08-26 无人值守链整体盘点：四类静默坏，判据都落了

账号所有者要求「完整 review 当前库里的自动化无人值守任务」。9 条定时 + 4 条
push 触发的链路逐条核过**真实运行记录**（不是只读 YAML），修了四类：

**① 共享 JSON 的 state 推送，`git pull --rebase` 重试是死路。** run #360：
orchestrate 点完 run 提交 state，push 被拒 → rebase 撞
`data/orchestration_state.json` 自己的内容冲突（另一方是 match-reel 的失败自愈
release claim）→ **abort 之后什么都没变，重试五次全撞同一个冲突** → state 丢，
下一班重复 dispatch。修法是 JSON 级三方合并（`tools/merge_orchestration_state.py`，
以远端为底、只加 ours−base 的新增），在 FETCH_HEAD 上重建提交。
⚠️ **不做裸 union**：远端刚 release 的 claim 会被 union 复活，那条失败的
probe 永远不重试。判据 `test_merge_orchestration_state.py` +
`test_编排工作流并发锁pipefail和state推送重试`。

**② workflow_dispatch 的 inputs 超过 25 个，整份工作流文件失效。**
match-reel 就是这么连死 14 趟的：每个 run（含 push 触发的）0 秒 failure，
run 的 `name` 退化成裸文件路径——**「表单加一个输入」能把这条工作流的所有
触发一起打死，而且报不出原因**。#606 修的是 match-reel 一个文件；
`test_所有工作流的dispatch表单都不许超过github的25项硬限制` 把那一类防住
（自己推导，不维护名单）。

**③ push 重试当时在八个工作流里有四种写法，三种是坏的。** 没 `|| abort` 的
`pull --rebase`（bash -e 下一失败整步死，或半截 rebase 毒死后续重试）、干脆
没重试的裸 push（auto-push 链由 push 事件触发，**失败没有任何东西会重触发**，
撞车一次那条片子就静默不发）。收成 `tools/git_push_retry.sh`（照
ci_apt_install.sh 的先例），七条链换装；判据 `test_git_push_retry.py`。
⚠️ 它只管「各写各的文件」的提交；共享 JSON 走 ①，match-reel/interview-clip
的「重取远端重放修改」循环各有自己的判据，不许换。

**④ 预检失败没有记忆，同样的坏候选每天重试。** knowledge-adhoc 九天里六天
红在同一句「素材预检失败」——trivia 池的同两条候选天天排最前、天天被同两条
判据拒掉，每日知识栏目一半天数静默停更。修法：被拒 slug 记进 story_state 的
`__visual_backoff__`（3 天），候选排序**降到队尾不剔除**（池子空了还轮得到，
素材修好它自己回来）；⚠️ **失败路径要单独把 state 提交回仓库**——生成步骤
红了常规提交步整个跳过，记忆只活在 runner 上等于没记。判据
`test_visual_backoff.py`。

顺带核实过、不用再查的：oncourt-interviews / interview-auto-render /
reel-auto-ready / official-social-images / pipeline-health / source-health /
auto-push-* / pages 近期运行全部健康；interview-clip 的 47% 失败率**全是
发布门禁在正常拦截**（没有废片发出去，按「闸拦下来和真出错分开数」的口径
不是故障）；orchestrate 长时间不 dispatch 是候选侧真空（集锦没发/已有 spec），
不是 dispatcher 坏了。
⚠️ 顺带一个我当场犯的错：给 oncourt-interviews 补 workflow 级并发锁——被
`test_oncourt_only_serializes_collect_and_commits_cursor_and_claims` 当场打红。
**它是故意不锁整条 workflow 的**（draft 矩阵的长 ASR 会阻住下一轮扫描），
共享 JSON 的提交只在 collect job，那儿早有 job 级锁。判据救回了一次
「按直觉改别人想清楚过的设计」。

### ⭐⭐ 同一轮的下半场：模型内容线（DeepSeek 文案 / MiniMax 视觉）的执法缺口

账号所有者点名重点看「codex 教 deepseek 和 minimax 自动化做视频的内容」。
教材本身（`skills/tennis-reel-production/` / `skills/tennis-interview-production/`）
核过一遍，#593~#611 那批教训都编进去了（算术自洽、证据窗口、口语百分比、
ASR 译名别名、冷开场覆盖、影子门槛），**教材没大问题；问题在执法**：

**措辞判据原来只活在 pytest 里，而自动链直推 main 不触发 CI——模型产的
spec 从生成到发进微信一次都没被这批判据扫过。** 证据两条：
`tiafoe-musetti-cincinnati-2026-qf` 带着「7点05分」（开球报到分）被并发会话
自动推送、事后只能挂豁免表；装闸当天又在 `specs/reels/pending/` 抓到现行
（`musetti-zheng` 的「十六强」，已改成 1/8 决赛）。

修法照本文件自己的教条（「只读 spec 就能判的规矩，出处必须在 validate_spec
够得着的地方」）：八条措辞判据（几成几/单分写秒/开球报到分/强字轮次/爱局/
要到/盘点主语/文案提字幕规格）连同豁免表收进 **`tools/spec_wording.py`
单一出处**，三方共用——pytest 全库扫描照旧（含表自检），
`build_match_reel.enforce_spec_wording` 在 load_spec 之后、模式分发之前
一个 seat 拦 dry-run/check-narration/render 三条路，`promote_reel_draft` 在
模型草稿转正那一刻拦。171 条存量 spec 全绿（和 pytest 现状逐条对过账）。

⚠️ 顺手抓的第三个：**两个 promote 模板自己就是违规工厂**——
`promote_reel_draft` 的小红书模板写着「…与中英字幕」、`promote_interview_draft`
写着「…和中英文字幕」，正是 2026-08-19「文案里不要说中英文字幕相关」那条
点名要去掉的制作规格话术（interview 那张 78 文件的豁免表就是这么攒出来的）。
模板已改，reel 侧 `BILINGUAL_MENTION` 零豁免拦在 check_spec_wording，
interview 侧起初只落了一道 BILINGUAL 模板自检——**同一天补齐成全套**：
`check_interview_copy_wording`（同在 spec_wording.py，零豁免——只跑在新草稿
转正那一刻，老 spec 不再过 promote）拦 几成几/写秒/报到分/强字轮次/爱局/
要到/字幕规格 七条，面是「我们的话」（push/takeaway/cover/小红书正文）。
⚠️ **故意不扫 `zh` 字幕行**：那是受访者引语的译文，硬扫会逼人改动引语
（他真说了「七成」只能照实译）；译文写法偏好归翻译教材，不落硬闸。
盘点主语那条也没进来（要人名表，而采访文案不叙述逐分——真出现归终审）。
违规**跳过不炸**（草稿留在原地等终审），判据在 tests/test_spec_wording.py。

⚠️ **「还能走多远」这类教材禁句故意没落成机械闸**：19 条已发 spec 都在用，
它是教材对模型的偏好（影子评分管），不是账号所有者的规矩——
别把两种口径混在一张豁免表里。

### ⭐⭐ 同一天的结果复盘：模型线第一条自动成片，把**反向比分**推上了微信

盘点完当晚按「查产物不查信号」把链条的真实产出拉回来打开看（medvedev-damm，
2026-08-26 05:16 自动推送，链条端到端无人值守跑通的第一条）。机械面全对
（QC pass、SLA 148 秒达标、音画正常、封面是官方实拍）；**打开抽帧才看见**：
封面比分板和顶栏印着「达姆 5 3 / 梅德韦杰夫 7 6（赢盘绿标在梅德韦杰夫行）」
——**图形在宣称输的那个人赢了**，而同一帧里烧死的转播记分条写着反的，
旁白说的也是对的（「达姆7-5、6-3爆冷」）。

**根子是「版式顺序 vs 赢家顺序」第三次复发，这次带着一条会骗过复核的注解**：
matchup[0] 是明星输家梅德韦杰夫（#590「爆冷优先明星输家」），比分照着**他的
视角**抄成了「5-7 3-6」；spec 的 `_source` 还写着「赛事官方赛后稿独立核验比分
5-7、3-6」——**核验的是输家视角的数，注解却没说视角**，读起来像双源确认过。
更要命的是 `reel_facts.verified_result_problem` 那道现成的方向闸**只在
`_match.status == "result_verified"` 时才跑**——#588 手工打通的 spec `_match`
全空，整套校验静默跳过，dry-run/渲染/QC/推送一路绿。

修在三处（都反向验证过）：

| 修什么 | 在哪 |
|---|---|
| **不依赖 `_match` 的方向下界**：完赛盘（任一方 ≥6 局）里赢家零盘、输家两盘以上必错；退赛（result 带 Ret./退赛）不判 | `reel_facts.result_direction_problem`，装在 `_topbar_lines`（validate_spec 调它，dry-run 0.2 秒就红）。171 条存量扫过唯一命中就是 medvedev-damm 本尊，零误伤 |
| **正片时长下界 40 秒**：5-10 段的闸挡不住 5 段 × 3 秒——medvedev-damm 正片合计 **16.1 秒**（账号所有者 08-12：「集锦的长度可以不要太短」；已发语料最短约 69 秒） | `promote_reel_draft.waiting_reasons` |
| spec 数据改回赢家视角 + 注解补上视角说明 | `specs/reels/medvedev-damm.json`（**已发成片不重渲**，反的比分板留在微信里，这是代价；push.summary 冻结） |

⚠️ **同一轮抓到的前瞻盲区：四大满贯的频道不在白名单里。** `channel_ok` 是
「三大官方 + 频道名＝赛事名精确等值」，而美网频道叫 **US Open Tennis
Championships**、法网叫 **Roland-Garros**（feed 里赛事叫 US Open / French
Open）——等值必拒，**美网一开打整条 reel 自动链对它结构性失明**（大满贯集锦
不上 ATP/WTA/TennisTV 频道，版权归赛事自己）。四个频道名逐个 curl 过
og:title，显式加进 `OFFICIAL_CHANNELS`；**不放宽等值匹配本身**——子集匹配会让
`US Open Tennis Fan` 这类搬运号继承官方身份。

⚠️ **采访线对大满贯照旧是盲的，还没修**：`collect_oncourt_interviews` 只扫
tennistv.com 的媒体库（ATP 巡回赛），美网的场上采访在 @usopen 频道上——
要接得单独写一档扫描器，不是改一行白名单的事。美网期间采访线的产出会归零，
这是**已知缺口**，不是链条坏了。

⚠️ 顺带两件没动的：pending 里三条永远转不了正的僵尸草稿（musetti-zheng 13 条
blocker、paul-vallejo / tiafoe-tien 超 20 小时新鲜窗）留在原地只产生日志噪音；
orchestrate 自 medvedev-damm 后 17 小时零 dispatch 是候选侧真空
（温斯顿-塞勒姆/克利夫兰的其余场次过不了热度闸），不是 dispatcher 坏了。
（僵尸草稿 2026-08-26 晚已清掉，#616。）

### ⭐ 产出慢的账（2026-08-26 深夜量的）：瓶颈是返工轮数，不是渲染速度

账号所有者：「当前产出视频太慢了」。拿链条上三条真实片子拆完：单趟机器时间
接单→成片 **148 秒**（SLA 台账）、ffmpeg 本身 ~40 秒——**编码这头别再碰**；
每趟 run 固定开销 8~10 分钟（装环境 3 分 + 渲染 5 分）；而 medvedev-damm 用了
2 probe + 2 render、郑钦文美网 Q2 用了 **4 趟 render**（三次被质量闸打回）。
**慢 = 打回轮数 × 每轮 10 分钟 + 每次打回等一个会话来改。** medvedev 那 4 小时
里最大的一块（dispatch→probe 之间 2.5h 空转）是 ATP 备料重试链断了，#586 已修。

**打回轮数怎么降——静音那一类现在能在 dry-run 预判了**（#617）：

zheng-burel 的三连打回里两次是渲后数字静音闸（run 33000830101：渲染 5 分钟
成功，QC 第 1 秒判死「第 175 秒 1 秒数字静音」）。根因摆出来很干净：
**第 8 段窗口尾部压着源片的静音区**（片尾板前），旁白离线估 ~9.7s、窗口
11.57s，最后一秒多没人说话、音床就是源片自己——静音直接漏进成片。而随后的
「修复」提交 **segments 一字没动**，只是旁白改长了恰好把洞盖住。

修法照 `point_ends` 的老形状：**probe 趁源片还在时跑一遍 silencedetect**
（`silent_audio_spans`，门槛对齐 QC 的 -60dB），静音区间写进 probe.json
（None=没音轨 / []=量过没有 / [[a,b]]，三种不许长一样）；`--dry-run` 拿
旁白离线估去对（`silence_risk` / `silence_findings`）：

| 情形 | 处置 |
|---|---|
| 旁白按最长估（+SPEECH_EST_ERR）也盖不住 ≥2s | **对谁都硬**——确定性的渲后失败，跑完渲染只是多付 8 分钟学费 |
| 按点估盖不住（掷硬币偏输） | **自动产的 spec**（`_production.status=ready_for_render`）**硬**——那头是模型，改一句旁白比烧一趟渲染便宜；**手写 spec 只报**，守住「哑场离线估只提醒不拍板」 |
| 盖得住的 | 一个字不报（别刷屏） |

⚠️ 存量 171 条 spec 扫过**零硬红**（老 probe 没这个键 → 只软报一句「还没量过」）。
⚠️ zheng-burel 那 1 秒的静音位置是从 QC 逐秒响度表**反推的不是量的**——
函数注释里写明了；下一次 probe 就有实测区间可对。
⚠️ 剩下的返工大头（编辑/来源类打回要等会话来改）的解法是「QC 判据自动回喂
模型修一轮」——#599/#601 已给备料阶段做过同款，推广到渲后 QC 是下一步，
动它要避开教学线会话正在改的 promote/finalize 链。

#### ⭐⭐ 上面那句「下一步」2026-08-27 做掉了：render 红了判据自动回喂，修一轮再自渲

耗时审计定案之后账号所有者一句「那帮我处理吧」。链路本身每一跳早就是自触发的
（probe 末尾 dispatch render、finalize 末尾 dispatch render、render 末尾
dispatch push），**唯一没有自动闭环的就是「render 红了」这一站**：QC 打回之后
什么都不发生，等一个会话来注意到——zheng-burel 那条 QC 18:47 打回、19:25 才有
修复提交，**空转 38 分钟**，而三次打回里两次的修法完全是机械的。

现在 `tools/repair_reel_spec.py` ＋ match-reel.yml 的 `if: failure()` 修复步
把这一站闭上：dry-run / render / QC 三步的输出 tee 进
`/tmp/render-findings.log`（**pipefail 不能省**，不带它 tee 会把失败吞成绿），
render 红了就把判据原话回喂 DeepSeek 修一轮 → 机械复检 → commit → 重新
dispatch render。**有界**在五层，缺一层都是往内容里放开一个口子：

| 层 | 边界 |
|---|---|
| 谁 | 只动自动 spec（`_production.status == "ready_for_render"`），手写的留给会话 |
| 什么 | 只许两类编辑：段窗口 start/end；旁白**只删不改写**（逐字符子序列判定——模型不可能借「修一下」发明新事实）。quote 段一个字段都不许动（双语字幕钉在真实时刻上） |
| 判据 | 先过内容类过滤（`CONTENT_CLASS` 只认 `[不合格]`/必红/装不下这类**失败专属**标记——裸的「不合格」会把「render 全绿、死在后面 push 步」那种 run 的绿日志喂给模型，「共 0 项不合格」里就有这三个字） |
| 几轮 | 最多 **2 轮**（`_production.qc_repair_attempts` 写进 spec 跨 run 生效），修不好还给会话 |
| 落盘 | 修完先过**同一套生产闸**（dry-run 子进程 = validate_spec + 措辞 seat + probe 层含静音预判），闸红一个字节都不写 |

⚠️ **修复步的顺序钉死：先 commit 再 dispatch。** render 组是
cancel-in-progress——新 run 一启动就把本 run（正在失败的这趟）掐掉，commit
排在 dispatch 后面就会跟着一起死，修订静默丢失。

⚠️ 工具**永远 exit 0**（skip/waiting 是正常结果），工作流靠 `git diff` 判断
有没有真的修出一版；判据
`test_判据回喂只删不改写…` 等四条，删字判定 / 本地闸不落盘 / tee 接线三个方向
分别反向验证过。job 级 timeout 45 → 53（新步骤声明 8 分钟最坏预算，
`test_job级超时要兜得住…` 的账要跟着平）。

⚠️ **故意没动 promote/finalize 那一带**（教学线会话在改）：修复环只挂在
render 失败之后，输入是判据文本，输出是 spec 的窗口/删字修订——和 #599
（MiniMax 机械证据自修）、#601（DeepSeek 算术反馈重试）同形状，推广到渲后。

### ⭐⭐ 2026-08-27「编排器 25 小时没点过 run」：一半是真空，另一半是 **GitHub 在丢弃 schedule 事件**

账号所有者甩来告警截图「这个异常还在啊」。拆开是两层，处置完全不同：

**① 前 20 小时是候选侧真空**（温斯顿-塞勒姆/克利夫兰尾声过不了热度闸），
班次一直在跑、一直如实报「0 条该点」——这层不是故障。

**② 后 5 小时是班次本身没来。** 本地干跑一下就把矛盾暴露出来：**候选有**
（chwalinska-parks 85 分、van-darderi 43 分，都标着「点 run」），而 runner 侧
01:02 之后再没有任何班次来接。拿 run 列表量：cron 写的每 10 分钟，GitHub 实际
给的是——

    orchestrate      …16:56 → 18:56 → 21:36 → 01:02 → （2 小时无班）
    reel-auto-ready  …19:14 → 22:17 → 03:24        ← 中间空 5 小时

**schedule 事件在平台高峰期会被 GitHub 丢弃，最饿的窗口 17:00–03:00 UTC
恰好是美网比赛打完、集锦上线的时段**——这不是 cron 表达式能修的，也解释了
耗时表里「发现 ≤10 分钟」那一行为什么在实践里不成立。

**修法（#629）：挨饿的班次互相叫醒。** 各条 cron 的挨饿互相错开（上面两行
的时刻就是证据），而 workflow_dispatch **不吃** schedule 的丢弃——所以每一班
顺手看一眼别的时间敏感班次是不是 20 分钟没跑，是就代点一趟。逻辑收在
`tools/nudge_stale_ticks.sh`（照 ci_apt_install / git_push_retry 的先例），
六个频繁 cron 的工作流都当宿主，目标是 orchestrate + reel-auto-ready 两个
内容链班次。效果：时间敏感班次的实际频率 ＝ **全部 cron 班次的并集**。

边界（判据 tests/test_nudge_stale_ticks.py，行为拿 gh 桩真跑，方向反向验证过）：

- 目标在跑/在排队/20 分钟内跑过 → 不点；**读不到运行列表 → 不点并说明**
  （「读不到」≠「没跑」，处置相反——API 抖动不许放大成 dispatch 风暴）
- 点失败出声不重试不报错：nudge 是顺手帮一把，目标自己的 cron 仍是兜底；
  任何路径不许把宿主步骤带红
- ⚠️ **叫醒 orchestrate 必须带 `-f apply=true`**——它 workflow_dispatch 的
  apply 默认 false，裸点一趟只扫候选不派发，「看起来醒了」其实什么都没做
- ⚠️ pipeline-health 的 `actions: read` 要提成 write，少了它 nudge 每次都
  「没成」，而那和「没挨饿」在结果上长得一样
- ⚠️ oncourt-interviews 的 nudge 挂在 **collect** job（kick-render 不
  checkout，`source` 不到共享脚本）

⚠️ **当场的处置别忘了**：结构修复合并之前，手动 `workflow_dispatch
orchestrate.yml -f apply=true -f max=N` 一趟就能把积压的候选踢出去（这次
就是这么把 85 分的 chwalinska-parks 送进生产的）；告警读的是 state 的
`last_dispatch_at`，派发一落库它自己就清。

⚠️ 顺带记一条边界：run #383 里 van-darderi 在 runner 上是「集锦还没探到，
下次再探」而沙箱干跑标着「点 run」——**干跑的「点 run」不等于集锦已探到**，
真正的探测在 apply 那一刻做；「下次再探」正是靠班次频率兜底的那一类，
也就是这条修复照顾的对象。

### ⭐⭐ 2026-08-27 字幕分行/分屏/TTS 断句的机器味清理：五处根子一次修掉

账号所有者：「还有 tts 断句和字幕分行和分屏，目前还有很多小细节问题，你帮我
全部处理掉，保证以后都正常，没有机器或 AI 味」。拿两条已发的自动成片
（zheng-burel / medvedev-damm）逐条 ASS 对着看，机器味不是一个毛病，是五个：

| 已发成片里的样子 | 根子 | 修在哪 |
|---|---|---|
| 「郑钦文6比1｜7比5击败布雷尔｜闯进决胜轮」三连闪屏 | 顿号连的**并列项**被当成三个子句各占一屏 | `subtitle_lines` 步骤 3a：顿号连的相邻片段装得下就整组一行 |
| 「2018年」独占 0.92 秒一屏 | 句内合并只救「短到读不到」的（<5 字），5 字的半截话够不着 | 步骤 3b：句内合并门槛放宽到 `_SUB_MERGE_SHORT=6`；**跨句照旧只认 ≤2 的地板** |
| 「…当届温网冠军｜莱巴金娜」人名孤成一行停 1 秒 | 「军」在 `_SUB_AFTER` 里，边界分压过一切 | `_best_break`：量词边界（数目字＋盘/局/轮）＋孤行罚分 |
| 「7{\\fs68} 比 {\\fs78}5」松松垮垮 | `readable()` 把比分写成「7 比 5」 | 空格去掉：「7比5」。TTS 读法不变，只有画面变 |
| quote 中文行「回球越来越短。」句号上屏 | `explicit_quote_cues` 原样透传，全站「不写标点」的规矩漏了这条路 | `_quote_display`：**中文行**照全站规矩去标点（？！留着），**英文行一个字符不动**（那是他真说的话，标点是语法的一部分） |

模型旁白那一头同时堵住源头（`draft_spec`）：SYSTEM prompt 补【配音断句】
（子句 ≤16 字＝一行字幕宽度、比分写汉字「六比一」、顿号只连并列项、长定语
别前置）；`normalize_editorial_for_speech` 执法——「7-5」一律规范成「七比五」
（两侧 ≤2 位数，四位年份区间不受影响）。dry-run 新增 `[断句]` 软报告
（`overwide_clauses`）：子句宽过 16 格只能从词语边界硬切，报出来让人加个逗号，
**只报不拦**（硬切是兜底不是错误，存量合格的长句不许被一条常年红挡住）。

四个实现坑，都是这一轮踩出来的：

- ⚠️ **量词 盘/局/轮 不能直接进 `_SUB_AFTER`**：无条件加分会把「三个盘点」从
  「盘」后劈开——比它要修的孤行人名更糟。数目字这一个前提（`text[i-2]` 是
  数）把「盘点」「局点」「轮到」全挡在外面
- ⚠️ **孤行罚分只罚 95＝不满一个边界档**：第一版罚 250，把唯一的词语边界罚到
  比裸的硬切还低，切出「…温网冠｜军莱巴金娜」。正确的语义是「有别的边界就换
  别的边界；只剩它时孤行仍好过把词劈开」
- ⚠️ **3a 的判据要用两边都是 7 字的用例**（「上半区斯瓦泰克、下半区萨巴伦卡」）：
  拿「6比1、7比5」验它，3b 的 ≤6 门槛把它盖住了——反向验证时拆掉 3a 照样绿，
  差点当成「这一支是多余的」删掉
- ⚠️ 全库 172 条 spec、5619 行字幕扫过：**0 行超宽、6 行 <3 字**——那 6 行全是
  宽度装不下的既有情形（「抢七。」整段就两个字），不是这次的回归

已发的 zheng-burel / medvedev-damm **不重渲**（消息发出去收不回来），这套只管
以后。判据：`test_顿号连的并列项和半截短句都并进一行`、`test_断行不许把人名
孤成一行`、`test_超宽子句报出来让人在标点处断开`、`test_原声字幕的中文行不带
标点上屏英文行原样`、`test_deepseek草稿把连字符比分规范成汉字几比几`——
七个方向分别反向验证过，各红在自己的断言行。

### ⚠️⚠️ 2026-08-27 第一条真 pending 草稿把 reel-auto-ready 炸了：import 一个工具不许要求教材在盘上

上面「字幕机器味清理」合并后顺手核 chwalinska-parks 那条链（probe ✓ →
草稿 ✓ → 卡在 promote 等证据），发现 reel-auto-ready 06:46 的班次
**33 秒 failure**——而它前面 12 趟全绿。日志一行就够：

    FileNotFoundError: missing production skill resource: skills/tennis-reel-production/SKILL.md

链条是 `refresh_reel_cover` → `assemble_spec` → `draft_spec`，而 `draft_spec`
在 **import 那一刻**就拼 SYSTEM prompt、读 SKILL.md；reel-auto-ready 的稀疏
检出没有 `skills`。⚠️ **前 12 趟全绿不是它对，是 pending 一直是空的**——
`for DRAFT in …` 循环没跑过，import 链一次都没走到；#616 清完僵尸草稿之后
第一条真草稿一来就炸。「查了 12 趟全绿」和「这条路通」不是一回事，
走不到的路怎么查都是绿的。

两半修法（缺一半都不算修）：

- **`draft_spec` 的 SYSTEM/_PUSH prompt 拖到第一次要用才拼**（`system_prompt()`
  / `push_system_prompt()`，lru_cache）：import 一个工具不许要求磁盘上有教材；
  真要起草时教材缺席，仍然当场响。判据 `test_import草稿工具不许要求教材在盘上`
- **稀疏检出补 `skills`**（reel-auto-ready + orchestrate）：`analyze_reel_visuals`
  在**调用时**真的要读 skill 给 MiniMax 拼 prompt，lazy 救不了那一步。
  判据 `test_跑教材工具的工作流都要检出skills目录` **从 import 图自己推**
  （直接或间接沾 `reel_skill` 的工具都算，宁可保守——几个 markdown 的检出
  成本是零），不维护名单；装上当场把 orchestrate.yml 也抓了出来
  （它在函数里 `from assemble_spec import assemble`，真装配那一刻要教材）

⚠️ 顺带两条同一趟日志里的好消息：#629 的 nudge 在生产里真的干活了
（「orchestrate.yml 已 108 分钟没班（schedule 被 GitHub 丢弃了），已代点一趟」），
而且它挂在失败步之后照样跑到（`if: always()` 那一支的价值）；schedule 丢弃
在 06:46（窗口外）仍然发生——「17:00–03:00 最饿」是实测的重灾区，不是边界。
