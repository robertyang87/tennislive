__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
dist/
build/
.pytest_cache/
.cache/
tmp/
probe-out/
*.log
.flash-publish.json
.content-publish.json
.DS_Store
# 本地自备图片（仅本地渲染用，绝不提交/上传）
assets/local/*
!assets/local/README.md

# v3 构建临时目录
output/grand-slam-vertical-v2/_work/

# 页面预览截图：看完即弃，别混进 output/
*.preview.png
*.preview.tail.png

# 找图时下载的候选，看完即弃
tools/broll/

# 海报模板的裁切中间物，全都落在原图旁边、渲完即弃，别进仓库：
#   `_precrop`        → .crop.jpg
#   `_cut_crop`       → .crop.png（抠图要保 alpha，所以走 PNG）
#   `_fade_cut_sides` → .faded.png（侧边淡出，接在 .crop.png 后面）
#
# ⚠️ **第三个是漏掉之后补的。** 上面这段注释原来只列了前两个，而
# `_fade_cut_sides` 存的是 `X.crop.png` → `X.crop.faded.png`——`*.crop.png`
# 匹配不上它，于是两张 127 KB / 94 KB 的中间物躺在 assets/players/ 里等着被提交。
# **同一条链上逐个列名字，就是挡不住下一个**——和 outdir 里 `source*` 那个坑
# 连栽四次是同一个形状。判据落在 test_海报的裁切中间物一个都不许进仓库。
*.crop.jpg
*.crop.png
*.faded.png
