# 张帅 2-6 6-2 6-2 戴伊（辛辛那提 2026 女单**第一轮**）——选题调研

2026-08-15 查的。**这一条还没做**，下次接着做的人从这儿起步，不用重查。
按 CLAUDE.md「选谁」那条它是**优先级 ①**（中国球员单打），去重也过了：
`specs/reels/` 里有 `zhang-putintseva` / `zhang-ostapenko` / `zhang-sabalenka`，
**没有 `zhang-day`**。

## ⚠️ 卡在源片上：只有 720p 的站内版，YouTube 上没有

|  | 情况 |
|---|---|
| **WTA 官网视频页** | `wtatennis.com/videos/4560170/zhang-comes-from-a-set-down-to-defeat-day-in-cincinnati-second-round` |
| **Brightcove** | account `6041795521001` · player `te01Hqw71_default` · **mediaId `6403473576112`** |
| 实测 | `dur=198.613` · **最高 1280×720**（`http-2131k-720p`），`--list-formats` 逐档看过，没有 1080 |
| **YouTube** | ❌ `@wta` 和 `@CincyProTennis` 两个频道**各查了四次**（18:52 / 19:00 / 19:22 / 19:41 UTC），一次都没有 |

`mediaId` 是从页面 HTML 的 `data-video-info` 那块 CMS JSON 里挖出来的
（URL 里那个 `4560170` 是 WTA 自己的内容 id，**拿它喂 Brightcove 会 404**）。

**下次先重查一遍 YouTube**：那两个频道明显在补 Round 1，而站内版 18:40 UTC
就发了——1080p 版晚几个小时冒出来完全可能。按 CLAUDE.md「WTA 也要优先用
YouTube 1080p」，能等到就等；等不到再用 720p 站内版，
**并在 spec 的 `_source` 里写清楚查过几次、什么时候查的**（这是「查过、当时还没有」
不是「这场没有」，和 MCP 那条同一个形状）。

⚠️ 720p 取 3:4 竖版窗口要放大 **2.0 倍**（1080p 只要 1.33 倍），画质差一档，
认领的时候要说。

## 轮次：**第一轮**，别信 WTA 自己的 URL

WTA 那条视频的 **URL slug 写着 `second-round`，标题写着 `first round`**，两者打架。
**WTA 官方接口说了算**：`…/tournaments/1017/2026/matches?states=C` 里
`LS082 | Round 1 | S. Zhang d K. Day 2-6,6-2,6-2`。Tennis.com 也标 Round 1。

## 比赛本身

    flashscore_id  4CYI9Ick        WTA MatchID  LS082
    home = 张帅（SH / stats 里的第一栏）   away = 戴伊（SI）

⚠️ **`stats.a` 要对应 `cover.matchup[0]`，不是 feed 的 home/away**——
`sonmez-anisimova` 刚在这上面把数据图渲反过（CLAUDE.md 有专门一节）。
这一条如果 matchup 写成「张帅、戴伊」，那 a 恰好就是 home，不用调换。

### 逐局（`df_mh_1`，`HH÷n` ＝第 n 方被破）

    第 1 盘  0-1 戴伊发  1-1 张帅发  1-2 戴伊发  1-3 张帅发 ← 张帅被破
             1-4 戴伊发  2-4 张帅发  2-5 戴伊发  2-6 张帅发 ← 张帅被破
    第 2 盘  0-1 戴伊发  1-1 张帅发  2-1 戴伊发 ← 破  3-1 张帅发
             4-1 戴伊发 ← 破  5-1 张帅发  5-2 戴伊发  6-2 张帅发
    第 3 盘  1-0 戴伊发 ← 破  2-0 张帅发  2-1 戴伊发  3-1 张帅发
             3-2 戴伊发  4-2 张帅发  5-2 戴伊发 ← 破  6-2 张帅发【赛点】

**首盘最后一局被破之后，她剩下的八个发球局一个没丢**，并且赢了最后十六局里的十二局。

### 技术统计（flashscore `df_st_1` 和 WTA 官方 `/stats` **逐项一致**）

| | 张帅 | 戴伊 |
|---|---|---|
| ACE | **0** | **8** |
| 双误 | 2 | 5 |
| 一发成功率 | 74% | 69% |
| 一发得分率 | 70%（38/54） | 63%（36/57） |
| 破发点转化 | 4/12 | 2/3 |
| 总得分 | **83** | 74 |
| 发球局保住 | 10/12 | 8/12 |

**分盘一发得分率（两条曲线正好交叉）**

    张帅  56% → 71% → 82%
    戴伊  78% → 71% → 45%

⚠️ **总分用 83:74**。`match_stat_hooks` 报的 82:74 读的是 `df_mh_1`，
而**逐分不列制胜的那一分**，所以它天生少一分；`df_st_1` 和 WTA 官方都是 83:74。

⚠️ **用时用 1:49**（WTA 官方 `settime` 35:18＋35:43＋38:16＝1:49:17，和
`MatchTimeTotal 01:49:18` 差 1 秒）。**flashscore 报 1:59，偏了十分钟**——
同一天它在 `sonmez-anisimova` 上也偏长，CLAUDE.md 已记。

## 人

| | |
|---|---|
| 张帅 | WTA id `311779`，1989-01-21 生，本场时 **37 岁**；当期（2026-08-10）**世界第 54**，1060 分 |
| 戴伊 | WTA id `322534`，1999-09-28 生，26 岁，美国；当期**世界第 121**，630 分；**本站资格赛打进来的** |

**戴伊的资格赛之路**（WTA 接口 `RS035` / `RS017`）：先 6-3 6-2 赢 17 号种子普雷斯顿，
再 2-6 7-5 6-3 赢**袁悦**——也就是说她正赛第一场碰上的张帅，是她连着遇到的第二个中国球员。

## ⚠️ 三条不许重讲的（同栏目）

- `zhang-putintseva`：24 连败、排名 595→57、九年没赢过普汀塞娃
- `zhang-ostapenko`：「六个中国球员走了五个」、奥斯塔彭科退赛
- `zhang-sabalenka`：多伦多那场本身

**这条完全用得上的新料**：一个 ACE 都没发却赢了发 8 个 ACE 的人、
首盘丢掉发球局之后连保八局、两条一发曲线交叉、37 岁、对手是刚淘汰袁悦的资格赛球员。
以上没有一条和那三条重合。

⚠️ 一发得分率摆动那一条 `sonmez-anisimova` 刚用过（隔一条片子），
**同一个数据形状连着用两条会显得机械**，优先考虑「0 个 ACE」和「连保八局」。
