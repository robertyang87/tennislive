# 比分板回贴：2026-09-09 阿尔卡拉斯—谢尔顿复盘

旧版成片 SHA256 c284ec0e9780d4aab17c860a7e9f4791e6107d1795515e73eb805f4e765a7024。

## 已复现的问题
- 源片2.17–22.77秒：大多数右缘读数在580附近，但763出现两次，整段被裁到736。多取约153源像素。
- 源片65–104.1秒：真实右缘约654，1333出现两次，整段再次裁到736。
- 源片104.1–122.17秒：横跨多种比分板宽度，整段固定宽度不成立。
- 125.49秒之后比分板仍显示到约131.7秒，不能将“赛点结束”当成“图形撤下”。
- 黄色抢七／赛点提示条也属于图形，必须完整处理，不能留下半截。

## 后续执行规则
由 tools/scoreboard_geometry.py 实现美网深蓝比分板配置：
1. 同时验证上、下边框、深蓝或白色内部，以及两行姓名文字特征。
2. 每个输出帧生成透明蒙版，框外保持透明；不再整块复制最大矩形。
3. 用连续时间内的多数读数确认宽度；两次离群最大值不构成边界证据。
4. 已确认不存在图形的帧不做时间桥接。几何无法确定时阻断，不退回最大宽度。
5. 不按赛果或镜头叙述推断图形是否存在。人工允许窗口只能收窄源画面检测。
6. 发布质检必须验证 scoreboard_qc.json、当前spec和实际使用蒙版的hash及片段覆盖。
7. 比分板样式不匹配时应增加已验证的新配置，不能降低当前配置的阈值。

验证：五项回归测试、完整源片相关六段扫描、真实cut_segment滤镜出片和画面检查。
